# Experiment logs

One log per bring-up experiment. The logs are ordered chronologically, and the
numbering is offset from the stage numbering: **experiment NN corresponds to stage
NN-3**. Experiment 01 and 02 predate the `stageN/` directory convention, so they
have no stage.

Path commands inside these logs are a record of what was actually run at the time.
For the six retained snapshots the paths have been updated to the current layout
(`stages/stage86/...`); the logs belonging to the archived stages (00-84) keep
their original commands, since those stages are no longer in the working tree -
see `../../stages/README.md` for the retention policy and how to restore an
archived stage with `tools/stage-archive.sh`.

| Stage | Experiment | Title |
| --- | --- | --- |
| — | [experiment-01](experiment-01-cmdline.md) | Custom Kernel Cmdline via Non-Persistent Boot |
| — | [experiment-02](experiment-02-usb-log-loop.md) | USB-Only Persistent Kernel Log Loop |
| stage0 | [experiment-03](experiment-03-stage0-bare-metal.md) | Stage0 Bare-Metal ARMv7 Payload |
| stage1 | [experiment-04](experiment-04-stage1-boot-wrapper.md) | Stage1 Boot Wrapper / XNU Boot Args Stub |
| stage2 | [experiment-05](experiment-05-stage2-c-runtime.md) | Stage2 C Runtime + Fuller Apple Device Tree |
| stage3 | [experiment-06](experiment-06-stage3-hardware-probes.md) | Stage3 Read-Only Hardware Probes |
| stage4 | [experiment-07](experiment-07-stage4-timebase-vectors.md) | Stage4 Timebase + Exception Vectors |
| stage5 | [experiment-08](experiment-08-stage5-xnu-skeleton.md) | Stage5 XNU-Adjacent Kernel Skeleton |
| stage6 | [experiment-09](experiment-09-stage6-pe-state-abort.md) | Stage6 PE_state Skeleton + Data-Abort Recovery |
| stage7 | [experiment-10](experiment-10-stage7-gic-skeleton.md) | Stage7 GIC Driver Skeleton (Read-Only) |
| stage8 | [experiment-11](experiment-11-stage8-sgi-irq.md) | Stage8 Controlled SGI/IRQ Delivery |
| stage9 | [experiment-12](experiment-12-stage9-timer-irq.md) | Stage9 ARM Generic Timer IRQ |
| stage10 | [experiment-13](experiment-13-stage10-mmu-identity.md) | Stage10 ARMv7 MMU Identity Mapping |
| stage11 | [experiment-14](experiment-14-stage11-high-alias.md) | Stage11 High Virtual Alias Mapping |
| stage12 | [experiment-15](experiment-15-stage12-high-call.md) | Stage12 High-Virtual Function Call |
| stage13 | [experiment-16](experiment-16-stage13-high-bootstrap.md) | Stage13 High-Virtual Kernel Bootstrap Handoff |
| stage14 | [experiment-17](experiment-17-stage14-high-pe-state.md) | Stage14 High-Virtual PE/XNU State Bootstrap |
| stage15 | [experiment-18](experiment-18-stage15-high-validation.md) | Stage15 High-Virtual Bootstrap Validation Status |
| stage16 | [experiment-19](experiment-19-stage16-high-init-sequence.md) | Stage16 High-Virtual Init Sequence |
| stage17 | [experiment-20](experiment-20-stage17-high-root.md) | Stage17 High-Virtual Root Kernel Path |
| stage18 | [experiment-21](experiment-21-stage18-high-dt-summary.md) | Stage18 High-Virtual Boot Args and Device-Tree Summary |
| stage19 | [experiment-22](experiment-22-stage19-platform-result.md) | Stage19 Versioned High-Root Platform Result |
| stage20 | [experiment-23](experiment-23-stage20-phase-table.md) | Stage20 High-Root Phase Table |
| stage21 | [experiment-24](experiment-24-stage21-service-table.md) | Stage21 High-Root Service Table |
| stage22 | [experiment-25](experiment-25-stage22-phase-service-deps.md) | Stage22 Phase-Service Dependencies |
| stage23 | [experiment-26](experiment-26-stage23-phase-dispatcher.md) | Stage23 Descriptor-Driven Phase Dispatcher |
| stage24 | [experiment-27](experiment-27-stage24-service-dispatcher.md) | Stage24 Descriptor-Driven Service Dispatcher |
| stage25 | [experiment-28](experiment-28-stage25-bootstrap-registry.md) | Stage25 High-Root Bootstrap Registry |
| stage26 | [experiment-29](experiment-29-stage26-boot-policy.md) | Stage26 Registry-Gated Boot Policy |
| stage27 | [experiment-30](experiment-30-stage27-bootstrap-manifest.md) | Stage27 Descriptor-Driven Bootstrap Manifest |
| stage28 | [experiment-31](experiment-31-stage28-launch-contract.md) | Stage28 Manifest-Driven Launch Contract |
| stage29 | [experiment-32](experiment-32-stage29-startup-boundary.md) | Stage29 Contract-Consuming Startup Boundary |
| stage30 | [experiment-33](experiment-33-stage30-startup-routine.md) | Stage30 Startup Routine Handoff |
| stage31 | [experiment-34](experiment-34-stage31-startup-entry.md) | Stage31 High-Virtual Startup Entry |
| stage32 | [experiment-35](experiment-35-stage32-kernel-callouts.md) | Stage32 Startup-Entry Kernel Callouts |
| stage33 | [experiment-36](experiment-36-stage33-kernel-context.md) | Stage33 Kernel-Start Context |
| stage34 | [experiment-37](experiment-37-stage34-vm-plan.md) | Stage34 VM Bootstrap Plan |
| stage35 | [experiment-38](experiment-38-stage35-vm-state.md) | Stage35 VM Bootstrap State |
| stage36 | [experiment-39](experiment-39-stage36-bootstrap-allocator.md) | Stage36 Bootstrap Allocator Descriptor |
| stage37 | [experiment-40](experiment-40-stage37-pmap-workspace.md) | Stage37 Pmap Bootstrap Workspace |
| stage38 | [experiment-41](experiment-41-stage38-kernel-object-table.md) | Stage38 Kernel Object Table |
| stage39 | [experiment-42](experiment-42-stage39-kernel-collection-handoff.md) | Stage39 Kernel Collection Handoff |
| stage40 | [experiment-43](experiment-43-stage40-kernel-collection-entry-table.md) | Stage40 Kernel Collection Entry Table |
| stage41 | [experiment-44](experiment-44-stage41-kernel-collection-object-graph.md) | Stage41 Kernel Collection Object Graph |
| stage42 | [experiment-45](experiment-45-stage42-kernel-collection-dependency-resolution.md) | Stage42 Kernel Collection Dependency Resolution |
| stage43 | [experiment-46](experiment-46-stage43-mach-o-xnu-loader-probe.md) | Stage43 Mach-O/XNU Loader Probe |
| stage44 | [experiment-47](experiment-47-stage44-mach-o-load-plan.md) | Stage44 Mach-O Fixture Load Plan |
| stage45 | [experiment-48](experiment-48-stage45-macho-materialization.md) | Stage45 Local Mach-O Materialization |
| stage46 | [experiment-49](experiment-49-stage46-xnu-tte-dryrun.md) | Stage46 XNU TTE Workspace Dry-Run |
| stage47 | [experiment-50](experiment-50-stage47-tte-verify-vtop-dryrun.md) | Stage47 TTE Descriptor Verification and VTOP Dry-Run |
| stage48 | [experiment-51](experiment-51-stage48-high-va-safe-table-materialization.md) | Stage48 High-VA Safe Table Materialization |
| stage49 | [experiment-52](experiment-52-stage49-controlled-ttbr-roundtrip.md) | Stage49 Controlled TTBR0 Round-Trip |
| stage50 | [experiment-53](experiment-53-stage50-public-xnu-workspace-cancro-scaffold.md) | Stage50 Public-XNU Workspace and Cancro Scaffold |
| stage51 | [experiment-54](experiment-54-stage51-public-xnu-object-subset-compile.md) | Stage51 Public-XNU Object Subset Compile |
| stage52 | [experiment-55](experiment-55-stage52-public-xnu-controlled-link-proof.md) | Stage52 Public-XNU Controlled Link Proof |
| stage53 | [experiment-56](experiment-56-stage53-xnu-compile-graph.md) | Stage53 Public-XNU Compile Graph Migration Proof |
| stage54 | [experiment-57](experiment-57-stage54-pexpert-platform-compile-graph.md) | Stage54 Public-XNU pexpert/platform Compile Graph Proof |
| stage55 | [experiment-58](experiment-58-stage55-pexpert-consistent-debug-compile-graph.md) | Stage55 Public-XNU pexpert consistent-debug Compile/Link Proof |
| stage56 | [experiment-59](experiment-59-stage56-xnu-bootstrap-mapping-contract.md) | Stage56 XNU bootstrap mapping contract |
| stage57 | [experiment-60](experiment-60-stage57-xnu-pmap-bootstrap-contract.md) | Stage57 XNU pmap/bootstrap allocation contract |
| stage58 | [experiment-61](experiment-61-stage58-xnu-pmap-table-dryrun-contract.md) | Stage58 XNU pmap table population dry-run contract |
| stage59 | [experiment-62](experiment-62-stage59-xnu-pmap-page-dryrun-contract.md) | Stage59 XNU pmap page-granular dry-run contract |
| stage60 | [experiment-63](experiment-63-stage60-xnu-pmap-attr-dryrun-contract.md) | Stage60 XNU pmap cache/MMU attribute dry-run contract |
| stage61 | [experiment-64](experiment-64-stage61-xnu-pmap-multiwindow-dryrun-contract.md) | Stage61 XNU pmap multi-window page-granular dry-run contract |
| stage62 | [experiment-65](experiment-65-stage62-xnu-pmap-transition-dryrun-contract.md) | Stage62 XNU pmap safe live-table transition prerequisite dry-run contract |
| stage63 | [experiment-66](experiment-66-stage63-xnu-pexpert-hook-readiness-contract.md) | Stage63 MSM8974 XNU pexpert interrupt/timer hook readiness contract |
| stage64 | [experiment-67](experiment-67-stage64-xnu-iokit-platform-scaffold.md) | Stage64 XNU IOKit/platform-driver scaffold readiness contract |
| stage65 | [experiment-68](experiment-68-stage65-xnu-iokit-match-dryrun-contract.md) | Stage65 XNU IOKit service/driver match dry-run contract |
| stage66 | [experiment-69](experiment-69-stage66-xnu-iokit-registry-service-dryrun-contract.md) | Stage66 XNU IOKit registry/service dry-run contract |
| stage67 | [experiment-70](experiment-70-stage67-xnu-iokit-provider-plane-dryrun-contract.md) | Stage67 XNU IOKit provider-plane dry-run contract |
| stage68 | [experiment-71](experiment-71-stage68-xnu-iokit-catalog-property-dryrun-contract.md) | Stage68 XNU IOKit catalog/property dry-run contract |
| stage69 | [experiment-72](experiment-72-stage69-xnu-iokit-property-inheritance-dryrun-contract.md) | Stage69 XNU IOKit property-inheritance / registry-entry dry-run contract |
| stage70 | [experiment-73](experiment-73-stage70-xnu-iokit-registry-topology-dryrun-contract.md) | Stage70 XNU IOKit registry-plane / IODeviceTree topology dry-run contract |
| stage71 | [experiment-74](experiment-74-stage71-xnu-iokit-attach-start-readiness-dryrun-contract.md) | Stage71 XNU IOKit attach/start readiness dry-run contract |
| stage72 | [experiment-75](experiment-75-stage72-xnu-iokit-lifecycle-register-service-readiness-dryrun-contract.md) | Stage72 XNU IOKit lifecycle/register-service readiness dry-run contract |
| stage73 | [experiment-76](experiment-76-stage73-xnu-iokit-provider-notification-delivery-readiness-dryrun-contract.md) | Stage73 XNU IOKit provider-notification / interest / delivery-readiness dry-run contract |
| stage74 | [experiment-77](experiment-77-stage74-xnu-iokit-provider-callback-client-notification-readiness-dryrun-contract.md) | Stage74 XNU IOKit provider-callback / client-notification readiness dry-run contract |
| stage75 | [experiment-78](experiment-78-stage75-xnu-iokit-client-open-provider-claim-close-readiness-dryrun-contract.md) | Stage75 XNU IOKit client-open / provider-claim / close-readiness dry-run contract |
| stage76 | [experiment-79](experiment-79-stage76-live-xnu-like-execution-probe.md) | Stage76 live XNU-like execution probe |
| stage77 | [experiment-80](experiment-80-stage77-xnu-entry-stub.md) | Stage77 XNU entry stub |
| stage78 | [experiment-81](experiment-81-stage78-early-pmap-platform-init.md) | Stage78 early pmap/platform-init micro-sequence |
| stage79 | [experiment-82](experiment-82-stage79-pe-init-platform-false.md) | Stage79 PE_init_platform(FALSE,args)-shaped pre-VM pexpert/platform micro-sequence |
| stage80 | [experiment-83](experiment-83-stage80-arm-init-post-pe-bootstrap.md) | Stage80 arm_init post-PE_FALSE bootstrap/timebase-registration boundary |
| stage81 | [experiment-84](experiment-84-stage81-arm-vm-init-live-pmap.md) | Stage81 arm_vm_init live-pmap installation window |
| stage82 | [experiment-85](experiment-85-stage82-renumbering-validation.md) | Stage82 Renumbering Validation Baseline |
| stage83 | [experiment-86](experiment-86-stage83-full-kernel-pmap.md) | Stage83 Full Kernel Virtual Address Space L1+L2 Pmap |
| stage84 | [experiment-87](experiment-87-stage84-sgi-irq-timer.md) | Stage84 GIC/IRQ Timer Boundary |
| stage85 | [experiment-88](experiment-88-stage85-high-va-code-exec.md) | High-Virtual Code Execution at 0x80000000 (L2 Pages) |
| stage86 | [experiment-89](experiment-89-stage86-high-va-irq-handler.md) | High-Virtual IRQ Handler Relocation at 0x80000000 (L2 Pages) |
| stage87 | [experiment-90](experiment-90-stage87-high-va-data-abort-handler.md) | High-VA Data Abort Handler |
| stage88 | [experiment-91](experiment-91-stage88-high-va-undef-handler.md) | High-VA Undefined Instruction Handler |
| stage89 | [experiment-92](experiment-92-stage89-macho-loader.md) | Mach-O Kernel Loader |
| stage90 | [experiment-93](experiment-93-stage90-phase0-preflight-watchdog.md) | Stage90 Phase 0: Preflight Watchdog Run |
| stage90 | [experiment-94](experiment-94-stage90-phase0-watchdog-and-baseline.md) | Stage90 Phase 0: Recovery Net Proved, and the Two Bugs the Baseline Run Found |
| stage90 | [experiment-95](experiment-95-stage90-phase1a-normal-nc.md) | Stage90 Phase 1a: Normal, Non-cacheable DRAM |
| stage90 | [experiment-96](experiment-96-stage90-phase1-exclusives-work.md) | Stage90 Phase 1: LDREX/STREX Work Here, and the Probe Said Otherwise for Four Runs |
| stage90 | [experiment-97](experiment-97-stage90-phase1-icache.md) | Stage90 Phase 1: The I-Cache On, and the Six Places That Asserted Caches Are Off |
| stage90 | [experiment-98](experiment-98-stage90-phase1-dcache.md) | Stage90 Phase 1: The D-Cache On, and Phase 1 Closed |
| stage90 | [experiment-99](experiment-99-stage90-phase2-boot-args-on-hardware.md) | Stage90 Phase 2: The Conforming boot_args on Hardware, Under the Caches |
| stage90 | [experiment-100](experiment-100-first-public-xnu-execution.md) | The First Public-XNU Code to Execute on the Device |
| stage90 | [experiment-101](experiment-101-phase3-shim-first-hardware-run.md) | Phase 3 Platform Shim, First Hardware Run: One Missing Assignment |
| stage90 | [experiment-102](experiment-102-xnu-consistent-debug-registry.md) | XNU's Crash-Log Registry Runs on MSM8974, and It Writes a Record |
| stage90 | [experiment-103](experiment-103-xnu-entry-point-assembles.md) | XNU's Entry Point Assembles, and the Missing Configuration Is Now a Symbol List |
| stage90 | [experiment-104](experiment-104-shim-drives-the-timer.md) | The Platform Shim Drives the Timer, Through XNU's Own Interface |
| stage90 | [experiment-105](experiment-105-xnu-console-and-boot-args.md) | XNU Writes Into the Crash Log, and Acts on Our Boot Arguments |
| stage90 | [experiment-106](experiment-106-xnu-start-executes.md) | XNU's `_start` Executes on MSM8974 |
| stage90 | [experiment-107](experiment-107-xnu-arm-entry-path-measured.md) | XNU's ARM Entry Path, Measured: Seven Missing Names, Three of Them MIG |
| stage90 | [experiment-108](experiment-108-mig-builds-and-generates-headers.md) | MIG Builds on This Host, and 23 Mach Interface Headers Are Generated |
| stage90 | [experiment-109](experiment-109-xnu-arm-entry-path-compiles.md) | XNU's ARM Entry Path Compiles: `arm_init.c` 35 Errors → 0 |
| stage90 | [experiment-110](experiment-110-xnu-arm-layer-compiles.md) | XNU's Entire ARM Layer Compiles: 32 of 32, 443 Symbols From Linking |
| stage90 | [experiment-111](experiment-111-osfmk-measured.md) | The Rest of osfmk Measured — and the Build Configuration Was Never Absent |
| stage90 | [experiment-112](experiment-112-apple-kernel-config-extracted.md) | Apple's Own Kernel Configuration, Extracted and Used |
| stage90 | [experiment-113](experiment-113-arm-kernel-build-manifest.md) | Apple's Own Build Manifest for an ARM Kernel, Resolved: 694 Files |
| stage90 | [experiment-114](experiment-114-kernel-manifest-compiled.md) | The Kernel's Own File List, Compiled: 172 of 569 |
| stage90 | [experiment-115](experiment-115-minimal-boot-configuration.md) | A Minimal-Boot Configuration, and One Define That Unlocked Twelve Files |
| stage90 | [experiment-116](experiment-116-define-generator-and-timeout.md) | A 45-Minute Hang, and Two Tooling Defects It Exposed |
| stage90 | [experiment-117](experiment-117-shadows-and-broad-paths.md) | Two Shims Were Shadowing Real Headers, and a Broad Include Path Cost Four Files |
| stage90 | [experiment-118](experiment-118-per-component-defines.md) | The Per-Component Defines, and the 127-File Defect They Were |
| stage90 | [experiment-119](experiment-119-build-generated-headers.md) | The Headers the Build Generates, and the Three Ways This Project Was Not Generating Them |
| stage90 | [experiment-120](experiment-120-options-generated-headers.md) | The Third Kind of Generated Header: `OPTIONS/`, and the Ten Shims It Replaced |
| stage90 | [experiment-121](experiment-121-apple-defines-and-two-pass-mig.md) | `__APPLE__`, and the MIG Run That Was Missing Half Its Arguments |
| stage90 | [experiment-122](experiment-122-makesyscalls-outputs.md) | `makesyscalls.sh` Has Six Output Kinds and This Project Asked for One |
| stage90 | [experiment-123](experiment-123-link-gap-and-force-includes.md) | The Link Gap, Measured: 1187 Symbols — and the Force-Include That Was Reaching Nine Files |
| stage90 | [experiment-124](experiment-124-mig-output-set.md) | Generate What the Makefiles Say: 40 MIG Outputs, Not Everything — and the Include Order Flips Back |
| stage90 | [experiment-125](experiment-125-first-link-and-option-scope.md) | The First Real Link, and the 47 Files That Were Compiled as the Wrong Configuration |
| stage90 | [experiment-126](experiment-126-clockt-and-libsa-types.md) | `CLOCK_T` Was a Workaround for the Old Defect, and `<types.h>` Is the Kernel's |
| stage90 | [experiment-127](experiment-127-device-headers-and-a-source-incompatibility.md) | The Loopback Header, and the First Failure That Is Not a Configuration Gap |
| stage90 | [experiment-128](experiment-128-manifest-missing-component.md) | The Manifest Was Missing a Whole Component, and It Cost 200 Symbols |
| stage90 | [experiment-129](experiment-129-caddr-t.md) | `caddr_t`, the Biggest Boot-Path File, and the Three Items Left Behind It |
| stage90 | [experiment-130](experiment-130-two-negative-results.md) | Two Boot-Path Files Investigated, Neither Fixed, and Why |
| stage90 | [experiment-131](experiment-131-device-table.md) | The Build Defined `MONOTONIC` and the Manifest Never Built the File |
| stage90 | [experiment-132](experiment-132-pty-device-and-a-named-unknown.md) | The pty Device, Resolved by Measurement — and One Value Left With No Evidence |
| stage90 | [experiment-133](experiment-133-device-baseline-revalidated.md) | The Device Line, Re-Validated After Fourteen Stages of Host-Side Work |
| stage90 | [experiment-134](experiment-134-pe-arm-init-interrupts-replacement.md) | The Replacement for `pe_arm_init_interrupts`, Run on the Device |
| stage90 | [experiment-135](experiment-135-first-xnu-image.md) | An XNU Image, Linked at XNU's Own Addresses, With a Real `_start` |
| stage90 | [experiment-136](experiment-136-assym-and-a-correction.md) | The Sixth Generator (`genassym.c`), and a Correction to the Fifth Experiment |
| stage90 | [experiment-137](experiment-137-work-order.md) | The Work Order, Computed From the Image: `arm_init` Reaches a Stub One Edge In |
| stage90 | [experiment-138](experiment-138-underscore-convention-and-work-list.md) | The Assembly Underscore Convention, and the Work List Completed |
| stage90 | [experiment-139](experiment-139-two-ast-headers.md) | Two Headers Named `kern/ast.h`, and Apple's Per-Component Include Order |
| stage90 | [experiment-140](experiment-140-three-missing-names.md) | Three Missing Names, Three Narrow Answers |
| stage90 | [experiment-141](experiment-141-vnode-trim-is-the-triple.md) | `vnode_trim` Is a Target-Triple Conflict, and No Macro Can Fix It |
| stage90 | [experiment-142](experiment-142-darwin-assembles.md) | The Darwin Target Assembles Every File the EABI Target Cannot |
| stage90 | [experiment-143](experiment-143-fiq-not-available.md) | MSM8974 Will Not Deliver an FIQ to Non-Secure PL1, Measured |
| stage90 | [experiment-144](experiment-144-sync-qos-and-the-export-lists.md) | Why `sync_qos_count_t` Is Not a Missing Typedef, and What the Export Lists Say |
| stage90 | [experiment-145](experiment-145-the-simport-answer.md) | The `simport` Answer, Found in Two Makefile Rules — 602 → 606 of 615 |
| stage90 | [experiment-146](experiment-146-arm-case-and-the-last-three.md) | `__ARM__` Versus `__arm__`, and the Three Single-File Failures Left |
| stage90 | [experiment-147](experiment-147-phase3-shim-design-closed.md) | The Phase 3 Shim's Design, Closed on Hardware |
| stage90 | [experiment-148](experiment-148-nbpfilter-and-the-last-category.md) | `NBPFILTER` Is a Count, and the Category of Files Only Valid With an Option Built |
| stage90 | [experiment-149](experiment-149-macho-path-measured.md) | The Mach-O Path, Built and Measured, So the Decision Has a Price Tag |
| stage90 | [experiment-150](experiment-150-lld-cannot-link-armv7-and-the-dialect-translation.md) | `ld64.lld` Cannot Link 32-bit ARM Mach-O; the Dialect Translation Closes All 17 |
| stage90 | [experiment-151](experiment-151-force-includes-and-stdbool.md) | The Force-Includes Were Breaking a File, and the Fix Is Per-File |
| stage90 | [experiment-152](experiment-152-the-cpp-block-and-one-dead-line.md) | The Whole C++ Block Is Behind One Dead Line, and No Flag Can Fix It *(retracted — see 154)* |
| stage90 | [experiment-153](experiment-153-shim-audit.md) | The Shims Audited: 16 Shadow Real Headers, and None of Them Matter |
| stage90 | [experiment-154](experiment-154-cpp-entered-the-build.md) | The C++ Block Entered the Build at 75 of 83 — and the Line Blocking It Was Mine |
| stage90 | [experiment-155](experiment-155-the-translator-wrote-into-the-tree.md) | The Assembler Translator Was Writing Into Apple's Tree, Through a Symlink |
| stage90 | [experiment-156](experiment-156-the-mig-run-was-missing-kernel.md) | The MIG Run Was Missing `-DKERNEL` — 13 Symbols, and Every Routine Name |
| stage90 | [experiment-157](experiment-157-the-force-include-was-rewriting-declarations.md) | `-include kern/queue.h` Was Rewriting Declarations Before the Compiler Saw Them |
| stage90 | [experiment-158](experiment-158-the-closure-is-the-whole-kernel.md) | `arm_init`'s Closure Is the Whole Kernel; the Manifest Omits 126 `optional` Sources |
| stage90 | [experiment-159](experiment-159-the-real-arm-init-ran.md) | The Real `arm_init` Ran on the Device, and Named `cpu_data_init` |
| stage90 | [experiment-160](experiment-160-vm-object-compiles.md) | `vm_object.c` Compiles at Last — 58 of the 189, and `-Dconst=` Is Per-File |
| stage90 | [experiment-161](experiment-161-the-target-abi.md) | The Target ABI: an ELF Triple with Darwin's Type Widths — 132 → 97 |
| stage90 | [experiment-162](experiment-162-the-firehose-seam-is-a-component.md) | The Firehose Seam Is a Component, Not a Value — `FIREHOSE_BUFFER_KERNEL_CHUNK_COUNT` Is 16, 97 → 72 |
| stage90 | [experiment-163](experiment-163-the-nearest-blocker-was-not-xnu-s.md) | The Nearest Blocker Was Not XNU's: the EABI Runtime, 72 → 63, and a Figure of Mine That Was Wrong |
| stage90 | [experiment-164](experiment-164-an-off-option-has-two-spellings.md) | An Off Option Has Two Spellings, and the Tree Depends on Both — `task.c` Compiles, 220 → 114 |
| stage90 | [experiment-165](experiment-165-two-files-one-line-each.md) | Two Files, One Line Each: the C++ Block Is Complete, and No Boot-Path Stub Is Behind a Compile Failure (114 → 102) |
| stage90 | [experiment-166](experiment-166-the-generated-inputs-existed-for-one-configuration.md) | The Generated Inputs Existed for One Configuration Only — `pty.h`/`loop.h` Fell Through to the Host's (102 → 94) |
| stage90 | [experiment-167](experiment-167-the-string-h-shim-was-one-block-short.md) | The `<string.h>` Shim Was One Block Short, and the Measurement Was Reading an Older Directory (94 → 92) |
| stage90 | [experiment-168](experiment-168-cpu-data-init-ran-and-named-pe-init-platform.md) | The Second Real XNU Object Ran on the Device, and Named `PE_init_platform` |
| stage90 | [experiment-169](experiment-169-the-image-crossed-the-limit-and-stopped-at-strlcpy.md) | The Image Crossed Its Old Page-Table Limit, and `PE_init_platform` Stopped at `strlcpy` |
| stage90 | [experiment-170](experiment-170-xnu-own-pe-state-named-the-device-tree.md) | XNU's Own `PE_state` Named the Device Tree: `xnu_entry_dtinit_base=0x00280000` |
| stage90 | [experiment-171](experiment-171-the-reader-ran-and-stopped-at-strcmp.md) | XNU's Own Reader Ran Against This Project's Tree, and Stopped at `strcmp` |
| stage90 | [experiment-172](experiment-172-the-reader-reached-a-cpu-nodes-state.md) | The Reader Walked to a CPU Node's `state` Property — Six Claims from `stub_hit=strncmp` |
| stage90 | [experiment-173](experiment-173-pe-init-platform-reached-its-last-statement.md) | `PE_init_platform` Reached Its Last Statement, `pe_init_debug` |
| stage90 | [experiment-174](experiment-174-pe-init-debug-ran-and-stopped-at-pe-boot-args.md) | `pe_init_debug` Ran, and the Frontier Is `PE_boot_args` |
| stage90 | [experiment-175](experiment-175-the-entry-image-is-derived-not-written-down-twice.md) | The Entry Image Is Derived From the Link, Not Written Down Twice |
| stage90 | [experiment-176](experiment-176-pe-init-platform-returned-and-the-soc-base-came-from-the-tree.md) | `PE_init_platform` Returned, and the SoC Base Came Out of the Tree |
| stage90 | [experiment-177](experiment-177-xnu-counted-four-cpus-and-the-frontier-is-cpu-processor-alloc.md) | XNU Counted Four CPUs in This Project's Tree, and the Frontier Is `cpu_processor_alloc` |
| stage90 | [experiment-178](experiment-178-the-two-halves-of-cpudataentries-agree-and-the-frontier-is-thread-bootstrap.md) | The Two Halves of `CpuDataEntries` Agree, and the Frontier Is `thread_bootstrap` |
| stage90 | [experiment-179](experiment-179-thread-bootstrap-ran-and-the-frontier-is-timer-init.md) | `thread_bootstrap` Ran Its Assignments, and the Frontier Is `timer_init` |
| stage90 | [experiment-180](experiment-180-thread-bootstrap-ran-to-its-last-statement-and-the-frontier-is-machine-set-current-thread.md) | `thread_bootstrap` Ran to Its Last Statement, and the Frontier Is `machine_set_current_thread` |
| stage90 | [experiment-181](experiment-181-the-tpidrprw-round-trip-held-and-the-frontier-is-rtclock-early-init.md) | The TPIDRPRW Round Trip Held, and the Frontier Is `rtclock_early_init` |
| stage90 | [experiment-182](experiment-182-the-timebase-callback-ran-and-the-frontier-is-the-toolchains-aeabi-uldivmod.md) | The Timebase Callback Ran, and the Frontier Is the Toolchain's `__aeabi_uldivmod` |
| stage90 | [experiment-183](experiment-183-the-thread-pointer-round-tripped-and-the-frontier-is-kernel-early-bootstrap.md) | The Thread Pointer Round-Tripped Through TPIDRPRW, and the Frontier Is `kernel_early_bootstrap` |
| stage90 | [experiment-184](experiment-184-the-timebase-came-out-as-the-trees-19-2-mhz-and-a-wrong-prototype-wrote-to-address-zero.md) | The Timebase Came Out as the Tree's 19.2 MHz, and a Hand-Written Prototype Wrote to Address 0 |
| stage90 | [experiment-185](experiment-185-lck-mod-init-ran-for-real-and-the-frontier-is-strncpy.md) | `lck_mod_init` Ran for Real, and the Frontier Is `strncpy` |
| stage90 | [experiment-186](experiment-186-the-lock-group-read-back-as-its-own-bytes-and-the-frontier-is-lck-mtx-init-ext.md) | The Lock Group Read Back as Its Own Bytes, and the Frontier Is `lck_mtx_init_ext` |
| stage90 | [experiment-187](experiment-187-three-lock-groups-read-out-of-xnus-own-list-and-the-frontier-is-timer-call-get-priority-params.md) | Three Lock Groups Read Out of XNU's Own List, and the Frontier Is `timer_call_get_priority_params` |
| stage90 | [experiment-188](experiment-188-the-timer-table-converted-into-ticks-and-the-frontier-is-do-cpuid.md) | The Timer Table Converted into Ticks, and the Frontier Is `do_cpuid` |
| stage90 | [experiment-189](experiment-189-the-cpu-identified-itself-and-the-frontier-is-processor-bootstrap.md) | The CPU Identified Itself, and the Frontier Is `processor_bootstrap` |
| stage90 | [experiment-190](experiment-190-the-schedulers-own-processor-checked-pointer-by-pointer-and-the-frontier-is-processor-data-init.md) | The Scheduler's Own Processor, Checked Pointer by Pointer, and the Frontier Is `processor_data_init` |
| stage90 | [experiment-191](experiment-191-processor-data-init-ran-the-interrupt-state-and-the-frontier-is-ml-set-interrupts-enabled.md) | `processor_data_init` Ran, the Interrupt State, and the Frontier Is `ml_set_interrupts_enabled` |
| stage90 | [experiment-192](experiment-192-the-interrupt-state-ran-for-real-and-the-frontier-is-a-device-tree-node-not-an-object.md) | The Interrupt State Ran for Real, and the Frontier Is a Device-Tree Node, Not an Object |
| stage90 | [experiment-193](experiment-193-defaults-was-the-missing-node-and-the-f-bit-does-not-read-1.md) | `/defaults` Was the Missing Node, and the F Bit Does Not Read 1 on This Device |
| stage90 | [experiment-194](experiment-194-the-image-describes-itself-in-a-mach-o-header-and-the-frontier-is-vm-set-page-size.md) | The Image Describes Itself in a Mach-O Header, and the Frontier Is `vm_set_page_size` |
| stage90 | [experiment-195](experiment-195-the-vm-object-runs-and-the-flush-was-reading-the-wrong-cache.md) | The VM's Own Object Runs, `pmap_bootstrap` Is Reached, and the Flush That Carries the Evidence Was Reading the Wrong Cache |
| stage90 | [experiment-196](experiment-196-the-payloads-whole-cache-flush-was-reading-the-l2-and-selecting-no-way.md) | The Payload's Whole-Cache Flush Was Reading the L2's Geometry and Selecting No Way |
| stage90 | [experiment-197](experiment-197-the-pmap-runs-and-the-frontier-is-patch-low-glo-static-region.md) | The pmap Runs, `arm_vm_init` Reaches Its Last Instruction, and the Frontier Is `patch_low_glo_static_region` |
| stage90 | [experiment-198](experiment-198-patch-low-glo-runs-arm-vm-init-returns-and-the-frontier-is-printf-init.md) | `patch_low_glo` Runs, `arm_vm_init` Returns, and the Frontier Is `printf_init` |
| stage90 | [experiment-199](experiment-199-printf-init-runs-and-the-frontier-is-bsd-log-init.md) | `printf_init` Runs, the Four Bytes Are Now the Object, and the Frontier Is `bsd_log_init` |
| stage90 | [experiment-200](experiment-200-an-empty-function-is-a-whole-object-and-the-frontier-is-panic-init.md) | An Empty Function Is a Whole Object, `arm_init` Walks Back Into `osfmk/`, and the Frontier Is `panic_init` |
| stage90 | [experiment-201](experiment-201-xnu-reads-this-images-mach-o-header-and-the-frontier-is-pe-consistent-debug-inherit.md) | XNU Reads This Image's Mach-O Header, the Panic Stand-In Is Retired, and the Frontier Is `PE_consistent_debug_inherit` |
| stage90 | [experiment-202](experiment-202-the-smallest-object-so-far-and-the-frontier-is-pe-init-kprintf.md) | The Smallest Object So Far, a Prediction That Depended on a Payload Switch, and the Frontier Is `PE_init_kprintf` |
| stage90 | [experiment-203](experiment-203-pe-init-kprintf-runs-to-serial-init.md) | `PE_init_kprintf` Runs Its Whole Body Up to `serial_init`, and Two Storage Stubs Become Real Variables |
| stage90 | [experiment-204](experiment-204-serial-init-runs-and-a-prediction-that-was-wrong-because-there-are-two-command-lines.md) | `serial_init` Runs, a Step With No Cost, and a Prediction That Was Wrong Because There Are Two Command Lines |
| stage90 | [experiment-205](experiment-205-initialize-screen-runs-its-no-video-branch-and-the-frontier-is-switch-to-serial-console.md) | `initialize_screen` Runs Its No-Video Branch, XNU Decides There Is No Framebuffer, and the Frontier Is `switch_to_serial_console` |
| stage90 | [experiment-206](experiment-206-arm-inits-tail-runs-and-the-frontier-is-io-map.md) | `arm_init`'s Tail Runs, XNU's Own Platform Interrupt Mapping Is Reached, and the Frontier Is `io_map` |
| stage90 | [experiment-207](experiment-207-io-map-runs-and-the-frontier-is-bcopy-phys.md) | `io_map` Runs, XNU Creates a Kernel Mapping for Itself, and the Frontier Is `bcopy_phys` |
| stage90 | [experiment-208](experiment-208-bcopy-phys-runs-and-the-frontier-is-clean-dcache.md) | `bcopy_phys` Runs, the Secondary-CPU Handshake Is Written Into the Low-Vectors Page, and the Frontier Is `CleanPoC_DcacheRegion` |
| stage90 | [experiment-209](experiment-209-caches-asm-links-and-the-frontier-is-clean-dcache.md) | The Cache-Maintenance Surface Links, `cpu_machine_idle_init` Reaches Its Last Call, and the Frontier Is `clean_dcache` |
| stage90 | [experiment-210](experiment-210-xnu-enters-pe-arm-init-interrupts-itself-and-the-frontier-is-early-random.md) | XNU Enters `pe_arm_init_interrupts` Itself, `cpu_machine_idle_init` Returns, and the Frontier Is `early_random` |
| stage90 | [experiment-211](experiment-211-the-prng-links-the-handoff-tree-gets-boot-entropy-and-the-frontier-is-ccdrbg-factory-nisthmac.md) | The PRNG Links, the Handoff Tree Grows Boot Entropy, and the Frontier Is `ccdrbg_factory_nisthmac` |
| stage90 | [experiment-212](experiment-212-the-drbg-factory-runs-and-the-frontier-is-cchmac-init.md) | the DRBG Factory Runs, `early_random` Makes Its First Indirect Call, and the Frontier Is `cchmac_init` |
| stage90 | [experiment-213](experiment-213-the-prediction-was-wrong-and-the-frontier-is-a-null-compress-pointer.md) | the Prediction Was Wrong, the Run Took a Prefetch Abort at Address Zero, and the Method That Predicted It Was Reading Only Half the Calls |
| stage90 | [experiment-214](experiment-214-ccsha1-eay-di-gets-its-value-and-the-frontier-is-cchmac-update.md) | `ccsha1_eay_di` Gets Its Value, Real SHA-1 Compression Runs on the Device, and the Frontier Is `cchmac_update` |
| stage90 | [experiment-215](experiment-215-four-bytes-and-the-frontier-is-ccdigest-update.md) | the Smallest Object There Is, `cchmac_update` Resolves and Adds Nothing, and the Frontier Is `ccdigest_update` |
| stage90 | [experiment-216](experiment-216-ccdigest-update-runs-to-completion-and-the-frontier-is-cchmac-final.md) | `ccdigest_update` Runs to Completion, the First Generic Digest Helper on the Device, and the Frontier Is `cchmac_final` |
| stage90 | [experiment-217](experiment-217-cchmac-final-resolves-and-the-frontier-is-ccdigest-final-64be.md) | `cchmac_final` Resolves, Nothing New Executes, and the Frontier Is `ccdigest_final_64be` |
| stage90 | [experiment-218](experiment-218-the-whole-hmac-finalisation-runs-and-the-frontier-is-cchmac.md) | the Whole HMAC Finalisation Runs on the Device, the Text Arithmetic Gets Three Terms, and the Frontier Is `cchmac` |
| stage90 | [experiment-219](experiment-219-a-whole-one-shot-hmac-runs-and-the-frontier-is-cc-clear.md) | a Whole One-Shot HMAC Runs, `cchmac` Resolves, and the Frontier Is `cc_clear` |
| stage90 | [experiment-220](experiment-220-cc-clear-adds-memset-s-and-the-frontier-is-memset-s.md) | `cc_clear` Resolves and Adds `memset_s`, `.text` Does Not Move, and the Frontier Is `memset_s` |
| stage90 | [experiment-221](experiment-221-the-whole-drbg-generate-path-runs-and-the-frontier-is-cc-cmp-safe.md) | the Whole DRBG Generate Path Runs to Its FIPS Compare, and the Frontier Is `cc_cmp_safe` |
| stage90 | [experiment-222](experiment-222-the-prng-is-finished-and-the-frontier-is-kernel-bootstrap.md) | the PRNG Is Finished, `arm_init` Completes, and the Frontier Is `kernel_bootstrap`'s `bsd_scale_setup` |
| stage90 | [experiment-223](experiment-223-bsds-unix-startup-links-and-the-frontier-is-a-group.md) | BSD's `unix_startup` Links, the Frontier's First Group, and the Next Stop Is `kernel_debug_string_early` |
| stage90 | [experiment-224](experiment-224-bsd-init-links-a-hundred-stubs-and-the-frontier-is-kernel-debug-string-early.md) | `bsd_init` Links, a Hundred Stubs Arrive, and the Frontier Is `kernel_debug_string_early` |
| stage90 | [experiment-225](experiment-225-the-first-neon-runs-and-the-frontier-is-vm-mem-bootstrap.md) | the First NEON Executes, `kernel_debug_string_early` Runs, and the Frontier Is `vm_mem_bootstrap` |
| stage90 | [experiment-226](experiment-226-the-prediction-was-wrong-and-why.md) | the Prediction Was Wrong — `vm_compressor_init_locks` for `zone_bootstrap` — and Why |
| stage90 | [experiment-227](experiment-227-the-tools-first-prediction-and-vm-map-steal-memory.md) | the Tool's First Prediction, `vm_map_steal_memory`, and the NEON That Waited Two Experiments |
| stage90 | [experiment-228](experiment-228-the-prediction-was-wrong-and-the-guard-rule-could-not-tell-two-loops-apart.md) | the Prediction Was Wrong — `OSCompareAndSwap16` for `zone_bootstrap` — and the Guard Rule That Could Not Tell Two Loops Apart |
| stage90 | [experiment-229](experiment-229-ten-stubs-for-nothing-and-the-frontier-moved-two-levels-in.md) | Ten Stubs for Nothing, Zero Added, and the Tail Term Identified to the Byte |
| stage90 | [experiment-230](experiment-230-the-stop-was-inside-the-object-just-linked.md) | the Stop Was Inside the Object Just Linked — and `--root <frontier>` Names It |
| stage90 | [experiment-231](experiment-231-the-prediction-holds-and-zone-bootstrap-at-last.md) | the Prediction Holds — `zone_bootstrap` at Last, on Two Source Branches Instead of a Walk |
| stage90 | [experiment-232](experiment-232-two-in-a-row-and-the-walk-and-the-root-agreeing.md) | Two in a Row, `thread_call_setup`, and the Walk and the Root Agreeing |
| stage90 | [experiment-233](experiment-233-three-in-a-row-and-vm-object-bootstrap.md) | Three in a Row, `vm_object_bootstrap`, and What the Walk Now Reaches |
| stage90 | [experiment-234](experiment-234-snprintf-the-runtime-flag-no-walk-can-read.md) | `snprintf` for `kmem_alloc_kobject` — a Runtime Flag No Walk Can Read, and the Guard Rule Wrong in the Other Direction |
| stage90 | [experiment-235](experiment-235-no-stub-at-all-an-undefined-instruction-and-the-two-udfs.md) | No Stub at All — an Undefined Instruction, and the Image's Exactly Two `udf`s |
| stage90 | [experiment-236](experiment-236-the-trap-named-xnu-panicked-and-the-message-is-in-dram.md) | the Trap Named — `xnu_entry_undef_pc=0x0022d1a8` Is `DebuggerTrapWithState`, so **XNU Panicked** |
| stage90 | [experiment-237](experiment-237-the-panic-state-three-globals-and-why-all-three-are-zero.md) | the Panic State — Three Globals, All Zero, and `handle_debugger_trap`'s Restore That Predicts It |
| stage90 | [experiment-238](experiment-238-the-panic-registers-and-the-string-they-point-at.md) | the Panic Registers — `"zfree: freeing invalid "` Out of the `va_list`, and `zalloc.c:1208` |
| stage90 | [experiment-239](experiment-239-zero-zone-map-bounds-and-an-element-that-cannot-be-a-kernel-address.md) | Zero Zone-Map Bounds — an Element That Cannot Be a Kernel Address, and the Frontier Ends at the Image's Base |
| stage90 | [experiment-240](experiment-240-the-frame-db-proceed-on-sync-failure-and-the-zone-named-maps.md) | the Frame and the Two Arguments — `db_proceed_on_sync_failure` in `r5`, the Zone Named `"maps"` |
| stage90 | [experiment-241](experiment-241-the-base-moves-to-80000000-and-the-panic-becomes-a-permission-fault.md) | the Base Moves to 0x80000000 — the `zfree` Panic Is Gone, and `DFSR=0x80f` Is a Write to a Read-Only Page |
| stage90 | [experiment-242](experiment-242-the-faulting-instruction-and-a-page-table-page-made-read-only-by-the-same-routine.md) | the Faulting Instruction — a Page-Table Page `pmap_init_pte_static_page` Had Already Made Read-Only, Reached Because This Mach-O Has No `__PRELINK_TEXT` |
| stage90 | [experiment-243](experiment-243-the-prelink-segment-and-the-frontier-is-an-object-again.md) | the `__PRELINK_TEXT` Segment — the Fault Is Gone, `vm_map_init` Passes, and the Stop Is `kmem_init` |
| stage90 | [experiment-244](experiment-244-vm-kern-linked-and-a-stub-that-names-its-caller.md) | `osfmk_vm_vm_kern.o` — `kmem_init` Runs, a Real `zalloc` Returns on the Hardware, and the Device Names Its Own Call Site (`vm_map_store_init`, from `vm_map_create+0x58`) |
| stage90 | [experiment-245](experiment-245-the-ll-store-and-a-dispatchers-first-statement.md) | `osfmk_vm_vm_map_store.o` — a Dispatcher's First Statement Stops the Run (`vm_map_store_init_ll`), and a Linked Object That Moves No Address |
| stage90 | [experiment-246](experiment-246-an-empty-function-a-tail-call-and-what-the-caller-key-names.md) | `osfmk_vm_vm_map_store_ll.o` — the Empty Function Returns, the RB Branch Is Taken, and a Tail Call Makes `xnu_entry_stub_caller` Name the Caller of the Caller |
| stage90 | [experiment-247](experiment-247-kmem-init-completes-and-the-drbg-runs.md) | `osfmk_vm_vm_map_store_rb.o` — `kmem_init` Completes, `kernel_map` Gets Its First Gigabyte Region, the HMAC-SHA1 DRBG Produces a Seed, and the Stop Is `vm_allocate_kernel` |
| stage90 | [experiment-248](experiment-248-vm-allocate-kernel-and-pmap-init-run.md) | `osfmk_vm_vm_user.o` — a Kernel Virtual Allocation and `pmap_init` Both Run, the Stop Is `kext_alloc_init`, and the Image Moves Again (`.bss` +0x4000, `end_kern` 0x800e3000) |
| stage90 | [experiment-249](experiment-249-zone-init-runs-and-the-stop-is-kalloc-init.md) | `osfmk_kern_kext_alloc.o` — Fifteen Instructions With No Calls, **`zone_init` Runs** (Answering Experiment 239's Zero Bounds), and the Stop Is `kalloc_init` |
| stage90 | [experiment-250](experiment-250-kalloc-init-completes-and-the-allocator-is-live.md) | `osfmk_kern_kalloc.o` — **`kalloc_init` Completes**: the kalloc map, 28 zones, the lookup table and the allocator's locks; stop `vm_fault_init`; the image moves again |
| stage90 | [experiment-251](experiment-251-vm-fault-init-completes-and-the-stop-is-memory-manager-default-init.md) | `osfmk_vm_vm_fault.o` (30392 B of text) — **`vm_fault_init` Completes**: the throttle threshold formula and the compressor boot-arg paths; stop `memory_manager_default_init`; the image moves a third time |
| stage90 | [experiment-252](experiment-252-two-stops-in-one-object-and-the-stop-is-device-pager-bootstrap.md) | `osfmk_vm_memory_object.o` — **Two Stops in One Object**: `memory_manager_default_init` and `memory_object_control_bootstrap` both complete; stop `device_pager_bootstrap` |
| stage90 | [experiment-253](experiment-253-vm-mem-bootstrap-returns-and-the-stop-leaves-it.md) | `osfmk_vm_device_vm.o` — **`vm_mem_bootstrap` Returns**, and the stop leaves it for the first time since 247: `cs_init` from `kernel_bootstrap` |
| stage90 | [experiment-254](experiment-254-the-stop-is-ledger-credit-inside-the-pmaps-page-table-expansion.md) | `bsd_kern_kern_cs.o` — a wrong prediction and what it measured: the stop is **`ledger_credit` inside the pmap's page-table expansion**, the first runtime-allocation frontier |
| stage90 | [experiment-255](experiment-255-the-ledger-is-live-and-the-frontier-leaves-the-tree.md) | `osfmk_kern_ledger.o` — the ledger is live, a 73728-byte guarded `kernel_map` allocation completes, and the stop is **`__firehose_buffer_create` — the first frontier symbol with no implementation anywhere in the tree** |
| stage90 | [experiment-256](experiment-256-the-firehose-port-opens-and-its-first-missing-header-is-from-a-newer-xnu.md) | host-only: the firehose **port opens** — the source is in the tree, and its first missing header (`os/atomic_private.h`) is from a **newer XNU** than the one this project builds |
| stage90 | [experiment-257](experiment-257-the-firehose-is-ported-and-it-runs.md) | **The firehose is ported and it runs** — Apple's implementation compiled for armv7 and linked in, `__firehose_buffer_create` real; stop `__firehose_allocate` inside it |
| stage90 | [experiment-258](experiment-258-the-firehose-kernel-side-is-in-the-tree.md) | **The firehose's kernel side is in the tree** — the four functions are one already-built object (`libkern/os/log.o`), the two data names come from the port (`16`/`8`); `oslog_init` returns and the stop is `telemetry_init` in `kernel_bootstrap` |
| stage90 | [experiment-259](experiment-259-telemetry-init-completes-and-the-stop-is-console-init.md) | **`telemetry_init` completes** — one more already-built object (`osfmk/kern/telemetry.o`, +17 boundaries) and a real 16 KB `kmem_alloc`; stop `console_init` at `kernel_bootstrap+0x1e4`; the image moves again |
| stage90 | [experiment-260](experiment-260-console-init-completes-with-no-new-boundaries.md) | **`console_init` completes, and the link adds nothing** — `osfmk/console/serial_console.o`, 27 references *all* already satisfied, 9 stand-ins become real; a real 16 KB console ring; stop `stackshot_init`; layout unchanged for the first time since 257 |
| stage90 | [experiment-261](experiment-261-stackshot-init-completes-and-the-scheduler-is-next.md) | **`stackshot_init` completes** — `osfmk/kern/kern_stackshot.o` (not `stackshot.o`), the largest new block since 254 (+40 boundaries, the kcdata/coalition/kdp machinery) and 3 resolved; stop `sched_init` at `kernel_bootstrap+0x204`; the image moves a 16 KB block again |
| stage90 | [experiment-262](experiment-262-the-scheduler-is-two-objects.md) | **The scheduler is two objects** — `sched_prim.o` + the `sched_multiq_dispatch` table it calls through (a NULL stand-in would have jumped to 0); 30 resolved, 28 added; a five-function chain predicted from the linked table and confirmed; stop `ltable_bootstrap` |
| stage90 | [experiment-263](experiment-263-ltable-bootstrap-completes-and-adds-nothing.md) | **`ltable_bootstrap` completes and the link adds nothing** — `osfmk/kern/ltable.o`, 19 references all already satisfied, 1 resolved; stop `waitq_bootstrap` at `kernel_bootstrap+0x224` |
| stage90 | [experiment-264](experiment-264-waitq-bootstrap-completes-and-the-frontier-closes-on-names-262-created.md) | **`waitq_bootstrap` completes, and the frontier closes on names 262 created** — `osfmk/kern/waitq.o`, 49 references of which 48 were already satisfied; the seven `waitq_*` boundaries the scheduler link added resolved; 12 resolved, 1 added; stop `ipc_bootstrap` at `kernel_bootstrap+0x234` |
| stage90 | [experiment-265](experiment-265-the-stop-moves-inside-the-object-ipc-space-create-special.md) | **The stop moves inside the object** — `osfmk/ipc/ipc_init.o` linked, 8 resolved and **16 added** (the whole Mach IPC init surface); the stop is `ipc_space_create_special` at **`ipc_bootstrap+0x168`**, not the next line of `kernel_bootstrap` |
| stage90 | [experiment-266](experiment-266-ipc-space-create-special-completes-and-the-stop-is-mig-init.md) | **`ipc_space_create_special` completes and the stop is `mig_init`** — `osfmk/ipc/ipc_space.o`, 4 resolved and 4 added (all off the path), a true no-op in the derived layout; ninth prediction in a row, at `ipc_bootstrap+0x178` |
| stage90 | [experiment-267](experiment-267-eighteen-objects-for-mig-init-and-the-two-defects-the-step-exposed.md) | **Eighteen objects for `mig_init`** — `ipc_kobject.o` plus the 17 `out/mach_headers/kserver` MIG descriptors its `mig_e[]` table points at (a one-object link would have walked 17 zero stand-ins into XNU's real `panic`); 2 resolved, **238 added**; stop `ipc_table_init` at `ipc_bootstrap+0x180` — and the step exposed two defects: the payload outgrew its 2 MB high-VA alias window (fixed: the window is now a loop over `__stage90_image_end`) and `run_and_capture.sh` silently skipped four runs |

| stage90 | [experiment-268](experiment-268-ipc-table-init-lands-and-a-full-buffer-that-looked-like-a-missing-call.md) | **`ipc_table_init` lands** — `osfmk/ipc/ipc_table.c`, 3 resolved and **0 added** (the 258/260/263/266 shape); stop `ipc_voucher_init` at `ipc_bootstrap+0x180`, the eleventh prediction in a row; and **267's "silent hang" was not a hang** — the payload had been built before the step's object was in the entry image, so it stopped one call early and its recovery path read as silence. A trace built for the suspected hang then produced a wrong *negative* of its own: `g_kv_buf` was 2048 bytes and filled at 2038, so `entry_kv` dropped the rest in silence and `ipc_table_init`'s two `kalloc`s looked like they never ran. Both repaired (`ENTRY_KV_BUF` 8192, a reported `xnu_entry_kv_dropped`, `_size`/`_actual` around the call), and the trace then **measured** both allocations: 256 bytes each, non-NULL, 256 bytes apart, `KERN_SUCCESS` from every `kernel_memory_allocate`, and zero `vm_page_wait`/`thread_block` |

| stage90 | [experiment-269](experiment-269-ipc-voucher-init-lands-and-the-two-defects-were-in-the-reporter.md) | **`ipc_voucher_init` lands, and the two defects were in the reporter** — `osfmk/ipc/ipc_voucher.o`, 22 resolved and 4 added; stop `ipc_importance_init` at `ipc_bootstrap+0x188`, the twelfth prediction in a row — after finding that the **data-abort handler's own report path could fault and re-enter itself** (313 entries, a full 8192-byte buffer of bare keys, a three-character `why`) and that `entry_epilogue`'s `why` line **had been printing garbage** (the parameter's stack slot did not survive to the report; the string now comes from `.bss`). The first fault decodes to a read at `low16(&digit_table) + index` with a *section translation* fault — two readings that cannot both be true, because the code between them is straight-line and the same address reads fine with the mmu off. Computing the digits arithmetically removes the read entirely and takes the run to **zero aborts** |

| stage90 | [experiment-270](experiment-270-ipc-importance-init-lands-inside-a-conditional.md) | **`ipc_importance_init` lands, inside a conditional** — `osfmk/ipc/ipc_importance.o`, **72 definitions of which only 3 are names the image carries** (250's shape: what an object defines is not what the link needs), 3 resolved and 7 added; stop `semaphore_init` at `ipc_bootstrap+0x18c`, the thirteenth prediction in a row and the first inside a `#if`. The function's six calls are all real — including `ipc_register_well_known_mach_voucher_attr_manager`, which **269 made real**, so this step's completion is evidence about 269 rather than an assumption. The run needed no repair (zero aborts, `why` correct): the return on 269's two fixes. And the `task_max` stand-in shrinks its *second* zone — both `zinit` ceilings a quarter small, with the ×2 that is missing from the instruction stream being exactly the factor whose other operand is the zero stand-in |

| stage90 | [experiment-271](experiment-271-semaphore-init-lands-and-the-reporter-depends-on-machine-state.md) | **`semaphore_init` lands, and the reporter depends on the machine state** — `osfmk/kern/sync_sema.o`, 5 resolved and 3 added; stop `mk_timer_init` at `ipc_bootstrap+0x190`, the fourteenth prediction in a row. `semaphore_max` turns out to be **live arithmetic**, not a constant: `scale_setup` (`kernel_bootstrap`'s first act) computes `PORT_MAX >> 1` from `task_max` and stores it — the **fourth** consumer of the zero stand-in, and a 1.4 MB `zinit`. And the run's caller record came out `0x800:<0;4` where the value is `0x800ac0b4`: the wrong characters are exactly `0x30 + d` where the code computes `0x57 + d`, so **the true value is recoverable from the corruption** — the whole image holds one `add rN,rN,#87`, so it has no second copy of the loop, and the ELF bytes are right. Three roads to one value then said: the bias is in `g_kv_buf` itself, two calls in the same state agree, and **the same instruction at the same address is correct when run from the epilogue with the caches and mmu off**. So the measurable statement is about *fetch* — with the mechanism recorded as an open question rather than claimed |

| stage90 | [experiment-272](experiment-272-the-entry-window-s-memory-is-faithful-and-its-execution-is-not.md) | **The entry window's memory is faithful, and its execution is not** — the measurement 271 left open, taken by dumping the report path's own instruction words from the epilogue with the mmu and both caches off: **661 of 661 words match the linked ELF**, `entry_kv` through `entry_stub_hit`, 2644 bytes — and the same run carries the proof, `entry_kv` writing `800:<254` into `g_kv_buf` during the run and `800ac254` from the epilogue for the same value with the right bytes verified in memory in between, so **DRAM is ruled out and the window's fetch is what is left**. The first probe took the loop's address with GCC's `&&label` inside `entry_kv` and **produced no report at all, three runs in a row**, where the unmodified 271 image on the same device reached `stub_hit=mk_timer_init` as predicted; the symbol base replaced the label. Stopped at `mk_timer_init` again, `xnu_entry_stub_caller=0x800ac254` = `ipc_bootstrap+0x190`. And the caution: an 88-word version of the *same* probe with an **identically sized `.text`** read the small `.bss` globals as garbage (`g_why` = the linker's `__entry_text_end`, the `g_first_abort_*` block as `.rodata` string bytes) — so the image's report is not a function of its source either, and every field wants a second road |

| stage90 | [experiment-273](experiment-273-mk-timer-init-lands-and-the-frontier-leaves-its-function.md) | **`mk_timer_init` lands, and the frontier leaves its function through a tail call** — `osfmk/kern/mk_timer.o`, 2 resolved and 1 added (`mach_msg_send_from_kernel_proper`, a syscall surface), 844 → 843 undefined; the function is a `zinit(104, 425984, 1664, "mk_timer")` — the largest ceiling of any step — plus a tail-called `zone_change`, and both callees were already proved on hardware by 271 *passing* `semaphore_init`. The prediction was written offset-first because `ipc_bootstrap` ends `bl mk_timer_init` / `pop {r4, r5, fp, lr}` / `b host_notify_init`, so the stub's `lr` is `ipc_bootstrap`'s own return address and **not** anything inside it: measured `stub_hit=host_notify_init`, `xnu_entry_stub_caller=0x8000e138` = **`kernel_bootstrap+0x238`**, four roads agreeing, zero aborts — the fifteenth consecutive prediction, and the first whose answer lies in a different function from the call under test. The run also re-verified 272's dump in a different image (661/661 words) and exposed an off-by-one in the `_w0`/`_w1` probe: the digits window began at the second digit, because the offset constant counted the key as 22 characters instead of 21. Corrected, and the corrupted digits 271 saw were inside the old window either way |

| stage90 | [experiment-274](experiment-274-ipc-bootstrap-returns-and-the-stop-is-mac-policy-init.md) | **`ipc_bootstrap` returns, and the walk's next stop is `mac_policy_init`** — `osfmk/kern/host_notify.o`, 3 resolved and **0 added** (the cheapest kind of step: 13 of its 15 references were already real and the other two were already stubs), 843 → 840 undefined, image bytes **unchanged** at 1048440. `host_notify_init` is one inlined loop, five real calls and a 64 KB `zinit`, with no branch in the object, so the prediction was about where control goes when it *returns*: the tail call 273 measured means it returns **into `kernel_bootstrap`**, whose next calls are a real debug-string print and the `bl mac_policy_init` at `+0x244` — measured `stub_hit=mac_policy_init`, `xnu_entry_stub_caller_v=0x8000e148` = **`kernel_bootstrap+0x248`**, zero aborts, and a second run byte-identical in every report line. So the whole Mach IPC bootstrap ran on the hardware end to end. And the run's buffer echo is **mangled in a new way**: the eight caller digits overwrote ` stub_hit=`'s first eight characters *in place* at `g_kv_buf[0..7]` (`t=mac_policy_init` is what survives), while the offset they belong at held zeros — three roads agree, the instructions are in memory exactly as linked (661/661 words against the ELF, third build running), and which instruction's effect differs is **not claimed**: it is 271's question with a third face, the digit arithmetic right there where it was wrong before |

| stage90 | [experiment-275](experiment-275-mac-policy-init-and-a-tail-call-that-reports-its-callers-caller.md) | **`mac_policy_init`, and a tail call that reports its caller's caller** — `security/mac_base.o`, the first object this walk takes out of `osfmk/`/`bsd/` into `security/`: **3 resolved, 32 added** (31 functions — the `sbuf_*` family, the MAC label surface, the `kauth_*` credentials, `namei`/`vn_setlabel`, … — and one 4-byte storage name, `sysctl__children`), 840 → 869 undefined, image bytes 1048440 → 1049720. The built function is data setup and nine calls, and the data half says `CONFIG_EMBEDDED` is on: the list header gets a 32-byte constant, `entries` is the object's own `mac_policy_static_entries` (a `SECURITY_READ_ONLY_LATE(static …)` array, so no stand-in), 2048 bytes of `bzero`, two `LIST_INIT`s, nine real lock calls — and then `pop {r4, r5, r6, lr}` / **`b mac_labelzone_init`**. So the prediction was `stub_hit=mac_labelzone_init` with the caller at `kernel_bootstrap+0x248` — **the same value 274 measured**, because the tail call leaves `mac_policy_init`'s own return address in `lr`: `caller-4` resolves to the same `bl mac_policy_init` for both stops, and **the name is what says which function the walk reached**, the first time this walk's ambiguity is between two consecutive stops. Measured exactly, three roads agreeing, zero aborts; and this run also verifies 273's off-by-one fix on hardware (`_w0`/`_w1` read `8000e148` from the first digit) and gives an **intact** report echo where 274's was mangled twice identically — same reporter source, two builds, two outcomes |

| stage90 | [experiment-276](experiment-276-mac-labelzone-init-and-a-stop-two-functions-further-on.md) | **`mac_labelzone_init`, and a stop two functions further on** — `security/mac_label.o` is 358 bytes of text whose seven references were *all* already real: **1 resolved, 0 added**, image bytes, `.bss` and layout all unchanged. `mac_labelzone_init` is a 128 KB `zinit("MAC Labels")` and three `zone_change`s, and it ends in a tail call of its own, so the `pop {r4, lr}` it inherited from `mac_policy_init` carries `kernel_bootstrap+0x248` through and control lands back in `kernel_bootstrap`. From there `bl ipc_init` — real since 265, but never run, because 265 stopped before `ipc_bootstrap` returned — and its body's last call is a stub: measured `stub_hit=ipc_host_init`, `xnu_entry_stub_caller_v=0x800ac340` = **`ipc_init+0xe4`**, i.e. the caller key is an address **inside** the function the run was in, the opposite of the previous three steps' tail calls. Three functions completed that had never run: `mac_labelzone_init`, and `ipc_init` with **two `kmem_suballoc` calls** building `ipc_kernel_map` and `ipc_kernel_copy_map`. 661/661 words against the ELF, fifth build |

| stage90 | [experiment-277](experiment-277-ipc-host-init-and-the-largest-stub-retirement-this-walk-has-made.md) | **`ipc_host_init`, and the largest stub retirement this walk has made** — `osfmk/kern/ipc_host.o` is 3668 bytes of text with 21 functions defined and 26 names referenced, and it takes **16 resolved, 8 added**: 868 → 860 undefined, 792 → 784 function stubs, storage unchanged at 76, text 949860 → 953220, image bytes 1049720 → 1066104. The 16 retired are the host-port surface the image has been stubbing for dozens of steps (`convert_port_to_host*`, `convert_pset_*`, `host_get/set/swap_exception_ports`, `ipc_processor_*`, `processor_set_default`, `ipc_host_init`); the 8 added are all functions defined by objects already compiled (`kernel_set_special_port`, `ipc_port_copyout_send`, five `mac_exc_*`, `mac_task_check_set_host_exception_ports`). Five of its 21 functions are referenced by nothing, so they are neither real nor stubs — absent. **Prediction: `stub_hit=ipc_port_alloc_special` at `ipc_host_init+0x30`** — the walk is standing *in* the function now, so the step runs it: `lck_mtx_init(&realhost.lock, …)` (real, and the first time real code **writes** a 404-byte storage stand-in, whose last store at offset 400 ends exactly at its last byte), then `ldr r0, [ipc_space_kernel]`, then `bl ipc_port_alloc_special`, which is what `ipc_port_alloc_kernel()` expands to. Measured exactly, three roads agreeing, zero aborts. 661/661 words against the ELF, sixth build — *after* a correction on this side of the wire: the first check compared the dump's printed word *values* against the image's little-endian bytes and read 660 of 661 mismatched, and comparing number to number reads 0. And 275/276's `manifest:549` for this file is one line late; it is 550 |

| stage90 | [experiment-278](experiment-278-ipc-port-alloc-special-and-the-first-allocation-this-kernel-has-made.md) | **`ipc_port_alloc_special`, and the first allocation this kernel has ever made** — `osfmk/ipc/ipc_port.o` is 8924 bytes of text, 48 definitions and 53 references, and it takes **20 resolved, 19 added** (bigger than 277 on both sides): 18 functions plus **two storage stand-ins** (`ipc_port_multiple_lock_data` 8 bytes, `ipc_port_timestamp_data` 4) retired, and 19 functions obliged, all defined by objects already compiled — including the first names this walk has pulled out of `osfmk/kern/task_policy.o` and `bsd/`'s `kern_event.c`, which sit on the sync-QoS path and are not reached. 860 → 859 undefined, 784 → 785 function stubs, storage 76 → 74, text 953220 → 962244, image bytes **unchanged** at 1066104. **Prediction: `stub_hit=ipc_mqueue_init` at `ipc_port_alloc_special+0x94`** — the allocation path is entirely real (`zalloc`, `__bzero` 128, `lck_spin_init` with the real `ipc_lck_grp`/`ipc_lck_attr`) until its last call, and `ipc_port_init` is inlined rather than called. Measured exactly, four roads agreeing, zero aborts, 661/661 against the ELF (seventh build). **So a real 128-byte IPC port was allocated on the device** — out of the `"ipc ports"` zone `ipc_bootstrap` created, zeroed, spin-locked, given `io_bits 0x80000000` and `ip_references 1`. The `zalloc`-succeeds argument was a measurement and not a hope: `ipc_space_create_special` calls `zalloc` from inside `ipc_bootstrap`, and 274 is the run in which `ipc_bootstrap` returned. This is the first time the walk has executed an *allocation* path rather than an initialization one |

| stage90 | [experiment-279](experiment-279-ipc-mqueue-init-and-a-stop-that-reports-278s-caller-key.md) | **`ipc_mqueue_init`, and a stop that reports the caller key 278 already reported** — `osfmk/ipc/ipc_mqueue.o` is 5436 bytes of text with 47 definitions and 46 references, and it takes **5 resolved, 9 added**, where the five retired are *exactly* the five names 278 obliged (`ipc_mqueue_init`, `_deinit`, `_changed`, `_destroy_locked`, `_override_send`) — the previous step's own additions coming good one experiment later. The nine added are all functions: seven out of `osfmk/ipc/ipc_kmsg.c` (the first names this walk obliges out of it), one more out of `bsd/kern/kern_event.c`, one out of `osfmk/ipc/mach_msg.c`. 859 → 863 undefined, 785 → 789 function stubs, text 962244 → 968100, image bytes 1066104 → 1082488. **Prediction: `stub_hit=klist_init` at `ipc_port_alloc_special+0x94` — the same value 278 measured**, because the last instruction of `ipc_mqueue_init` is a `b` and the stub therefore sees the `lr` its own `pop` restored rather than the address of the tail call. Measured exactly: `_v`/`_a`/`_e` all `0x800ba300`, `caller-4` the same `bl ipc_mqueue_init` 278's stop resolved to — so `caller-4` alone no longer says which function's body the run is in, and the stub's name is the only discriminator between two consecutive stops. 275's shape, for the second time in the walk. What ran: a real `waitq_init(port+16, SYNC_POLICY_FIFO)` from a *port allocation* rather than from the walk's own bootstrap. 661/661 against the ELF, eighth build |

| stage90 | [experiment-280](experiment-280-klist-init-and-a-step-that-never-finishes.md) | **`klist_init`, and a step that is built, predicted and never finishes** — `bsd/kern/kern_event.o`, the largest step this walk has taken by a wide margin (43084 bytes of text, 229 definitions, 165 references), and it takes **10 resolved, 69 added**: 68 names generated (48 function stubs + 20 storage stand-ins) plus one hand-written, `bpfread_filtops`, which has **no definition anywhere in the pool** because `bsd/net/bpf.c` is `optional bpfilter` and this configuration does not select it — the first concrete instance of the ~126 `optional` sources the manifest lists and the build omits. 863 → 921 undefined, 789 → 827 function stubs, storage 74 → 94, text 968100 → **1013620**, image bytes 1082488 → 1115688, `.bss` `0x8010fb50` .. `0x80145c88`, headroom 1845944 → 1811320. The prediction was `stub_hit=kernel_set_special_port` at `ipc_host_init+0x78`, and every link of that chain verifies in the linked image (`800b75c4 bl 800dd29c <kernel_set_special_port>` at `+0x74`; the stub 24 bytes; `klist_init` real, 12 bytes, at `0x800c0788`). **The run measured neither half: it stopped nowhere.** The payload's log is structurally identical to the 279 control's — 3918 lines, zero differences after normalisation — and the jump is taken; after it the entry image writes **not one byte**, `stub_hit` count 0, no `exception:` line, none of the epilogue's `csselr`/`ccsidr`/`kv_words` records, across **three runs**. The 279 configuration rebuilt from the same ledger in the same session reached `stub_hit=klist_init` at `0x800ba300` exactly, so the image is the only variable. **The new instrument — `STAGE90_ENTRY_CHECKPOINT=<symbol>` + `entry_checkpoint.c`, `--wrap`ping one named symbol into a terminal stop of the same shape a missing symbol produces — returned its own negative**: a checkpoint at `klist_init`, the very point 279 reported from, was also silent, which its design reads as *the reporting path, not the frontier*. Host-side rule-outs all clean (the early text instruction-identical after `movw`/`movt` resolution; every callee real; all 94 stand-ins sized from a pool definition; no size-dependent gate before the first report), and the layout now **measured** rather than argued: 2726 symbols address-identical below `0x800bb000`; `.data` `+0x8000` by the 16 KB snap with `intstack`/`fiqstack` riding at `.data + 0x8000` in both; `.bss` `+0x81c0`; the only negative deltas are two absolute linker *values*, not addresses. Safety held: `fastboot boot` only, nothing flashed, `persistent_write_attempted=0x00000000` ×25, `failure_mask=0x00000000` ×87, zero aborts, and the device came back to Android — with the honesty gap that the checkpoint run's log was overwritten before its counters were read. And a **correction owed to 279**, whose committed layout paragraph says "only 101 symbols moved" where the true figure is **2959** |

| stage90 | [experiment-281](experiment-281-the-pad-and-the-first-silence-that-was-the-instruments.md) | **the pad, and the first silence that turned out to be the instrument's** — the bisect 280's silence forced, and its first product: XNU's own `bcopy_phys` calls at `cpu.c:570-580` write `BootArgs_paddr` and `CpuDataEntries_paddr` to `gPhysBase + (&ResetHandlerData.boot_args - &ExceptionLowVectorsBase)`, which Apple's own link resolves into the vectors blob at the image base and *this* link resolves to **0x80002408 and 0x80002404 — inside `entry_epilogue` at `0x80002348`**, the function that writes every log line this walk has read. The two instructions destroyed were `way_shift = 32u - n` and `way = 0`; the values written are addresses, which decode as register-form `ANDS` with `cond=HI` that set neither register, so the way loop counts from whatever `lr` held and the epilogue never reaches its ram-console write — **a hang inside the reporting path looks exactly like a hang in the boot**. The fix is on this side: a NOP pad placed *first* in the sweep block, wider than the eight bytes at risk. Also the `AFTER` variant of the checkpoint instrument (`STAGE90_ENTRY_CHECKPOINT_AFTER=1`: call the real function, record `cp_ret`, report after it — so `cp_ret=<v>` means it returned and a silence means it did not), which composes with `SKIP` and whose one limitation is measured, not argued: the wrapper's frame sits where a callee reads its *stack* arguments, so `bcopy_phys` is not an `AFTER` candidate while `bcopy` is. Runs: `pmap_map_cpu_windows_copy` silent (one call site, not reached); `mmu_kvtop_wpreflight` reports `0x80002408`/`0x80000000`/`0x80800000`; the same with `AFTER` reports `cp_ret=0x80002408` — the preflight returns; `bcopy SKIP=2 AFTER` reports `cp_calls=3` with args `0x80300000`/`0x80304000`/`0x4000` and caller `arm_vm_init+0xb0`, **refuting the call census while naming itself through its arguments**. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, zero aborts |

| stage90 | [experiment-282](experiment-282-r7-and-the-four-reports-that-cleared-the-instrument.md) | **`r7`, and the four reports that showed the silence was the instrument's** — the pad was only half a fix, and the missing half was the report path *itself*. Three versions: no pad (lost `way_shift`/`way`, so the sweep hangs); **a pad of NOPs** ("a NOP overwritten by an `ands` is still a NOP" — false whenever something *executes* it, which the report always does); a run-time repair of the NOPs with `dsb`/`isb`, built, verified in the linked image and **still silent** because the store lands in the D-cache while the fetch of the pad's line may already have been prefetched — an instrument whose correctness rests on an unmeasurable cache property is not an instrument. The weapon, measured in the linked disassembly: `entry_epilogue` loads **`r7` from `[entry_vectors_stack + 4]` at `0x80002358` and does not touch it again until `0x80002574`**, so `0x80147000` decoding as `andshi r7, r4, r0` (register-form `ANDS`, `cond=HI`, so it writes `r7` exactly when C is set and Z clear) destroys a live value whose later uses are a kv word, half a pointer and a byte-table base. The fix is structural: the pad now opens with **`b 1f` over 31 NOPs and is never executed**, so XNU's writes land on NOPs nothing fetches as instructions, whatever the caches and flags do; `build_entry.sh` checks that *structure* against two linked labels (`entry_skip_pad` branches to `entry_skip_pad_end` over ≥128 bytes, itself ≥4 bytes below `0x80002404`, with both written addresses strictly inside what it skips). Then three reports: `CleanPoC_DcacheRegion` (caller `0x80004310`, args `0x80000000`/`0x00001000`) proves **both `bcopy_phys` calls returned** — so 282m's silence was never "the stop is inside `bcopy_phys`"; `machine_startup` (caller `0x8000334c`, `cp_arg0=0x80147000`) proves the whole of `arm_init`'s ladder ran; and a plain run — the walk's normal mode, unavailable for four experiments — reports `stub_hit=kernel_set_special_port`, the symbol the 280 era predicted from reading `ipc_host.c`. The walk bisected a four-byte `memmove` that returns, with an instrument that was the thing that was wrong: **this project's "a measurement can be the thing that is wrong" defect for the eighth time**. 25/87 clean on every run |

| stage90 | [experiment-283](experiment-283-osfmk-kern-host-o-and-the-three-special-port-writes.md) | **`osfmk_kern_host.o`, three `kernel_set_special_port` calls, and a payload defect the step's own growth triggered** — `osfmk/kern/host.c`, **4920 bytes of text, 26 definitions, 60 references**, **19 resolved** (eighteen function stubs plus **`realhost`**, a 0x194-byte storage stand-in whose real definition this object carries — the size 277 measured against this same object, confirmed to the byte at `0x80148240`) and **7 added**, totals predicted to the unit (921 → 909 undefined). The step cannot stop short, measured on the host by listing every `bl` and checking it against the image's undefined list: `kernel_set_special_port` is the **only** stub in `ipc_host_init`'s body (a three-fold `ipc_port_alloc_special`/`ipc_kobject_set`/`ipc_port_make_send`/`kernel_set_special_port` repetition, the one stopped at being `HOST_SECURITY_PORT`), `ipc_host_init` is the **last** call in `ipc_init`, and `kernel_bootstrap` has **0 of 46** stub calls — so the next frontier is *transitive*, and the name had to come from the run. **Run one was refused before it booted**: `loader_xnu_entry_stub_failure_mask=0x00008910` with the only new line `high_va_data_verified=0x00000000` (against `0x00000001` everywhere before), `kernel_entry bad: Mach-O/XNU loader preflight`, `platform_reboot`, device back on its own — the safety gate working. Two defects in `stages/stage90/xnu_arm_vm_init_full_pmap.c`, both the class its own Phase 2 comment already records: Phase 1's low identity map was a hardcoded **pair of sections** for PA [0, 2 MB) while the probe word moved from `0x001fc0b4` to `0x002000b4` (now a loop over the image, as Phase 5 already is); and Phase 3's RAM direct map, written as **sections** after Phase 2's page-mapped window, **claimed the same L1 entries**, sending the high-VA read to PA `0x802000b4` (now skips the slots the image's window covers). `HIGH_VA_DATA` fires for either half and named the wrong one. **Run two: `stub_hit=clock_config` at `machine_init+0xc`** — the first of the twelve functions `kernel_bootstrap` calls after `ipc_init`, with `ipc_host_init` and `ipc_init` complete and `kernel_bootstrap` stub-free. Text 1014960 → 1019088, image 1115688 → 1132120, headroom 1811320 → 1794616. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000` on both runs |

| stage90 | [experiment-302](experiment-302-commpage-o-and-the-walk-arrives-at-the-context-switch.md) | **the commpage, and the walk arrives at the context switch itself** — `osfmk/arm/commpage/commpage.c` (manifest:433), `.text` 0x5B0 / `.bss` 0xC / `.rodata.str1.1` 0x13, **10 resolved** (ten functions, **no storage at all**) and **0 added** (all 18 references - 21 minus three versuffix duplicates - are symbols the image carries, and **none is a stub**). Five definitions are not stubs and have no reference yet, of which **`commpage_populate` is the one worth naming**: it installs the shared page and its callers are in `arm_vm_init`/`pmap`, untouched by this walk - so the step links the commpage's *updaters* while nothing has created the commpage, and that is safe in exactly the way the stopping function shows, its first four instructions being a `commPagePtr` guard whose storage is a 4-byte `.bss` slot **in the same object**: `commpage_update_active_cpus` **ran and published nothing**, and the value it would have published (`processor_avail_count`, `B 0x4` at 0x80136f80, a real linked variable) was 1 by then. **The first step whose next stop is not in the linked object at all**, so the prediction is a reading of `load_context` and had to close **two reachable stubs the path does not take**: `splx(s)` → `ml_set_interrupts_enabled` → `ast_taken_kernel` (0x80016fd8), closed by the **boot order** rather than assumed - `kernel_bootstrap` (`startup.c:252-411`), which calls `load_context`, contains **no interrupt-enabling statement at all**, while `assert(ml_get_interrupts_enabled() == FALSE)` (548) and `(void) spllo()` (560) are both inside `kernel_bootstrap_thread` (413-660), on the far side of the context switch - so `splx(0)` takes the `cpsid if` arm; and `stack_alloc_try`, closed by `kernel_thread_create`'s `stack_alloc(thread)` + `assert(thread->kernel_stack != 0)` (`thread.c:1644`), which had already run in 300. **`stub_hit=machine_load_context` at `xnu_entry_stub_caller=0x8000e804`** — a key set by `mov lr, pc` at 0x8000e7fc (ARM `pc` reads as instruction + 8), not by a `bl`. All three counts exact (745/658/87), `__bss_start` and `__bss_end` 0x80167798 unchanged for the **third step running** (the object's 0xC of `.bss` lands inside the existing 0x38 `*fill*` before `realstubs.o`'s 64-aligned arrays), image unchanged at 1247700. **`.text` 0x1176C0 → 0x117AA0, predicted 0x40 low**: the four named terms are each exact in the map (+0x5B0 object `.text`, **+0x13 its `.rodata.str1.1`, not relaxed this time**, −0xF0 ten retired stub bodies, −0x148 their name strings, measured one at a time out of the stub object) and sum to +0x38B against a section delta of +0x3E0, so **the aligned-fill term is +0x55** where the band allowed 0..0x1F. 301 measured the same term as +0x12. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, no `exception:` line, device back on its own (release 10, cancro) |

| stage90 | [experiment-303](experiment-303-machine-load-context-and-the-first-thread-runs.md) | **`machine_load_context`, and the first thread runs** — `osfmk/arm/cswitch.s` (not in the manifest, required by `genassym.c`), `.text` 0x9E0 (five functions, no storage, no data). **6 resolved / 0 added**: the object defines six global function stubs (`machine_load_context`, `Call_continuation`, `Switch_context`, `Shutdown_context`, `Idle_context`, `Idle_load_context`; each referenced by exactly one linked object — `startup.o`, `pcb.o`, `pcb.o`, `machine_routines.o`, `machine_routines_asm.o`, `cpu.o` — and the only other globals it defines, `vfp_save`/`vfp_load`, are referenced by nothing yet) and **all eleven of its references were already accounted for** — ten are real symbols of this image and `thread_terminate` was already a stub, referenced by nine linked objects (`thread.o`, `startup.o`, `thread_call.o`, `kern_exit.o`, …). Retiring the six stubs deletes six 24-byte bodies and six name strings from `realstubs.o`, so the counts move by exactly six: 745 → **739** undefined, 658 → **652** function stubs, 87 storage (unchanged). **`stub_hit=bsd_setthreadname` at `xnu_entry_stub_caller=0x800a2f4c`** = `idle_thread_create + 0x5c` — **the context switch worked**. `load_context` tail-called `machine_load_context`, which restored a thread's saved state (r4–r11, sp, lr), switched `TPIDRPRW`, and returned into the restored frame. The stop is twelve instructions past `kernel_thread_create`'s return in `idle_thread_create`'s body (after `snprintf`, inside `thread_set_thread_name`), proving the scheduler selected a thread, restored it, and it reached its first stub. Thread creation (300), context switching, and scheduler selection all work. All three counts and all three layout lines exact (third consecutive `__bss_end` exact), image unchanged at 1247700. `.text` predicted 0x20 low (alignment across five inputs). 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, device back on its own (release 10, cancro). **Correction (2026-09-19):** this row first claimed **6 resolved / 1 added** with an added name `commpage_update_user_timebase`, and reported the measured counts as 739/641/59 with an explanation built on `xnu_entry_stub_target_*` storage slots. None of that survives re-measurement — `commpage_update_user_timebase` is not referenced by `cswitch.o` and does not exist in this tree, and no artifact of the build produces 641 or 59 (the build prints 739 / 652 / 87; `nm` on `realstubs.o` gives 652 `T` + 87 `B`; the image holds 652 stub functions and zero `xnu_entry_stub_target_*` names). The prediction was exact and the measurement was the wrong thing — the fortieth entry in the measurement-defect list. **Next:** no longer an object frontier; link the object defining `bsd_setthreadname` — which is `bsd/kern/proc_info.c`, **not** `kern_prot.c` — and let the device tell us where the restored thread goes next |
| stage90 | [experiment-304](experiment-304-thread-start-and-the-first-thread-runs-a-whole-initialisation-call.md) | **`thread_start`, and the object is `proc_info.c` rather than `kern_prot.c`** — `bsd/kern/proc_info.c` (manifest:73, `bsd_kern_proc_info.o`), `.text` 0x3AD8 / `__DATA, __data` 0xD8 / `.rodata.str1.1` 0x29 / `.rodata` 0x48, 74 definitions and 112 references. **9 resolved / 37 added.** Resolved, all functions: `bsd_setthreadname` (the stop, referenced by `thread.o`), `bsd_copythreadname`/`bsd_getthreadname`/`bsd_hasthreadname`/`bsd_threadcdir`, the three `get_*_offset_from_proc` ABI queries and `proc_pidpathinfo_internal` — each referenced by exactly one or two linked objects (`thread.o`, `task.o`, `bsd_kern.o`, `task_policy.o`). Added, **35 functions and 2 storage** (`dead_mountp` B 4, `zombproc` B 4, sized from their defining objects): the BSD surface `proc_info.c` is built out of — vnode, `vfs_context_*`, `kauth`/`suser`/`cansignal`, the `proc_*` queries, `fileport_*`/`fp_*`, the eight `fill_*` helpers — **none of it on this step's path**, which is the 251 shape. 739 → **767** undefined, 652 → **678** function, 87 → **89** storage. **The build was predicted to the unit**: the step was first built with an empty stand-in object, which reproduces 303 exactly (739/652/87, `.text` 0x117BE0, image 1247700), so the deltas are this object's alone — and all six numbers landed: `.text` **0x11BB00**, `.data` **0x8011C000** (a 16 KB step, because `.data` is 16 KB aligned and `.text` crossed 0x118000), `__bss_start` **0x80134AC0**, `__bss_end` **0x8016B8D8**, image **1264300**. The `.text` delta 0x3F20 = object 0x3B49 + stub-set +0x3A3 (**0x3EEC**) + **a 0x34 fill term, predicted rather than back-solved** — the first step where both sides of the fill are known before the link. **`stub_hit=thread_start` at `xnu_entry_stub_caller=0x8000a0a0`** = `kernel_thread_start_priority + 0x3c`, the `bl` at 0x8000A09C — predicted address and offset, and **the caller-key idiom is back** after 303's excursion. What ran between the two stops: `bsd_setthreadname` in full (all six callees real; `ut->pth_name` NULL so the kalloc arm was taken), `idle_thread_create`'s tail, `kernel_bootstrap_thread`'s log, `sched_startup`, and `kernel_thread_start_priority`. **The finding: `thread_deallocate` returns on its ref-count check** — threads are created with `thread_template.ref_count = 2` (`thread.c:244`) and its first act after the null test is `cmp r1,#1 / popne {r4,r5,fp,pc}`, so `panic("thread_deallocate: thread not properly terminated")` (state `TH_RUN|TH_IDLE` = 0x84 lacks `TH_TERMINATE2` 0x20) and the four functions behind it are never reached. Falsifiers closed by measurement: `kpc_counterbuf_free` (kpc buffer NULL, 299), `ast_taken_kernel` (IRQs disabled two instructions earlier), `trace_backtrace`/`btlog_*` in `zfree` (flag zero, as 243–247 measured), and `kalloc_canblock`'s closure. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, no `exception:` line, device back on its own (release 10). **This step also corrects 303's build record** — see that row. **Next:** `osfmk/kern/thread_act.c` (manifest:597), which resolves 25 names including `thread_start` and `thread_terminate`, and adds 5 |
| stage90 | [experiment-305](experiment-305-thread-act-and-the-stop-one-object-internal-hop-later.md) | **`thread_act.c`, and the prediction is wrong by one call inside one object** — `osfmk/kern/thread_act.c` (manifest:597, `osfmk_kern_thread_act.o`), `.text` 0x1288 / `.rodata.str1.1` 0x34, no `.data`/`.bss`, 38 definitions and 38 references. **25 resolved / 5 added**: the thread-state API this image has carried as stubs since `thread.o` arrived — `thread_start` (304's stop), `thread_start_in_assert_wait`, `thread_terminate` (referenced by nine linked objects, and the name 303's own "next" called its falsifier), `thread_terminate_internal`, the abort/hold/release/suspend/resume/dup/state family, `thread_apc_ast`, `set_astledger` and the nine `act_*` AST setters — and **5 added, all functions, no storage** (`nm -S` over the pool reports `T` for each: `extmod_statistics_incr_thread_set_state`, `thread_affinity_dup`/`_terminate`, `thread_depress_abort_internal`, `thread_exception_return`). 767 → **747** undefined, 678 → **658** function, 89 storage. **Every build number landed, and the baseline was measured rather than recalled** (an empty stand-in object reproduces 304 exactly — 767/678/89, `.text` 0x11BB00, image 1264300): `.text` **0x11CAE0** (predicted 0x11CABC + fill, band 0x11CAC0..0x11CB00 — **fill 0x24**, against 0x12/0x55/0x34 in 301/302/304), `.data` **0x80120000** (the 16 KB step again, because `.text` crossed 0x11C000), `__bss_start` **0x80138AC0**, `__bss_end` **0x8016F8D8**, image **1281452 = 0x138AAC**, headroom 1640232. **`stub_hit=sfi_thread_classify` at `xnu_entry_stub_caller=0x8009dc90` = `thread_setrun+0x3c`** — *not* the predicted `device_service_create` (0x8000e59c) and *not* the predicted falsifier `compute_averages`. The prediction's **shape** held (the bootstrap thread stayed in `sched_startup`: `thread_block` does not put a `TH_RUN`-only thread off the processor, so `sched_init_thread` never ran its body) and **the walk that said "no stop in `clear_wait`'s closure" was blind**: `entry_frontier.py` followed only `bl 0 <name>` in `objdump -dr`, the form a call to another object's symbol takes, while a call to a symbol the *same* object defines prints as `bl c24 <thread_setrun>` — so **every intra-object edge was missing from the graph**. The chain the device took is all intra-object after `clear_wait`: `clear_wait -> clear_wait_internal -> thread_go (inlined) -> thread_setrun -> sfi_thread_classify`, four of the five in `sched_prim.o`; `sched_startup -> kernel_thread_start_priority` was visible in 304 only because those two functions live in different objects, which is why 304 was exact and 305 was not. **The tool is fixed in this step** (any target address followed, local labels `foo+0x10`/`.LBB1_2` skipped; the same walk then reports the measured stop, with 371 stops instead of 133 because the panic paths come in too — the run is still the measurement). What the run measures: **`thread_start`'s body ran** — `clear_wait` → `clear_wait_internal` → `thread_go` → `thread_setrun` is what wakes a started thread and puts it on a run queue, and `thread_setrun`'s first act is `thread->sfi_class = sfi_thread_classify(thread)` — so a thread created by `kernel_thread_start_priority` now reaches the run queue. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, no `exception:` line, log 301120 bytes, device back on its own (release 10). **Next:** `osfmk/kern/sfi.c` (manifest:583) — 2 resolved (`sfi_thread_classify`, `sfi_reevaluate`), 0 added, **0x54 bytes of `.text`** because `CONFIG_SCHED_SFI` is 0 and only sfi.c's `#else` arm compiles; the prediction is `device_service_create` again, with `ast_on` (0x8009E5D0/0x8009E618, needs `preempt != AST_NONE`) and `PE_cpu_signal_deferred` (behind `machine_signal_idle` at 0x8009E6D4, needs an idle processor) as the named alternatives **Correction (2026-09-19):** two build numbers in this row do not survive re-measurement. `.text` is **0x11CAA0**, not 0x11CAE0 — rebuilding 305's own stub set (747/658/89) measures 0x11CAA0, and 306's configuration differs from it by exactly +0x54 − 0x30 − 0x24 = 0, so the two cannot have differed. The image figure's decimal is also wrong: 0x138AAC is **1280684**, which is what the device's own `xnu_entry_copied_bytes=0x00138aac` and the build report both say; 1281452 is 0x138C2C. Both are the same defect class as 303's — a number recorded as a measurement instead of re-derived in the session that compares it. |
| stage90 | [experiment-306](experiment-306-sfi-and-the-stop-is-inside-sched-startup.md) | **`sfi.c`, and the stop is inside `sched_startup`'s own body** — `osfmk/kern/sfi.c` (manifest:583, `osfmk_kern_sfi.o`), **84 bytes of `.text` and nothing else** (no `.data`, no `.bss`, no `.rodata`), 8 definitions and 1 reference, because `CONFIG_SCHED_SFI` is 0 and only the `#else` arm compiles. **2 resolved / 0 added** (`sfi_thread_classify`, this stop, and `sfi_reevaluate`; its one undefined name is `kernel_task`, which the linked `task.o` defines) → 747 → **745** undefined, 658 → **656** function, 89 storage. The build was measured against a baseline built in the same session (an empty stand-in object reproduces 305's stub set: 747/658/89, `.text` 0x11CAA0): `.text` **0x11CAA0** — **unchanged**, because +0x54 − 2 stub bodies (0x30) − 2 stub name strings (0x24) is exactly zero — `.data` **0x80120000**, `__bss_start` **0x80138AC0**, `__bss_end` **0x8016F8D8**, image **0x138AAC**, headroom 1640232, and the device's own `xnu_entry_bss_start`/`_end`/`copied_bytes` are those three numbers exactly. **`stub_hit=ast_off` at `xnu_entry_stub_caller=0x800a04c4` = `thread_block_reason+0x44`** — the `bl ast_off` at 0x800a04c0 with `r0 = 31` (`AST_SCHEDULING`), the fifth statement of `thread_block_reason` and the first thing `sched_startup`'s `thread_block(THREAD_CONTINUE_NULL)` does. **Both predictions falsified** (306's `ast_on` at 0x8009e61c and 305's `device_service_create`): the next stop is neither, it is *inside `sched_startup`'s own body*, three statements after the thread was put on a run queue. What the run **measures**: `thread_setrun` **returned** — through `clear_wait_internal`, `clear_wait`, `thread_start`, `kernel_thread_start_priority` and `thread_deallocate` — so the first thread this kernel starts is **on a run queue** (the enqueue is unconditional on the non-RT arm, `sched_pri` 95 < `BASEPRI_RTQUEUES` 97) and **the enqueue did not preempt the bootstrap thread**: `ast_on` was never called. What it does **not** measure is which gate closed — `preempt` is `AST_PREEMPT` for certain (`mov r1, #5` at both `thread_setrun` call sites, `TH_MODE_FIXED` so the timeshare arm is skipped, and the net `ubfx r2, r6, #2, #1` at 0x8009E044) — so the survivors are (a) `processor->state` 2 or 3, whose jump-table arm exits with no AST, (b) state `PROCESSOR_DISPATCHING` (5), whose arm returns without an AST either way, and (c) `csw_check_locked` returning `AST_NONE`, which needs `MAX(main_entryq->highq, bound_runq->highq) < processor->current_pri` **or** `first_timeslice == TRUE` (that flips `>=` to `>` in `sched_multiq_processor_csw_check`, and 95 > 95 is false). Those four variables are not printed: **the run measures the branch and not the variable**, and settling it needs a checkpoint probe, not another object. **Why both readings missed it:** the chain's last hops are intra-object (`sched_startup -> thread_block -> thread_block_reason` are all `sched_prim.o`) — 305's blind spot, now fixed — but the *fixed* walker still does not report `ast_off` as the frontier, because its visited set is global and first-visit-wins and a chain through `panic -> panic_trap_to_debugger -> kdbg_dump_trace_to_file -> ... -> thread_block_reason` claims `thread_block_reason` first: `ast_off` lands 40th in the list and `device_service_create` 313th (as a *direct* call of `kernel_bootstrap_thread`). Pruning the panic subtree is not enough (567 stops → 243, `ast_off` 9th, and the list is then led by `ast_taken_kernel`, `ast_on`, `PE_cpu_signal_deferred` — branches the tool's docstring says it walks without modelling), so the tool is unchanged and the lesson is the docstring's own line: the answer is a candidate and the run decides — **the tool chose the object, the run named the call, and the hand-reading that replaced the tool's ordering with a derivation from `preempt`/`has_higher` is what was wrong**. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, no `exception:` line, log 301108 bytes, device back on its own (release 10). **Next:** `osfmk/kern/ast.c` (manifest:534, `osfmk_kern_ast.o`), which defines `ast_off` — `.text` 0x440, 11 definitions, 22 references, **6 resolved** (`ast_check`, `ast_context`, `ast_off`, `ast_on`, `ast_propagate`, `ast_taken_kernel`) and **2 added** (`bsd_ast`, `kperf_kpc_thread_ast`) → 743 undefined / 654 function stubs **Correction (2026-09-19):** that last sentence is wrong, and 307 is the step that paid for it: **745 − 6 + 2 is 741**, not 743, and **656 − 6 + 2 is 652**, not 654 — which is what linking `osfmk_kern_ast.o` actually measures. The slip was copied forward into 307's own first ledger block and corrected there; it is the second time in this walk that a number was carried into the next step instead of being recomputed from it (the first is 305's `.text` 0x11CAE0). |
| stage90 | [experiment-307](experiment-307-ast-and-the-first-call-thread-invoke-makes.md) | **`ast.c`, and the candidate is the first call `thread_invoke` makes** — `osfmk/kern/ast.c` (manifest:534, `osfmk_kern_ast.o`), **0x440 bytes of `.text` and nothing else** (no `.data`, no `.bss`, no `.rodata` section of its own), 11 definitions and 22 references. **6 resolved / 2 added**: `ast_check`, `ast_context`, `ast_off` (306's stop), `ast_on`, `ast_propagate`, `ast_taken_kernel`; added `bsd_ast` and `kperf_kpc_thread_ast`, both `R_ARM_CALL` so both function stubs → 745 → **741** undefined, 656 → **652** function, 89 storage. Four of its definitions (`ast_consume`, `ast_dtrace_on`, `ast_peek`, `ast_taken_user`) and its local `thread_preempted` are referenced by nothing yet, so 7 of its 11 functions link unreferenced. **The step's prediction was written as a candidate list, from the walker rather than from a reading** — 306's closing paragraph asked for exactly that, one step after the hand-reading it replaced was refuted. The build was measured against a baseline built in this session (an empty stand-in object reproduces 306 exactly: 745/656/89, `.text` 0x11CAA0, image 0x138AAC, bss 0x80138AC0..0x8016F8D8, headroom 1640232): `.text` **0x11CE40** (**+0x3A0 measured against a +0x3B4 component sum** — 0x440 for the object, −0x90 for the 6 stub bodies retired, −0x4C for their 6 name strings, +0x30 and +0x20 for the 2 stubs created, and the **0x14 between the two is the fill term**, because the stub object's string section shrank 0x2C and the ~160 objects whose `.rodata.str1.1` follows it inside the `.text` output section shift down with it), `.data` **0x80120000**, `__bss_start` **0x80138AC0**, `__bss_end` **0x8016F8D8**, image **0x138AAC**, headroom **1640232** — all as predicted, and the string arithmetic was checked against the objects (the 652 `T` symbols of `realstubs.o` sum to 0x32E0, which is that object's own `.rodata.str1.4`; the reconstructed 656-name set sums to 0x330C, which is the 306 map's figure). **`stub_hit=mt_sched_update` at `xnu_entry_stub_caller=0x800a0c14` = `thread_invoke+0x74`** — the `bl` at 0x800a0c10, the **first** call `thread_invoke` makes (`#if MONOTONIC mt_sched_update(self); #endif`), and the first candidate in this walk that came from the walker's own guarded-site list. What the run **measures**: `thread_select` returned (the stop is one call past it), so the enqueue 305 and 306 watched go by did produce a dispatchable thread, and the inlined `sched_timeshare_consider_maintenance` in front of the stop either declined on a deadline that had not passed or ran its `thread_wakeup_prim` tail without stopping. What it does **not** say is *which* thread is about to be switched to — `thread_select` has an `idle` arm reachable when `!processor->is_recommended`, and that is a data question for the next step. **This step also adds `tools/stub_calls_in_function.py`**, because the by-hand scan it replaces had a wrong predicate: it called any `bl` target at or above the stub text's start a stub, and the linker keeps placing **real** code above that boundary (in the 306 image the stub text runs 0x80101690..0x80105410 and libgcc's `__aeabi_ldivmod` sits at 0x80105410). Stub-ness is now decided **by name, against the stub object's own symbol table**, and every report carries the function's indirect-call count — `thread_select` has **30** of those and no direct stub call, which is a different statement from "cannot stop". 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, no `exception:` line, log 301116 bytes, device back on its own (release 10). **Next:** `osfmk/kern/kern_monotonic.c` (manifest:558, `osfmk_kern_kern_monotonic.o`), `.text` 2900 / `.bss` 24, 23 definitions and 12 references, **7 resolved / 2 added** → 736 undefined / 647 function stubs — **predicted to stop *outside* the object it links**: `mt_sched_update` is `if (!mt_update_thread(thread)) return;` and `mt_update_thread` opens with `if (!mt_core_supported) return false;`, where `mt_core_supported` is a never-initialized storage stand-in (`8016ef80 B`) that reads 0, so the new code returns at its first statement and the candidates are the first dispatch of the thread `sched_startup` created — `sched_init_thread` (call-free) running `sched_timeshare_maintenance_continue`, which reaches **`compute_averages` at caller 0x800A3178**, the name 305 named as a falsifier, arriving three steps later by a different road — with the idle arm of `thread_select` as the alternative |
| stage90 | [experiment-308](experiment-308-kern-monotonic-and-the-run-ends-on-an-interrupt.md) | **`kern_monotonic.c`, and the run ends on an interrupt** — `osfmk/kern/kern_monotonic.c` (manifest:558, `osfmk_kern_kern_monotonic.o`), `.text` **2900 bytes** / `.bss` **24**, 23 definitions and 12 references, **7 resolved / 2 added** (`mt_core_snap`, `mt_cur_cpu`, both `R_ARM_CALL`) → 741 → **736** undefined, 652 → **647** function, 89 storage **unchanged** — the storage count does not move because the object's one storage reference, `mt_core_supported` (`8016ef80 B`, one of the 89), is a name it uses and does not define. **The step was predicted to stop *outside* the object it links, and it did**: `mt_sched_update` is `if (!mt_update_thread(thread)) return;` and `mt_update_thread` opens with `if (!mt_core_supported) return false;` — a never-initialized stand-in reads 0, so the new code executes a load, a test and a return. Build: counts exact, `.text` **0x11D8C0** (predicted 0x11D8B0 + **0x10 fill**), `.data` 0x80120000, `__bss_start` 0x80138AC0, image 0x138AAC as predicted — and **two misses, both explained by one mechanism**: `__bss_end` and the headroom did *not* move (**0x8016F8D8**, **1640232**, against the predicted 0x8016F8F0/1640208), because the object's 0x18 of `.bss` lands at 0x8016E198 inside the **0x2C of alignment padding** the link script already carried in front of the stub object's 16-byte-aligned stand-ins. **`__bss_end` is the end of the padded output section, not the sum of its inputs** — 301's mechanism for the third time, and the first time it was predicted wrongly. **The run: `real XNU entry: exception: irq`, and no `stub_hit` line at all** (`xnu_entry_kv_written=0x00000000`, where 306 and 307 wrote 0x58 and 0x60) — **for the first time in this walk the run was not stopped by a name this image lacks.** The vector is the one XNU's own `start.s` installs (`LOAD_ADDR_GEN_DEF(fleh_irq)`, start.s:429), and `fleh_irq` resolves to `entry_stubs.c`'s reporting handler at 0x80003160 (`{ entry_epilogue("exception: irq"); }`) — real code this project wrote on purpose, in no undefined list and in no stand-in count. Since `cswitch.s`'s `machine_switch_context` contains no `cpsie`/`ml_set_interrupts_enabled` and everything from `splsched` in `thread_block_reason` to the switch runs masked, **the timer fired on the far side of the context switch, in the newly dispatched thread's context** — but the run cannot say *which* thread, because an interrupt report carries no PC (the `abort_*` block is about data aborts and is all zero), so 307's open question stands and `compute_averages` was not reached. This is a **fourth kind of stop**, after 285/286/287's missing symbol / invented zero / boot-arg string: **a vector this image has no real implementation for** — not a name to link but a working exception exit to own. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, log 300987 bytes, device back on its own (release 10). **Next:** `osfmk/arm/locore.s` (`out/xnu_asm_obj/locore.o`, `.text` 0x30C8, 99 definitions), which owns the real `fleh_irq`/`fleh_irq_kernel`/`fleh_irq_handler` and the whole vector table — the first step that is *also* an edit to the instrument, because `locore.o` defines all eight `fleh_*` that `entry_stubs.c` provides and its header already says the link error is what decides which stand-ins have to go |
| stage90 | [experiment-309](experiment-309-no-object-the-instruments-own-interrupt-source.md) | **No object — the instrument's own interrupt source, and the boot leaves the scheduler** — the first step in this walk that links nothing, and the step that moved the frontier furthest. 308 ended on `exception: irq` with no `stub_hit` line, and the interrupt was **this project's own**: `stage90_arm_deadman_reset` arms the dead-man through `stage90_arm_pc_sampling_watchdog`, which enables the timer PPIs at the distributor, starts the physical timer and unmasks IRQs — all three of which the *payload's* vector table services, and `_start` replaces it. The entry image's own header already said the dead-man cannot be honoured across the jump; 308 showed the cost is worse than an unhonourable claim, because the tick lands on `fleh_irq`, which reports one line and stops the machine. `stage90_disarm_deadman_timer()` (`stages/stage90/gic.c`, called from `xnu_entry_jump.c` on the payload's last line) turns the source off at **three levels** — `CNTP_CTL` (`generic_timer_shutdown`), `GICD_ICENABLER0`, `GICD_ICPENDR0` — because the fourth (CPU-level masking) **cannot** hold across the jump: the entry image's exception return restores the CPSR of the interrupted context, so the I bit comes back from XNU's own state. Measured: `isenabler0` **0x000c7fff → 0x00007fff** (bits 18/19, the timer PPIs, were the only thing this boot enabled beyond aboot), `ispendr0` **0x20480000 → 0x20400000** (one had already asserted), `cntp_ctl` **0x00000005 → 0x00000002** (`ENABLE|ISTATUS` — an armed timer that had *already expired* — to `IMASK` only), and `disarm_hw_watchdog_en=0x00000001`, a live read of the SoC's `WDT0_EN` through a new `stage90_hw_watchdog_enabled_readback()`, because the step that removes one recovery net has to show the other is still armed. The entry image changed additively in the same step: `fleh_irq` sets a flag and `entry_epilogue` reads `GICC_IAR`/`GICD_ISPENDR0`/`GICD_ISENABLER0` after the MMU comes off — it *cannot* be read from the vector, which runs with XNU's page tables mapping nothing at 0xf9002000 — and that code **did not run** (`xnu_entry_irq_iar` appears **zero** times, the disarm's own confirmation from a different direction). `.text` **0x11D980** (+0xC0); image 0x138AAC, `.data`, bss and headroom unchanged; counts stay 736/647/89 because no object moved. **`stub_hit=device_service_create` at caller key `0x8000E61C` = `kernel_bootstrap_thread + 0x9c`** — the `bl` at 0x8000E618, right after `bl clock_service_create` at 0x8000E614, which *returned*. **This is the largest single advance in the walk:** the stop is twelve calls into `kernel_bootstrap_thread`'s straight line, so **`sched_startup()` returned** (the bootstrap thread blocked, the scheduler chose a thread, `thread_select`/`thread_invoke` reached `machine_switch_context`, a *different* thread ran, and the bootstrap thread was switched back to — the first full round trip through this kernel's own context switch on hardware), and seven entries that had never executed now have: `idle_thread_create` (305's predicted stop, reached and passed), `thread_daemon_init`, `vm_kernel_reserved_entry_init`, `thread_call_initialize`, `thread_bind`, `ipc_thread_call_init`, `mapping_adjust`, plus `clock_service_create`. 305's prediction of `device_service_create` at 0x8000E59C was **the same call** — the 0x80 difference is the image growing across four steps — so it was right about the call and four experiments early about the step. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, no `exception:` line, log 301633 bytes, device back on its own (release 10). **Next:** `osfmk/device/device_init.c` (manifest:506, `osfmk_device_device_init.o`), `.text` 188 / `.bss` 28 / `.rodata.str1.1` 43, **1 resolved / 0 added** — all eleven of its references are already real and none of their bodies has a stub call, so `device_service_create` is predicted to run to its end and the stop is **`kdp_init` at caller key 0x8000E63C**, with the body's own `panic("can't allocate master device port")` (`bl panic` at +0x30, taken when `ipc_port_alloc_special` returns `IP_NULL`) as the falsifier — the first candidate in this walk that would be a *panic* rather than a stub |

| stage90 | [experiment-310](experiment-310-device-init-and-the-port-was-real.md) | **`device_init.c`, and the master device port was real** — `osfmk/device/device_init.c` (manifest:506), `.text` **188** / `.bss` 28 / `.rodata.str1.1` 43, **9 definitions and 11 references**, **1 resolved** (`device_service_create`, 309's stop) and **0 added**. The prediction was `device_service_create` runs to its last instruction and the stop is the next call `kernel_bootstrap_thread` makes — **`kdp_init` at caller key `0x8000E63C`** — with the body's own `panic("can't allocate master device port")` (`bl panic` at +0x30, taken when `ipc_port_alloc_kernel()` returns `IP_NULL`) as the **first falsifier in this walk that would be a panic rather than a stub**. It did not fire: **`stub_hit=kdp_init` at `xnu_entry_stub_caller=0x8000e63c` = `kernel_bootstrap_thread + 0xbc`**, the `bl` at 0x8000E638 right after `bl device_service_create` at 0x8000E618, which returned; no panic line and no `master device` string anywhere in the log. The step's content is not the symbol count but a **port**: a real port object out of the real `ipc_space_kernel`, tagged `IKOT_MASTER_DEVICE` with `master_device_kobject` as its kobject, given a send right and installed at `HOST_IO_MASTER_PORT` on `host_priv_self()` — the same three port calls the image has been resolving since 278/283, reached from this caller for the first time. Its six globals (`master_device_port`, `master_device_kobject`, `dev_lck_grp_attr`, `dev_lck_grp`, `dev_lck_attr`, `iokit_obj_to_port_binding_lock`) are new *names* rather than replacements for stand-ins — `grep` against `xnu_arm_entry_realstubs.c` is 0 — and its second `.rodata.str1.1` string is the lock group name `"device"`. Counts exact (736 → **735** undefined, 647 → **646** function stubs, 89 storage); `.text` 0x11D980 → **0x11DA40** (+0xBC object, +0x2B merged `.rodata.str1.1`, −0x18 retired stub body, −0x18 retired name string, **+0x9** net alignment fill), image unchanged at 0x138AAC (absorbed by the slack below the fixed `.data` at 0x80120000). **The `.bss` prediction was wrong and it is the mirror image of 308's**: predicted unmoved, measured **0x8016F8D8 → 0x8016F918 (+0x40)** and headroom 1640232 → **1640168**. The map says why — the stub object's `.bss` is **64**-byte aligned (`readelf -S`: `al 64`, because all 89 stand-ins carry `aligned(64)`; 308's note said 16, which was the spacing it saw and not the constraint), so the 0x10 of fill in front of it could not hold the new 0x1C and the whole 0x1704 moved to the next 64-byte boundary — 0x1C of input plus 0x24 more fill, which is the same 0x40. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, no `exception:` line, log 301620 bytes, device back on its own (release 10). **Next:** `osfmk/kdp/kdp_udp.c` (manifest:529, `osfmk_kdp_kdp_udp.o`), 72 bytes of `.text` and nothing else, 12 definitions and 1 reference — the KDP-disabled arm, so `kdp_init` is a bare `bx lr`; **2 resolved / 1 added**, 735 → 733 undefined and 646 → 647 function stubs, with the stop predicted at **`kpc_init`, caller key 0x8000E640** |
| stage90 | [experiment-311](experiment-311-kdp-udp-and-the-step-that-moved-nothing.md) | **`kdp_udp.c`, and the step whose `.text` did not move at all** — `osfmk/kdp/kdp_udp.c` (manifest:529), the smallest object this walk has ever linked: **72 bytes of `.text`** and no `.data`, no `.bss`, no `.rodata` of any kind, **12 definitions and 1 reference**. This configuration compiles the KDP-disabled arm, so `kdp_init`, `kdp_register_send_receive`, `kdp_unregister_send_receive`, `kdp_set_ip_and_mac_addresses`, `kdp_set_gateway_mac`, `kdp_set_interface`, `kdp_register_link` and `kdp_unregister_link` are each a bare `bx lr`, `kdp_get_interface`/`kdp_get_ip_address` are `mov r0, #0; bx lr`, and `kdp_raise_exception` is `mov lr, pc; b panic_spin_forever`. The prediction named the key and the build corrected the counts: **`stub_hit=kpc_init` at caller key `0x8000E640`** = `kernel_bootstrap_thread + 0xc0` (the `bl` at 0x8000E63C, the next instruction after the `bl kdp_init` this step made return), but the count was **2 resolved / 0 added**, not 2/1 — `panic_spin_forever` is *not* a new name, `osfmk/kern/debug.c`'s definition has been linked all along and nothing had ever referenced it, so it appeared in neither the undefined list nor the stub set, and "absent from the undefined list" was read as "will become a stub" when it also means "already defined by something linked". Counts measured 735 → **733** undefined, 646 → **644** function stubs, 89 storage. **`.text` 0x11DA40 → 0x11DA40, unmoved — the first step that linked an object and changed nothing about the section**, and the arithmetic is exact: +0x48 object text − 0x30 for the two retired stub bodies (`kdp_init`, `kdp_raise_exception`) − 0x20 for their two padded name strings + 0x8 alignment fill = 0; the map shows the stub object's `.text` 0x3C90 → 0x3C60, its `.rodata.str1.4` 0x3260 → 0x3240, and the section's total fill 35047 → 35055. Image, `.data`, `__bss_start`, `__bss_end` and headroom all unchanged. The step's content is deliberately negative and said so: it does not measure that the kernel debugger works, it measures that this configuration has none. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, no `exception:` line, log 301620 bytes, device back on its own (release 10). **Next:** `bsd/kern/kern_kpc.c` (manifest:39), `.text` 1796 / `.bss` 24 / `.rodata.str1.1` 428 / `.data` 672 / `__sysctl_set` 56, 33 definitions and 28 references, with the stop predicted at **`kpc_arch_init`, caller key 0x8010279C** and `kpc_common_init` at 0x801027A0 as the alternative |
| stage90 | [experiment-312](experiment-312-kern-kpc-and-the-first-data-section-move.md) | **`kern_kpc.c`, and the first step to move `.data` and `__sysctl_set`** — `bsd/kern/kern_kpc.c` (manifest:39), `.text` 1796 / `.bss` 24 / `.rodata.str1.1` 428 / `.data` **672** / `__DATA,__sysctl_set` **56**, 33 definitions and 28 references: **1 resolved / 15 added**, 733 → **747** undefined and 644 → **658** function stubs, 89 storage unchanged. **The prediction before the build said 19 added and the build said 15, and all four of the difference are names defined by `osfmk_kern_kpc_thread.o`, which has been in `LINK_OBJS` since long before the walk reached `kpc_init`** (`kpc_thread_init`, `kpc_get_curthread_counters`, `kpc_get_thread_counting`, `kpc_set_thread_counting`) — the same misreading as 311, twice in two steps from one cause. **`stub_hit=kpc_arch_init` at caller key `0x8010279c` = `kpc_init + 0x48`**, the `bl` at 0x80102798, exactly as predicted; the linked body is `lck_grp_attr_alloc_init` → `lck_grp_alloc_init` → `lck_mtx_init` → **`kpc_arch_init`** → `kpc_common_init` → `kpc_thread_init` (the last real), so the three lock calls ran and returned and a lock group, a lock group attribute and a mutex named `"kpc"` now exist in the kernel. `.text` 0x11DA40 → **0x11E500** (+0xAC0: +0x704 object, +0x181 for its `.rodata.str1.1` **linked** — 0x1AC in the object, 43 bytes of duplicate strings relaxed away, the 301 lesson arriving again — +0x150 net stub bodies, +0x100 net name strings, −0x21 fill, **and a 0xC residual that is stated and not attributed**); `.data` 0x18A08 → **0x18CA8** (+0x2A0) and `.sysctl_set` 0xA4 → **0xDC** (+0x38), so image 0x138AAC → **0x138D84**. **`.bss` did not grow at all** and it is the third case of the alignment rule — the map's fill in front of the 64-byte-aligned stand-ins goes `0x34 → 0x1c`, exactly the object's 0x18, so the stand-ins keep their offset from the section start — while `__bss_start` 0x80138AC0 → **0x80138DC0** and `__bss_end` 0x8016F918 → **0x8016FC18** both move +0x300 (the `.data`/`.sysctl_set` growth plus the 64-byte turn after them) and headroom 1640168 → **1639400**. This object's `__sysctl_set` entries are the registration of the 15 `sysctl__kpc*` nodes, so the KPC sysctl tree is now part of the kernel's set — a structural change no earlier step made. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, no `exception:` line, log 301625 bytes, device back on its own (release 10). **Next:** `osfmk/arm/kpc_arm.c` (manifest:442), `.text` 3916 / `.rodata.str1.1` 42 / `.bss` 184, **6 resolved / 7 added** (747 → 748 undefined, 658 → 659 function stubs), and since `kpc_arch_init`'s whole body in the object is `mrc p15, 0, r0, cr9, cr12, {0}; bx lr` the stop is predicted one call further on, at **`kpc_common_init`, caller key 0x801027A0** |
| stage90 | [experiment-313](experiment-313-kpc-arm-and-a-3916-byte-object-one-call-wide.md) | **`kpc_arm.c`, and a 3916-byte object whose step is one call wide** — `osfmk/arm/kpc_arm.c` (manifest:442), `.text` **3916** / `.rodata.str1.1` 42 / `.bss` **184**, 45 definitions and 38 references. Almost all of that text is the ARM PMU half of KPC (`kpc_pmi_handler`, the four `kpc_*_xcall` cross-call stubs, `kpc_set_{config,period,running}_arch`) and **none of it is reached**, because `kpc_arch_init` in the object is `mrc p15, 0, r0, cr9, cr12, {0}` then `bx lr` — a PMCR read whose value is discarded — so the step is two instructions of execution wide. **`stub_hit=kpc_common_init` at caller key `0x801027a0` = `kpc_init + 0x4c`**, exactly as predicted. **Resolved 6** (`kpc_arch_init` plus `kpc_get_classes`, `kpc_get_pmu_version`, `kpc_idle`, `kpc_idle_exit`, `kpc_set_sw_inc`) and **added 7 — but as 6 functions and 1 storage, not 7 functions**: `kpc_actionid` is a *variable* in `kpc_common.c` and the generator sized its stand-in from the real definition, `uint8_t kpc_actionid[0x18] __attribute__((aligned(64)))`, so the function count does not move at all (658 → **658**, −6 retired +6 created) while storage goes 89 → **90** and undefined 747 → **748**. `.text` 0x11E500 → **0x11F4C0** and **it closes exactly this time, with no residual**: +0xF4C object, +0x02A its `.rodata.str1.1`, +0x000 net stub bodies, +0x044 net name strings (six retired = 0x60, six created = 0xA0, 4 bytes of padding), +0x006 fill. Image, `__bss_start` and `.data` unchanged; `.bss` grew by exactly its inputs — 0xB8 (the object's 184 bytes) + 0x8 (alignment) + 0x40 (the new 64-byte-aligned `kpc_actionid` slot) = **+0x100**, so `__bss_end` 0x8016FC18 → **0x8016FD18** and headroom 1639400 → **1639144**, which is the third case of the 308/310/312 alignment rule arriving as the 310-shaped one. What the step measures is mostly what it makes possible: 184 bytes of real PMU state (`saved_PMOVSR`, `saved_PMCNTENSET`, `saved_PMXEVTYPER`, `saved_counter`, `kpc_running_classes`, `kpc_xcall_sync`, …) now exist as correctly sized variables rather than as nothing. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, no `exception:` line, log 301627 bytes, device back on its own (release 10). **Next:** `osfmk/kern/kpc_common.c` (manifest:562), `.text` 8072 / `.bss` 64 / `__DATA,__data` 120, **23 resolved / 1 added** (`kperf_sample`), whose `kpc_common_init` is three real lock calls and a tail branch — so `kpc_init`'s body completes and the step should end with `kpc_init` **returning** and the bootstrap thread moving on to **`ktrace_init`, caller key 0x8000E660** |
| stage90 | [experiment-314](experiment-314-kpc-common-and-the-term-that-is-zero.md) | **`kpc_common.c`, and the step where `.text` closes exactly because one term is zero** — `osfmk/kern/kpc_common.c` (manifest:562), `.text` **8072** / `.bss` 64 / `.rodata.str1.1` **4** / `__DATA,__data` **120**, 45 defined symbols and 38 references: **23 resolved / 1 added** — and the first count prediction in four steps that was right in both halves (313 got the total and missed the split; 312 and 311 each missed it the same way). Measured 748 → **726** undefined, 658 → **637** function stubs, 90 → **89** storage, with the split exact as well: of the 23 resolved, **22 are functions and one is storage** (`kpc_actionid`, which 313 created as a 24-byte stand-in and this object defines as `uint8_t kpc_actionid[0x18] __attribute__((aligned(64)))`), and the one added (`kperf_sample`) is a function. **`stub_hit=ktrace_init` at caller key `0x8000e660`** = `kernel_bootstrap_thread + 0xe0`, the `bl` at 0x8000e65c — the first stub after `kpc_init` in that straight line, exactly as predicted — so `kpc_init` returned and KPC's initialization is complete in this image: a lock group named `"kpc"` and a mutex over it (312), 184 bytes of PMU state (313), and now 64 more bytes of lock state, 120 bytes of `site` records and the whole `kpc_*` API as real code. **The 313 prediction that this object would add 4 bytes of strings was wrong in the most useful way: it adds zero.** The 4 bytes are the string `"kpc"` and 312's object already put the same four bytes in the same mergeable group — the image holds exactly one `kpc\0`, at **0x8011B622**, and the linked `kpc_init` loads that address as `lck_grp_alloc_init`'s first argument (`movw r0, #0xb622` / `movt r0, #0x8011` at 0x8010276c) — while the map still prints a `0x4` contribution line for it at 0x8011D402 followed by `*fill* 0x2`, both at pre-relaxation addresses. That is the second half of the 301/312 lesson: **a mergeable input's contribution line is an upper bound too**. `.text` 0x11F4C0 → **0x1210A0** (+0x1BE0) then closes exactly with no residual: +0x1F88 object, −0x1F8 net stub bodies (22 × 0x18 retired, 1 created), −0x1A8 net name strings (0x1B8 retired padded, 0x10 created), −0x008 net fill (map total 0x88D0 → 0x88C8). `.data` 0x18CA8 → **0x18D20** is +0x78 = the object's 120 bytes to the byte (five 24-byte `site` records), **but the output section's start steps a whole 16 KB**, 0x80120000 → **0x80124000**, because `.text`'s end crossed that boundary (304's mechanism: `.data` is 16 KB aligned) — so image 0x138D84 → **0x13CDFC**, `__bss_start` 0x80138DC0 → **0x8013CE00** (+0x4040 = 0x4000 step + 0x78 object − 0x38 alignment), `__bss_end` 0x8016FD18 → **0x80173D58** (+0x4040 with `.bss` size unchanged at 0x36F58), headroom 1639144 → **1622696** (−0x4040). **`.bss` is the fourth case of the alignment rule and the first where nothing moves**: the object's 0x40 does not fit the 0x20 gap in front of the 64-byte-aligned stand-ins (the 310-shaped case, so the block moves to the next boundary), but the slot it displaces is the retired 0x40 `kpc_actionid` stand-in, so the insert and the retire cancel and only the two addresses inside the section change. The run also measures `bootprofile_init` returning (0x8000e64c) — two more entries of `kernel_bootstrap_thread` consumed. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, no `exception:` line, log 301623 bytes, device back on its own (release 10). **Next:** `bsd/kern/kern_ktrace.c` (manifest:40), `.text` 2564 / `.bss` 82 / `.rodata.str1.1` 250 / `.data` 288 / `__sysctl_set` 24, **11 resolved / 4 added** (726 → 719 undefined, 637 → 630 function stubs) — and the stop is predicted to leave `kernel_bootstrap_thread`'s straight line for the first time in a long while: `kdebug_init` at 0x8000e674 is a one-instruction trampoline into `kdebug_trace_start`, which calls four of the eleven names this step resolves at six call sites, and after they resolve the predicted stop is **`sysctl_early_init`, caller key 0x8000E6A4** (via `bsd_early_init`, another one-instruction trampoline whose `b` does not set `lr`) |
| stage90 | [experiment-315](experiment-315-kern-ktrace-and-a-predicted-caller-key.md) | **`kern_ktrace.c`, and a prediction that named the caller key exactly** — `bsd/kern/kern_ktrace.c` (manifest:40), `.text` **2564** / `.bss` 82 / `.rodata.str1.1` 250 (linked **215**, 35 bytes relaxed) / `.data` 288 / `__DATA,__sysctl_set` **24**, 47 defined symbols and 25 references: **11 resolved / 4 added**, both halves exact — the eleven are the `ktrace_*` set (`ktrace_init` plus `ktrace_assert_lock_held`, `ktrace_configure`, `ktrace_end_single_threaded`, `ktrace_get_owning_pid`, `ktrace_kernel_configure`, `ktrace_lock`, `ktrace_read_check`, `ktrace_reset`, `ktrace_start_single_threaded`, `ktrace_unlock`) and the four added are `kperf_reset`, `kperf_sampling_disable`, `ktrace_background_available_notify_user`, `sysctl_handle_string`, all functions because none has a defining object in the pool (the 304 rule). Measured 726 → **719** undefined, 637 → **630** function stubs, storage unchanged at 89. **`stub_hit=sysctl_early_init` at `xnu_entry_stub_caller=0x8000e6a4`** = `kernel_bootstrap_thread + 0x124` — the predicted name *and* the predicted key, and the key is the interesting part: `caller − 4` is 0x8000e6a0, `bl bsd_early_init`, and `bsd_early_init` is **one instruction** (`b sysctl_early_init` at 0x8003a9e0), so a `b` that does not set `lr` leaves the stub reporting the bootstrap thread's own call. `kdebug_init` at 0x8000e674 is the same shape (`b kdebug_trace_start`), and that target calls four of the eleven resolutions at six call sites — so the run exercised `ktrace_init`'s four lock calls plus its `assert` (which did not fire), then `kdebug_trace_start`'s whole control flow (start, configure, three lock assertions, end of single-threaded tracing) and its return, then `prng_cpu_init`, before the tail branch stopped. `.text` 0x1210A0 → **0x121A60** (+0x9C0) closes exactly: +0xA04 object, +0x0D7 linked strings, −0x0A8 net stub bodies (11 × 0x18 retired, 4 created), −0x080 net names (0xE4 retired padded, 0x64 created), +0x00D fill — with the map agreeing per region (`.text` non-fill +0x9B3, fill +0xD), **which is why the fill had to be read per region: the whole map's fill total went down 5 while `.text`'s went up 0xD, because `.bss`'s fell by 0x12.** `.data` +0x120 (288) at the same start 0x80124000, `.sysctl_set` 0x8013CD20 → **0x8013CE40** size 0xDC → **0xF4**, `.bss` 0x8013CE00 → **0x8013CF40** size 0x36F58 → **0x36F98** (non-fill +0x52, fill −0x12, so +0x40 — the 0x52 object splits across the 0x20 gap and the 64-byte-aligned stand-ins move +0x40 to offset 0x35880, with no storage standing-in retired to cancel it), image 0x13CDFC → **0x13CF34**, `__bss_end` 0x80173D58 → **0x80173ED8**, headroom 1622696 → **1622312**. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, no `exception:` line, log 301629 bytes, device back on its own (release 10). **Next:** `bsd/kern/kern_newsysctl.c` (manifest:46), `.text` 8132 / `.bss` 28 / `__DATA,__data` 96 / `.rodata.str1.1` 71 / `.data` 240 / `__sysctl_set` 20, **6 resolved / 6 added** with only four of the six arriving as functions (`securelevel` and `sysctl__sysctl_children` are `B`, so they become 4-byte storage stand-ins) — 719 → 719 undefined, 630 → 629 function stubs, 89 → 90 storage, with the stop predicted at **`StartIOKit`, caller key 0x80004A20** (`PE_init_iokit`'s one conditional stub call; the walk reports no stub on its straight-line path and names the indirect calls it cannot follow) |
| stage90 | [experiment-316](experiment-316-kern-newsysctl-and-a-stop-the-run-never-reached.md) | **`kern_newsysctl.c`, and a stop the prediction named but the run never reached** — `bsd/kern/kern_newsysctl.c` (manifest:46), the object that defines `sysctl_early_init`: **8132 bytes of text, 59 definitions, 35 references**, **6 resolved** (five functions — `sysctl_early_init`, `sysctl_handle_int`, `sysctl_handle_quad`, `sysctl_handle_string` which 315 added and lasts one experiment, `sysctl_io_number` — and one storage, `sysctl__children`) and **6 added** (four functions `fuulong`/`suulong`/`mac_system_check_sysctlbyname`/`proc_suser`, and two 4-byte `B` stand-ins `securelevel` and `sysctl__sysctl_children`): two counters moving in opposite directions, −6 + 6 = 0, so the undefined total does not move. **Counts right in every column** (719 → **719** undefined, 630 → **629** function stubs, 89 → **90** storage) and **the stop wrong**: predicted `StartIOKit` at caller key `0x80004A20`, measured **`stub_hit=OSKextKextForAddress` at `xnu_entry_stub_caller=0x80096298`** = `_os_log_to_log_internal+0x2c`. `PE_init_iokit` *was* entered, but its **first** `printf` at 0x800046fc — 0x320 bytes before the conditional `bl StartIOKit` at 0x80004a1c — put the run five frames deep into a logging stack: `printf → vprintf_internal → os_log_with_args → _os_log_to_log_internal → OSKextKextForAddress`. The prediction named a call the run never reached, not a branch decided against; the tool that says so had been run and read past (`--root PE_init_iokit` reports **no stub on its straight-line path**, while `stub_calls_in_function.py` truthfully reports one stub call one level deep — **a real function can hide a stub below itself**). `.text` 0x121A60 → **0x123A00** closes exactly (object 0x1FC4, its `.rodata.str1.1` linked 0x2D, 5 bodies retired −0x18, 5 names retired + 4 created −0x2C, fill −0xD); `.data` 0x18E40 → **0x18F90** (+0x150 = `.data` 0xF0 + `__DATA,__data` 0x60), `.sysctl_set` 0xF4 → **0x108**, `.bss` size 0x36F98 → **0x37018** (+0x1C object + 0x40 stand-in block + 0x24 fill, the block growing 0x1704 → 0x1744 because one 64-byte storage slot retires and two are added — every 4-byte `aligned(64)` variable takes a whole slot). Image 0x13CF34 → **0x13D098**, `__bss_end` → **0x801740D8**, headroom 1622312 → **1621800**. Log 301632 bytes, one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, device back on its own |
| stage90 | [experiment-317](experiment-317-oskextlib-and-a-stop-that-moved-down.md) | **`OSKextLib.cpp`, and a stop that moved *down* instead of forward** — `libkern/OSKextLib.cpp` (manifest:360), the object that defines `OSKextKextForAddress`: a **small** object, `.text` **1424** / `.rodata.str1.1` **474** / `.data` 4 (`gOSKextUnresolved`), 17 definitions, 25 references of which 12 are already defined and none currently undefined. **Counts exact in every column** — 728 undefined, **638** function stubs, 90 storage, **4 resolved / 13 added** (the 13 all functions defined by `libkern_c++_OSKext.o`: `OSKextLog` plus the twelve `_ZN6OSKext*` methods) — after the *first draft* of the prediction wrote 642 by adding the 13 without subtracting the 4, which is 307's count defect in miniature. `.text` 0x123A00 → **0x1243E0** (+0x9E0, against a predicted +0x9A0 ± 0x40) closes to a **2-byte residual**: +0x590 object, +0x1C6 linked strings, +0xD8 stub bodies (9 × 0x18), +0x1A1 stub names, +0x13 fill. `gOSKextUnresolved` lands at **0x80140128** and **`.data` does not grow at all** — the 4 bytes displace the `*fill* 0x4` that used to sit after `const_boot_args`, so the section's fill total falls by exactly 4 (8 fills / 0x7AAA → 7 / 0x7AA6) and everything from `osfmk_arm_cpu.o` onward keeps its offset: **312's `.bss` case in `.data`**. `.text`'s end crossed another 16 KB boundary, so the whole data block steps +0x4000 (except `.bss`'s *size*, unchanged to the byte — the first step in eight where the alignment rule has nothing to say). **The stop is the surprise: `stub_hit=_ZN6OSKext14kextForAddressEPKv` at `xnu_entry_stub_caller=0x80096298` — the *same caller key 316 reported*, not the predicted `StartIOKit` at 0x80004A20 — because the function this step made real is one instruction, `80108c7c <OSKextKextForAddress>: b 8010c7b8 <_ZN6OSKext14kextForAddressEPKv>`, a tail branch into a name *this step itself created as a stub*. A `b` does not set `lr` (315's idiom), so the stub reports the `bl` five frames up: **resolving a name can move the frontier one frame down into the function the step just linked rather than forward**, and the walk is not in a different place but one frame deeper in the same place. Image 0x13D098 → **0x141098**, headroom 1621800 → **1605416**. Log 301642 bytes, one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, device back on its own |


| stage90 | [experiment-318](experiment-318-oskext-and-a-b-symbol-that-is-not-a-stand-in.md) | **`c++/OSKext.cpp`, a build that refused the step, and a `B` symbol that is not a stand-in** — `libkern/c++/OSKext.cpp` (manifest:369), the largest object in the walk: `.text` **74436** + a COMDAT `.text.*`, `.rodata.str1.1` **15092** (439 strings), `.rodata` 424, `.data` 347, `.bss` 477, `__DATA,__data` 216, `__DATA,__sysctl_set` 4, `.init_array` **4**, 181 definitions, 186 references. **The build refused the step before it could be run**, and that is half the result: `.init_array` is a section `entry.ld` had no `*(…)` for, so the linker made it a fifth **orphan output section** and `verify_sections` failed the build by name — the check written after 297/298 doing its job for the first time since 291. `entry.ld` now names `.init_array` between `.sysctl_set` and `.bss` (it is `WA`, so an input inside `.text` would put writable bytes in the read-only `__TEXT` region 298 establishes; it keeps its *name* so the table stays visible in the map), and the expected list is five names. One entry, an `R_ARM_TARGET1` to `_GLOBAL__sub_I_OSKext.cpp`, which constructs `OSMetaClass("OSKext", &OSObject::gMetaClass, 96)` — and **nothing in this image runs it**: XNU's `OSRuntimeInitializeCPP` finds constructors by *section name* (`__mod_init_func`/`__constructor`, `OSRuntime.cpp:208`), neither of which `entry_macho.s` describes, so `OSKext::gMetaClass` is 96 bytes of `.bss` that stay zero. **Counts exact in every column** — 814 undefined, **709** function stubs, 105 storage, **18 resolved / 104 added**, the largest single jump in this walk in both directions at once (86 functions: 82 `T` in the pool plus 4 with no defining object, which by the 304 rule still arrive as function stubs; 18 storage: 7 `B`, 11 `R`). `.text` 0x1243E0 → **0x13B060** (+0x16C80) closes to a **0x23 residual, over-counting direction** (only a mergeable term can do that; a 439-string scan finds exactly 35 already in the base image — recorded, explicitly **not** claimed as the mechanism, since one 51-byte dedup alone would pay for it). `.data` +0x238 = 0x15B + 0xD8 content + 0x5 fill; `.sysctl_set` +0x4; `.bss` +0x580 = 0x1DD + 0x3C0 − 0x1D, the 0x3C0 being **15 net new 64-byte stand-in slots** (18 in, 3 out) exactly as the `aligned(64)` rule says; and `.text`'s end crossed four 16 KB boundaries at once, so `.data`'s start steps **+0x14000** — 304's mechanism at its largest. **The stop is not the predicted one, and its stated premise is wrong in the step's headline way:** `stub_hit=_os_trace_addr_in_text_segment` at `xnu_entry_stub_caller=0x800962b0` = `_os_log_to_log_internal+0x44` (the `bl` at 0x800962AC). The prediction reasoned that `vm_kernel_stext`/`vm_kernel_etext` are `B` and absent from the stub set, therefore zero, therefore `kextForAddress` returns NULL without a call. **Absent from the stub set means a *real defining object*** — `osfmk_arm_arm_vm_init.o`, linked and already run: `arm_vm_init.c:406 segTEXTB = getsegdatafromheader(&_mh_execute_header, "__TEXT", &segSizeTEXT)`, `:496 vm_kernel_stext = segTEXTB`, `:497 vm_kernel_etext = segTEXTB + segSizeTEXT`. So `kextForAddress` takes its **early return** `&_mh_execute_header` (0x8011A36C) — a *correct* answer for a `format` string inside `__TEXT` — `dso` is non-NULL, and the function proceeds to the next stub. **A `B` symbol in the linked image is not automatically a stand-in**: the stub set answers "does this image define the name", and "what is in it" is a different question with a different instrument. Image 0x141098 → **0x1552D8**, `__bss_end` → 0x8018C898, headroom 1605416 → **1521512**. Log 301642 bytes, one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, device back on its own |

| stage90 | [experiment-319](experiment-319-os-internal-and-the-chain-that-unwound.md) | **`os/internal.c`, and the frame chain that unwound** — `libkern/os/internal.c` (manifest:402), the object that defines `_os_trace_addr_in_text_segment`: the **smallest step in a long time**, `.text` **356** (0x164) for one function, `.rodata.str1.1` **7**, **1 definition**, **1 reference** (`strncmp`, real). Its body switches on `mhp->magic` and walks the load commands for `LC_SEGMENT`/`__TEXT`, returning whether `addr` lies in `vmaddr .. vmaddr + vmsize`. **Counts exact** — 813 undefined, **708** function stubs, 105 storage, **1 resolved / 0 added** — against a baseline built in this session with an empty stand-in in the slot (709 / 105 / 814, `.text` 0x13B060), which is 318 reproduced to the byte. `.text` 0x13B060 → **0x13B1A0** is +0x140 with **no residual**: +0x164 object, **+0x000 strings**, −0x018 body, −0x020 name, +0x014 fill. The string term is the **third sighting of 301/312/314's rule** and the first where the duplicate can be *pointed at*: the map prints `0x7` for this object's `.rodata.str1.1`, the very next input (`.rodata.macho`, `_mh_execute_header`) starts at the **same address 0x80136CA8**, and the built code loads the surviving `"__TEXT"` from **0x80128CFF** (`movw r6,#0x8cff / movt r6,#0x8012`) where the image holds `__TEXT\0__DATA\0__LAST…` — the image's only two occurrences of `__TEXT\0` being that one and the Mach-O header's own segname field. **Every other section is unchanged to the byte** (`.data` 0x8013C000/0x191C8, `.sysctl_set` 0x801551C8/0x10C, `.init_array` 0x801552D4/0x4, `.bss` 0x80155300/0x37598), image 1397464 **unchanged** and headroom 1521512 **unchanged**, because `.text` grew into the 0xE60 of slack before the 16 KB-aligned `.data` — 304's mechanism read the other way, after 318 crossed four boundaries at once and moved the whole data block +0x14000. **And the stop is the one 316 and 317 both predicted and neither reached: `stub_hit=StartIOKit` at `xnu_entry_stub_caller=0x80004a20` = `PE_init_iokit+0x34c`, `caller-4` = `80004a1c: bl 8011dccc <StartIOKit>`.** Four functions closed at once: `_os_trace_addr_in_text_segment` returned **true**, `OSKextKextForAddress(addr)` returned the same `&_mh_execute_header` so the equality test passed, `_os_log_to_log_internal` ran its whole real body (two `memset`s, `hw_atomic_add`, `strchr`, the inlined `_os_log_encode`/`_os_log_actual`, a real `_firehose_trace` write, its `va_end`), `printf` and `vprintf_internal` returned, and `PE_init_iokit` ran the rest of its own body — so **`StartIOKit` is the first stop of this walk that is inside `PE_init_iokit` itself rather than below a call it made**. Also recorded: **no XNU `printf` text appears in any log**, because this image's `printf` *is* Apple's os_log shim — the diversion 316 found is not a log-setting accident. Log 301622 bytes, one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, device back on its own |

| stage90 | [experiment-320](experiment-320-iostart-iokit-and-the-line-that-was-not-this-objects.md) | **`iokit/IOStartIOKit.cpp` — the step that enters IOKit, and a `.text` closure that needed a line belonging to another object** — `iokit/Kernel/IOStartIOKit.cpp` (manifest:346), the object that defines `StartIOKit`: `.text` **876** (0x36C) for **5 functions** (`StartIOKit` 0x184, `IOKitInitializeTime` 0x3C, `iokit_post_constructor_init` 0xB4, `IORecordProgressBackbuffer` 0xA8, `IORegistrySetOSBuildVersion` 0x50), `.bss` **12**, `.rodata.str1.1` **142**, **3 `B`** and **29 references**. **Every count exact** — 832 undefined, **724** function stubs, **108** storage, **2 resolved / 21 added** (18 functions plus 3 storage stand-ins sized from the pool: `gCanSleepTimeout` 4, `gIOKitDebug` 8, `gIOKitTrace` 8) — against a baseline built in this session with an empty stand-in in the slot, which reproduces 319's image to the byte. **`stub_hit=IOLibInit` at `xnu_entry_stub_caller=0x8011b264` = `StartIOKit+0xC4`**, `caller-4` = `0x8011b260: bl 8011bf38 <IOLibInit>` — the instruction the prediction named. The prediction quoted that call as `StartIOKit+0x1B4`; the relocation offsets are **`.text`-relative**, and the two functions before `StartIOKit` in the section are 0xF0 bytes, so the base was off by exactly that and the substance was right. Like 319, the run is the second step in a row that crosses no 16 KB boundary: `.data` 0x8013C000/0x191C8, `.sysctl_set` 0x801551C8/0x10C, `.init_array` 0x801552D4/0x4 all unmoved, image **1397464 unchanged**, headroom 1521512 → **1521320** — and `.bss` closes as 312's did, the section growing 0xC0 for the object's 12 bytes plus three 64-byte stand-in slots because the fill in front of the stand-ins fell by exactly 0xC (0x106 → 0xFA). **`.text` 0x13B1A0 → 0x13B960 is +0x7C0 and closes with no residual, but only with a term nobody predicted**: +0x36C object, **+0x08B** its strings (139 placed — the object's own section is 142, and the three missing bytes are `io\0`, whose reference resolves to **0x80127FC7** where the image already holds `io\0/cpus\0running\0tim…`: read out of the image, the *fourth* sighting of 301/312/314/319's rule and the second that is *shown* rather than inferred), **−0x008 for `pexpert_arm_pe_init.o`'s `.rodata.str1.1`, which fell 446 → 438 — an object this step does not touch**, +0x180 stub bodies, +0x258 name strings, −0x007 fill. The region identity is exact: Σ(placed inputs) 0x13A490 → 0x13AC57 = +0x7C7, Σ(fill) 0xD2F → 0xD28 = −0x007, sum +0x7C0 = the section's own delta — **so a mergeable contribution line is not a property of the object that owns the section but an allocation out of a pool, and an insertion anywhere in the pool can move any line.** Getting that number required a new parser: the map's mergeable contributions are **two-line records**, and a parser wanting section name and input on the same line drops every one of them (the first attempt reported +0x4EC and would have hidden the `pe_init` line). The prediction's 0x12 miss is then fully accounted: +3 for the object's 0x8E against its placed 0x8B, +8 for the pool taking eight bytes back out of another object's line, +7 of fill. What the step measures is IOKit's first function: `StartIOKit`'s three `PE_parse_boot_argn` calls are real and the third **stores** `pmtimeout`'s value into `gCanSleepTimeout` at 0x8018C500 — a `.bss` stand-in this step itself created, holding a value nothing reads, the stand-in hazard stated rather than discovered. Log **301621** bytes (one fewer than 319's, the payload recording the stub's name verbatim), one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, device back on its own (release 10) |

| stage90 | [experiment-321](experiment-321-iolib-and-an-image-that-paid-only-for-the-step.md) | **`iokit/IOLib.cpp` — IOKit's allocator becomes real, and an image that paid only for the step** — `iokit/Kernel/IOLib.cpp` (manifest:322), the object that defines `IOLibInit` (320's stop): `.text` **6476** (0x194C) holding **38 functions**, `.bss` **384**, `.rodata` **56**, `.rodata.str1.1` **299**, a `__DATA, __data` of **72**, and **52 references** — the largest step in `.text` since 316's 8132 and the largest closure since then. **Every count exact** — 835 undefined, **725** function stubs, **110** storage, **4 resolved / 7 added** (`IOLibInit`, `IOMalloc`, `IOFree`, `IOSleep` resolved; 5 functions and 2 storage stand-ins added) — against a baseline built in this session with an empty stand-in in the slot, reproducing 320's image to the byte. **`stub_hit=IOLockAlloc` at `xnu_entry_stub_caller=0x8011b528` = `IOLibInit+0x10C`**, `caller-4` = `0x8011b524: bl 8011d86c <IOLockAlloc>` — the predicted call at the predicted offset, and the first prediction whose offset base needed no conversion, because `IOLibInit` is the *first* function in its object's `.text` (320 needed the 0xF0 correction and is what taught the difference). **`.text` 0x13B960 → 0x13D3E0 is +0x1A80 and closes with no residual**: +0x194C object, **+0x06F** its strings (111 placed against the object's own 0x12B = 299), +0x038 its `.rodata`, +0x018 stub bodies, +0x070 name strings, +0x005 fill. The prediction missed by **0x10F**, all three reasons measurable: −0xBC for the string term taken as the object's section size instead of its placed size, −0x58 for the name term estimated at 0xC8 against 0x70 placed, +0x05 of fill (inside the band and the only one allowed for) — and the ledger's own slip is part of it (the name term was written as 0xC8 while its own parenthetical gave created 0xC8 minus retired 0x28, i.e. 0xA0). **188 of the 299 string bytes went away and the four dropped strings can be pointed at**: the object holds ten strings and the linker placed six, the four missing ones — `"overflow detected"` and the three `queue.h` `print_queue` panics — each surviving **exactly once** at **0x8012A592**, **0x80129F8E**, **0x80129FA9**, **0x80129FE2**, all *below* the object's placed range at 0x80138B7B: the **fifth sighting** of the 301/312/314/319/320 rule, the third shown rather than inferred, and the largest (63% of the section). The name term is the same rule at pool scale — 832 names sum 0x4458 by `align4(len+1)` and 0x3F98 by `len+1` against 0x3C77 in the map, so the pool merges **shared tails** too and a name term is an upper bound however it is computed. **The 16 KB boundary was crossed and the image grew by only 0x4048 while `.text` grew 0x1A80**: `.text` now ends at 0x8013D3E0, past 0x8013C000, so `.data` stepped **+0x4000** to **0x80140000** (size +0x48), and the image — the span [base, `.init_array`'s end] — grew by the *step* plus the object's data and **not** by the text, which sat entirely inside the gap before the boundary (0x6A0 wide before, 0x2C20 after). That is 304's rule for the third time (318 moved the block +0x14000 for +0x16C80 of text, 319 moved nothing for +0x140) and why the image prediction was 0x1CF7 high. `.bss` closes plainly: the object's **0x180** plus **two** 64-byte stand-in slots = **0x200**, with the stand-in fill unmoved (0xFA/38 in both maps) because no stand-in is retired — `__bss_start` 0x80159340, `__bss_end` **0x80190B98**, headroom 1521320 → **1504360**. What it measures: `IOLibInit` ran its **whole body** — the guard's `libInitialized` byte at 0x8018EDFC was zero, so no early return; `lck_grp_alloc_init("IOKit")`; the **96 MB pageable map** carved out of `kernel_map` by `kmem_suballoc` (the first this boot has made, and its failure branch not taken, so the `panic` was not reached); both page allocators `bzero`'d and locked; `gIOPageAllocChunkBytes` set to 64. One detail kept: the `bzero` relocation resolved to **`__bzero`**, so an "already real" name can still be an alias at link time. Log **301623** bytes, one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, device back on its own (release 10) |
| stage90 | [experiment-322](experiment-322-iolocks-a-step-that-added-nothing-and-the-name-terms-rule.md) | **`iokit/IOLocks.cpp` — a step that added nothing, a `.text` that closed in four terms, and the name term's rule** — `iokit/Kernel/IOLocks.cpp` (manifest:323), the object that defines `IOLockAlloc` (321's stop): `.text` **820** (0x334) for **25 functions**, **no `.bss`, no `.data`, no `.rodata` and no `.rodata.str1.1` at all** — the first step in this walk whose object brings no storage of any kind — 25 `T`, 1 `t`, **19 references, every one already defined**. **Every count exact** — 829 undefined, **719** function stubs, **110** storage, **6 resolved / 0 added** (`IOLockAlloc` plus the five `IORecursiveLock*` names; the second step in the walk with an added count of zero, after 314's eleven-to-one) — against a baseline built in this session with an empty stand-in in the slot, reproducing 321's image to the byte. **`stub_hit=OSlibkernInit` at `xnu_entry_stub_caller=0x8011b268` = `StartIOKit+0xC8`**, `caller-4` = `0x8011b264: bl 8011d378 <OSlibkernInit>` — the predicted name *and* the predicted key, at the call 320 created as a stub. **`.text` 0x13D3E0 → 0x13D600 is +0x220 and closes with no residual** — +0x334 object, −0x090 stub bodies (6 retired, none created), −0x080 name strings (exactly the six retired names' `align4(len+1)`), −0x004 fill — the **closest `.text` point estimate in this walk, 4 bytes**, and the first step where the name term is not a band but a number. **That is where the name term becomes a rule**: the pool holds **one padded slot per *function* stub and nothing else**, so the term is Σ`align4(len+1)` over added function names − the same over retired ones, byte-exact on two consecutive steps (320's pool extent 0x3C87 → 321's 0x3CF7 = +0x70 = 0x98 added − 0x28 retired; 321 → 322's 0x3C77 = −0x80 = the six). `align4(len+1)` over *all* an image's function names reproduces its pool's span to within one byte (0x3CE8 vs 0x3CE7), all 719 names sit in one dense run 0x80138FD0…0x8013CC37 each in its own NUL-preceded slot, and of the 110 storage stand-ins **none has a slot** — the only two whole-string matches are coincidences (`mountroot\0` is the tail of the pool's own `vfs_mountroot\0`, `systemdomain` sits in another object's kprintf format). **So 321's 0x58 is not tail merging and that reading is withdrawn**: the 832 names whose `align4` sum 0x4458 was held against a 0x3C77 pool include the 108 storage stand-ins, whose own sum is **0x810** — the whole of that gap. The 0x58 is exactly 0x30 + 0x28: **0x30** of storage names (`debug_iomalloc_size` 20 + `debug_iomallocpageable_size` 28) that never enter the pool and appear nowhere in the image, and **0x28** of resolved names the prediction had already subtracted in prose but left out of its sum (the slip of 307 and 317). **Nothing else moved, to the byte** — `.data` 0x80140000/0x19210, `.sysctl_set` 0x80159210/0x10C, `.init_array` 0x8015931C/0x4, `.bss` 0x80159340/0x37858, `__bss_end` **0x80190B98**, image **1413920** and headroom **1504360** all unchanged, because `.text` ends 0x2A00 below the 16 KB boundary (319's case; the third step in four to cost only `.text`). **And the frontier moved *up* a frame for the first time**: 320 entered `StartIOKit`, 321 ran `IOLibInit`'s body to its one stub, and this step makes the stub real so `IOLibInit` sets its own `libInitialized`, pops, and **returns**, leaving `StartIOKit` to advance to its next statement. `IOLockAlloc` is 12 bytes ending in `b lck_mtx_alloc_init`, a tail branch, so the stub was entered and never returned to and the key jumped from `IOLibInit+0x10C` to `StartIOKit+0xC8` across a whole function's return. IOKit's lock family (`IOLock`, `IORecursiveLock`, `IORWLock`, `IOSimpleLock`) is complete over the `lck_*` primitives, 19 of its entry points unreferenced. Log **301625** bytes, one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, device back on its own (release 10) |
| stage90 | [experiment-323](experiment-323-osruntime-the-miss-that-was-the-previous-steps-zero.md) | **`libkern/c++/OSRuntime.cpp` — the kernel's C++ runtime becomes real, and a `.text` miss that is the previous step's zero** — `libkern/c++/OSRuntime.cpp` (manifest:374), the object that defines `OSlibkernInit` (322's stop): `.text` **2476** (0x9AC) for 15 functions, `.data` **4**, `__DATA, __data` **48**, `.rodata.str1.1` **320**, `.bss` **1**, and **26 references** — the linker's `operator new`/`new[]`/`delete`/`delete[]` over `kern_os_malloc`, the constructor initialiser `OSRuntimeInitializeCPP` (0x310) that 318 found walking `__mod_init_func` by *section name*, `OSlibkernInit` which calls it and `panic`s on failure, the unload paths and `__cxa_pure_virtual`. **Every count exact** — **4 resolved / 6 added** (831 undefined, **721** function stubs, storage 110; the six added all functions: the five `OSMetaClass*` mod-load and `OSMetaClassBase::initialize` names plus `OSSymbol::checkForPageUnload`) — against a baseline built in this session with an empty stand-in in the slot, reproducing 322's image to the byte. **`stub_hit=_ZN15OSMetaClassBase10initializeEv` at `xnu_entry_stub_caller=0x8011d8e4` = `OSlibkernInit+0x8`**, `caller-4` = `0x8011d8e0: bl 8012190c <_ZN15OSMetaClassBase10initializeEv>` — the predicted name *and* key, and the first key since 320 whose offset needed **converting** (the `bl` is at object offset 0x844, `OSlibkernInit` starts at 0x840, so the offset is function +0x04 and the key +0x08 — 322's rule in the direction that needs arithmetic). **`.text` 0x13D600 → 0x13E180 is +0xB80 and closes with no residual in five terms** — +0x9AC object, **+0x131** its strings (305 placed of 320: the *smallest* dedup measured yet, after 319's 100% and 321's 63%), +0x030 stub bodies (4 retired, **6 created**, 24 bytes each), +0x080 name strings (the 322 model exact again: created 0xE0 − retired 0x60), −0x00D fill — with the region identity agreeing (Σinputs +0xB8D, Σfill −0xD) and only two inputs in the map moving. **The point estimate missed by 0x154 and one omission pays for all of it: the prediction treated the stubs this step *creates* as nothing** (+0x90 of bodies, +0xE0 of name slots) minus 0xF of strings and 0xD of fill = the miss exactly — and the reason it could happen is that **322 created no stubs at all** (6 resolved, 0 added), so the created half of both terms was zero there and copying that shape forward dropped it silently (the same shape as 316's storage resolution that retired no body). The data block grew by its own arithmetic and did not move: `.data` 0x80140000 (**0x19240**, +0x30 for 0x34 of input — the 4 bytes were absorbed by the section's fill falling 4, **317's rule arriving in `.data`**), `.sysctl_set` 0x80159240, `.init_array` 0x8015934C, `.bss` 0x80159380 (**+0x40 for a one-byte object**, 0x3F of it new fill — the byte pushed the 64-byte stand-in block a slot, **310/313's rule**), `__bss_end` **0x80190C18**, image **1413968**, headroom **1504232**, because `.text` ends 0x1E80 below the 16 KB boundary. Log **301646** bytes, one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, device back on its own (release 10) |
| stage90 | [experiment-324](experiment-324-osmetaclass-a-storage-resolution-and-a-stop-two-frames-inside.md) | **`libkern/c++/OSMetaClass.cpp` — the metaclass machinery, a *storage* resolution the count missed, and a stop two frames inside the C++ runtime** — `libkern/c++/OSMetaClass.cpp` (manifest:370), the object that defines five of the six names 323 added and the largest linked since 318: `.text` **6608** (0x19D0) for 63 `T`, a 4-byte COMDAT `.text._ZN15OSMetaClassMetaD0Ev`, `.rodata` **180**, `.rodata.str1.1` **939**, `.data` **8**, `__DATA, __data` **72**, `.bss` **48**, `.init_array` **4**, **31 references** — `OSMetaClassBase` (`initialize`, 323's stop) and the `OSMetaClass` mod-load path (`preModLoad`, `checkModLoad`, `postModLoad`, `modHasInstance`) with the three static locks. **The function counts are exact and the storage count is one low** — measured **24 resolved / 3 added** against the predicted 23/3, i.e. **810/699/111**, because the prediction intersected the object's 63 `T`s with the stub list and never looked at its `B`/`D`/`R` definitions, which resolve stand-ins too (`_ZN11OSMetaClass9metaClassE` was a 64-byte storage stand-in) — **316's rule in mirror image**. **`.text` 0x13E180 → 0x13FA80 is +0x1900 and closes with no residual in four terms**: +0x1E20 the object (0x19D0 text + 0x4 COMDAT + 0xB4 rodata + **0x398 of 939 strings placed**, the fourth dedup measured), −0x210 stub bodies (23 retired, 1 created), −0x310 name slots (0x354 retired − 0x44 created), **fill +0x000** (0xD1C in both; 55 → 56 fills), with the region identity agreeing (Σinputs +0x1900, Σfill 0). The point estimate missed by **5 bytes while being wrong twice** (19 bytes of strings the pool dropped, one created stub body the body term did not count). `.text` ends **0x580 below the 16 KB boundary — the tightest margin this walk has had — and it held**: `.data` 0x80140000 (**0x19290**, +0x50, exactly the object's 8 + 72 with the fill untouched), `.sysctl_set` **0x80159290**, `.init_array` **0x8015939C** (**0x8**, doubled — an entry nothing runs, since the table keeps its ELF name `.init_array` and `sectionIsConstructor` accepts only `__mod_init_func`/`__constructor`), `.bss` **0x801593C0** (0x378D8, +0x40 from 0x18 object + 0x80 two stand-ins − 0x40 one retired, with the fill falling 0x30), `__bss_end` **0x80190C98**, image **1414052**, headroom **1504104**. **`stub_hit=_ZN12OSDictionary12withCapacityEj` at `xnu_entry_stub_caller=0x8011e468` = `_ZN11OSMetaClass11postModLoadEPv+0xd0`** (`caller-4` = `0x8011e464: bl <_ZN12OSDictionary12withCapacityEj>`, preceded by `mov r0, #40`) — **neither the predicted `devsw_init` nor the alternative it named**, but `postModLoad`'s first call; and the linked body of the caller proves the whole mod-load protocol ran: `OSRuntimeInitializeCPP` looks up the kernel's own kmod by identifier, calls `preModLoad` (`+0x84`), walks segments calling `checkModLoad` (`+0xC8`, a 44-byte gate, not the near-trivial body the prediction assumed), compares each section name against `__mod_init_func` (15) and `__constructor` (13), matches nothing, and calls `postModLoad` on the loop's exit (+0x23C) — **three names 323 created as stubs now really entered in sequence**, since any of them still being a stub would have stopped the walk earlier under its own name. Log **301645** bytes, one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, device back on its own (release 10) |
| stage90 | [experiment-325](experiment-325-osdictionary-the-boundary-crossed-and-a-stop-named-to-the-byte.md) | **`libkern/c++/OSDictionary.cpp` — the 16 KB data boundary crossed for the first time since 321, and the sharpest stop of the walk** — `libkern/c++/OSDictionary.cpp` (manifest:367), the object that defines 324's stop: `.text` **5640** (0x1608) for 47 `T`, a 4-byte COMDAT `.text._ZN12OSDictionary9MetaClassD0Ev`, `.rodata` **240**, `.rodata.str1.1` **53**, `.bss` **24**, `__DATA, __data` **48**, `.init_array` **4**, no plain `.data`, 64 definitions and 47 references. **Every count exact** — **3 resolved / 9 added** (810 → **816** undefined, 699 → **704** function, 111 → **112** storage) with every name read **by kind** on both sides, which is what 324's miss taught: resolved are `OSDictionary::withCapacity` (the stop) and `withDictionary` (functions) plus the `R` storage stand-in `OSDictionary::metaClass`; added are the six `OSCollection` names and `OSSymbol::bsearch` (functions) plus `OSCollection::gMetaClass` `B` and `OSCollection::metaClass` `R` (storage). **`.text` 0x13FA80 → 0x1412E0 is +0x1860 in three terms** — +0x1731 the object (0x1608 + 0x4 COMDAT + 0xF0 rodata + **0x35 of 0x35 strings: the first dedup of the five measured at *zero***), +0x120 the stub object (7 bodies created / 2 retired = +0x78; 7 names created 0xF4 − 2 retired 0x4C = +0xA8), +0x00F fill (0xD1C → 0xD2B; 56 → 57) — and the region identity is exact (Σinputs **+0x1851**, Σfill **+0xF**), so the prediction's point estimate was the inputs term to the byte and only the band differed. **The 16 KB boundary was crossed, the first time since 321**: `.text` ended 0x580 below it and +0x1860 landed at 0x801412E0, so `.data` stepped a whole 0x4000 — **0x80144000 (0x192C0)** exactly as predicted, `.sysctl_set` **0x8015D2C0**, `.init_array` **0x8015D3CC** (**0xC**, a third entry nothing runs), `.bss` **0x8015D400** (0x37958, +0x80 = 0x58 of inputs and the fill **rising** 0x28 instead of absorbing — 324's fell, 325's rose), `__bss_end` **0x80194D58**, image **1430488** (0x15D3D8), headroom **1487528** (it follows `__bss_end` exactly). **`stub_hit=_ZN8OSObjectnwEm` at `xnu_entry_stub_caller=0x8011f8d8` = `_ZN12OSDictionary12withCapacityEj+0x10`** (`caller-4` = `0x8011f8d4: bl 80124ab4 <_ZN8OSObjectnwEm>`, preceded by `mov r0, #32`) — **the predicted symbol, the predicted caller function *and* the predicted offset within it, all three right: the sharpest hit of the walk** (321/323 named name and key but not the offset; 324 named neither). `OSObject::operator new` is still a 24-byte caller-key stub and its real body (`kalloc_canblock` + `assert` + `bzero`, both already real) should return, so the next frontier should be `OSCollection::OSCollection` in `withCapacity`'s own body — a stub this step created. Log **301628** bytes, one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, `xnu_entry_abort_entries=0x00000000`, device back on its own (release 10). Also carries a correction back into the 321/322/323 rows: their image sizes restated in hex (`0x1592C0`, `0x1592D0`) were never recomputed — the span's four printed parts give **0x159320** and **0x159350** |
| stage90 | [experiment-326](experiment-326-osobject-a-step-that-adds-nothing-and-a-frontier-that-still-moves.md) | **`libkern/c++/OSObject.cpp` — a step that adds nothing, an allocator that returns, and a frontier that moves anyway** — `libkern/c++/OSObject.cpp` (manifest:372), the object that defines 325's stop: `.text` **0x408** for 40 definitions, a 4-byte COMDAT `.text._ZN8OSObject9MetaClassD0Ev`, `.bss` **0x1C**, `.rodata` **0x84**, `.rodata.str1.1` **0xA3** (163), `__DATA, __data` **0x18**, `.init_array` **4**, no plain `.data`, and **27 references every one of which the image already defines** — `OSObject` itself (the two allocators over `kalloc_canblock`/`bzero`/`OSAddAtomic`, `OSObject::MetaClass` and `gMetaClass`, `init`/`free`, the ctor/dtor, the retain/release family, `osobject_retain`). **All three counts exact, read by kind: 816 → 800 undefined, 704 → 689 function, 112 → 111 storage — 16 resolved / 0 added, the second step in the walk with an added count of zero** (322 was the first, and the difference is that this zero is written down *as a term* rather than copied forward, which is what cost 323 0x154). `.text` 0x1412E0 → **0x141520** is +0x240 in three terms — +0x533 the object (0x408 + 0x4 COMDAT + 0x84 rodata + **0xA3 of 0xA3 strings: all 163 bytes placed, the second dedup in a row measured at zero**), −0x2F4 the stub object (15 bodies 0x168 + 15 names 0x18C exactly; the created halves are +0x000), +0x001 fill — with the region identity exact (Σinputs **+0x23F**, Σfill +0x1), so the predicted range's top *was* the inputs term and only the strings' placement was in doubt. **Nothing after `.text` moved except by the object's own data** (0x2AE0 of slack below the boundary): `.data` **0x80144000** (**0x192D8**, +0x18 with the fill unmoved), `.sysctl_set` **0x8015D2D8**, `.init_array` **0x8015D3E4** (**0x10**, a fourth entry nothing runs), `.bss` **0x8015D400** (**0x37918**, **−0x40** — inputs −0x24 *and* the fill falling 0x1C, so the retired stand-in handed its whole slot back: 324's case mirrored), `__bss_end` **0x80194D18**, image **1430516** (0x15D3F4, +0x1C exactly as predicted), headroom **1487592**. **`stub_hit=_ZN12OSCollectionC2EPK11OSMetaClass` at `xnu_entry_stub_caller=0x8011f8ec` = `_ZN12OSDictionary12withCapacityEj+0x24`** (`caller-4` = `0x8011f8e8: bl 8012482c <_ZN12OSCollectionC2EPK11OSMetaClass>`) — the predicted name *and* key, read off `withCapacity`'s disassembly before the build; two things a name alone would not prove: **the allocator that stopped 325 ran its whole real body and returned**, and **a step with zero added names still moved the frontier**, because the frontier was sitting inside a function this step made real. Log **301647** bytes, one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, entry counters zero, device back on its own (release 10) |
| stage90 | [experiment-327](experiment-327-oscollection-a-stop-five-real-calls-away.md) | **`libkern/c++/OSCollection.cpp` — a stop five real calls away, and the first stop that is *not* the name the step defines** — `libkern/c++/OSCollection.cpp` (manifest:364), which defines **all seven** of the names 325 created: `.text` **0x194** for 22 definitions, a 4-byte COMDAT `.text._ZN12OSCollection9MetaClassD0Ev`, `.bss` **0x18**, `.rodata` **0xB0**, `.rodata.str1.1` **0x6B** (107), `.init_array` **4**, **no `.data` and no `__DATA, __data` at all**, 31 references — `OSCollection` the base class (`setOptions` a leaf that masks a flag word, `init` no more than `OSObject::init` plus a store, the ctor no more than `OSObject::OSObject` plus a vtable store, plus `haveUpdated`/`copyCollection`/the dtor and the two stand-ins). **All three counts exact — 800 → 793 undefined, 689 → 684 function, 111 → 109 storage (8 resolved / 1 added)** — six function stubs and two storage stand-ins resolved, and `OSReportWithBacktrace` added (referenced only from `haveUpdated`'s `gIOKitDebug`+`panic` path, which nothing on the walk reaches). `.text` 0x141520 → **0x1416A0** is +0x180 in three terms — +0x2B3 the object (0x194 + 0x4 COMDAT + 0xB0 rodata + **0x6B of 0x6B strings: all 107 placed, the third zero dedup in a row**), −0x134 the stub object (6 bodies 0x90 + 6 names 0xD4 retired, less 1 body + 1 name created), +0x001 fill — with the region identity exact (Σinputs **+0x17F** = the top of the predicted range for the *second* step running, Σfill +0x1). **`.data` did not move at all** (0x80144000/0x192D8 — the first object in the walk with neither a `.data` nor a `__DATA, __data`), `.sysctl_set` **0x8015D2D8** unmoved, `.init_array` **0x8015D3E4** (**0x14**, a fifth entry nothing runs), `.bss` **0x8015D400** (**0x37898**, **−0x80**: inputs −0x68 from **two** retired stand-ins and the fill falling a further 0x18 — 324/325/326/327 now give all four signs of the fill's move), `__bss_end` **0x80194C98**, image **1430520** (0x15D3F8, +0x4 exactly as predicted), headroom **1487720**. **`stub_hit=_ZN8OSSymbol17withCStringNoCopyEPKc` at `xnu_entry_stub_caller=0x8011e4a0` = `_ZN11OSMetaClass11postModLoadEPv+0x108`** (`caller-4` = `0x8011e49c: bl 80124e90 <_ZN8OSSymbol17withCStringNoCopyEPKc>`) — the predicted name and key, and **the first stop in the walk that is not the name the step defines**: the walk had to pass `OSCollection::OSCollection` (real this step → `OSObject::OSObject`), `instanceConstructed` (real since 324), the vtable call into the real `initWithCapacity` (whose first instruction is `bl _ZN12OSCollection4initEv`, real this step), and the real `OSDictionary::setOptions(2, 2, 0)` returning **at its first branch** — which the run confirms indirectly, since a non-zero count would have stopped the walk in `OSMetaClassBase::safeMetaCast` or a `blx` per member first. Log **301647** bytes, one `stub_hit=` line, no `exception:` line, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, entry counters zero, device back on its own (release 10) |
| stage90 | [experiment-328](experiment-328-ossymbol-a-real-definition-holding-zero-and-a-stop-that-is-not-a-stub.md) | **`libkern/c++/OSSymbol.cpp` — a real definition holding zero, and the first stop that is not a stub at all** — `libkern/c++/OSSymbol.cpp` (manifest:380), which defines the current stop and also the `bsearch` 325 created: `.text` **0x10A4** for 60 definitions, a 4-byte COMDAT, `__DATA, __data` **0x90**, `.rodata` **0xC0**, `.rodata.str1.1` **0x25** (37), `.bss` **0x1C**, `.init_array` **4**, 54 references — `OSSymbol` the interned-symbol class and `OSSymbolPool` its hash table and lock, with `withCString`/`withCStringNoCopy`/`withString` all going through the pool under a lock. **All three counts exact — 793 → 801 undefined, 684 → 692 function, 109 → 109 storage (6 resolved / 14 added)** — five function stubs and one storage stand-in resolved, thirteen function names and one storage stand-in added (every one of them `OSString`'s, and this is the first step since 323 whose created half is the larger). `.text` 0x1416A0 → **0x1429C0** is +0x1320 in three terms — +0x1184 the object (0x10A4 + 0x4 COMDAT + 0xC0 rodata + **0x1C of 0x25 strings: the pool dropped 9 bytes**, so the predicted range's top was 9 high by exactly that), +0x198 the stub object (13 bodies 0x138 + 13 names 0x18C created against 5 bodies 0x78 + 5 names 0xB4 retired), +0x004 fill (0xD2D → 0xD31, 59 → 60 fills) — with the region identity exact (Σinputs **+0x131C**, Σfill +0x4). `.data` **0x80144000** (**0x19368**, +0x90 exactly the object's own data with the fill unmoved at 0x7AA7/8 fills, the third step running), `.sysctl_set` **0x8015D368**, `.init_array` **0x8015D474** (**0x18**, a sixth entry nothing runs), `.bss` **0x8015D4C0** (**0x378D8**, **+0x40**: inputs +0x1C and the fill **rising** a further 0x24 — 324/325/326/327/328 now give five signs of the fill's move), `__bss_end` **0x80194D98**, image **1430668** (0x15D48C), headroom **1487464**. **And the run reports no `stub_hit=` line at all: the walk ran past the three stubs that ended 325, 326 and 327, entered `withCStringNoCopy` at 327's own key (`_ZN11OSMetaClass11postModLoadEPv+0x108`), and took a `data abort` at `_ZN8OSSymbol17withCStringNoCopyEPKc+0x14`** — `ldr r0, [r0, #16]` (`first_insn=0xe5900010`), `first_dfar=0x00000010`, `first_dfsr=0x00000005`, `first_lr=0x80121c80` — **the null is `_ZL4pool`, the object's own `static OSSymbolPool *` at 0x80193158 in `.bss`**, whose only writer is `str r4, [r6]` at `OSSymbol::initialize()+0x88`, a function with exactly two callers in the image: `OSSymbol::MetaClass::MetaClass()+0x30` (which **nothing** calls) and `_GLOBAL__sub_I_OSSymbol.cpp+0x38` — the sixth entry of a `.init_array` nothing runs, because XNU's own `OSRuntimeInitializeCPP` finds constructors by section name (`__mod_init_func`/`__constructor`) and `entry_macho.s` describes no such section (318). So the prediction (stop `_ZN8OSStringC2EPK11OSMetaClass`, key `withCStringNoCopy+0x50`) was right about the call and the key and was never reached: the body faulted 0x38 earlier, one instruction into `lck_mtx_lock(pool->lock)` — **the walk's first fault rather than a stop, and the frontier has left the undefined-symbol list**: it is now a value the image never writes. Log **301505** bytes, no `stub_hit=` line, one `exception: data abort` line, `xnu_entry_failures=0x00000000`, `xnu_entry_image_bytes=0x0015d48c`, `disarm_hw_watchdog_en=0x00000001`, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, device back on its own (release 10) |
| stage90 | [experiment-329](experiment-329-the-constructor-table-described-to-xnu-and-a-stop-one-instruction-past-the-first-static-constructor.md) | **the constructor table described to XNU under the one name its runtime looks for — and a stop one instruction past the first static constructor this kernel has ever run** — a step that **links nothing**: `entry_macho.s` gains a second `__DATA` section, **`__mod_init_func`**, whose `addr`/`size` are `entry.ld`'s own `__entry_init_array`/`__entry_init_array_size` (`sizeofcmds` 236 → **304**, `__DATA` `cmdsize` 124 → **192**, `nsects` 1 → **2**), because `OSRuntimeInitializeCPP`'s `sectionIsConstructor` (`OSRuntime.cpp:246`) matches constructors **by section name** and then calls every pointer the section holds (`:458-481`) — a scan that really runs in this image (324 watched it match nothing) and whose silence is what made 328's `_ZL4pool` zero. **The counts do not move at all** (801/692/109), and **neither does one byte above the boundary**: `.text` 0x1429C0 → **0x142A00** (+0x40, closing in two measured terms: **+0x44** of header — `xnu_arm_entry_macho.o`'s `.rodata.macho` 0x108 → **0x14C**, in place at 0x8013E5E0 — and **−0x04** of fill, 0xD31/60 → 0xD2D/59), `.data` **0x80144000**/0x19368, `.sysctl_set` **0x8015D368**, `.init_array` **0x8015D474**/0x18, `.bss` **0x8015D4C0**/0x378D8, `__bss_end` **0x80194D98**, image **1430668**, headroom **1487464** — all unmoved, because the span's first term rounds both text ends up to 0x144000. **`stub_hit=__cxa_atexit` at `xnu_entry_stub_caller=0x8011d750` = `OSRuntimeInitializeCPP+0x184`**, whose `caller-4` is the **`blx r0` at 0x8011d74c** — symbol and key both predicted, and the key is not the initializer's own because its transfer into the C++ runtime is a **tail `b`**; one key measures five facts (the section-name match, six constructors counted from the size, the scan's indirect call, `_GLOBAL__sub_I_OSKext.cpp`'s whole body including the real `OSMetaClass::OSMetaClass("OSKext", …)`, and the tail branch into the stub). `__cxa_atexit` is defined **nowhere in XNU**, so no link can fix it: the next step is the compile flag `-fno-use-cxa-atexit`. The host check caught the step's own first build one byte short (`sectname` is 16 bytes, the name is 15) before any device run — `FAIL: load command 2 is not LC_SEGMENT (cmd=0x38000000)`. No `exception:` line, `abort_entries=0x00000000`, `kv_written=0x5d`/`kv_dropped=0`, log **301624** bytes, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, device back on its own (release 10) |
| stage90 | [experiment-330](experiment-330-two-runtime-symbols-defined-for-real-and-the-whole-constructor-table-running.md) | **`__cxa_atexit` and `__dso_handle` defined for real, and the whole constructor table running** — a step that **links nothing** and changes no object: two definitions in `entry_stubs.c`, the C++ runtime's static-destructor registration as a no-op returning success (**a kernel never exits**, so registering a destructor is a successful nothing) and the `__dso_handle` it is passed, signature read off the call sites. **The flag 329 predicted is not this step, and that is a measurement**: all 83 C++ files were compiled with each candidate — `-fno-use-cxa-atexit` **only renames** the call (`b atexit`, which XNU also does not define) while `-fapple-kext` (`MakeInc.def:371`, Apple's own `CXXFLAGS_GEN`) removes it but moves vtable emission to the key function's TU, adding `_ZTV8OSObject`/`_ZTV12OSCollection`/**`_ZTV8OSString`** references (the last defined by an object this image does not link) and ~13 KB of C++ text, moving the 16 KB boundary and re-baselining 324–329 — so the flag is a step of its own, measured before it is taken (`build_xnu_arm_kernel.sh` grew an `XNU_KERNEL_EXTRA_CXXFLAGS` hook, the C++-only twin of the define hook). **Both halves exact**: counts 801/692/109 → **799 undefined / 690 function / 109 storage** (two *function* stubs retired — `__dso_handle` was classified as a function because no kernel object defines it and the generator's kind lookup found nothing, which is how four bytes of data were being passed as an address), `.text` 0x142A00 → **0x1429A0** (−0x60 in four terms, no residual: **+0x08** the two definitions, **−0x30** the two retired stub bodies 0x18 each, **−0x20** their name literals in the merged pool 0x390F → **0x38EF**, **−0x18** fill **0xD2D/59 → 0xD15/57**), and every address above the boundary **identical to the byte** (`.data` **0x80144000**/0x19368 with fill 0x7AA7 → **0x7AA3**, `.sysctl_set` **0x8015D368**, `.init_array` **0x8015D474**/0x18, `.bss` **0x8015D4C0**/0x378D8 with its fill unmoved, `__bss_end` **0x80194D98**, image **1430668**, headroom **1487464**) — a *shrinking* `.text` inside the same 16 KB bucket, which is 329's span rule proved from the other side. 329's image rebuilds byte-for-byte, so the tables were compared directly: of 6875 symbols **none added and none retired**, `__cxa_atexit` 0x801224D0 (stub) → **0x80002CD8**, `__dso_handle` 0x801225C0 (a stub *function*) → **0x80144000** (a real 4-byte object first in `.data`), and **one data symbol in the whole image moves** (`pc_trace_cnt` → +4) while no `.bss` symbol moves at all. **The run stops at 328's own prediction, 0x38 further than 328 could reach**: `stub_hit=_ZN8OSStringC2EPK11OSMetaClass` at `xnu_entry_stub_caller=0x80121cb4` = `_ZN8OSSymbol17withCStringNoCopyEPKc+0x50`, whose `caller-4` is a direct `bl 80126058 <_ZN8OSStringC2EPK11OSMetaClass>`. **328's `data abort` is gone, and its absence is the proof of the step's claim**: `withCStringNoCopy`'s second instruction dereferences `_ZL4pool` and the walk cannot reach +0x50 unless that load succeeded — and the pool's only writer is `OSSymbol::initialize()`, the **sixth** entry of the table. So **all six constructors ran, in order, through `checkModLoad` between them**, and the C++ static-constructor machinery of this kernel is complete. `abort_entries=0x00000000`, `checks=5`, `failures=0`, `kv_written=0x6f` (111, up from 0x5d), log **301642** bytes, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, device back on its own (release 10). Next: link **`libkern/c++/OSString.cpp`** — the object defining this stop and two more stubs in the same body — then the `-fapple-kext` re-baseline, now with its cost measured |
| stage90 | [experiment-331](experiment-331-osstring-o-seventeen-names-and-a-null-whose-only-writer-has-no-caller.md) | **`OSString.cpp` linked, and a stop that is not a symbol at all** — one object (`libkern_c++_OSString.o`, manifest:381: `.text` **2224**, `.rodata` **172**, `.rodata.str1.1` 32, `__DATA,__data` **48**, `.bss` **24**, `.init_array` 4; 43 definitions / 38 references), and **the counts are 799/690/109 → 782/675/107 — 17 resolved, 0 added, not the 15 predicted**: the two extra names are `OSString::gMetaClass` (24 bytes, `.bss`) and `OSString::metaClass` (4, `.rodata`), both carried as **storage stand-ins**, and the prediction tool missed them because `xnu_arm_entry_stubnames.txt` has **two record shapes** (675 `func NAME T` and 107 `data NAME TYPE SIZE`) while its parser admitted `len(f) == 3` — the third time in this walk a hand-written parser of a generated list has been the defect. **`.text` 0x1429A0 → 0x142FE0 closes on +0x640 in five measured terms and a fill, with no residual**: +0x8B0 object text, +0x004 COMDAT, +0x0AC placed `.rodata`, **+0x009** of its 32 string bytes as placed (23 deduped — the smallest placement after 319's 0%), −0x168 fifteen retired stub bodies, −0x1D0 their fifteen name slots, **+0x00F** fill (0xD15/57 → 0xD24/59); the placed half 0x631 is inside the predicted 0x628..0x648 band. **`.data` does not move and grows by exactly the object's own data** (0x19368 → **0x19398**, the two 24-byte `VM_ALLOC_SITE_STATIC` sites at 0x8015D368 and 0x8015D380, fill unchanged 0x7AA3), `.sysctl_set` **0x8015D398**, **`.init_array` 0x18 → 0x1C** — the first growth since 318 — and because its seventh entry ends at exactly 0x8015D4C0 = `align64(...)` **`.bss`'s start does not move**, while its **size falls 0x80** in three measured parts: −0x80 of placed slots (the two stand-ins each occupied a **0x40-byte 64-aligned** slot in `realstubs.o`, whose own `.bss` goes 7172 → 7044), +0x18 free (the real `gMetaClass` lands at **0x8019315C**, in the gap after `_ZL4pool` that was *fill*), and −0x18 fill — so `__bss_end` **0x80194D18**, image **1430720**, headroom **1487592**. 330's image rebuilds byte-for-byte, and of 7798 symbols **none retired / 21 added** (the object's whole public surface, `_ZTV8OSString` among it — the very symbol 330's flag measurement said `-fapple-kext` would need from an unlinked object). **The stop is `exception: data abort` with `dfar=0x0000000c`, `pc_abt=0x8011cebc` = `IORecursiveLockLock+0x8` (`ldr r5, [r0, #12]`), `dfsr=5`, insn `e590500c`, `abort_entries=1`, `kv_written=0` — no `stub_hit` line at all**, called from `_ZN6OSKext24lookupKextWithIdentifierEP8OSString+0x24`: the walker had already reported **no stub on the straight-line path** from `postModLoad` through `OSKextLog` and on to `panic`/`DebuggerTrapWithState`, and the source says why — `sKextLock` (0x80192D90) is `.bss`-zero because **nothing calls `OSKext::initialize()`**: its only caller `iokit_post_constructor_init` has no caller either, being reachable only from `libsa/lastkernelconstructor.c`, an object this image does not link. Same shape as 328's stop, with a different writer: 328's `_ZL4pool` had a writer in the same object whose initialiser nothing ran; this one's writer is in the image and *its* caller is not. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `checks=5`/`failures=0`, log **301505** bytes, device back on its own (release 10). Next: the *call*, not another object |
| stage90 | [experiment-332](experiment-332-the-last-kernel-constructor-a-call-not-an-object.md) | **`libsa/lastkernelconstructor.c`, and a step that is a *call* rather than an object** — the constructor (`libsa/lastkernelconstructor.c:35`) that is the only caller of `iokit_post_constructor_init` (`iokit/Kernel/IOStartIOKit.cpp:89`), which is the only caller of `OSKext::initialize()` (`libkern/c++/OSKext.cpp:628`): 331 said the writer of its NULL was in the image with its only caller outside, and this links the outside. `iokit_post_constructor_init` has been *in* the image all along (0x8011b0ec, 0xb4 bytes) and has never run, because `libsa` is not a component of Apple's ARM manifest at all (722 lines, `grep -c libsa` = 0). **Apple's file cannot be compiled for this target, and both reasons are measured**: the project's own pipeline refuses it with `<inline asm>:2:1: error: unknown directive / .zerofill __LAST, __last, _last_kernel_symbol, 0` — the four-operand `.zerofill` is the **Darwin** assembler's form and this image is ELF — and `_last_kernel_symbol` carries Mach-O's spelling of a C identifier while the reference in `arm_vm_init.o` is the ELF spelling `last_kernel_symbol` (`osfmk/arm/arm_vm_init.c:57` "Denotes the end of xnu", stored at `:499` as `vm_kernel_top`), so a definition under the Darwin name would satisfy nothing. **That pipeline run is a trap worth recording**: its first act is `rm -f "$OUT"/*.o` (`tools/build_xnu_arm_kernel.sh:146`), so a one-line `MANIFEST` deleted the whole 695-object pool and the entry image could not be built again until the full manifest was compiled — a narrow manifest is for measurement, never for a build step. So the object is a translation unit of this image like `entry_stubs.c` (`entry_last_kernel_constructor.c`, same flags) and the second half of Apple's file goes into `entry.ld` as `last_kernel_symbol = __bss_end;`, next to `ExceptionLowVectorsBase` and `ResetHandlerData` and by the same mechanism — pass 1 links with the script, so the name is defined before the stub generator runs and the 24-byte **function** stand-in is retired (`nm` then reports `A 0x80194D58` = `__bss_end`, where `vm_kernel_top` had been the stub's own 0x80123CE4). **1 resolved / 0 added**, and the object is four bytes: `.text.startup` = `b iokit_post_constructor_init` — a **tail branch**, not a `bl` — plus one `.init_array` pointer. **Every section is the prediction's except one sign**: `.text` 0x142FE0 → **0x142FC0**, placed 0x1422BC → 0x142294 (−0x28) plus fill +0x8, closing on **+0x004** (`.text.startup`), **−0x18** (the retired stub body: `realstubs.o` `.text` 16200 → **16176**), **−0x14** (its name slot: `realstubs.o` `.rodata.str1.4` 14111 → **14091** = `align4(18 + 1)`, measured twice over) and **+0x008** fill, with no residual; `.data` (0x80144000/0x19398) and `.sysctl_set` (0x8015D398/0x10C) do not move; **`.init_array` 0x1C → 0x20 at the same start**, and its eight pointers resolve against the image to the seven `_GLOBAL__sub_I_*.cpp` functions in 331's order and then **`last_kernel_constructor` at 0x80122928** — the first entry in that table that is not a `_GLOBAL__sub_I_*.cpp` and the last the scan runs; **`.bss`'s start moves for the first time in the walk** (0x8015D4C0 → **0x8015D500**, because 0x1C was exactly `align64` of itself, so four more bytes cost 0x40) with its size unmoved, `__bss_end` **0x80194D58**, image **1430724** (+4), headroom **1487528** — **−0x40, where the prediction said +0x40**, a sign written by pattern rather than measured (the headroom is the room *below* `topOfKernelData`), which is the family 317 and 321's `created − retired` pair belongs to. The layout checks pass on the moved arithmetic too: `ResetHandlerData − ExceptionLowVectorsBase` is now 0x194D44 with both write addresses inside the reserved `.bss` slot. **The run stops one `bl` into the function this step made reachable**: `stub_hit=_Z15IOCPUInitializev` at `xnu_entry_stub_caller=0x8011b0f4` = `iokit_post_constructor_init+0x08` — the return address of the `bl` at that function's +0x04, its **first** statement, and a direct `bl` so the key names its own call site (329's inherited-`lr` case is the opposite, and this step's own constructor contributes no key at all because its body is a `b`). `why_byte=0x61`, `kv_written=0x65` (non-zero where 331's was 0 — the stub path records before the epilogue runs), `digits=0x38` (a real caller record, unlike 328's and 331's KV-buffer heads), `abort_entries=0x00000000`. **That key resolving inside a function nothing else in the image calls is the measurement of the whole chain**: the scan reached the eighth and last `.init_array` entry, the seven `_GLOBAL__sub_I_*.cpp` entries returned, and nothing faulted through them — which 331's run could not say, because its stop came after all seven. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `checks=5`/`failures=0`, log **301632** bytes, device back on its own (release 10). **Next:** `_Z15IOCPUInitializev`, then the three IOKit `initialize` stubs after it in the same body (+0x0C, +0x14, +0x18), after which `OSKext::initialize()` runs for real — curing 331's NULL at its first five instructions (`IORecursiveLockAlloc` ×2, `IOLockAlloc` ×2, `OSDictionary::withCapacity`, all already real) — and its own sixth call is a container stub; its `+0x2C4` is predicted in advance as a **wrong-*kind* stand-in**: `extern OSBoolean * const & kOSBooleanTrue` is a *reference*, whose ELF storage is the address of its referent, so four zeroed bytes are dereferenced twice (`dfar = 0x0`, `insn = 0xe5912000`) |
| stage90 | [experiment-333](experiment-333-iocpu-o-the-biggest-created-half-and-the-boundary-crossed.md) | **`IOCPU.cpp` linked, and the biggest created half in the walk** — one object (`iokit_Kernel_IOCPU.o`, manifest:301, `.text` **6592**, `.rodata` **1956**, `.rodata.str1.1` **500**, `.bss` **176**, `.init_array` 4, 116 definitions / 280 references), and it is the only object in the pool that defines 332's stop `_Z15IOCPUInitializev`. **9 resolved / 221 added — 781/674/107 → 993/880/113**, exact to the name, and the 221 exist because the object is written against the whole IOKit class hierarchy (`IORegistryEntry::*`, `IOService::*`, `IOInterruptController::*`) which is not linked yet: of them **215 are functions and 6 are storage** (`_ZN21IOInterruptController10gMetaClassE` and `_ZN9IOService10gMetaClassE` at 0x18, plus `gIOInterruptControllersKey`, `gIOInterruptSpecifiersKey`, `gIOServicePlane`, `gPlatformInterruptControllerName` at 4), classified with the generator's own `nm -S` table rather than by eye, and none of the six is dereferenced before the stop — the metaclasses are *addresses* passed to `OSMetaClass::OSMetaClass` (which stores `superClassLink` and never follows it) and the four pointers are written by `IOService::initialize`, the caller's third call. **`.text` closes on +0x5B60 in nine terms with no residual**: +0x19C0 object text, +0x008 the two COMDATs, +0x7A4 `.rodata` placed whole, **+0x104** of 0x1F4 string bytes as placed (0xF0 deduped), −0x0D8 nine retired stub bodies, −0x0E0 their name slots, +0x1428 **215 created stub bodies**, +0x2688 their name slots, **−0x008** fill (0xD2C → 0xD24, 59 records both builds) — and the four stub terms are checked twice because `realstubs.o` measures them separately (`.text` 16176 → **21120** = +0x1350 = +0x1428 − 0x0D8; `.rodata.str1.4` 14091 → **23731** = +0x25A8 = +0x2688 − 0x0E0). **`.text` crossed the 16 KB boundary** (0x80148B20), so `.data` steps to **0x8014C000** (+0x8000) with its size, its fill 0x7AA3 and its placed total 0x118F5 all unmoved — the content is untouched, only the address — and everything after it follows by the same 0x8000; **`.init_array` 0x20 → 0x24**, nine entries with #8 `_GLOBAL__sub_I_IOCPU.cpp` at 0x80124248 and #9 `last_kernel_constructor` at 0x801242F0, which is the order the step depends on; **`.bss` +0x240 in three measured parts** — +0x180 from six new stand-in **slots** (`realstubs.o`'s own `.bss` 7044 → **7428**, the 0x18/0x4 names each buying a 0x40 slot exactly as 331 measured), +0x0B0 the object's own, +0x010 fill (0x109 → 0x119); `__bss_end` **0x8019CF98**, image **1463496** (+0x8004), headroom **1454184** (−0x8240 = the `__bss_end` move). Of 7820 symbols **0 retired / 304 added** (a resolved stub name does not disappear, it is replaced). **The stop is inside the newly real body, not after it**: `stub_hit=_ZN7OSArray12withCapacityEj` at `xnu_entry_stub_caller=0x80122c58` = `_Z15IOCPUInitializev+0x1c`, whose `caller-4` is the predicted `bl` at +0x18 — so the key measures that `iokit_post_constructor_init`'s first call is real and returned, that `IOLockAlloc` returned, and that the store between the two calls (`str r0, [r1]` into the object's own `_ZL11gIOCPUsLock`) executed; `why_byte=0x61`, `kv_written=0x6c`, `digits=0x3f`, `abort_entries=0x00000000`. One size in this step was read in the wrong base and caught before the build: `nm -S -P` prints **hex**, so `184` was 0x184 and the function is 388 bytes — the check is the distance to the next symbol, as since 307. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `checks=5`/`failures=0`, log **301639** bytes, device back on its own (release 10). **Next:** `libkern/c++/OSArray.cpp` — the container both paths now stop on |
| stage90 | [experiment-334](experiment-334-osarray-o-the-container-both-paths-stop-on-and-a-stop-one-call-forward.md) | **`libkern/c++/OSArray.cpp`, the container both paths stop on, and a stop one call forward in the same frame** — `libkern_c++_OSArray.o` (manifest:362), the only object in the pool that defines 333's stop, and the **first step of the whole walk with a zero created half**: **3 resolved / 0 added** (two function stubs, `withCapacity` — 333's stop — and `withArray`, plus one storage stand-in, `OSArray::metaClass`), because all 41 of its references are already satisfied by objects linked in 324/326/327/330/331. **993 → 990 undefined, 880 → 878 function, 113 → 112 storage**, exact to the name. `.text` **+0xDC0** (0x148B20 → 0x1498E0) closing in seven terms with no residual: the object's **+0xD48** text, **+0x004** COMDAT, **+0x0E0** `.rodata` placed whole, **+0x008** of its 0x0E string bytes (six deduped into the pool — the row that was written "or less" and came out at 8), **−0x030** for two retired stub bodies, **−0x038** for their two name slots (`align4(26+1)` twice, a *fourth* confirmation of 322's rule), and **−0x00C** of fill; `realstubs.o` measures the two stub terms independently at exactly −0x30 and −0x38. **The 16 KB boundary 333 crossed was not crossed again**: `.text` ends 0x801498E0, 0x2720 below it, so `.data` stays at 0x8014C000 and grows only its own +0x30 `__DATA,__data`; `.sysctl_set` and `.init_array` move +0x30 with it, and **`.bss`'s start does not move at all** — `.init_array`'s new end 0x801654FC rounds up to the 0x80165500 `.bss` already started at, so the growth vanished into alignment (312's rule, absorbing in the opposite direction from 331). `.bss` itself **−0x40** in two measured parts: **−0x28 placed** (the retired stand-in frees its **0x40-byte 64-aligned slot**, not the 4 bytes it held, while the object's own `gMetaClass` 0x18 arrives at 0x8019B268) and **−0x18 fill**; the headroom *rose* 0x40 because it is the room below `topOfKernelData` and the image got shorter. `.init_array` **0x28, ten entries**, `_GLOBAL__sub_I_OSArray.cpp` at #9 and `last_kernel_constructor` still #10 — the order the step depends on, since the class constructor must run before the call it enables. **The stop is *outside* the object it links**, which is the point of the step: `withCapacity` returns and so does the rest of `IOCPUInitialize`, so the walk lands one call forward in the *same* frame as 333's key — **`stub_hit=_ZN15IORegistryEntry10initializeEv` at `xnu_entry_stub_caller_v=0x8011b0f8` = `iokit_post_constructor_init+0xc`** (`caller-4` = `0x8011b0f4: bl 80128d24`), with `abort_entries=0`. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no exception line, device back on its own on `MI 4LTE` release 10 |
| stage90 | [experiment-335](experiment-335-ioregistryentry-o-the-registry-api-in-one-step-and-a-stop-the-prediction-called-unreachable.md) | **`iokit/Kernel/IORegistryEntry.cpp` — the registry API in one step, the 16 KB boundary crossed again, and a stop the prediction called unreachable** — `iokit_Kernel_IORegistryEntry.o` (manifest:339), the only object in the pool that defines 334's stop, and the largest single step of the walk: 175 definitions against 75 references, **67 resolved / 5 added**, so 990/878/112 → **928/815/113**. The undefined count is exact and the *kind* split is off by one in each direction: one of the five added names is **storage**, not a function — `_ZN10OSIterator10gMetaClassE`, because `libkern_c++_OSIterator.o` (in the 695-object pool, not in the link) defines it `B 0x18`. **The cause is which population each side looked in** — the generator classifies by `nm -S` over the whole pool, the prediction tool by the object being linked, where the name is undefined. `tools/entry_object_effect.py` now scans the pool once (25176 names, 0.5 s) and classifies as the generator does, verified against this step's own refutation. A one-name kind error is worth a **0x40 `.bss` slot**: the created stand-in is why `realstubs.o`'s `.bss` goes 7364 → **7428**. `.text` **+0x3700** (0x1498E0 → **0x14CFE0**) closing in **eight** terms with no residual (+0x47AC object, +0x00C COMDATs, +0x2B8 `.rodata`, **+0x0CC** of 0x0DF string bytes as placed, −0x648 67 retired bodies, −0xEBC their name slots, +0x060 four created bodies, +0x060 their slots) with the fill 0xD18 → **0xD20**; `realstubs.o` measures the four stub terms again at exactly −0x5E8 and −0xE5C. **The 16 KB boundary crossed for the second time in three steps**: `.text` ends past 0x8014C000 so `.data` steps a whole 16 KB to **0x80150000** (0x193D8 — the object's own +0x8 *and* the fill +0x8, the only place in the walk where a `.data` contribution did not fit in the fill already there) and `.sysctl_set` **0x801693D8**, `.init_array` **0x801694E4** (**0x2C, eleven entries**, `_GLOBAL__sub_I_IORegistryEntry.cpp` at #10 and `last_kernel_constructor` still #11) follow. `.bss`'s start moves by 332's idiom rather than the boundary (**0x80169540** = `align64` of `.init_array`'s end) and the section is **+0xC0**: +0x7C the object's own `.bss`, +0x40 the created storage slot, +0x4 fill; `__bss_end` **0x801A1058**, image **1479952**, headroom **1437608**. **The prediction's stop was `iokit_post_constructor_init+0x14` on `IOService::initialize`; the run says otherwise** — `stub_hit=_ZN20OSCollectionIterator14withCollectionEPK12OSCollection` at `xnu_entry_stub_caller_v=0x80128680` = `_ZNK15IORegistryEntry7inPlaneEPK15IORegistryPlane+0x4c`, through `setProperty("IORegistryPlanes", gIORegistryPlanes)` → `setProperty(const OSSymbol*, OSObject*)` → `inPlane(NULL)` (slot 0xd0), which the source has as `coll && inPlane()` with `gIORegistryPlanes` **being** an `OSCollection`. The closure walk was wrong because it followed `R_ARM_CALL` only and its vtable-root resolver returned `None` for **all five** patch roots — it printed a four-entry root list where it should have had nine and the prediction was written anyway: **a walker that can silently drop edges cannot be made safe by printing what it kept**. The stop name was on the ledger's own list of ten still-stubbed callers, under a sentence declaring them unreachable. Also the first successful **dynamic cast in real XNU C++ at kernel level** this walk has run. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, no exception line, device back on its own on `MI 4LTE` release 10 |
| stage90 | [experiment-336](experiment-336-oscollectioniterator-o-a-stop-on-the-stub-335-created.md) | **`libkern/c++/OSCollectionIterator.cpp` — the smallest count change of the walk, and a stop on the stub 335 created** — `libkern_c++_OSCollectionIterator.o` (manifest:365), the only object in the pool defining 335's stop, and the first step whose **kind split was predicted with the pool-aware classifier** the 335 defect produced (and the first where that prediction was right): **1 resolved / 0 added**, 928/815/113 → **927/814/113** — the single function stub `withCollection`, all 34 references already satisfied. `.text` **+0x4C0** (0x14CFE0 → **0x14D4A0**) closing in **six** terms with no residual (+0x464 object, +0x004 COMDAT, +0x094 `.rodata` placed whole, **+0x015** — all 21 string bytes, the first step since 328 with **nothing deduped** — −0x018 one retired body, −0x03C its one name slot) with the fill 0xD20 → **0xD23**; `realstubs.o` confirms `.text` 19560 → **19536** and `.rodata.str1.4` 19999 → **19939**, with its `.bss` **unmoved at 7428** (the section that moved 0x40 in 335, because this step resolves one *function* stub and touches no storage). **The prediction's slot term said 0x38 and the answer is 0x3C — the name is 58 characters and the ledger said 55**; the range 0x8014D48C..0x8014D4A1 still contained the measured 0x8014D4A0, only because four bytes are smaller than the string uncertainty the term was added to. `.text` ends 0x8014D4A0, **0x2B60 below the boundary**, so `.data` does not move for the first time in four steps: **0x80150000** (0x19408, +0x30 placed with the fill *unmoved* at 0x7AAB — 334's shape, not 335's), `.sysctl_set` **0x80169408**, `.init_array` **0x80169514** (**0x30, twelve entries**, `_GLOBAL__sub_I_OSCollectionIterator.cpp` at #11, `last_kernel_constructor` still #12), `.bss` **0x80169580** (the `align64` of `.init_array`'s end — 332's idiom for the fourth step running) sized **0x37B58** (+0x18 placed, +0x28 fill), `__bss_end` **0x801A10D8**, image **1480004**, headroom **1437480**. **The stop was predicted from the object *and* checked independently before the device run** with `xnu_entry_callwalk.py`, which walks direct calls *and tail branches* and reports the indirect calls it cannot follow rather than walking past them (its guarded list runs through `zalloc_internal`): it named the same `_ZN10OSIteratorC2EPK11OSMetaClass`. The run agrees exactly — `stub_hit=_ZN10OSIteratorC2EPK11OSMetaClass` at `xnu_entry_stub_caller_v=0x80129954` = `_ZN20OSCollectionIterator14withCollectionEPK12OSCollection+0x24` (`caller-4` = `0x80129950: bl 8012d6e0`), the symbol **and** the offset both predicted — and reaching +0x24 measures the `OSObject::operator new` path through `kern_os_malloc`/`zalloc` returning 24 bytes. **The first step of the walk whose frontier is a stub the walk itself created** (335 added it). 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, no exception line, device back on its own on `MI 4LTE` release 10 |
| stage90 | [experiment-337](experiment-337-ositerator-o-the-stub-335-created-and-the-frame-that-unwound.md) | **`libkern/c++/OSIterator.cpp` — the stub 335 created, the frame that unwound, and a `.bss` term that is exact for the first time** — `libkern_c++_OSIterator.o` (manifest:368), the only object in the pool defining 336's stop: **3 resolved / 0 added**, exactly the **three names 335 added** (two functions, `OSIterator::OSIterator` and `~OSIterator`, plus one storage stand-in `OSIterator::gMetaClass` 0x18), all 28 references already satisfied — 927/814/113 → **924/812/112**, exact to the name. `.text` **+0x100** (0x14D4A0 → **0x14D5A0**) closing in **six** terms with no residual (+0x0BC object, +0x004 COMDAT, +0x090 `.rodata` placed whole, **+0x00B** — all 11 string bytes, nothing deduped — −0x030 two retired bodies, −0x038 their two name slots) with the fill 0xD23 → **0xD30**; the two slot terms are the two names **counted**, `align4(33+1)` = **0x24** and `align4(19+1)` = **0x14**. **The printed range was 0x20 low — its own six rows sum to +0xE8..+0xF3 and it said +0xC8..+0xD3 — so the measured end 0x8014D5A0 landed 0x2D above the top of it**: 336's defect in a different column (there a name length was eyeballed, here the table's own sum was never taken), generalizing 336's rule to *the range a prediction prints should be the sum of the rows above it*. `realstubs.o`, whose `size` `text` column reads **39371** = `.text` + `.rodata.str1.4` (a *total* read as a component), measures the three stub terms by section: `.text` 19536 → **19488** (−0x30), `.rodata.str1.4` 19939 → **19883** (−0x38), `.bss` 7428 → **7364** (−0x40, one retired 64-byte slot). `.text` ends **0x2A60 below the boundary**, so `.data` is unmoved for the second step running: **0x80150000** (0x19408), `.sysctl_set` **0x80169408**, `.init_array` **0x80169514** (**0x34, thirteen entries**, `_GLOBAL__sub_I_OSIterator.cpp` at #12, `last_kernel_constructor` still #13), `.bss`'s start **0x80169580** again (the `align64` of `.init_array`'s end — 332's idiom for the third step running) and its size **0x37B18**: placed 0x37A2B → **0x37A03** = **−0x28**, the **first exact storage term** of the walk — a retired stand-in gives back its full 0x40 slot for the 0x18 it held and the object's own `gMetaClass` (0x8019F394, the same 0x18) arrives in its place, 0x40 out and 0x18 in, 331's rule a fifth time and the first where it is arithmetic on two known sizes — with the fill 0x12D → **0x115** (−0x18); `__bss_end` **0x801A1098**, image **1480008**, headroom **1437544**. **The stop is the prediction 335 made one step too early**: `stub_hit=_ZN9IOService10initializeEv` at `xnu_entry_stub_caller_v=0x8011b100` = **`iokit_post_constructor_init+0x14`** (`caller-4` = `0x8011b0fc: bl 8012dc74`), symbol, function and offset all predicted and — **for the first time, confirmed before the run** — named identically by `xnu_entry_callwalk.py`, the tool that failed in 335, run from `iokit_post_constructor_init`; run from `inPlane`, the frame 335 died in, it now reports **no stub on the straight-line path at all**. The key is two calls forward in 334's frame, so reaching it measures that `IORegistryEntry::initialize` **returned** — the whole registry chain (iterator construction through the new `OSIterator::OSIterator`, `getNextObject`, the dictionary store) ran and unwound with `abort_entries=0`, so this step measured the *completion* of the registry path, not one more symbol. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, no exception line, device back on its own on `MI 4LTE` release 10 |
| stage90 | [experiment-338](experiment-338-ioservice-o-the-largest-object-of-the-walk-and-a-stop-0x4d4-bytes-in.md) | **`iokit/Kernel/IOService.cpp` — the largest object of the walk, `.data` three boundaries away, and a stop 0x4D4 bytes into the biggest initializer in IOKit** — `iokit_Kernel_IOService.o` (manifest:339), the only object defining 337's stop and **nearly four times** 335's registry entry: 553 definitions against 278 references, `.text` **49996** (0xC34C, 176 functions), nine COMDATs, `.rodata` 3260, `.rodata.str1.1` **4308** (the walk's largest string input), `.bss` 573, **no `.data` of any kind**. **102 resolved / 27 added** (94 function + 8 storage out, 21 function + 6 storage in) → **849/739/110**, all three exact. `.text` **+0xCE80** (0x14D5A0 → **0x8015A420**) closing in **eight terms with no residual on the largest object yet** — +0xC34C object, +0x024 nine COMDATs, +0xCBC `.rodata` placed whole, **+0xFE8** of 0x10D4 string bytes (236 deduped — inside the predicted 0x000..0x10D4 range), **−0x8D0** 94 retired bodies, **−0xEEC** their 94 slots, **+0x1F8** 21 created bodies, **+0x42C** their 21 slots; fill 0xD30 → **0xD2C**. `realstubs.o` measures the terms by section: `.text` 0x4C20 → **0x4548** (−0x6D8 = 0x18 × 73), `.bss` 0x1CC4 → **0x1C44** (−0x80 = 2 net slots × 0x40), and `.rodata.str1.4` 0x4DAB → **0x42EC** — **−0xABF where the model says −0xAC0, and the byte is a tail artifact**: `Σ align4(len+1)` over each build's function stub names is **19884** (812 names) and **17132** (739 names), reproducing each *absolute* size to ≤1 byte with the name sets differing by exactly 0xAC0, so the unpadded byte sits on the 337 end — the check is the sum over the *names*, not the difference over the *sections* (322's rule). **`.data` steps three 16 KB boundaries at once to 0x8015C000** (+0xC000, size 0x19408 and fill 0x7AAB **unmoved in both builds**) — the first step where that was a *choice between two boundaries*, so the prediction stated the margin: `.text` would have had to grow another **0x5BE0** for 0x80160000 to be the answer. `.sysctl_set` **0x80175408**, `.init_array` **0x80175514** (**0x38, fourteen entries**, `_GLOBAL__sub_I_IOService.cpp` at #13 / 0x80135E00, `last_kernel_constructor` still #14), `.bss` **0x80175580** (the `align64` of `.init_array`'s end, 332's idiom a fourth time) sized **0x37CD8** — and its **placed bytes are exact for the second step running**: **+0x1BD** = **−0x200** (eight retired 64-byte slots) **+0x23D** (the object's own `.bss`) **+0x180** (six created slots), with fill 0x115 → **0x118**; `__bss_end` **0x801AD258**, image **1529164**, headroom **1387944**. **Two *derived* rows were wrong and neither was a term**: `image` (predicted ~1529268, i.e. `.bss` start + 0x34 instead of `__init_array_end`) and `__bss_end`/headroom (predicted ~0x801AD140, the placed end with the fill left out) — the same defect 337 recorded, applied to a derivation rather than a sum, and the fix is to name the expression rather than the number. **The stop is the longest straight line this ledger has ever bet on**: predicted from the object at `_ZN9IOService10initializeEv+0x4D8` after **51 calls of only three symbols** (`makePlane` ×2 since 335, **48** × `OSSymbol::withCStringNoCopy` since 331, one `OSDictionary::withCapacity` since 330) and confirmed before the run by `xnu_entry_callwalk.py` from both `_ZN9IOService10initializeEv` and `iokit_post_constructor_init` (the first pre-run walk to resolve a stop two frames below its root). The run: **`stub_hit=_ZN6OSData15withBytesNoCopyEPvj`** at `xnu_entry_stub_caller_v=0x8012a9b0` = **`_ZN9IOService10initializeEv+0x4d8`** (`caller-4` = `0x8012a9ac: bl 8013a0a4`), `abort_entries=0`, all three readings agreeing — reaching it measures 52 calls and their transitive bodies returning, including the two registry planes and the 48 interned key symbols the rest of IOKit matches on. Next: `libkern/c++/OSData.cpp` — where the call passes `&gIOConsoleUsersSeed`, a **four-byte file-static inside this image's own `.bss`** (`IOService.cpp:387`), and `withBytesNoCopy` *shares* it (`OSData.cpp:161`), freeing nothing because the two-argument form supplies no dealloc function (`OSData.cpp:200-213`). 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, no exception line, device back on its own on `MI 4LTE` release 10 |
| stage90 | [experiment-339](experiment-339-osdata-o-a-stop-computed-from-the-caller-and-a-dropped-row.md) | **`libkern/c++/OSData.cpp` — the object 338 stopped on, a stop computed from the caller, and one dropped row that moved four addresses** — `libkern_c++_OSData.o`, the only object defining 338's stop: **5 resolved / 0 added** (the four `with*` constructors plus `OSData::metaClass`), all 46 references already satisfied — the walk's **second zero created half** (334 was the first) — for 849/739/110 → **844/735/109**, all three exact. `.text` **+0xF40** (0x15A420 → **0x15B360**) closing in **six** terms with no residual (+0xEEC object, +0x004 COMDAT, +0x0D0 `.rodata` placed whole, **+0x048** of 0x61 string bytes, −0x060 four retired bodies, −0x074 their four slots `align4(31+1)`/`align4(26+1)`/`align4(24+1)`/`align4(25+1)` = 0x20+0x1C+0x1C+0x1C), fill 0xD2C → **0xD38**; `realstubs.o` confirms all three retired terms by section **to the byte** — `.text` 0x4548 → **0x44E8** (−0x60), `.rodata.str1.4` 0x42EC → **0x4278** (−0x74), `.bss` 0x1C44 → **0x1C04** (−0x40). **One row of the object table was silently dropped and it moved four addresses**: the section summary (built from `objdump -h` with a fixed three-field parse) lost the object's **`__DATA, __data` of 0x60** because a Mach-O-style name contains a *space* and shifts every field, the `except: continue` turning `int('__data', 16)` into a skip — so the prediction said "no `.data` at all" and the build says `.data` **0x19408 → 0x19468** (+0x60 placed, fill **unmoved** at 0x7AAB), pushing `.sysctl_set` to **0x80175468**, `.init_array` to **0x80175574** (**0x3C, fifteen entries**, `_GLOBAL__sub_I_OSData.cpp` at #14) and `.bss` to **0x801755C0** (+0x40 rather than +0x60, because `align64` of `.init_array`'s new end 0x801755B0 rounds up); `__bss_end` **0x801AD298**, image **1529264**, headroom **1387880**. The same defect class as the stub-name list's two record shapes (331): **a parser that drops a whole record class cannot report that it did**, and the check is to take the size from the end of the `objdump -h` line rather than from a fixed field count. **`.bss` placed is exact for the third step running** — 0x37BC0 → **0x37B98 = −0x28** (−0x40 one retired 64-byte slot + 0x18 `OSData::gMetaClass`) — and here the **fill went +0x28, exactly the placed term's magnitude, so the section's size is unmoved at 0x37CD8 for two different layouts**: the first exact cancellation in the walk, and a reminder that `.bss`'s size is a worse progress indicator than its placed bytes. **The stop is not in the object being linked**: `withBytesNoCopy` is `operator new` + `OSObject` ctor + `instanceConstructed` (all real) and a vtable dispatch to `OSData::initWithBytesNoCopy`, whose body is `OSObject::init` (real) and three stores (`bytes`, `length`, `−1` → `capacity = EXTERNAL`) — so it returns, and the prediction was the **next call in `IOService::initialize`**, computed from the caller's disassembly (`0xcfc − 0x7bc = 0x540`, return +0x544) and confirmed before the run by `xnu_entry_callwalk.py` from `_ZN9IOService10initializeEv` (and from `withBytesNoCopy` it correctly reports *no* stub, listing its two `blx` rather than walking past them). The run: **`stub_hit=_ZN12OSOrderedSet12withCapacityEjPFlPK15OSMetaClassBaseS2_PvES3_`** at `xnu_entry_stub_caller_v=0x8012aa1c` = **`_ZN9IOService10initializeEv+0x544`** (`caller-4` = `0x8012aa18: bl 8013ad24`), `abort_entries=0` — symbol and offset both as predicted. The key moving one call forward in 338's frame is the measurement: `OSData::withBytesNoCopy` ran to completion (`new` returning 28 bytes, the vtable dispatch, `init` returning true so the stores happened) and the returned `OSData` was stored into `gIOConsoleUsersSeedValue`; a failure would have taken the `bne` to the release-and-return-0 arm. Next: `libkern/c++/OSOrderedSet.cpp` — the call is `gJobs = OSOrderedSet::withCapacity(10)` (`IOService.cpp:418`, `mov r0, #10` / `mov r1, #0` in front of it), the single-argument form with the **default** comparator, so 340 is where the walk stops building key symbols and starts building the **containers** IOKit keeps services in. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, no exception line, device back on its own on `MI 4LTE` release 10 |
| stage90 | [experiment-340](experiment-340-osorderedset-o-the-first-abort-in-eight-steps-and-the-zero-332-predicted.md) | **`libkern/c++/OSOrderedSet.cpp` — the first abort in eight steps: a stand-in's *value* stops the boot, and 332 had written this zero down in advance** — `libkern_c++_OSOrderedSet.o`, the only object defining 339's stop, and the object behind `gJobs = OSOrderedSet::withCapacity(10)` (`IOService.cpp:418`, `mov r0, #10` / `mov r1, #0` in front of the call — the *default* comparator, so this is the ordered container IOKit keeps its job and probe lists in, not the match-ordering machinery): **2 resolved / 0 added** (`withCapacity` and `OSOrderedSet::metaClass`), all 42 references satisfied and — the check that made the stop prediction possible — **every one of the 42 checked against `xnu_arm_entry_stubnames.txt` by name, all real rather than stubs**, for 844/735/109 → **842/734/108**, all three exact. `.text` **+0xB00** (0x15B360 → **0x15BE60**) closing in **six** terms with no residual (+0xA74 object, +0x004 COMDAT, +0x0E8 `.rodata` placed whole, **+0x00D** — all 13 string bytes placed, the first full string term of the walk, no dedup — −0x018 one retired body, −0x044 its slot, the counted `align4(64+1)` for the longest stub name the walk has retired); placed 0x15A628 → **0x15B139** (+0xB11), fill 0xD38 → **0xD27** (−0x11); `realstubs.o` confirms all three terms to the byte — `.text` 0x44E8 → **0x44D0**, `.rodata.str1.4` 0x4278 → **0x4234** (−0x44), `.bss` 0x1C04 → **0x1BC4** (−0x40). `.text` ends at the *bottom* of the predicted range, 0x8015BE60, **0x1A0 below the 0x8015C000 boundary**, so `.data` stays at 0x8015C000 and grows **+0x30** (0x19468 → **0x19498**). **One derived row had a sign error and it produced five wrong rows**: `.data` growing by 0x30 means every later section starts 0x30 *later*, and the table moved `.sysctl_set` (**0x80175498**, 0x10C), `.init_array` (**0x801755A4**, **0x40, sixteen entries**) and `.bss` (**0x80175600**) 0x30/0x30/0x40 *earlier* — the prediction's *argument* (`.init_array`'s end rounding up to `.bss`'s start) was right and the deltas it was applied to were reversed; `__bss_end` **0x801AD298** and headroom **1387880** are unchanged from 339 (`.bss`'s start +0x40, its size −0x40), image **1529316**. `.bss` placed **0x37B98 → 0x37B70 = −0x28** for the fifth step running (−0x40 one retired 64-byte slot + 0x18 `OSOrderedSet::gMetaClass`) but the fill moved **−0x18** (0x140 → 0x128), so this time the two halves did **not** cancel and the size moved the whole 0x40 (0x37CD8 → **0x37C98**) — 339's cancellation was the exception, not the rule. **The run did not stop on a stub at all**: `MI4IOS6_STAGE90_XNU real XNU entry: exception: data abort`, `xnu_entry_why_byte=0x00000065` (`'e'` — the first byte of `"exception: ..."`, a one-character telegraph of the stop's *kind*), `xnu_entry_stub_caller_v=0x00000000` (no stub entered), `abort_entries=0x00000001`, **`abort_first_dfar=0x00000000`**, `pc=0x8012aaa0`, `dfsr=0x00000005`, `lr=0x8012aaa8`, `insn=0xe5952000` = `ldr r2, [r5]`. `r5` is loaded 0x1C bytes above by `ldr r5, [r1]` with `r1 = 0x801AC940` = **`kOSBooleanTrue`**, one of the 108 storage stand-ins (`data kOSBooleanTrue R 0x4`, `kOSBooleanFalse` next at 0x801AC980, exactly 0x40 away) — so `r5 = 0` and the next load faults **at address zero**. Two loads for one name because `OSBoolean.cpp` declares `OSBoolean * const & kOSBooleanTrue = gOSBooleanTrue`, a **reference**: the symbol is the reference *variable*, the first load fetches its referent and the second the `OSBoolean *` itself, and a stand-in gives that variable its right address and its right 4 bytes **and no value whatever**. Source: `IORegistryEntry::getRegistryRoot()->setProperty(gIOConsoleLockedKey, kOSBooleanTrue);` (`IOService.cpp:427`, the line after the `thread_call_allocate` whose result the `str` 0xC above performs). **This is frontier kind 2 — an invented zero — and the first one this walk had written down in advance** — and the tools' blind spot stated plainly: `callwalk.py` and `first_stub_call.py` answer "is this name in the image", and a zero-valued stand-in *is* in the image; the prediction's falsifier (`abort_entries=` non-zero ⇒ a `blx` faulted) fired while being wrong in the informative way, since the fault is a **global load**, the one instruction class the model treats as free. The fault is 0x84 bytes before the end of a function the walk had already entered twice, so the key at 339 (`+0x544`) and this fault (`+0x5C8`) are the same frame and everything between them — the job list, busy lock, console-users lock, semaphore and `thread_call_allocate` — ran and returned; only the store of `kOSBooleanTrue` into the registry failed, on the value rather than the API. Safety exact: non-persistent `fastboot boot`, nothing flashed, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log **301505** bytes, device back on Android on its own on `MI 4LTE` release 10 — **an abort is an instrument outcome, not a hang**. Next: `libkern/c++/OSBoolean.cpp`, the object that defines the zero — 4 resolved (1 function, `OSBoolean::withBoolean`, a stub since 335, and 3 storage) / **1 added** (`_ZN11OSSerialize15binarySerializeEPK15OSMetaClassBase`, verified absent from the stub-name list rather than assumed) → 842 → **841**/734/**105**; its constructor `_GLOBAL__sub_I_OSBoolean.cpp` runs `OSBoolean::initialize()`, which allocates the two instances and stores them into `gOSBooleanTrue`/`gOSBooleanFalse` — **the store that makes the reference non-zero**, and the first time in this walk that the fix is not "resolve the symbol" but "initialize the value" |
| stage90 | [experiment-341](experiment-341-osboolean-o-the-zero-is-cured-and-the-stop-340-could-not-reach.md) | **`libkern/c++/OSBoolean.cpp` — the zero is cured, and the first step whose fix is "initialize the value" rather than "resolve the symbol"** — `libkern_c++_OSBoolean.o`, the only object that defines `kOSBooleanTrue` (340's `dfar=0` zero) and the only one that writes it: **4 resolved / 1 added** (`OSBoolean::withBoolean`, a stub since 335, and the three storage stand-ins out; `_ZN11OSSerialize15binarySerializeEPK15OSMetaClassBase` in, verified absent from the stub-name list) → 842/735/109 → **839**/734/**105**, the two kind columns exact and the *derived* total two low (842 − 4 + 1 = 839; the prediction counted the created stub as a second retirement). The cure is **two-level**: `kOSBooleanTrue` is `R 0x4` in the object — a reference word the link relocates to `&gOSBooleanTrue`, so the load that returned 0 now returns a valid address — and the second load is cured by `_GLOBAL__sub_I_OSBoolean.cpp`, whose `bl` at object offset 0x70 calls `OSBoolean::initialize()`, which stores the two instances into the `.bss` statics; that constructor sits in `.init_array` before `last_kernel_constructor`, so XNU's own scan (329) runs it before the call that faulted. `.text` **+0x3E0** (0x15BE60 → **0x15C240**) with the six-term table **omitting the created body** — the prose said "one body in, one body out" and the table listed only the −0x20 retirement, which is exactly the 0x20 the placed move (**+0x3DF**, 0x15B139 → **0x15B518**) came in high by; `realstubs.o` measures the omission as its `.text` unchanged at **0x44D0**, and confirms the other two terms to the byte (`.rodata.str1.4` 0x4234 → **0x424C** = −0x20 + 0x38, `.bss` 0x1BC4 → **0x1B04** = −0xC0). **The 16 KB boundary moved for the third time and for the first time by construction**: `.text` ended 0x240 past 0x8015C000, so `.data` stepped a whole 0x4000 to **0x80160000** with its size and fill unmoved, and `.sysctl_set` **0x80179498**, `.init_array` **0x801795A4** (**0x44, seventeen entries**) and `.bss` **0x80179600** followed; `.bss` placed exact at **−0xA0** (−0xC0 three retired slots + 0x20 this object's own) with fill **0x108** read rather than predicted; `__bss_end` **0x801B11D8**, image **1545704** (exact), headroom **1371688**. **The run: `stub_hit=_ZN11IOCatalogue10initializeEv`** at `xnu_entry_stub_caller_v=0x8011b104` = **`iokit_post_constructor_init+0x18`** (`caller-4` = `0x8011b100: bl 8013b9bc`), **`abort_entries=0x00000000`** — the prediction 340 wrote and its abort pre-empted by one instruction, matched name and key. `abort_entries=0` is the cure measured: it requires the relocation, the constructor running under the scan, and the `setProperty` call taking the object — after which `IOService::initialize` ran its remaining 0x12C bytes (`operator new`, the `IORegistryEntry` base ctor, `instanceConstructed`, `IOLockAlloc`, two `OSArray::withCapacity`) and returned, the third key of the walk to be a forward move in a frame the previous run already stopped in (337, 339, 341). `why_byte=0x00000061` = **`'a'`**, the first byte of *"a symbol this image does not provide was called"* — **correcting 340's claim that a stub stop's byte is `'s'`**; the pair is `'a'`/`'e'`. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log **301642** bytes, device back on its own on `MI 4LTE` release 10. Next: `iokit_Kernel_IOCatalogue.o` (16428 bytes) — and the prediction has to be settled by reading `OSKext::initialize`, which sits *between* the object being linked and the next stub (`_ZN12IOUserClient10initializeEv` at +0x20), and whose body 332 read as four `OSArray::withCapacity` plus two `OSSet::withCapacity` — so whether `OSSet` is still a stub decides which object 342 links |
| stage90 | [experiment-342](experiment-342-iocatalogue-o-the-biggest-object-since-338-and-a-stop-the-pre-run-check-moved.md) | **`iokit/Kernel/IOCatalogue.cpp` — the biggest object since 338, and a stop the pre-run check moved to a stub this step itself created** — `iokit_Kernel_IOCatalogue.o` (manifest:341), the only object defining 341's stop, and the largest step since 338: **9 resolved / 2 added** (six `IOCatalogue` methods and `gIOCatalogue`, `gIOClassKey`, `gIOProbeScoreKey` out; `_Z13OSUnserializePKcPP8OSString` and `gIOKernelConfigTables` — a `D 4` name arriving as a 0x40 `.bss` slot — in), 839/734/105 → **832/729/103**, all three exact *and* checked against each other in the prediction (729 + 103 = 832) because 341's were not. `.text` **+0x19A0** (0x15C240 → **0x15DBE0**) with **seven** terms closing to the byte: **+0x19A7 placed** (0x15B518 → **0x15CEBF**) = +0x19B0 object, +0x004 COMDAT, +0x084 `.rodata`, **+0x0CB** of 0xF6 string bytes (43 deduped), **−0x090** six retired bodies, **+0x018** one created body, **−0x0E4** the name slots (−0x104 + 0x020), with the fill 0xD28 → **0xD21** (−7); `realstubs.o` proves the pair and the 0x18 rule a second time — `.text` 0x44D0 → **0x4458**, `.bss` 0x1B04 → **0x1A84**, `.rodata.str1.4` 0x424C → **0x4167** (model 0x4168: 338's ≤1-byte tail artifact, same section). **The `.text` size row was mis-derived for the third step running** — the printed band 0x8015E82C..0x8015E952 added the placed band to the fill as if from a size, 0xBD8 high, so the rule is now explicit: `size_new = size_old + placed_delta + fill_delta`, the fill read and never added twice. `.data` **0x80160000** (unmoved, fill 0x7AAB), `.sysctl_set` **0x80179498**, `.init_array` **0x801795A4** (**0x48, eighteen entries**), `.bss` **0x80179600** (placed **0x37A7C** exact at −0x54 = −0xC0 three retired slots + 0x40 one created + 0x2C own, fill 0x11C), `__bss_end` **0x801B1198**, image **1545708**, headroom **1371752**, `.text` ending 0x2420 below the 0x80160000 boundary. **The stop was not the predicted one**: the run says `stub_hit=_Z13OSUnserializePKcPP8OSString` at `xnu_entry_stub_caller_v=0x80137e08` = **`_ZN11IOCatalogue10initializeEv+0x1c`** (`caller-4` = `0x80137e04: bl 8013d2b0`) — one call into the object being linked, never reaching `OSKext::initialize`, and stopping on **a name this step's own link created**: the pre-run check asked "is any of the object's 109 `bl` targets a stub **today**" (zero, true and useless, since a referenced-but-undefined name becomes a stub *in this link*) instead of counting the `added` column `entry_object_effect.py` had already printed — the same class as 335's walker that dropped five roots, and the run was information either way. `abort_entries=0`: the fault the site could have been is absent because `gIOKernelConfigTables` is a 4-byte pointer stand-in read **as a value** (`ldr r0,[r0]` → 0, `standin_value_loads.py` called it `read-through` before the run) — `IOCatalogue.cpp:100` passes it to `OSUnserialize`, which is a stub, so **the NULL is masked rather than cured**: linking `OSUnserialize` alone next would turn a stub stop into a `data abort` inside a real function, which is why **343 is two objects** — `libkern_c++_OSUnserialize.o` (the stop) and `iokit_KernelConfigTables.o` (912 bytes: `.data` 4 + `.rodata.str1.1` 0x82, the value), the first time a step's second object is a *stand-in's data* rather than a callee's definition. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log **301643** bytes, device back on its own on `MI 4LTE` release 10 |
| stage90 | [experiment-343](experiment-343-osunserialize-o-the-first-stop-inside-the-object-it-links.md) | **`libkern/c++/OSUnserialize.cpp` **and** `iokit/KernelConfigTables.cpp` — the first stop *inside* the object the step links, and the first step whose second object is a stand-in's data rather than a callee's definition** — `libkern_c++_OSUnserialize.o` (a bison parser: `.text` 0xF80 of which `_Z18OSUnserializeparsev` is 0xEA4, `.rodata` 0x253 holding nine `yacc` tables and five `.L.str` blocks, `.rodata.str1.1` 0x5C, `.bss` 0xA8 of eleven parser-state words, no COMDAT and no `.init_array`) **plus** `iokit_KernelConfigTables.o` (912 bytes: one symbol `gIOKernelConfigTables D 4` and the 0x81-byte legacy plist at `KernelConfigTables.cpp:35` — `( { 'IOClass' = IOPanicPlatform; 'IOProviderClass' = IOPlatformExpertDevice; 'IOProbeScore' = 0:32; } )`). **The second object exists only for its value**: 342's run had `IOCatalogue::initialize` read the stand-in as **NULL** and hand it to a stub, and linking `OSUnserialize` alone would have turned that stub stop into a `data abort` inside a real function — the walk's second value stop and the first it could have prevented by reading its own ledger. **Predicted 1 resolved / 1 added / 1 stand-in resolved — 832 → 831 undefined, 729 → 729 function, 103 → 102 storage — and the stop at `_ZN8OSNumber10withNumberEyj` on `_Z18OSUnserializeparsev+0xAAC`**; all three counts exact and the stop landed **name and key**, so the run bought `OSUnserialize`'s whole body, five `OSString::withCString` returns and the `0:32` reduction. Measured `stub_hit=_ZN8OSNumber10withNumberEyj` at `xnu_entry_stub_caller_v=0x8013a164` = **`_Z18OSUnserializeparsev+0xaac`** (`caller-4` = `0x8013a160: bl 8013e5f0`), with **`abort_entries=0`** — the four things that had to hold are none of them calls: the `.data` word received its relocation (**0x8015a1a3**, exactly where the map placed the string), the string was placed whole and undeduped (0x5C at 0x8015A147 + 0x82 at 0x8015A1A3, contiguous), `OSUnserialize` ran, and `OSString::withCString` returned five times. The other candidate was never reachable: there is no `[` in the string, so `OSSet::withArray` — the one name **this link created**, which is why 342's "is it a stub *today*" check had to be replaced by classifying all **39** `bl` targets against the stub list *plus* the `added` column — is a link-time stub and not a run-time stop. `.text` closed to the byte at the **top** of the predicted band: **+0x12C0** (0x15DBE0 → 0x15EEA0) from placed **+0x12B1** (0x15CEBF → 0x15E170) and fill 0xD21 → 0xD30, the band's low end having allowed for dedupe and there being none. `realstubs.o` is where the three stub terms **cancel for the first time**: `.text` **0x4458 unchanged** (one body out, one in), `.rodata.str1.4` **0x4167 unchanged** (two 31-character names — and still the ≤1-byte tail artifact below the model's 0x4168 that 338 and 342 both saw), `.bss` 0x1A84 → **0x1A44** exactly −0x40. **The one wrong row is the most useful thing in the step**: `.data` predicted 0x19498 + 4 and measured **0x19498, unchanged**, because the symbol table shows `cpu_data_alloc.site` at 0x801783e8 — the next 8-byte-aligned input — *did not move* while everything from it to the end of `.data` is where 342 left it, so the four new bytes were absorbed by the four bytes of alignment padding between `BootArgs` and it: placed +4, fill −4. 340's `.data` +0x30 was *not* absorbed, which is the same rule from the other side — **size is `size_old + placed_delta + fill_delta` and the fill delta is a measurement, not a rounding**. `.bss` placed exact at **+0x68** (−0x40 one retired 64-byte slot, +0xA8 this object's own `.bss`; a *function* stub takes no slot, the one place the two directions do not cancel), `.bss` 0x80179600 with size 0x37C18 and fill 0x134, `__bss_end` 0x801B1198 → **0x801B1218**, image **unmoved at 1545708** (`bin_size` is `.init_array`'s end minus `ENTRY_BASE`), headroom 1371624, `.init_array` unchanged at eighteen entries. The `headroom` constant was corrected before the run: `ENTRY_DATA_LIMIT` is `align_up(ENTRY_ARGS_OFFSET + ARGS_BYTES + 0x100000, 0x100000)`, so it is 0x300000 by **megabyte** granularity and not the 0x2FFFC0 the row was first written with. **And one promise the build could not keep**: the plan to re-run `tools/standin_value_loads.py _ZN11IOCatalogue10initializeEv` and see the same site read `read-through` now returns **`0 storage stand-ins in the image`** — the tool's stand-in set is `realstubs.o`'s `B` list and the stand-in is retired by the cure, so a tool whose subject is a stand-in goes silent when the stand-in is defined, and silence there is success. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301639 bytes, device back on its own |
| stage90 | [experiment-344](experiment-344-osnumber-o-the-plist-parses-end-to-end.md) | **`libkern/c++/OSNumber.cpp` — the plist 342's abort never reached is now parsed end to end and built into a container, and the stop is 342's prediction four frames down** — `libkern_c++_OSNumber.o` (6488 bytes: `.text` 0x504 of 28 functions, a 4-byte COMDAT, `.rodata` 0xB0 of two vtables plus the `R 0x4` reference word `_ZN8OSNumber9metaClassE`, `.rodata.str1.1` 0x34, `.bss` 0x18 of `OSNumber::gMetaClass`, and `_GLOBAL__sub_I_OSNumber.cpp` in `.init_array` — 341's shape again, an `R` word plus a constructor). **Predicted 2 resolved / 0 added — 831/729/102 → 829/728/101 — and the stop at `_ZN5OSSet12withCapacityEj` on `_ZN6OSKext10initializeEv+0xA8`**; all three counts exact, and the stop landed **name and key**: `stub_hit=_ZN5OSSet12withCapacityEj` at `xnu_entry_stub_caller_v=0x801093a4` = `_ZN6OSKext10initializeEv+0xa8` (`caller-4` = `0x801093a0: bl 8013eac8`), with **`abort_entries=0`**. That is **342's prediction**, made two steps earlier out of `OSKext::initialize`'s own body and deferred twice — and it is the **longest chain the walk has predicted through**, four frames (`parse` → `OSUnserialize` → `IOCatalogue::initialize` → `iokit_post_constructor_init` → `OSKext::initialize`), every link read out of the instrument before the device was touched. `abort_entries=0` is what makes the key a measurement of the chain: between 343's stop and this one the run returned from `OSNumber::withNumber` (three real calls plus a `blx` into `OSNumber::init`, defined in this object), reduced the last `;` into a `pair`, **built the dictionary** (`OSDictionary::withCapacity` and three `d->setObject((OSString *)key, value)` — each going `OSDictionary::setObject` → `OSSymbol::withString` → `OSMetaClassBase::safeMetaCast`, all read real in the 343 image), **built the array** (`OSArray::withCapacity` and one `setObject(unsigned int, ...)` with its single real `OSCollection::haveUpdated`), released six objects and `tags`, unlocked in `OSUnserialize`, ran `IOCatalogue::initialize`'s remaining 0x118 bytes to completion, and let `OSKext::initialize` run its first 0xA4 bytes. **The first step of the walk where a whole data structure, not just a call, crosses from stubbed to real code.** `.text` closed **+0x5C0** (0x15EEA0 → **0x15F460**) from placed **+0x5B8** (0x15E170 → 0x15F728) and fill 0xD30 → **0xD38** — the **top** of the predicted band for the second step running, for the same reason both times (the band's low end allows for dedupe and nothing was deduped). **`realstubs.o` moves on both sections for the first time since 341**, because this is the first step since then with **no created stub**: `.text` 0x4458 → **0x4440** (−0x18 exactly) and `.rodata.str1.4` 0x4167 → **0x414B** against a model 0x414C — the **fourth** appearance of the ≤1-byte tail artifact 338, 342 and 343 each recorded on that section, now an expected row rather than a surprise — and `.bss` 0x1A44 → **0x1A04** (−0x40 exactly). `.data` unmoved at 0x80160000, `.sysctl_set` 0x80179498, `.init_array` 0x801795A4 (**0x4C, nineteen entries**, `last_kernel_constructor` still last at 0x8013AB40 = +0x508), `.bss` 0x80179600 with placed **0x37ABC** exact at −0x28 and start unmoved for the **fifth** step running, `__bss_end` 0x801B11D8, image **1545712** exact, headroom 1371688. **The one missed row is a sign**: `__bss_end`/`headroom` were 0x18 the wrong way because the `.bss` fill was carried forward from 343 — 344's fill is **0x11C**, exactly 342's, and the `.bss` fill went 0x11C → 0x134 → 0x11C across three steps, which is 343's `.data` lesson from the other side. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301637 bytes, device back on its own |
| stage90 | [experiment-345](experiment-345-osset-o-the-arc-the-walk-opened-and-a-boundary-missed-by-0x20.md) | **`libkern/c++/OSSet.cpp` — the walk closes a stub it invented two steps earlier, and the 16 KB boundary missed by 0x20** — `libkern_c++_OSSet.o` (9248 bytes: `.text` 0xB44 of 50 functions, a 4-byte COMDAT, `.rodata` 0xE0, `.rodata.str1.1` 0xA, `.bss` 0x18 of `OSSet::gMetaClass`, `_GLOBAL__sub_I_OSSet.cpp` in `.init_array`), the object that defines 344's stop. **Predicted 4 resolved / 0 added — 829/728/101 → 825/725/100 — the stop at `OSKextParseVersionString` on `_ZN6OSKext10initializeEv+0x224`, and `.text` ending at *exactly* `0x8015FFE0`, 0x20 below the boundary**; all three counts exact (725 + 100 = 825). **The arc is the story of the step**: 343's link *created* `_ZN5OSSet9withArrayEPK7OSArrayj` as a stub because the object it linked referenced a name nothing defined, 344 never called it (no `[` in the plist), and 345 gives it a body — **the first stub in this walk's history that the walk invented, deferred, and then defined**, and the concrete form of 342's lesson about checks that cannot see the name a link is about to create. **`added = 0` makes the 342/343 check completable rather than arguable**: with no created name, `bl` targets *and* `R_ARM_ABS32` relocations can be classified against the stub list over the whole object — `bl sites naming a CURRENT stub: 0` of 50 functions, `relocations naming a CURRENT stub: none in any section` — and the relocation check is the one no earlier step needed, because `withCapacity` ends in a `blx r2` and `initWithCapacity` calls through two vtables, which a `bl`-only check cannot see. `.text` **+0xB80** (0x15F460 → **0x15FFE0**) from placed **+0xB82** and fill 0xD38 → **0xD36**; the string row is a new measurement — the object's ten bytes are `"OSSet\0set\0"`, six placed at 0x8015B3DC and four **deduped into them** because `"set"` is a *suffix* of `"OSSet"`, the first partial `.rodata.str1.1` dedupe of the walk and one string of a single input deduping into its own sibling. **Two rows wrong, both informative.** (1) The fill *model* — the 344 map's own `*fill*` rows under a uniform shift, the method 343's `.data` and 344's `.bss` rows were argued from — gave **−0x30**; measured **−0x2**, wrong by 0x2E, because removing three stub bodies and three name slots from inside the generated stub object re-aligns everything after them and a map has no row for a fill that is zero today. What saved the prediction is that it was stated as an **end address**: `.text` closes with `. = ALIGN(32)`, so `align32(0x8015F450 + Q)` is **0x8015FFE0 for every Q in (0xB70, 0xB90]** — the placed term alone lands inside that window with 0xE to spare, and the model's −0x30 would have given 0x8015FFC0, **0x20 low**. So 343's rule has a second half: **a fill model is a hypothesis, and a 32-byte quantiser absorbs a ≤15-byte model error in an address prediction that it would turn into a miss in a size prediction.** (2) `__bss_end`/`headroom` did not move **at all** (0x801B11D8 / 1371688), and not because two 0x18s cancelled as in 344: the `.bss` fill grew **+0x28**, exactly the magnitude of the placed **−0x28**, by one row — `*fill* 0x801AF7C4` went **0x14 → 0x3C**, ending on a 64-byte boundary — and **every named `.bss` symbol kept its 344 address** (checked for thirty, `allproc` 0x801B11C0 back to `KERNEL_AUDIT_TOKEN`). With 343's `.data` (+4 placed, −4 fill) the two brackets close the rule: **a fill can absorb a placed delta of either sign and the only way to know which is to read it**. `realstubs.o` moves on all three sections — `.text` 0x4440 → **0x43F8** (−0x48 exact), `.bss` 0x1A04 → **0x19C4** (−0x40 exact), `.rodata.str1.4` 0x414B → **0x40E7** against a model 0x40E8 — that ≤1-byte tail artifact for the **fifth** time and the first time it was **predicted** rather than noticed. `.data` 0x80160000, `.sysctl_set` 0x80179498, `.init_array` **0x801795A4 (0x50, twenty entries)**, `.bss` 0x80179600 unmoved for the sixth step, image **1545716** exact, `last_kernel_constructor` 0x8013AB40 → **0x8013B688** (+0xB48 = the object's 0xB44 + its 4-byte COMDAT). **The crossing condition was written as an inequality before the run and did not fire**: `.text` crosses ⟺ `fill_delta + string_bytes_placed > 0x34`, where the last two fill deltas were +0xF and +0x8 — measured −0x2, i.e. the margin went from 0xBA0 to **0x20**, which is the smallest in the walk and makes the next step the one where the question is *when*. **`stub_hit=OSKextParseVersionString`** at `xnu_entry_stub_caller_v=0x80109520` (**and `_a` and `_e`**) = `_ZN6OSKext10initializeEv+0x224` (`caller-4` = `0x8010951c: bl 8013d4c8`), **`abort_entries=0x00000000`**, `why_byte=0x61` `'a'` — so the falsifiers named in advance (a stop *inside* `OSSet`, or at `+0xBC` meaning the first `withCapacity`'s return value gated on something) both did not fire, and `OSKext::initialize` really does make two unconditional `OSSet::withCapacity` calls. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000` with no non-zero reading, `xnu_entry_checks=5`/`failures=0`, log **301636** bytes, device back on its own on `MI 4LTE` release 10 |
| stage90 | [experiment-346](experiment-346-oskextversion-o-the-boundary-crossed-and-a-constant-withdrawn.md) | **`libkern/OSKextVersion.c` — the 16 KB boundary crossed exactly as 345 predicted, and a "constant" withdrawn one step after it was written** — `libkern_OSKextVersion.o` (9476 bytes: `.text` 0x1018 of three functions, `.rodata` 0x24 of one switch table, `.rodata.str1.1` 0x20, and **no `.bss`, no `.data`, no COMDAT and no `.init_array`** — it is C, not C++), the object that defines 345's stop. **Predicted 2 resolved / 0 added — 825/725/100 → 823/723/100 — the stop at `_ZN12IOUserClient10initializeEv` on `iokit_post_constructor_init+0x20`, and `.data` stepping a whole 0x4000 to 0x80164000**; all three counts exact (723 + 100 = 823), and the stop landed **name and key**: `stub_hit=_ZN12IOUserClient10initializeEv` at `xnu_entry_stub_caller_v=0x8011b10c` = **`iokit_post_constructor_init+0x20`** (`caller-4` = `0x8011b108: bl 80140358`), **`abort_entries=0`** — which is **341's reading of the constructor's line, five steps later**. `digits=0x43` with `w0=0x31313038` (`"8011"`) / `w1=0x63303162` (`"b10c"`) renders the key as its own ASCII digits. `abort_entries=0` makes the two retirements a measurement: the run had to execute **`OSKextParseVersionString` in full** (through `snprintf` → `__doprnt`, `strlcpy`, `strlcat`, `bzero`, `strlen`, three `__aeabi_ldivmod`), then the **0x264 bytes of `OSKext::initialize` past +0x224** (`strlcpy`, `OSDictionary::withCapacity`, two `OSString::withCStringNoCopy`, `IORegistryEntry::getRegistryRoot`, two `OSNumber::withNumber`, `OSKextLog`) plus **ten `blx` vtable dispatches**, and then return — so both named falsifiers (a stub deeper than one level inside the parse, a `blx` landing on a stub) did not fire, and **331's target function has been entered and exited for good**. **`.text` closed +0xFE0** (0x15FFE0 → **0x160FC0**) from a placed term of **+0xFEC** — **the first placed term of the walk to leave its predicted band**, by 4 bytes low: the band's low end was "all string bytes deduped" and the outcome was a third mechanism neither end allowed for, `.rodata.str1.1` **relaxed** from 0x20 to **0x14** (the map prints `0x14` / `0x20 (size before relaxing)`, with the `0x2` fill after it as the leftover) — so **a string term has three outcomes, not two: placed whole, deduped against a *previous* input, or partly relaxed against its *own* duplicates**. The fill went 0xD36 → **0xD2A** (−0xC) across 65 rows where 345 had 66, and **the row 345's own string bytes had created is gone again**. **The boundary crossed as predicted and it moved four sections at once**: `.data` **0x80164000** (size 0x19498 unmoved), `.sysctl_set` **0x8017D498**, `.init_array` **0x8017D5A4** (**0x50, twenty entries, unmoved**), `.bss` **0x8017D600** with placed term **+0x000** — the walk's first — so `__bss_end` **0x801B51D8** and the headroom **1355304** are **exact to the byte for the first time in the walk, with no fill term consulted** (a `.bss` whose content does not change has a fill that does not change — 344's and 345's lesson as a contrapositive). **The one wrong row is the same mistake twice and the prediction's own table refutes it**: the `.init_array` row predicted a twenty-first entry (`_GLOBAL__sub_I_OSKextVersion.cpp`) and 4 bytes when the object table two paragraphs above says the object has **no `.init_array`** — it is C — so that row and the image size (1562104 against a measured **1562100**) are both exactly 4 high. **And one claim is withdrawn**: `realstubs.o`'s `.rodata.str1.4` went 0x40E7 → **0x40B3** = `0x40E7 − 0x1C − 0x18` **to the byte**, i.e. the model with **no tail artifact at all** — so the `≤1-byte` artifact 338/342/343/344 recorded and 345 wrote down as "a constant of this section: measured = model − 1" **does not exist**: five samples of a rounding that sometimes does not happen are a distribution, and four of those five shared a construction. The claim is corrected in 345's block and document rather than deleted, because the interesting part is that a rule was promoted from five observations one step before anything could test it — the same error 345's fill *model* made one row above. `realstubs.o` moves on two of three sections: `.text` 0x43F8 → **0x43C8** (−0x30 exact), `.bss` **0x19C4 unchanged**, and `last_kernel_constructor` 0x8013B688 → **0x8013C6A0** (+0x1018). 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000` with no non-zero reading, `xnu_entry_checks=5`/`failures=0`, log **301643** bytes, device back on its own on `MI 4LTE` release 10. Next: `iokit_Kernel_IOUserClient.o` (**88856 bytes**, the largest step since 342) — **7 resolved / 21 added**, 823 → **837**/736/**101**, crossing the boundary again (0x88DC of `.text` against 0x3020 of margin), and the interesting part is that classifying its **71** stub-or-added `bl` sites finds **zero in `IOUserClient::initialize`**, so the stop should **not** move to a name the step creates even though it creates twenty-one — predicted `_ZN18IOMemoryDescriptor10initializeEv` at `+0x24`, key **0x8011b110** |
| stage90 | [experiment-347](experiment-347-iouserclient-o-twenty-one-names-and-none-of-them-the-stop.md) | **`iokit/Kernel/IOUserClient.cpp` — the step that creates twenty-one stub names and moves the stop to none of them** — `iokit_Kernel_IOUserClient.o` (**88856 bytes**, the largest step since 342: `.text` 0x88DC of 321 definitions, a 0x48 `.group` with a 0x18 COMDAT, `.bss` 0xA0, `.rodata` 0x6B8, `.rodata.str1.1` **0x5EB** — the largest string input of the walk — and an `.init_array`). **Predicted 7 resolved / 21 added — 823/723/100 → 837/736/101 — `.data` stepping 0x8000 to 0x8016C000, and the stop at `_ZN18IOMemoryDescriptor10initializeEv` on `iokit_post_constructor_init+0x24`, key 0x8011b110**; all three counts exact (736 + 101 = 837), every derived address exact, and the stop landed **name and key**. **Twenty-one created names is more than any earlier step** (343's one was the previous maximum), and the reason the frontier moved to none of them is a classification rather than luck: every `bl` target in the object was checked against the stub list **and** its own `added` column — 342's and 343's rule, applied to both populations at once — giving **71** sites with **zero in `IOUserClient::initialize`**, and that function is **0x28 bytes** whose body makes two calls, both `IOLockAlloc`. So the constructor advanced **one** call to a stub that has been next on its line since 341: `stub_hit=_ZN18IOMemoryDescriptor10initializeEv` at `xnu_entry_stub_caller_v=0x8011b110` = `iokit_post_constructor_init+0x24` (`caller-4` = `0x8011b10c: bl 80148e8c`), **`abort_entries=0`** — the first step of the walk whose stop is **not** a name the step creates. `.text` closed **+0xA860** (0x160FC0 → **0x16A820**) from a placed term of **+0x985C** (the band was 0x9284..0x986F, 0x5EB wide, and the implied 0x5D8 of 0x5EB string bytes placed puts 0x13 bytes of merging inside it — 346's third mechanism, now inside a band wide enough to hold it) with fill 0xD2A → **0xD2E** (+4) across 65 rows both times; `.data` **0x8016C000** (0x19498 unmoved), `.sysctl_set` **0x80185498**, `.init_array` **0x801855A4** (**0x54, twenty-one entries**), `.bss` **0x80185600** with placed term **+0x0E0** exact (−0x40 one retired slot + 0x80 two created + 0xA0 own — the first **positive** `.bss` placed term since 344, and the first step that creates more storage stand-ins than it retires); image **1594872** exact. **`realstubs.o` moved on all three of its sections by exactly the predicted amount, with no tail artifact, for the first time in the walk** — `.text` 0x43C8 → **0x4500** (+0x138 = +13 bodies), `.rodata.str1.4` 0x40B3 → **0x4253** (+0x1A0 = 0x2A4 created slots − 0x104 retired), `.bss` 0x19C4 → **0x1A04** (+0x40 = 2 × 0x40 − 1 × 0x40) — and 346's withdrawal of the `≤1-byte` tail artifact holds one step later. **The one missed row is the fill again**: `.bss`'s fill went 0x144 → **0x124**, so `__bss_end` (0x801BD298) and the headroom (1322344) are 0x20 out while the placed term was exact — the third step running in which the fill is the only thing between this ledger and an exact row, and the fourth different way for it to move (344 carried it forward, 345 modelled it, 346 assumed it constant, 347 assumed it would stay). `last_kernel_constructor` 0x8013C6A0 → **0x80144F94** (+0x88F4). 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000` with no non-zero reading, `xnu_entry_checks=5`/`failures=0`, log **301649** bytes, device back on its own on `MI 4LTE` release 10. Next: `iokit_Kernel_IOMemoryDescriptor.o` (**59796 bytes**) — **8 resolved / 17 added** → 837 → **846**/743/**103** (743 + 103 = 846), bringing `.text` 0x7E6C, `.bss` 0x58, `.rodata` 0x2D8, `.rodata.str1.1` 0x314, **8 bytes of `.data`** and an `.init_array`; `IOMemoryDescriptor::initialize` is **six instructions with zero direct calls**, so the stop should be `_ZN12IORootParent10initializeEv` (`iokit_Kernel_IOPMrootDomain.o`) at `+0x28`, key **0x8011b114** — and one of its added names, `upl_get_internal_vectorupl`, is **defined nowhere in the 695-object pool** (329's `__cxa_atexit` / 330's `__dso_handle` shape) |
| stage90 | [experiment-348](experiment-348-iomemorydescriptor-o-a-stop-the-step-creates-and-then-reaches.md) | **`iokit/Kernel/IOMemoryDescriptor.cpp` — the stop a step creates and then reaches, and a body read out of the wrong image** — `iokit_Kernel_IOMemoryDescriptor.o` (**59796 bytes**: `.text` 0x7E6C of 198 definitions, a 0x24 `.group` with a 0x0C COMDAT, `.bss` 0x58, `.rodata` 0x2D8, `.rodata.str1.1` 0x314, **`.data` 8** — the first object of the walk that brings `.data` of its own — and an `.init_array`). **Predicted 8 resolved / 17 added — 837/736/101 → 846/743/103 — `.data` stepping 0x8000 to 0x80174000, and the stop at `_ZN16IOPMinformeeList22getSharedRecursiveLockEv` on `iokit_post_constructor_init+0x28`, key 0x8011b118**; all three counts exact (743 + 103 = 846), every derived address exact, `realstubs.o` exact on **all three** of its sections again — and **the stop was not the predicted one**: `stub_hit=IOGetLastPageNumber` at `xnu_entry_stub_caller_v=0x8014c050` = **`_ZN18IOMemoryDescriptor10initializeEv+0x24`** (`caller-4` = `0x8014c04c: bl 8014d884`), **`abort_entries=0`**. The stop is a name **this step itself created** (its `added` column) reached from **inside** the object this step linked — 343's shape and 342's at once, and exactly the falsifier the prediction named. **The classification did not fail; the body was read from the wrong image.** The prediction said `initialize` is "six instructions with zero direct calls" because that is what `first_stub_call.py` reports for a symbol that is still a **stub** in the 347 image — a stub's body is the reporting tail. The object says **0x34 bytes** and two calls (`ldr r0,[r4]` on the lock word at 0x801c38d8, `bne` over `IORecursiveLockAlloc`, then `bl IOGetLastPageNumber` at object +0x20 / image +0x24, `str r0,[r1]` into `gIOLastPage`, `pop {r4,pc}`), and the image agrees instruction for instruction with the lock word zero, so `IORecursiveLockAlloc` **was** called at 0x8014c044. The rule: **to predict a stop inside a function a step is about to make real, read the body from the OBJECT that defines it, never from the image where the symbol is still a stub** — `nm -S` on the object (0x34 against the image's 0x18) is the cheap cross-check 347's prediction happened to have (0x28) only because that function really is small. **The consequences are small** — `IOGetLastPageNumber` is the *last* call in `initialize`, so once real the function returns, `IORecursiveLockParent::initializeEv` (4 bytes, real from this link) returns, and the constructor reaches the call 348 retired without ever calling. Layout: `.text` closed **+0x85C0** (0x16A820 → **0x172DE0**) from placed **+0x85C4** (inside the band 0x82C4..0x85D8; 0x2D0 of 0x314 string bytes placed, 0x44 merged) with fill 0xD2E → **0xD2A**; **the `.data` row is off by exactly 8 and for the right reason** — the object's 8 bytes land at 0x8018C2A8 where nothing follows but an `ALIGN(0x8)` already satisfied, so **placed +8**, and then a *second* 8 bytes of fill appears behind them, giving `size = placed + fill = 8 + 8 = 0x10` (**0x194A8**): 343's row in both directions at once, where placed +4 and fill −4 cancelled and here they add, and the prediction used only the placed half — every derived row below `.data` inherits the 8 (`.sysctl_set` **0x8018D4A8**, `.init_array` **0x8018D5B4 (0x58, twenty-two entries)**, image **1627660** against 1627140). `.bss` **0x8018D640** exact, unmoved relative to `.init_array` for the ninth step running; its size 0x37D58 with fill **0x10C** implies an input sum 0x37C4C against a modelled 0x37CB0, and the `.bss` placed model does not carry the section's direct `. += 16` reserved-slot advance (`entry.ld:201`) — the start was exact and the fill row is the wrong one for the fifth step running regardless. `realstubs.o` exact again with no tail artifact: `.text` 0x4500 → **0x45A8** (+0xA8 = 14 created − 7 retired bodies), `.rodata.str1.4` 0x4253 → **0x431F** (+0xCC), `.bss` 0x1A04 → **0x1A84** (+0x80 = 3 × 0x40 − 1 × 0x40). **Three added names are defined nowhere in the 695-object pool** (`upl_get_internal_vectorupl`, `upl_get_internal_pagelist_offset`, `upl_get_internal_vectorupl_pagelist`) — 329's `__cxa_atexit` / 330's `__dso_handle` shape, printed by the tool as `(nowhere in the pool)` rather than as a pool size. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000` with no non-zero reading, `xnu_entry_checks=5`/`failures=0`, log **301631** bytes, device back on its own on `MI 4LTE` release 10. Next: `osfmk_device_iokit_rpc.o` — **15 resolved / 0 added → 831/728/103**, the largest single-object retirement of the walk (its `.text` 0x830, `.rodata` 0x18, `.rodata.str1.1` 0x14, `.bss` 4, and it creates **nothing**, so `realstubs.o` should give back 15 bodies and 15 name slots and shrink); predicted stop `_ZN16IOPMinformeeList22getSharedRecursiveLockEv` at `+0x28`, key **0x8011b118** |
| stage90 | [experiment-349](experiment-349-iokit-rpc-o-fifteen-names-retired-and-a-premise-corrected.md) | **`osfmk/device/iokit_rpc.c` — fifteen names retired in one step, and a premise corrected before the build** — `osfmk_device_iokit_rpc.o` (**6140 bytes**: `.text` 2096 of 23 definitions, `.rodata` 24, `.rodata.str1.1` 20, `.bss` 4, no `.data`/`.init_array`/COMDAT) is the only object defining 348's stop `IOGetLastPageNumber` and the largest single-object retirement of the walk: **15 resolved / 0 added**, 846 → **831** undefined, **743 → 728 function, 103 → 103 storage** (728 + 103 = 831), creating **nothing** — no stub bodies, no name slots, no storage stand-ins — so `realstubs.o` **shrinks on two of its three sections for the first time in the walk** (`.text` 0x45A8 → **0x4440**, `.rodata.str1.4` 0x431F → **0x41cf**, `.bss` **0x1A84** unchanged). **The experiment is the correction.** 348's write-up and this step's first prediction both said that once `IOGetLastPageNumber` is real, `_ZN12IORootParent10initializeEv` ("4 bytes in the object") returns and the constructor reaches the call at +0x28 — but `iokit_Kernel_IOPMrootDomain.o` is that name's only definer in the pool **and 348 does not link it**, so the image carries it as **`T 0x18` — a stub — at 0x80150cbc**, before and after 348's link; the constructor advances **one** call, not two. The rule, 348's read in the other direction: **read a body from the object that defines it, and read a stub's *identity* from the image** — the pool says who *could* define a name, only the image says whether anything has. Corrected in 348's prediction block, 348's measured section and 349's own block **before the build**, in place and not deleted (as 345's withdrawn constant was). The run then landed **name and key exactly where the correction said**: `stub_hit=_ZN12IORootParent10initializeEv` at `xnu_entry_stub_caller_v=0x8011b114` = **`iokit_post_constructor_init+0x28`** (`caller-4` = `0x8011b110: bl 80151384 <_ZN12IORootParent10initializeEv>`, the 0x18 stub), **`abort_entries=0`**, and the call one instruction further on (`0x8011b114: bl 8015142c <_ZN16IOPMinformeeList22getSharedRecursiveLockEv>`) is the one the original prediction named one step early — now 350's. `IOGetLastPageNumber`'s body was read **from the object** this time (348's lesson one step later): 8 bytes, `mov r0, #0` / `bx lr`, **no calls**, so nothing else in the object can be on the path and its other stub-facing `bl` sites (`ipc_object_translate`, `io_free`, `ipc_object_copyout`) are off it **by construction**. Layout: **every row exact** — `.text` closed **+0x5A0** (0x172DE0 → **0x173380**, the low end of the predicted band) with the closure read entirely from this map's own lines (+0x830 the object's `.text`, +0x18 `.rodata`, +0x14 strings placed whole, −0x168 fifteen bodies, −0x150 fifteen slots → placed **+0x5A4**, fill delta **−0x4**); **the fill row closes the identity for the first time in six steps**, and the honest part is that the map's own fill total (66 rows, 0xD3A) does *not* subtract to that delta from 348's recorded 0xD2A — the two totals were summed under a convention nobody restated (whether the trailing pad to the `.data` boundary is inside `.text`; it is), so the row carried is the **delta from the closure, not a difference of two totals** (343's rule from a seventh direction: a fill is read, and read in the frame it is used in). And **−0x4 did not move the address**: the placed term alone gives `align32(0x80173384)` = 0x801733A0, the top of the band, and 345's 32-byte quantiser absorbed the fill error. `.data` **0x80174000** (0x194A8), `.sysctl_set` **0x8018D4A8**, `.init_array` **0x8018D5B4** (0x58, 22), image **1627660** all unmoved and exact; **`.bss` size unmoved at 0x37D58** with the object's 4 bytes placed at 0x801c38fc — placed +4, fill **−4** — 343's `.data` row reproduced on `.bss` nine steps later, and the reason `__bss_end` (0x801C5398) and the headroom (1289320) come out **exact for the first time since 346**: not because the fill's value was known but because the fill *absorbed* the placed term. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log **301643** bytes, device back on its own on `MI 4LTE` release 10. Next: `iokit_Kernel_IOPMrootDomain.o` — **4 resolved / 30 added → 857/747/110**, the largest object the walk has considered (144744 bytes, `.text` 0xE8E8 of 565 definitions, `.bss` 0x250, `.rodata` 0xA7C, `.rodata.str1.1` 0xB5C, `.data` 0x338, `__DATA,__sysctl_set` 0x2C, `.init_array` 4), which should move `__DATA` for the first time since 347 (`.data` stepping to at least **0x80184000**); predicted stop `_ZN16IOPMinformeeList22getSharedRecursiveLockEv` at `iokit_post_constructor_init+0x2C`, key **0x8011b118**, with `_ZN12IORootParent10initializeEv` read from its object as 4 bytes, `bx lr`, no calls |
| stage90 | [experiment-350](experiment-350-iopmrootdomain-o-four-bytes-retiring-a-hundred-and-forty-kilobytes.md) | **`iokit/Kernel/IOPMrootDomain.cpp` — a 4-byte function retires a 144 KB object, and `__DATA` moves** — `iokit_Kernel_IOPMrootDomain.o` (**144744 bytes**, the largest object the walk has linked: `.text` 0xE8E8 of **565 definitions**, `.group` 0x60 / COMDAT 0x20, `.bss` 0x250, `.rodata` 0xA7C, `.rodata.str1.1` **0xB5C** — the largest string input of the walk — `.data` 0x338, **`__DATA,__sysctl_set` 0x2C**, `.init_array` 4) is the only pool definer of 349's stop `_ZN12IORootParent10initializeEv`, which is `T e3d0 4` and whose whole body is `bx lr`: **4 resolved / 30 added** — that function, `_ZN14IOPMrootDomain10tracePointEh`, `_ZN14IOPMrootDomain13startSpinDumpEj` and `hibernate_should_abort` out, **twenty-three functions and seven storage stand-ins in** — for 831 + 30 − 4 = **857** undefined, **728 → 747 function, 103 → 110 storage** (747 + 110 = 857), all three exact. **The stop landed name and key exactly as predicted**: `stub_hit=_ZN16IOPMinformeeList22getSharedRecursiveLockEv` at `xnu_entry_stub_caller_v=0x8011b118` = **`iokit_post_constructor_init+0x2c`** (`caller-4` = `0x8011b114: bl 8015fd38`), **`abort_entries=0`** — the call 348's original prediction named two steps early, and the one 349's run stopped one instruction short of. **`__DATA` moved for the first time since 298**: `.data` crossed a whole 0x4000 to **0x80184000** (size **0x197E0** = 0x194A8 + the object's 0x338, **no fill**), `.sysctl_set` **0x8019D7E0** with size **0x138** = 0x10C + **0x2C** — the first step of the walk to bring `__DATA,__sysctl_set` of its own — `.init_array` **0x8019D918** (0x5C, twenty-three) and the image **1694068**, all exact as arithmetic. `.text` closed **+0x10360** (0x173380 → **0x801836E0**) inside the predicted band 0x80182D18..0x80183874 with a fill delta of **+0xA** (67 rows summing 0xD44 against 349's 66 summing 0xD3A, both counted under the convention 349 had to write down), so **0x9BE of the object's 0xB5C string bytes were placed** and 0x19E merged. **`.bss` is the one miss, for the sixth step running**: 0x38198 against a placed model of 0x38168 (0x37D58 + 0x250 object + 0x1C0 of seven created storage stand-in slots), so `__bss_end` (0x801D5B18) and the headroom (1221864) are 0x30 out — every term measured, only the alignment behind them not. One row was a **drafting slip corrected before the build**: the image row read `0x19D974 = 1693044` where 0x19D974 is 1694068 (a 0x400 error in the one row that is a base conversion, 333's defect shape). 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log **301659** bytes, device back on its own on `MI 4LTE` release 10. Next: `iokit_Kernel_IOPMinformeeList.o` — **1 resolved / 1 added → 857/747/110**, with `getSharedRecursiveLock`'s body read from its object (0x24 bytes, one `IORecursiveLockAlloc` call, **real since 328**, and nothing else) so it returns; predicted stop `_ZN16IOKitDiagnostics11diagnosticsEv` at `iokit_post_constructor_init+0x74`, key **0x8011b160** — read from the constructor's line in the 350 image (`+0x28` stub retired, `+0x34` real, `+0x70` stub) |
| stage90 | [experiment-351](experiment-351-iopminformeelist-o-the-stop-read-from-the-image-one-step-early.md) | **`iokit/Kernel/IOPMinformeeList.cpp` — the stop read out of the image one step early, and `.bss` absorbed** — `iokit_Kernel_IOPMinformeeList.o` (6092 bytes: `.text` 0x40C of 30 definitions, COMDAT 4, `.bss` 0x1C, `.rodata` 0x84, `.rodata.str1.1` 0x11, `.init_array` 4) is the only pool definer of 350's stop: **1 resolved / 1 added** — `_ZN16IOPMinformeeList22getSharedRecursiveLockEv` out, `_ZN12IOPMinformee10withObjectEP9IOService` in — for **857 / 747 / 110, all three unmoved**, the walk's first step whose arithmetic is an identity. **The stop landed name and key exactly as predicted**: `stub_hit=_ZN16IOKitDiagnostics11diagnosticsEv` at `xnu_entry_stub_caller_v=0x8011b160` = **`iokit_post_constructor_init+0x74`** (`caller-4` = `0x8011b15c: bl 8015fd38`), **`abort_entries=0`** — and it is the first stop of the walk **predicted by reading the image past the callee the step retires**, which is what 349's correction bought: the constructor's call list was walked in the 350 image, the retired call skipped, `OSString::withCString` at +0x34 read as real (0x74 bytes) and the first 0x18 body after it named two steps early. The complementary reading said the same thing from the other side — `getSharedRecursiveLock` is 0x24 bytes with one `IORecursiveLockAlloc` call (real since 328) on the cold path and nothing on the warm one — and **the object's call graph faces nothing stubbed at all** (`bl`+relocation sweep: zero stub-facing sites apart from the one it retires), the strongest form that check has taken. Layout: `.text` closed **+0x4A0** (0x1836E0 → **0x80183B80**, the band's low end) with fill delta **−0x1** (68 rows summing 0xD43 against 350's 67 summing 0xD44, same convention), so the placed term is **+0x4A1** — the *top* of the predicted band, all 0x11 string bytes placed and the body terms cancelling exactly; `.data` **0x80184000**, `.sysctl_set` **0x8019D7E0** both unmoved; `.init_array` **0x8019D918** size **0x60** (twenty-four, the start fixed and only the size growing — a row first drafted the other way round and corrected before the build); image **1694072** ✓. **`.bss`'s size is unmoved for the second step running** — the object's 0x1C placed and a fill of **−0x1C** absorbing it (343's rule on `.bss`, opposite sign from 350's +0x30) — which is why `__bss_end` (0x801D5B18) and the headroom (1221864) are unmoved **and exact** where the banded prediction could not claim it. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log **301648** bytes, device back on its own on `MI 4LTE` release 10. Next: `iokit_Kernel_IOKitDebug.o` — **7 resolved / 0 added → 850/746/104** (six storage stand-ins retired, the largest storage retirement of the walk) — **the step that ends `iokit_post_constructor_init`** (its last `bl` is the call 351 retired, so it returns and the walk unwinds: `OSRuntimeInitializeCPP+0x184`, its real `checkModLoad`/`nextsegfromheader`/`postModLoad`/`OSRuntimeFinalizeCPP` tail, `OSlibkernInit+0x1C`, then `StartIOKit` at 0x8011b264); predicted stop **`devsw_init` at `StartIOKit+0xCC`, key `0x8011b26c`** (`caller-4` = `0x8011b268`), with the falsifiers named: a stub through a `blx` inside `postModLoad` (0x474 bytes of this walk's blind spot), or **`panic`** if `OSRuntimeInitializeCPP` returns nonzero (`OSlibkernInit` calls the real `panic` with `"OSRuntime: C++ runtime failed to..."`) |
| stage90 | [experiment-352](experiment-352-iokitdebug-o-the-constructor-returns-and-data-moves.md) | **`iokit/Kernel/IOKitDebug.cpp` — the constructor returns and `__DATA` moves** — `iokit_Kernel_IOKitDebug.o` (27044 bytes: `.text` 0x864 of 9 definitions, COMDAT 0x8, `.bss` 0x50, `.rodata` 0x45C, `.rodata.str1.1` 0x17A, `.data` 0x60, `__DATA,__sysctl_set` 8, `.init_array` 4) is the only pool definer of 351's stop: **7 resolved / 0 added** — `_ZN16IOKitDiagnostics11diagnosticsEv` out plus **six storage stand-ins**, the largest storage retirement of the walk — for **857 → 850** undefined, **747 → 746 function, 110 → 104 storage**, all three exact. **This is the step that ends `iokit_post_constructor_init`**: the diagnostics call it retires is that function's last `bl`, so the constructor *returns* — for the first time in the walk's history — and the run unwinds through `OSRuntimeInitializeCPP+0x184` (table exhausted), its `checkModLoad`/`nextsegfromheader` tail, **`postModLoad`'s 0x474 bytes with eight `blx` dispatches**, `OSRuntimeFinalizeCPP`, and `OSlibkernInit`'s `cmp r0,#0; beq` gate (nonzero would `bl panic` at 0x8002DEB4) into `StartIOKit`. **The stop landed name and key exactly as predicted**: `stub_hit=devsw_init` at `xnu_entry_stub_caller=0x8011b26c` = **`StartIOKit+0xCC`** (`caller-4` = `0x8011b268: bl 8015c818`), **`abort_entries=0`** — so all three named falsifiers did not fire, including the two this walk's tooling cannot see. Layout: **`.data` moved a whole 0x4000 to 0x80188000** (the second `__DATA` move of the walk, the first since 350) and was predicted from the boundary rather than the fill — the object's own `.text` (0x864) exceeds the 0x480 of room below 0x80184000, so no fill could have kept it. `.text` closed **+0xDE0** to **0x80184960** with **fill zero** (every term measured, the object's `.rodata.str1.1` placed at 0x158 of 0x17A), the first zero fill since 346; `.data` size exactly 0x197E0 + 0x60, `.sysctl_set` **0x801A1840** (0x140 = 0x138 + 8), `.init_array` **0x801A1980** (0x64, twenty-five), `.bss` **0x801A1A00**, image **1710564** exact — and the one miss is the fill again for the seventh step running (`.bss` 0x38058 against a placed 0x38068, so `__bss_end` 0x801D9A58 and the headroom 1205672 are 0x10 out). `realstubs.o`: `.text` **0x45F0** = 746 × 0x18, `.bss` **0x1AC4** = 104 × 0x40 + 0xC4, and `.rodata.str1.4` **0x44D0** which is **the sum of `align4(len + 1)` over the 746 function stubs to the byte** — the first time that section's size was a closed form rather than a difference, and the identity that corrected the name-slot row (`align4(37)` = 0x28, not `align4(36)` = 0x24) after the build and before the run. One claim corrected in place: the 351 frontier's "`IOPlatformExpertDevice`'s constructor is defined nowhere in the 695-object pool" is false — it is `iokit_Kernel_IOPlatformExpert.o`, `T 0x38`, and the sweep had looked for the **C name** where the pool holds `_ZN22IOPlatformExpertDeviceC1Ev`, which is 348's rule one turn further out (**a definition is a mangled-name fact**). 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301622 bytes, device back on its own on `MI 4LTE` release 10. Next: `iokit_Kernel_IOInterruptAccounting.o` — **4 resolved / 2 added → 848/745/103** — predicted stop **`interruptAccountingInit` at `StartIOKit+0x104`, key `0x8011b2a4`**, with `OSSymbol::withCStringNoCopy` and `OSSet::withObjects` swept two levels deep (no stub sites at either level; three `blx` remain the blind spot) and the stop's own body read from its object (0x60 bytes, `PE_parse_boot_argn` and `IOLockAlloc`, both real) |
| stage90 | [experiment-353](experiment-353-iointerruptaccounting-o-the-step-that-did-not-move-the-frontier.md) | **`iokit/Kernel/IOInterruptAccounting.cpp` — the step that did not move the frontier** — `iokit_Kernel_IOInterruptAccounting.o` (6808 bytes: `.text` 0x244 of 9 definitions, `.data` 4, `.bss` 0x10, `.rodata.str1.1` 0x15, no `.rodata`/COMDAT/`.init_array`/`__sysctl_set`): **4 resolved / 2 added** — three functions and the `D 0x4` stand-in `gInterruptAccountingStatisticBitmask` out, `IOSimpleReporter::getValue` and `::setValue` in — for **850 → 848** undefined, **746 → 745 function, 104 → 103 storage**, all three exact. **And the run stopped on `devsw_init` at key `0x8011b26c`, byte for byte where 352 stopped** — because the block contradicted itself: it named `_Z23interruptAccountingInitv` both as the object it links *and* as the stop it predicts, and linking the object that defines a stub is exactly what retires it. That pins the rule every step since 332 has followed and none had stated: **the step's object is the definer of the PREVIOUS step's stop**, and the run then advances one call to the next stub on the path. The mechanism was carried in every document as a *shape* ("the object defining this step's stop…"), which cannot be checked against itself; writing the name twice turned it into a claim, and a run can falsify a claim. Layout: `.text` exact at its band's low end (**0x80184B00**, +0x19C..+0x1B1 predicted), every `__DATA` row **unmoved** and 4 high as predicted, every `.bss` row unmoved and 0x30 low — because two alignments absorbed the whole step and the map prints both: the object's +4 of `.data` is swallowed by the `ALIGN(8)` that closes `.data` (last input end 0x801A1838 → 0x801A183C, both rounding to 0x801A1840), and the object's 0x10 of `.bss` lands on a 64-byte-aligned slot so a **`*fill* 0x30`** appears exactly where the retired 0x40 stand-in went and cancels it to the byte — the first time that word has been the answer rather than the residual. Image 1710564, `__bss_end` 0x801D9A58, headroom 1205672, all unmoved. `realstubs.o` exact on all three sections by the closed forms: `.text` 0x45D8 = 745 × 0x18, `.rodata.str1.4` 0x4440 = 0x44D0 − 0xD8 + 0x48, `.bss` 0x1A84 = 0x1AC4 − 0x40. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301622 bytes, device back on its own on `MI 4LTE` release 10. Next: **`bsd_kern_bsd_stubs.o`** — the definer of `devsw_init` and where this step's object belonged — **2 resolved / 10 added → 856/748/108**, with `devsw_init`'s own body read from the object (0x4C bytes, `lck_grp_alloc_init` and `lck_mtx_init`, both real and both already on the path) and predicted stop **`interruptAccountingInit` at `StartIOKit+0x104`, key `0x8011b2a4`** — the first stop of the walk inside a function it has already entered once |
| stage90 | [experiment-354](experiment-354-bsd-stubs-o-the-stand-in-model-and-the-stop-four-calls-on.md) | **`bsd/kern/bsd_stubs.c` — the stand-in model, and the stop four calls on** — `bsd_kern_bsd_stubs.o` (2432 bytes: `.text` 0x980 of **30 definitions**, `.rodata.str1.1` 0xBF, `.data` 0x58, `.bss` 0x20, `__DATA,__data` 0x18 placed *inside* `.data`, no `.rodata`/COMDAT/`.init_array`/`__sysctl_set`) is the pool definer of 353's stop `devsw_init`: **2 resolved / 10 added** — `current_proc` and `devsw_init` out, five functions (`chrtoblk_set`, `enodev`, `enodev_strat`, `msleep`, `seltrue`) and **five storage stand-ins** (`bdevsw`, `cdevsw`, `cdevsw_flags`, `nblkdev`, `nchrdev`) in — for **848 → 856** undefined, **745 → 748 function, 103 → 108 storage**, all three exact. **The stop was four calls past the prediction**: `stub_hit=_ZN22IOPlatformExpertDeviceC1Ev` at `xnu_entry_stub_caller=0x8011b2b4` = `StartIOKit+0x114`, `abort_entries=0`, because the block predicted `interruptAccountingInit` at `+0x104` — a name that is `T 0x60` in the image the run used, real since 353's own link. A frontier's predicted stop has to be read from the image the step will start from, not carried from the block that named it; 353's rule for the other column (the step's object is the definer of the *previous* stop) plus this one covers both, and neither can be inherited from prose. Layout: `.text` **0x801855A0** inside its +0x9E4..+0xAA3 band with the closure's fill **−3** (three of the six terms pinned by `realstubs.o`'s closed forms), `.data` **0x80188000** size **0x198B8** = 0x19840 + 0x58 + 0x18 + 8 of pad (placements read from the map at 0x801A0650 and 0x801A18A0), `.sysctl_set` 0x801A18B8, `.init_array` 0x801A19F8 (0x64, 25) ending 0x801A1A5C, `.bss` start **0x801A1A80 exact** with size **0x38E98** = 0x38058 + 0xE40, image **1710684**, `__bss_end` 0x801DA918, headroom **1201896**. The `.bss` row is the step's one miss and it is **not a fill**: the five stand-ins cost `align64(size)` = 0x40 + 0x40 + 0x180 + **0x980** (`cdevsw`, 0x968 bytes — the first storage stand-in of the walk larger than a slot) + 0x2C0 = **0xE40**, and the object's own 0x20 is absorbed by the following `*fill*`, which fell 0x30 → 0x10. The 0xCE0 miss was a model — *a stand-in costs a 0x40 slot* — printed as a caveat by `tools/entry_object_effect.py` and read off its sentence instead of off the generator's `uint8_t SYM[0xSZ] __attribute__((aligned(64)))`. `realstubs.o` exact on all three: `.text` 0x4620 = 748 × 0x18, `.rodata.str1.4` 0x445C = 0x4440 − 0x1C + 0x38, `.bss` 0x28C4 = 0x1A84 + 0xE40. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301643 bytes, device back on its own on `MI 4LTE` release 10. Next: **`iokit_Kernel_IOPlatformExpert.o`** — the definer of the constructor — **8 resolved / 16 added → 864/753/111**, `.text` 0x2E74, `.rodata` 0x13A4, `.rodata.str1.1` 0x313; `StartIOKit+0x114`'s next call dispatches through vtable slot `0x340` (`.rodata` + 0xBB4) to **`initWithArgs(this, PE_state.deviceTreeHead, PE_state.bootArgs, 0)`**, whose first call is `_Z17IODeviceTreeAllocPv` — a name this step creates — so the predicted stop is **`IODeviceTreeAlloc` at `initWithArgs+0x18`**, with `IOWorkLoop::workLoop` named as the falsifier, and the first predicted key of the walk that does not exist in the previous image |
| stage90 | [experiment-355](experiment-355-ioplatformexpert-o-the-first-key-that-was-not-read-off-an-image.md) | **`iokit/Kernel/IOPlatformExpert.cpp` — the first key that was not read off an image** — `iokit_Kernel_IOPlatformExpert.o` (**11892 bytes**: `.text` 0x2E74 of **228 definitions**, five 4-byte `.text.*` COMDAT pieces with five `.group` sections, `.bss` 0x94, `.rodata` **0x13A4**, `.rodata.str1.1` 0x313, `.rodata.cst16` 0x10, `.init_array` 4, and **no `.data`, no `__DATA,__data`, no `__sysctl_set`**) is the only pool definer of 354's stop `_ZN22IOPlatformExpertDeviceC1Ev`: **8 resolved / 16 added** — `PEGetUTCTimeOfDay`, `PEHaltRestart`, `PEReadNVRAMProperty`, `PERemoveNVRAMProperty`, `PESetUTCTimeOfDay` and the constructor out, `_ZN16IOPlatformDevice9metaClassE` (`R 4`) and `gPlatformInterruptControllerName` (`B 4`) out — **eleven functions and five storage stand-ins in** (`SHA1Init`/`Update`/`Final`, `_Z17IODeviceTreeAllocPv`, `IODTCompareNubName`, `IODTResolveAddressing`, `IODTFindMatchingEntries`, `IOWorkLoop::workLoop`, `IORangeAllocator::withRange`, `IOMapper::setMapperRequired`, `IODTNVRAM::IODTNVRAM`; `IORangeAllocator::metaClass`, `IOInterruptController::metaClass`, `IODTNVRAM::metaClass`, `gIODTCompatibleKey`, `gIODTModelKey`) — for **856 → 864** undefined, **748 → 753 function, 108 → 111 storage**, all three exact. **All five created stand-ins are 4 bytes, so this is a step where 354's correction and the stale 0x40 model agree and 354's defect is not retested** — stated rather than assumed. **The stop landed name and key exactly**: `stub_hit=_Z17IODeviceTreeAllocPv` at `xnu_entry_stub_caller_v=0x8015ff40` (all three of `_v`, `_a`, `_e` agreeing), `caller-4` = `0x8015FF3C: bl 80164298`, **`abort_entries=0`** — and **the key was arithmetic, not a reading**: the object's `.text` was predicted at 0x8015D788 (after `bsd_kern_bsd_stubs.o`'s 0x980 ends and `xnu_arm_entry_last_kernel_constructor.o` contributes 0x0), the step's own map confirms both the section at **0x8015D788** and `initWithArgs` (`T 0x84`) at **0x8015FF28**, so the `bl` at object +0x14 lands on +0x18. It is the first predicted stop of the walk whose address exists in no previous image. **The named falsifier did not fire, and that is the run's other measurement**: `PE_state.deviceTreeHead != 0`, so the p1 == 0 branch (`IOService::init(OSDictionary*)`) was not taken and `PE_init_platform`'s `deviceTreeP` reached `PE_state` through the payload. The three calls the run made to get there are all real in this image: the constructor (`T 0x38`: `IOService::IOService(OSMetaClass const*)`, the vptr store, `OSMetaClass::instanceConstructed`), `StartIOKit+0x114`'s `ldr r0,[r0]; ldr r5,[r0,#0x340]; blx r5` (vtable `_ZTV22IOPlatformExpertDevice` at `.rodata` + 0x86C size 0x34C, relocation at `.rodata` + 0xBB4, and 0x86C + 8 + 0x340 = 0xBB4 exactly), and the four arguments `pexpert/arm/pe_init.c:279` names. Layout: `.text` closed **+0x4600** (0x1855A0 → **0x80189BA0**) against a placed sum of **+0x45E8** — fill **+0x18**, with every term measured (+0x2E74 body, +0x14 COMDAT, +0x13A4 `.rodata`, **+0x254 of the object's 0x313 string bytes placed**, +0x10 `.rodata.cst16`, −0x90 six retired bodies, −0x84 six retired name slots, +0x108 eleven created bodies, +0x164 eleven created slots) — and **the crossing was free**: the band's ends sit on opposite sides of 0x80188000 and the same side of 0x8018C000, so one `ALIGN(0x4000)` moved the whole `__DATA` segment and the `.bss` start by a flat 0x4000, which is why **five of the eight rows are exact and not banded**: `.data` **0x8018C000** size **0x198B8 unmoved**, `.sysctl_set` **0x801A58B8** (0x140), `.init_array` **0x801A59F8** (**0x68, twenty-six**), `.bss` start **0x801A5A80 exact**, image **1727072**, headroom **1185128**. **The `.bss` row is the one miss, 0x2C, and the culprit is the fill**: the stand-in net is `+0xC0` (five created slots at 0x40 against two retired) and shows as `realstubs.o`'s `.bss` going 0x28C4 → **0x2984**; the object's own 0x94 lands at 0x801DC030 (`IOPlatformExpert::gMetaClass`, `gPlatformInterruptControllerName` at 0x801DC048 — the retired stand-in's name, now a real definition) and the `*fill*` in front of `realstubs.o`'s `.bss` **grew 0x10 → 0x3C**, so nothing was absorbed. That makes the three cases a rule the ledger keeps re-learning: the object's bytes are absorbed when the alignment gap is at least as large as they are (353: 0x10 into 0x30; 354: 0x20 into 0x30) and are not when the gap is smaller (355: 0x94 into 0x30, and the gap grows) — **the fill is a function of the running total and is measured, never modelled**. `realstubs.o` exact on all three closed forms: `.text` **0x4698** = 753 × 0x18 − 0x90 + 0x108, `.rodata.str1.4` **0x453C** = 0x445C − 0x84 + 0x164, `.bss` **0x2984** = 0x28C4 + 0xC0. **The step's tooling defect, fixed here**: `tools/entry_object_effect.py` printed `len(image)` (10121 symbols) under the word "undefined" where the quantity meant is the number of names the link needs and the image does not provide — this step's **864**, which is the build's own `N symbol(s) undefined` line and the stub list's function-plus-storage record count. The deltas were always right and every block takes its base from the build's line, so nothing in the ledger moves — a wrong label can survive arbitrarily many steps when nothing depends on it. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301635 bytes, device back on its own on `MI 4LTE` release 10. Next: **`iokit_Kernel_IODeviceTreeSupport.o`** — the definer of `IODeviceTreeAlloc` — **11 resolved / 2 added → 855/748/107** (`.text` 0x2920 of 82 definitions, `.bss` 0x58, `.rodata.str1.1` 0x307, and **no `.rodata`/`.init_array`/COMDAT/`.data`**, so its closure has four terms and the only `__DATA` motion is four 0x40 retirements); **predicted stop `_ZN10IOWorkLoop8workLoopEv` at `initWithArgs+0x6C`, key `0x8015FF94`** — the stub 355 created, at the far end of the call this step retires — with the body read from the object per 348's rule (**0x694 bytes, 41 direct calls**) and its walk against this image putting **79 direct external targets on the path, not one of them a stub or absent** (verified with `nm -S -P`, because the first pass dropped the three-field record a size-less defined symbol prints and called the real `strncmp` at `T 0x80006cc0` "not in the image"); three named falsifiers: one of the 29 `blx` dispatches, the real `panic` at 0x8002DEB4 reached from object +0x4F0 on a failed DT lookup, or `IOService::init` returning zero |
| stage90 | [experiment-356](experiment-356-iodevicetreesupport-o-the-virtual-dispatch-the-tooling-cannot-see.md) | **`iokit/Kernel/IODeviceTreeSupport.cpp` — every row exact, and the stop the tooling cannot see** — `iokit_Kernel_IODeviceTreeSupport.o` (**10528 bytes**: `.text` 0x2920 of 82 definitions, `.bss` 0x58, `.rodata.str1.1` 0x307, and **no `.rodata`/`.init_array`/COMDAT/`.data`**) is the only pool definer of 355's stop `_Z17IODeviceTreeAllocPv`: **11 resolved / 2 added** — seven functions and four `B 4` storage stand-ins out, `IODeviceMemory::withSubRange`/`withRange` in — for **864 → 855** undefined, **753 → 748 function, 111 → 107 storage**, all three exact. **Every row of the layout came out exact or inside its band**, including the one the walk usually misses, because **`.data` was predicted from the boundary and not from the fill**: `.text` ended at 0x80189BA0 with 0x2460 of room below `0x8018C000` and the closure is at least +0x27F8, so no fill could have kept it — `.data` stepped a whole 0x4000 to **0x80190000** (size 0x198B8 unmoved), `.sysctl_set` **0x801A98B8**, `.init_array` **0x801A99F8** (0x68, 26) ending 0x801A9A60, `.bss` **0x801A9A80** − **0x801E29D8**, image **1743456** exact, headroom 1168936. `.text` closed +0x2A60 against a placed +0x2A6A (object `.text` at 0x80160610, its strings **0x272 of 0x307** placed at 0x801873F0), so fill **−0xA**; `realstubs.o` exact on all three again (`.text` 0x4620, `.rodata.str1.4` 0x448C = 0x453C − 0xFC + 0x4C, `.bss` 0x2884 = 0x2984 − 4×0x40); and the `.bss` row is inside its band **for the first time in ten steps**, with the absorption now measured from the other side (the object's 0x58 at 0x801E00C4, `realstubs.o`'s `.bss` at 0x801E0140, the `*fill*` shrinking 0x3C → 0x24 so the net is 0x58 − 0x18 = 0x40). **The stop is not the predicted one**: `stub_hit=_ZN9IOService6PMfreeEv` at `xnu_entry_stub_caller_v=0x8012ad60` = **`IOService::free+0x2c`** (`caller-4` = `0x8012AD5C: bl 80167548`), `abort_entries=0` — the walk deviated through a **chain of virtual dispatches** (`regEntry->init()` via vptr+0x50 = vtable+0x58 = `IOService::init(OSDictionary*)` read off `_ZTV9IOService` at 0x801808dc, `regEntry->release()` via vptr+0x14, and `release`'s own virtual `free()`), which is exactly falsifier (a) in its sharpest form — the class of call this walk's direct-call tooling has never been able to see. The freed IOService can only have come from `MakeReferenceTable` (`new IOService` is its only site, object +0x6B0; `IODeviceTreeAlloc` its only caller), and **three sites can free it, one eliminated**: `MakeReferenceTable`'s init-failure branch is ruled out four ways (`OSObject::init()` is `mov r0,#1; bx lr`; both kalloc paths work because `zalloc_internal` loads `[zone+0xa1]` so a NULL zone would abort and `abort_entries=0`; `IORecursiveLockAlloc`'s `if (!IOLockGroup) return NULL` cannot fire because `IOLibInit` stores the real `lck_grp_alloc_init` result at `0x801df508` and its call site 0x8011b260 precedes the `devsw_init` call site 0x8011b268 that 352 stopped at; and a NULL result would be dereferenced by `child->attachToParent`/`AddPHandle` — a virtual call on NULL, an abort), leaving `IODeviceTreeAlloc`'s per-child `child->release()` (IODeviceTreeSupport.cpp:186) or its root `parent->release()` (line 205): **both reach `IOService::free` through the same virtual `free()`, so the record cannot tell them apart** — the instrument names one frame, and this is the walk's first stop where the frames above it are not direct calls, so the site is the step's open question and 357's run names it. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301634 bytes, device back on its own on `MI 4LTE` release 10. Next: **`iokit_Kernel_IOServicePM.o`** — the definer of `PMfree` — **69 resolved (68 function, 1 storage) / 32 added (30 function, 2 storage) → 818/710/108**, the largest single-object retirement of the walk (69 names, on the largest `.text` it has linked: 0x113A4); **predicted `.data` steps 0x10000 to 0x801A0000** (the largest `__DATA` move of the walk: `.sysctl_set` **0x801B9980**, `.init_array` **0x801B9AD0** (0x6C, 27), `.bss` **0x801B9B40** size 0x3910C+fill, image **1809212**, and the boot_args move with it by construction: `ENTRY_ARGS_OFFSET = align_up(bss_end − ENTRY_BASE, 4096) + 0x1000`, so 0x801E4000 → 0x801F4000 and topOfKernelData 0x80300000 → 0x80400000); predicted stop **`_ZN10IOWorkLoop8workLoopEv` at `initWithArgs+0x6C`, key `0x8015FF94`**, with three named falsifiers — the release site was the per-child one, `IOService::init`'s failure path reached again from `initWithArgs+0x7C`, or a stub inside `IODTMapInterruptsSharing` arriving through one of its `blx` sites |
| stage90 | [experiment-357](experiment-357-ioservicepm-o-the-largest-retirement-and-a-fill-that-became-a-formula.md) | **`iokit/Kernel/IOServicePM.cpp` — the largest single-object retirement of the walk, and a question this instrument cannot ask** — the only pool definer of 356's stop `IOService::PMfree`, **69 resolved (68 function, 1 storage) / 32 added (30 function, 2 storage)** — the whole IOService power-management API out plus the `gCanSleepTimeout` stand-in, thirty functions in (the `IOEventSource`/`IOPowerConnection` APIs, `IOCommand`'s init and destructor) and two `gMetaClass` stand-ins — for 855 → **818** undefined, 748 → **710** function, 107 → **108** storage; **69** names against 349's 15 and 355's 8. **The stop's name and key fired to the byte**: `stub_hit=_ZN10IOWorkLoop8workLoopEv` at `xnu_entry_stub_caller_v=0x8015ff94` = `IOPlatformExpertDevice::initWithArgs+0x6c`, `abort_entries=0` — two frames of this step's own new code away (`PMfree` real ⇒ `IOService::free` runs out, `release` returns, `IODeviceTreeAlloc` finishes, `IOService::init` succeeds). **Every layout row exact**, including the largest `__DATA` move of the walk (`.data` `0x80190000` → **`0x801A0000`**, `.sysctl_set` **0x801B9980**, `.init_array` **0x801B9AD0** 0x6C/27, `.bss` **0x801B9B40**, `__bss_end` **0x801F2C58**, image **1809212**, headroom **1102760**) — but **three of the prediction block's own numbers were wrong, all the same way**: a stated value contradicting the formula printed beside it (`realstubs.o`'s `.rodata.str1.4` 0x43F0 where its own `0x448C − 0xAD8 + 0x48C` is **0x3E40**; `.bss` `0x2884 = 0x2884 − 0x40 + 0x80` whose right side is **0x28C4**; `topOfKernelData` 0x80400000 where the formula gives **0x80300000**), so the new tell is *a closed form written with both sides is self-checking — evaluate it*. **The `.bss` fill — weakest row since 346 — became a formula:** `new_pad = (old_pad − object_size) mod 64` reproduces all five of the walk's `.bss` padding measurements (353 0x20/0x30→0x10, 354 same, 355 0x94/0x10→0x3C, 356 0x58/0x3C→0x24, 357 0x174/0x24→0x30), which is the whole of what the ledger had been writing as separate cases of "absorption"; the row then closes `0x38F58 + 0x174 + 0x40 + 0xC = 0x39118`. **And 356's open question is re-stated as unanswerable**: both surviving release sites free only when an `attachToParent` did not take a retain, everything else on that path is real, and their two continuations reconverge before the next stub — a stop names the first stub met, so sites that reconverge before the next stub are indistinguishable to it (357's falsifier (a) assumed what 356's own 79-target walk had refuted). `.text` closure `+0x11680` vs placed `+0x11698` → fill −0x18; 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301638 bytes, device back on its own |
| stage90 | [experiment-358](experiment-358-ioworkloop-o-the-step-that-brings-a-whole-vtable.md) | **`iokit/Kernel/IOWorkLoop.cpp` — the step that brings a whole vtable, and a stop whose `bl` was in the map before the run** — the only pool definer of 357's stop `IOWorkLoop::workLoop`, **1 resolved / 3 added** (`IOCommandGate::commandGate`, pool `T 0x7C`; `IOCommandPool::metaClassE`, `IOInterruptEventSource::metaClassE`, both `R 4`) for 818 → **820** undefined, 710 → **710** function, 108 → **110** storage — **and `resolved = 1` is a lower bound, which is the step's point**: the object also defines `_ZTV10IOWorkLoop` (26 function slots), `IOWorkLoop::init` (0x128), `free` (0x1E0) and twenty more, and **not one of them is a stub in the image because nothing has ever referenced them directly** — a stub list is the closure of the walk's *direct* references, so a vtable-live family is invisible to it. **The stop fired to the byte**: `stub_hit=_ZN13IOCommandGate11commandGateEP8OSObjectPFiS1_PvS2_S2_S2_E` at `xnu_entry_stub_caller_v=0x80174494` = `IOWorkLoop::init+0xc0`, `abort_entries=0` — and the built image's own disassembly had already placed that `bl` at 0x80174490 (object `.text` at 0x801742EC, `init` at 0x801743D4), the first time a predicted key could be checked against the thing about to run. The vtable was read off the object's `.rodata` relocations (first function slot at `_ZTV + 8`, so `.rodata + 0x40` = `init` = slot 12 and `+0x44` = `free`), and `init`'s four "already have it" tests all fall through because `OSObject::nw` calls `kalloc_canblock` and then `__bzero(r0, size)`. **Every layout row exact**: `.text` at the top of its band (0x8019ED00), `.data`/`.sysctl_set` unmoved, `.init_array` **0x70** ending **0x801B9B40**, `.bss` **0x801B9B40** size **0x39198**, `__bss_end` **0x801F2CD8**, image **1809216**, headroom **1102632**, `args`/`topOfKernelData` unmoved — including **`the copied image ends at 0x801b9b40, 0 bytes below __bss_start`**, the first time that line reads zero. `.text` closure `+0x1080` vs placed `+0x108F` → fill −0xF; `realstubs.o` closes all three (`.text` 0x4290 = 710 × 0x18, `.rodata.str1.4` **0x3E64** = 0x3E40 + 0x24 = `sum(align4(len+1))` over the new 710-name list, `.bss` **0x2944** = 0x28C4 + 0x80). **The pad rule's sixth confirmation**: the object's 0x18 lands exactly on 357's `*fill* 0x30` address and the map prints `*fill* 0x801F0368 0x18` = `(0x30 − 0x18) mod 64`. One slip of the block's own, same class as 357's three: the band's distance to the boundary written 0x1291 where it is 0x12F1 — **no block reads its own prose**. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301672 bytes, device back on its own |
| stage90 | [experiment-359](experiment-359-iocommandgate-o-the-base-constructor-and-a-pad-that-lands-on-zero.md) | **`iokit/Kernel/IOCommandGate.cpp` — the base constructor whose object supplies the metaclass it hands it, and a pad that lands on zero** — the only pool definer of 358's stop `IOCommandGate::commandGate`, **1 resolved / 6 added** (six `IOEventSource` methods: `wakeupGate`, `checkForWork`, `openGate`, `closeGate`, `sleepGate(Pv,m)`, `sleepGate(Pv,y,m)`) for 820 → **825** undefined, 710 → **715** function, 110 → **110** storage. **Every row exact and the key fired to the byte** — `stub_hit=_ZN13IOEventSourceC2EPK11OSMetaClass` at `xnu_entry_stub_caller_v=0x8017534c`, `abort_entries=0` — and the *built image had already disassembled that `bl` at 0x80175348* before the device was touched, the second step in a row whose key the map printed first; the same disassembly holds the whole chain (`80175334: bl OSObject::nw`, `80175348: bl IOEventSource::IOEventSource(OSMetaClass const*)`, then the `str` of `vtable for IOCommandGate`'s first slot at `0x8019AAB4 + 8`). The stop's own body is what the step makes real: `movw r7, #936 / movt r7, #32799` is **`r7 = 0x801F03A8 = IOCommandGate::gMetaClass`** — this object's own `.bss`, a storage stand-in until this step — and it is `r1` when the base constructor is called. **The pad rule's seventh confirmation is its first genuine zero**: the object's 0x18 `.bss` lands at 0x801F03A8 exactly in the `*fill* 0x18` in front of `realstubs.o`'s `.bss`, `new_pad = (0x18 − 0x18) mod 64 = 0`, the map prints no fill there, and the three `.bss` inputs read 0x801F0390 / 0x801F03A8 / 0x801F03C0 unbroken. Layout: `.text` 0x8019ED00 → **0x8019F5C0** (closure +0x8B5 against a placed +0x8C0, fill **+0xB**), `.init_array` 0x70 → **0x74** ending 0x801B9B44, `.bss` start **0x801B9B80** (+0x40) with its size unmoved at 0x39198, `__bss_end` 0x801F2D18, image 1809220, headroom 1102568, `args`/`topOfKernelData` unmoved; `realstubs.o` exact on all three closed forms (`.text` 0x4308 = 715 × 0x18, `.rodata.str1.4` 0x3EF4 = 0x3E64 − 0x40 + 0xD0, `.bss` 0x2944). 358's own block said the pad rule "predicts a pad of zero" where the measurement was 0x18 — corrected in place, because the zero it describes is this step's. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301648 bytes ending in the kernel's own `No errors detected`, device back on its own |
| stage90 | [experiment-360](experiment-360-ioeventsource-o-the-first-text-that-shrinks-and-a-stub-address-arithmetic-refutes.md) | **`iokit/Kernel/IOEventSource.cpp` — the first step whose `.text` shrinks, and a stub address arithmetic refutes** — `iokit_Kernel_IOEventSource.o`, the only pool definer of 359's stop `_ZN13IOEventSourceC2EPK11OSMetaClass` and of the six methods 359 created, so the accounting is *subtractive*: **22 resolved (21 function, 1 storage) / 0 added** (`gMetaClass` `B 0x18` out as a storage stand-in, twenty-one functions out) for 825 → **803** undefined, 715 → **694** function, 110 → **109** storage, all three exact. The object: `.text` **0x2A4** (676) of 37 definitions, one 4-byte `.text.*` COMDAT piece, `.bss` 0x18, `.rodata` **0xB4**, `.rodata.str1.1` 0xE, an `.init_array` entry, and no `.data` and no `__sysctl_set`. **The stop fired to the byte**: `stub_hit=vm_shared_region_init` at `xnu_entry_stub_caller_v=0x8000e6cc`, `abort_entries=0`, and the built image had printed that `bl` at 0x8000E6C8 before the device was touched. It is the *enclosing* frame's next call — `kernel_bootstrap_thread` calls `PE_init_iokit` at 0x8000E6B0 and the three stubs follow at 0x8000E6C8/0x8000E6CC/0x8000E6D0 with `ml_set_interrupts_enabled` between them as the inlined `spllo()` — because the step's whole family (the constructor, `setAction`, `setWorkLoop`, `init`, and the `gMetaClass` its own base-constructor call is handed) is real after it: **the sixth frontier kind in its plainest form**. Every row exact (`.data` 0x801A0000, `.sysctl_set` 0x801B9980, `.init_array` 0x801B9AD0 (0x78, thirty) ending 0x801B9B48, `.bss` 0x801B9B80 (0x39198), `__bss_end` 0x801F2D18, image 1809224, headroom 1102568, `args`/`topOfKernelData` unmoved). `.text` closes `−0x170` against a placed `−0x160` with **two newly-measured terms** — the align4 pad that closes a mergeable string run (+0x2) and the section's trailing `ALIGN(0x20)` (+0x10, a real term whenever the placed end is not 32-aligned) — and `realstubs.o` is exact on all three forms (`.text` **0x4110** = 694 × 0x18, `.rodata.str1.4` **0x3C10** = 0x3EF4 − 0x2E4, `.bss` **0x2904**). **The pad rule's eighth confirmation is the wrap branch**: the retired `B 0x18` slot at 0x801F03C0 is taken by the object's own `.bss`, and the map prints `*fill* 0x801F03D8 0x28` = `(0x00 − 0x18) mod 64`, closing `0x39198 − 0x40 (the stand-in) + 0x18 (the object) + 0x28 (the fill) = 0x39198` — which is why the size and `__bss_end` do not move at all. **One field of the prediction was 359's**: the block's stub addresses 0x801790DC and 0x80178D64 are 0x10 past the 0x18 grid from this image's `realstubs.o` base 0x80175BD4 (`0x3508 = 565 × 0x18 + 0x10`) and cannot be stub bodies in it — 0x801790DC is exactly 359's 594th stub against *that* image's base 0x8017592C (`0x37B0`) — and the check that catches this class with no run and no map is `(address − base) mod 0x18 == 0`, because **a predicted stub address is the one field of a prediction this walk has never once verified** (the run reports the key and nothing else). 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301633 bytes whose last line is the kernel's own `No errors detected`, device back on its own |
| stage90 | [experiment-361](experiment-361-vm-shared-region-o-a-stop-the-scheduler-hands-the-walk.md) | **`osfmk/vm/vm_shared_region.c` — the first stop the scheduler hands the walk, and three slips in one closure band** — `osfmk_vm_vm_shared_region.o`, the pool definer of 360's stop `vm_shared_region_init`: **13 resolved / 0 added** for 803 → **790** undefined, 694 → **682** function, 109 → **108** storage, all exact; the object is `.text` **0x3FFC**, `.data` 0x18 plus a `__DATA,__data` 0x18, `.bss` **0x70**, `.rodata.str1.1` **0x85**, and because 0x8019F460 + 0x3FFC crosses `0x801A0000`, **`.data` steps a whole `ALIGN(0x4000)` to 0x801A4000** — the walk's second `__DATA` step, by the same mechanism as 357's. **The stop is neither of the two the block named**: `stub_hit=compute_averages` at `xnu_entry_stub_caller_v=0x800a31f8` (the `bl` at 0x800A31F4 = `sched_timeshare_maintenance_continue+0x108`), `abort_entries=0` — and it has **no calling frame above it at all**, because the frame was entered by a context switch: the maintenance thread was created by `sched_startup`'s `kernel_thread_start_priority(sched_init_thread, …)` which `kernel_bootstrap_thread` called at 0x8000E5AC *before* `PE_init_iokit`, and it first ran at the preemption point `spllo()` (0x8000E6C4, `ml_set_interrupts_enabled`, real and inlined). **A seventh frontier kind: a stop the scheduler hands you** — the instrument's `caller − 4` idiom still names the `bl`, but "what runs next" is no longer "what does this code call next", which is exactly the assumption the block made. Both predictions refuted (`ccdrbg_factory_yarrow` at key 0x8003827C and the fallback `throttle_init` at 0x8003A9FC) and the reason is structural, not arithmetic. Every row but `.text` exact (`.data` 0x801A4000 (0x199B0), `.sysctl_set` 0x801BD9B0, `.init_array` 0x801BDB00 (0x78) ending 0x801BDB78, `.bss` 0x801BDB80 (0x391D8), `__bss_end` 0x801F6D58, image 1825656, headroom 1086120, `args` +2064384); `.text` closed **0x801A3280** against a predicted 0x801A31E0, and **the 0xA0 band had four separate slips, each now a measured rule** — 12 retired bodies is `−0x120` and not `−0x1B0` (arithmetic), **a storage stand-in has no name slot** so the retired-name term is `−0x12C` and not `−0x148` (`realstubs.o`'s `.rodata.str1.4` = 0x3AE4 = 0x3C10 − 0x12C), the object's strings are `+0x084` and not `+0x088` (its 0x85 *replaces* the 0x2 pad in front of `macho.o`'s piece, plus a 0x1 pad of its own, measured as `*fill* 0x8019EC97 0x1`), and the tail's own fill `−0x008` was not modelled at all. **The pad rule's ninth confirmation, from a pad of 0x28**: the object's 0x70 at 0x801F43D8 ends at 0x801F4448 where the map prints `*fill* 0x801F4448 0x38` = `(0x28 − 0x70) mod 64`, closing `0x39198 − 0x40 + 0x70 + 0x10 = 0x391D8`. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301628 bytes ending `No errors detected`, device back on its own |
| stage90 | [experiment-362](experiment-362-sched-average-o-the-run-that-panics-and-the-machine-with-no-platform-driver.md) | **`osfmk/kern/sched_average.c` — the run that panics, and the machine that has no platform driver** — `osfmk_kern_sched_average.o`, the pool definer of 361's stop `compute_averages`: **5 resolved (1 function, 4 storage) / 1 added** (`compute_averages` out, four storage stand-ins out — `avenrun` `B 0xC`, `mach_factor` `B 0xC`, `sched_load_average` `B 0x4`, `sched_mach_factor` `B 0x4` — and `compute_averunnable` in) for 790 → **786** undefined, 682 → **682** function, 108 → **104** storage; the object is `.text` 0x48C, `.data` 0xA8, `.bss` 0x38 and nothing else. **Every count and every row exact** (`.text` **0x801A3720**, `.data` 0x801A4000 (0x19A58), `.sysctl_set` 0x801BDA58, `.init_array` 0x801BDBA8 (0x78, thirty) ending 0x801BDC20, `.bss` **0x801BDC40** (0x0390D8), `__bss_end` 0x801F6D18, image 1825824, headroom 1086184, `args` +2064384, `topOfKernelData` +3145728, `the copied image ends at 0x801bdc20, 32 bytes below __bss_start`) and **the pad rule's tenth confirmation is its third zero**: the object's 0x38 lands exactly in 361's `*fill* 0x38`, ends at 0x801F4540 where `realstubs.o`'s `.bss` (0x27C4) begins, and the `.bss` run reads as one unbroken chain with not a byte of fill. **But the run did not stop: it panicked inside real code** — `real XNU entry: exception: undefined instruction`, no `stub_hit`, no `stub_caller` (`_v`, `_a`, `_e` all zero), `abort_entries=0`, `why_byte=0x65` ('e') where every stop of this walk reads `0x61` ('a'), `undef_pc=0x8002dd88` / `undef_lr=0x8002dd8c`. The faulting instruction is the deliberate `udf #65006` (`TRAP_DEBUGGER` for `__arm__`, `debug.c:120-121`) inside **`DebuggerTrapWithState`**, preceded by `bl DebuggerSaveState`; of the tree's four calls to that function only `debug.c:645` carries a format string (`DebuggerTrapWithState(DBOP_PANIC, "panic", panic_format_str, …)`), and the trap frame agrees on every field it can (`frame_r4 = 0` = `panic_options_mask` low, `frame_r5 = 1` = `db_proceed_on_sync_failure`). **`trap_r9_fmt=0x8019DE16` and the image's own `IOPanicPlatform::start` loads exactly that address into `r0` before `bl panic`** (`movw r0,#0xde16 / movt r0,#0x8019`), with the other arm loading 0x8019DDFE = `(unknown platform name)`. The image's bytes at that address are `"Unable to find driver for this platform: \"%s\".\n"` **with the quotes and backslashes in them**, because under `CONFIG_EMBEDDED` Apple's own macro is `#define panic(ex, ...) (panic)(# ex, ## __VA_ARGS__)` (`debug.h:414-419`): the image's panic literals are *source text*, and a trap's format field is a source-text address. **The chain that reached it**: `IOStartIOKit.cpp:155` `rootNub = new IOPlatformExpertDevice`, `:165` `rootNub->registerService()` → the async `_IOServiceJob::startJob` (360's own falsifier (a), runnable only once `spllo()` made the boot preemptible) → matching against the catalogue's **one** personality (`KernelConfigTables.cpp:35`: `IOClass = IOPanicPlatform; IOProviderClass = IOPlatformExpertDevice; IOProbeScore = 0:32`) → `OSMetaClass::allocClassWithName` (`IOService.cpp:3296-3301`) → `IOPanicPlatform::start` (`IOPlatformExpert.cpp:1725-1733`), Apple's designed fallback. So the panic is correct XNU behaviour reporting a real absence — **the walk's eighth frontier kind: an event handed to the walk by the kernel itself, in real code, with no `stub_hit`**, the outcome 361's falsifier (c) said the instrument cannot report as a stop — and 363 must supply the platform expert, since the tree has none: `IOPanicPlatform` is the only concrete one and panics by design, `IODTPlatformExpert` is `OSDefineMetaClassAndAbstractStructors` (`IOPlatformExpert.cpp:1243`), and `ApplePlatformExpert` is abstract with no implementation in the tree at all. One decode table also moved under the instrument: `fleh_undef`'s prologue now saves six words (including `str lr,[sp,#20]`), so the frame block's +20 is the trapping `lr` and the `frame_sl`/`frame_lr` keys read uninitialized stack — visible in four agreeing values (`frame_r9 = undef_lr`, `frame_r8 = trap_r8_args`, `frame_r4 = 0`, `frame_r5 = 1`), and the comment now carries the current prologue. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 302277 bytes whose last line is the kernel's own `No errors detected`, device back on its own |
| stage90 | [experiment-363](experiment-363-platform-expert-the-driver-apple-does-not-have.md) | **The platform expert this machine needs is a class Apple's tree does not contain — so the walk authors one, and the panic stops.** Two new objects, neither in any manifest: `stages/stage90/xnu_platform/MSM8974PlatformExpert.cpp` — a concrete `IODTPlatformExpert` whose entire content is its metaclass, the two pure virtuals the base leaves (`deleteList`, `excludeList`) and a `start` that forwards to `super::start`, written out also because rule 358 says a class with no virtual of its own gets no vtable and a vtable nothing defines arrives as a storage stand-in — and `stages/stage90/xnu_platform/stage90_platform_config_tables.c`, which **replaces** `iokit_KernelConfigTables.o` one object for one object with this machine's personality in front of Apple's `IOPanicPlatform` fallback, score `1616:32`, carrying `'IONameMatch' = "qcom,msm8974-xnu-stage90"` (the root node's `compatible`, quoted because the old-style plist lexer's unquoted form takes only `[A-Za-z0-9-]`) and required because `IODTPlatformExpert::probe` ends in `provider->compareNames(getProperty("IONameMatch"))`, false for a missing property. **The step is behaviour, not retirement: 0 resolved / 0 added**, 786 undefined unchanged (682 function + 104 storage), `realstubs.o` unchanged in all three sections (`.text` 0x3FF0 = 682 x 0x18, `.rodata.str1.4` 0x3AE4, `.bss` 0x27C4) — and **362's `Unable to find driver for this platform` did not recur**: the class was found by name, the probe matched, `startCandidate` called our `start`, and Apple's `IOPlatformExpert::start` ran until it reached `stub_hit=_ZN8IOMapper17setMapperRequiredEb` at `xnu_entry_stub_caller_v=0x8015d8fc` (= `IOPlatformExpert::start + 0x84`, the `bl` at 0x8015D8F8, same address as in 362 because both objects link after it) — **name and key exactly as predicted**. Every section boundary below `.text` exact: `.text` end **0x801A3D80** (text 1719680), class `.text` 0x8017A008/0x150, `realstubs.o` `.text` 0x8017A1B4, `.data` 0x801A4000/0x19A58 unmoved with the table's word back at 0x801BC2A0, `.sysctl_set` 0x801BDA58/0x150, `.init_array` 0x801BDBA8/**0x7C** with the class's entry at 0x801BDC1C and the sentinel still last at 0x801BDC20, `.bss` 0x801BDC40/**0x39118**, `__bss_end` **0x801F6D58** — the pad rule's **eleventh** confirmation (362's was the tenth) and the first where the pad *grows* (`(0 - 0x18) mod 64` = 0x28, `realstubs.o`'s `.bss` pushed 0x801F4540 → 0x801F4580) — image **1825828**, headroom **1086120**, and `args`/`topOfKernelData`/`dt` all unmoved. **Three misses, all recorded**: the `.rodata` rows landed **8 low** (0x8019F330/0x8019F770/0x8019F8D4), the raw `.text` end moved +0x660 where the model said +0x670 with the prediction's *explanation* wrong though its value right, and the `trap_r9_fmt` falsifier value was wrong — the panic string is at **0x8019E02B**, not 0x8019E486, because the `.rodata` run has **two shift regimes** (+0x215 before the class's own `.rodata`, +0x670 after) and lifting the end-of-section shift onto a mid-run input is off by exactly 0x45B. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `abort_entries=0`, `xnu_entry_checks=5`/`failures=0`, no `exception:` line, device back on Android on its own |
| stage90 | [experiment-364](experiment-364-iomapper-o-the-first-data-move-in-five-steps.md) | **`iokit/Kernel/IOMapper.cpp` — the definer of 363's stop, and the step that puts the walk four statements deeper into Apple's `IOPlatformExpert::start`.** **3 resolved (2 function, 1 storage) / 0 added** — `setMapperRequired`, `waitForSystemMapper` and the `IOMapper::gSystem` stand-in out, nothing in — for 786 → **783** undefined, 682 → **680** function, 104 → **103** storage, all three exact. `waitForSystemMapper` coming real is the useful half: `IOMapper::get()` is `if ((uintptr_t) gSystem & kWaitMask) waitForSystemMapper()` and `kWaitMask` is 3 while `setMapperRequired` writes 0 or 2, so a zero `gSystem` never enters that loop. The object's body is eleven instructions whose every callee is real — and **`IOLockLock`/`IOLockUnlock` exist in no symbol list at all**, because `IOLocks.h:99-126` makes them **macros** to `lck_mtx_lock`/`lck_mtx_unlock` under `IOLOCKS_INLINE`; the check is those three names, all real. `sMapperLock`'s lock is live because the object's `_GLOBAL__sub_I_IOMapper.cpp` is an `.init_array` entry. **The stop fired name and key exactly as predicted**: `stub_hit=_ZN16IORangeAllocator9withRangeEmmmm` at `xnu_entry_stub_caller_v=0x8015d98c` (= `IOPlatformExpert::start + 0x114`, the same address in both images because the object links after `IOPlatformExpert.o`), `abort_entries=0`, no panic — so `setMapperRequired` **ran and returned**, and the four real calls behind it (`OSDictionary::withCapacity(1)`, `IOLockAlloc`, `OSData::withBytesNoCopy`, `OSSymbol::withCStringNoCopy`) plus two virtual dispatches (`IORegistryEntry::setProperty("clock-frequency",…)` and `_ZNK8OSObject7releaseEv`, which is `const` — the mangling without it finds nothing and looks like a stub) all did too. **`.data` moved a whole `ALIGN(0x4000)` to 0x801A8000** — the first `.data` move in five steps, predicted from "363 left 0x280 of room, this step needs 0xFD0" rather than from the shift model, which was 0x80 high and got the bucket right anyway; its *size* is unchanged at 0x19A58 because the object's 4 bytes were swallowed by `.data`'s closing `ALIGN(8)` (353's rule again). `.sysctl_set` 0x801C1A58 (0x150), `.init_array` 0x801C1BA8 (**0x80**, thirty-two, the object's entry at 0x801C1C1C and the sentinel still last at 0x801C1C24), `.bss` 0x801C1C40 (**0x390D8**), `__bss_end` **0x801FAD18**, image **1842216**, headroom **1069800**, **`args` +2080768** (moved) with `topOfKernelData` +3145728 unmoved — the first step since 331 where those two differ. The `.bss` pad narrowed 0x28 → **0xC** (`*fill* 0x801f8574 0xc`), the pad rule's **twelfth** confirmation, and `realstubs.o`'s `.bss` shrank 0x27C4 → 0x2784 — one retired storage stand-in is one 0x40 slot. **Two misses, one cause**: retiring two function stubs also shrinks `realstubs.o`'s `.text` (0x3FF0 → 0x3FC0) and `.rodata.str1.4` (0x3AE4 → 0x3A9C), which the layout model cannot know because it reads input sizes from a map — so it is exact only for a step that does not change the stub set, now a written caveat. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log ends `No errors detected`, device back on Android on its own |
| stage90 | [experiment-365](experiment-365-the-platform-experts-plane-ends-at-the-uuid-generator.md) | **`iokit/Kernel/IORangeAllocator.cpp` — the definer of 364's stop, and the step that ends the platform expert's plane at the kernel's UUID generator.** **2 resolved (1 function, 1 storage) / 0 added** — `IORangeAllocator::withRange` and the `IORangeAllocator::metaClassE` stand-in out, nothing in — for 783 → **781** undefined, 680 → **679** function, 103 → **102** storage, all three exact; `withRange`'s body is real from end to end (`.text` 0x9D4, `.rodata` 0xAC, `.rodata.str1.1` 0x11, `.bss` 0x1C, `.init_array` 4, no `.data`), and `gIORangeAllocatorLock` is a **tentative** definition so it never enters an undefined list. **The stop fired name and key exactly as predicted**: `stub_hit=uuid_generate` at `xnu_entry_stub_caller_v=0x8014e540` (= `IOPMrootDomain::start + 0x5F0`, the `bl` at 0x8014E53C), `abort_entries=0`, no panic — so the run went through everything `IOPlatformExpert::start` had left (`setPlatform`, then all three dispatches of `PMInstantiatePowerDomains` — `init(0)` slot +0x50, `attach` slot +0x1c4, tail `start` slot +0x168) and 0x5F0 bytes into `IOPMrootDomain::start`, which in source is line 1200's `initializeBootSessionUUID()` **inlined** into `start` (`IOPMrootDomain.cpp:3247-3248`; the out-of-line copy at 0x8014EBEC is an independent second caller of the same stub) with its `if (_bootSessionUUID == 0)` guard gone in the linked code. **A measurement defect, now carried as a rule: "no stub on the straight-line path" is not "the closure is real"** — `xnu_entry_callwalk.py` says that for all three of those functions, the first version of the block read it as a clean closure and even wrote that `IOPMrootDomain::start` has "not one non-real direct call in it", and the function has **three**; the tool stops reading a function's own instructions at its first `blx`, which for `start` is 0x10 bytes in. What answers instead is the scan the other way round — **every `bl`/`b` whose target lands inside `realstubs.o`'s `.text`** (0x8017B778..0x8017F720), grouped by caller — which names `uuid_generate` at +0x5F0, then `uuid_unparse_upper` and `PMPowerStateQueue`, and shows that only **two vtables in the whole image** have a slot pointing into that range (`_ZTV11IOPMRequest +0x30 -> _ZN9IOCommand4initEv`, eight slots of `_ZTV24IOCPUInterruptController -> _ZN21IOInterruptController*`), neither touched before +0x5F0 — so no indirect dispatch can stop the run earlier, and the frontier is `libkern/uuid/uuid.c:117`, i.e. `libkern_uuid_uuid.o`, **named before the run**: 366's object. Layout: counts exact, every rule-derived row exact, and **the `.data` claim refuted by one never-performed hex subtraction** — the block said "only 0x320 left" where `0x801A8000 − 0x801A4CE0 = 0x3320` and the step's inputs were 0xA40, so `.data` stayed at **0x801A8000** (0x19A58, unmoved) with every row below it 364's; the `.text` end landed in its band at **0x801A5720** (text 1726240), the object's `.text` at **0x8017ABF4**/0x9D4 with its COMDAT at 0x8017B5C8, its `.init_array` word 0x801C1C20 as the thirty-third entry with the sentinel still last, `realstubs.o` at **0x8017B778** (0x3FA8 = 679 × 0x18) / `.rodata.str1.4` **0x801A12F8** (0x3A74) / `.bss` **0x801F85C0** (0x2744), `.sysctl_set` 0x801C1A58 (0x150), `.init_array` 0x801C1BA8 (0x84) ending 0x801C1C2C, `.bss` 0x801C1C40 (0x390D8), `__bss_end` **0x801FAD18**, image **1842220**, headroom **1069800**, `args`/`topOfKernelData` unmoved. **The pad rule's thirteenth confirmation, 0xC → 0x30** (`*fill* 0x801f8590 0x30`), and `.bss`'s *size* unmoved because +0x1C new object − 0x40 retired stand-in + 0x24 wider pad cancel exactly. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301625 bytes ending in the kernel's own `No errors detected`, device back on Android on its own |
| stage90 | [experiment-366](experiment-366-uuid-c-the-stop-one-frame-past-the-object.md) | **`libkern/uuid/uuid.c` — the definer of 365's stop, and a stop one frame past the object being linked.** **5 resolved (5 function) / 2 added (2 function), 0 storage either way** — `uuid_compare`, `uuid_generate`, `uuid_parse`, `uuid_unparse`, `uuid_unparse_upper` out and `sscanf`, `uuid_get_ethernet` in — for 781 → **778** undefined, 679 → **676** function, 102 → **102** storage, all three exact. The stop is **not** the object's own name: `uuid_generate` is 0x34 bytes and calls one thing, `read_random` (real, 0x800381D4), and that is where the frontier is — the first time anything in this kernel asks for random bytes. `read_random` is `osfmk/prng/random.c:575` with `prng_infop` inlined, and the whole decision is visible: `pp->infop` is NULL, `prng_ccdrbg_factory` is **not**, so control reaches `8003826c: ldr r2,[r6]` / `80038278: blx r2`. **The factory is non-NULL because of one call the boot already made**: `kernel_bootstrap_thread` calls `prng_cpu_init(master_cpu)` at **0x8000E690**, 0x20 bytes *before* the `PE_init_iokit` at 0x8000E6B0 the walk has been inside since 360, and `prng_cpu_init` (`random.c:497`) ends with `prng_factory_register(ccdrbg_factory_yarrow)` — real, and it stores **the address of the yarrow stub itself** into the global. The argument is literally `master_cpu`, so the `if (cpu != master_cpu) return;` above it cannot be taken. A stop with an unusual shape: **kind 1, but the object that retires it is not the object being linked** — a five-step-old stub that only became *reachable* now. **The run**: `stub_hit=ccdrbg_factory_yarrow` at `xnu_entry_stub_caller_v=0x8003827c`, `abort_entries=0`, no `exception:` line, `why_byte=0x61`, log ends `No errors detected` — **the name exactly as predicted, no falsifier fired**; the only field that missed is the key's convention by 4 bytes (the block quoted the `blx r2` at 0x80038278 where the field holds the return address, call + 4 — 365's 0x8014E540 for a `bl` at 0x8014E53C, 364's 0x8015D98C for 0x8015D988, 354's 0x8011B2B4 for 0x8011B2B0). Layout: **every section boundary below `.text` is 365's row unchanged** (`.data` 0x801A8000/0x19A58, `.sysctl_set` 0x801C1A58/0x150, `.init_array` 0x801C1BA8/0x84 ending 0x801C1C2C, `.bss` 0x801C1C40/0x390D8, `__bss_end` 0x801FAD18, image 1842220, headroom 1069800) — for a structural reason, not luck: **the object contributes no `.bss` at all** and no storage stand-in retires, so the pad keeps its 0x30 and `.bss`'s size *cannot* change; `.init_array` is unmoved because `uuid.c` is C. The object's `.text` landed exactly where predicted, **0x8017B5CC** (0x438), and `realstubs.o`'s `.text` at **0x8017BBB0**. The `.data` argument was **shown this time, not asserted** (`0x801A8000 − 0x801A5720 = 0x28E0` of room against a <0x500 step). **Four misses, all in the block's own arithmetic.** (1) `676 × 0x18` was written as 0x3F90 and it is **0x3F60** — 0x3F90 = 678 × 24, so the term needed was `−0x48`, not `−0x18`; with it the estimate is `0x801A5720 + 0x438 + 0x10 + 0xE1 − 0x48 − 0x34 = 0x801A5BCD` against a measured **0x801A5BE0**, the remaining 0x13 being alignment. 365's rule was *show the subtraction*; this step's is **evaluate the closed form**. (2) The `.rodata`-run row came from `predict_layout.py` without checking the tool's own output for self-contradiction — it printed `.text libkern_uuid_uuid.o NEW 0x801a5820`, a `.text` input placed *after* the `.rodata` run has begun, because its `:ANCHOR` rule found no successor and appended; every `.rodata` row it printed was consequently **0x3F0 low** and the block copied 0x801A13EC with a mis-scaled ±0x40 caveat. (3 and 4 are the measurement's): the `.rodata` run shifted by exactly **+0x3F0** — IORangeAllocator's `.rodata` 0x801A0C94 → **0x801A1084**, i.e. the `.text` run's own `+0x438 − 0x48` — which is 320's run-order rule measured directly and would have caught miss 2 before the build; and the name-slot arithmetic was **exact**, `realstubs.o`'s `.rodata.str1.4` at **0x801A17D8 (0x3A40)** = 0x3A74 − 0x50 (five retired `align4(len + 1)` = 0x10, 0x10, 0xC, 0x10, 0x14) + 0x1C (two created: `sscanf` 0x8, `uuid_get_ethernet` 0x14). 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `xnu_entry_checks=5`/`failures=0`, log 301633 bytes ending in the kernel's own `No errors detected`, device back on Android on its own |
| stage90 | [experiment-367](experiment-367-prng-yarrow-c-the-step-whose-counts-go-up.md) | **`osfmk/prng/prng_yarrow.c` — the definer of 366's stop, and the first step since 342 whose counts go *up*.** **1 resolved (1 function) / 6 added (6 function), 0 storage either way** — `ccdrbg_factory_yarrow` out, `prngInitialize`/`prngInput`/`prngOutput`/`prngForceReseed`/`FIPS_SHA1Init`/`FIPS_SHA1Update` in — for 778 → **783** undefined, 676 → **681** function, 102 → **102** storage, all three exact. Five more stubs than it retires is not a regression: it retires the name the run stopped on, and the six it creates are the next six frontiers — the object is a leaf whose references point into a library nothing else in the image touches (`osfmk_prng_YarrowCoreLib_src_prng.o`, `osfmk_prng_fips_sha1.o`; the latter measures 0/0 so it was never the frontier). `.text` **0x494**, **nine** definitions — five `T` (`add_blocks`, `CalculateCRC`, `random_block`, `fips_initialize`, `ccdrbg_factory_yarrow`) and four local `t` (`yarrow_init`, `yarrow_generate`, `yarrow_reseed`, `yarrow_destroy`, the four the factory stores) — `.rodata` **0x414** (`g_crc_table` 0x400 + `kKnownAnswer` 0x14, the FIPS known-answer vector), `.rodata.str1.1` **0x92**, `.bss` **0x2C** (`zeros`), no `.data`/`.sysctl_set`/`.init_array`. **The stop is two indirect dispatches deep**: the factory is 0x34 bytes and makes **no call at all** — it writes `infop->init` = `yarrow_init` — so after `prng_ccdrbg_factory` returns, `read_random` allocates `kalloc(infop->size)` = `kalloc(64)`, reads 64 bytes through the real `entropy_buffer_read`, and reaches `ccdrbg_init`'s `blx r6` at **0x800382FC**, whose target this time **is real** (it is `yarrow_init`, in the object being linked); `yarrow_init` (+0x2C4) then makes its first call, `prngInitialize`, which is a stub **this step created**. That is the shape 365 named — a stop two virtual dispatches on, inside a plane the step makes reachable — and 367 is the first step to exercise it end to end. **A composed key, confirmed end to end for the first time**: `0x8017BCEC` appears in no earlier image, and it is `uuid .text 0x8017B5CC + 0x438 = 0x8017BA04`, `yarrow_init` at object +0x2C4, the `bl` at object +0x2E4, field = call + 4 (366's convention, now written out because 366 got it wrong). **Prediction exact**: `stub_hit=prngInitialize` at `xnu_entry_stub_caller_v=0x8017bc**e**c`, `abort_entries=0`, `abort_first_dfar=0`, no `exception:` line, no panic, `xnu_entry_checks=5`/`failures=0`, log 301626 bytes ending in the kernel's own `No errors detected` — and every one of the five falsifiers checked in the log and silent. The `blx r6` not faulting is itself a measurement: it says the factory really wrote a **code** pointer, not a zero or a storage stand-in. Layout: counts exact, every boundary-derived row exact — object `.text` **0x8017BA04** (0x494), `realstubs.o` **0x8017C044 (0x3FD8 = 681 × 0x18)**, `.text` end **0x801A65E0** (the `.data` argument shown and evaluated: `+0x50C + 0x4E6 = 0x9F2`, closing to 0x801A65E0, against `0x801A8000 − 0x801A65E0 = 0x1A20` of room, so `.data` **cannot** move and did not), `.data` 0x801A8000 (0x19A58), `.sysctl_set` 0x801C1A58 (0x150), `.init_array` 0x801C1BA8 (0x84) ending 0x801C1C2C, `.bss` 0x801C1C40 (0x390D8), `__bss_end` **0x801FAD18**, image **1842220**, headroom **1069800** — all unchanged, for a structural reason: **the pad rule's fourteenth confirmation, in its cancellation form** (the one 366 could not exercise, its object having no `.bss` at all) — object `.bss` 0x801F8578 (0x2C), the fill in front of `realstubs.o`'s `.bss` **0x30 → 0x04** = `(0x30 − 0x2C) mod 64`, so `+0x2C` object and `−0x2C` pad cancel exactly. **One miss, and it is 366's miss 2 again with 366's own check in hand**: the `realstubs.o` `.rodata.str1.4` row was written **0x801A1C80** where the map says **0x801A2190** — the size exact to the byte (`0x3A40 − 0x18 + 0x58 = 0x3A80`) and the offset short by **+0x510**, because only the `.rodata` run's own new inputs (0x4A6) were applied and not the `.text` run's net delta (`+0x494 + 0x78 = +0x50C`) that stands in front of it; with both terms `0x801A17D8 + 0x50C + 0x4A6 = 0x801A218A`, against 0x801A2190 with 0x6 of alignment. 366's paragraph ends by calling that run-order rule "a check that would have caught miss 2 before the build" — this step had it in the same file, thirty lines up, and did not run it: **a rule written down in a defect note is not a check until it is arithmetic in the prediction.** 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, device back on Android on its own |
| stage90 | [experiment-368](experiment-368-prng-c-the-definer-of-367s-stop-and-a-single-section-object.md) | **`osfmk/prng/YarrowCoreLib/src/prng.c` — the definer of 367's stop, and the first object in this walk with a single allocated section.** **4 resolved (4 function) / 11 added (11 function), 0 storage either way** — `prngForceReseed`/`prngInitialize`/`prngInput`/`prngOutput` out and `YSHA1Final`/`YSHA1Init`/`YSHA1Update`/`comp_end`/`comp_get_ratio`/`comp_init`/`mmFree`/`mmGetPtr`/`mmInit`/`mmMalloc`/`trashMemory` in — for 783 → **790** undefined, 681 → **688** function, 102 → **102** storage, all three exact. Seven more stubs, the second step in a row whose counts go up, and correct for the same reason 367 was: it retires the name the run stopped on and the eleven it creates are the next frontiers, defined by four sibling `YarrowCoreLib` objects (`src/sha1mod.o` 3, `src/comp.o` 3, `port/smf.o` 4, `src/yarrowUtils.o` 1). The object is `.text` **0x7C4** (eight `T`, all of them: the four the pool stops on plus `prngProcessSeedBuffer`/`prngStretch`/`prngAllowReseed`/`prngDestroy`, which nothing references yet) and **nothing else that is allocatable** — no `.rodata`, no `.rodata.str1.1`, no `.bss`, no `.data`, no `.sysctl_set`, no `.init_array` — the first object here with one allocated section, which is why the layout arithmetic is short and why `.bss`'s size *cannot* change (366's case, not 367's: the `0x04` fill at 0x801F85BC and `realstubs.o`'s 0x2744 are carried through untouched). **The stop is the first call in `prngInitialize`**, one instruction past the prologue: `bl mmInit` at object +0xC, and the key is composed for the second step running — yarrow's `.text` 0x8017BA04 + 0x494 = **0x8017BE98**, `prngInitialize` at object +0x0, the `bl` at +0xC, field = call + 4 → **0x8017BEA8**. The run reported `stub_hit=mmInit` at exactly that key (`w1=0x38616562` = `"bea8"`), `abort_entries=0`, `abort_first_dfar=0`, `xnu_entry_checks=5`/`failures=0`, no `exception:` line, no panic, log 301618 bytes ending in the kernel's own `No errors detected`, and all five falsifiers silent (no `mmMalloc` at 0x8017BEB0, no `mmGetPtr` at 0x8017BEC4, no `panic`, no `prngInitialize` at 0x8017BCEC — so the run went *through* 367's stop — and no `YSHA1Init` at 0x8017BEF4). Layout: counts exact, every `.text` placement exact (object **0x8017BE98**, `realstubs.o` **0x8017C808 (0x4080 = 688 × 0x18)**, platform expert 0x8017C65C), **everything below `.text` exactly 367's row** (`.data` 0x801A8000/0x19A58, `.sysctl_set` 0x801C1A58/0x150, `.init_array` 0x801C1BA8/0x84 ending 0x801C1C2C, `.bss` 0x801C1C40/0x390D8, `__bss_end` 0x801FAD18, image 1842220, headroom 1069800). **One miss, and it is the third step in a row with a sum that was written and not evaluated**: the `realstubs.o` `.rodata.str1.4` size was predicted 0x3AE0 and measured **0x3AC8** — the block enumerated all eleven created name slots and then summed them to `0x98` where they are **0x80** (eight `0xC`-names + `comp_get_ratio` 0x10 + `mmFree`/`mmInit` 0x8 each), so `created − retired = 0x48` was written as 0x60; 366 never multiplied `676 × 0x18`, 367 never added the run-order rule to a row, 368 never added the addends it printed — **print the sum of the terms you just printed**. The row's *address* was 4 low for a reason that is a rule and not a slip: `.rodata` rows shift by the `.text` run's delta *as the linker consumed it* (uuid's `.rodata` 0x801A1651 → **0x801A1EB9** = +0x868, the run's +0x86C minus 0x4 of boundary fill), so `0x801A2190 + 0x86C = 0x801A29FC` against a measured **0x801A29F8**. The `.text` end is 0x40 high (**0x801A6E80** measured against 0x801A6EC0) and decomposes: 0x18 the name-sum slip, 0x4 the boundary fill, 0x10 the **tail pad 367 needed and 368 does not** — 367's last `.text` row ended 0x801A65D0 with the section closing at 0x801A65E0, and 368's same row moved to **0x801A6E78** (a row shift of exactly **0x8B0**) ending at 0x801A6E80, already 32-aligned, so the section closes with no pad at all: **a section end is a row shift plus a pad, and the pad is not the same on both sides of a step.** The `.data` argument held as written, and the next object is named before its run: **369 is `osfmk_prng_YarrowCoreLib_port_smf.o`** (4 resolved / 0 added → 786 / 684 / 102), 0x38 of `.text` and **0x18 of `__DATA, __data`** — the first `.data` move since 364 — and its stop will not be in its own object but one call deeper in `prngInitialize`, `YSHA1Init` at key **0x8017BEF4**. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, device back on Android on its own |
| stage90 | [experiment-369](experiment-369-yarrowcorelib-port-smf-c-the-first-data-move-in-five.md) | **`osfmk/prng/YarrowCoreLib/port/smf.c` — the first `.data` move in five steps, and a stop three frames away from anything the step touches.** **4 resolved (4 function) / 0 added** — `mmInit`, `mmMalloc`, `mmGetPtr`, `mmFree` out, nothing in — for 790 → **786** undefined, 688 → **684** function, 102 → **102** storage, all exact, the first step in three that *shrinks* the stub set. The object is **0x38 of `.text` for six definitions** and **0x18 of `__DATA, __data`** (`mmMalloc.site`, a `VM_ALLOC_SITE_STATIC`), nothing else allocatable; the four functions are trivial and say exactly what the step buys — `mmInit` is `bx lr`, `mmGetPtr` is `bx lr` (**it returns its argument**), `mmFree` is `b kfree_addr`, and `mmMalloc` is `kalloc_canblock(size, 1, &mmMalloc.site)` — so `prngInitialize`'s `mmMalloc(0xAC)` becomes a real `kalloc` and the `memset(ptr, 0, 0xAC)` that follows is over that allocation. **The stop is not in the step's object**: all four `mm*` names retire at once and the object adds nothing, so the frontier is the next stub in `prngInitialize`'s own instruction order, three frames past the object just linked — past the real `memset` at +0x3C — which is `YSHA1Init` at object +0x58, key **0x8017BEF4** (366's shape, second appearance; the tool is disassemble the resumed function, list its stub calls in address order, take the first whose definer is not in the step). **Every row of the prediction exact, and no falsifier fired** — the first step in a long while with no miss anywhere: counts, object `.text` **0x8017C65C** (0x38), `realstubs.o` `.text` **0x8017C840** (0x4020 = 684 × 0x18), platform expert 0x8017C694, `.text` end **0x801A6E40** (text 1732160), and the `.rodata` row that 366 got 0x3F0 wrong, 367 got 0x510 wrong and 368 got 0x18 wrong — `realstubs.o` `.rodata.str1.4` **0x801A29D0 (0x3AA0)** — now exact in *both* parts, because the run-order rule was applied as arithmetic and the name slots were summed with a calculator. **The `.data` move was derived rather than asserted and the derivation is the point**: 368's `.data` ended exactly on `.sysctl_set`'s start (last input `osfmk_vm_vm_shared_region.o`'s `__DATA, __data` at 0x801C1A40/0x18, ending on 0x801C1A58, `. = ALIGN(0x8)` already satisfied), so no fill could absorb the new input and the 0x18 had to land at the section's end — the error case of 364 and 365 run correctly this time. `.data` **0x801A8000 (0x19A70)**, `.sysctl_set` **0x801C1A70** (0x150), `.init_array` **0x801C1BC0** (0x84) ending **0x801C1C44**, `.bss` **0x801C1C80** (0x390D8 — a copy in size and a *move in place*, since `align64(0x801C1C44)` is the new base), `__bss_end` **0x801FAD58**, image **1842244**, headroom **1069736** (−0x40), and `args`/`topOfKernelData` **unmoved** (`align_up(0x1FAD58, 0x1000)` is still 0x1FB000). The run reported `stub_hit=YSHA1Init` at exactly `xnu_entry_stub_caller_v=0x8017bef4` (`w1=0x34666562` = `"bef4"`), with `abort_entries=0`, `abort_first_pc=0` — **the NEON sequence at object +0x40..+0x50 executed without a coprocessor fault**, the argument the block made in advance from `start.s`'s `vmsr fpexc` at 0x800003A8 — no `panic` (so `kalloc(0xAC)` did not return NULL and the `beq 0x124` branch was not taken), `xnu_entry_checks=5`/`failures=0`, no `exception:` line, log 301621 bytes ending in `No errors detected`. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, device back on Android on its own. **Next: 370 is `osfmk_prng_YarrowCoreLib_src_sha1mod.o`** (3 resolved / 0 added → 783 / 681 / 102), `.text` 0x14AC — the largest single step of the last twenty — with a `.bss` of 0x40, and its stop *is* in its own object this time |
| stage90 | [experiment-370](experiment-370-yarrowcorelib-src-sha1mod-c-the-step-that-pushes-data-off-its-bucket.md) | **`osfmk/prng/YarrowCoreLib/src/sha1mod.c` — the SHA-1 the FIPS layer has been waiting for, and the step that pushes `.data` off its bucket.** **3 resolved (3 function) / 0 added** — `YSHA1Init`, `YSHA1Update`, `YSHA1Final` out, nothing in — for 786 → **783** undefined, 684 → **681** function, 102 → **102** storage, all exact. The two references are `memcpy` and `memset`, both real: **this is the first object in the walk whose entire closure is already satisfied**, so nothing inside it can stop the run and the frontier moves eight calls further on. `.text` **0x14AC** — the largest single step of the last twenty — for `YSHA1Transform` (0x12A4), `YSHA1Init` (0x40), `YSHA1Update` (0xAC), `YSHA1Final` (0x110), plus `.bss` 0x40 (`YSHA1Transform.workspace`) and a 2-byte `.rodata`/`.rodata.str1.1`. **The object is 16-byte aligned and every object before it in this walk was 4-aligned, which exposed a defect in `predict_layout.py`**: `section_align()` begins `if not path.startswith("/"): return 4`, so a *relative* path — which is what this walk has always passed — silently answers 4, and the tool placed the input at 0x8017C694 where `align_up(…, 16) = **0x8017C6A0**` is right. Twenty steps of relative paths were correct because every one of those objects was 4-aligned: **a default that is right for its whole sample is indistinguishable from a reading.** The 0xC fill is functional, not bookkeeping — `YSHA1Init` contains `vld1.64 {d16-d17}, [r2 :128]`, a 128-bit *aligned* load of the SHA-1 IV at object +0x12E0, which faults if the input is not 16-aligned. **The `.data` move is the second in this walk and the first driven by an overflow rather than a margin**: `0x801A82A0 > 0x801A8000` by 0x2A0, so the section jumped a whole `ALIGN(0x4000)` to **0x801AC000** (0x19A70) and dragged `.sysctl_set` **0x801C5A70**, `.init_array` **0x801C5BC0** (0x84) ending **0x801C5C44**, `.bss` **0x801C5C80** (0x39118 = +0x40, the pad in front of `realstubs.o` unchanged at 0x04 because 0x40 is a multiple of 64 — the pad rule's fifteenth confirmation), `__bss_end` **0x801FED98**, image **1858628** (0x1C5C44), `args` **+2097152**, `topOfKernelData` **+4194304**, tree **+6291456**, window 8388608 and headroom **2101864** — **all four dragged rows and all three derived layout numbers exact**. Every `.text` placement exact too (object 0x8017C6A0, `realstubs.o` **0x8017DCF8** = 0x3FD8 = 681 × 0x18, platform expert 0x8017DB4C). **The run reported `stub_hit=comp_init` at exactly `xnu_entry_stub_caller_v=0x8017BF7C`** (`w1=0x63376662` = `"bf7c"`), with `abort_entries=0`, `abort_first_pc=0`, `abort_first_dfar=0`, `xnu_entry_checks=5`/`failures=0`, no `exception:` line, no panic, log 301621 bytes ending in `No errors detected` — so **0x12A4 bytes of the real SHA-1 compression function ran on this path, twice**, and neither the aligned NEON load nor the transform faulted. **Four misses, three of them the block's own arithmetic.** (1) The object's `.bss` row was left in the *pre-move* frame (0x801F85E4 written, **0x801FC5E4** measured): the block derived the `.data` move, shifted `.bss`'s base and `__bss_end` correctly, and did not shift this one — **a prediction containing a section move invalidates every row derived before it**. (2) The `.rodata` row used two values for one quantity *inside the one block*: it derived the `.text` run delta as `+0x14AC + 0xC − 0x48 = +0x1470` and then wrote `0x801A29D0 + 0x1464 + 0x4`; with the block's own number it is `0x801A3E44` = measured, the written form is **0x801A3E38** — the size (0x3A7C) exact and the address 0xC low. (3) Three hex↔decimal conversions by hand, two wrong (`image` 1860676 for 1858628; `headroom` 4199016 for 2101864) — both rows' hex was right. (4) The `.text` end 0x20 high (**0x801A8280** against 0x801A82A0): the `.rodata` run's delta was −0x30, not the modelled −0x20, the documented ±0x10..0x40 caveat. **The next object is named before its run and it is the last on this path: 371 is `osfmk_prng_YarrowCoreLib_src_comp.o`** (3 resolved / 0 added → **780 / 678 / 102**, `.text` 0x28, **0 references**, four trivial bodies — `comp_init`/`comp_add_data` are `mov r0,#0; bx lr` and `comp_get_ratio` writes 1.0f through its out-parameter) — after which **every call `prngInitialize` and `yarrow_init` make is real**, the yarrow subtree completes, and the walk returns eleven steps' worth of frames back into `IOPMrootDomain::start` past 365's stop at +0x5F0. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, device back on Android on its own |
| stage90 | [experiment-371](experiment-371-yarrowcorelib-src-comp-c-the-step-that-corrects-370s-closing-claim.md) | **`osfmk/prng/YarrowCoreLib/src/comp.c` — the step that completes the yarrow call graph, and whose stop is not where 370 said it would be.** **3 resolved (3 function) / 0 added** — `comp_init`, `comp_get_ratio`, `comp_end` out, nothing in — for 783 → **780** undefined, 681 → **678** function, 102 → **102** storage, all exact. **The object has zero references** — the first in this walk with none at all — and `.text` is **0x28** for four definitions, 4-aligned, single section: `comp_init`/`comp_end`/`comp_add_data` are `mov r0, #0 ; bx lr` and `comp_get_ratio` writes **1.0f** through its out-parameter. **370's closing claim that “371 completes the yarrow subtree and its stop will not be in `osfmk/prng/` at all … the walk resumes eleven frames back into `IOPMrootDomain::start`” is wrong, and this block overrules it before its own run**: it read `yarrow_init`'s four remaining calls (+0x314 `prngInput`, +0x338 `prngOutput`, +0x34C `prngForceReseed`, +0x358 `fips_initialize`) as *cut off* rather than *made*, and as real *call sites* rather than real *closures*. `prngInitialize` returns as soon as `comp_init` is real, and `prngForceReseed` then reads **`state->0xa8 == 33`** — a flag `prngInitialize` writes at its +0x11C only on the path where all three `comp_init` calls returned 0 (`cmp r0, #0 ; bne 0x128` three times at +0xE8/+0xF8/+0x108). **So 371 is the step that makes `prngForceReseed` take its long path**, and the frontier is its tail block's **`bl trashMemory`** at prng.o +0x434 — a new shape for this walk: *a step whose own return value is what makes the next function's guard fall through*. Predicted key **0x8017C2D0** (prng.o unmoved at 0x8017BE98, `prngForceReseed` at +0x2A0, call at 0x8017C2CC, +4), and **the run reported `stub_hit=trashMemory` at exactly `xnu_entry_stub_caller_v=0x8017C2D0`** (`w1=0x30643263` = `"c2d0"` reversed), with `abort_entries=0`, `abort_first_pc=0`, `abort_first_dfar=0`, `xnu_entry_checks=5`/`failures=0`, no `exception:` line, no panic, log 301623 bytes ending in `No errors detected`. **The falsifiers**: no `comp_init` at 0x8017BF7C (the object took — and that key is 370's own stop, so it is now a two-way fact); **no `FIPS_SHA1Init` at 0x8017BB0C** — the informative one, because it stays silent only if `prngForceReseed` did *not* take either early return, i.e. only if the 33 really was set; no `trashMemory` at 0x8017C490 (so the stop is `prngForceReseed`'s call site and not `prngStretch`'s); `abort_entries=0` (the NEON tail zeroing and the 50 ms generation loop of real `prngOutput` + `YSHA1Update` + `mach_absolute_time` ran without a fault); and no panic, because `fips_initialize`'s known-answer `memcmp`/`panic` at +0x264/+0x278 is *behind* the stop. **Layout: every row below `.text` exact and unmoved** — `.data` **0x801AC000** (0x19A70; the bucket was not re-entered, `align_up(0x801A8260, 0x4000)` is still 0x801AC000, with 0x3DA0 of room), `.sysctl_set` 0x801C5A70, `.init_array` 0x801C5BC0 (0x84) ending 0x801C5C44, `.bss` 0x801C5C80 (0x39118 — the object has no `.bss`, so the pad rule is not exercised), `__bss_end` **0x801FED98**, image **1858628**, headroom **2101864**, and `args`/`topOfKernelData`/`tree`/`window` all unmoved. Object `.text` **0x8017DB4C** (0x28) and platform expert **0x8017DB74** (0x150) exact. **One miss, and it is 370's miss 2 class in a new place: a quantity with two values inside one block** — the `.text` term retired **two** stub bodies where the object retires **three** (`-0x30` written, `-0x48` correct), while the same block used **three** correctly for the `.rodata` term (0x28 = 0xC + 0x10 + 0xC, exact at size 0x3A54). The 0x18 lands in exactly three rows and all three missed: `realstubs.o` `.text` **size** (0x3FA8 written, **0x3F90** = 678 × 0x18 measured), the `.rodata` row **address** (0x801A3E3C written, **0x801A3E24** measured), and `.text` end / text size (0x801A8260 / 1737312 written, **0x801A8240 / 1737280** measured). With the term right all three are exact: `0x801A8280 − 0x28 − 0x48 = 0x801A8238`, `ALIGN(32)` → 0x801A8240. **Tell: a step retires one name *and* one body per resolved function, so both terms must come from the same integer — the first number the effect tool prints.** **The next object is `osfmk_prng_YarrowCoreLib_src_yarrowUtils.o`** (1 resolved / 0 added → **779 / 677 / 102**, `.text` 0x44, the real debug-poisoning `trashMemory`: memset 0x00, memset 0xff, tail `b memset` 0x00), **and 372's stop is named in advance**: the same object walks `prngForceReseed`'s second call site and its return to `yarrow_init`, then `fips_initialize` → `random_block` → **`FIPS_SHA1Init` at key 0x8017BB0C**, whose definer `osfmk_prng_fips_sha1.o` (`.text` 0x1600, `.data` 0x40, 2 resolved / 0 added) is the first `.data` input in eleven steps — so the yarrow subtree ends at **372**, not 371. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, device back on Android on its own |
| stage90 | [experiment-372](experiment-372-yarrowcorelib-src-yarrowutils-c-the-stub-371-named-in-advance.md) | **`osfmk/prng/YarrowCoreLib/src/yarrowUtils.c` — the stub 371 stopped on, and a step that adds a `panic` to the path ahead.** **1 resolved (1 function) / 0 added** — `trashMemory` out, nothing in — for 780 → **779** undefined, 678 → **677** function, 102 → **102** storage, all exact. `.text` **0x44**, 4-aligned, single section, nothing else allocatable; the body is real and is the only *debug* helper this walk has linked — `trashMemory(buf, len)` does `memset 0x00`, `memset 0xff`, then a tail `b memset 0x00`, which is why the step runs real code over buffers whose lengths come from its caller (0x14 and 0x40 bytes of stack). **The stop is `FIPS_SHA1Init` at `xnu_entry_stub_caller_v=0x8017BB0C`** (`w1=0x63306262` = `"bb0c"`), exactly as predicted and exactly the key 371's block named a step early as its own falsifier (b): `trashMemory` returns, `prngForceReseed` takes its **second** call site 0x8017C2DC, falls out at 0x44C and returns to `yarrow_init` +0x350, which reaches its last call `fips_initialize` (+0x358) → `random_block` (+0x250) with **`r2 = 0`**, so `random_block`'s `ldr r0,[sp,#8] ; cmp r0,#0 ; beq 0x100` **skips the `bl prngOutput` at +0xD0** and the first call it makes is `bl FIPS_SHA1Init` at **obj+0x104** (0x8017BA04 + 0x104 + 4 = 0x8017BB0C). Falsifiers all silent: no `trashMemory` at 0x8017C2D0 (the object took, and that key is 371's stop — a two-way fact) nor at 0x8017C2DC; no `FIPS_SHA1Update` at 0x8017BB48, which is what proves both that the name is *still* a stand-in and that the `beq` really skipped `prngOutput`; no `panic` (so `yarrow_init`'s +0x2E8 guard saw a zero `prngInitialize` return again); `abort_entries=0` with `abort_first_pc=0` and `abort_first_dfar=0` — the reading this prediction was about, since `trashMemory` wrote 0x14 + 0x40 bytes of stack three passes each without faulting. **Layout: every `.text` row exact, the text size exact to the byte, every row below `.text` exact and unmoved** — object `.text` **0x8017DB74** (0x44), platform expert **0x8017DBB8** (0x150), `realstubs.o` **0x8017DD64** (0x3F78 = 677 × 0x18) with its end at **0x80181CDC** (start +0x44, size −0x18, end +0x2C — the two-directions row 371's table shape predicts), `.text` end **0x801A8260**, text size **1737312**, and `.data` 0x801AC000 (0x19A70), `.sysctl_set` 0x801C5A70, `.init_array` 0x801C5BC0 (0x84) ending 0x801C5C44, `.bss` 0x801C5C80 (0x39118), `__bss_end` **0x801FED98**, image **1858628**, headroom **2101864**, args/topOfKernelData/tree/window all unmoved. **One miss, of 0x4, on the `.rodata` row — the model's, not the block's**: size 0x3A48 exact, address 0x801A3E50 predicted against **0x801A3E54** measured, the fill the linker inserts where the `.text` run hands the cursor to the `.rodata` run (368 measured the same 0x4, 371 measured 0x0). **The next object is `osfmk_prng_fips_sha1.o`** (2 resolved / 0 added → **777 / 675 / 102**; `.text` **0x1600** with `FIPS_SHA1Init` 0x40, `FIPS_SHA1Update` 0xE0, `FIPS_SHA1Final` 0x14C and a local `SHA1Transform` 0x1394, *plus* a `.data` of **0x40** — **the first `.data` input in eleven steps**, so 373 is where `.data` next moves) — **and 373 is the first step on this path with a real `panic` in front of the frontier**: `fips_initialize` compares `random_block`'s output against `kKnownAnswer` and calls the real `panic` at obj+0x278 on a mismatch, so the run's first line to read is whether the FIPS known-answer test passes. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, log 301625 bytes / 3975 lines ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-373](experiment-373-osfmk-prng-fips-sha1-c-the-step-that-leaves-the-yarrow-subtree.md) | **`osfmk/prng/fips_sha1.c` — the step whose self-test runs, and the first stop outside `osfmk/prng/`.** **2 resolved (2 function) / 0 added** — `FIPS_SHA1Init` and `FIPS_SHA1Update` out, nothing in — for 779 → **777** undefined, 677 → **675** function, 102 → **102** storage, all exact. `.text` **0x1600** — the largest step since 370 — 16-aligned, four definitions (`FIPS_SHA1Init` 0x40, `FIPS_SHA1Update` 0xE0, the local `SHA1Transform` 0x1394, `FIPS_SHA1Final` 0x14C) **plus a `.data` of 0x40** (`PADDING`), the first `.data` input in eleven steps; both references (`memcpy`, `memset`) are already real, so the whole closure is satisfied and nothing inside it can stop the run. **The stop is `_ZN19IOPMPowerStateQueue17PMPowerStateQueueEP8OSObjectPFvS1_zE` at `xnu_entry_stub_caller_v=0x8014E6D0`** (`w0` spells `8014`, `w1` spells `e6d0`) — a C++ constructor, and the first stop in this walk that is not a `osfmk/prng/` name: `FIPS_SHA1Init` returns (a leaf — `vld1.64 {d16-d17}, [r2 :128]` off object+0x30 and back), `FIPS_SHA1Update` runs twice, `random_block`'s first call takes `beq 1f8` (`state->0x30` is 0) and returns, `fips_initialize`'s known-answer **`memcmp` agrees** so the tail call `b random_block(state, state+0x18, 1)` runs `bl prngOutput` — **the call 372's block said was skipped, now executed** — the second CRC differs, `yarrow_init` returns 0, `read_random` runs its whole tail including `blx r7` = `yarrow_generate` (a real `memmove` of 16 bytes) and `lck_mtx_unlock`, `uuid_generate` finishes, and `IOPMrootDomain::start` — stopped at its `bl uuid_generate` since 365 — resumes at +0x5F4 and runs 100 real C++ instructions to `bl` at +0x780 (`0x8014DF4C + 0x780 = 0x8014E6CC`, key +4). `tools/first_stub_call.py` from +0x5F4 names it, and between +0x5F4 and +0x77C there is **no return and no conditional branch at all**. **No falsifier fired and no `panic`**: no `FIPS random self test failed` (the FIPS known-answer test *passed*), no `FIPS random self-test failed.` (the CRC changed), no `Couldn't initialize/seed Yarrow.`, neither `FIPS_SHA1Init` 0x8017BB0C nor `FIPS_SHA1Update` 0x8017BB48, no vtable-slot stop (365's kind 9), and `abort_entries=0` with the 16-byte-aligned `vld1.64` not faulting. **Layout: every `.text` row exact, the text size exact to the byte, the whole `.data` derivation exact** — object `.text` **0x8017DBC0** (0x1600, after an 0x8 fill), platform expert **0x8017F1C0** (0x150), `realstubs.o` **0x8017F36C** (0x3F48 = 675 × 0x18), `.rodata.str1.4` **0x801A542C (0x3A28) exact in both parts** (so the boundary fill measured **0x0** again — 368: 0x4, 371: 0x0, 372: 0x4, 373: 0x0), `.text` end **0x801A9820**, text size **1742880**, `.data` **0x801AC000 (0x19AB0)** — address unmoved, +0x40 of input landing at the section's end 0x801C5A70 — `.sysctl_set` **0x801C5AB0**, `.init_array` **0x801C5C00** (0x84) ending **0x801C5C84**, `.bss` **0x801C5CC0** (0x39118), `__bss_end` **0x801FEDD8**, image **1858692**, headroom **2101800**, and args/topOfKernelData/tree/window all unmoved because align_up(0x1FEDD8, 0x1000) is still 0x1FF000. **One miss, of 0x5C, on a row located rather than derived**: `realstubs.o`'s `.text` start was written as the platform expert's end (0x8017F310) but three constant inputs sit between them — the expert's own `.text._ZN21MSM8974PlatformExpert9MetaClassD0Ev` 0x4, `xnu_arm_entry_last_kernel_constructor.o`'s `.text.startup` 0x4 and `xnu_arm_entry_rtabi.o`'s `.text.eabi` 0x54 — so it is **0x8017F36C**, the same 0x5C gap 372's map has; nothing derived from that row moved. **The next object is `iokit_Kernel_IOPMPowerStateQueue.o`** (2 resolved / 0 added → **775 / 673 / 102**; `.text` **0x318** *plus* `.rodata` 0xB4, `.rodata.str1.1` 0x14, **`.bss` 0x18** and **`.init_array` 0x4** — a static constructor, so 374 is where `.init_array` next grows). 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, log 301674 bytes / 3975 lines ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-374](experiment-374-iokit-kernel-iopmpowerstatequeue-c-the-step-that-makes-the-queues-vtable-real.md) | **`iokit/Kernel/IOPMPowerStateQueue.cpp` — the object the walk has been stopped inside since 365, and the step that makes its vtable real.** **2 resolved (2 function) / 0 added** — `submitPowerEvent` and `PMPowerStateQueue` out, nothing in — for 777 → **775** undefined, 675 → **673** function, 102 → **102** storage, all exact; all 47 of the object's references are already real, so nothing inside it can stop the run. `.text` **0x318** plus a separate `.text._ZN19IOPMPowerStateQueue9MetaClassD0Ev` input of **0x4**, `.rodata` 0xB4 (two vtables), `.rodata.str1.1` 0x14, **`.bss` 0x18** and **`.init_array` 0x4** — a static constructor, so `.init_array` grows for the first time since 364. **The step's subject is the vtable, not a symbol**: before it the queue's `.rodata` is a storage stand-in of zeros, so the constructor's two `blx` (`+0x13C` through vtable+0x38 to the queue's own `init`, `+0x154` through vtable+0x14 to `OSObject::release`) would have been jumps to 0. **The stop is `_ZN9IOCommandC2EPK11OSMetaClass` at `xnu_entry_stub_caller_v=0x8017302C`** (`w0` spells `8017`, `w1` spells `302c`) — `IOPMRequest::IOPMRequest(OSMetaClass const *)`+0x90, one `bl` **before** the predicted `_ZN9IOCommand4initEv` at 0x801658D4, because `IOPMRequest::create()` is *not* inlined: `IOPMRequest::MetaClass::alloc` (0x8017300C) calls the constructor stub directly at `+0x1C`, and the block had marked that `blx` "real" without asking whether its own straight line reaches a stub — `tools/first_stub_call.py` answers it in one command and names the measured key to the byte. **The whole path above it ran exactly as derived**: the queue was built, `IOWorkLoop::addEventSource` ran (the command gate's `closeGate`/`onThread`/`sleepGate` — the boot thread slept and the PM work loop thread ran `IOWorkLoop::_maintRequest`'s 17 `blx` over the queue's vtable), `IORootParent` was constructed, `init`ed, `attach`ed and `start`ed, `PMinit()` ran, and `registerPowerDriver` reached `acquirePMRequest` with all three data conditions satisfied — **the first time this walk has come back from a thread switch**. **Layout: every `.text` row exact including the one 373 missed** (the `.text.*` wildcard input derived rather than located), object `.text` **0x8017F1C0**, its `MetaClassD0Ev` **0x8017F4D8**, platform expert **0x8017F4DC** (0x150), `realstubs.o` **0x8017F688** (0x3F18 = 673 × 0x18) ending 0x801835A0, `.text` end **0x801A9B60** and text size **1743712** exact, `.data`/`.sysctl_set` unmoved, `.init_array` **0x801C5C00 (0x88)**, `.bss` **0x801C5CC0 (0x39158)** with the object's 0x18 at 0x801FC664 and the `new_pad = (old_pad − object_size) mod 64` rule read *forward* for the first time (0x4 → 0x2C, `realstubs.o` 0x801FC680 → 0x801FC6C0), `__bss_end` **0x801FEE18**, image **1858696**, headroom **2101736**, args/topOfKernelData/tree/window unmoved. **One miss, of 0x4, on the object's `.rodata` row** — the `.text`/`.rodata` boundary fill for the fifth time (368: 0x4, 371: 0x0, 372: 0x4, 373: 0x0, **374: 0x4**), everything below it 0x4 low and internally consistent — and **the `.text` end is unmoved by it**, because the section end is an `ALIGN(0x20)` that erases shifts smaller than its granularity minus the fill. **The next object is `iokit_Kernel_IOCommand.o`** (5 resolved (3 function, 2 storage) / 0 added → **770 / 670 / 100**; the walk's first *storage* retirees in a long time), and `IOCommand::init` is also `_ZTV11IOPMRequest`'s slot 0x30. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, log 301643 bytes / 3975 lines ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-375](experiment-375-iokit-kernel-iocommand-cpp-the-step-that-makes-iopmrequests-vtable-real.md) | **`iokit/Kernel/IOCommand.cpp` — the object that makes `IOPMRequest`'s vtable real, and the first stop in the power-connection layer.** **5 resolved (3 function, 2 storage) / 0 added** — `IOCommand::IOCommand(OSMetaClass const *)`, `IOCommand::init`, `~IOCommand()` plus the storage stand-ins `IOCommand::gMetaClass` (0x18) and `IOCommand::metaClass` (0x4) out, nothing in — for 775 → **770** undefined, 673 → **670** function, 102 → **100** storage, all exact; all 30 of the object's references are already real, so the whole closure is satisfied. `.text` **0x15C** (fifteen functions) plus a separate `.text._ZN9IOCommand9MetaClassD0Ev` input of **0x4**, `.rodata` 0x84 (`_ZTV9IOCommand`), `.rodata.str1.1` 0xA, `.bss` 0x18, `.init_array` 0x4 (a static ctor at `.text`+0x108). **The stop is `_ZN17IOPowerConnectionC1Ev`** — a new object family, the power-connection layer — called from `IOService::addPowerChild` (0x80166024), which `IOPMrootDomain::start` enters at 0x8014E76C as `patriarch->addPowerChild(this)`, the first call after `patriarch->start` returns: `+0x1FC bl OSObject::nw(0x38)` then **`+0x204 bl 801833a8 <_ZN17IOPowerConnectionC1Ev>`**, with `setAwaitingAck` at +0x4E8 and `setReadyFlag` at +0x4F4 behind it. The block reached it by *measuring* rather than assuming: this step retires the two stub sites in `IOService::acquirePMRequest` (+0x30 and +0x70, keys 0x801658D4 and 0x80165914), and `tools/first_stub_call.py` then finds **no stub call anywhere on the straight line** of `registerPowerDriver`, `IORootParent::start`, `makeUsable`, `submitPMRequest`, both `changePowerStateToPriv`s or `IOPMrootDomain::start` itself (88 direct calls, every one real) — so the frontier moves one frame out. The branch to +0x204 is taken because `addPowerChild`'s guard reads **data**, not a symbol: `initialized` is set on both sides (`PMinit()` in `IORootParent::start`, and the root domain's own `PMinit()`) and `child->getParentIterator(gIOPowerPlane)` yields no child of the patriarch. **Layout: every row exact to the byte, including the four the table's four routes could have disagreed on** — object `.text` **0x8017F4DC (0x15C)**, its `D0Ev` **0x8017F638 (0x4)**, platform expert 0x8017F63C (0x150), its `D0Ev` 0x8017F78C, last-kernel-ctor 0x8017F790, rtabi 0x8017F794 (0x54), `realstubs.o` **0x8017F7E8 (0x3ED0 = 670 × 0x18)** ending 0x801836B8, `.rodata` run start **0x80183764**, queue `.rodata`/`.str1.1` 0x801A5288/0x801A533C, object `.rodata`/`.str1.1` **0x801A5350 (0x84)/0x801A53D4 (0xA)**, expert 0x801A53E0/0x801A5820, `.rodata.macho` 0x801A5838, `realstubs.o` `.str1.4` **0x801A5984 (0x396C)** ending 0x801A92F0, `.text` end **0x801A9CC0** and text size **1744064** *by both routes for the first time*, `.data`/`.sysctl_set` unmoved, `.init_array` **0x801C5C00 (0x8C)**, `.bss` **0x801C5CC0 (0x390D8)** with the object's 0x18 at **0x801FC67C** and `realstubs.o`'s row at **0x801FC6C0 (0x26C4)** — the two 64-aligned retired slots giving back 0x80 and the pad giving up 0x18, exactly offsetting the object's own 0x18 — `__bss_end` **0x801FED98**, image **1858700**, headroom **2101864**, args/topOfKernelData/tree/window unmoved; **and the `.text`/`.rodata` boundary fill read 0x0 this time**, so no band was needed. **One miss, of 0x4, on the stop's key**: the block wrote the formula `0x80166024 + 0x204 + 4` correctly and then printed the **call site** as its value, where every other key in the same block is a return address — the run reports `0x8016622C`, the `bl` is at `0x80166228`. **The data-record layout rule is now measured, not hypothesised**: all 102 records are 64-aligned slots of `align64(size)` in `realstubs.o`'s `.bss` (every symbol offset a multiple of 0x40, packed end 0x2744 = the section size exactly), which closes the open 0x3C discrepancy. **The next object is `iokit_Kernel_IOPowerConnection.o`** (14 resolved (13 function, 1 storage) / 0 added → **770 → 756 / 670 → 657 / 100 → 99**; `.text` **0x1E0**, `.rodata` **0x38C** — and it defines `_ZTV17IOPowerConnection`, which has no stand-in today because the only thing that would reference it is the constructor this step retires, so 376 is where a vtable nothing can see arrives with the object that needed it). 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `checks=5`/`failures=0`, `abort_entries=0`, log 301638 bytes / 3975 lines ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-376](experiment-376-iokit-kernel-iopowerconnection-cpp-the-object-that-ends-addpowerchild.md) | **`iokit/Kernel/IOPowerConnection.cpp` — the object that ends `IOService::addPowerChild`, and the first stop whose site is chosen by a *value* rather than by a symbol.** **14 resolved (13 function, 1 storage) / 0 added** — the constructor `_ZN17IOPowerConnectionC1Ev` plus twelve one-line accessors and the `metaClass` R 0x4 stand-in out, nothing in — for 770 → **756** undefined, 670 → **657** function, 100 → **99** storage, all exact; all 224 of the object's references are already real, so nothing inside it can stop the run. `.text` **0x1E0** (thirteen functions) plus a separate `.text._ZN17IOPowerConnection9MetaClassD0Ev` input of **0x4**, `.rodata` **0x38C**, `.rodata.str1.1` 0x12, `.bss` 0x18, `.init_array` 0x4. **The object's real subject is the call it ends**: `addPowerChild` has exactly three stub sites — the constructor at `+0x204`, `setAwaitingAck` at `+0x4E8`, `setReadyFlag` at `+0x4F4` — and this object defines all three, so a function the walk has been standing in since 375 becomes stub-free in one step. And `_ZTV17IOPowerConnection` has **no stand-in record at all**, because the only thing that would reference it is the constructor this step retires: a stub body's own closure is empty, so a name becomes visible to the linker only when a real object needs it (374's lesson from the other side). **The stop is `__MALLOC` at `xnu_entry_stub_caller_v=0x80106760`** (`w0` spells `8010`, `w1` spells `6760`) — inside `libkern`'s `sysctl_register_oid` at `+0x2C`, reached from `IOPMrootDomain::start`: after `addPowerChild` returns at 0x8014E76C the walk runs `withCStringNoCopy`, `serviceMatching`, `getMatchingServices`, `publishFeature`, `OSObject::release` and then eight `bl sysctl_register_oid`, and `IOPMrootDomain::start` has **88 direct calls and not one stub on its straight line**. **Which of the eight reaches the `bl __MALLOC` is decided by data**: `+0x08 ldrb r0,[r0,#14]` / `+0x10 tst r0,#0x40` / `+0x14 bne` skips the allocation when `CTLFLAG_OID2` is set in the oid's own `oid_kind`, and oids #1–#3 (`sysctl__kern_sleeptime` 0x83C00005, `_waketime` 0x83C00005, `_willshutdown` 0xC3C00002) all carry 0xC0 in byte 14 and take the real `+0x4C` path (they register for real, `lck_rw_lock_exclusive` and all), while **#4 `sysctl__kern_iokittest` (0x801FCE80) is a `data D 0x30` storage stand-in — all zeros at run time — so the bit is clear and it takes the malloc**. That is a new stop kind: not a missing symbol and not a missing vtable slot, but a *value* inside a zeroed stand-in. **Layout: every row below `.text` exact and unmoved** — object `.text` **0x8017F63C (0x1E0)**, its `D0Ev` **0x8017F81C (0x4)**, platform expert 0x8017F820 (0x150), its `D0Ev` 0x8017F970, last-kernel-ctor 0x8017F974, rtabi 0x8017F978 (0x54), `realstubs.o` **0x8017F9CC (0x3D98 = 657 × 0x18)**, `.text` end **0x801A9EA0** and text size **1744544** *from the position route* (the delta route ran 0x6 of raw end high and the block named both terms), `.data`/`.sysctl_set` unmoved, `.init_array` **0x801C5C00 (0x90)**, `.bss` **0x801C5CC0 (0x390D8)** with the object at 0x801FC694 and `realstubs.o` at 0x801FC700 (0x2684) — the object's 0x18, the pad's give-back and the retired 0x40 slot summing to **0**, so `__bss_end` **0x801FED98** is unmoved for the first time — image **1858704**, headroom **2101864**, args/topOfKernelData/tree/window unmoved. **Two misses, both at the `.rodata` boundary and neither visible in the printed number**: the three `.rodata` addresses are 0x4 high (the `.text`/`.rodata` fill for the sixth time — 368 0x4, 371 0x0, 372 0x4, 373 0x0, 374 0x4, 375 0x0, **376 0x4**), and `realstubs.o`'s `.rodata.str1.4` is **0x3716 where the block predicted 0x3718** — the packing rule is `Σ align4(len+1)` over all but the **last** name plus the last name's raw `len+1`, because no pad follows the final string (`_ZN9IODTNVRAMC1Ev`, and `readelf -x` shows its NUL as the section's last byte). At 375 the last name needed no pad, which is why the rule had read as exactly `Σ`. The `.text` end is unmoved by both: the row's address is 0x4 high but its size 0x2 low, and the fill before `__TEXT,__const` goes 0x4 → 0x2, so everything downstream lands where predicted and the `align16` before `initcode` costs 0x0. **The next step is two objects because the stop is coupled**: `bsd_kern_kern_malloc.o` (5 resolved (5 function) / 0 added → **756 → 751 / 657 → 652 / 99 → 99**) defines `__MALLOC`, `_FREE`, `_FREE_ZONE`, `__MALLOC_ZONE` and `kmeminit` — but linking it alone would convert the stub stop into a data abort at **0x801067A0** (`+0x70 ldr r0,[r6]` reads through `oid_parent` = 0 on the malloc path, 342's shape exactly), so **377 also links `iokit_Tests_Tests.o`** (1 resolved (0 function, 1 storage) / 0 added), which makes `sysctl__kern_iokittest` real `.data` with `oid_kind = 0xC3C00002` and `CTLFLAG_OID2` set, so call #4 never reaches the allocation. One for the symbol, one for the value. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `checks=5`/`failures=0`, `abort_entries=0`, log 301620 bytes / 3975 lines ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-377](experiment-377-bsd-kern-kern-malloc-c-and-iokit-tests-tests-cpp-one-stop-two-objects-one-for-the-symbol-and-one-for-the-value.md) | **`bsd/kern/kern_malloc.c` *and* `iokit/Tests/Tests.cpp` — one stop that is two defects in one site, one object for the symbol and one for the value.** **6 resolved (5 function, 1 storage) / 0 added** — `_FREE`, `_FREE_ZONE`, `__MALLOC`, `__MALLOC_ZONE` and `kmeminit` out of `bsd_kern_kern_malloc.o`, plus the `sysctl__kern_iokittest` `data D 0x30` stand-in out of `iokit_Tests_Tests.o` — for 756 → **750** undefined, 657 → **652** function, 99 → **98** storage, all exact, and `xnu_arm_entry_stubnames.txt` reading 652 function / 98 data records. 376's stop `__MALLOC` (`sysctl_register_oid + 0x2C`, the *fourth* of `IOPMrootDomain::start`'s eight oid registrations) is **two defects at once**: the first three oids take the real `+0x4C` leg because their `.data` carries `CTLFLAG_OID2`, while #4's storage is a zeroed stand-in so the bit is clear and it takes the malloc. Fixing the symbol alone would have converted the stub stop into a data abort at **0x801067A0** (`+0x70 ldr r0,[r6]` through `oid_parent` = 0, 342's shape exactly), so the step links both objects: `iokit_Tests_Tests.o`'s real 0x30-byte oid has `oid_kind = 0xC3C00002` at offset 12 and the flag *is* set. `.text` rows exact to the byte — `kern_malloc` **0x8017F820 (0x49C)**, `Tests` **0x8017FCBC (0x38)**, platform expert 0x8017FCF4, `realstubs.o` **0x8017FEA0 (0x3D20 = 652 × 0x18)** — and `realstubs.o`'s `.rodata.str1.4` came out **0x36DA** exactly, the second confirmation of 376's name-slot rule and its first use with five retired names (Σ 0x3C). `.bss` and everything derived below it is exact: `bss_start` **0x801C6540**, `bss_end` **0x801FF5D8**, `args_pa` **0x80201000** (the args page moving, as predicted), `top_of_kernel_data` 0x80400000, headroom **2099752**, image 0x1C6518. **The stop is not the one predicted**: the run reports `stub_hit=work_interval_thread_terminate` at `xnu_entry_stub_caller_v=0x80009544` = `thread_terminate_self + 0xE8` (`bl` at 0x80009540, `eb05e8ba`, disassembled out of the image the device ran), while **`throttle_init` is still record 552 of the stub list** — so `bsd_init` was never entered and the boot instead reached a **thread teardown**, the walk's first stop in a teardown rather than a bring-up frame. Its site is chosen by a byte of the thread's own state: `thread_apc_ast` is the only caller of `thread_terminate_self`, behind `ldrb r0,[r5,#708]` / `tst r0,#1` / `bne` — `thread->active` at +0x2C4, inactive variant taken. The falsifier that fired is (f), the one the block wrote for a stop in a frame the model could not place; (b) no `__MALLOC` stop, (c) no abort at 0x801067A0, (d) the counts and (e) the layout all held. **Two layout misses, both explained and both general.** (1) The `.rodata` run's shift is **+0x438**, not +0x45C: 0x24 of its content disappeared *upstream* of the insertion point. The block's first account of this was an attribution — "every input between the two is byte-identical" — and it is false; the correction came from **rebuilding 376** (the same build script with the two objects taken out of the link list, into a scratch `out/`, reproducing 376's published numbers exactly) and diffing the two maps. Five inputs had shrunk, total **−0x18** — `osfmk_arm_pmap.o` 0xBBF→0xBBA, `bsd_kern_bsd_init.o` 0x184→0x17F, `bsd_kern_kdebug.o` 0x125→0x123, `osfmk_kern_task.o` 0x3F9→0x3F3, `bsd_kern_kern_event.o` 0x712→0x70C — and the alignment fills over the same window fell 89 → 77 bytes (**−0xC**, the term a sum of section contents cannot see): −0x18 + −0xC = **−0x24**, and the window span measures 0x20EE0 against 0x20EBC, so the arithmetic closes. **Five objects nowhere near the insertion point** — `pmap.c`, `bsd_init.c`, `kdebug.c`, `task.c`, `kern_event.c` — which is 301's rule read forward as its sharper form: *the saving is not local either*, and booking it where the object was inserted gets every earlier row wrong too. The object's own chunk is **0x4CF** (band [0x4BD, 0x501]; the map's number, not the object's raw 0x501, is the answer) and `Tests`'s 0x0A exactly. (2) **`.data` is placed in two groups, not one**: `entry.ld` has `*(.data .data.*)` and then `*("__DATA,*__data" "__DATA,*__const")`, so a `.data`-named input can only append inside the first, whose last member before the insertion point is `osfmk_prng_fips_sha1.o` ending 0x801C4874 — not `smf.o`. `kern_malloc`'s 8-aligned 0x848 lands at **0x801C4878** behind a **0x4 fill**, `Tests`'s 0x30 at 0x801C50C0, group 1 ends 0x801C50F0, and group 2 — upstream at 0x801C4874 — starts 0x8 later with one of its own internal fills changed by another 0x4: `.data` grows **+0x880** to **0x1A330**, `.sysctl_set` 0x801C6330 (0x158), `.init_array` 0x801C6488, image 0x1C6518. **And the extra 0x8 reached nothing below it** — `.bss` starts at `align64(.init_array end)` and 0x801C6518 rounds to 0x801C6540 exactly as 0x801C6510 does. Text size **1746816** (0x1AA780) against 1746848: −0x14 from the `.rodata` shift, +0x4 because the measured pad before `__TEXT, initcode` is 0xC rather than 0x8, −0x10 because the measured raw end is already 32-aligned so the closing `ALIGN(0x20)` contributes 0 — 377 inverts 376, where a 4-byte shift was absorbed by a fill. **The next object is `osfmk_kern_work_interval.o`** (2 resolved (2 function) / 0 added → **748 / 650 / 98**), the smallest kind of step, because what 377 bought is a place to stand; the still-ahead named frontier is `bsd_miscfs_specfs_spec_vnops.o` (4 resolved, **91 added** — `buf_getblk`/`buf_brelse` down to `VNOP_IOCTL`, the buffer-cache and vnode-operation face of VFS, the largest incoming step the walk has faced). 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `checks=5`/`failures=0`, `abort_entries=0`, `checksum=0x907fe5c5`, log 301642 bytes / 3975 lines ending `No errors detected`, image 4880384 bytes sha256 `509faf051da25de77b8a61a3eed5c2ecde71e78be4eae25edf0be75b60ed56fb`, device back on Android on its own |
| stage90 | [experiment-378](experiment-378-osfmk-kern-work-interval-c-the-stub-377-stopped-at-and-a-stop-one-frame-down.md) | **`osfmk/kern/work_interval.c` — the object that retires 377's stop, and a stop one frame *down* rather than one call on.** **2 resolved (2 function, 0 storage) / 0 added** — `work_interval_port_notify` and `work_interval_thread_terminate`, the pool's only definitions of either — for 750 → **748** undefined, 652 → **650** function, 98 → **98** storage, all exact, and `xnu_arm_entry_stubnames.txt` reading 650 function / 98 data records. All 21 of the object's references are already real, so nothing inside it can stop the run; both retired records are *function* records, so no data record retires and `realstubs.o`'s `.bss` is unmoved, which is why every row at or below `.bss` was predicted unmoved and came out unmoved. Seven sections and a piece of arithmetic new to this walk: the object carries **both** a `.data` (0x8) and a `__DATA,__data` (0x18), and they land in the **two different groups** of one output section that 377 found — `entry.ld` places `*(.data .data.*)` first and `*("__DATA,*__data" "__DATA,*__const")` after it, so the `.data` appends inside group 1 at **0x801C50F0** (8-aligned, no fill) while the 0x18 goes to group 2, and the bucket grows by two named terms rather than one. **The prediction was `proc_encode_exit_exception_code` at key `0x80009628`** — `thread_terminate_self`'s next stub three calls on from 377's site, read out of `osfmk/kern/thread.c:585` — **and the run reports `stub_hit=os_reason_free` at `xnu_entry_stub_caller_v=0x800d69c4` = `uthread_cleanup + 0xD0`**, the return address of the `bl 801821a4 <os_reason_free>` at 0x800d69c0. The frame chain is the one the prediction was standing in, one level deeper than it looked: `thread_apc_ast` → `thread_terminate_self +0xE4` (377's stop, now gone from the stub list) → `+0xEC..+0x108` real, as written → `+0x11C` **`uthread_cleanup`, which the block's own tool listing labelled *real*** → `uthread_cleanup +0xD0` `os_reason_free`. **The defect is a tool-semantics one: "not a stub" is not "does not contain a stub."** `tools/first_stub_call.py` answers a per-callee question — is *this* symbol a stub — which is true of `uthread_cleanup` and is not the question the block meant to ask; `uthread_cleanup`'s body reaches a stub of its own before the frame returns to `+0x1C8`. **The instrument that answers the right question is the call *walk*, which descends** — `tools/xnu_entry_callwalk.py --root uthread_cleanup` names `os_reason_free` as the first stub on the straight-line path and lists the `throttle_lowpri_io` call at `+0x2C` as the first *guarded* alternative, and the run took neither: both guards (`+0x24` `ldr r0,[r1,#0x140]` / `cmp #0` / `bne`, then `+0x28` `ldr r0,[r5,#0x138]` / `beq`) read zero on this thread, so the walk went straight to the unconditional `+0xD0`. This is 375's lesson in a new place: the frontier is the first stub the **path** reaches, and a path descends — the cheap check is to walk from each frame the previous run named, not to list that frame's direct calls. **Layout: one miss, of 0x4, and it is a phase shift.** Every `.text` row exact — object **0x8017FCF4 (0x5AC)**, platform expert 0x801802A0 (0x150), its `D0Ev` 0x801803F0, last-kernel-ctor 0x801803F4, rtabi 0x801803F8 (0x54), `realstubs.o` **0x8018044C (0x3CF0 = 650 × 0x18)** — and every row *below* `.text`: `.rodata` run start **0x801843E8**, `.data` **0x801AC000 (0x1A350)**, `.sysctl_set` **0x801C6350 (0x158)**, `.init_array` **0x801C64A8 (0x90)**, `.bss` **0x801C6540 (0x39098)** with start, size *and* end unmoved, `__bss_end` **0x801FF5D8**, args **0x80201000**, `topOfKernelData` 0x80400000, headroom **2099752**, image **1860920 (0x1C6538)**, **text size 1748576 (0x1AAE60)**. From `osfmk_arm_pmap.o`'s `.rodata` (0x8018DA18) onward every `.rodata` row is **+0x4** of the +0x57C predicted, and the term that appears is the *fill* in front of it, **0x2 → 0x6**: the `.text` run's delta 0x57C is **4 mod 8**, so every 8- or 16-aligned input in the `.rodata` run re-rounds its fill by 4 (`fill = (−cursor) mod align`), and that one fill rides the run — `realstubs.o`'s `.rodata.str1.4` at **0x801A6E08** (0x369E) where 0x801A6E04 was predicted, `__TEXT,__const` at 0x801AA4A8. **And it is absorbed at the tail**: the pad before `__TEXT, initcode` measures **0x4** where 0xC was 377's value and 0x8 was predicted, so `initcode` still begins 0x801AA4B0, the three `__TEXT,__os_log` rows and `.ARM.exidx` land unchanged, and the raw end 0x801AAE60 is already 32-aligned so the closing `ALIGN(0x20)` costs 0. **What decides whether a shift survives a row is `delta mod alignment`** — 0x57C is 4 mod 8 — so 374's and 376's "a shift upstream of an `ALIGN` is invisible" now has a *cause* rather than a coincidence; and the pad is the term no sum of section *contents* can represent (375's rule), moving this time *against* the shift that caused it, which is why the miss touched no printed number. **And the mergeable row was predicted, not banded, for the first time since 301**: the object's eight `.rodata.str1.1` strings were checked against the image the device ran and none occurs in it, so there is nothing to collapse into and nothing for an earlier chunk to share, and the row measured **0x1A2** — exactly the object's own size. **The next object is `bsd_kern_sys_reason.o`** (`bsd/kern/sys_reason.c`), the pool's only definer of `os_reason_free`: 6 resolved (6 function) / 0 added → **742 / 644 / 98**, with `.text` 0x378, a 0x10 `.bss` (**the first `.bss` input in four steps**, so the pad rule is back in play for that bucket) and a group-2 `__DATA,__data` 0x30. Two other measured, unvisited candidates are recorded: `bsd_kern_kern_exit.o` (1 resolved / **33 added** → 783/684/99, the definition of `proc_encode_exit_exception_code`, still downstream of `uthread_cleanup`'s return at `thread_terminate_self +0x1C8`) and `bsd_miscfs_specfs_spec_vnops.o` (4 resolved / **91 added**, via the guarded `throttle_lowpri_io` at `uthread_cleanup +0x2C`). 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `checks=5`/`failures=0`, `abort_entries=0` with `abort_first_pc=0` and `abort_first_dfar=0`, `checksum=0x907fe5e5`, no `panic`/`exception:`, log 301626 bytes / 3975 lines ending `No errors detected`, image 4880384 bytes sha256 `d8da8daaba396dff83fb45c8c2308bb0602ed2889fc1292108d82b8ef64432fa`, device back on Android on its own |
| stage90 | [experiment-379](experiment-379-bsd-kern-sys-reason-c-the-frame-that-empties-and-the-leg-that-blocks-instead-of-returning.md) | **`bsd/kern/sys_reason.c` — the frame 378 stopped in, and the leg that blocks instead of returning.** **6 resolved (6 function, 0 storage) / 0 added** — `os_reason_alloc_buffer`, `os_reason_alloc_buffer_noblock`, `os_reason_create`, `os_reason_free`, `os_reason_init` and `os_reason_ref`, the pool's only definitions of all six — for 748 → **742** undefined, 650 → **644** function, 98 → **98** storage, all exact, with `xnu_arm_entry_undef.txt` reading 742 lines and `xnu_arm_entry_stubnames.txt` 644 `func` / 98 `data` records. All 17 of the object's references are already real, and all six retired records are *function* records, so no data record retires and `realstubs.o`'s `.bss` is unmoved — which is why every row at or below it was predicted unmoved. **The prediction was `proc_encode_exit_exception_code` at key `0x80009628`** — `thread_terminate_self`'s `+0x1C8`, the call that follows `uthread_cleanup`'s return and the reason this step was chosen — **and the run reports `stub_hit=ipc_kmsg_dequeue` at `xnu_entry_stub_caller_v=0x800dad30` = `ipc_mqueue_destroy_locked + 0x54`**, the return address of the `bl 8018125c <ipc_kmsg_dequeue>` at 0x800dad2c: the `while ((kmsg = ipc_kmsg_dequeue(kmqueue)) != IKM_NULL)` loop of `osfmk/ipc/ipc_mqueue.c`'s port-message-queue teardown, a frame that has nothing to do with the thread teardown the block was reasoning about. **The reason is the guard above the predicted call, and the block had already named it as falsifier (e)**: `thread_terminate_self`'s post-`uthread_cleanup` code computes `threadcnt = hw_atomic_sub(&task->active_thread_count, 1)` at `+0x168`, and the block that *contains* `proc_encode_exit_exception_code` is entered only if `threadcnt == 0 && task->bsd_info != NULL && !task_is_exec_copy(task)`; the walk took the `bne 0x80009684` leg instead — `uthread_cred_free` at `+0x228`, the wait-timer `timer_call_cancel`/`delay` loop, `stack_free_reserved`, `thread_mark_wait_locked` — and then **`thread_block` at `+0x2F8`**, which is unconditional on that path. **Which of the three conditions was false is a state the log does not settle** (a non-zero `active_thread_count` and a NULL `bsd_info` skip the same block), and the block says so rather than picking one. **And `thread_block` is the structural finding: this is the first stop since 302 that the walk reached through a context switch rather than by returning through frames**, so `lr` no longer describes a call and the frames above the stop are the scheduler's — a wall of a new kind, and the honest statement of the frontier is one frame wide. That one frame is exact: the in-image callers of `ipc_mqueue_destroy_locked` are only `ipc_port_clear_receiver+0x60` and `ipc_port_destroy+0x1C4`; `ipc_port_destroy` is additionally **tail-called** (`b 0x800D9664`) from `ipc_port_dealloc_special+0x2C`, whose own in-image callers are all teardown paths (`ipc_thread_terminate`, `ipc_thread_reset`, `ipc_task_terminate`, `ipc_task_reset`, `iokit_destroy_object_port`, `work_interval_port_notify`, `semaphore_dereference`, `iv_dealloc`, `convert_voucher_to_port`); and the two port functions plus `ipc_kobject_server` have **no in-image caller at all**, neither `bl` nor tail. **Layout: every `.text`-side row exact** — object `.text` **0x801802A0 (0x378)**, platform expert **0x80180618 (0x150)**, rtabi 0x80180770 (0x54), `realstubs.o` **0x801807C4 (0x3C60 = 644 × 0x18)**, `.rodata` run start **0x801846D0**, object `.rodata.str1.1` **0x801A6B49 (0x40)** — the first mergeable row in five steps that measured the object's own size *and* landed at the predicted address, because none of its three strings occurs in the image the device ran — `realstubs.o`'s `.rodata.str1.4` **0x801A7130 (0x3622)**, `__TEXT,__const` 0x801AA754 (0x4), **the `initcode` pad 0x8** (0x4 at 378, as the block's accounting said), `initcode` 0x801AA760 (0x64C), `.ARM.exidx` 0x801AB108 (0x8), raw end 0x801AB110, **the closing `ALIGN(32)` 0x10**, text size **1749280 (0x1AB120)**, `.data` **0x801AC000 (0x1A380)**, object `__DATA,__data` **0x801C6350 (0x30)**, `.sysctl_set` **0x801C6380 (0x158)**, `.init_array` **0x801C64D8 (0x90)**, image **1860968 (0x1C6568)**, args 0x80201000, `topOfKernelData` 0x80400000, tree 0x80600000 — **and one miss, in `.bss`, of the shape "the container moved and its contents did not"**: the block wrote that the bucket's start moves 0x40 (0x801C6540 → **0x801C6580**, measured exact) while its *end* stands still, and then kept 378's addresses for the entries inside it, so it has `iokit_Kernel_IOPowerConnection.o` at 0x801FCF14 where the map says **0x801FCF54**; the same 0x40 carries the whole tail, the object's 0x10 begins where IOPowerConnection.o's 0x18 ends at **0x801FCF6C**, `MSM8974PlatformExpert.o` follows at 0x801FCF7C, `xnu_arm_entry_macho.o`'s 0x0 at 0x801FCF94, `align64` puts `realstubs.o`'s slot at **0x801FCFC0** with its pad giving up exactly the 0x10 (`*fill* 0x3C` → **`*fill* 0x2C`**), and `__bss_end` is **0x801FF618** rather than 0x801FF5D8. **The pad rule was right and the base was stale**: the bucket's content span is unchanged at 0x39084 (start +0x40, content +0x10, pad −0x10), so the map's `.bss` size prints **0x39098** — 378's own number, for the opposite reason — the image is unaffected (it is `.init_array` end − base, no `.bss`), `args` stays the same page, and only `headroom` moves, by exactly the 0x40: 2099752 → **2099688**. **And this is 378's phase rule from its other side**: the `.bss` delta 0x40 is 0 mod 64, the alignment of the one input that could absorb it, so no fill re-phases and a shift that is a multiple of an alignment survives a round of it invisibly. The block's second defect was a transcription in the prediction itself — the text size written as 1753376 beside the hex 0x1AB120, which is 1749280 (the hex/decimal class again, 370's and 377's) — corrected in place rather than recorded as a miss. **The next object is `osfmk_ipc_ipc_kmsg.o`** (`osfmk/ipc/ipc_kmsg.c`), the pool's only definer of both stubs in the frame the run stands in: 54 definitions, 73 references of which 58 are satisfied, **13 resolved (12 function, 1 storage) / 15 added (15 function)** → **744 / 647 / 97**, the retired storage record being `ipc_kmsg_zone` (`pool B 4`) so `realstubs.o`'s `.bss` gives up `align64(4)` = 0x40 for the first change since 374. **380's prediction was corrected before its build, and the correction is the interesting part**: as first written it named `ipc_kmsg_delayed_destroy` at key **`0x800dad44`** if the queue is non-empty, and no stop in this frame otherwise — but `ipc_kmsg_delayed_destroy` *is* one of the twelve records this same object retires, so that branch is empty; `ipc_mqueue_destroy_locked` is `0xa0` bytes with exactly three stub calls (`ipc_kmsg_dequeue` at keys `0x800dad30` and `0x800dad54`, `ipc_kmsg_delayed_destroy` at `0x800dad44`), all three in the object 380 links, and everything after the loop is real (`waitq_invalidate_locked` is a four-instruction leaf, `waitq_clear_prepost_locked`, `pop {…, pc}`), so **the frame cannot stop in either queue state**. What the walk does next is decided by which caller it returns into, which the log cannot read — the frame was reached through a context switch — and both continuations are nameable: `ipc_port_clear_receiver+0x60` has no stop left (its one stub call, `ipc_pset_remove_from_all` at `+0x24`, precedes the call and is guarded), while `ipc_port_destroy+0x1C4` continues at `0x800d81e8` into a tail whose first surviving stubs are `ipc_notify_send_once` at key **`0x800d82C0`**, `ipc_notify_dead_name` at key **`0x800d8330`** and `io_free` at key **`0x800d8380`** (`ipc_kmsg_free` `0x800d828C` and `ipc_kmsg_reap_delayed` `0x800d82CC` are retired by the same link). So **380 predicts (a) `ipc_notify_send_once` `0x800d82C0`, (b) `ipc_notify_dead_name` `0x800d8330`, (c) `io_free` `0x800d8380`, or (d) no nameable stop — never a stop in the frame the run stands in**. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `checks=5`/`failures=0`, `abort_entries=0` with `abort_first_pc=0` and `abort_first_dfar=0`, `checksum=0x907fe6b5`, no `panic`/`exception:`, log 301628 bytes / 3975 lines ending `No errors detected`, image 4880384 bytes sha256 `0b0b8f9d3f8b4d85855d373eb35d9c9c07d790ff09b36d38af8b2d0f2fc6bdc6`, device back on Android on its own |
| stage90 | [experiment-380](experiment-380-osfmk-ipc-ipc-kmsg-c-the-object-that-empties-the-frame-and-the-prediction-that-retired-itself.md) | **`osfmk/ipc/ipc_kmsg.c` — the object that empties the frame 379 stopped in, and a next-stop prediction corrected before its build because it named a stop the same object retires.** **13 resolved (12 function, 1 storage) / 15 added (15 function)** — the twelve retired are `ipc_kmsg_alloc`, `ipc_kmsg_copyout_object`, `ipc_kmsg_copyout_size`, `ipc_kmsg_delayed_destroy`, `ipc_kmsg_dequeue`, `ipc_kmsg_destroy`, `ipc_kmsg_enqueue_qos`, `ipc_kmsg_free`, `ipc_kmsg_override_qos`, `ipc_kmsg_queue_next`, `ipc_kmsg_reap_delayed`, `ipc_kmsg_rmqueue`, and the one retired *storage* record is `ipc_kmsg_zone` (`pool B 4`) — the first storage retirement since 374, and record **21 of 98**, so it is not the last record whose term the slot table takes raw and the table gives up exactly `align64(4)` = 0x40 → **744 / 647 / 97**, all three measured exact (`xnu_arm_entry_undef.txt` 744 lines; 647 `func` + 97 `data` records). The three synthetic sizes, each recomputed from its rule: `realstubs.o` `.text` **0x3CA8** (647 × 0x18), `.rodata.str1.4` **0x366E** (0x3622 − 0x104 + 0x150, a term that needs the last-name-stays-last rule: as a plain `Σ align4(len+1)` the same list gives 0x3670), `.bss` **0x2604**. **Every section-level number exact**: object `.text` **0x80180618 (0x3BD0)**, platform expert **0x801841E8**, rtabi **0x80184340**, `realstubs.o` `.text` **0x80184394**, the `initcode` pad **0x4**, text size **1764896 (0x1AEE20)**, `.data` **0x801B0000 (0x1A3B0)**, the object's `__DATA,__data` **0x801CA380 (0x30)**, `.sysctl_set` **0x801CA3B0 (0x158)**, `.init_array` **0x801CA508 (0x90)**, image **1877400 (0x1CA598)**, `.bss` **0x801CA5C0 .. 0x80203618** with the object's 0x8 at **0x80200FBC**, `realstubs.o`'s slot at **0x80201000 (0x2604)**, the fill **0x2C → 0x24** (exactly the inserted 0x8 — 379's pad rule from its other side), printed `.bss` size **0x39058**, args **0x80205000**, `topOfKernelData` **0x80400000**, headroom **2083304** — and the run's own markers agree to the byte (`bss_bytes=0x00039058`, `bss_start=0x801ca5c0`, `bss_end=0x80203618`, `image_bytes=0x001ca598`, `args_pa=0x80205000`). **The tooling change underneath it**: every earlier layout prediction used `align_up(old_address + shift, alignment)`, which double-counts an input's old padding because `old_address` is already aligned; the linker's rule is `align_up(cursor, alignment)`, which reproduces the map at **every one of the 366 rows** of `.data`/`.sysctl_set`/`.init_array`/`.bss` (0 mismatches) and which 379's own `.bss` measurement confirms where the two disagree (a 0x40 shift puts `realstubs.o` at 0x801FD000 under the old form, and the map says 0x801FCFC0 = `align64` of the new cursor). **One prediction miss, in the mergeable `.rodata` run, and a two-map diff settles it**: rebuilding 379 with this object taken out reproduces its published numbers exactly (742/644/98, text 1749280, image 1860968, `.bss` 0x801C6580..0x801FF618, headroom 2099688), and across the whole `.text` section **exactly two pre-existing rows changed size** (`realstubs.o` `.text` 0x3C60 → 0x3CA8, its `.rodata.str1.4` 0x3622 → 0x366E) — **no earlier `.rodata.str1.1` chunk shrank at all**, every row from `osfmk_arm_arm_init.o` to `bsd_kern_sys_reason.o` moving by the same 0x3C18 and every row after the insertion point by 0x3CBC. So the 0x14 of `"overflow detected"` came out of the **new** object's own chunk (0xA5 as a sum of its four strings, **0x91** as placed) and `osfmk_kern_locks.o` keeps its full **0x10B** — the opposite direction from 377's five earlier chunks shrinking by 0x18 plus 0xC of fills, so **the net is the rule (own sum minus every string the image already had, 0xA5 − 0x14 = 0x91) and which chunk carries the saving is a measurement per case**; the addresses inside the run are 0x18 low and then 0x4 low (0x14 the wrong chunk, 0x4 the `SHF_MERGE` upper-bound rows — `osfmk_kern_kpc_common.o` (0x4) and `bsd_kern_kern_ktrace.o` (0xD7) print the *same* address), and both were absorbed by `initcode`'s pad (0x4 → **0x0**), so `initcode`, the content end and the text size are exact and five rows inside the run are not. **And the stop is `stub_hit=io_free` at key `0x800d8380`** = `ipc_port_destroy + 0x35C`, the last-reference free — prediction **(c)** of the corrected set, on branch (b) of the caller question, which also settles *which of the two callers* the frame returned into, the one thing 379's record said no log could settle. The correction mattered because 379's prediction named `ipc_kmsg_delayed_destroy` at key `0x800dad44` — one of the twelve records this same object retires: `ipc_mqueue_destroy_locked` is 0xa0 bytes with exactly three stub calls, all three in the loop and all three retired here, and everything after the loop is real, so **the frame cannot stop in either queue state**. The structural finding: the return address is missing when a frame is reached through a context switch, but the caller's own *continuation* is in the image, so a stop can be predicted from the code the thread is about to run. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `checks=5`/`failures=0`, `abort_entries=0` with `abort_first_pc=0` and `abort_first_dfar=0`, `checksum=0x90406605`, `why=0x80188824` (379's 0x80184C0C plus the step's 0x3C18 shift), no `panic`/`exception:`, log 301619 bytes / 3975 lines ending `No errors detected`, image 4896768 bytes sha256 `a492fb87fd6a1ad369025a8f79289747bf32094a75f9b61a12541396bd41d73b`, device back on Android on its own. **381 is `osfmk_ipc_ipc_object.o`** (`osfmk/ipc/ipc_object.c`, the pool's only definer of `io_free` — XNU's own, not IOKit's): 24 definitions, 37 references, **12 resolved (11 function, 1 storage) / 8 added (8 function)** → **740 / 644 / 96**, the retired storage record `ipc_object_zones` (`B 8`) giving up `align64(8)` = 0x40 again, five of the eleven retired functions being names this step obliged and the eight it adds (`ipc_entry_*`, `ipc_right_*`) making it the first step where linking an object *creates* new stubs; and `io_free`'s own body reaches no new stub (`ipc_port_finalize` is real, `io_lock_destroy` and `zfree` are macros), so that frame cannot stop either. |
| stage90 | [experiment-381](experiment-381-osfmk-ipc-ipc-object-c-the-object-that-makes-io-free-real-and-a-run-that-was-silent.md) | **`osfmk_ipc_ipc_object.o`, an exactly-predicted layout, and a run that was silent** — `osfmk/ipc/ipc_object.c`, **8140 bytes, 24 definitions, 37 references, 3564 bytes of `.text`**, **12 resolved (11 function + the storage record `ipc_object_zones`, `B 8` = record 22 of 97) / 8 added (all functions: `ipc_entry_alloc`, `ipc_entry_alloc_name`, `ipc_entry_get`, `ipc_entry_modified`, `ipc_right_inuse`, `ipc_right_lookup_two_write`, `ipc_right_lookup_write`, `ipc_right_rename`)** — **the first step where linking an object *adds* stubs as well as retiring them**, because the object that makes `io_free` real is also what `ipc_object_copyin`/`copyout` are built on. Counts exact (744 → **740** undefined, 647 → **644** function, 97 → **96** storage), the three synthetic sizes each recomputed from their rule and each first checked against 380's map (`realstubs.o` `.text` 0x3C60, `.rodata.str1.4` **0x3622** — which lands exactly on 379's value, since the 0xF0 of retired names and the 0xA4 of added ones cancel against the untouched last term — and `.bss` 0x25C4), and **every layout number exact**: text **1768512 (0x1AFC40)**, image **1877400 (0x1CA598, unchanged — the growth stays inside the same 0x4000 bucket)**, `.bss` **0x801CA5C0 .. 0x802035D8** (printed **0x39018**), `realstubs.o`'s `.bss` **0x80201000 (0x25C4)**, the fill **0x24 → 0x1C**, args **0x80205000**, headroom **2083368** — with the run's own markers agreeing to the byte. The one miss is a **0x4 residue inside the mergeable `.rodata` run**, and the two-map diff says what it is not: only the three new rows were added, only two pre-existing rows changed size (both `realstubs.o`'s, both as predicted), and no `.rodata.str1.1` row changed anywhere — so it is the known `SHF_MERGE` upper-bound band, absorbed by the `initcode` pad. **The run was silent:** the ladder completes through `jumping to XNU's _start`, and then no `stub_hit`, no `exception:`, no epilogue report — so **neither predicted stop (`task_affinity_deallocate` key `0x800c0fb8` / `vm_purgeable_disown` key `0x800c0fc0`) was reached**, and neither was any key inside `ipc_port_destroy`'s frame: the prediction's falsifier in its strongest form. Silence has two readings (a hang before the next stub, or a stub whose reporting path failed) that the log cannot separate, which is what the checkpoint instrument exists for — the next run is `STAGE90_ENTRY_CHECKPOINT=ipc_port_dealloc_special`, which would both prove the reporting path and name the caller of the frame above the stop. Safety: 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `panic`, no `exception:`; log 294553 bytes, 3929 lines; the device came back to Android on its own, by the hardware watchdog (the payload's dead-man never dumped) |
| stage90 | [experiment-382](experiment-382-the-checkpoint-reports-and-names-the-caller-a-thread-teardown-not-the-task-teardown.md) | **The checkpoint reports, and the caller it names is a thread teardown, not the task teardown** — 381's run was silent, so the next build was a *measurement*, not a link: `STAGE90_ENTRY_CHECKPOINT=ipc_port_dealloc_special` (the call the frame above `ipc_port_destroy` is entered through), and **it reported** — `stub_hit=ipc_port_dealloc_special`, `xnu_entry_stub_caller=0x800c9a0c` = **`ipc_thread_terminate + 0x18c`** (the `bl` at 0x800c9a08), with `cp_arg0=0xc056f300`, `cp_arg1=0xc056c840`, `cp_arg2=0xc056f304`, `cp_arg3=0`; the checkpoint costs 192 bytes of `.text` (1768512 → 1768704) and moves nothing else (image 1877400 unchanged, `.bss` 0x801CA5C0 .. 0x802035D8, headroom 2083368). **Two conclusions.** (1) **The reporting path works in this build** — a wrapper reached `entry_stub_hit` and the payload wrote its keys in the same image family that was silent in 381, so 381's ending is *not* the instrument's defect (which is what 280's silence turned out to be); the walk reaches this call and after it nothing reports, so the silence lives at or after `ipc_thread_terminate + 0x18c`. (2) **The caller is named by measurement, and it corrects a reading defect:** 381's block enumerated `ipc_port_dealloc_special`'s callers from `ipc_port.c` and concluded "what is left is `ipc_task_terminate`", but the linked image has **fifteen call sites in eleven functions** (`iv_dealloc`, `convert_voucher_to_port`, `ivac_dealloc`, `convert_voucher_attr_control_to_port`, `semaphore_dereference`, `ipc_task_terminate` ×3, `ipc_task_reset`, `ipc_thread_terminate` ×2, `ipc_thread_reset` ×2, `iokit_destroy_object_port`, `work_interval_port_notify`) — **a caller list is a property of the link, so read it out of the linked image**. The forward reading from the image is what the next run tests: the whole subtree under `io_free` — `ipc_port_finalize` → `ipc_table_free`/`kfree` and `ipc_mqueue_deinit` → `waitq_set_deinit`/`waitq_deinit` → `waitq_lock`, `walk_waitq_links`, `delay` …, plus `lck_spin_destroy` — contains **no stub call at all**, and the only stubs anywhere in it are the four inside `zfree` (`trace_backtrace`, `btlog_add_entry`, `OSBacktrace`, `btlog_remove_entries_for_element`), each guarded by `DO_LOGGING(zone)`/`corruption_debug_flag`, which this payload's boot-args leave off. So the next probe is the `_AFTER` variant on the function this step made real — `STAGE90_ENTRY_CHECKPOINT=io_free STAGE90_ENTRY_CHECKPOINT_AFTER=1` — whose *silence* is a finding rather than an absence because the call site (key 0x800d8380) is already proved reached: report ⇒ `io_free` returns and the silence is after the port free; silence ⇒ it does not return and the silence is inside it. Safety: no `exception:`, no `panic`, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, log 301716 bytes / 3979 lines, device back on Android on its own and confirmed there |


| stage90 | [experiment-383](experiment-383-the-checkpoint-chain-across-the-context-switch.md) | **The checkpoint chain: the boot thread's own teardown, the context switch, and the ladder's top** — eleven *measurement* builds on the 381 link, one symbol per run, answering what 381's silence and 382's report left open. **383–388 are one straight line inside a single `bl thread_deallocate` near the end of `kernel_bootstrap`:** `io_free` `_AFTER` returns `cp_ret=0x60000013` from `ipc_port_destroy + 0x35C` (so 381's object works and that frame empties); `ipc_thread_terminate` `_AFTER` returns `0x00000000` from `thread_deallocate + 0x64`; `task_deallocate` `_AFTER` returns `0xc044b678` from `thread_deallocate + 0xF8`; `thread_deallocate` `_AFTER` returns `0x801CF7EC` from **`kernel_bootstrap + 0x36C`** — so the teardown is `kernel_bootstrap` handing the boot thread's reference back before it hands the processor over, and **382's address is not a frontier**: the frame was right, the location was wrong. **389–393 cross the context switch:** `machine_load_context` is reached from `load_context + 0xE8` (389) and does not return (390, silent — its body is `mrc`/`ldr`/`mcr`/`ldr`/`bx lr` with no callee, so a context switch is the only thing that can be happening), and the walk continues on the new thread's stack — `sched_startup` reports from `kernel_bootstrap_thread + 0x30` and returns `0xFFFFFFFF` (391/392, proving the scheduler woke the boot thread's `thread_block(THREAD_CONTINUE_NULL)`), then `ipc_thread_call_init` from `kernel_bootstrap_thread + 0x78` (393). **The one reading the series could not explain from its own numbers:** run 386 (`SKIP=1` + `_AFTER`, banner *"running call 2 for real and reporting its return value"*) reports `cp_calls=0x00000008` where the wrapper's own arithmetic — `if (calls < SKIP) { calls++; return real(…); } calls++; report` — can only yield 2, and the same log shows `xnu_entry_kv_written=0xD7` against `xnu_entry_kv_in_dram=0xFB`, i.e. 36 records in the buffer this run did not write; both point at `.bss` a previous boot left behind, and neither is explained. **And the thing the series did not notice until 404:** since 381 the link has not changed and **not one report has come from a stub** — every report is a checkpoint wrapper and every silent run is the *same* 294553-byte, byte-identical event with no `stub_hit`, no `exception:` and no epilogue — so 381's silence is not a second phenomenon to be explained by the teardown chain; it is a **block, not a missing symbol**, and experiments 394–404 name it. Safety: all runs non-persistent `fastboot boot` through the two gated scripts, no `exception:`, no `panic`, `persistent_write_attempted=0x00000000` ×25 and `failure_mask=0x00000000` ×87, `abort_entries=0`, device back on Android on its own each time |
| stage90 | [experiment-394](experiment-394-the-ladder-bisection-and-the-frame-that-never-wakes.md) | **The ladder bisection, and the frame that never wakes: `ml_get_max_cpus` blocks in `commpage_populate` for a driver this platform never starts** — eleven measurement builds on the 381 link. The walk climbs `kernel_bootstrap_thread`'s whole pre-context-switch ladder past `mapping_adjust` (394 reached; 395 `_AFTER` returns `0xC05BB7FC`), `PE_init_iokit` (396), `vm_shared_region_init` (397, caller `kernel_bootstrap_thread + 0x14C` — so `PE_init_iokit` *ran and returned*, `early_boot_complete` was stored and **`spllo()` executed: interrupts are enabled from here on**), and into `commpage_populate` (401 reached; 402 `pmap_create_sharedpage` returns `0x8050E000`), and stops **inside `ml_get_max_cpus`**, in its `thread_block`, waiting for `max_cpus_initialized` to become `MAX_CPUS_SET`: 403 reports its call site `commpage_populate + 0x70` (its first invocation on this boot) and 404 is **silent — it does not return**. `arm_vm_prot_finalize` (398) and `panic` (399) are both silent, so the ending is neither a fault nor a panic, and `OSCompareAndSwap64` (400) being silent clears `commpage_populate`'s CAS64 loops. **The search space was finite before the device was touched:** `bsd_init`'s **first** call is the stub `throttle_init` (`bsd_init + 0x8`, key `0x8003A9FC`), which this walk has never reported, so the hang is strictly before `bsd_init` — a bracket of 21 frames that 398 and 400 divide rather than extend. **The root cause is a cross-subsystem ordering dependency — a fourth kind of stop.** `ml_init_max_cpus` is the only writer of `max_cpus_initialized` anywhere in the tree, and its only caller in the linked image is `IOCPUInterruptController::initCPUInterruptController(int,int)+0x94`; `ml_install_interrupt_handler`'s only caller is the same class's `enableCPUInterrupt`, and `createCPUInterruptController` — the platform-expert hook that would instantiate it — **is not in the open-source tree at all**, while this stage's platform expert overrides only `start`/`deleteList`/`excludeList`. So **no interrupt handler is ever installed on this boot**, nothing can set that event, and the boot thread is **parked, not spinning**: no `exception:`, no `panic`, no stub report, and the hardware watchdog is what returns the device — which is the shape of every silent run since 381. The next step is platform-expert code, not another link step: create the controller and `initCPUInterruptController(sources, cpus)` (or, as a probe, call `ml_init_max_cpus` directly). Safety: non-persistent `fastboot boot` only, no `exception:`, no `panic`, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, device back on Android on its own and confirmed there |
| stage90 | [experiment-405](experiment-405-the-block-is-cured-and-the-frontier-moves-23-frames.md) | **The block is cured and the frontier moves 23 frames: the platform expert announces one CPU, and `bsd_init`'s first statement stops on `throttle_init`** — no object linked: the step is stage-owned code, `stages/stage90/xnu_platform/MSM8974PlatformExpert.cpp`'s `start` calling `ml_init_max_cpus(1)` after `super::start(provider)`. It is the probe, not Apple's path: `ml_get_max_cpus` is a wait (`assert_wait` + `thread_block`) whose only writer `ml_init_max_cpus` is called in the image solely from `IOCPUInterruptController::initCPUInterruptController` (`IOCPU.cpp:765`), whose platform-expert hook `createCPUInterruptController` is not in the open-source tree; the faithful fix (the CPU interrupt controller, which also installs the per-CPU interrupt handler the timer needs) is therefore **still owed**, and `1` is the number of processors this payload actually brings up (no CPU driver exists, so nothing can start a second one; four would size `cpu_data`, the scheduler's per-CPU arrays and `_COMM_PAGE_NCPUS` for processors that never register). **Measured: `stub_hit=throttle_init` at `xnu_entry_stub_caller_v=0x8003A9FC` = `bsd_init + 0x8` (the `bl throttle_init` at 0x8003A9F8), name and key exactly as predicted**, with `abort_entries=0`, `checks=5`/`failures=0` and — as the prediction required — the stub set untouched (`throttle_init` still record 544 of 740, `func throttle_init T`), because the step only adds a reference to an already-defined symbol (`U ml_init_max_cpus` → the image's real `0x800078FC`). The prediction was made by walking every frame between the block and `bsd_init` in the image the step would start from: `vm_commpage_init`, `vm_commpage_text_init`, `mac_policy_initmach`, `arm_vm_prot_finalize`, `read_random` and `vm_set_restrictions` all **reach no stub on the straight-line path**, and `bsd_init`'s first call is the stub. **Three readings beyond the prediction:** the block was the *only* thing between `commpage_populate + 0x70` and `bsd_init` (23 frames, including `commpage_populate`'s remaining ~100 instructions and **both** `OSCompareAndSwap64` sites at `+0x238`/`+0x288` that 400 could never reach); the boot then crosses those frames with `SCTLR.I` set since 397's `spllo()` and no handler installed, with no vector fired and no `exception:`; and nothing about the stub table moved. Layout: text **1768544** (381's 1768512 + 0x20), image **1877400** and `.bss` **0x801CA5C0 .. 0x802035D8** unchanged, headroom **2083368**; `xnu_entry_why=0x801895e8` is the payload's own message `"a symbol this image does not provide was called"` (`why_byte=0x61`), so the ending is a stub stop and not a fault. Next: `throttle_init` is defined in `bsd/miscfs/specfs/spec_vnops.c:1287`, so the next step links `bsd_miscfs_specfs_spec_vnops.o` (measured at 378 as 4 resolved / 91 added) — the frontier has left `osfmk`'s commpage and IOKit's platform plane and is now in the BSD kernel's own init sequence. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301625 bytes / 3975 lines ending `No errors detected`, device back on Android on its own and confirmed there |
| stage90 | [experiment-406](experiment-406-spec-vnops-retires-throttle-init-and-the-frontier-enters-the-bsd-subsystem-init.md) | **`bsd/miscfs/specfs/spec_vnops.c` — the step that retires `throttle_init`, and the frontier leaves the kernel's start-up machinery for the BSD subsystem's own init** — one object linked, `bsd_miscfs_specfs_spec_vnops.o` (the pool's only definer of `throttle_init`, `spec_vnops.c:1287`), appended to `LINK_OBJS` after `osfmk_ipc_ipc_object.o`; 405's stop was `throttle_init`, the **first statement of `bsd_init`**. **Effect measured before the build: 4 resolved (3 function + 1 storage — `rethrottle_thread`, `spec_filtops`, `throttle_init`, `throttle_lowpri_io`) / 91 added (55 function + 36 storage, the largest `added` column since 342)**, and the build confirms every column to the record: **740 → 827** stub names, **644 → 696** function (−3/+55), **96 → 131** storage (−1/+36), `throttle_init` gone. **Prediction: `stub_hit=kauth_init` at caller key `0x8003ABB4`; measured exactly that**, `abort_entries=0`, `checks=5`/`failures=0`. The reading has three parts: (1) `throttle_init`'s body calls eight names — `lck_grp_attr_alloc_init`, `lck_grp_alloc_init`, `lck_attr_alloc_init`, `lck_mtx_init`, `PE_get_default` ×3, `PE_parse_boot_argn` ×4, `thread_call_allocate`, `vm_io_reprioritize_init` — and **every one is real and reaches no stub on its own straight line**, so it runs to its end (`throttle_init_throttle_window` is inlined); (2) the frames between its return and the next stub — `_consume_printf_args` (+0x10), `kmeminit` (+0x14), four `PE_parse_boot_argn` — also reach no stub, and `bsd_init` makes no other call between +0x9C and +0x1C0; (3) `kauth_init` is record 175 of 740 before the link and 215 of 827 after, neither retired nor created by it, and the site is unconditional (`bl kauth_init` at 0x8003ABB0, then `procinit` at +0x1C4 and `tty_init` at +0x1C8, both stubs). **And 342's trap checked as an intersection: none of the 91 added names is a `bl` target anywhere inside `bsd_init`'s 696 instructions** — they are the BSD VFS/buf face (`buf_*`, `err_*`, `nop_*`, `spec_*`, `VNOP_*`, `vnop_*_desc`, `iskmemdev`, `check_mountedon`, `chrtoblk`, `bpfkqfilter`, `session_lock`, …) reached only from `spec_vnops`'s own functions. Layout: text **1786464**, image **1911120**, `.bss` **0x801D2980 .. 0x8020FD98** (250904), args **0x80211000**, headroom **2032232**, and the run's markers agree to the byte (`bss_bytes=0x0003D418`, `image_bytes=0x001D2950`, `args_pa=0x80211000`); **`.bss` is the section this step moves and it is the stub object that moves it** — the 36 new storage stand-ins cost `align64(size)` each (354's rule) and land inside `.bss`, so its *start* steps for the first time in a long while. Next: `kauth_init` is defined by `bsd/kern/kern_authorization.c:143` and that object measures **1 resolved / 11 added** (10 function + 1 storage). Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301622 bytes ending `No errors detected`, device back on Android on its own and confirmed there |
| stage90 | [experiment-407](experiment-407-kern-authorization-the-stop-the-step-itself-creates.md) | **`bsd/kern/kern_authorization.c` — the step whose stop is a name the step itself creates, one call into the function it makes real** — one object linked, `bsd_kern_kern_authorization.o` (the pool’s only definer of `kauth_init`, `kern_authorization.c:143`) — **1 resolved / 11 added (10 function + 1 storage)** → **827 → 837** stub names, **696 → 705** function (−1, +10), **131 → 132** storage (+1). **Prediction: `stub_hit=kauth_cred_init`, caller key = `kauth_init + 0x58`; measured exactly that at `xnu_entry_stub_caller_v=0x80188A38`** (return address of the `bl 8018b160 <kauth_cred_init>` at `0x80188A34`), `abort_entries=0`, `checks=5`/`failures=0`. The stop is a name **this step creates** (342/348’s shape), and unavoidably so: `kauth_init`’s first three calls — `lck_grp_attr_alloc_init`, `lck_grp_alloc_init`, `lck_grp_attr_free` — are all real **and each was walked to no stub beneath it**, so the hole is the fourth call, `kauth_cred_init`, which nothing linked defines. 342’s trap checked and empty: of the 11 created names, **`bsd_init` calls none**. Layout: text **1792096** (0x1B58C0), image **1911216** (0x1D29B0), `.bss` **0x801D29C0 .. 0x8020FE18** (250968), args **0x80211000**, headroom **2032104**, run markers agreeing (`bss_bytes=0x0003D458`, `args_pa=0x80211000`). Next: `kauth_cred_init` is defined by `bsd/kern/kern_credential.c:3499`. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301627 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-408](experiment-408-kern-credential-empties-kauth-init-and-the-frontier-steps-into-bsd-init.md) | **`bsd/kern/kern_credential.c` — the object that empties `kauth_init`’s frame, and the frontier steps one call further into `bsd_init`** — one object linked, `bsd_kern_kern_credential.o` (the pool’s only definer of `kauth_cred_init`, `kern_credential.c:3499`) — **23 resolved (22 function + 1 storage) / 7 added (7 function)** — the **largest resolution since 381** — → **837 → 821** stub names, **705 → 690** function (−22, +7), **132 → 131** storage (−1); it closes five of the eleven names 407 itself created. **Prediction: `stub_hit=procinit` at `bsd_init + 0x1C4`; measured exactly that at `xnu_entry_stub_caller_v=0x8003ABB8`** (return address of the `bl 8018e9f0 <procinit>` at `0x8003ABB4`), `abort_entries=0`, `checks=5`/`failures=0`. Three measured parts: `kauth_cred_init`’s body calls only `lck_mtx_alloc_init`, `__MALLOC` (real since 377; walked to no stub) and `panic`, the rest being an unrolled `TAILQ_INIT` loop with no calls; after it returns, `kauth_init`’s remaining path — `lck_mtx_alloc_init`, `kauth_register_scope` ×3, `lck_grp_free` (whose bodies are all real) — empties the frame; so the stop is the caller’s next statement, `bl procinit`. **Falsifier named and silent: the real `panic("startup: kauth_cred_init")`, taken iff the 0xC800-byte hash-table `MALLOC` returns NULL.** Layout: text **1802336** (0x1B7EE0), image **1927656** (0x1D69E8), `.bss` **0x801D6A00 .. 0x80213E58**, args **0x80215000**, headroom **2015656**, run markers agreeing. Next: `procinit` is defined by `bsd/kern/kern_proc.c` — the walk is retiring one stub per object along `bsd_init`’s own statement list. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301620 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-409](experiment-409-kern-proc-and-conf-param-the-process-table-and-the-zero-cured-one-step-early.md) | **`bsd/kern/kern_proc.c` + `bsd/conf/param.c` — the process table goes real, and the zero that would have panicked it is cured one step before the symbol that reads it arrives** — **two objects, and the second is not optional**: `bsd_kern_kern_proc.o` is **55 resolved (49 function + 6 storage) / 7 added** — the largest single resolution of the walk, retiring `allproc`, `zombproc`, `pidhash`, `pidhashtbl`, `pgrphashtbl`, `sesshashtbl` and the whole `proc_*` accessor family including 408’s own `proc_ucred` — and `bsd_conf_param.o` is **7 resolved (all storage) / 0 added, 0 references**. Counts **821 → 773 → 766** stub names, **690 → 647** function, **131 → 119** storage. **The reason for the pair:** `procinit`’s first call is `hashinit(maxproc / 4, …)` and `maxproc` was a **zeroed data stand-in** (`data maxproc D 0x4`), while `hashinit`’s first statement is `if (elements <= 0) panic("hashinit: bad cnt")` — so a `maxproc` still reading zero turns a stub stop into a **panic one step later**. 341/343/377’s rule, applied at the first place the read exists; the value is measured out of `bsd_conf_param.o`’s own `.data` — word 0 is **0x3E8 = 1000** (`CONFIG_EMBEDDED`’s `NPROC`), so the count is 250. **Prediction: `stub_hit=hashinit` at `procinit + 0x6C`; measured exactly that at `xnu_entry_stub_caller_v=0x8018C550`** (the `bl 8018c54c <hashinit>`), `abort_entries=0`, `checks=5`/`failures=0`. `procinit`’s body makes exactly four calls, all `hashinit` (`personas_bootstrap` is behind `CONFIG_PERSONAS`, undefined here). Layout: text **1819776**, image **1944256**, `.bss` **0x801DAAC0 .. 0x80217C58**, args **0x80219000**, headroom **1999784**, run markers agreeing (`bss_bytes=0x0003D198`). Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301620 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-410](experiment-410-kern-subr-empties-procinit-and-the-frontier-moves-to-tty-init.md) | **`bsd/kern/kern_subr.c` — the object that empties `procinit`’s frame, and the frontier moves to `tty_init`** — one object, `bsd_kern_kern_subr.o` (the pool’s only definer of `hashinit`, `kern_subr.c:307`): **6 resolved (6 function) / 2 added** (`copywithin`, `subyte`) → **766 → 762** stub names, **647 → 643** function, **119 → 119** storage. **Prediction: `stub_hit=tty_init`, caller key `0x8003ABBC`; measured exactly that**, `abort_entries=0`, `checks=5`/`failures=0`. `hashinit`’s 0xC8-byte body calls only `__MALLOC` (real since 377) and `panic`, so nothing stops inside it and `procinit` returns; the stop is then `bsd_init`’s next statement, the `bl 80194ae4 <tty_init>` at `0x8003ABB8`. **And the panic `hashinit` is one statement away from did not fire — this run is the measurement of 409’s cure**: `maxproc` reads 1000, so `elements = 250 > 0`. Layout: text **1824288**, image **1944328**, `.bss` **0x801DAB40 .. 0x80217CD8**, args **0x80219000**, headroom **1999656**, run markers agreeing. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301620 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-411](experiment-411-tty-c-and-the-frontier-jumps-to-init-system-override.md) | **`bsd/kern/tty.c` — `tty_init` retires and the frontier jumps 0x1E8 bytes down `bsd_init`'s statement list** — one object, `bsd_kern_tty.o` (the pool's only definer of `tty_init`): **8 resolved (7 function, 1 storage) / 21 added (18 function, 3 storage)** → **762 → 775** stub names, **643 → 654** function, **119 → 121** storage; the three storage entries are the line-discipline table (`linesw`, `nlinesw`) and `averunnable`. **Prediction: `stub_hit=init_system_override`, caller key `0x8003ADA4`; measured exactly that** at `xnu_entry_stub_caller_v=0x8003ada4`, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. `tty_init`'s whole body is three real lock-group calls, so the frame returns — **but the next stub is 22 calls away, not one**: `init_system_override` sits at `+0x3B0` and `ulock_initialize` at `+0x3B4` (corrected in 412 — 411's first write-up said `+0x1B0`/`+0x1D8`; the byte jump of 0x1E8 was right, the offsets were not), the compiler having ordered the sites against source order. The whole reading rested on none of the 22 being a stub or containing one, and each of the **15 non-trivial callees** (`kalloc_canblock`, `mac_policy_initbsd`, `get_bsdthread_info`, `set_bsdtask_info`, `__bzero`, `_consume_printf_args`, `strlcpy`, `current_thread`, `current_task`, `lck_mtx_init`, `lck_spin_init`, `lck_mtx_alloc_init`, `lck_grp_alloc_init`, `lck_attr_alloc_init`, `lck_grp_attr_alloc_init`) was walked with `xnu_entry_callwalk.py` to "reached no stub on the straight-line path" — 374/378's rule, since "not a stub" is not "does not contain one". The intersection of the 21 added names with `bsd_init`'s call set is empty, so 342's trap is checked and comes out empty too. Layout: text **1844544**, image **1960760**, `.bss` **0x801DEB40 .. 0x8021BE18** (250584 bytes), args **0x8021D000**, headroom **1982952**, payload sha256 `de3e22d88c7404d72ad50fddf8009daca4666753fa8e631f09d9e78b4f1046a9`, run markers agreeing to the byte. Ahead on the same line: `ulock_initialize`, `file_lock_init`, `ubc_init`, `vfsinit`, `mcache_init`, `mbinit`, `aio_init`, `socketinit`, `domaininit`, `inittodr`, `IOFindBSDRoot`, `vfs_mountroot`. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301632 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-412](experiment-412-kern-overrides-retires-init-system-override-and-the-frontier-steps-to-ulock-initialize.md) | **`bsd/kern/kern_overrides.c` — `init_system_override` retires, and the frontier takes one step to `ulock_initialize`** — one object, `bsd_kern_kern_overrides.o`: **1 resolved (1 function, 0 storage) / 0 added**, all **16 references already satisfied** — the cleanest step shape there is, an object that can retire a stub and cannot create one → **775 → 774** stub names, **654 → 653** function, **121 → 121** storage. **Prediction: `stub_hit=ulock_initialize`, caller key `0x8003ADA8`; measured exactly that** at `xnu_entry_stub_caller_v=0x8003ada8`, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. `init_system_override`'s body makes exactly four calls (`lck_grp_attr_alloc_init`, `lck_grp_alloc_init`, `lck_attr_alloc_init`, `lck_mtx_init`), all real and each walked to "reached no stub on the straight-line path"; the rest is stores into its own `.bss` statics, two zeroed counters and `sys_override_enabled = 1`. **The next stop is the very next instruction**: `bl <init_system_override>` at `0x8003ADA0`, `bl <ulock_initialize>` at `0x8003ADA4`, `bl <proc_list_lock>` at `0x8003ADA8` — so the frontier moves 4 bytes. **This step corrects 411's offsets**: `ulock_initialize` is at `bsd_init+0x3B4`, not `+0x1D8` (the 0x1E8-byte jump was right, the offset quoted for the next stub was not). A build-order note that cost a device run: the first build of this step produced a payload **bit-identical to 411's** because `stages/stage90/build.sh` does not relink the entry image — `xnu_arm_boot/build_entry.sh` with `STAGE90_ENTRY_REAL_ARM_INIT=1` is what consumes `LINK_OBJS`; the unchanged sha256 and the unchanged 775-record stub list were the tell, caught before the device was touched. Layout: text **1845312**, image **1960812**, `.bss` **0x801DEB80 .. 0x8021BE98** (250648 bytes), args **0x8021D000**, headroom **1982824**, payload sha256 `80aa88db6538bdc8a1a063dbcf01a49c612079c96f9ea522a01adcdb0ce6ce2b`, run markers agreeing (`image_bytes=0x001DEB6C`, `checksum=0x90406E31`). Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301628 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-413](experiment-413-sys-ulock-retires-ulock-initialize-and-the-stop-is-a-guarded-site-the-walk-had-named.md) | **`bsd/kern/sys_ulock.c` — `ulock_initialize` retires, and the run stops one level down at a guarded site the walk had already named** — one object, `bsd_kern_sys_ulock.o`: **2 resolved (2 function) / 2 added** (`ulock_initialize`, `kdp_ulock_find_owner` out; `port_name_to_thread_for_ulock`, `workqueue_get_sched_callback` in) → **774 → 774** stub names, **653 → 653** function, **121 → 121** storage, all three exactly as predicted (two cancel). **Prediction: `stub_hit=file_lock_init` at `0x8003B024`; measured `stub_hit=mac_cred_label_init` at `xnu_entry_stub_caller_v=0x8018ADB4` — a miss**, though `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. **Why it missed, and this is the experiment's point:** the prediction read the walk's *first line* ("walk from `kauth_cred_create` reached no stub on the straight-line path") and not its *second list*, which named the stop as its first entry — `kauth_cred_create+0xcc -> mac_cred_label_init STUB`. The two lines say different things, and the tool's own header says a run can stop earlier than the straight-line answer on a conditional branch that is taken. The site is the **inlined copy of `kauth_cred_alloc`**: its body is in the image twice (out of line at `0x8018AC80` with `bl mac_cred_label_init` at `+0x58`, and inlined into `kauth_cred_create` at `+0x90..+0xCC`), and the run reached the second copy's `+0xCC` site, not the `+0x5C` one. **Measured defect 128: a negative from a straight-line walk is not a negative for the function — read the guarded list before predicting.** The step itself is sound: neither created name is reachable from `ulock_initialize` or from `bsd_init`'s line (342's trap empty), and the body's two `assert`s were checked against a **measured** value — `thread_max` is real data, read out of the image's `.data` at `0x801D4378` as **0x600 = 1536** (`startup.c:803`'s `thread_max = task_max * 5` is inside `#if defined(__LP64__)`, not taken here), so `ull_hash_buckets = 512`, `512 >= 384` holds, `kalloc` is 2048 bytes, no panic. Layout: text **1848224**, image **1960836**, `.bss` **0x801DEBC0 .. 0x8021BED8**, args **0x8021D000**, headroom **1982760**, payload sha256 `2a3379bf71524bb556288509fd6204d7614f6bcc2540bfd67fd01a04a6751675`, run markers agreeing. Frontier: `mac_cred_label_init` from **`security/mac_process.c`** — **17 resolved / 0 added**, all 19 references satisfied, and all seven targets of those seventeen (`mac_labelzone_alloc`, `mac_policy_list_conditional_busy`, `mac_policy_list_unbusy`, `mac_error_select`, `mac_externalize`, `posix_cred_get`, `bcmp`) are real. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301631 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-414](experiment-414-mac-process-the-mac-cred-label-family-goes-real-and-the-frontier-moves-to-file-lock-init.md) | **`security/mac_process.c` — the whole `mac_cred_label_*` family goes real, and the frontier moves to `file_lock_init`** — one object, `security_mac_process.o`: **17 resolved (17 function) / 0 added**, all 19 references already satisfied → **774 → 757** stub names, **653 → 636** function, **121 → 121** storage, all three exactly as predicted. **Prediction: `stub_hit=file_lock_init`, caller key `0x8003B024`; measured exactly that**, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. This is the step 413's prediction was for: same name, same key, one run later, with the guarded site that pre-empted it now retired. **Two readings made it, and 413 had only one of them.** First, *both* lists of the walk for every function on the path (defect 128 fixed at the point of use): the only stubs they name are the four `mac_cred_label_*` this step retires. Second, **the `zalloc` path, which is new territory** — `mac_cred_label_init` → `mac_cred_label_alloc` → `mac_labelzone_alloc` → `zalloc` is the first real `zalloc` this boot would run: `MAC_WAITOK` is defined **0** (`security/mac_policy.h:6960`), so the `tst r1,#1 / bne` at `mac_labelzone_alloc+0x18` falls through to `zalloc`, and the tool's guarded chain names `OSBacktrace`, `btlog_add_entry` and `trace_backtrace` as stubs "1 guard further". **Those were closed by a source reading, not a walk** — they sit behind `__improbable(DO_LOGGING(zone))` and the `#if ZONE_DEBUG` block, and a zone's `zlog_btlog` is NULL unless `Z_LOGGING` is set, which `mac_label.c:43` does not set (only `Z_EXPAND`, `Z_EXHAUST`, `Z_CALLERACCT`). And the MAC machinery is bounded by there being **no policy loaded**: `mac_policy_init` sets `mac_policy_list.numloaded = 0` and nothing here is a kext, so no `mpo_*` callback is reachable. Every call target of all seventeen resolved names was checked against the stub list (`mac_labelzone_alloc`, `mac_policy_list_conditional_busy`, `mac_policy_list_unbusy`, `mac_error_select`, `mac_externalize`, `posix_cred_get`, `bcmp` — all real). Layout: text **1855296**, image **1977244**, `.bss` **0x801E2BC0 .. 0x8021FED8**, args **0x80221000**, headroom **1966376**, payload sha256 `f9a55c88b07b1842e48e90cae34a8dfc7146a69ce7153bec6e216b632f12d903`, run markers agreeing (`image_bytes=0x001E2B9C`, `checksum=0x9043EEC1`). Frontier: `file_lock_init` from `bsd/kern/kern_descrip.c`. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301808 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-415](experiment-415-kern-descrip-retires-file-lock-init-and-the-frontier-moves-to-bufinit-by-a-tail-call.md) | **`bsd/kern/kern_descrip.c` — `file_lock_init` retires, and the frontier moves to `bufinit` by a tail call** — one object, `bsd_kern_kern_descrip.o`: **18 resolved (18 function) / 60 added (59 function, 1 storage)**, the largest creation since 406 → **757 → 799** stub names, **636 → 677** function, **121 → 122** storage, all three exactly as predicted. **Prediction: `stub_hit=bufinit`, caller key `0x8003B1B0`; measured exactly that**, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. The reading: `file_lock_init` is 0x5C bytes of four real lock calls; past it `mac_cred_label_associate_kernel`, `lck_mtx_init` ×2, `chgproccnt`, `kmem_suballoc` are all real with **both lists** walked clean (defect 128 as a standing rule), and then `bsd_bufferinit` — real, and **whose walk stops one level down at `bufinit`**, because its **last instruction is a tail call** (`8003a9d0: b 80199940 <bufinit>`). **That is why the caller key is not a site inside `bsd_bufferinit`**: the `pop {fp, lr}` two instructions earlier restores *its* caller's `lr`, so the stub is entered holding `bsd_init`'s return address — the predicted `0x8003B1B0` is the instruction after `bl <bsd_bufferinit>` at `0x8003B1AC`. **A tail-called stub reports its grandparent's call site, and this is the first step in the walk where that happened.** Falsifiers named in advance and all silent, including the one the prediction turned on: a stop at `ubc_init` (`+0x7C4`, key `0x8003B1B8`), which would mean `bsd_bufferinit` had returned instead. Layout: text **1884768**, image **2010252**, `.bss` **0x801EACC0 .. 0x80228058**, args **0x8022A000**, headroom **1933224**, payload sha256 `5a04839c8e55dfc10283336b7fb606d5f492e96daafb89ae841d6638f4485e5d`, run markers agreeing (`image_bytes=0x001EAC8C`, `checksum=0x90402051`). Frontier: `bufinit` from **`bsd/vfs/vfs_bio.c`** — the buffer cache's own init, the first stub of the filesystem layer proper. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301801 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-416](experiment-416-vfs-bio-retires-bufinit-and-the-frontier-moves-to-microuptime-inside-bufinits-own-loop.md) | **`bsd/vfs/vfs_bio.c` — `bufinit` retires, and the frontier moves to `microuptime` inside `bufinit`'s own header loop** — one object, `bsd_vfs_vfs_bio.o`: **21 resolved (21 function) / 15 added (15 function)** → **799 → 793** stub names, **677 → 671** function, **122 → 122** storage, all three exactly as predicted. **Prediction: `stub_hit=microuptime`, caller key = the linked address of `bufinit + 0x158`; measured exactly that** — `xnu_entry_stub_caller_v=0x801A2A78` with `bufinit` linked at `0x801A2920` — `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. `bufinit` is 0x46C bytes and this step has 342/407's shape — **a stop it creates**: its call sites in address order are `hashinit` (+0x7C, real since 410), `bzero`, **`microuptime` (+0x154, a stub, record 340)**, `panic` ×6, the four `lck_*_init`, **`cluster_init` (+0x398, the stub this step creates)**, … — two candidate stops **0x244 bytes apart in the same function**, and which one is reached turns on whether `for (i = 0; i < max_nbuf_headers; i++)` executes (`buf_timestamp()` is `static inline`, so its `microuptime` call lands inside `bufinit`; the guard is `cmp r0,#1 / blt`). **It is not zero, and the evidence was the previous run's own success**: `bsd_startupearly()` runs at the head of `bsd_bufferinit` before the tail call and sets `max_nbuf_headers = atop_kernel(sane_size/50)` (≤16384, ≥`CONFIG_MIN_NBUF`), and both `kmem_suballoc` failure paths panic — neither fired. Both walk lists read after the link (standing rule since 413): straight-line answer `microuptime`, guarded entries all panic paths. Falsifiers named in advance and all silent, including the one that mattered: a stop at `cluster_init`. Layout: text **1909600**, image **2026804**, `.bss` **0x801EED40 .. 0x8022C218**, args **0x8022E000**, headroom **1916392**, payload sha256 `df0b53a1bb15749d08615702d51bfecb9f2d9846ea16e0476891bc88106a003e`, run markers agreeing (`image_bytes=0x001EED34`, `checksum=0x90402229`). Frontier: `microuptime` (record 340) from **`bsd/kern/kern_time.c`** — the first stub reached inside a function the *previous* step made real rather than in `bsd_init`'s own statement list. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301805 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-417](experiment-417-kern-time-retires-microuptime-and-the-frontier-collects-the-stub-416-created.md) | **`bsd/kern/kern_time.c` — `microuptime` retires, and the frontier collects the stub 416 created** — one object, `bsd_kern_kern_time.o`: **10 resolved (10 function) / 1 added** (`net_uptime2timeval`) → **793 → 784** stub names, **671 → 662** function, **122 → 122** storage, all three exactly as predicted. **Prediction: `stub_hit=cluster_init`, caller key = the linked address of `bufinit + 0x39C`; measured exactly that** — `xnu_entry_stub_caller_v=0x801A2CBC` with `bufinit` at `0x801A2920` — `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. `microuptime` is 0x2C bytes with **one call**, `clock_get_system_microtime` (real, `osfmk/arm/rtclock.o`), so the frame returns into `bufinit`'s loop; and asked precisely, **the only stub call left inside `bufinit` is `cluster_init` at `+0x398`** — the stub 416 itself introduced, everything else in the function being real (the four `lck_*_init`, the inlined `binsheadfree`/`binshash`) or a `panic` that does not fire. **Markers note, recorded as defect 130:** this run's `image_bytes` (`0x001EED34`), `bss_start`/`_end` and `checksum` are **byte-identical to 416's** — `image_bytes` is derived from where `.bss` starts, which moves in the linker's `2**14` `.data` alignment steps rather than with the code (417 grew `.text` by 3940 bytes inside the same 0x4000 window), and the checksum XORs the *result record*'s fields, carrying no image bytes at all. **What confirmed which image ran is the stub name and key** — a site 416's run never reached. Layout: text **1913540**, payload sha256 `43db0c5a681fa7282175d3159eb34790f6464c6e32311363902855db9066b1d8`. Frontier: `cluster_init` from **`bsd/vfs/vfs_cluster.c`**, with its sibling `cluster_bp` (created as a stub by 416) still one. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:`, no `panic`, log 301806 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-418](experiment-418-vfs-cluster-retires-cluster-init-and-the-stop-is-the-buffer-clean-thread.md) | **`bsd/vfs/vfs_cluster.c` — `cluster_init` retires, and the stop is the buffer-clean thread 415's own step started** — one object, `bsd_vfs_vfs_cluster.o`: **3 resolved (2 function, 1 storage) / 12 added (12 function, 0 storage)** → **784 → 793** stub names, **662 → 672** function, **122 → 121** storage — the storage column falls for the first time in this stretch because a `D` stand-in (`speculative_reads_disabled`) is retired; predicted before the build and confirmed *after* it by arithmetic on 419's measured figures (787/667/120 plus 419's measured 6-resolved/1-added gives back 793/672/121), since the 418 artifacts were replaced in `out/` and this doc does not invent the `.text`/image numbers it did not capture. **Prediction: `stub_hit=ubc_init`, caller key `0x8003B1B8`; measured `stub_hit=msleep0` at `xnu_entry_stub_caller_v=0x801A590C`** — which is `bcleanbuf_thread + 0x94`, with `bcleanbuf_thread` at `0x801A5878`, so **the report came from a thread other than the boot thread**. Nothing on the predicted path is a stub: `cluster_init` is 0xD4 bytes of six real calls, and `bufinit` has nothing left to stop on (`_consume_printf_args`, `zinit` ×2, `zone_change` ×2, `kernel_thread_start`, `thread_deallocate`, `vm_set_buffer_cleanup_callout` — all real, both walk lists read for each). What the run reached is the thread `bufinit` starts at `+0x438`, whose loop blocks on an empty laundry queue through `msleep0(&bufqueues[BQ_LAUNDRY], buf_mtxp, PRIBIO|PDROP, "blaundry", 0, …)`. **The rule this step adds: "real" is not "returns promptly"** — the pre-build reading called `IOKitInitializeTime` (the statement between `bufinit`'s return and `ubc_init`, at `0x8003B1B0`) real and treated it as a pass-through; 419 shows it blocks the boot thread, and indefinitely. Falsifiers named in advance and all silent. `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, log 301801 bytes ending `No errors detected`, device back on Android on its own |
| stage90 | [experiment-419](experiment-419-kern-synch-retires-msleep0-and-the-boot-thread-waits-thirty-seconds-for-iortc.md) | **`bsd/kern/kern_synch.c` — `msleep0` retires, and the run reports nothing at all: the boot thread is found blocked on an `IORTC` this environment never publishes** — one object, `bsd_kern_kern_synch.o`: **7 resolved (6 function, 1 storage) / 1 added (1 function)** — `msleep0`, `msleep`, `tsleep`, `wakeup`, `resetpriority`, `compute_averunnable` and the `D 0x10` stand-in `averunnable` out, `CURSIG` in — → **793 → 787** stub names, **672 → 667** function, **121 → 120** storage, all three exactly as predicted. **Prediction `stub_hit=ubc_init`, caller key `0x8003B1B8`; measured: no stub was hit at all.** The log is 294735 bytes / 3932 lines and ends at `stage90 xnu_entry: jumping to XNU's _start` then `No errors detected`, **`grep -c 'real XNU entry'` = 0** — the teardown never ran, so there is no `stub_hit`, no `xnu_entry_stub_caller_v`, no `abort_entries`, no `exception:` and no `panic`; the link *did* take effect (787/667/120 exactly as predicted, `msleep0`/`tsleep`/`wakeup` retired, `CURSIG` a stub, payload sha256 changed, `image_bytes` `0x001FADC0 → 0x001FADD0`). With `msleep0` real the clean thread blocks for good (empty laundry queue, `hint = 0`), and the boot thread never resumes: `thread_deallocate` returns on its `refcount-1 > 0` path, `vm_set_buffer_cleanup_callout` is a single CAS, and the one statement between them and `ubc_init` is `IOKitInitializeTime` (`iokit/Kernel/IOStartIOKit.cpp:67`) → `IOService::waitForService(IOService::resourceMatching("IORTC"), &t)` with `t = 30 s` → `IORecursiveLockSleepDeadline` → `lck_mtx_sleep_deadline` → `assert_wait_deadline` + `thread_block`. **The match fails because `IOResources::matchPropertyTable` requires the matched service to carry an `IORTC` property and nothing publishes one** — the only `IOService` this environment creates is `IOResources` (`lastkernelconstructor.c` → `iokit_post_constructor_init` → `IOService::initialize()` → `IOResources::resources()`), and the API that would publish it is `IOService::publishResource`, the same one the platform expert uses for `IONVRAM` (`IOPlatformExpert.cpp:1209`), `IOKit` (`IOService.cpp:1212`) and `IOBSD` (`IOKitBSDInit.cpp:95`). **And the wait cannot end even so: this environment has no timer** — `ml_set_decrementer` (disassembled at `0x8000D8C8` in this image) calls the per-CPU `cpu_set_decrementer_func` if non-NULL and otherwise takes the `!__ARM_TIME__` branch, which only stores to per-CPU memory and touches no timer register; that pointer is NULL because it comes from `rtclock_timebase_func.tbd_set_decrementer`, assigned only by `ml_init_timebase`, whose one caller in the tree is `pe_identify_machine` — real and linked here at `0x80005B88`, but a chain of `strcmp(gPESoCDeviceType, "t8000-io")`-style Apple board tests that `return 0` for MSM8974. The net cannot cover it either: the bark/bite registers are 20 bits at 32765 Hz, so the bark tops out at 29 s after the vendor's 3 s gap (this run armed bark 25.0 s / bite 28.0 s, `0x000C7FB5` / `0x000DFFAC`). **The two readings of the silence are therefore one, and the frontier is the platform expert's obligations in one piece** — the `IORTC` resource, the timebase/decrementer functions, and 405's `IOCPUInterruptController`. Layout: stub names 787, `.text` **1953024**, image **2076112** (0x1FADD0), `.bss` **0x801FAE00 .. 0x80238358**, args **0x8023A000**, tree 0x80600000 (len 0x7358), headroom **1866920**, payload sha256 `11c00602c092ae38f20f753899f0359bde585687b837e9766fe60e3e7c92bd03`. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `checks=5`/`failures=0`, hardware watchdog armed and counting (`hw_watchdog_counter_running=0x00000001`), device back on Android on its own |
| stage90 | [experiment-420](experiment-420-msm8974-platform-expert-publishes-iortc-and-the-boot-thread-reaches-ubc-init.md) | **the platform expert publishes `IORTC`, and the boot thread reaches `ubc_init`** — no object linked and no symbol added: **one statement, four instructions / sixteen bytes**, `IOService::publishResource( "IORTC" )` at the end of `MSM8974PlatformExpert::start`. **0 resolved / 0 added — 787 / 667 / 120, the same three numbers as 419** — while the payload sha256 did change (`11c00602…` → `24f42358…`). **Prediction, written into the source before the build: `stub_hit=ubc_init`, caller key `0x8003B1B8`; measured exactly that** (`xnu_entry_stub_caller_v=0x8003b1b8`, `xnu_entry_stub_caller=0x8003b1b8`, whose ASCII form the report also prints as `w0=0x33303038` / `w1=0x38623162`), `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. **This is defect 130 in its cleanest form, and it was predicted in the comment before the build: every pre-jump marker is byte-identical to 419's** — `image_bytes=0x001FADD0`, `bss_start=0x801FAE00`, `bss_end=0x80238358`, `args_pa=0x8023A000`, `device_tree_pa=0x80600000`, `checksum=0x904020CD` — because the added sixteen bytes fit the `.text` section's final `ALIGN(32)` round-up (both links are `0x1DCD00`; the last input before that `ALIGN` is `.ARM.exidx` ending exactly on `0x801DCD00`, so the previous link's cursor sat sixteen bytes below the boundary), and because the statement lives in an object linked at `0x801B01E4`, after every XNU object `bsd_init` can reach: **no section boundary, no `.bss` entry and no `bsd_init` address moves**. The stop is therefore the only thing that can prove the new image ran. The reading: with the resource published, the match succeeds and the wait 419 measured ends — `resourceMatching` is `serviceMatching` plus an `IOResourceMatch` entry, and **`serviceMatching` sets `IOProviderClass`** (`IOService.cpp:4801-4817`), which is the one spelling `copyExistingServices` has a dedicated fast path for (`:4282-4299`, testing `gIOResources` directly against `kIOServiceMatchedState`); `matchPassive` then reaches the one-argument virtual through `IOService::matchPropertyTable(table, &score)`'s `return( matchPropertyTable(table) )` (`:968-974`), i.e. `IOResources::matchPropertyTable`'s pure property test (`:5083`), and `publishResource(key, 0)` is what sets that property and `registerService()`s the nub (`:3532-3544`) — **the identical call Apple's own `IOService::setPlatform` makes a few statements earlier in this very function** (`publishResource("IOKit")`, `:1212`), with `gIOResources` created by `IOService::initialize()` (`:433`) from `IOLibInit()` at `IOStartIOKit.cpp:91`, before `rootNub->registerService()` at `:165` can call this `start`. **The report came from the boot thread** — the caller key is a site inside `bsd_init`, the first time since 415 — so 418/419's diagnosis is now confirmed rather than inferred: the frontier was an unmet resource, not a missing symbol. Falsifiers named in advance and all silent, two of them carrying information: `ast_taken_kernel` did not fire, so **`clock_initialize_calendar` ran clean — its first execution ever in this image**, its `PEGetUTCTimeOfDay` virtual now really dispatching because `gIOPlatform` is set (and `IOPlatformExpert::getGMTTimeOfDay` returns 0, `IOPlatformExpert.cpp:333`); and `vfsinit` (`+0x7C8`, key `0x8003B1BC`) was not reached. **The timer is still owed and is now the largest open item** — this step removed exactly one wait, by supplying the resource, with no timer involved, and every later deadline in the boot is still unbounded while `cpu_set_decrementer_func` is NULL. Frontier: **`ubc_init`** (`bsd/kern/ubc_subr.c:761`, linked at `0x801B2E10`, pool object `bsd_kern_ubc_subr.o`), reached from `bsd_init + 0x7C4`. Layout: stub names **787** (667 function, 120 storage), `.text` **1953024** (0x1DCD00), image **2076112** (0x1FADD0), `.bss` **0x801FAE00 .. 0x80238358** (251224 bytes), args **0x8023A000**, tree 0x80600000 (len 0x7358), headroom **1866920**, payload sha256 `24f423581cb34703e934b5529b11f9a9c71c17b2d539f851b16abaa47565baf8`. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`, log 301802 bytes / 3978 lines ending `No errors detected`; the only net across the jump armed and counting (`hw_watchdog_counter_running=0x00000001`, `hw_watchdog_bark_after=0x000c7fb5`, `hw_watchdog_bite_after=0x000dffac`, `hw_watchdog_bite_truncated=0x00000000`) and it is what ended the run ~28 s after the jump, the dead-man PPI deliberately disarmed before it (`disarm_isenabler0 0x000C7FFF → 0x00007FFF`), device back on Android on its own |
| stage90 | [experiment-421](experiment-421-ubc-subr-retires-ubc-init-and-the-frontier-becomes-vfsinit.md) | **`bsd/kern/ubc_subr.c` — `ubc_init` retires, and the frontier becomes `vfsinit`: the walk is back on `bsd_init`'s own statement list** — one object, `bsd_kern_ubc_subr.o`: **26 resolved (26 function, 0 storage) / 20 added (19 function, 1 storage)** — `ubc_init` out with the whole `ubc_*` face around it (`UBCINFOEXISTS`, `cs_*`, `ubc_blktooff`, `ubc_create_upl_kernel`, `ubc_cs_blob_*`, `ubc_getobject`, `ubc_getsize`, `ubc_page_op`, `ubc_range_op`, `ubc_strict_uncached_IO`, `ubc_upl_*`) and the vnode/code-signing face in (`SHA384_Final`/`_Init`/`_Update`, `VNOP_MMAP`, `VNOP_MNOMAP`, `add_fsevent`, `lock_vnode_and_post`, `mac_vnode_check_signature`, `memchr`, `need_fsevent`, `vnode_getname_printable`, `vnode_iterate`, `vnode_lock_spin`, `vnode_mtime`, `vnode_pager_deallocate`, `vnode_pager_setup`, `vnode_putname_printable`, `vnode_ref_ext`, `vnode_size`) plus the **storage** stand-in `root_fs_upgrade_try` (`B 0x4`) → **787 → 781** stub names, **667 → 660** function, **120 → 121** storage, all three exactly as predicted. **Prediction: `stub_hit=vfsinit`, caller key `0x8003B1BC`; measured exactly that** at `xnu_entry_stub_caller_v=0x8003b1bc` (ASCII `w0=0x33303038` / `w1=0x63623162`), `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. `ubc_init` is four instructions of substance: `mov r0, #64` (`sizeof(struct ubc_info)`), `movw/movt r1, #640000` (`10000 * 64`), `mov r2, #8192`, `bl zinit` (0x8006DE00), `str r0, [ubc_info_zone]` (0x80239814), then **`b zone_change` (0x800709A0) — a tail call**, so 415's rule governs the prediction's shape. **Both callees are real and both have already run to completion on this machine** — `zinit`'s callers are `thread_init` (0x80009270, 0x80009298), `vm_page_module_init`, `pmap_init`, `vm_compressor_init` and `vm_map_init`, and `zone_change`'s are `thread_init` ×4 and `vm_page_module_init` ×6, every one of them early boot. **That is the experiment's reading rule: `zinit`'s guarded list names `lck_mtx_init_ext` (2 guards), `lck_spin_unlock` (2), `kmem_alloc_kobject` (1) and two `panic`s — and the answer to "which guard matters" is none of them, because the walk's guarded column is a *static bound* and the image's own execution history is where the decision is made.** `zone_change(ubc_info_zone, 6, TRUE)` is `Z_NOENCRYPT` (`zalloc.h:262`), so its one guarded site (`+0x54 -> panic`) is the `default:` arm it does not reach; `ubc_info_zone` is **defined by this object** (`B`, linked at 0x80239814), not a stand-in, and had no symbol in the image before this step. The 20 added names are reached only from `ubc_info_init*`, `ubc_cs_blob_*`, `ubc_upl_*` and `ubc_page_op`, which nothing calls yet — 342's trap checked and empty. **This is the first stub stop on `bsd_init`'s own line since 415** (416–419 stopped inside a function, or nowhere; 420 was not a stub stop at all but the removal of an unbounded wait). Falsifiers named in advance and all silent, including `proc_uuid_policy_init` (`+0x7CC`, key `0x8003B1C0`) and the three nameable panics. Layout: text **1967232** (0x1E0480), image **2092828** (0x1FEF1C), `.data` **0x801E4000 .. 0x801FECF0**, `.bss` **0x801FEF40 .. 0x8023C558** (251416 bytes), args **0x8023E000**, tree 0x80600000, headroom **1850024**, payload sha256 `9d64c77052ab39e7406a8932a23b43990909a9bd515fa61235b3a3d560478c87`. **Every run marker differs from 420's** (`image_bytes=0x001FEF1C`, `bss_start=0x801FEF40`, `checksum=0x90402541`), so defect 130 does not arise: this link moved `.bss` and the image's own identity is visible again. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`, log 301801 bytes / 3978 lines ending `No errors detected`; the watchdog armed and counting (`hw_watchdog_bark_after=0x000c7fb5`, `hw_watchdog_bite_after=0x000dffac`, `hw_watchdog_bite_truncated=0x00000000`) and it is what ended the run, the dead-man PPI deliberately disarmed before it, device back on Android on its own |
| stage90 | [experiment-422](experiment-422-vfs-init-retires-vfsinit-and-the-step-creates-its-own-stop-at-vntblinit.md) | **`bsd/vfs/vfs_init.c` — `vfsinit` retires, and the step creates its own stop: `vntblinit`, the first of four consecutive new stubs** — one object, `bsd_vfs_vfs_init.o`: **3 resolved (2 function, 1 storage) / 18 added (10 function, 8 storage)** — `vfsinit` and `vn_default_error` out with the **storage** stand-in `dead_mountp` (`B 0x4`); `decmpfs_init`, `dqinit`, `mac_mount_label_associate`, `mac_mount_label_init`, `nchinit`, `nspace_handler_init`, `vfs_event_init`, `vfs_sysctl_node`, `vnode_authorize_init`, `vntblinit` and **eight** storage stand-ins (`maxvfsslots` `R 4`, `maxvfstypenum` `D 4`, `numregistered_fses` `B 4`, `numused_vfsslots` `B 4`, `sysctl__vfs_children` `B 4`, `vfs_op_descs` `D f8`, `vfs_opv_descs` `D 18`, `vfsconf` `D 4`) in → **781 → 796** stub names, **660 → 668** function, **121 → 128** storage, all three exactly as predicted. **Prediction: `stub_hit=vntblinit`, caller key = the linked address of `vfsinit + 0x230`; measured exactly that** — `xnu_entry_stub_caller_v=0x801b3ab0` with `vfsinit` at `0x801B3880`, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. `vfsinit` begins with the whole of XNU's VFS lock furniture (`lck_grp_attr_alloc_init`, `lck_grp_alloc_init`, `lck_attr_alloc_init`, `lck_spin_alloc_init`, `lck_mtx_alloc_init` ×4, across `vnode_list`, `vnode`, `trigger_vnode`, `fd_vnode_data`, `fs conf`, `mount list`, `mount`, `sync thread`) — **every one already real**, since they are the calls 415's `file_lock_init` and 419's `msleep0` went through — and the first name this image does not provide is `vntblinit` at `+0x22C`, one call after the last `lck_mtx_alloc_init`: `+0x22c bl vntblinit`, `+0x230 bl vfs_event_init`, `+0x234 bl nchinit`, `+0x238 bl nspace_handler_init` — **four new stubs four bytes apart**, so `vfs_opv_init` (`+0x2C4`) and the `blx r1` (`+0x324`) behind the `vfsconf` loop are unreachable until one of them returns, and a stand-in never does. **The forward reading after the link agrees, and its guarded column is the second confirmation of 421's rule in two steps:** it names `lck_mtx_lock_spin_always`, `thread_wakeup_prim`, `lck_mtx_unlock`, `assert_wait_timeout`, `thread_block`, `try_alloc_from_zone`, `lck_mtx_sleep` and `panic` ×3 (all from `zalloc_internal`, the path the `lck_*` allocations take) plus 55 more — and none of them is the stop. **A defect this step caught on itself before the device ran: an object disassembly's offsets are relative to the object, not to the function.** The first draft of the comment quoted `+0x49C` for `bl vntblinit` — the object's own offset — while `vfsinit` begins at object offset `0x270`, so the function-relative site is `+0x22C`; the linked image confirms it to the byte, and had it not been caught the published key would have been wrong by `0x270` and a hit would have read as a miss. Also named: `maxvfsslots` (a zeroed `R 4`) is the `vfsconf` loop's bound, so that loop is inert while the stand-in stands — behind the stop, not in front of it. Layout: text **1970944** (0x1E1300), image **2092948** (0x1FEF94), `.data` **0x801E4000 .. 0x801FED68**, `.bss` **0x801FEFC0 .. 0x8023C898** (252120 bytes), args **0x8023E000**, tree 0x80600000, headroom **1849192**, payload sha256 `bebb880951fe234e249e55a86f0fd1800fb3564f76b4dee8f11ac8a09a4753c87`; every run marker differs from 421's (`image_bytes=0x001FEF94`, `checksum=0x90402889`). Frontier: **`vntblinit`** (`0x801B7F18`, `bsd/vfs/vfs_subr.c:327`), whose pool definer `bsd_vfs_vfs_subr.o` is the next object. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`, log 301803 bytes / 3978 lines ending `No errors detected`; the watchdog armed and counting (`hw_watchdog_bark_after=0x000c7fb5`, `hw_watchdog_bite_after=0x000dffac`, `hw_watchdog_bite_truncated=0x00000000`) and it is what ended the run, the dead-man PPI deliberately disarmed before it, device back on Android on its own |
| stage90 | [experiment-423](experiment-423-vfs-subr-retires-vntblinit-and-the-frontier-becomes-nchinit.md) | **`bsd/vfs/vfs_subr.c` — `vntblinit` retires, and the frontier becomes `nchinit`, the second of 422's four consecutive new stubs** — one object, `bsd_vfs_vfs_subr.o`: **35 resolved (33 function, 2 storage) / 64 added (60 function, 4 storage)**, the largest step since 406. The whole `vnode_*` face that 418–422 had been creating piecemeal goes real at once — `vntblinit`, `vfs_event_init`, `vnode_authorize_init`, `vfs_sysctl_node`, **`vfs_mountroot`**, `vcount`, `check_mountedon`, `lock_vnode_and_post`, `vfs_mountedon`, `vn_getcdhash`, `vn_getpath`, `vn_path_package_check`, `vnode_authorize`, `vnode_close`, `vnode_getwithref`, `vnode_getwithvid`, `vnode_iterate`, `vnode_lock`, `vnode_lock_spin`, `vnode_lookup`, `vnode_mtime`, `vnode_open`, `vnode_put`, `vnode_ref`, `vnode_ref_ext`, `vnode_rele`, `vnode_setsize`, `vnode_size`, `vnode_startwrite`, `vnode_unlock`, `vnode_waitforwrites`, `vnode_writedone`, plus the storage stand-ins `fs_filtops` (`R 0x28`) and `mountlist` (`B 0x8`) → **796 → 825** stub names, **668 → 695** function, **128 → 130** storage, all three exactly as predicted. **Prediction: `stub_hit=nchinit`, caller key = the linked address of `vfsinit + 0x238`; measured exactly that** — `xnu_entry_stub_caller_v=0x801b3ab8` with `vfsinit` at `0x801B3880` (ASCII `w0=0x62313038` / `w1=0x38626133`), `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`. **`kernel_thread_start` is the call that made 418 a miss, so this step walks the thread it starts too:** `vntblinit`'s three calls (`microuptime` object `+0x60`, `kernel_thread_start` `+0xA8`, `thread_deallocate` `+0xB0`) are all satisfied, and `async_work_continue` (`vfs_subr.c:4062`) is an empty-queue blocking loop whose four calls — `vnode_list_lock`, `assert_wait`, `vnode_list_unlock`, `thread_block` — are all satisfied too (`vnode_list_lock`/`_unlock` are **defined by `bsd_vfs_vfs_init.o`, the object linked at 422**); `process_vp` and its `panic("found VBAD vp ...")` are behind a non-empty queue, and `vntblinit` itself just ran `TAILQ_INIT(&vnode_async_work_list)`, so the queue is empty. `vfs_event_init`'s body is three real calls (`klist_init` — `bsd_kern_kern_event.o` has been linked since 409 — plus `lck_grp_alloc_init` and `lck_mtx_alloc_init`). `nchinit` is defined by `bsd_vfs_vfs_cache.o` and its neighbour `nspace_handler_init` by `bsd_vfs_vfs_syscalls.o`; neither is linked, so both remain stand-ins. **The forward readings agree as a pair:** `walk from vfsinit` answers `nchinit` on the straight-line path while both functions this step makes real answer *no stub on the straight-line path*. Falsifiers named in advance and all silent, including `nspace_handler_init` (`+0x23C`) and `vfs_opv_init` (`+0x2C4`). Layout: text **2019520** (0x1ED0C0), image **2142992** (0x20B310), `.data` **0x801F0000 .. 0x8020B0B8**, `.bss` **0x8020B340 .. 0x80248D58** (252440 bytes), args **0x8024A000**, tree 0x80600000, headroom **1798824**, payload sha256 `02d2c9fd8c5741f49de4b5e820b6e8b4fe4ca438af7d833c4eeefbf3b70493bf`; every run marker differs from 422's (`image_bytes=0x0020B310`, `checksum=0x90402D4D`). Frontier: **`nchinit`** (0x801C0F70), whose pool definer `bsd_vfs_vfs_cache.o` is the next object. Safety: non-persistent `fastboot boot` only, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:`, no `panic`, log 301801 bytes / 3978 lines ending `No errors detected`; the watchdog armed and counting and it is what ended the run, the dead-man PPI deliberately disarmed before it, device back on Android on its own |
| stage90 | [experiment-424](experiment-424-vfs-cache-retires-nchinit-and-the-frontier-becomes-nspace-handler-init.md) | **`bsd/vfs/vfs_cache.c` — `nchinit` retires, and the frontier becomes `nspace_handler_init`: the third of 422's four consecutive new stubs** — one object, `bsd_vfs_vfs_cache.o`: **12 resolved (11 function, 1 storage) / 4 added (3 function, 1 storage)**, so the name count *falls* — **825 -> 817** undefined, **695 -> 687** function, **130 -> 130** storage — because the object resolves more `cache_*`/`vnode_*` names than its own body references. The step's reading is the object's own call list rather than the walk's: the linked image still carries `nchinit` as a stand-in, so `xnu_entry_callwalk.py --root nchinit` answers `nchinit STUB` and an *empty* guarded list, while the object shows eight calls, all satisfied (`hashinit` x2, the seven `lck_*` allocators), and its two remaining callees `init_crc32`/`init_string_table` are **`static` and inlined**, their only trace being the `.bss` they fill — so there is no fourth call site hiding behind them, and the object's six other stub references (`vfs_context_*`, `vnode_getattr`, `vnode_isdir`, `vnode_vid`) belong to name-cache *lookup* functions `nchinit` does not call. Prediction written before the build and **measured exactly**: `stub_hit=nspace_handler_init`, `xnu_entry_stub_caller_v=0x801b3abc` = `vfsinit + 0x23C`. **And the image was already within 16 KB of the alias window 425 then broke**: the high-alias probe word sat at PA `0x002FC0B4` and the candidate table's window ended at `0x00300000` — `0x3F4C` bytes, which the next step spent. |
| stage90 | [experiment-425](experiment-425-the-thirteen-object-batch-and-the-preflight-that-refused-it.md) | **thirteen objects in one link — the first *batch* step, and the loader preflight that refused it: the image had outgrown the candidate table's image-alias window** — the first step in this project whose prediction is written *after* the build and before the device run, because a batch changes the stub set by construction and the only honest way to say where a batched link stops is to read the linked image (`walk from bsd_init`). The batch retires fourteen init bodies (`nspace_handler_init`, `proc_uuid_policy_init`, `mcache_init`, `mbinit`, `net_str_id_init`, `eventhandler_init`, `aio_init`, `pipeinit`, `pshm_lock_init`, `pshm_cache_init`, `psem_lock_init`, `psem_cache_init`, `pthread_init`, `select_waitq_init`) and creates the whole `mbuf`/`m_tag`/`mcl_*` KPI face and the VFS syscall surface behind them: net **817 -> 906** names, **687 -> 777** function, **130 -> 129** storage. Its first draft was twelve objects and left the frontier *unmoved*, because `nspace_handler_init` is called from inside `vfsinit` and blocks the other twelve — **`bsd_init`'s line is a chain of nested calls, not a flat list** — so batch membership is ordered by the call graph. The device run was then **refused before the jump**: `loader_xnu_arm_vm_init_full_pmap_failure_mask=0x80000040` = `FAIL_HIGH_ALIAS` with a derived `FAIL_SAFETY_BOUNDARY`, `high_alias_verified=0`. Root cause: **one alias window with two limits, in two files** — the full-pmap candidate table stopped its image alias at its own `0xc0300000` RAM-console alias (3 MB) while `mmu.c`'s identity table stopped at the GIC alias `0xc0400000` (4 MB), and the batch moved `stage90_full_pmap_probe_word` from PA `0x002FC0B4` to `0x0032C0B4`, so `alias_probe = 0xc032c0b4` resolved through the RAM-console alias to PA `0xde52c0b4` and read ramoops content instead of `0x11223344`. Nothing faulted — the identity table was never at risk (its window is 4 MB and complete), the *dry-run* refused, which is the preflight working. Fixed in the same step by giving the window one definition: `STAGE90_IMAGE_ALIAS_WINDOW` (64 MB) in `stage90.h`, both device aliases moved above it, `#error` guards, and a post-link `build.sh` gate comparing `__stage90_image_end` against the limit. The corrected run measured `stub_hit=dqinit` at `vfsinit + 0x4A0` (key `0x801b3d20`) — **not the predicted `nwk_wq_init`**, because `vfsinit`'s post-loop block was classified cold by the walk (rule 426, the loop-exit blind spot: the site is structurally identical to the never-executed `zfree+0x5f8`, so the tool now *reports* guarded stub-callee sites instead of trying to classify them). **The frontier is now `dqinit`** (`bsd_vfs_vfs_quota.o`, 1 resolved / 0 added on its own — the next step, whose prediction is written before its build again). |
| stage90 | [experiment-427](experiment-427-vfs-quota-retires-dqinit-and-the-frontier-becomes-mac-mount-label-init.md) | **`bsd/vfs/vfs_quota.c` — `dqinit` retires, and the frontier becomes `mac_mount_label_init`: the first one-object step after the batch, and the first step whose net count is zero** — one object, `bsd_vfs_vfs_quota.o`: **2 resolved (2 function, 0 storage) / 2 added (2 function, 0 storage)** — `dqinit` and `munge_dqblk` out, `VNOP_READ`/`VNOP_WRITE` in as new stubs this object's own vnode ops reference — so **906 -> 906** undefined / **777 -> 777** function / **129 -> 129** storage, all exact. Prediction written before the build and **hit to the byte**: `stub_hit=mac_mount_label_init`, `xnu_entry_stub_caller_v=0x801b3e1c` = `vfsinit + 0x59C` (`vfsinit` did not move; `mac_mount_label_init`'s stand-in moved `0x801F3060 -> 0x801F4948`, which is why the key is on the caller). **The reading is the one shape rule 426 leaves exact**: `dqinit` is `0x98` bytes with **seven calls and no branches at all** (four `lck_*_alloc_init` pairs plus `.bss` stores), `munge_dqblk` is two `bcopy`s, and the instructions between the `bl <dqinit>` at `+0x49C` and the `bl <mac_mount_label_init>` at `+0x598` contain **no conditional branch and no `b`** — so once `dqinit` is real the stop is decided entirely by the next call on `vfsinit`'s line. 342's trap checked by ownership: the two created stubs are referenced only by `dqfileclose`/`dqfileopen`/`dqget`, none of which is on `vfsinit`'s path. Also recorded: `xnu_entry_why` is the *reason* string and not the stub's name, and `xnu_entry_stub_caller_digits` spells the key back as ASCII — the same number from a second path. Next three sites on the line: `vfs_context_kernel` (`bsd_vfs_kpi_vfs.o`), `mac_mount_label_associate` (same object as this stop), `decmpfs_init` (`bsd_kern_decmpfs.o`). |
| stage90 | [experiment-428](experiment-428-mac-vfs-retires-mac-mount-label-init-and-the-frontier-becomes-vfs-context-kernel.md) | **`security/mac_vfs.c` — `mac_mount_label_init` retires with fifty-four more names, and the frontier becomes `vfs_context_kernel`** — one object, `security_mac_vfs.o`: **55 resolved (55 function, 0 storage) / 2 added** — the widest single object in this run of steps and the largest step since 423, retiring every `mac_vnode_*`/`mac_mount_*`/`mac_vnode_notify_*`/`vn_setlabel` stand-in at once — so **906 -> 853** undefined / **777 -> 724** function / **129 -> 129** storage, all exact. Prediction written before the build and **hit to the byte**: `stub_hit=vfs_context_kernel`, `xnu_entry_stub_caller_v=0x801b3e20` = `vfsinit + 0x5A0`. **Two readings this step needed rather than assumed:** its argument is a real allocation (`vfs_init.c:479` `MALLOC_ZONE(…, struct mount …)` + `bzero`, then `:505` the call), so the trailing `[r4,#2364]` store lands in a real `struct mount`; and `mac_mount_label_init`'s two **`blx r2`** indirect calls are closed by *storage* rather than by the call graph — `mac_policy_list` is real and zeroed (`security_mac_base.o`, `0x1C`-byte `.bss`), its count word at `+12` is what the loops test, and with no policy module linked they are skipped. 342's trap checked by ownership: the two created stubs are referenced only by the exec/label-setting paths. **And the frontier is not the name the object is linked for**: `mac_mount_label_associate(vfs_context_kernel(), mp)` is one source statement (`:506`) compiled as two calls in argument order, so the second argument's callee is retired by this object while the first is not — a stop at `mac_mount_label_associate` was named as a falsifier and did not happen. |
| stage90 | [experiment-429](experiment-429-kpi-vfs-retires-vfs-context-kernel-and-the-frontier-becomes-decmpfs-init.md) | **`bsd/vfs/kpi_vfs.c` — `vfs_context_kernel` retires with 108 more names, and the frontier becomes `decmpfs_init`, the last stub call left in `vfsinit`** — one object, `bsd_vfs_kpi_vfs.o`: **109 resolved (109 function, 0 storage) / 27 added (4 function, 23 storage)**, the widest step of the whole walk — **853 -> 771** undefined / **724 -> 619** function / **129 -> 152** storage, all exact. One file is this wide because `kpi_vfs.c` defines the `VFS_*`/`VNOP_*` wrappers **by hand** (98 `VNOP_` occurrences), so the whole vnode-op front door goes real at once. Prediction written before the build and **hit to the byte**: `stub_hit=decmpfs_init`, `xnu_entry_stub_caller_v=0x801b3e38` = `vfsinit + 0x5B8`. **The step's new exposure is the 23 created *storage* stand-ins** — the `vnop_*_desc` tables, `23 × 0x40 = 0x5C0` bytes of zeroed `.bss` — because unlike a function stub, **a storage stand-in is not a stop, it is a wrong value**; 342's trap asked of each: referenced only by the wrappers this same object defines, none on `vfsinit`'s line. And `vfs_context_kernel`'s safety is the run's first argument about **boot order rather than storage**: it dereferences the real `kernproc` → `proc0` (`0x2B8`-byte `struct proc`) and reads `p_ucred` at `+0x90`, which `bsd_init.c:638` assigns 97 lines before it calls `vfsinit()` at line 735. Layout: text `0x230840`, image `0x24FF04`, `.bss` `0x8024FF40 .. 0x80291A58`, args `0x80293000`; **checks=5 / failures=0**, `abort_entries=0`, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, no exception, no panic, `No errors detected` — the hardware watchdog the only net across the jump, and the device back on Android on its own. |
| stage90 | [experiment-430](experiment-430-decmpfs-retires-decmpfs-init-and-the-frontier-becomes-ioservicepublishresource.md) | **`bsd/kern/decmpfs.c` — `decmpfs_init` retires, the last stub call left in `vfsinit`, and the frontier becomes `IOServicePublishResource`** — one object, `bsd_kern_decmpfs.o`: **1 resolved (1 function, 0 storage) / 3 added (3 function, 0 storage)**, **771 -> 773** undefined / **619 -> 621** function / **152 -> 152** storage, all exact. **This is the first step whose caller key could not be carried forward**: every key since 422 was `<function> + <offset>` on a function linked long ago, but this stop is *inside the object being linked*, so the address had to be derived from where the linker would put that object's `.text` — the contiguous chain `0x801ED620+0x3D64`, `+0x1900`, `+0x756C`, `+0x53A0` = **`0x801FF590`**, whose 429 map is checked a fifth time here, giving `decmpfs_init` = `0x80202824` and the caller key `0x80202904` (`+0xE0`, the return address of the `bl` at `+0xDC`). **Predicted and hit to the byte on hardware** — `stub_hit=IOServicePublishResource`, `xnu_entry_stub_caller_v=0x80202904` — and the two cross-check addresses `register_decmpfs_decompressor` `0x802006C0` / `unregister_decmpfs_decompressor` `0x80200760` also landed exactly. The reading: clang inlined `register_decmpfs_decompressor` **and folded both of its tests away** (`compression_type >= CMP_MAX` and the `static inline` `registration_valid` are constants), leaving a function of eleven calls and **two conditional branches, both guards on a word this object's own `.bss` holds** (`decompressors[CMP_Type1]`, 0x3FC = 255 × 4 matching `CMP_MAX = 255`, and `decmpfs_init.done`) which the payload's `bzero` zeroes. Layout: **`.text` grew 0x36E0 and the image grew only 0x3A8** — `.data` is placed at `ALIGN` boundary `0x80234000` in both images, so the gap swallowed the whole text growth and the image delta is exactly the object's own `__DATA, __data`. The next frontier is the first in this walk whose definer is **an I/O Kit object** (`iokit_bsddev_IOKitBSDInit.o`, which holds `IOFindBSDRoot`): **the timer stops being a footnote here.** |
| stage90 | [experiment-431](experiment-431-iokitbsdinit-leaves-vfsinit-and-the-frontier-becomes-a-panic-in-pthread-init.md) | **`iokit/bsddev/IOKitBSDInit.cpp` — the walk leaves `vfsinit` for the first time, and the frontier becomes a `panic` in `pthread_init`: a stop that is not a stub** — one object, `iokit_bsddev_IOKitBSDInit.o`: **9 resolved (9 function, 0 storage) / 4 added (4 function, 0 storage)** → **773 → 768** undefined / **621 → 616** function / **152 → 152** storage, all exact, and the object's `.text` landed on **0x80202A0C** exactly as the contiguous-chain reading said (the fifth check of it, the fourth time it decided an address). Three of the nine resolved are 430's own created stubs; the other six are the BSD-device front door, **including `IOFindBSDRoot`**. **The prediction was `stub_hit=nwk_wq_init`, key `0x8003B1FC` — and it was wrong, in the most informative way available.** The run went *through* `IOServicePublishResource`, through `IOService::publishResource`, back through `decmpfs_init` and **out of `vfsinit` for the first time in this whole walk**, then down `bsd_init`'s line — ten calls ran clean — and stopped at the eleventh, `pthread_init` (`bsd_init + 0x7F4`), on a **`panic`**: `xnu_entry_undef_pc=0x8002DD88` (`DebuggerTrapWithState`'s `udf`) with the message read out of **`r9`** (the registers are the only place it survives; `debugger_panic_str` is cleared before the trap): `"pthread kernel extension not loaded (function table is NULL)."`, which occurs once in the tree, at `pthread_shims.c:275`. **Why the walk could not have said so:** both of its lists are built from the *stub* set, and the second is filtered to guarded callees that are stubs or lead to one — the panic is a guarded call to **`panic`, which is real**, so it appears in neither. The walk answers *"the first stub"*; the method read that as *"where the run stops"*, and those stopped being the same thing here. **This is a fourth kind of stop and the second kind in its most visible form:** `pthread_functions` is a real 4-byte `.bss` pointer at `0x802927C8` whose only writer is `pthread_kext_register`, whose only caller in the source tree is **`pthread.kext` — an Apple binary this tree does not contain and no object in the pool supplies**. So 432 is a new shape: **the first table the image supplies rather than links** — a Stage-owned `struct pthread_functions_s` sized from Apple's own header, registered from an `.init_array` constructor, with every unimplemented entry pointing at a stand-in that stops the run and names itself (the `pthread_init` tail branch means such a stand-in records `bsd_init + 0x7F8`). Layout: text `0x2353E0`, image `0x2542AC`, `.bss` `0x802542C0 .. 0x802961D8`, headroom `1482280`; `checks=5`/`failures=0`, no data abort, device back on Android on its own. |
| stage90 | [experiment-432](experiment-432-the-image-supplies-the-pthread-function-table-and-the-frontier-becomes-its-first-slot.md) | **`stages/stage90/xnu_supply/stage90_pthread_functions.c` — the image supplies the kernel's `struct pthread_functions_s`, and the frontier becomes its first slot: the first step in this walk whose object changes no count at all** — 431's run died on `pthread_shims.c:275`'s guard on `pthread_functions`, a real 4-byte `.bss` pointer whose only writer, `pthread_kext_register`, is called only by `pthread.kext` — an Apple binary the tarball does not contain — so **no link can move that frontier, and the only way past it is to write the value**. 432 is therefore the first step that is not an object appended to a list of Apple's but **a table this project writes**: `stages/stage90/xnu_supply/stage90_pthread_functions.c`, compiled by `tools/build_xnu_arm_kernel.sh`'s platform block (a third list there, added for this step) and linked by `build_entry.sh` before `STAGE90_PLATFORM_EXPERT_OBJ`. Its effect is **0 resolved / 0 added** — `768 / 616 / 152` unchanged, exactly as `tools/entry_object_effect.py` predicted — because **what it supplies is a value, not a name**; its two references (`pthread_kext_register`, real since 425, and `entry_stub_hit`, defined by the entry image's own `entry_stubs.o`) are both already satisfied, which makes it **the first pool object in the walk with a reference back into the entry image** (resolved in pass 1, not stubbed — the same fact that lets `panic` be deliberately ungenerated). The layout comes from Apple's own `<sys/pthread_shims.h>` (one definition, not a mirror): `sizeof` = 508 = (1 + 39 + 87) words, `offsetof(pthread_init)` = 4, `offsetof(_pad)` = 160 — and the kernel's own compiled code agrees, `pthread_init` being `ldr r0, [r0, #4]` then `pop {r4, lr}; bx r0`. All 39 named slots (including Apple's three `__unused*` reservations) point at stand-ins that stop and name themselves, and **before registering, the constructor scans the table's 40 named words and stops reporting the word index of any NULL** — the check for a slot forgotten in the list, which C silently zeroes. Registration is from an `.init_array` constructor, which runs during `kernel_bootstrap` (`PE_init_iokit` → `StartIOKit` → `OSlibkernInit` → `OSRuntimeInitializeCPP`); the map puts its word at `0x802542A4`, the platform expert's at `0x802542A8` and `last_kernel_constructor`'s at `0x802542AC`. **Prediction written before the build and hit to the byte:** `stub_hit=stage90_pthread_functions.pthread_init`, `xnu_entry_stub_caller_v=0x8003b1e8` = `bsd_init + 0x7F8` — the return address of the `bl <pthread_init>` at `bsd_init + 0x7F4`, because `pthread_init` reaches the slot through a tail branch, so the stand-in is entered with `pthread_init`'s own caller in `lr`; the call site is then confirmed against the linked image (`8003b1e4: bl <pthread_init>`, `8003b1e8: bl <pshm_cache_init>`, `8003b1f8: bl <nwk_wq_init>`), the key is spelled back as ASCII by `_w0`/`_w1` (`"8003"`, `"b1e8"`), three caller records agree, and there is **no panic and no exception** — 431's `panic` is gone, replaced by an ordinary `stub_hit=` report with a key, which is what a value-supplied frontier looks like in this instrument. Layout: text `0x235FA0`, image `0x2542B0` — **four bytes**, this object's `.init_array` word, because the `.data` boundary at `0x80238000` swallowed the whole 0xBC0 of text growth (the object's own `.text` 0x2CC + `.rodata` 0x1FC + `.rodata.str1.1` 0x6F1 = 0xBB9, plus a 7-byte fill term) and the object's `.text` landed on `0x80203D48` = `0x80202A0C + 0x133C`, the sixth consecutive check of the contiguous-chain reading and the first time it has placed an object that is not Apple's; `.bss` start, end and size all unmoved (the object's 4 bytes went into the run's own alignment padding at `0x80292C60`). `checks=5` / `failures=0`, `abort_entries=0`, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, the hardware watchdog the only net across the jump, and the device back on Android on its own. **The next stop is `nwk_wq_init`** (`bsd_net_nwk_wq.o`, `bsd_init + 0x808`, key `0x8003B1FC`) — 425's prediction, four steps old, and still a plain missing symbol — and beside it the table's own remaining slots, which no link can retire either. |
| stage90 | [experiment-433](experiment-433-one-slot-gets-a-body-and-the-frontier-leaves-the-table.md) | **`stage90_pthread_functions.c`'s `pthread_init` slot gets a body, and the step that links *nothing* retires five of `bsd_init`'s calls** — **no object is added to `LINK_OBJS`**, and the three counts stand at **768 / 616 / 152** for the third step running: the whole change is one word of `stages/stage90/xnu_supply/stage90_pthread_functions.c`'s table, whose `.pthread_init` slot stops pointing at a stand-in. **And this is where 432's own write-up turns out to be wrong**, by one call site: it said "433 is shaped like every step from 425 to 431: link the object that defines `nwk_wq_init`", but 432's run had stopped at `stage90_pthread_functions.pthread_init` at key `0x8003B1E8`, and `bsd_init`'s `bl <pthread_init>` at `+0x7F4` is four `bl`s *before* `bl <nwk_wq_init>` at `+0x808` — so the run stops at the slot every time and **no object appended to `LINK_OBJS` could have moved the boot one instruction further**. The body records the kernel's own pointer and stops if it is not this table: `entry_kv("xnu_entry_stage90_pthread_functions_ptr", pthread_functions)`, then `entry_stub_hit("stage90_pthread_functions.not_registered", 0u)` when the pointer differs — it does *not* dereference it (a NULL must be a stop that names itself, not a `first_dfar`), and the disassembly shows `ldr r1, [r4]` / `ldr r0, [r4]` / `cmp r0, r1` / `popeq {r4, pc}`, with the failure path a *tail branch* so it reports the same call site. **Prediction written before the build and hit to the byte: `stub_hit=nwk_wq_init`, `xnu_entry_stub_caller=0x8003b1fc` = `bsd_init + 0x80C`** — and the value the body recorded, `xnu_entry_stage90_pthread_functions_ptr=0x80231228`, is the table's own linked address (`nm` says `80231228 t stage90_pthread_functions`), so the registration reached the kernel by two independent routes: the image's read of the kernel's `.bss` pointer and the host's symbol table. **The positive evidence is the negative one**: the run contains neither 431's `panic` nor 432's `stage90_pthread_functions.pthread_init` line, and it does contain the record only the new body can write. Layout: text `0x235FA0` → **`0x236000`** (+0x60), image **`0x2542B0` unmoved for the second step**, `.bss` `0x802542c0..0x802961d8` unmoved, `.init_array` `0x94` unmoved with this object's word still at `0x802542A4`; the object's own inputs moved +0x5E (`.text` 0x2CC → 0x300, +0x34, a 0x44-byte body for a 0x10-byte stand-in; `.rodata.str1.1` 0x6F1 → 0x71B, the two new strings in and the retired one out) against a measured +0x60, so **the fill term is 2 bytes, the smallest yet**, and the string term is the one that does not close to the byte because `.rodata.str1.1` is *mergeable* and an object's size for it is an upper bound. Every symbol after the object moved by exactly +0x34 (the stub region's `nwk_wq_init` `0x80206278` → `0x802062AC`) and nothing before it did (`bsd_init` at `0x8003A9F0` in both images), which is what makes the two runs' caller keys comparable at all. **`xnu_entry_checksum=0x9040E1ED` is unchanged from 432 and that is correct, not a defect-130 case**: it XORs `struct stage90_xnu_entry_result`'s words, every one of which is identical because this object is linked after `bsd_init` and moved no counter; the evidence that a different image ran is `xnu_entry_kv_written` 0x77 → **0x90** and `xnu_entry_kv_words`, which differs from 432's in **43 of its 661 words, every one by exactly +0x34**, and which **matches the 433 ELF byte for byte at `0x80002024`**. (432's write-up called that dump "the vector table's first 2644 bytes"; `entry_stubs.c:1116` passes `entry_kv` as its base, so the region is the report writer's own code and the vector trampolines at `0x80001F00` are not in it — defect 140.) 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:` and no `panic:`, the hardware watchdog the only net across the jump, `kv_dropped=0`, log 301860 bytes / 3979 lines ending `No errors detected`, device back on Android on its own (`cancro`, release 10). **Next: `nwk_wq_init` (`bsd_net_nwk_wq.o`, `bsd_init + 0x808`, key `0x8003B1FC`) — a plain missing symbol for the first time in six steps, and the first stop this walk reaches *through* a table it supplies — with `dlil_init` at `+0x80C`/`0x8003B200` behind it; and the table's other 38 slots, which no link can retire either. |
| stage90 | [experiment-434](experiment-434-nwk-wq-c-the-first-stop-reached-through-a-table-and-the-step-that-starts-a-kernel-thread.md) | **`bsd/net/nwk_wq.c` — the object that retires `nwk_wq_init`, the first stop the walk reaches *through* a table the image supplies, and the step whose own object starts a kernel thread** — `bsd_net_nwk_wq.o` (manifest: `bsd/net/nwk_wq.c`), the pool's only definer of 433's stop, appended to `LINK_OBJS` between `IOKIT_BSDDEV_IOKITBSDINIT_OBJ` and `STAGE90_PTHREAD_FUNCTIONS_OBJ`. **1 resolved (1 function, 0 storage) / 0 added** → **768 → 767** undefined, **616 → 615** function, **152 → 152** storage, exact. **All 13 of its references are already satisfied *and all 13 are real*** — the second half is not what the tool answers (a generated stub satisfies a reference too), so each was checked against the linked image: `lck_grp_attr_alloc_init`, `lck_grp_alloc_init`, `lck_attr_alloc_init`, `lck_mtx_init`, `lck_mtx_lock`, `lck_mtx_unlock`, `kernel_thread_start` (0x8000b384), `panic`, `thread_deallocate`, `msleep0`, `wakeup`, `assfail`, `_FREE` — thirteen `T`s in the real region, none in the stub run at `0x8020xxxx`. **Its other 18 definitions change no count**, and that is a reading rather than an omission: six `.bss` variables (32 bytes), nine `.rodata.str1.1` strings and three more functions, none of which the image had ever referenced, so none had ever become a stand-in — the effect tool counts *references*. **The body starts a kernel thread**: `kernel_thread_start(nwk_wq_thread_func, 0, &thread)` with a failure `panic` (`.L.str.1`, `"%s: couldn't create network work queue thread"`) and `thread_deallocate` after it, and the thread it starts takes `nwk_wq_lock` and blocks in `msleep0(nwk_wq_waitch, nwk_wq_lock, 21, "nwk_wq", 0, nwk_wq_thread_cont)` — **a zero timeout, so no deadline**, which is why this step does not need the timer; it sleeps until a `wakeup` only `nwk_wq_enqueue` sends, and nothing calls that in this boot. `kernel_thread_start` is the only name here the walk had never gone through, and `xnu_entry_callwalk.py --root kernel_thread_start` answers "no stub on the straight-line path". **Prediction written before the build and hit to the byte: `stub_hit=dlil_init`, caller key `0x8003B200`** = `bsd_init + 0x810`, the return address of the `bl <dlil_init>` at `+0x80C`, the next call after `bl <nwk_wq_init>`; `dlil_init` is defined by `bsd/net/dlil.c` (`bsd_net_dlil.o`, `T dlil_init` at +0x1BEC), **which is not linked**. `xnu_entry_stage90_pthread_functions_ptr=0x802315cc` is the table's *new* linked address, so 433's body ran, compared and recorded — no `not_registered`, no panic, `abort_entries=0`, `_w0`/`_w1` spelling the key back as ASCII (`"8003"`, `"b200"`). **One row of the layout prediction is wrong and the miss is the finding**: `.text` grew 0x3A0 (0x236000 → 0x2363A0) against a predicted range of 0x2A0..0x396 whose top was the sum of the named terms, so **the fill term is 0xA** — but **`.bss` moved**, 0x41F18 → 0x41F58 (+0x40) with `__bss_end` 0x802961d8 → 0x80296218, the object's 32 bytes landing at 0x80292c60 exactly where `stage90_pthread_callbacks` had been and pushing it to 0x80292c80: **the first direct measurement of "an object's `.bss` costs `align64`" for a real object**, which the ledger had only for stand-ins, and a reminder that *a row which has not moved for two steps is not a row that cannot move*. `.text` ends 0x802363A0, **0x1C60** below the `.data` boundary at 0x80238000, so `.data`, `.sysctl_set`, `.init_array` and the image size (0x2542B0) all stayed put — the third step running; the table moved +0x3A4 against a +0x3A0 text growth, the extra 4 being the orphan `.rodata` region's alignment. **`xnu_entry_kv_written` fell 0x90 → 0x8E and that is not a lost record**: it is `g_kv_len`, *bytes* not records (`entry_stubs.c:749`), the record set is identical between the runs (3794 `key=0x…` occurrences), and the two bytes are the stub name — `nwk_wq_init` is 11 characters, `dlil_init` 9, and 11 − 9 = 2. `xnu_entry_checksum` moved the other way, `0x9040E1ED` → `0x9040E22D`, because `bss_end` is one of the words it XORs. Object `.text` landed on **0x80203D48** for the eighth consecutive check of the contiguous-chain reading. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:` and no `panic:`, `kv_dropped=0`, hardware watchdog the only net across the jump, the two `data abort` lines the payload's own probes at 201 and 3443, log 301858 bytes / 3979 lines ending `No errors detected`, device back on Android on its own (`cancro`, release 10). **Next: `dlil_init`** (`bsd_net_dlil.o`, `bsd_init + 0x80C`, key `0x8003B200`) — the start of the network layer's initialisation, which starts threads of its own — then `proto_kpi_init` (`+0x810`/`0x8003B204`), `socketinit` (`+0x814`/`0x8003B208`), `domaininit` (`+0x818`/`0x8003B20C`) and `iptap_init` (`+0x81C`/`0x8003B210`), with the mount machinery beyond them. |
| stage90 | [experiment-435](experiment-435-the-bsd-init-tail-batch-and-the-measurement-that-settles-the-strategy.md) | **the `bsd_init` tail batch — 22 objects in one step, and the measurement that settles the strategy: 41 names retired, the frontier moved *one call*, and the boot path measured as a closed set of 1056 objects short 122 names** — the distance to the goal was measured before anything was linked. **`bsd_init()` is the *last* thing between this boot and the OS**: in `kernel_bootstrap_thread` what follows it is `OSKextRemoveKextBootstrap`, `kdebug_free_early_buf`, `serial_keyboard_init`, `vm_page_init_local_q`, `thread_bind`, `vm_pageout()` — and **every one of those is already real in the linked image** (`0x80108C4C`, `0x8003D3B0`, `0x80035DE8`, `0x800196B0`, `0x8009F8D0`, `0x80064950`), so the goal's bar ("enter the OS") is exactly "`bsd_init` returns". `bsd_init`'s own remaining work, read from the linked image: **49 calls after `+0x80C`, 24 of them stubs**, of which 23 are defined by 22 pool objects — this step links all 22. The 24th, `kmstartup` (`+0x844`), is **not** covered: `bsd/kern/subr_prof.c` defines it, `bsd_init.c` calls it from the `#ifdef GPROF` arm, and the file does not compile because it uses `STATIC`, which `kern_sysctl.c` defines at its own line 204 and no header `subr_prof.c` includes (that is why `bsd_kern_subr_prof.o` does not exist). **Effect: 41 resolved (35 function, 6 storage) / 312 added (264 function, 48 storage)** → `767 → 1038` undefined, `615 → 844` function, `152 → 194` storage, the build's stub list coming out at exactly 1038 records. **312 is the number the step exists to produce, and the first attempt at it was 7** (defect 142): that pass subtracted every name the *pool* defines, and **the pool is a superset of the link** — `entry_object_effect.py` asks `n not in image` and is right, so 22 objects out of 695 satisfy 41 stubs and create 312 more. **Prediction written before the build and hit to the byte: `stub_hit=ifnet_llreach_init`, caller key `0x80205F7C`** — the walk's answer `bsd_init → dlil_init → ifnet_llreach_init STUB`, the *first call inside the first object*, confirmed against the linked image (`dlil_init` `0x80205BFC`, the `bl` at `+0x37C`), with `_w0`/`_w1` spelling `"8020"`/`"5f7c"`. **So the batch retires 41 names and moves the frontier one call** — `dlil_init` cannot return while its first callee is a stub, and `bsd_init` gets no further than `+0x80C`. **And the same measurement gives the road's length: the 22 objects contain 889 `bl`-to-stub call sites covering 300 distinct stub names, whose definers number 84 more objects — while `tools/boot_closure.py` measures the whole boot as 1214 objects available, 1056 on the boot path, 8342 symbols needed, 122 undefined everywhere, and exactly one failing file on the path (`osfmk/kperf/kperfbsd.c`, 1 symbol).** That is the finding: the walk that retires one call per step is not how to reach a booted OS, and the memory `mi4-a-kernel-entry-points-closure-is-the-kernel` had already recorded the same for `arm_init`. **Layout: the largest change this ledger has made, and every row moves** — text `0x2363A0 → 0x2A5980` (+0x6F5E0), image `0x2542B0 → 0x2C6BD0` (2911184), `.data` `0x80238000 → 0x802A8000` (0x1E6C8), `.sysctl_set` `0x802C66C8`, `.init_array` `0x802C6B3C`, `.bss` `0x802C6C00..0x8030B090` (0x44490; placed 0x12DE plus 0x125A of `align64`), and **the whole memory layout re-placed by the build rather than overflowed**: args `0x80298000 → 0x8030D000`, `topOfKernelData` `0x80400000 → 0x80500000`, tree `0x80600000 → 0x80700000`, headroom 2051952. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, `checks=5`/`failures=0`, no `exception:` and no `panic:`, `kv_dropped=0`, hardware watchdog the only net across the jump, log 301867 bytes / 3979 lines ending `No errors detected`, device back on Android on its own (`cancro`, release 10), both stages rebuilt byte-identically. **Next: not another one-name batch — the step that links the boot path's closure (1056 objects) and reads the 122 names nothing in the pool defines**, each supplied or left as a self-naming stand-in. |
| stage90 | [experiment-436](experiment-436-the-whole-kernel-link-and-the-stop-that-stops-being-a-stub.md) | **the whole-kernel link — 1038 stubs become 44, 194 storage stand-ins become 0, and the stop stops being a stub: the frontier's kind changes for the fourth time, to a NULL `g_crypto_funcs` dereferenced by `tcp_init`** — the 436 block in `build_entry.sh` **globs `out/xnu_kernel_obj/` and `out/xnu_asm_obj/`** and appends everything not already named, minus three objects that cannot be linked beside them: **423 objects added, 286 already named above**, out of a 712-object pool. It is 435's own measurement continued to its conclusion: 435 priced its method (889 `bl`-to-stub sites over 300 names in 22 objects, 84 more definers behind them; `boot_closure.py` putting the whole boot at 1056 objects short 122 names) and the memory `mi4-a-kernel-entry-points-closure-is-the-kernel` had already recorded the answer for `arm_init` — **the closure of a kernel entry point is the kernel** — so 436 stops choosing and links the pool. **The three refusals are collisions, not choices: fifteen names are shared between the eleven entry-side objects and the pool** — `start.o` (4: `_start`, `start_cpu`, `resume_idle_cpu`, `arm_init_tramp`, all `xnu_arm_start.o`'s), `locore.o` (**10**: `ExceptionVectorsBase`, `ExceptionVectorsTable`, the eight `fleh_*` handlers — *this is the vector table, and the image has its own*), `iokit_KernelConfigTables.o` (1: `gIOKernelConfigTables`, MSM8974's tables not Apple's) — and `locore.o` is also the pool's only definer of `thread_bootstrap_return`/`thread_exception_return`/`thread_syscall_return`, which is why refusing it costs a name and not only a duplicate. The build checks the refusal list **by name** (a stale list would surface later as a link error and read as something else) and refuses a glob that finds fewer than 300 objects, because a pool that was never built would produce a smaller image *and a smaller stub list* — a step that looks like progress and is the opposite. **Effect: `pass 1 undefined` 1038 → 44 (994 names retired in one step), function 844 → 44, storage 194 → 0, and zero multiple definitions** — which is the check on the fifteen-name list rather than a hope. **Zero storage stand-ins remain**, so `mi4-stand-in-size-is-not-value` is retired for the whole image and replaced by its sibling, which the run then demonstrates. **Prediction written before the build and confirmed: `stubs: 44 function(s), 0 storage`.** The stop prediction, from `xnu_entry_callwalk.py --root bsd_init` on the built image, was **`stub_hit=kmstartup`, key `0x8003B228`** — the twelfth `bl` in `bsd_init`'s last straight run, no conditional branch between them. **Measured on hardware: the falsifier, not the prediction.** `xnu_entry_stub_caller_v=0x00000000` — **no stub was reached at all** — and instead `exception: data abort` at `pc=0x80409DB8`, `dfar=0x3C`, `insn=0xe590603c` (`ldr r6, [r0, #0x3c]`, `r0` = 0), inside **`aes_encrypt_key128`** (`0x80409DA0`, `libkern/crypto/corecrypto_aes.o`), whose third instruction loads **`g_crypto_funcs`** — a real `.bss` global at `0x8053381C` that is NULL. Its defect is `libkern/crypto/register_crypto.o` (`B`), its only writer is `register_crypto_functions()`, and **its only caller in the whole tree is Apple's `com.apple.kec.corecrypto` kext** — 432's `pthread_functions` shape exactly, and the second table this boot needs that nothing in the tree writes. Of the four `bl <aes_encrypt_key128>` sites the reachable one is `tcp_init + 0x104`, through the **inlined `tcp_tfo_init()`** (`tcp_subr.c:449`: `read_frandom(key,16)` at `+0xF4`, then the AES call, **no branch between `tcp_init`'s entry and it but `tcp_initialized`**), reached from `bsd_init + 0x818` (`domaininit`). **That the call is unconditional is the finding: on the real device `g_crypto_funcs` is non-NULL before `bsd_init`, from the prelinked corecrypto kext — so this table is a thing the kernel is supposed to have and does not, not a workaround.** **The walk was right about the stubs and the stop was a different kind of stop: kind 2 of `mi4-stub-walk-frontier-kinds`, "an invented zero" — and 436 is the first step whose stop is one**, because before it every name of this sort was a *storage stand-in* and a stand-in that stops is not a zero that gets dereferenced. **The whole-kernel link did not create these failures; it converted an unknown number of them from a named stop into an opaque fault.** The walk cannot see it for 431's reason: both of its lists are built from the *stub* set, and `g_crypto_funcs` is not a stub. **What the step did accomplish: five calls further than 435.** `dlil_init`, `proto_kpi_init`, `socketinit` and `domaininit` all ran to completion — the whole network-layer initialisation 435 measured at one step per name — and the frontier is now **one named, reachable table** with a 44-long queue behind it, only one of which (`kmstartup`, `bsd_init + 0x844`) is on the statement list. **Layout, and the window doubles**: text `0x2A5980 → 0x4B2A80` (+0x20D100), image `0x2C6BD0 → 0x4E344C` (5125196), `.data` `0x802A8000 → 0x804B4000` (0x2E360), `.sysctl_set` `0x802C66C8 → 0x804E2360` (0xFD8), `.init_array` `0x804E3338` (0x114), `.bss` `0x804E3480..0x805348F8` (0x51478 = 332920, was 0x44490), args `0x80536000`, `topOfKernelData` `0x80700000`, tree `0x80900000`, **window `8388608 → 16777216`** because `ENTRY_SIZE` doubles until it covers the tree, headroom 1881864. All four layout invariants hold and the fifth is the build's own line: *"the copied image ends at 0x804e344c, **52 bytes below** `__bss_start`"*. **`verify_sections` still reports `.bss .data .init_array .sysctl_set .text` and nothing else** — the check that matters for a 423-object step: the new material brings `.rodata.cst4/8/16/32`, `.rodata.str1.8`, `.rodata.str4.4`, 40 `.text._ZN*` COMDATs, `__TEXT,__os_log` and `__DATA,__sysctl_set`, every one matched by a pattern `entry.ld` already had, **none an orphan**. `bsd_netinet_tcp_subr.o` `0x8035A9C0` (`tcp_init` `0x8035AB6C`), `libkern_crypto_corecrypto_aes.o` `0x80409BB0` (`aes_encrypt_key128` `0x80409DA0`), `bsd_vfs_vfs_cprotect.o` `0x803E0240`. **Safety: the fault is a *read* of `0x3C`** — `dfsr=0x5`, a section translation fault on read, not a write to storage and not a wild write — caught by the payload's handler, 19 words of machine state recorded, nothing written anywhere. 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=1`, `checks=5`/`failures=0`, one `exception:` and no `panic:`, `kv_dropped=0`, hardware watchdog the only net across the jump, payload 7954 KB accepted by `fastboot boot`, log 301742 bytes / 3976 lines ending `No errors detected`, device back on Android on its own (`cancro`, release 10). **Next: not another object.** The remaining 44 are one on the boot path (`kmstartup` — a one-line fix to `subr_prof.c`'s missing `STATIC`) and 43 in subsystems this pool does not build; linking nothing else can move the boot. **437 is a stage-owned `stage90_crypto_functions.c`** — a `crypto_functions_t` from Apple's own `register_crypto.h` whose AES entries are real (because `tcp_tfo_init`'s call is unconditional) and whose other entries are self-naming stand-ins (because the next zero in that table must be a `stub_hit=` line with a caller key, not a `first_dfar`), installed from an `.init_array` constructor the way 432's table is. |
| stage90 | [experiment-437](experiment-437-the-image-supplies-g-crypto-funcs-and-a-real-aes-128-goes-on-the-boot-path.md) | **`g_crypto_funcs` — the image supplies the kernel's crypto dispatch table, a real AES-128 goes on the boot path, and `bsd_init` is down to one stub** — two new files in `stages/stage90/xnu_supply/`: `stage90_aes.{c,h}` (AES-128 per FIPS-197 — key expansion, both ciphers — and SP 800-38A CBC, plain C with no tree headers) and `stage90_crypto_functions.c` (Apple's `struct crypto_functions`, **45 self-naming stand-ins and two real AES-CBC entries**, installed from an `.init_array` constructor). `build_entry.sh` gains `STAGE90_AES_OBJ`, a **host known-answer gate that runs the same source before the object is linked**, `STAGE90_CRYPTO_FUNCTIONS_OBJ` and the record; `build_xnu_arm_kernel.sh`'s platform block gains the second out-of-manifest file. **The design decision is the load-bearing one, and it is forced: a stand-in earns its keep by stopping and naming itself, and `aes_encrypt_key128`'s call site cannot stop** — it is unconditional inside `tcp_init` (inlined `tcp_tfo_init`), so stopping there stops `domaininit` and therefore `bsd_init`, and a stand-in that returned without writing would leave `tfo_ctx` uninitialized, which is 436's own defect class (a wrong value with nothing to report it). Real AES is the only option that is both honest and survivable — and the only one that can be *measured*. It is checked twice: **on the host against 19 published vectors** (FIPS-197 A.1's whole key schedule — the thing the boot path actually calls, so a wrong `Rcon` would be invisible in a block test — Appendix B both ways, Appendix C.1 both ways, SP 800-38A F.2.1/F.2.2 with the chained IV; a failure fails the build) and **in the image against C.1** by the constructor. Every other member points at a body that reports `stub_hit=g_crypto_funcs.<field>` with a caller key, so the next zero in that table is a *named stop* rather than a `first_dfar`; the one knowingly imperfect stand-in (a `ccdigest_info` whose `state_size = 0` makes `ccdigest_di_decl` size a zero-length stack context, so the stop arrives one statement later than ideal) is recorded rather than hidden, and nothing on the boot path reaches a digest. **Prediction written before the build and before the run: stub set unmoved at `44 function(s), 0 storage`, nine records and no `stub_hit`, stop at `stub_hit=kmstartup`, key `0x8003B238`** (`bsd_init + 0x848`). **Measured on hardware: exactly that**, and eight of the nine records to the byte — `table_addr=0x8047939C`, `words=0x2F` (47, read from `sizeof(struct crypto_functions)/sizeof(void*)` rather than a hand-kept list, which is what makes the completeness scan cover Apple's whole struct), `nulls=0x00000000`, `cbc_desc=0x80479518`, `cbc_size=0x000000C0`, `kat=0x00000001`, `kat_w0=0x69C4E0D8`, `abort_entries=0`, `checks=5`/`failures=0`, `kv_dropped=0`, no `exception:` and no `panic:` line. **The key is the proof and not merely the absence of an abort**: `kmstartup` is at `bsd_init + 0x844` and the AES call is inside `tcp_init`, which `domaininit` (`bsd_init + 0x818`) calls — **so `0x8003B238` cannot be reached unless `tcp_init` returned, and `tcp_init` cannot return unless `aes_encrypt_key128` completed.** 436's `data abort` is gone and the boot is four real calls past it. **The two misses are each worth more than the hits.** `crypto_funcs_ptr=0x00000000` where the table's address was predicted: the record is read at the top of the constructor, before `register_crypto_functions()`, and the `.init_array` walk runs before Apple's own `OSKext::initialize()` because `last_kernel_constructor.o`'s word is linked last by construction — so the zero is 436's central fact *measured from inside the image*, and the pair `ptr = 0 at entry` / no `wrong_table` at exit brackets the registration between them. And `aes_ctx_size=0x000007C0` (1984) where 288 was predicted: a wrong *record*, not a wrong step — `AES_CBC_CTX_MAX_SIZE` (`libkern/libkern/crypto/aes.h`) has two branches and this build takes the `__ARM_NEON__` one (`-mfpu=neon-vfpv4` does define it), so the context is the **bit-sliced** 13 × 128 + 32. The arithmetic from the tree's own macros — `cc_config.h:190` `__arm__` → `CCN_UNIT_SIZE 4`; `ccn.h:104` `ccn_sizeof_size(n) = 4 * ceil(n/4)`; `ccmode_impl.h:68` `cc_aligned_struct(16) cccbc_ctx` → 16; `cc.h:37` `cc_ctx_n(T,n) = ceil(n/sizeof(T))` — gives **4 + 16 + 256 + 1696 = 1972 → ceil(1972/16) = 124 → 1984 (0x7C0)**, and the branch without NEON gives **276 → 18 → 288 (0x120)**, which is *exactly* the predicted number: **so the miss is located in the `#if` and not in the arithmetic**, and it is corrected as a *check* — a branch-dependent `_Static_assert` on `sizeof(aes_encrypt_ctx)` and on `AES_CBC_CTX_MAX_SIZE` in `stage90_crypto_functions.c`, compiled by the same build with the same flags, so a flag change stops the build instead of quietly moving the record. `verify_sections` still five names; `.init_array` `0x114 → 0x118` (+4, the whole of the new constructor — the one row the step was entitled to move), `.bss` size unmoved at 0x51478 with both ends +0x4000, text `0x4B2A80 → 0x4B4AC0` (+0x2040), image `0x4E344C → 0x4E7450` (5141584), `.data` `0x804B4000 → 0x804B8000` (0x2E360), `.sysctl_set` `0x804E6360` (0xFD8), bss `0x804E7480..0x805388F8`, args `0x8053A000`, window 16777216, headroom 1865480, payload 7970 KB sha256 `5725e989…`. **Safety: 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `exception:` and no `panic:`, `kv_dropped=0`**, hardware watchdog the only net across the jump, `fastboot boot` only, nothing flashed, log 302266 bytes / 3991 lines ending `No errors detected`, device back on Android on its own. **`bsd_init` now has 232 calls and exactly one stub left** — `kmstartup` at `+0x844`, reported here as a *compile error* in `bsd/kern/subr_prof.c` (`STATIC`, which only `bsd/kern/kern_sysctl.c` defines at its own line 204). **438 corrected the cause**: `subr_prof.c` does use `STATIC`, but what stopped it is that `libkern/conf/files:5` declares `gprof`, and the *single flat* `meta_features.h` told every translation unit `#define GPROF 0` — which `#ifdef` reads as *defined* — so `bsd_init.c:852` guarded a call whose only definer is dead. The call had to stop existing, which is the per-component option header. |
| stage90 | [experiment-438](experiment-438-the-option-headers-carry-their-component-and-the-stop-becomes-a-pseudo-inits-entry-read-out-of-a-stub-body.md) | **the option headers carry their *component*, two stubs retire at once, and the stop becomes an instruction prefetch abort at an address that *is* an instruction** — `tools/gen_option_headers.py` now writes `out/xnu_options/<CONFIG>/<component>/meta_features.h` beside the flat one, `build_xnu_arm_kernel.sh` puts a component's own directory **ahead of** the flat header for every manifest file that belongs to it, and the new `tools/check_option_headers.py` — run by the build — makes both halves structural. **The cause 437 named was wrong and the measurement is two files away.** 437 said `kmstartup` (`bsd_init + 0x844`) was stopped by `bsd/kern/subr_prof.c` using the macro `STATIC`. It is stopped by `bsd_init.c:852` being `#ifdef GPROF` while **`libkern/conf/files:5` is `OPTIONS/gprof optional gprof`** — so the single flat `meta_features.h` defined `GPROF 0` for every translation unit, and `#define GPROF 0` is not *off* to `#ifdef`, it is *defined*. A BSD file called a profiler entry point, whose only definer is `subr_prof.c` — and that file's body is inside the same `#ifdef` and does not compile, because `STATIC` is *also* undefined there. The two facts are one fact. **The fix is a faithful reproduction of a checked piece of Apple's build, not a fudge:** `mkheaders.c:71-79` walks *the file table it is given*, `makedefs/MakeInc.dir` runs the config tool over each component's own `conf/files`, and `MakeInc.def:466` is `INCFLAGS_LOCAL = -I.` — the component's own object directory. Which options a translation unit sees is a property of the **component**. The comment in `scan_options()` asserted the opposite (*"the value is the same whichever component declared it"*) — the *value* is, the *membership* is not. **Membership alone is not a rule either, and the scan is what says so: exactly two cross-component reads exist in the whole tree.** `GPROF` (libkern → bsd) is **not shared** — every one of those blocks is dead when the option is off, `nm` over all 695 objects finds no reference to `mcount`, `_gmonparam` or `cfreemem`, and it is the only reading under which Apple's own `subr_prof.c` compiles; `CONFIG_MACF` (bsd+security → osfmk) **is shared**, because `osfmk/kern/task.h:241` guards a `struct task` field with `#ifdef` while `task.c` guards the same parameter with `#if` — narrowing it would give `struct task` two layouts in one kernel, an ABI mismatch no compiler can see. The check keys on *declarers*, not on first-declarer: `MACH_ASSERT` is declared by osfmk, bsd **and** iokit, so a bsd file reading it reads its own (found by exactly that mistake on `bsd/kern/kern_credential.c`), and its `KNOWN_READS` table is validated in both directions so it cannot rot. **Prediction: check passes both configs, 695 objects → 696, 3 failures → 2, `U kmstartup` gone, every other object byte-identical, stubs 44 → 43, and the run returns from `bsd_init` into `kernel_bootstrap_thread`. Falsifiers: a stop still at `kmstartup`, a profiler-era `data abort`, `bsd_init` still not returning, a `struct task` of two sizes.** **Measured: no falsifier fired, and three predictions were wrong.** Pool **695 → 696, 3 → 2 failures**, `bsd_kern_subr_prof.o` new — and the byte comparison is the load-bearing half: **exactly two objects changed, `bsd_kern_bsd_init.o` and `bsd_kern_subr_xxx.o`, the other 693 byte-identical**, including `bsd_kern_kern_clock.o`, the one the prediction called identical. **Miss (a): 44 → 42 stubs, because `subr_prof.c` is not the profiler.** Its `#ifdef GPROF` is 83–339 and **`addupc_task` at line 370 is outside it**; `resourcevar.h:124` expands it unguarded and `kern_clock.c:379`/`kern_sig.c:3346` call it, so it *was* a stub, and `T addupc_task 0x8028bb7c` is now real. Two stubs retire from two opposite directions — `kmstartup` because its **caller** stopped existing, `addupc_task` because its **definer** started existing. *A file's name is not its `#ifdef` structure.* **Miss (b): `.text` grew `0x4B4AC0 → 0x4B4B00`, +0x40, and changed nothing else** — image still 5141584, `.data` `0x804B8000`/`0x2E360`, `.sysctl_set` `0x804E6360`/`0xFD8`, `.init_array` `0x804E7338`/`0x118`, bss `0x804E7480..0x805388F8` all unmoved, the copied image still 48 bytes below `__bss_start` — **and the entry bin's hash moved anyway** (sha256 `2e735451…`): equal size is not equal layout. Closure 423 → 424 objects; `copyin`/`copyout` cost nothing because the image already defines both. **Miss (c), the run: the stop is not in `kernel_bootstrap_thread`.** The result line has **no `stub_hit=` for the first time**, and a fault block 436 and 437 never printed: `exception: prefetch abort`, `ifar=0xE52DE004`, `ifsr=0x00000005` (translation fault, section), `lr=0xE52DE008`, `spsr=0xA0000013`, `ttbr0=ttbr1=0x8070404A`, `abort_entries=0`, `checks=5`/`failures=0`, `kv_dropped=0`. `kv_written 0x226 → 0x346` — **+0x120 = 9 × 32 bytes and nothing else moved**, because `fleh_prefabt` writes its records only when it runs: 437 printed *nothing* for those keys, not zeros, so **no stub was hit at all**. **`0xE52DE004` is the ARM encoding of `push {lr}`**, and both `IFAR` and `LR_abt` agree on it, so the processor really was fetching from that address. It occurs in the image as a code word in exactly 43 places: **42 are the `push {lr}` of the 42 stub bodies** (laid out in the order of `xnu_arm_entry_undef.txt`, 0x18 bytes apart — a second, independent measurement of the stub count) and the 43rd is `inv_shift_rows`' first instruction. Every stub is `movw r0,#name; push {lr}; mov r1,lr; movt r0,#name; pop {lr}; b entry_stub_hit`. **And that is the whole stop, measured end to end:** `bsd_init.c:861` is `bsd_autoconf();` — inside `bsd_init` itself — and `bsd_autoconf` is `for (pi = pseudo_inits; pi->ps_func; pi++) (*pi->ps_func)(pi->ps_count);` over `struct pseudo_init { int ps_count; int (*ps_func)(int); }`, which `bsd/dev/busvar.h:46` declares **`extern struct pseudo_init pseudo_inits[]`**. The linked loop is `movw/movt r4,#0x80444e3c; ldr r1,[r4,#4]; beq exit; ldr r0,[r4]; blx r1`, and at `0x80444e3c` — `pseudo_inits`, entry **28** of the alphabetical undefined list — the two words are `0xE3020928` (`movw r0`) and **`0xE52DE004`** (`push {lr}`). **`pseudo_inits` is a *function* stub where the header says the symbol is an *array of structs***: the `0` that ends the walk is the array's first `ps_func`, and our stand-in put its own prologue there, so `blx r1` at `bsd_autoconf+0x28` jumps to it and the fault follows. `tools/xnu_entry_callwalk.py --root bsd_autoconf` reports *"no stub on the straight-line path … indirect calls the walk could not follow: `bsd_autoconf+0x28: blx r1`"* — **the walk's blindness and the device's fault are the same instruction.** **A third face of the symptom, and the first of its kind on hardware: the stop is a stand-in of the wrong *kind*.** The undefined list shows names and never kinds, so nothing host-side could have said the symbol was declared an array. **Safety: 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `abort_entries=0`, no `panic:` line, `kv_dropped=0`**, hardware watchdog the only net across the jump (`bite_truncated=0`, `timeout_s=25`), dead-man disarmed before it, `fastboot boot` only, nothing flashed, 302532 bytes / 3994 lines ending `No errors detected`, device back on Android on its own (`cancro`, release 10). **Next: supply `pseudo_inits` as the array the header declares** — `mkioconf.c:79-100` says what it contains (one `{count, func}` per `PSEUDO_DEVICE` with a `d_init`, terminated by `{0,0}`), so it is *generated* from the device table, and the check that must stop the build is a **kind** check: a symbol declared an array must not be stubbed as a function. |
| stage90 | [experiment-439](experiment-439-the-configuration-supplies-pseudo-inits-and-the-kind-check-stops-the-build.md) | **`pseudo_inits` becomes the array its header declares, the guard becomes a *kind* check, and the stop moves one level deeper -- into the array, at entry 4 of 6** — `tools/gen_pseudo_inits.py` derives the array from the configuration exactly as Apple's `SETUP/config/mkioconf.c:79-100` does (`expand.sh CONFIG` | the `pseudo-device` lines | those with an `init` word, in order, `count <= 0 -> 1`, `{0, 0}` terminator), and the platform block compiles it with the bsd define set because it includes `<dev/busvar.h>`. **The 438 stop is a kind error and no number could see it**: `build_entry.sh` decides function-or-storage from `nm` over the object pool, and when the pool defines *nothing* there is no type information at all, so `pseudo_inits` -- declared `extern struct pseudo_init pseudo_inits[]` at `bsd/dev/busvar.h:46` -- became a function whose own prologue *was* the array's first `ps_func`, non-NULL, which `blx r1` jumped to as `0xE52DE004` (`push {lr}`). `tools/check_stub_kinds.py` now reads the tree's headers for every name the pool does not define and **stops the build on anything declared an object**; it fails on the 438 image's own list, on exactly the right name, and that failure is its positive test. Four latent object-kind stand-ins are recorded in a `KNOWN_KINDS` table validated in **both** directions (`etherbroadcastaddr`, `lo_ifp`, `osrelease`, `ostype`) and 21 names it cannot classify are counted and printed on every run rather than passed. **Measured**: stub count **42 -> 42** (`pseudo_inits` out, `bpf_init` in -- a count is not a composition), closure **424 -> 424** (the one prediction miss: an unresolved symbol adds no *object*), `.data` **+0x38** and `image bytes` **+56** exactly as predicted, and `.text` **-0x20** while the image *grew* -- decomposed by relinking both shapes with only the stand-in swapped (faithful: it reproduces 438's `4934656`/`0x804E7480` and 439's `4934624`/`0x804E74C0` to the byte), giving **-4 of merged-string content and -28 of aligned fill** in two steps at 16-aligned symbols, `mi4-linker-fill-term` from the shrinking side. **On hardware**: no `exception:` and no `panic:`, every `abort_first_*` zero, and `stub_hit=bpf_init` at `xnu_entry_stub_caller=0x8003B260`. That key is the proof and it corrects 438: `bsd_autoconf` is **inlined into `bsd_init`**, so the running walk is at `bsd_init+0x86c` (`r4 = 0x804D3D4C` = `&pseudo_inits` as `nm` reports it, `blx r1` at `+0x86c`, key `+0x870`) and the standalone symbol 438 walked at `0x8003B4D0` is a second copy -- **a symbol existing is not evidence that it is the code that ran**. `bpf_init` is entry **4**, and a stub does not return, so `pty_init(16)`, `ptmx_init(1)` and `mdevinit(1)` **ran to completion on the device**. Safety: non-persistent `fastboot boot` through both gates, 25 x `persistent_write_attempted=0`, 87 x `failure_mask=0`, `abort_entries=0`, hardware watchdog armed with no bite, dead-man disarmed (`0x000C7FFF -> 0x00007FFF`), device back in Android on its own. Payload `cdf58fb9...` 7970 KB. **Where the frontier is**: `bpf_init` (`bsd/net/bpf.c`, `optional bpfilter`) has its cause in a measured defect -- the configuration declares eight devices (`bpfilter ether fsevents loop mdevdevice ptmx pty random`) and `out/device_table.txt` holds four, so `bpf.c`, `ether_if_module.c` and `if_loop.c` never enter the manifest. That is the next step. |
| stage90 | [experiment-440](experiment-440-the-device-conditions-come-from-the-configuration-and-one-decision-gets-three-consumers.md) | **the device conditions come from the configuration, and one decision gets three consumers that are compared** — "is `bpfilter` on?" was answered in three places with three answers: `out/device_table.txt` said absent (so the manifest dropped `bsd/net/bpf.c`), the hand-written `bpfilter.h` said `#define NBPFILTER 0`, and the `pseudo_inits[]` array 439 derived from `config/MASTER` carried `{4, bpf_init}` — a count written into a table **this image executes**, which is a declaration that the device exists, and nothing compared it with the manifest. The old table's premise was one grep (`grep -c '^pseudo-device' */conf/files` = 0) and the grep is right: `*/conf/files` is where an `optional` **condition is tested**, `config/MASTER` is where the device set that answers it is **declared**, and it publishes **24** `pseudo-device` lines (167-459) — [[mi4-not-absent-its-build-output]] for the twelfth time. `tools/xnu_config/devices.py` is the one parse (the four `parser.y:207-229` alternatives; `number is None` is `d_slave == UNKNOWN` and **not** a written 0), and its consumers apply Apple's two different rules on that one value with their citations (`mkheaders.c:85-101` maps `UNKNOWN` to 1; `mkioconf.c:79-100` maps `<= 0` to 1). `device_table.py` derives all ten rows; `gen_device_headers.py` replaces `gen_device_headers.sh` and takes the **intersection** of `dtab` and the `conf/files` need words (so `mdevdevice fsevents random` get no header — they are `standard`) with `header_count()` for the value; `gen_option_headers.py` stops writing device names. `tools/check_device_conditions.py` compares all five pairs in both directions, and both failure modes were made to happen: a deleted `ether.h` gives `NETHER is read by the tree (4 site(s), first bsd_init.c:890)`, and a colliding `out/xnu_options/RELEASE/ether.h` gives `two headers, one name, one include path, and -I order decides which a source gets` — which was **live**, `-I$OPTION_HEADERS` first, so the device header was shadowed and `#if NETHER > 0` in `bsd_init.c:890` stayed a silent zero while the manifest built `ether_if_module.c`. **A defect inside the fix**: the first 440 kernel build reported `615 tried / 614 compile / 1 fail` and had built nothing the step was about — `out/xnu_arm_manifest.txt` was a hand-run prerequisite — so `build_xnu_arm_kernel.sh` now generates it; the second reports `621 tried / 620 compile / 1 fail` (`osfmk/kperf/kperfbsd.c`, off the boot path), 83/83 C++, 703 objects. **Measured**: the manifest is `722 → 728` selected / `680 → 686` present, and the six are exactly `bpf.c`, `bpf_filter.c`, `ether_if_module.c`, `ether_inet_pr_module.c`, `ether_inet6_pr_module.c`, `if_loop.c`; undefined `42 → 27`; entry objects added `424 → 431`, refusals unchanged; text 4934624 → **4995104**, image 5141640 → **5192212**, `.bss` 0x804f3a40..0x80544a18, headroom **1816040**. Five of the 15 retired names are *named* by comparing the two builds' own reports (`bpf_init`, `bpfkqfilter`, `sysctl__net_link_ether_children` left the unclassified list; `etherbroadcastaddr` and `lo_ifp` left `KNOWN_KINDS`), and a **1.57-second A/B** — the six objects moved out of the pool and the entry rebuilt — gives **46** undefined, the **19 names the six objects supply**, and makes `check_stub_kinds.py` refuse the link on exactly those two rows: its both-direction table earning its keep a second time. Text rose **+60480** where the prediction said the six objects' own 27420 minus the retired stubs' 360, and the remaining **+33420 is in objects that are not among the six** — the device headers changed `NBPFILTER 0→4`, `NETHER 0→1`, `NLOOP 0→1`, `NPTY 1→16` and the 18 `NBPFILTER` sites compile bodies they dropped; the A/B is blind to them by construction and the remainder is recorded as open rather than attributed. **On hardware: no stub is hit at all.** `xnu_entry_stub_caller_v = _digits = _e = 0` and there is no `stub_hit=` line, so the walk ran to its `{0,0}` terminator and `bsd_init`'s remaining line executed — the prediction's premise. What stops it is `xnu_entry_undef_pc=0x8002dd88`, `lr=0x8002dd8c`, `spsr=0x60000093`, `why="exception: undefined instruction"`, `abort_entries=0`: the instruction is `DebuggerTrapWithState`'s own `udf #0xfdee` (`osfmk/kern/debug.c:371`, from `panic` at `:645`), and the message out of `r9` — the instrument's established route, with `frame_r4/r5/r8/r9` all agreeing with its table — is `0x8045bf97`, the first byte of `"Device tree property overflow: prop %p, length 0x%x\n"`, i.e. `pexpert/gen/device_tree.c:56`'s overflow check in `next_prop()`. The caller is not in the log and the instrument's own comment says it cannot be, so it is recovered from the image: `next_prop` is `static inline`, its panic is inlined into its four callers and each materializes the same literal, giving `DTLookupEntry+0x50`, `DTIterateEntries+0x6c`, `DTIterateProperties+0x60` and `skipTree+0x20` — and **`DTIterateProperties` has exactly one caller in the whole image**, `MakeReferenceTable+0x98` (`iokit/Kernel/IODeviceTreeSupport.cpp:344`), called only by `IODeviceTreeAlloc`, called only by `IOPlatformExpertDevice::initWithArgs+0x14`, reached from `IOKitBSDInit` — the call at `bsd_init+0x880`, the first statement after the walk. So the frontier moved from *a missing symbol* to **a malformed value in data this project hands the kernel**: the tree `stages/stage90/apple_dt.c` builds (`deviceTreeP=0x006083a4`, length 0x7358, 4 root properties, 0x15 root children) has a property whose `length` cannot be a length — `next = (prop + length + 11) & ~3` is `prop + 8 + round_up(length,4)` for every value, so it is not an alignment convention this project got wrong, and `DTIterateProperties` advances at most `nProperties` times, so the drift is inside one node's own list. **A second first in the same run**: `xnu_entry_zone_map_min=0xc056f000` / `zone_map_max=0xc07ad000` are **non-zero** — `zone_init` has run in this image for the first time. Safety: `fastboot boot` only through both gates, 25 x `persistent_write_attempted=0`, 87 x `failure_mask=0`, `checks=5`/`failures=0`, `checksum=0x90702a09`, no `panic:` line, log 302922 bytes / 4008 lines ending `No errors detected`, device back in Android on its own. Payload `f80dc972...` 8020 KB. **Where the frontier is**: host-side — build `apple_dt.c` for the host, dump the blob and replay XNU's own walk (`next = (prop + length + 11) & ~3` for `nProperties` steps per node) to name the node and the offset. |
| stage90 | [experiment-441](experiment-441-the-instruments-guard-refuses-the-kernels-own-stack-and-the-tree-the-device-builds-walks-clean.md) | **the instrument's guard refuses the kernel's own stack, and the tree the device builds walks clean under XNU's own formula** — 440's panic (`next_prop`, `pexpert/gen/device_tree.c:56`) names two values and the run printed neither: `xnu_entry_panic_ap`, `_element` and `_zonename` were all `0x00000000`. 440's doc read that correctly as *"the `va_list` read is refused by the image guard"* — and the sentence is true, reads like the guard working, and is the guard being wrong. `fleh_undef` gated every argument read on `entry_image_ptr()`; that bound was right when experiment 239 measured `r_args = 0x0029be88` with the image at 0x00200000 (inside the image — 240's log shows the whole chain working: `ap=0x0029be94`, `element=0x40401f20`, `zonename=0x0028b1b6`), and experiment 241's move to 0x80000000 moved `virtual_space_start` from 0x40400000 to **0xC0000000** (`arm_vm_init.c:505`) and the bootstrap stack with it, so 362 read `r_args=0xc80abeb0` and 440 `0xc2113d80` — outside the image, refused, and **printed as a zero that is indistinguishable from a panic that named nothing**. Same defect class as the guard's own comment already describes for the literals it replaced. The replacement needs no base and no image size: `r_args` is `panic`'s live frame address and the CPU is executing on that page, `ap = r_args + 12` and the two varargs follow it (239's arithmetic), so the rule is `[r_args, r_args + 32)` **and** 32 bytes in one page — 32 from the frame layout, 4096 from the page size, both derived. `entry_image_ptr` stays as the first branch (the strings and digit table really are in the image). Four new keys and the refusal is now visible (`xnu_entry_panic_args_page`, `0` = did not read; `xnu_entry_panic_ap_delta` is the control that says the frame layout has not moved); `xnu_entry_panic_arg0`/`arg1` are the generic names and `_element`/`_zonename` are kept after them because three experiments quoted that pair — **and the old names are half the defect**, since for this panic `element` *is* `prop` and `zonename` *is* `prop->length`. Host side first, and it is a negative result: `tools/apple_dt_host_dump.sh` slices the **real** `build_stage90_apple_dt()` out of `stage90_main.c` by asserted line ranges and compiles it with the real `apple_dt.c` (a re-implementation would have measured the re-implementation), and `tools/xnu_dt_walk.py` replays XNU's own formula over the 29528-byte result — **26 nodes, no property length that cannot be one, every node's declared `nProperties` matching the headers under it, XNU's walk and the payload's `skip_node` both ending at 0x7358, which is the device's own `deviceTreeLength`**. `--probe` (new) explains an address and prints what `next_prop` would compute there, and derives `ENTRY_DT_PA = 0x80900000` from `out/stage90/xnu_arm_entry.h` rather than writing it down. **On hardware: the same stop** (`undef_pc=0x8002de88`, `r9_fmt`→the same literal), and the three words arrive — `args_page=0xc20cb000`, **`ap_delta=0x0000000c` exactly as predicted**, `ap=0xc20cbd8c`, **`arg0=0x8090cae0`**, **`arg1=0xff000000`**. The overflow is exact (`0x8090cae0 + 0xff000000` carries out of 32 bits). `prop` is inside the `0x20000`-byte tree buffer but **0x5788 bytes past the end of the 0x7358-byte tree** — the uninitialized part `xnu_entry_copy_device_tree` never wrote — so the walk consumed the tree and kept going, which refutes the prediction's "inside the tree, between two properties" and its "ASCII-shaped" `length` (that shape is what drifting into a *value* looks like, and `--probe` demonstrates it: `0x7461706d` = `"mpat"`). Text 4995104 → **4995456**, **image bytes unchanged at 5192212** (the linker's fill term: `.text` ends 0x804c3980, `.data` pinned to 0x804c4000 by its `2**14` alignment); `zone_map_min/max` non-zero for the second run in a row. Safety: `fastboot boot` only through both gates, 25 x `persistent_write_attempted=0`, 87 x `failure_mask=0`, 0 x `stub_hit=`, log 303063 bytes / 4012 lines ending `No errors detected`, device back in Android on its own. Payload `6c4bd9fa...` 8020 KB. **Where the frontier is**: read `DTRootNode` through XNU's own `DTLookupEntry(NULL, "/", &root)` — no offset, no `nm` literal, no link change — and the 32 name bytes at `prop`, which are provably readable because `next_prop` read `prop->length` at `prop+32` before it panicked |
| stage90 | [experiment-442](experiment-442-what-the-walk-was-handed-and-the-thirty-six-bytes-it-died-on.md) | **what the walk was handed, and the thirty-six bytes it died on** — 441 read `panic`'s two operands out of the trap's own frame (`prop=0x8090cae0`, `length=0xff000000`, `ap_delta=0x0000000c`) and could not tell whether `prop` was a property header some walk had drifted *onto* or the middle of a value it read as one; this step asks XNU's own accessor instead of naming the static. `DTLookupEntry(NULL, "/", &root)` takes `DTRootNode` when `searchPoint == NULL` (`device_tree.c:226-229`) and returns it on the second statement for the path `"/"` (`:230-235`) — one call, no walk, no `kalloc`, no `nm` literal and no link change. **All four predicted rows came back exact**: `xnu_entry_dt_root=0x80900000`, `_nprops=0x00000004`, `_nchildren=0x00000015`, `_first_prop_w0=0x656d616e` (`"name"`), `w1..w3=0`. That pair of readings together is the host walk's `nProperties`-against-headers check re-run on the device's own memory, and it settles the disjunction 441 left open: the walk started at the right address on the right bytes, and `PE_state.deviceTreeHead` is not the frontier. **The new fact is the thirty-six bytes at `prop`** — `xnu_entry_panic_prop_w0..w7 = 0xff000000`, eight words one value, so the name words that would have to hold a C string hold nothing and `prop->length` was read *at* `prop` rather than produced by an unaligned landing; `prop` is not a boundary, not a value, and not a header. 441's structural audit becomes a tool: `tools/xnu_dt_walk.py --landings` prints every address `next_prop` can put the pointer at — **603 landings over 26 nodes (629 properties; each node's first property is set as `entry + 1` and never reaches `next_prop`), 603 distinct, 0 that are not a node header, a property header or the tree end, the highest exactly `0x7358` where the tree ends, and none at `0xcae0`**. That is stronger than "the tree walks clean": a walk over these bytes moves only between those 603 addresses, so `0x8090cae0` is not an output of `next_prop` on this blob at all — and to arrive there from inside the tree one `length` would have to cross at least ~`0x5000` in a single step, which no property in the blob is. So exactly two explanations survive, and they are the two halves of what a walk needs, **its bytes and its pointer**: the tree in memory is not the tree in the file (a clobber, and the `0xff000000` region is its signature), or the bytes are the file's and `next_prop`'s first call was already handed a pointer off this tree (the caller, not the tree). `xnu_entry_panic_str`/`_caller`/`_message` are all `0x00000000` and that is expected, not a refusal: they are real XNU `.bss` globals (`0x804fedec`/`0x804fee04`/`0x804fee00`) and `panic()`'s single-CPU path sets none of them, which is why 441 made `r9` the route — `r9_fmt=0x8045c41f` reads as the `device_tree.c:56` literal and `undef_pc=0x8002e028` disassembles to `udf #0xfdee` (`debug.c:371`). The size prediction held to the byte: `.text` **4996256** (arm-none-eabi-size prints `5000380`, which is `.text` plus the orphan `.sysctl_set`, 4124 bytes of it, that 298 named), entry image `STAGE90_XNU_ENTRY_BIN_BYTES` **5192212** unchanged, payload sha256 `a6be1b17...` 8212480 bytes as predicted; `args_page=0xc2013000` is a different `kernel_map` bootstrap-stack page from 441's `0xc20cb000`, which is the number 441 recorded itself as having over-specified rather than predicted. Safety: `fastboot boot` only through both gates, 25 x `persistent_write_attempted=0`, 87 x `failure_mask=0x00000000`, 0 x `stub_hit=`, `kv_written=0x779`/`kv_dropped=0`, log 303614 bytes / 4027 lines ending `No errors detected`, device back in Android on its own. **Where the frontier is**: the device's tree checksummed against the blob's, and a replay of the device's own tree with XNU's formula through every node and property — equal checksums and a replay ending at `0x7358` with 26 nodes and 629 properties retires the bytes and leaves the caller; anything else bounds the writer. |
| stage90 | [experiment-443](experiment-443-the-device-replays-its-own-tree-and-hashes-the-bytes-it-walked.md) | **the device replays its own tree, and the bytes in memory are not the file's** — 442 left exactly two explanations one reading apart: the bytes in memory are not the blob's (then a `length` the walk reads is not the value the dump holds, one step crosses the tree, and the walk dies at `0xcae0` in the untouched tail of the `0x20000`-byte buffer), or the bytes are the file's and `next_prop`'s first call was already handed a pointer off this tree (then the caller is the frontier, not the tree). Both are statements about **device memory**, so `fleh_undef` now carries a replay of XNU's own walk over the bytes at `root` — the same advance `tools/xnu_dt_walk.py` uses, the first property set as `entry + 1` and never passed through `next_prop`, the overflow test on **absolute addresses** because `os_add3_overflow` is 32-bit arithmetic on the pointer and `0xff000000` plus an offset of `0x1234` does not carry while plus `0x80901234` does — reporting `nodes`, `props`, where it stopped and **which of four named conditions** stopped it, plus FNV-1a 32 over the bytes it consumed and over each eighth of them. **Measured: the replay reproduces the panic exactly** — `replay_end=0x0000cae0`, `replay_stop=0x0000cae0`, `replay_stop_kind=0x00000001` (`os_add3_overflow`), where `0xcae0` is the offset 442 read out of `panic`'s own frame as `prop` and the overflow there is the `length=0xff000000` 441 read beside it: a walk started at the tree's root, knowing nothing about the panic, arrives at the same event. **So the first branch is the true one and the prediction named the second** — recorded as the miss it is, because the reasoning was about the file and the measurement is about the memory. And the divergence is **bounded by the counts**: `replay_nodes=0x0000000d` (13) and `replay_props=0x000001b7` (439) against the host's 26 and 629, while the root's four header words and its first property's name (`0x656d616e`) came back exact — and 368 is exactly the host's cumulative property count after twelve nodes, so 439 minus 368 = **71** property steps inside the thirteenth node, the host's node at `0x43d4` holding 100. That is a hypothesis and not a second measurement (the identity of the device's thirteen nodes has not been read), and it is the only reading consistent with all four numbers; it points at the node's `nProperties` word or a `length` in its 72nd header, at or just before `0x50cc` — the property named `callback-runtime-exec`, declared length 4. **An instrument defect, recorded**: the hash range was `[root, root + end)`, so in the case the hash exists for the two ranges are not the same range — the device's `end` is `0xcae0` and the host's `0x7358`, `chunk_bytes` came back `0x195c` instead of `0xe6b`, and the nine hashes describe different bytes; the range-free readings settled it, and a fixed range would have made the hash work when it was needed. Control readings unchanged: `dt_root=0x80900000`, `nprops=4`, `nchildren=0x15`, `map_base=0x80000000` (gVirtBase, the range the replay bounded its reads by, derived from `arm_vm_init.c`'s `MEM_SIZE_MAX` rather than written down). `undef_pc=0x8002e468` exactly as predicted (`DebuggerTrapWithState`+0x28, `udf #0xfdee`); `trap_r9_fmt=0x8045ca13` is the literal's **opening quote** — the prediction said `0x8045ca14` because its search started at the first character of the text, a one-byte miss in the search and not in the finding. `.text` 4996256 → **4997792** (+1536) and, because `.text` now crosses its `2**14` boundary, `.data` moved from `0x804c4000` to `0x804c8000` and the entry bin from **5192212 to 5208596** bytes (+16384, all of it the alignment gap) with `.bss` following to `0x804f7a40` — the linker's fill term from the other side. Safety: `fastboot boot` only through both gates, 25 x `persistent_write_attempted=0`, 87 x `failure_mask=0x00000000`, 0 x `stub_hit=`, `kv_written=0x9c7`/`kv_dropped=0`, log 304204 bytes / 4044 lines ending `No errors detected`, device back in Android on its own. Payload `1043464d...` 8228864 bytes. **Where the frontier is**: the same replay with a **trace** — every node's `(offset, nProperties, nChildren)` in visit order and a ring of the last sixteen property reads as `(offset, length)` pairs — compared against the host's tables, which names the header that differs and therefore the byte. |
| stage90 | [experiment-444](experiment-444-the-tree-was-inside-xnus-free-memory-and-moves-below-topofkerneldata.md) | **the tree was inside XNU's own free memory, and moves below `topOfKernelData`** — 443 left a contiguous prefix of the tree intact and everything from one offset suspect, and `osfmk/arm/arm_vm_init.c:370-399` is why: `avail_start = cpu_ttep + 6 pages = 0x8070A000` is where the bootstrap allocator begins handing memory out (`first_avail = avail_start`, `:534`), `:286` maps exactly the *other* region for the tree (`arm_vm_page_granular_RWNX(end_kern, phystokv(args->topOfKernelData) - end_kern)`), and `build_entry.sh` put the tree at `ENTRY_DATA_LIMIT + 0x200000` = `0x80900000` — **2 MB inside `avail_start`'s range**, so the 2.07 MB of allocation 443's counts measured ran straight over it. The fix is `ENTRY_DT_OFFSET=$((ENTRY_DATA_LIMIT - ENTRY_DT_MAX))` plus an invariant that conserves the right pair (`ARGS + ARGS_BYTES <= DT_OFFSET`, `DT_OFFSET + DT_MAX <= DATA_LIMIT`) where the old one conserved the tree's distance from the *tables*; the window stays 16 MB on purpose now, because the minimal covering power of two for the new home is 8 MB and letting it shrink would silently halve `boot_args->memSize`. A trace of the walk's own node headers and property reads went in to falsify the fix, and **the run is silent: the entry image wrote 0 lines where 443 wrote 112** — `grep -c xnu_entry_dt` 0 against 24, no `exception:`, no `stub_hit=`, the payload's first 3931 lines identical to 443's but for five timing-dependent values, device back in Android on its own with 25 x `persistent_write_attempted=0` and 87 x `failure_mask=0`. **The panic is gone and the walk is not shown to work**, because the trace lives in `fleh_undef`: the instrument sat on the branch whose non-execution is the success condition. Silence here means what both instrument headers say it means — a loop, a spin, or a block that never wakes — so the next run is `STAGE90_ENTRY_TRACE=1`, whose terminal `thread_block` wrapper reports the block's caller and continuation beside every `kalloc`'s returned pointer and `vm_page_free_count`, which is the reading that separates a block from an allocator with nothing left. `.text` 4997792 → **5000512**, entry bin 5208596 unchanged, payload `8af1ca19...` 8228864 bytes |
| stage90 | [experiment-445](experiment-445-the-trace-names-the-silent-stop-as-a-block-and-drops-the-record-that-names-its-site.md) | **the trace names 444's silent stop as a `thread_block`, and the record that would name its site is the one the collector refuses** — `STAGE90_ENTRY_REAL_ARM_INIT=1 STAGE90_ENTRY_TRACE=1`, no source change (`.text` 5000512 → **5001344**, entry bin 5208596 unchanged, layout unmoved, the five wrappers linked with 108 `bl` sites routed to `__wrap_thread_block`), turns the terminal wrapper into the report: `real XNU entry: t268: thread_block was called`, with **no `exception:` and no `stub_hit=`** anywhere in the 322 lines the entry image wrote — so the frontier is neither a fault nor a missing symbol, and 439's `stub_hit=bpf_init` is behind the boot. **And `t268_thread_block_caller`/`_continuation` are not in the log**: `kv_dropped=0x3f8d` = **16269** records refused against `kv_written=0x1fea` = 8170 bytes, so the 8192-byte buffer holds the **first 280** records of the boot and the terminal wrapper's two — the only ones that matter — are written last into a collector that is full by construction. The survivors are the boot's earliest allocations, resolved against the image it ran: `lck_grp_alloc_init+0x2c`, `lck_grp_attr_alloc_init+0x24`, `ledger_entry_add+0x88`, `ivac_alloc+0x54`, `ipc_table_init+0x34`, `zalloc_internal+0x3cc` (16 of 21 `kma`s), `zone_init+0xec`, `ltable_init+0x6c`, `waitq_bootstrap+0x114`, every `kalloc` served by the zone it named and **every `kernel_memory_allocate` returning 0**. Two readings follow: 439's stop is behind the boot (≥16269 further records before the block), and **444's "free memory essentially gone" is corrected by the machine — `vm_page_free_count=0x7aa` = 1962 pages ≈ 8 MB against the derivation's `0x1F6000` = 502 pages, and `vm_page_wait` was never called while the buffer was live**; the two disagree by 4x and the one taken from the device is the one to keep. Next: a `.bss` slot pair for the terminal record, reported by the epilogue outside the buffer the way `g_first_abort_*` already is (`entry_stubs.c:1067-1080`), and the run names the block's call site. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, device back in Android on its own, 309832 bytes / 4254 lines ending `No errors detected`. Payload `b158c9f5...` 8228864 bytes |
| stage90 | [experiment-446](experiment-446-the-terminal-record-leaves-the-collector-and-the-report-carries-the-call-site.md) | **the terminal record leaves the collector and the report carries the call site — the block is `ml_get_max_cpus`, and the flag it waits on condemns a step 405 had cured and this image does not run** — the change is the pattern `entry_stubs.c` already had for the abort slots: five `.bss` slots (`g_block_caller`/`_continuation`/`_kv_len`, `g_vmwait_caller`/`_count`) written by `entry_epilogue_block`/`entry_note_vmwait` and printed **outside** the dump, with `build_entry.sh` passing `-DSTAGE90_ENTRY_TRACE=1` to `entry_stubs.c`'s own compile as well as the link, so that *the presence of `xnu_entry_block_caller`* is a statement about the image and a `0` can only mean the wrapper never ran. **Measured: `xnu_entry_block_caller=0x80008224`** = `ml_get_max_cpus+0x3c`, i.e. the `bl __wrap_thread_block` at `+0x38` after `cmp r1,#1` / `str #2` / `assert_wait` — `osfmk/arm/machine_routines.c:184-196` rendered instruction for instruction, with all three of its constants agreeing between source and image (`MAX_CPUS_SET`=1, `MAX_CPUS_WAIT`=2, `thread_wakeup_prim` at `ml_init_max_cpus+0x54`) and **one definition of `max_cpus_initialized` at `0x804fbe88` in both functions** (`nm`: `b max_cpus_initialized`), which is the check this project's most repeated defect class would fail. Three of the four mechanical rows came in exact (`_continuation=0`, `_kv_len=0x1fea`, `_vmwait_count=0`) and **the branch missed by one degree**: not the IOKit state/arbitration family and not `read_random`, but the wait one frame *above* that family's own writer — the image's two `bl ml_init_max_cpus` sites are `IOCPUInterruptController::initCPUInterruptController(int,int)+0x94` (`IOCPU.cpp:765`) and **`MSM8974PlatformExpert::start+0x28`, this project's own file**, whose third statement is experiment 405's `ml_init_max_cpus(1)` probe. So the run says the probe did not reach that statement: *either* the nub path in `StartIOKit` — called by `PE_init_iokit` at `0x80005298`, before `vm_shared_region_init` — no longer reaches it, *or* `super::start` returned false and the guard ended `start` early; the same function publishes `IORTC`, so 419's 30 s `waitForService` is unarmed too. **The collector fix is visible in its own arithmetic**: `xnu_entry_kv_dropped` `0x3f8d` → **`0x3f8b`** (16269 → 16267) with `xnu_entry_kv_written` unmoved at `0x1fea` — exactly the two records that used to be written as the run's last act. The six `bl ml_get_max_cpus` callers are resolved (`vm_page_init_local_q`, `waitq_alloc_prepost_reservation`, `commpage_populate`, `mcache_init`, `mbinit`, `sysctl_mib_init`) and the run **names none of them**: 445's "the boot passed `bpf_init`" is corrected, because that name's absence proves only that entry 4 of `pseudo_inits` is no longer a stub (439/440 made it real; the stop that named it was `bsd_init+0x86c`'s `blx r1`), while the 16547 record-writes point the other way — the run cannot choose, and a non-terminal wrapper on `ml_get_max_cpus` and `ml_init_max_cpus` (446's `vm_page_wait` shape, pointed at the waiter itself) is 447. `.text` 5001344 → **5001536**, entry bin 5208596 unchanged, `.bss` unmoved at `0x804f7a40 .. 0x80548a18`, no `exception:`/`panic:`/`stub_hit=`, payload `9f3b2384...` 8228864 bytes, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, device back in Android on its own, 310014 bytes / 4259 lines ending `No errors detected` |
| stage90 | [experiment-447](experiment-447-the-non-terminal-wrapper-names-the-site-and-the-count-says-whether-the-writers-ran.md) | **the non-terminal wrapper names the site, and the count says the writer never ran — the walk is some thirty frames *behind* the last two hardware runs** — `ml_get_max_cpus` **returns** once its flag is set, so it can be wrapped the way `vm_page_wait` is and not the way `thread_block` is (record the caller, call through), and the one design decision is that the **first** caller is the reading and not the last: the call that blocks is the first to find the flag unset, and keeping the last would name the one site that cannot be the one the run ended in. `g_maxcpus_caller`/`_count` and `g_initmax_cpus_caller`/`_count`/`_arg` join 446's slots in `.bss` (`0x804f8ae8 .. 0x804f8af8`) with five keys printed outside the dump, and `build_entry.sh`'s `TRACE_LDFLAGS` goes five -> **seven**; the routing was checked in the image before the device was touched (**6** `bl __wrap_ml_get_max_cpus`, **2** `bl __wrap_ml_init_max_cpus`, at exactly the eight sites). **Measured, every row as predicted:** `xnu_entry_maxcpus_caller=0x800e7598` = **`commpage_populate+0x70`** (403's first-invocation site, the one 405 cured) with `_count=0x1`, and `xnu_entry_initmax_cpus_count=0` with `_caller`/`_arg` = 0 — so the first call of `ml_get_max_cpus` on this boot *is* the call that blocked, and the flag's only runnable writer (`MSM8974PlatformExpert::start+0x28`, the 405 probe) never executed its third statement. **What the run adds over the prediction is direction:** `commpage_populate` runs in `vm_shared_region_init`, which `kernel_bootstrap` calls **before** `bsd_init` (`osfmk/kern/startup.c:576`), so the walk is ~30 frames *earlier* than 439's `stub_hit=bpf_init` (`bsd_init+0x870`) and 440's tree panic (`bsd_init+0x880`) — 445's "the boot passed `bpf_init`" is refuted by measurement, not argument. **The regression narrows to `StartIOKit`'s first six statements** (`IOStartIOKit.cpp:120-170`): `PE_init_iokit` has no early `return` (its only `StartIOKit` is its last statement, `pexpert/arm/pe_init.c:279`) and the ladder proves it returned, and the config table keeps Apple's `IOPanicPlatform` behind this project's personality *on purpose*, so a failed match would have **panicked** — it did not, which leaves `new IOPlatformExpertDevice` returning NULL (a metaclass that never registered, since matching instantiates by name), `initWithArgs` returning false (the `IODeviceTreeAlloc`/`MakeReferenceTable` walk that panicked at 440), or the fallback absent. Those are host-side questions first, and 448 starts there. `.text` 5001536 -> **5001888**, entry bin 5208596 unchanged, `.bss` unmoved, payload `05651c4c...` 8228864 bytes, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:`/`panic:`/`stub_hit=`, device back in Android on its own, 310209 bytes |
| stage90 | [experiment-448](experiment-448-the-platform-experts-start-is-never-entered-and-the-chain-that-explains-it.md) | **the platform expert's `start` is never entered, and the chain that explains it is `StartIOKit`'s first six statements** — 447's zero can be tightened from a count into a statement about control flow because every link of the guard is unconditional in the source (`IOService::start` is `{ return true; }`, `IOPlatformExpert::configure` returns true on every path in source *and* disassembly — `0x8015e734`, every branch landing on `mov r0,#1` at `0x8015e848` — and `IODTPlatformExpert::configure` discards `processTopLevel`'s result): if `start` is entered it reaches `ml_init_max_cpus(1)`, so it was never entered, and the failure is inside `StartIOKit`'s first six statements. The step wraps the **direct** calls in that chain — `registerService` and `initWithArgs` are vtable slots, and the disassembly settles an error the step's own prediction made and 447 inherited: `new IOPlatformExpertDevice` is `mov r0,#0x60` / `bl OSObject::operator new` / `bl IOPlatformExpertDevice::IOPlatformExpertDevice()` — a **direct** allocation and constructor, not an `allocClassWithName`, so the class cannot fail to exist and `allocClassWithName`'s two sites are both downstream of a match. Six non-terminal wrappers (`IODeviceTreeAlloc`, `IOWorkLoop::workLoop`, `IORecursiveLockAlloc`, `IOSimpleLockAlloc`, `IOCommandGate::commandGate`, `kernel_thread_start`), twenty `.bss` slots (`0x804f8afc .. 0x804f8b44`) and twenty keys outside the dump, `TRACE_LDFLAGS` seven -> **thirteen**. **Measured, and rank 1 is the branch that fell:** `xnu_entry_dtalloc_count=1` at caller `0x80160aa0` = `initWithArgs+0x14` with `_arg=0x806e0000` (the tree 444 moved below `topOfKernelData`, read back from the device for the first time since) and `_ret=0xc0530e70`, and `xnu_entry_workloop_ret=0xc04fa848` with `_count=1` — so `initWithArgs` returned **true**, `StartIOKit` called `attach(0)` and `registerService()`, and 447's `initmax_cpus_count=0` still stands: `start` was never entered. **The prediction's `why` column is corrected in place rather than smoothed**: `IORecursiveLockAlloc`/`IOSimpleLockAlloc`/`IOCommandGate::commandGate` return a **pointer**, so their `_bad` slots (`0xc052f7e0`, `0xc04eeae0`, `0xc04fa820`) are three kernel-heap pointers, i.e. successes — only `kernel_thread_start`'s non-zero return is a failure, and its `_bad=0` with `_count=2` is the reading that the step's successor turned into an identity. `.text` 5001888 -> **5003264**, entry bin 5208596 unchanged, `.bss` unmoved at `0x804f7a40 .. 0x80548a18`, 8228864-byte payload, 310870 bytes, no `exception:`/`panic:`/`stub_hit=`, device back on its own |
| stage90 | [experiment-449](experiment-449-the-catalogue-and-the-two-handles-on-it-that-wrap-can-reach.md) | **the catalogue, and the two handles on it that `--wrap` can reach — the instrument, not the boot, is what stops the run** — the step answers the instrument question first, host-side and with the project's own linker: `--wrap` renames an **undefined** reference, and a reference that resolves inside its own object is left alone (`arm-none-eabi-ld --wrap=real_a` over one translation unit: `bl 8000 <real_a>`, not the wrapper), which is why `doServiceMatch`, `probeCandidates`, `findDrivers`, `_IOConfigThread::main` and `pingConfig` — defined *and* called inside `IOService.cpp`, four of them virtual and present in the image's 24 `IOService`-derived vtables — are unreachable, while the five that cross an object boundary are not. Five wrappers (`iokit_post_constructor_init`, `IOCatalogue::initialize`, `OSUnserialize` as a **three-deep ring**, `allocClassWithName`, `publishResource` as a four-deep ring — a ring does not have to assume which call is first) plus 448's `kernel_thread_start` extended with the **continuation** of each of the first four calls; twenty slots `0x804f8b48 .. 0x804f8be4`, 22 keys, `TRACE_LDFLAGS` thirteen -> **eighteen**. **Measured, and the answer is not in IOKit:** `xnu_entry_unser0_caller=0x80138c48` = `IOCatalogue::initialize+0x1c` with `xnu_entry_unser0_ret=0xc054d9e0` — **non-NULL, the table parses**, so the empty catalogue is dead; `_unser_count=1` (neither `IODTMatchNubWithKeys` nor `IODTFindMatchingEntries` ever ran: no probe), `_alloc_count=0` (nothing was ever instantiated by name), `_pub2_count=0` (no `publishResource` at all, so `start` was never entered — 447's deduction from the other side), and `_kthread_cont0=0x801759f8`/`_site0=0x80175324` = `IOWorkLoop::threadMain`/`IOWorkLoop::init+0x110`, `_kthread_cont1=0x80132960`/`_site1=0x8012d124` = **`_IOConfigThread::main`/`pingConfig+0x124`** — the async service-matching thread **was created** and never ran. The one prediction that missed is the better reading: `_postctor_caller=0x8011e590` = `OSRuntimeInitializeCPP+0x184`, the `blx r0` of the `.init_array` walk, because a tail `b` writes nothing to `lr` — **a caller key on a tail-branched call names the branch's caller**. And the answer: `__wrap_thread_block` is **terminal** and has never called the real function since experiment 268, so the run halts *at* the first `thread_block` — which is the first call's `ml_get_max_cpus+0x3c` (0x800087e4, `_count=1`) — one instruction before the scheduler would hand the CPU to `_IOConfigThread::main`. **446/447/448's "the run ends in that `thread_block`" is "the report is dumped there"**, and 439's `stub_hit=bpf_init` at `bsd_init+0x870` / 440's tree panic at `bsd_init+0x880` are the same boot built without the trace layer; 444's silence has the same cause. 450 retires the halt: `thread_block` goes non-terminal with a ring of the first eight `(caller, continuation)` pairs written live into the ram-console as well as into `.bss`, and the prediction is the whole chain this step measured the absence of. `.text` 5003264 -> **5004992**, entry bin 5208596 unchanged, `.bss` `0x804f7a40 .. 0x80548ad8`, payload `c2cf13be...` 8228864 bytes, 311915 bytes, no `exception:`/`panic:`/`stub_hit=`, device back on its own |
| stage90 | [experiment-450](experiment-450-the-block-stops-terminating-the-run-and-the-matching-thread-gets-the-cpu.md) | **the block stops terminating the run, and the matching thread gets the CPU — the step that retires the instrument's own halt, and cannot read its result** — 449 measured that `__wrap_thread_block` had been *terminal* since 268, so the report ("the run ends in `ml_get_max_cpus`'s `thread_block`") was true of the report and false of the boot; 450 makes the wrapper record and call through (`g_block_count`/`_returned`, a ring of the first eight `(caller, continuation)` pairs, the last pair, the first and last returning caller — eleven `.bss` slots, eighteen keys, printed outside the dump, no new `--wrap`, `TRACE_LDFLAGS` unchanged at eighteen, 108 `bl __wrap_thread_block` sites). The *why* is read rather than assumed: `sched_startup` blocks **without** `assert_wait`, so `TH_WAIT` is clear, `thread_select`'s `still_running` stays true and the dequeue requeues instead of switching — while `ml_get_max_cpus` asserts first, so `TH_WAIT` is set and `thread_invoke` must commit a switch to `_IOConfigThread::main`. **Measured: 294738 bytes, no `stub_hit=`, no `exception:`, no `panic:`, not one of the step's eighteen keys** — every `xnu_entry_*` line is the payload's ladder, written before the jump, so the step could neither confirm nor refute its own table and the honest reading of 450 alone is "the boot is no longer stopped at the block, and the instrument cannot see where it went". 452 closes the loop: 33 blocks, 8 returns, `initmax_cpus` called once with `arg = 1`, `"IORTC"` published, the first block at `ml_get_max_cpus+0x3c` — every row as predicted. `.text` 5004992 -> **5005952**, entry bin 5208596 unchanged, `.bss` `0x804f7a40 .. 0x80548b58`, payload `1435f822...` 8228864 bytes, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, device back on its own |
| stage90 | [experiment-451](experiment-451-the-live-console-refuses-and-the-refusal-is-invisible.md) | **the live console refuses, and the refusal is invisible — the instrument's failure mode was the silence it was built to remove** — 450's last line is the payload's ladder and nothing after it, because every record the instrument keeps lives in `.bss`/`g_kv_buf` and only the epilogue writes them out; the ram console survives (Android preserves it as `/proc/last_kmsg`) but is written at VA `0xde500000`, which has no translation while XNU's tables are live (268's `exception: data abort`, `dfar=0xde500000`, `pc` inside `entry_write_kv`). So the design is one section descriptor into XNU's live L1, memory type Strongly-ordered, with four refusals before the write and the console's signature read back after it (1 no neighbour, 2 slot in use, 3 domain fault, 4 no signature, 5 no uncached encoding), and the descriptor's protection bits *inherited* from a neighbour section — XNU's own BLOCK for `RAM_CONSOLE_BASE - 1 MB`. **The neighbour does not exist.** The 451 comment asserted `boot_args.memSize` is `0x5e500000`; that is `xmaxmem`, the clamp `arm_vm_init` applies, and the log the same run produced says `xnu_entry_args_memSize=0x01000000` in its own ladder, three thousand lines above the live records that were never written. `_start`'s `mapveqp` walks `memSize` / 1 MB = **16** sections and `invalidate_tte` faults the whole 16 KB boot table first, so the live L1 holds indices `0x800..0x80f` and `0xfff` and nothing else: index `0xde4` is fault, the 4096-entry scan found nothing, and it refused with 1 — into `.bss`, where nothing reads it. Two smaller facts fall out and are recorded: nothing the payload plants in the boot table can survive (`invalidate_tte` runs at `_start`, after the payload's last act), and "one section past the end of memory" was never the right description of the console's address. `.text` 5004992 -> **5013116**, `.bss` `0x804f7a40 .. 0x80548b98`, payload `6fc92d46...` 8228864 bytes, 294738 bytes byte-for-byte the same shape and size as 450's log, 25/87 safety counts, device back on its own |
| stage90 | [experiment-452](experiment-452-the-live-console-works-and-the-boot-publishes-iobsd.md) | **the live console works, and the boot publishes `IOBSD` — 146 live records, 33 blocks, 16 kernel threads, and the BSD subsystem up** — the anchor is removed rather than moved: type, `AP[0]`, `S` and domain come from XNU's own section convention (`ARM_TTE_TYPE_BLOCK`, `ARM_TTE_BLOCK_AF`, `ARM_TTE_BLOCK_SH`, domain 0 checked against DACR), the memory type is the only measured field (`SCTLR.TRE` decides whether TEX is remapped, PRRR says which encodings are Strongly-ordered, refusal 5 if none is), the table base comes from `TTBCR.N` (`N == 0` means TTBR0 answers for everything) with an unusable base a *retry* rather than a refusal, and three descriptors are written — the console's MB, the MB after it, and one VA `0x01000000` past it as a second path to the same MB — each slot required fault first, each validated by reading the console's signature back through it. **The header settles four things this project had only argued about:** `ttbcr = 1`, so TTBR1 answers and the register choice is the hardware's; `l1 = 0x80704000`, literally `topOfKernelData + 4 pages`, i.e. `arm_vm_init`'s `cpu_ttep`; `slot_before = 0`, so the identity case really does degenerate `arm_vm_init`'s "clear V==P" loop to nothing and `start.s` really is what fills the table; and `desc = 0xde51040e` with `attr = 0xc` out of `prrr = 0x1f08022a` under `sctlr = 0x30c5787d` (TRE set), the value derived by hand in the design, with `alias_read = 0x43474244` = `'DBGC'` through the second VA. **Measured:** 299468 bytes, 146 records, `attempts = 1` (the first hooked call, `IOCatalogue::initialize`'s `OSUnserialize`, is already past the table switch), 33 blocks with 8 returns, 16 `kernel_thread_start`s naming `IOWorkLoop::threadMain`, `_IOConfigThread::main`, `bcleanbuf_thread`, `async_work_continue`, `mbuf_worker_thread_init`, `aio_work_thread` x2, `nwk_wq_thread_func`, `pf_purge_thread_fn`, `flowadv_thread_func`, `dlil_main_input_thread_func`, `ifnet_detacher_thread_func`; `initmax_cpus_caller = MSM8974PlatformExpert::start+0x28` with `arg = 1` (447's zero was the halt, not the boot); `pub2_key` naming **`"IORTC"`** and then **`"IOBSD"`**, i.e. `bsd_autoconf` reached `IOKitBSDInit()`, its last statement; and every block site resolved against the sources' own order (`mcache_init` 745, `mbinit` 749, `aio_init` 772, `nwk_wq_init` 815, `dlil_init` 817, `sysctl_mib_init` 858, `bsd_autoconf` 861). Two readings the prediction did not contain: a `block_enter` at `Call_continuation+0x18` is not a block inside that function (449's tail-branch rule at thread startup — `Call_continuation` is nine instructions ending in `blx r6`), and a block from `thread_terminate_self+0x2f8` is a dying thread parking. **The frontier is a wait, not a stop**: after the publish, one record — `lck_mtx_sleep_deadline+0x88`, which never returns — and that primitive has exactly two callers in the tree (`msleep` with a timeout at `bsd/kern/kern_synch.c:197`, and `vm_pageout.c:3491`), but the record names the primitive and never its caller, and nothing in the log says which thread a record belongs to. `.text` 5009408, entry bin 5208596, `.bss` `0x804f7a40 .. 0x80548b98`, payload `a50b2e22...` 8228864 bytes, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:`/`panic:`/`stub_hit=`, device back on its own |
| stage90 | [experiment-453](experiment-453-the-thread-identity-and-the-site-above-the-sleep.md) | **the thread identity, and the site above the sleep — the frontier is `init_thread`, XNU's own bootstrap thread, waiting in an IOKit deadline sleep that 452's caller list did not contain** — 452 could say *what* the boot waited in and never *who* waited or *where* the wait was asked for. The thread is free on ARMv7: `current_thread()` is a read of TPIDRPRW (`arm/cpu_data.h:58`, written on every switch by `cswitch.s`), so the block record becomes a triple `(thread, caller, continuation)`, and the reading checks itself — the first value read is `0x804fcdd0`, which is `init_thread` in the image's own symbol table (`osfmk/kern/thread.c:184`), while every other thread in the run is above `0xc0000000`, i.e. heap, i.e. `kernel_thread_start`. One frame up from a sleep is **not** walkable here (only 9 functions in the 5 MB image set `fp` from `sp`, and `msleep`'s `push {r4-r9, fp, lr}` carries `fp` for 8-byte alignment), so the step wraps the family instead: `_sleep` is `static`, but `grep -n '_sleep(' kern_synch.c` finds exactly seven call sites and all seven are global — `sleep`, `msleep`, `msleep0`, `msleep1`, `tsleep`, `tsleep0`, `tsleep1` — so seven wrappers are *complete*, each recording its own `lr` plus `chan`, `wmsg`, `pri` and the fifth argument (`TRACE_LDFLAGS` 18 -> 25; the first draft wrote six, having assumed from the name that `tsleep0` was the deadline variant of `tsleep`, and the source says it takes `int timo` while `tsleep1` takes the `u_int64_t`). **Measured: 301811 bytes, 221 live records (206 after the header), 33 blocks on 23 distinct threads with 8 returns, 6 sleep calls, 16 `kernel_thread_start`s, `"IOBSD"` published again**, and the six sleeps pair one-to-one by thread with the block in the primitive the source's own branch leads to (`msleep0` with a NULL mutex -> `_sleep+0x110`; `msleep` with a mutex and no deadline -> `lck_mtx_sleep+0x80`), each carrying its own `wmsg` read out of the image (`"blaundry"`, `"nwk_wq_thread_func"`, `"pf_purge"`, `"flowadv"`, `"ifnet_detacher"`) and `tmo = 0`, i.e. no timeout. **The finding is a negative: the boot thread has 6 blocks and 0 sleeps**, and since `lck_mtx_sleep_deadline` has exactly three callers in the image — `_sleep+0x21c`, `IOLockSleepDeadline+0x20`, `IORecursiveLockSleepDeadline+0x38` — and `_sleep` is reachable only through the seven wrapped entries, the boot thread's deadline wait came from **IOKit**. 452's "two callers" was wrong twice: `vm_pageout_wait` is inside `#if !CONFIG_EMBEDDED` and this boot is `CONFIG_EMBEDDED=1` (`nm | grep -c vm_pageout_wait` = 0), and the two IOKit entries that *are* in the image were not in the list. `.text` 5009408 -> **5011584**, entry bin 5208596, `.bss` `0x804f7a40 .. 0x80548c18`, payload `57ded60f...` 8228864 bytes, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:`/`panic:`/`stub_hit=`, device back on its own |
| stage90 | [experiment-454](experiment-454-the-two-thirty-second-waits-and-the-clock-that-is-not-the-problem.md) | **the two thirty-second waits, and the clock that is not the problem — the frontier is `IOFindBSDRoot`'s 30 s deadline wait for the IOBSD resource, and the run's own numbers name it** — 453 proved the boot thread's `lck_mtx_sleep_deadline` wait came from IOKit, so 454 wraps the two `...SleepDeadline` entries (both `T`, `TRACE_LDFLAGS` 25 -> 27), recording `(site, lock, event, interType, deadline, now)` — and `now` is read with the same single instruction `ml_get_timebase` uses (`mrrc p15, 0, lo, hi, c14`), so the report carries *both ends* of the comparison `assert_wait_deadline` will make. **Measured: 303430 bytes, 272 live records, 33 blocks with 8 returns, 6 sleeps, 2 IOKit waits, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:`/`panic:`/`stub_hit=`, device back on its own.** Both waits are the single `SLEEPNOTIFYTO` in `IOService::waitForMatchingService` — the image's `bl __wrap_IORecursiveLockSleepDeadline` is at `0x80135a50` and the run reports the return address `0x80135a54` = `+0xe0` — with `inter = 0` (`THREAD_UNINT`), on `gNotificationLock` (`0x8052dc6c` -> `0xc0471810`), and **`dl - now = 575999988` ticks in both**, so the deadline is `mach_absolute_time() + 30 s` computed inside IOKit, not a caller's absolute time. The clock calibration is the step's second result: between the two calls the counter advances 206259 ticks and the deadline advances by *exactly* the same 206259, so the wrapper's `mrrc` and XNU's `mach_absolute_time` are the same clock to the tick — which also **measures the counter's rate as 19.2 MHz from the device** and fixes the timebase offset at 0 (`ml_init_timebase` never ran, so the bare counter *is* `mach_absolute_time`). The caller is named by **one measured integer**: the two `event` values (`&result` in `waitForMatchingService`'s frame) differ by `0x30`, and since both call sites are in `bsd_init`'s straight line the difference is the difference of the callee frame sums — `IOKitInitializeTime`(16) + `waitForService`(16) = **32** versus `IOFindBSDRoot`(**80**) gives 48, while the alternative `IOSecureBSDRoot`(40) gives 8 or 40, neither 48. So **wait 1 is `IOKitInitializeTime`'s 30 s wait for the IORTC resource and wait 2 — the one that never returned — is `IOFindBSDRoot:379`'s wait for the IOBSD resource** (`iokit/bsddev/IOKitBSDInit.cpp:379`), reached from the inlined `setconf` in `bsd_init`'s mount loop (`bsd_init.c:940`); wait 1 **returned** after 2033 ticks (106 us) with 30 s left on its deadline, woken by the notification from `MSM8974PlatformExpert::start`'s `publishResource("IORTC")` (`0x80268398`; the trace's `pub2_key = 0x8046f668`, seven records earlier), so **wakeups work and only the timer is missing**. And because `IOKitBSDInit()` publishes `"IOBSD"` (`bsd_init+0x880`, before the loop) and the wait still slept, the entry into sleep 2 *is* the measurement that the published resource does not satisfy the dictionary. The 30 s site list had to be built from the source **and** checked against the image: `IOKitInitializeTime`'s `t.tv_sec = 30` (`IOStartIOKit.cpp:72`) has no 30 s immediate, because `waitForService` multiplies by 1e9 at run time (defect 163); `ROOTDEVICETIMEOUT` is 60 here, not 120. `dl - now` is 575999988 ticks = **30.000 s** each time, the deadline never arrives (the last record is 29.9996 s short of it) and the watchdog (`hw_watchdog_timeout_s=0x19`, 25 s) returns the device first — so **the timer work item and this frontier are one work item**, and it must land inside a 25 s window. `.text` 5011584 -> **5013120**, entry bin 5208596, `.bss` `0x804f7a40 .. 0x80548c58` (`init_thread` moved 0x804fcdd0 -> 0x804fce10, exactly the 0x40 of new slots), payload `eaac0aa8...` 8228864 bytes, 27 `__wrap_` symbols |
| stage90 | [experiment-455](experiment-455-the-wrap-that-cannot-see-the-call-and-the-boots-own-strings.md) | **the wrap that cannot see the call, and the boot's own strings — the query wrappers are dead by construction while the factories fire, and the publishes name themselves** — 454 left the registry as the frontier, so 455 wraps the query itself (`copyExistingServices`, whose return value is the measurement, and `matchPassive`, filtered on `this == gIOResources`) plus the four `serviceMatching`/`resourceMatching` factories, `TRACE_LDFLAGS` 27 -> 33, with `entry_rs_state()` reading `__state[0]`/`__state[1]` through `getResourceService()` (the only way another object can see the resource root — `_ZL12gIOResources` is local) at `+36`/`+40`, offsets the *build* takes from `IOService::getState()`'s own load and refuses the link over. **The two query wrappers produced ZERO records; the four factories fired three times**, and `objdump -d` on the linked image says why: `--wrap` rewrites an *undefined* reference, and `waitForMatchingService`/`getMatchingServices`/`copyMatchingService` call `copyExistingServices` from `IOService.cpp`, the object that defines it, so all **11** call sites go to the real function (`0x8012e18c`) and not one to `__wrap_` (`0x80455c4c`) — while the factories' callers are in `IOKitBSDInit.cpp`, `IOStartIOKit.cpp` and `IOPMRootDomain.cpp`, three other objects. A wrapper links whether or not anything reaches it: the symbol is defined, the undefined count does not move, the other checks pass, and a silence looks exactly like a run where the measured thing did not happen. The repair is a build-time reachability check with three honest readings of zero (`b`/`bl`/`blx` in the image: **30** / same-object-only: `copyExistingServices`, `matchPassive` / never called here: `sleep`, the KPI entry to `_sleep`), and **the check was wrong twice before it was right** — the first version matched the `<__wrap_symbol>:` label every wrapper has by construction and called all thirty-three reachable including the two the device had just disproved, and the second required a `bl`, which is not what a tail call is (`last_kernel_constructor` is `movw/movt` plus `b 8045551c <__wrap_iokit_post_constructor_init>`). **The frontier is unchanged and reproduces 454 to the tick**: site `0x80135f94` = `waitForMatchingService+0xe0` (the function moved `0x540`, the growth of the instrument's object linked ahead of the XNU ones), `dl - now = 575999988` ticks in both records, `dl` and `now` advancing together by 205641 ticks between them, `inter = 0`, `ent = 1`. What the run *does* give is the boot's own strings, resolved host side out of `xnu_arm_entry.bin` at `VA - 0x80000000`: `"IOPMPowerSource"` (`serviceMatching(PKc)`, `IOPMrootDomain::start`), `"IORTC"` (`resourceMatching(PKc)`, `IOKitInitializeTime`), and the heap `OSSymbol` `"IOResources"` (`serviceMatching(PK8OSString)`, `IOFindBSDRoot`) — plus the publishes in the log's own order: `publishResource("IORTC")` -> wait 1 -> `publishResource("IOBSD")` -> `serviceMatching("IOResources")` -> the wait that never returns, so **the dictionary was built after `"IOBSD"` was published and nothing in this image republishes it**. And **wait 1 slept too, although its property was published seven records earlier**: `resourceMatching("IORTC")` ends at `IOResources::matchPropertyTable`'s first branch (`ok = (0 != getProperty(str))`, `:5095`), which is satisfied — so the guard *before* the matcher is what fails for both queries, `inState == (__state[0] & inState)` with `inState = kIOServiceMatchedState = 0x4`, a bit with no `|=` anywhere in the tree whose only writer is `copyNotifiers`' `orNewState` argument in `doServiceMatch`'s tail (`:3754`, `__state[0] = (__state[0] | orNewState) & andNewState` at `:4782`) behind a guard on `kIOServiceInactiveState`/`kIOServiceModuleStallState` (`:3748`) — and that guard also holds the *other* candidate, `if (resourceKeys) setProperty(gIOResourceMatchedKey, resourceKeys)`, where `resourceKeys` is a local written only in the `this == gIOResources` branch of `if (keepGuessing && matches->getCount() && kIOReturnSuccess == getResources())` (`:3729`) and `publishResource` never adds to any array (`:3532`). **Measured: 303883 bytes, 287 live records, 33 blocks with 8 returns, 6 sleeps, 2 IOKit waits, 3 dictionary records, 0 query records**, trace span 375487 ticks (19.6 ms, first block t = 3.9327 s), the last record 575992236 ticks (29.9996 s) short of wait 2's deadline when the 25 s watchdog returns the device. `.text` 5013120 -> **5016032**, entry bin 5208596 -> **5224980**, `.bss` `0x804f7a40 .. 0x80548c58` -> `0x804fba40 .. 0x8054cd98` (+0x4000 of aligned copied data and 320 bytes of new slots), payload `cfbc8cc4...` 8245248 bytes, 33 `__wrap_` symbols, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:`/`panic:`/`stub_hit=`, device back on its own, and the payload rebuilds byte-identical after the check was added |
| stage90 | [experiment-456](experiment-456-the-match-fails-in-the-matcher-and-the-state-says-it-should-have-passed.md) | **the match fails in the matcher, the state says it should have passed, and the two readings bracket the registration — the resource root has no `IOResourceMatched` array, and `__state[0]` goes `0x0` -> `0x1e` between the two waits** — 455 could not see the query (`--wrap` reaches only a call from another object) but could see `IORecursiveLockSleepDeadline`, which `waitForMatchingService` enters from `IOService.cpp`, so 456 reads the registry **inside the wait that never returns**: `entry_registry_probe(seq, site)` in `entry_stubs.c`, called from `entry_note_iolock`, records `gIOResources`' `__state[0]`/`__state[1]` (via `getResourceService()`, `+36`/`+40`), `copyProperty(gIOResources, gIOResourceMatchedKey)` — the matcher's own call, `IOService.cpp:5109` — and then `getCount()`/`getNextIndexOfObject(gIOBSDKey, 0)`. Safe by construction and not by assertion: `copyProperty` takes `IORecursiveLockLock` (`IORegistryEntry.cpp:119`) in the same context the matcher calls it from, nothing between `IOResources` and `IORegistryEntry` overrides it (so a direct call is the vtable's answer), and what it returns is released with `OSObject::release`, the image's only `release`. Live only, because 455's run is the demonstration that a boot hanging at the frontier never reaches the epilogue; and one new build check, because a mistyped mangled name is *not* a link error here (the generator invents a stub and the probe would stop the boot at itself) — the five names are checked against the image and against `xnu_arm_entry_undef.txt`. **Measured: 304425 bytes, 305 live records, 33 blocks with 8 returns, 6 sleeps, 2 IOKit waits, 3 dictionary records, 2 registry readings**, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:`/`panic:`/`stub_hit=`, device back on its own. The prediction (bit 0x4 clear, guard failing before the matcher) was **wrong for the wait it mattered and right for the one it did not**: at the second wait `__state[0] = 0x1e` = registered|**matched**|first-publish|first-match with the inactive and module-stall bits clear, so the guard passes, `matchPassive` **is** called on the resource root, and `IOResources::matchPropertyTable` answers false in its third branch because `reg_rm = 0` — **there is no `IOResourceMatched` array, at either wait**. The state that *is* set carries the second result: `0x4` has exactly one writer, `copyNotifiers(gIOMatchedNotification, kIOServiceMatchedState, 0xffffffff)` (`:3754`), and in `doServiceMatch`'s tail the same guard also holds `if (resourceKeys) setProperty(gIOResourceMatchedKey, resourceKeys)` (`:3751`), so the tail ran and `resourceKeys` was NULL — the branch that fills it (`:3729`, `this == gIOResources` inside `keepGuessing && matches->getCount() && kIOReturnSuccess == getResources()`) did not. `publishResource` never touches that array (`:3532`). **And the two readings, 205152 ticks (10.7 ms) apart with `reg_ptr` identical at `0xc0589f78`, bracket the registration**: `__state[0] = 0x00000000` at the first wait — not even registered — which is why 454's IORTC wait slept although `resourceMatching("IORTC")` is satisfied by the matcher's *first* branch; the resource root is registered and matched between the two by XNU's **I/O config thread** (`_IOConfigThread::main`, 449's find), because `publishResource` only queues the work — which closes 454's positive control from the other side. The frontier is therefore **the kernel's drivers, not the kernel's bring-up**: the array is written only when the catalogue hands `doServiceMatch` a non-empty candidate set, and an empty set is what a kext-less kernel returns. `.text` 5016032 -> **5016704** (+672 = the probe), entry bin 5224980 unchanged, `.bss` `0x804fba40 .. 0x8054cd98` unchanged (the seven new slots landed in the existing fill), payload `1518e329...` 8245248 bytes, site `0x80136174` = `waitForMatchingService+0xe0` for the third build running |
| stage90 | [experiment-457](experiment-457-the-resource-root-gets-its-first-driver-and-the-wait-ends.md) | **the resource root gets its first driver, the 30-second wait that could not end ends 0.32 ms in, and the boot arrives at the root-device wait** — 456 named every ingredient of the match except the candidate set, and this step links it: **`MSM8974RootResource`**, a concrete `IOService` subclass whose personality is `IOProviderClass = IOResources`, `IOResourceMatch = IOBSD` (no `IOProbeScore`: a score ranks siblings in one provider-class array and this array has one member), plus one `--wrap` on `IOCatalogue::findDrivers(IOService *, SInt32 *)`. The derivation is the source's own: `doServiceMatch`'s tail needs `keepGuessing && matches->getCount() && kIOReturnSuccess == getResources()` (`IOService.cpp:3724`), `IOResources` overrides no `getResources` (`IOServicePrivate.h:183-198`) and `IOService::getResources` returns `kIOReturnSuccess` unconditionally (`:982-985`), so `matches` was the only variable — and `findDrivers` files personalities by **`IOProviderClass`** (`IOCatalogue.cpp:124-132`) and looks them up along the service's class chain (`IOResources` → `IOService`, `:199-231`), where a kextless kernel has nothing. **The same missing array is why nothing woke the waiter**: `copyNotifiers` keeps a notifier only if `matchPassive(notify->matching, 0)` (`IOService.cpp:4782-4784`) and the waiter's dictionary is the same one. Measured: `finddrv_seq` 1 and 2 with `finddrv_svc=0xc04861e0` and **`finddrv_count=0x00000001`** (456 could only infer 0), `rootdrv_seq=1` with `iobsd=0xc0459400`, and **three** sleep records at one site (`waitForMatchingService+0xE0`; the image has exactly one `bl IORecursiveLockSleepDeadline`, at `+0xDC`), each identified by the dictionary built just before it: `resourceMatching("IORTC")` from `IOKitInitializeTime` (30 s, `reg_state0=0x0`, no array yet — the first wait 455 and 456 never saw, and it fails because `copyExistingServices` will not return an unregistered service), `serviceMatching(gIOResources)+IOResourceMatched="IOBSD"` from `IOFindBSDRoot` (30 s, array 2 keys, no `IOBSD`), and `serviceMatching("IOMedia")+Content="Apple_HFS"` (60 s = `ROOTDEVICETIMEOUT`, array 5 keys with `"IOBSD"` at index **4**). The second wait ends **0.32 ms in**, at `block_return=0x800144bc block_returns=9`, on the tick the config thread's second pass allocated and started the driver — a **wake, not a deadline** (waits 1 and 2 died 0.18 ms and 0.32 ms into 30 s), so the timer is not on this path. `reg_idx=4` is the match and the doc's pre-run gloss ("0 means it holds IOBSD") was a misreading of `ok = (0xffffffffu != keys->getNextIndexOfObject(prop, 0))` (`IOService.cpp:5116`); the two per-build constants that did not hold are the resource root (`0xc04861e0`, not 455/456's `0xc0589f78`) and the site (`0x80136214`, not the predicted `0x80136174` — the step's own code moves `waitForMatchingService` by 0x80), which is why the durable form is symbol+offset. Build: `.text` 5016704 → **5018624**, `.init_array` 70 → **71** (index 36, before the platform expert's and `last_kernel_constructor`), image 5224980 → **5224984**, payload `c7cb2884…` 8245248 bytes. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:`/`panic:`/`stub_hit=`, log 306521 bytes, device back on its own. **Next:** the frontier is `IOFindBSDRoot`'s `do { waitForService(IOMedia+Apple_HFS, 60 s) } while (!service)` (`IOKitBSDInit.cpp:552-569`) — a loop that can only end when a root device exists, i.e. a storage driver or the `rd=`/`rootdev=` memory-device path. |
| stage90 | [experiment-458](experiment-458-the-oss-own-console-text-into-the-log.md) | **the OS's own console text is in the log for the first time — and so are the two reasons it had never been visible, plus a third kind of silence settled at compile time** — every reading this project has taken is its own instrument's words; this step puts the two sinks XNU's console text leaves through behind `--wrap`s: `vcputc` (reached *only* through `cons_ops[cons_ops_index].putc`, i.e. through an address reference — `build_entry.sh` reads the table out of the linked image and fails the build unless `cons_ops[1].putc` holds `__wrap_vcputc`, and checks all four words against `nm`) and **`uart_putc` rather than `serial_putc`**, because `PE_init_kprintf` stores `serial_putc` into `PE_kputc` with a `movw`/`movt` pair against a symbol its own object defines (`pexpert/arm/pe_kprintf.c:36-40`), which `--wrap` cannot reach — a second image check reads `serial_putc`'s one-instruction body (`b __wrap_uart_putc`) so the pointer the panic path prints through is covered. The capture takes one **128 KB block** out of the ram console's size field on its first character, publishes it *before* space-filling it, and keeps the records above it from then on: one read-modify-write on the shared field instead of one per kilobyte, self-healing once per kilobyte of text, and no `ldrex`/`strex` (unpredictable on this region's ARMv7 mapping). Measured: log 437874 bytes, the block at data offset `0x4a9ec` with **176 bytes of text — `iBoot version: \n\r` (a real `printf`, `pexpert/arm/pe_init.c:187`, with an *empty* firmware version, held in the 8 KB `.bss` tank because it arrived before the live channel's first record) and `Waiting on <dict ID="0"><key>IOProviderClass</key><string ID="1">IOMedia</string><key>Content</key><string ID="2">Apple_HFS</string></dict>` — which is `IOLog("Waiting on %s\n")` at `IOKitBSDInit.cpp:547`, the OS naming the frontier 457 could only infer**; the `\n\r` pairs are `_cnputs`'s own fingerprint (`serial_console.c:294-297`). The state record answers why none of it was visible: `opsidx=0x0` (the **serial** console, switched by `initialize_screen`'s no-video branch, `video_console.c:2846-2852`, from `PE_create_console` at `arm_init.c:365`) and `kputc=0x8009b1a4` = **`cnputc`**, so `serial_init()` failed, `uart_initted` is 0 and **`uart_putc` is a function that does nothing** — the console was wired to the one sink that cannot print, while the video sink's `gc_initialized` gate is never set either; `noserial=1` (kprintf off), `noconout=0`. **The copyright banner was never printed**: `bsd_init`'s `printf(copyright)` is `mov r0,#0; bl _consume_printf_args` in the image — `CONFIG_NO_PRINTF_STRINGS` (`config/MASTER:302`, `bsd/libkern/libkern.h:171-173`, `osfmk/kern/misc_protos.h:150-152`) makes `printf` a no-op in `bsd/kern` and most of BSD/OSFMK, while `pexpert/arm/pe_init.c` includes neither header and calls the real one — so `cannot mount root, errno = %d`, `We are hanging here...` and `bsd_init: failed to mount network root` **will never print** until the build changes, while every `IOLog` will. Frontier unmoved: three `iolock` records at `waitForMatchingService+0xE0` (site `0x801365d4`, symbol `0x801364f4`), `deadline − now` 29.9966/29.9936/59.9943 s, `finddrv` 2, `rootdrv` 1, 38 `block_seq`; `IOLog("Still waiting for root device\n")` (`:559`) is absent, so the watchdog returned the device at ~25 s inside the boot's own 60 s deadline. Prediction errors: `opsidx` 0 not 1 (the grep missed `initialize_screen` as a third caller of `switch_to_serial_console`), `kputc` is `cnputc` not `serial_putc`, the tank held 17 characters not 0, and the counters (`ostext_chars`/`_total`/`_heals`) were never published — the count had to be taken from the block's own bytes. `.text` 5018624 → **5020000**, `.bss` 340888 (0x2024 of it this step's), image **5224984** unchanged, payload `e5562bdd…` 8245248 bytes; 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:`/`panic:`/`stub_hit=`, device back on its own. **Next:** the root device — a storage driver giving `IOMedia` with `Content = Apple_HFS`, or the `rd=`/`rootdev=` path — in a build with `CONFIG_NO_PRINTF_STRINGS` off, because the messages a failure there would print are exactly the compiled-out class. |
| stage90 | [experiment-459](experiment-459-the-root-device-is-memory.md) | **the root device is memory — and the OS says the property is not there: `rd=md0` is parsed, the `IOMedia` wait is skipped, `mdevadd` is never called, and `IOFindBSDRoot` panics 0.6 ms later** — the first step in this walk whose product is a *device* rather than an instrument: 256 KB of the entry image's own `.bss` handed to the kernel as a disk (`g_stage90_ramdisk`, whose address and size are read out of the *link* by `nm`/`nm -S` into `out/stage90/xnu_arm_entry.h`, so the symbol and the DT property cannot disagree), a `/chosen/memory-map` node whose `RAMDisk` is exactly two machine words (`stage90_main.c`), `rd=md0` in **both** copies of the boot args (`boot_args.c`'s `CommandLine`, which is what `PE_parse_boot_argn` parses, and the tree's own `/chosen` `boot-args`), a root filesystem that can be mounted at all (`tools/xnu_config/boot/STAGE90_XNU.local` composes `STAGE90_XNU = [ RELEASE mockfs ]`, and mockfs is the only filesystem in this tree whose `vfc_mountroot` is non-NULL), the printf class restored (`-UCONFIG_NO_PRINTF_STRINGS`, so `mdevadd`'s five messages and `load_init_program`'s two exist as strings again), and a `config.stamp` recording what the entry link's object glob actually is (`STAGE90_XNU`, 706 objects, 129 defines, 0 printf shims, 3 mockfs objects) — because the link globs all of `out/xnu_kernel_obj/*.o` and whichever build ran last decides what this image's kernel *is*. Measured: log 457959 bytes, exit 0, device back on its own; **prediction 1 holds twice** — only two `iolock` records (site `0x80136908`, the boot thread `0x805540f0`, 5.9231 s and 5.9342 s, each a 30 s deadline: `IOKitInitializeTime`'s `IORTC` and `IOFindBSDRoot`'s own `IOBSD`) and no 60-second one, **and** the console block has no `IOLog("Waiting on %s")`/`Apple_HFS` dictionary, which is built *after* the `rd` branch; **prediction 3 holds** — the block now carries **921 bytes and 19 newlines** of XNU's own text against 458's 176, `Copyright (c) 1982, 1986, 1989, 1991, 1993` among them, i.e. `printf(copyright)` (`bsd_init.c:452`) is a real `printf` again. **The frontier moved back 20 lines, inside the same function**: `IOFindBSDRoot` was entered (its IOBSD wait is the second record), the media matching was not reached, and of every statement in between only `:490` can panic on this tree — `mdevlookup(0)` returned -1 because `mdevadd` was **never called**, which is a print's absence doing real work (`memdev.c:560-635` prints on all five of its outcomes and the capture demonstrably carries `printf`); the trap is `exception: undefined instruction` (`DebuggerTrapWithState`'s `udf`) at 5.9348 s, 0.6 ms after the IOBSD wait, silent because `xnu_live_console_noserial = 0x1`. **The run could not name the trap**, and that is this step's instrument defect: `fleh_undef` writes through `entry_kv` into the 8 KB `g_kv_buf`, which the `t268_*` kalloc tracer had already filled — `kv_written = kv_in_dram = 0x1fea` (8170 of 8192 bytes) with `kv_dropped = 0x66bb` (**26299 refused**) — so `xnu_entry_panic_str`, the key the falsifier named, was written and dropped. Prediction errors: 2 and 4 (no `Added memory device`, no `BSD root:`, no `load_init_program`), and 5 is the sharp one — its observation holds (no `cannot mount root`, no `We are hanging here...`, no `vfs_mountroot: can't setup bdevvp`) but its *reading* does not: those are absent because the mount never started, and absence of a failure message is only evidence when the code that prints it ran. The host side passes everything it can check — `host_dt_harness.c` reads `/chosen/memory-map`'s two words, their 8-byte length and `/chosen`'s `rd=md0` back through XNU's own reader (29632 of 32768 bytes of tree, 90.4%) — so the gap the run measured is between the **DT blob** and the **IODT registry plane** `IORegistryEntry::fromPath` walks, not a wrong value. The build also caught two defects in itself: an unquoted heredoc whose comment's ``verify_root_device`` ran the function again and substituted its output into the header (fixed by a quoted body and `@NAME@` placeholders with a refusal if any survives), and the first draft of that check exiting 1 *silently on success* under `set -e`/`pipefail` — 455's class again; after the fix the image is byte-identical (`ab253a424a12f5433f94eb5cee7b4659`). 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `stub_hit=`, `abort_entries=0`, watchdog armed and returned. **Next:** wrap `IORegistryEntry::fromPath` and `mdevadd`/`mdevlookup` (all ordinary undefined references, so `--wrap` reaches them; `getProperty` is a vtable call and is not needed) to say whether the path resolved, whether the property was read and what the device lookup returned — and move `fleh_undef`'s six keys off the tracer's buffer, because a silent panic's whole diagnosis is in them. |
| stage90 | [experiment-460](experiment-460-the-blob-the-walker-reads.md) | **the blob the walker reads — 459's edit broke the tool that dumps it, and the walker went on reading a tree from before the edit** — `tools/apple_dt_host_dump.sh` found the payload's builder by **hard-coded line numbers** (4, 31, 34, 41, 43, 831, each end asserted) while `tools/host_dt_check.sh` found it by matching braces, so 459's edit moved the lines, the dump exited 1 **and left `apple_dt.bin` on disk from the previous run** — a 29528-byte tree, 104 bytes short of the device's own `xnu_entry_device_tree_len=0x73c0`, whose walk is internally consistent and prints `ok` while saying `/chosen` has **zero children**: no `memory-map`, the exact missing-property shape 459 was hunting. **One extractor now** (`tools/apple_dt_extract.py`, pieces found by declaration and closed by braces, de-`static`ing the buffer and both functions), **the buffer's bound 32768 no longer stands in three files uncompared** (the payload's declaration is the storage, the shim's `extern` and the harness's `sizeof` take it from the extractor's `--facts`), the dump compiles against the generated `out/stage90/xnu_arm_entry.h` the payload uses, and `tools/xnu_dt_walk.py` **refuses a blob older than its inputs** (tested both ways: touch → exit 1 naming the newer file, re-dump → exit 0, byte-identical blob). **The plane replay answers 459's host-side half: the shape is not the problem.** `xnu_dt_walk.py --plane` models `IODeviceTreeAlloc`'s stack loop literally — `DTEnterEntry` returns `kSuccess` for any non-NULL child so **every** child is pushed, leaves included, and `DTExitEntry` restores the scope *with the index it had reached*, which is what makes the outer `while (stack->getCount() && DTExitEntry)` a depth-first walk instead of a re-walk — and reports 27 nodes, 26 `attachToParent` calls, **every node attached to the parent the blob's own walk gives it**, every node carrying a `name`, and `/chosen`, `/chosen/memory-map`, `/cpus`, `/arm-io`, `/device-tree`, `/memory` all resolving to the same node as `DTLookupEntry` (0x693c for memory-map in both). So `fromPath` fails on a tree that is correct, which leaves **the one thing no host tool can see**: `gIODTPlane` is created as `IODeviceTreeAlloc`'s first statement and `fromPath` returns 0 the moment its plane argument is NULL (`IORegistryEntry.cpp:1257-1258`), so if `StartIOKit(PE_state.deviceTreeHead, …)` (`pe_init.c:279`) passed a **zero**, every `fromPath` in the boot returns NULL over a perfect blob — and that predicts 459's log exactly. **Next (461):** read `gIODTPlane` and `PE_state`'s `deviceTreeHead`/`initialized`, wrap `IODeviceTreeAlloc` (cross-object from `IOPlatformExpert.cpp:1565`, so `--wrap` reaches it) and `IORegistryEntry::fromPath` plus `mdevadd`/`mdevlookup` — and move `fleh_undef`'s six keys off the kalloc tracer's full buffer first, because a silent panic's whole diagnosis is in them. No device touched in this step. |
| stage90 | [experiment-461](experiment-461-the-trap-has-its-own-buffer.md) | **the trap has its own buffer, and the plane says the property is there — 459's silent panic is named by the instruction that took it, and the OS's own `fromPath` fails at the *first component*** — `fleh_undef`'s whole report had gone through `entry_kv` into the 8 KB buffer the `t268_*` kalloc tracer fills (`kv_written = kv_in_dram = 0x1fea`, 26299 refusals on 459), so all 47 of its keys were written and dropped in silence; 461 splits the writer (`entry_kv_into(buf, len, dropped, max, …)` — one definition of how a record is spelled) behind `entry_kv`/`entry_panic_kv`, gives the trap `g_panic_buf`, 8 KB of its own, cleaned by address and *separately* (its neighbour is the payload's 256 KB RAM disk), printed under its own heading, with `g_panic_entered` so "the handler ran and wrote nothing" and "it never ran" are different logs, and moves the epilogue's four post-MMU keys too. **The record is complete for the first time:** `xnu_entry_panic_len = 0x13b5` (5045) with `_dropped = 0`; `xnu_entry_undef_pc = 0x80030f1c` (the `udf` in `DebuggerTrapWithState`, `debug.c:371`) with `_lr = 0x80030f20`; `xnu_entry_trap_r9_fmt = 0x8048bb98` = `"IOFindBSDRoot: specified root memory device, %s, has not been configured\n"` (`IOKitBSDInit.cpp:490`) with `arg0`'s first words `'m','d','0',0`, and `xnu_entry_panic_str = 0` — the *format in a register* is the road that works, so 459's inference from a one-statement window becomes a named instruction. **And the root-device chain is correct on the device all the way to the registry:** `gIODTPlane = 0xc05d6848` (the same pointer the path records carry), the tree's root nub `0xc060d130`, the instrument's own `fromPath("/chosen/memory-map", gIODTPlane)` `0xc060d2e8`, `getProperty("RAMDisk")` answering (`obj = 0xc0626580`, bytes `0x806e6998`) with `0x80512000` + `0x40000` — the payload's own RAM disk, so the blob→plane link is not the failure. `getProperty` is virtual and cannot be `--wrap`ped (455's negative result), so the instrument *calls* `_ZNK15IORegistryEntry11getPropertyEPKc` and `_ZNK6OSData14getBytesNoCopyEv` by mangled name, and a new check fails the build if any of the three mangled names is in pass 1's undefined set — a name nothing defines is a name the stub generator invents a stand-in for, and a misspelling would then look like a finding about the device. **The frontier:** the whole boot makes exactly two `fromPath` calls, both the OS's own from `IOFindBSDRoot` (`0x802067f4` for `/chosen` at `:404`, `0x8020696c` for `/chosen/memory-map` at `:440`, both on plane `0xc05d6848`, **both returning 0**), `mdevadd` is never called and `mdevlookup(0)` returns `0xffffffff` — and **`/chosen` alone does not resolve either**, so the failure is at the first component, the meta root's IODT child lookup, not a shape or a name difference (460's replay had cleared the latter). The run is byte-identical to 459's OS block (`ostext_chars = 0x399` = 921, `_lines = 0x13` = 19, `block_count = 0x22`, `_returned = 0x0c`), so the step measured the run it was built for rather than perturbing it; `xnu_entry_zone_map_min/_max` are non-zero for the first time (`zalloc.o` and `kext_alloc.o` are in the pool, so `zone_init` ran, which retires the comment that predicted otherwise); and 448's `_bad` slots turn out to be inverted for three of their four functions, which is the ledger's "a measurement can be the thing that is wrong" waiting to happen. Entry image 5291016 → 5307400 (`.bss` +8192 to 0x805a5f38), payload 8327168 md5 `e50df27f9a8dc52d287e264e21f1a19d`, 39 wraps (35 reached / 2 same-object / 1 never-called / 1 by-address, none dead), log **465077 bytes**, exit 0, device back on Android by itself. **Next (462), three readings at the failing moment:** `IORegistryEntry::getRegistryRoot()`, `gRegistryRoot->getChildEntry(plane)` (`0xc060d130` attached / 0 detached-or-freed), and the instrument's own `fromPath("/chosen", plane, 0, 0, 0)` as the control. |
| stage90 | [experiment-462](experiment-462-the-delete-list-was-every-entry.md) | **the delete list was every entry, and the boot mounts its root filesystem** — `MSM8974PlatformExpert::deleteList()` answered `(const char *)0`, and to `IODTFindMatchingEntries` (`IODeviceTreeSupport.cpp:886`) a NULL key list is not "no names" but the *else* branch `else result->setObject(next)` — collect **every** entry — so `IODTPlatformExpert::processTopLevel` (`IOPlatformExpert.cpp:1322`, `options = 0`, no `kIODTExclusive`) called `next->detachAll(gIODTPlane)` on all of `rootEntry`'s direct IODT children and removed the device tree's whole top level from the IODT plane, **`chosen` included**. Answering `"()"` — the OSUnserialize list grammar's empty list (`OSUnserialize.y:168` is `array: '(' ')' { $$ = NULL; }`, so it parses to a non-NULL empty `OSArray`, the only thing `IODTMatchNubWithKeys` tests; the same spelling answers `excludeList()`, where 0 was always the meaning) — and the tree survives its own platform expert: `xnu_live_pexpert_kids_before=0x15` and `_after=0x15` (the blob's own `apple_dt_root_children=0x15`), `xnu_live_deletelist_len=0x2` read out of the image at the returned pointer `0x8049a728`. **The whole root-device chain then runs**: the three `xnu_live_walk_seq` records read t1 `first=0xc04e1f78` (the tree's root node, from the `IODeviceTreeAlloc` wrapper) and t2 `first=0xc04ad0a0` (= the root nub = `xnu_live_pexpert_prov`) with the same `set=0xc04e9520` and `kids=0x15` both times — `IORegistryEntry::init(old, plane)` moving the nodes plus `attachToParent` putting the meta root above them, the two moments 461 could only bracket; the OS's own two `fromPath` calls now return non-zero (`/chosen` `0xc04e28c0` at `0x802069f4`, `/chosen/memory-map` `0xc04e2188` at `0x80206b6c`); `xnu_live_mdevadd_*` has `base=0x00080512` `size=0x00000040` `ret=0x01000000` and `xnu_live_mdevlookup_ret=0x01000000`; and **XNU's own console ends with two lines 461's block does not have** — `Added memory device md0/rmd0 (01000000/0B000000) at <ptr> for 0000000000040000` and `BSD root: md0, major 1, minor 0` — `base << 12 = 0x80512000` the payload's own 256 KB RAM disk (`<ptr>` is `doprnt_hide_pointers`, `osfmk/kern/printf.c:584`), `size << 12 = 0x40000`, and `makedev(1,0) = 0x01000000`; 459's `panic("IOFindBSDRoot: specified root memory device, %s, has not been configured")` is retired by measurement. **The frontier is the next statement**: `IOSecureBSDRoot` (`bsd_init.c:966`, the first line after the mount loop's `break`, so reaching it *is* `vfs_mountroot() == 0`) opens with 30 s `waitForMatchingService(serviceMatching("IOPlatformExpert"))` and blocks the boot thread — three `xnu_live_iolock_*` records whose `_dl_lo - _now` are 575999982/575999988/575999988 = 576000000/19.2e6 = 30.000 s exactly at the GPT's own 19.2 MHz, the last (`xnu_live_iolock_site=0x801372c8`, `IOService::waitForMatchingService+0xe0`, `dict_seq=4` at `IOSecureBSDRoot+0x30`) never returning (`block_seq=0x3c` `enter=0x800152fc` = `lck_mtx_sleep_deadline+0x8c`, `now=0x06b52513`, nothing else on that thread) while the payload's 25 s watchdog resets the device ~19 s early. Also: the two methods' claim ("the empty one", since 363) was **a claim in a comment**, now `tools/check_platform_lists.py` — new, run by `tools/build_xnu_arm_kernel.sh` against `PLATFORM_SOURCES` itself, failing on NULL, the empty C string, any non-literal, a name bound to two values, and a file with fewer than two definitions (the stub generator would supply the other and the link would succeed); 448's `_bad` slots are inverted for three of four functions, still owed. Also measured: `xnu_live_ostext_chars = 0x3ee` (1006) against the captured block's 1033 bytes and 461's 921 — **the counter and the bytes part company by a few dozen characters and this step does not reconcile them**, 1006 is the number to quote; and **the report never ran** (neither `entry_epilogue` nor `fleh_undef`), so every reading above is a *live* record — 455's warning, demonstrated twice. Two discrepancies recorded rather than smoothed: the `ostext` counter/block gap, and the block tracer's `returns=12` against 63 enter records with only 3 `__wrap_thread_block` sites. One invocation worth writing down: `--platform-only` with `XNU_KERNEL_CONFIG` alone exits 2 with three `DISAGREEMENT …` lines that are about **RELEASE**'s `-D` list, not `STAGE90_XNU`'s — `XNU_MASTER_LOCAL=$PWD/tools/xnu_config/boot/STAGE90_XNU.local` is the other half. Entry image 5307400 → 5307400 (`.text` 5097184 → 5099072, +1888, inside the 1016 bytes of slack below `__bss_start`; `.bss` and headroom byte-identical to 461; 13 new globals at 0x80511154..0x805111f4), payload 8327168 md5 `e0cc79991502cebb2227233176296f60`, 39 wraps (35 reached / 2 same-object / 1 never-called / 1 by-address, none dead), log **447772 bytes**, exit 0, device back on Android by itself. **Next (463): the wait's own predicate** — wrap `waitForMatchingService` (caller, dictionary, timeout, return); inside it, when `IOProviderClass` is the `IOPlatformExpert` symbol, call `copyExistingServices(dict, kIOServiceMatchedState, kIONotifyOnce)` by mangled name and record the answer; and the platform expert's own `__state[0]` at the end of its `start` — the two candidates (`OSMetaClass::applyToInstancesOfClassName`'s registry set, `OSMetaClass.cpp:817-823`, and the `inState == (service->__state[0] & inState)` bit test) are both consistent with the source and neither is ruled out. |
| stage90 | [experiment-463](experiment-463-the-wait-has-no-instances.md) | **the wait has no instances — and both mechanisms 462 named are unreachable, because the set they filter is empty** — 463 `--wrap`s `IOService::waitForMatchingService` (two records per call, `ent=0` before the real call and `ent=1` after, so a wait that never returns still says so) and inside it asks the wait's own predicate by mangled name: `copyExistingServices(dict, kIOServiceMatchedState, kIONotifyOnce)` (`wsvc_p4`), the same with the state filter off (`wsvc_p0`), the dictionary's `IOProviderClass` object, `getMetaClassWithName`, `getClassName` (read as two words, stopping at the terminator), and **`applyToInstancesOfClassName(sym, applier, 0)` with an applier that is `instanceMatch` with its filters removed** — the same call `copyExistingServices` makes at `IOService.cpp:4307`, so `wcls_seen` counts the instances `instanceMatch` would be entered with, and 0 is not "the state test failed". **The control is in the same log**: `IOFindBSDRoot`'s wait for a service that has matched the `IOBSD` resource **returns** the resource root (`xnu_live_wmatch_seq=2 ent=1 ret=0xc04bcd10`, = `getResourceService()`), so the recording reads a success too — and it returns *through the sleep*, because its own first call (`wsvc_p4=0`, the same three arguments as `waitForMatchingService`'s first statement) found nothing: what satisfied it is a registration of the resource root (`xnu_live_finddrv_seq=2 site=0x801359d0` = `doServiceMatch+0x3c`, `svc=0xc04bcd10`, whose `registerService` ends `setProperty(gIOResourceMatchedKey, resourceKeys)` + `copyNotifiers(gIOMatchedNotification, kIOServiceMatchedState, 0xffffffff)`, `IOService.cpp:3751`/`:3754`) between the two waits' own readings of `IOResourceMatched`: **2 entries with `gIOBSDKey` absent (`idx=0xffffffff`)** at the first wait's sleep, **5 entries with it at index 4** at the second's, with `publishResource("IOBSD")` (`IOKitBSDInit.cpp:95`) as the `pub2` record just before the first. **The frontier wait never returns**: `dict_seq=4` at `IOSecureBSDRoot+0x30` builds `{IOProviderClass: "IOPlatformExpert"}`, the probe resolves the name to the metaclass `0x8058b4f0` with the class's own bytes in the log (`0x6c504f49`/`0x6f667461` = "IOPl"/"atfo"), and `xnu_live_wcls_seen=0` / `wcls_shown=0` with `p4=p0=0` and no `ent=1` record at all — `OSMetaClass::addInstance` has exactly one caller (`registerService`, `:3694`) and `IOService::startCandidate` (`:3479-3517`) registers nothing, so nothing in this tree publishes an `IOPlatformExpert`. The platform expert's own last two statements corroborate it: `xnu_live_pexpert_self=0xc04bf800`, `xnu_live_pexpert_state=0x00000000` — bit 1 (`kIOServiceRegisteredState`) clear at the end of `start`, against the walk's `seen=0` five milliseconds later, after `start` returned and after the mount. Three sleeps on `gNotificationLock`, all at `waitForMatchingService+0xe0` (`0x801377c8`; `0x801372c8` in 462's build — **XNU's text moved 0x500 with the entry image's growth, so an address is only meaningful against its own build's ELF**, defect 179), each `dl−now = 575999988` = 30.000 s − 12 ticks at 19.2 MHz; the first is `IOKitInitializeTime`'s `waitForService(resourceMatching("IORTC"), &t)`, which has **no `wmatch` record because `waitForService` calls `waitForMatchingService` from inside the same object** where `--wrap` cannot reach (455) — two waits with call records, three sleeps. The two waits' sleeps are 20773 ticks (1.081 ms) apart, so `mdevadd`, both console lines, `mdevlookup` and the whole mount fit in about a millisecond, and the second wait's deadline (34.074 s) is ~21 s past the payload's 25 s watchdog; the mount is real (`vfs_mountroot() == 0` broke the loop, no `cannot mount root` and no `mount(N) failed`, and the build's own `vfstbllist` check names **mockfs**). Build: entry `.text` 5099072 → 5102144 (+3072), image bytes 5307400 unchanged, `.bss` 0x80510000..0x805a5f38 and `headroom` 1417416 byte-identical to 462, payload 8327168 md5 `0913ed254e4c933019beeff2fcb12b7e`, 40 wraps (37 reached / 1 same-object / 1 never called / 1 by address, none dead), pass 1 = 27 undefined; **and `build_entry.sh` re-run after the run reproduces `xnu_arm_entry.bin` byte for byte (`cmp` clean, md5 `96661f098cb86cedecc09cad7c865657`)**, with run b's four call-site addresses resolving to the expected symbols against that ELF. Two defects found, both recorded: the first run's platform-expert edit **was never compiled** (the platform objects come from `tools/build_xnu_arm_kernel.sh`, a different script with its own two env vars; now checked by `platform_obj_fresh()`, exit 2 with the exact command), and the console block's *position* in the log is its **reserve time**, not its print time (`entry_os_reserve` reserves 0x20000 bytes and fills it while the records append above it — which is why the post-mount `Added memory device`/`BSD root` lines sit before the first wait's records), so the log's order is a total order over all threads: the platform expert's `start` (4150→4378) brackets the boot thread's `bsd_init:728` IORTC wait (4189→4225), and `commpage_populate`'s `ml_get_max_cpus` call blocks its thread from 3972 until `ml_init_max_cpus(1)` at 4379. Next (464): the registration itself — `registerService()` on the platform expert, predicted `wcls_seen=1` with `winst_inst=0xc04bf800`, `winst_state` bit 1 set, and an `ent=1` record with a non-zero return. |
| stage90 | [experiment-464](experiment-464-the-registration-and-the-first-process.md) | **the registration, and the boot bootstraps its first process** — 463's frontier retired by one statement: `MSM8974PlatformExpert::start` now ends with `this->registerService()`, the statement Apple's own closed-source platform experts carry and the tree's own callers (`IOStartIOKit.cpp:167`'s root nub, `IOPlatformExpert.cpp:205`/`:1306`/`:1340`, `IOService.cpp:3543`) do not cover for an `IOPlatformExpert`. **The registration is a job, not a call, and the log says so rather than the source**: `xnu_live_pexpert_state_reg=0x00000000` with a `--wrap=kernel_thread_start` record *between* it and `xnu_live_pexpert_reg_seq=0x00000001` — `kthread_cont=0x801355f4` = `_IOConfigThread::main+0x0`, `ret=0` — because `startMatching`'s `sync` is false (the root nub's own `registerService()` cleared its synchronous bit) so `_IOServiceJob::startJob` enqueues `kMatchNubJob` and `pingConfig` creates the config thread. No driver is instantiated: personalities are filed by `IOProviderClass` (`IOCatalogue.cpp:124-132`) and this image's three are under `IOPlatformExpertDevice`/`IOResources`, so `findDrivers` is empty and `probeCandidates` is skipped (`IOService.cpp:3720`) — 362's `IOPanicPlatform` panic stays out of reach (and the expert's own `findDrivers` is *deliberately* absent from the log: 455's filter records only `getResourceService()`). **All three of 463's predictions held**: the walk over `IOPlatformExpert` (`wcls_meta=0x8058b4f0`, the same address as 463's) is `wcls_seen=1`/`wcls_shown=1` with `winst_inst=0xc06088c0` and `winst_state=0x0000001e` (bits 1,2,3,4 — `addInstance` + `kIOServiceRegisteredState` at `:3702` + the two `copyNotifiers` at `:3695`/`:3753`/`:3756`); the wait's own first call answers (`wsvc_p4=wsvc_p0=0xc06088c0`); and the wait **returns** — `xnu_live_wmatch_seq=4 ent=1 ret=0xc06088c0` at caller `IOSecureBSDRoot+0x44` — so `pe->callPlatformFunction("SecureRootName", …)` runs, answers `kIOReturnUnsupported` (`IOPlatformExpert.cpp:1212-1228`: `getResourceService()` has no such property), no `mdevremoveall`, and `bsd_init` continues. **Two sleeps now, not three** (both `waitForMatchingService+0xe0`, each `dl−now=575999988` = 30.000 s − 12 ticks at 19.2 MHz, 246651 ticks apart), and 463's account of wait 2 is refined by two records: at its sleep `rm=0xc0634a20 count=2 idx=0xffffffff` (IOBSD still absent — `publishResource("IOBSD")` sets a *property* named IOBSD, `IOService.cpp:3534`, and the array is rebuilt by the resource root's own `doServiceMatch` tail at `:3751`, itself enqueued as a job), so the answer arrives through the sleep. **The new frontier is the first pthread-table slot the boot reaches**: `stub_hit=stage90_pth` (truncated) with `xnu_entry_stub_caller_v=0x800d9bb8` = `forkproc+0x5a8` -> the `bl` at `forkproc+0x5a4` = `bl pth_proc_hashinit` (`kern_fork.c:1393`, `#if PSYNCH`), through the 20-byte shim at `0x801f0a5c` (`pthread_shims.c:360`, `bx` to the table word at offset **0x20** (word 8; this row said +0x18, which is `workqueue_mark_exiting` — corrected by 465, defect 185)) to this project's 16-byte stand-in at `0x80269f4c` — reached from `bsd_init.c:1033`'s last statement `bsd_utaskbootstrap()` -> `cloneproc` (`:1144`, `kern_fork.c:966`) -> `forkproc`: **the whole of `bsd_init` ran and the boot is bootstrapping process 1**. The 8 KB kv buffer was full (`written=in_dram=0x2000`, `dropped=0x8080` = 32896 refusals) so the stub's own name is cut (defect 181: `entry_stub_hit` still shares the tracer's buffer — 461's fix, one channel over); the identity comes from the caller and the disassembly. XNU's console block is **byte-identical to 463's** (1051 bytes, 22 lines, ending `BSD root: md0, major 1, minor 0`; `ostext_chars=total=0x3ee`), the block count is the same 60 with no block in the post-wait path (the last is 11865 ticks after the second sleep's entry), no `exception:`, `abort_entries=0`, log 474170 bytes, exit 0, device back on Android 10 by itself. Build: `.text` 5102144 -> 5102304 (+160, and the metaclass address `0x8058b4f0` unchanged), image bytes 5307400 unchanged, `.bss`/`headroom` byte-identical to 462/463, payload 8327168 sha256 `84acb13641eeb3fe5ea63c8cbea7f3ba3b86383e36da6399de851128718979df` (this row said md5; the build prints it with `sha256sum` — corrected by 465, defect 186), 40 wraps (37/1/1/1, none dead), pass 1 = 27 undefined, and the entry blob rebuilt after the run is byte-identical (`4806a0a6f0aa186824e78202a56ce1cb`). **Next (465):** give `pth_proc_hashinit` a body, and put the terminal `stub_hit` record on the live channel first so a full buffer cannot cut the one name a stop depends on. |
| stage90 | [experiment-465](experiment-465-the-first-slot-and-the-record-that-cannot-be-cut.md) | **the first pthread slot gets a body, and the terminal record stops being cuttable** — two changes: `stage90_pthread_functions.c` gives `pth_proc_hashinit` a hand-written body (433's `pthread_init` shape: declared below the macros, defined below the table, set explicitly as `.pth_proc_hashinit = &stage90_pthread_slot_pth_proc_hashinit`), and `entry_stub_hit` writes the terminal record to the **live channel first** — `xnu_live_stub_hit_seq`, `_name_ptr`, `_name_w0`/`_w1` (the name's first eight bytes), `_caller` — because 464's stop was named only by a truncated `stub_hit=stage90_pth`. **The slot's body ran, and every prediction held**: `xnu_live_pth_hashinit_seq=0x00000001`, `_p=0xc057db80`, `_tbl=0x80499978` = `nm`'s address for `stage90_pthread_functions` to the digit, no stop at the slot, and the same value on the live channel from 433's body (`xnu_live_pthread_init_ptr=0x80499978`). The body leaves `p->p_pthhash` NULL (the psynch consumer is a closed kext that is not here), does not dereference `p`, and does not call the callbacks table — `forkproc` ignores the return, so it cannot change what the boot does next. **The frontier is `thread_bootstrap_return`** — `osfmk/arm/locore.s:1897`'s `load_and_go_user`, the ARM port's return-to-userland path, the last gap before user mode — reached from process 1's own thread: `fork_create_child` (`kern_fork.c:811-814`) creates it with continuation `task_wait_to_return` (`osfmk/kern/task.c:478-503`), `bsd_utaskbootstrap`'s tail `task_clear_return_wait` releases it, `Call_continuation` (`cswitch.s:107`, entered from `pcb.c:291`) `blx`s it, and its tail ` b 8045a104 <thread_bootstrap_return>` is a tail branch — which is why the recorded caller is `Call_continuation+0x1c` (the `blx`'s return address) rather than an address inside `task_wait_to_return`. **Three instrument fixes, each with its own number in the report**: the off-by-one that made 464 write `g_kv_buf[g_kv_len] = '\0'` at `&g_kv_buf + 8192` = `0x80558000` = the first byte of the kernel's `ExceptionVectorsTable` (loop bound `+2u` now; `kv_written=kv_in_dram=0x1fff` = 8191); the `_w0`/`_w1` probe that read 25 bytes **past** the buffer and printed neighbouring `.bss` as the caller's digits (now gated by `xnu_entry_stub_caller_in_buf`, which reads `0`, with `_w0`/`_w1` = 0); and the terminal record's channel. **The tracer's buffer turns out to have been full since the IOKit bring-up** — 464's dump holds only early allocations (`0xc05c0520`..`0xc05d9800`) against the frontier's own objects at `0xc06062e8`/`0xc06088c0`, so `xnu_entry_stage90_pthread_functions_ptr` (433's record for `bsd_init:798`'s `pthread_init()`) is absent from 464 **and** 465 while the live copy of the same value is present in 465: one call, two channels, one absence — an absence read as an event is defect 187. XNU's console block is **byte-identical to 464's** (1050 bytes to the end of the `BSD root` line, 22 lines; report `ostext_chars=total=0x40a`/`_lines=0x15`, live `0x3ee`/`_tank=0x262` — 464's numbers to the digit), no new OS text because the stop is before the return to user mode, `abort_entries=0`, `panic_entered=0`, one `stub_hit`, 66 thread blocks over 45 threads against 60 over 45, log 477837 bytes, exit 0, device back on Android 10 by itself. Build: `.text` 5102304 -> 5103168 (+864), image bytes 5307400 unchanged, `.bss` `0x80510000..0x805a5f58` (+32), `g_kv_buf` still `0x80556000` and `ExceptionVectorsTable` still `0x80558000`, 40 wraps (37/1/1/1, none dead), pass 1 = 27 undefined, table word 8 = `0x8026a13c` read out of the ELF, and the shim's offset is `[r1, #32]` = **0x20/word 8** (464's doc and row said `#0x18`/word 6, which is `workqueue_mark_exiting` — corrected here, defect 185; the row's 64-hex payload hash is also a **sha256** misnamed md5, defect 186). Entry `.bin` md5 `ca3cebfbf465e1450a690fc9b905b34e`, embedded verbatim at payload offset `0x791b4`; payload `stage90-qcdt.img` sha256 `19b0e5b19d05cef09dba3f10237c79c4ca27c565cc8f15e63c94ca7c9686465e`. **Next (466):** `thread_bootstrap_return` — a body cannot answer it (a stand-in that returned would go back into `Call_continuation`'s `thread_terminate` path), so the question is that path's closure and whether process 1 has a `uthread` to return to. |
| stage90 | [experiment-466](experiment-466-the-arm-layer-gets-the-configuration-and-the-stop-is-a-recoverable-fault.md) | **the ARM layer is assembled with the configuration, and the boot's first stop is a fault the kernel is built to service** — two changes. **One: the seventeen `.s` files of the ARM manifest had never been given the kernel's own options.** Every C object gets `make_defines.sh <CONFIG>` (108 flags for RELEASE); `tools/assemble_arm_layer.sh` and `stages/stage90/xnu_arm_assemble.sh` passed the toolchain flags and nothing else, so every `#if CONFIG_*` in the tree's assembly was false while the same macro was 1 in every C object — and one file consults them: `osfmk/arm/locore.s`, assembled to call `timer_state_event_user_to_kernel` and `timer_state_event_kernel_to_user`, which `osfmk/arm/machine_routines.c:1077-1107` does not compile, so they were undefined in *both* links (`out/link/RELEASE-measure-undef.txt:42,43`) and no object in the pool could have supplied them. One source now (`tools/xnu_config/arm_asm_defines.sh`), the control is the whole-kernel link's undefined count **37 -> 35** with exactly those two names gone, `SLIDABLE` is the one named exception (MASTER.arm says 1, `globals_asm.h:29` cannot assemble that, so both scripts keep 0 and record the disagreement), and re-assembling moves four objects (`locore` 12488->12560, `cswitch` 564->568, `caches_asm` 596->600, `start` 1040->1044) and leaves thirteen byte-identical. **Two: `locore.o` is linked with its seventeen globals renamed** (`locore_*`) because twelve collide with the image's own vectors, which retires the three names 465 stopped at: `thread_syscall_return=0x80014f68`, `thread_bootstrap_return=thread_exception_return=load_and_go_user=0x80014f98`; pass 1's undefined set 27 -> **24**. The rename is a rule with both ends checked — the kept names must be *defined by this object* and *referenced by another object in the link*, two measurements neither of which is the other's output — and the renamed set is asserted name for name. **The run: not one stand-in was entered in the whole boot** (`xnu_entry_stub_hit_count=0`, no `xnu_live_stub_hit_*`, and `tools/xnu_entry_callwalk.py --root bsdinit_task` says "reached no stub on the straight-line path"), and XNU's own console block grows by one line that names the frontier: `load_init_program: attempting to load /sbin/launchd` (`kern_exec.c:5164`), reached through `load_and_go_user` -> `ast_taken_user` -> `bsd_ast` (`kern_sig.c:3445`) -> `bsdinit_task` -> `load_init_program` (`bsd_init.c:1079`). **And the stop is not a stub at all: it is the boot's first data abort, on a fault XNU services by design.** `dfar=0x1000`, `dfsr=0x805` (`FSR_SFAULT` + `DFSR_WRITE`), `pc=0x80010fc0` = the first store of `copyout`'s bytewise path (`machine_routines_asm.s:706`), which is `load_init_program_at_path`'s `copyout(path, argv0, 15)` (`kern_exec.c:5020`) with `argv0 = scratch_addr` — and `0x1000` is a source-level fact, not an inference: `mach_vm_allocate_kernel`'s ANYWHERE case does `if (map_addr == 0) map_addr += VM_MAP_PAGE_SIZE(map)` (`vm_user.c:182-184`) and `VM_MIN_ADDRESS` is 0 for ARM32 (`osfmk/mach/arm/vm_param.h:143`), so the first page of process 1's empty map is `0x1000`. XNU's own handler is `fleh_dataabt` (`locore.s:992`) -> `dataabt_from_kernel` -> `sleh_abort(regs, T_DATA_ABT)` (`locore.s:1140`), and `trap.c:274` takes `map = thread->map` and calls `vm_fault(map, 0x1000, READ|WRITE)` before returning to **retry the `strb`** — all of it in this image (`sleh_abort=0x80437528`, `vm_fault=0x800936e4`) and none of it running, because the installed `fleh_dataabt` is the instrument's (`entry_stubs.c:5030`, `0x80006a3c`) while XNU's sits renamed at `0x800146d0` as `locore_fleh_dataabt`, referenced by nothing. **Two defects, both caught by the build**: the pool glob adopted a hand-assembled scratch object sitting in `out/xnu_asm_obj/` (the link failed three steps later on `_start` defined twice; the pool counted 435 where every other build counts 434) — the fix is structural, a `<name>.o` with no `<name>.log` beside it was not written by the assembler and is now reported and skipped; and the keep-name check as first written asked the freshly-written `xnu_arm_entry_undef.txt` whether the three names were in it, i.e. its own output, so it passed once and failed on its own success. Entry image `.text` 5103168 -> 5118848, image bytes 5307400 -> **5323784** sha256 `c23bcee4809590091f620cc6251371daef62d3d2cb7d1f6e283a68a732dbd76f`, `.bss` `0x80514000..0x805a9f58` (614232, unchanged), headroom 1401000, 40 wraps unchanged; payload `stage90-qcdt.img` **8343552** bytes md5 `fd1dff405a01033822e9d3a844a6a7fd`; one `fastboot boot`, log 478248 bytes, `No errors detected`, exit 0, device back on Android 10 by itself. **Next (467): the vector table, so that a page fault can be a page fault** — install the kernel's own first-level handlers for the aborts the boot now takes on purpose, which means the ten `locore_fleh_*` names have to become reachable from whatever patches the vector page (`ExceptionVectorsTable`, `0x8055c000`) with the instrument's report kept on the paths XNU itself treats as fatal. |
| stage90 | [experiment-467](experiment-467-the-vector-page-gets-the-kernels-data-abort-handler-and-the-boot-enters-the-exec-path.md) | **the vector page gets the kernel's own data-abort handler, the fault 466 stopped on is serviced and retried, and the boot enters the exec path** — one change: slot 4 of `entry_vectors.s`'s eight trampolines now loads **`locore_fleh_dataabt`**, the renamed copy of Apple's `locore.s:992` handler that 466 linked and nothing referenced, instead of this image's `entry_stubs.c:5030` `fleh_dataabt` which records a fault and leaves. It is the one slot that is not the instrument's, and the reason is that the frontier moved: while the first fault *was* the end of the run, recording it was the point; on a demand fault into a page the kernel has just created a map entry for (`mach_vm_allocate_kernel`'s ANYWHERE case handing process 1 `0x1000`), recording it **is** the stop. XNU's handler reads the fault, calls `sleh_abort(regs, T_DATA_ABT)` (`locore.s:1140`) and, when the page is paged in, returns through `load_and_go_sys` and **retries the instruction**. The report is not lost, and that is a property of XNU rather than of this file: a `sleh_abort` that cannot answer the fault panics (`trap.c:313`, `:393`, `:464`) and `panic()` reaches `DebuggerTrapWithState`'s `udf`, which is slot 1 — still this image's `fleh_undef`, still writing the trap's own buffer. What the kernel *decided* is measured on the other side by **`--wrap=sleh_abort`** (41 wraps, was 40), and the wrap is reached: `objdump -d` finds four `bl __wrap_sleh_abort` sites, two in `locore_fleh_prefabt` and two in `locore_fleh_dataabt`, which is where `locore.s` calls it. The build check reads the eight `vec_tramp_N_handler` literals out of the ELF by index into `objdump -s`'s 16-byte lines (they are 4-byte aligned, not 16), asserts each is inside the one page mapped at `0xffff0000`, compares each against `nm`, and checks **slot 4 in both directions** — it must be `locore_fleh_dataabt` and must not be `fleh_dataabt` (`0x80006c18`) — because the failure this step removes is the second one. **The run: three data aborts, all serviced.** `xnu_live_sleh_seq` 1/2/3, all `type=4` (`T_DATA_ABT`), thread `0xc0672080`; the first is 466's exactly (`dfsr=0x805` `FSR_SFAULT`+write, `dfar=0x1000`), the other two are `0x807` (`FSR_PFAULT`) writes at kernel addresses `0xc82dd000` and `0xc831e000`; **`xnu_entry_sleh_back=3`** — every entry returned — and the live channel's records go 859 -> **880**, a delta of exactly the 21 records the new instrument writes (3 aborts x (5 on entry + 2 on return)). That the first fault was answered and not merely reported is what the OS console says: it gains one line over 466 and it is two frames into the function the fault was in — `load_init_program: failed loading /sbin/launchd: errno 8` (`kern_exec.c:5169`), after `load_init_program_at_path`'s three `copyout`s and its `execve`; the tracer's own `_dropped` goes 33017 -> **33079**, 62 more recorded events than 466 reached, and `stub_hit_count=0` says none of them was a stand-in. **The stop is XNU's own named panic, one level below the fault.** `panic_entered=1`, `panic_len=0x1295` with `_dropped=0`, and the trap is the predicted `udf`: `undef_pc=0x800355ac` in `DebuggerTrapWithState` (`800355ac: e7ffdefe udf #65006`), `trap_r9_fmt=0x804a0766` = `"Process 1 exec of %s failed, errno %d"`, `panic_arg0=0x804a0919` = `"/sbin/launchd"`, `_arg1=8`. **`errno 8` is `ENOEXEC` and it is not the malformed-Mach-O answer** (`load_return_to_errno` maps `LOAD_BADMACHO` to `EBADMACHO`, `kern_exec.c:5203`): it is "Image not claimed by any activator" (`kern_exec.c:2803-2805`), because all three activators decline a file whose first four bytes are zero — `exec_mach_imgact` (`:855`), `exec_fat_imgact` (`:643`), `exec_shell_imgact` (`:477`, `:482`), each returning `-1`. The bytes are known without guessing: **the RAM disk is `g_stage90_ramdisk`, 0x40000 bytes of `.bss`, and nothing in this build writes a byte of it**, while mockfs serves that file node straight out of the memory device's physical pages (`mockfs_fsnode.c:333-342` `pager_map_to_phys_contiguous(..., mockfs_memdev_base << PAGE_SHIFT, fsnp->size)`, size from the device at `:80`) — so `/sbin/launchd` is not missing (`mockfs_lookup` maps `sbin` to the root and `launchd` to the file node, `mockfs_vnops.c:119-124`) and not unreadable: `namei`, `exec_check_permissions` and a page read all succeed, and then the bytes are zeros. **Two defects, both caught by the build**: twenty-five `entry_write_kv` calls appended to `entry_epilogue` put its constant pool exactly 4096 bytes past `ldr r0, .L277` — one byte outside the +-4095 PC-relative range, the same edge 455 hit, found by the same `-S` reading of an error that names a compiler output line rather than the function that grew — fixed by moving the keys and their comment into `entry_write_269_kv()` called at the position they occupied, so the key order is unchanged (`.text` 5118848 -> **5119200**, +352); and the new check itself failed first with `objdump: --stop-address: bad number: 80001f10` because its own address was printed without the `0x` its neighbour had, and it is recorded rather than quietly fixed because a check that exists to fail on a wrong vector page instead failed on the shape of its own invocation. Entry image image bytes **5323784 unchanged** sha256 `fe3d9ac551a3e1d8bc682b20d15021905ce18f75ad0a7de68c44cc6dffb704b3`, `.bss` `0x80514000..0x805a9f58` (614232, byte-identical: the six new globals at `0x80515068` sit before `g_stage90_ramdisk`, whose 4096-byte alignment absorbs them), headroom 1401000, pass 1 24 undefined unchanged, `g_kv_buf` still `0x8055a000`; payload `stage90-qcdt.img` 8343552 bytes md5 `b2176ce70f9be60a862c3d9b3ad0e311`; one `fastboot boot`, log 483896 bytes, `No errors detected`, exit 0, device back on Android 10 by itself. **Next (468): a Mach-O for process 1** — mockfs's file node *is* the memory device's physical pages, so whatever occupies offset 0 of `g_stage90_ramdisk` is `/sbin/launchd` byte for byte, and a static ARMv7 `MH_EXECUTE` there is the difference between `error = -1` at `kern_exec.c:855` and the image being claimed; that reaches `load_machfile`, the per-segment mappings and `thread_bootstrap_return` with a user PC for the first time. |
| stage90 | [experiment-468](experiment-468-the-ram-disk-is-a-mach-o.md) | **the RAM disk is a Mach-O, and the ARM layer was a configuration behind** — `entry_ramdisk.s` writes the bytes `g_stage90_ramdisk` *is*, i.e. `/sbin/launchd`: a static ARMv7 `MH_EXECUTE` (header, `__PAGEZERO`, `__TEXT` at `[0x1000,0x2000)` R\|X, one `LC_UNIXTHREAD` with `pc = 0x10e0`, `cpsr 0x10`, `sizeofcmds 0xC4`) whose entry point is one `udf #0`; asserted twice, by three `.if/.error` pairs over label differences in the assembler and by `tools/host_ramdisk_macho_check.py` decoding the emitted bytes against `parse_machfile`/`load_segment`/`load_threadstate`'s rules with Apple's constants read out of the headers (22 mutations all refused under `--selftest`, negative control = 467's `.bss` image, refused by section kind). **And the run never reached it**: five `sleh_abort` panics with `pc = machine_load_context`'s `ldm r3!` and `lr = slave_main`, console stopped at `mbinit: done` (12 lines, 467 had 23), because `assym.s` is **per configuration** — `RELEASE` has `TH_KSTACKPTR 1464 / TH_CTH_SELF 1480`, this configuration `1480 / 1496 / 1504` — and `gen_assym.sh` + `assemble_arm_layer.sh` were hand-run steps nothing invoked, so the ARM layer was a configuration behind every C object. Found by disassembly, not by guessing. Fixed structurally in 471 by `tools/check_assym_cswitch.py` |
| stage90 | [experiment-469](experiment-469-the-pool-was-built-from-the-previous-configurations-table.md) | **the pool was built from the previous configuration's device table** — `device_table.py --write` ran *below* the manifest block that reads it, so `list_sources.py` always saw the table the previous build left behind: 440's defect in the other direction. Experiment 468's configuration selects `pseudo-device vndevice 4 init vndevice_init` (`config/MASTER:436`), the manifest written from the old table omitted `bsd/dev/vn/vn.c` and `shadow.c`, and `pseudo_inits[]` — generated from the *configuration*, which was current — named a symbol no object defined. **The first stand-in the boot has entered in four hundred experiments**: `xnu_entry_stub_hit_count = 1`, caller `0x80043ae4` = `bsd_init+0x354`, name `0x65646e76`/`0x65636976` = `vndevice_init`. Measured `733` selected files against the stale manifest's `731`, and `T vndevice_init` in the recompiled object. Fixed by moving the block, plus a new entry-build check that **every name `stage90_pseudo_inits.o` references is defined by this link's own objects** (`nm -u` on that object *is* the array's `ps_func` targets, by its `.data` relocations) with the defect itself as the negative control |
| stage90 | [experiment-470](experiment-470-a-stale-option-header-and-the-exec-that-killed-initproc.md) | **a stale option header, and the exec that killed `initproc`** — the boot reaches `load_init_program` for the first time: `Added memory device md0/rmd0 (02000000/0D000000) at 00000000804FD000 for 0000000000002000`, `BSD root: md0, major 2, minor 0`, then `/usr/local/sbin/launchd.development` → `errno 2` → `/sbin/launchd` **and no `failed loading` line**, which 467 printed (`errno 8`). One trap, in kernel mode, at the `udf` in `DebuggerTrapWithState` (`pc 0x800364c0`, `spsr 0x60000093`), `xnu_entry_panic_entered = 1` — **one entry, so no user-mode fault first** — and `r9` points at `unexpected SIGKILL of %s %s with reason -- ...` (`kern_sig.c:2138`), with `arg1 = ''` giving the `unexpected_failure` branch. The `badtoolate` label sets `error = 0` under *"just pretend we succeeded"*, so the console cannot tell success from failure there — 471 measures it. The defect this run also exposed: `out/xnu_options/STAGE90_XNU/development.h` held `#define DEVELOPMENT 0` while the configuration selects it (measured against `make_defines.sh`'s `-DDEVELOPMENT=1`), because `gen_option_headers.py` was **also** a hand-run step and `check_option_headers.py` compares structure, not values; fixed by generating the headers in the build, after which `bsd_kern_kern_exec.o` carries `U task_importance_update_owner_info` and `development.h` says `1` — and the rebuilt image is byte-identical in `.text`, `__bss_start` and size, which is the second statement that it was not the frontier |
| stage90 | [experiment-471](experiment-471-the-exec-failure-names-itself.md) | **the exec failure names itself, and it is a secure kernel refusing an unsigned file** — two `--wrap`s that read *arguments* rather than structures: `os_reason_create` (namespace and code, in registers, so `struct os_reason`'s private layout is never reproduced) and `load_machfile` (the independent reading). One device run: `xnu_entry_lmf_ret = 0x04` (`LOAD_FAILURE`), `xnu_entry_lmf_caller = 0x80284bb4`, `xnu_entry_osr_ns = 9` (`OS_REASON_EXEC`), `xnu_entry_osr_code = 1` (`EXEC_EXIT_REASON_BAD_MACHO`), `xnu_entry_osr_caller = 0x80284d98`. Not `LOAD_BADARCH`/`LOAD_BADMACHO`/`LOAD_NOSPACE`/`LOAD_IOERROR`, so the Mach-O's **shape passed** — and `parse_machfile` is `static`, so the remaining site is the one at the end of it, `mach_loader.c:1126-1130`: `if (!got_code_signatures) { if (cs_enforcement(NULL)) ret = LOAD_FAILURE; }`. Answered by the object: `nm` reports `cs_enforcement_enable` as **`R`** — the `const` of `kern_cs.c:82-84`'s `#if SECURE_KERNEL` — and the `cs_enforcement_disable` boot-arg string is absent, because `cs_init`'s `#if !SECURE_KERNEL` block is compiled out. `SECURE_KERNEL` is inherited through `RELEASE` → `BSD_RELEASE` (`config/MASTER.arm:24`), so **this project's configuration is a production secure kernel and cannot activate an executable without an embedded code signature at all**. Both new wraps are checked to be real symbols rather than stand-ins, and `tools/check_assym_cswitch.py` makes 468's defect structural |
| stage90 | [experiment-472](experiment-472-the-signature-gate-is-cancelled.md) | **the signature gate is cancelled, and the exec reaches the task transition** — one word in a list that already existed: **`-USECURE_KERNEL`** in `tools/build_xnu_arm_kernel.sh`'s cancellation list, beside `-UCONFIG_NO_PRINTF_STRINGS`, because **twenty-four files read the macro in all four spellings** (`#if SECURE_KERNEL`, `#if !SECURE_KERNEL`, `#ifdef`, `#ifndef`) so the spelling that looks equivalent, `#define SECURE_KERNEL 0`, flips only two of them — **leaving it undefined is the one form all four readers agree on**. The build now reads the object it just compiled instead of trusting the flag: `bsd_kern_kern_cs.o`'s `nm` type must not be `R` and the `cs_enforcement_disable` string must be present, measured `cs_enforcement_enable is B (writable) and cs_enforcement_disable is a boot arg` — `B` rather than `D` because `SECURITY_READ_ONLY_LATE(int) cs_enforcement_enable = DEFAULT_CS_ENFORCEMENT_ENABLE;` with `DEFAULT_CS_ENFORCEMENT_ENABLE` 0 (`CONFIG_ENFORCE_SIGNED_CODE` is not in the generated headers) puts a zeroed word in `.bss`. **On hardware the OS console is byte-identical through `load_init_program: attempting to load /sbin/launchd` and the stop has moved off the exec path**: `xnu_live_stub_hit_name_ptr = 0x804b3da9` reads out of `.text` as `stage90_pthread_functions.workqueue_mark_exiting`, `xnu_live_stub_hit_caller = 0x8029e764` resolves to **`load_machfile + 0x354`** (the return address of the `bl <workqueue_mark_exiting>` at `0x8029e760`, `load_machfile` at `0x8029e410`), and `xnu_entry_panic_entered = 0`. The caller key names `load_machfile` and not the shim because `bsd/kern/pthread_shims.c:337-340` is a one-line forwarder the compiler emits as a load plus `bx`, with no `bl` to write `lr` — 433's shape. **The position is the result**: `mach_loader.c:511` is inside the `in_exec` block opening at `:493` and immediately precedes `*mapp = map; return (LOAD_SUCCESS);`, so the magic, the CPU type and subtype, the two segments, the entry point and the code-signature gate all passed. **`xnu_entry_lmf_ret = 0` here is not a reading and the key beside it says why**: `xnu_entry_lmf_calls = 0` — a stop *inside* `load_machfile` precedes both wrappers' records, where a lone zero is indistinguishable from `LOAD_SUCCESS`. Falsifiers named before the run: a stop still at the signature site, or a `panic_entered = 1` with the `SIGKILL` format. **Measured:** 479736 bytes, 708 objects (C 625/626, the one failure `osfmk/kperf/kperfbsd.c`, off this path since 440), `.text` 5223520, image 5438068, `.bss` `0x8052fa80..0x805874c0`, undefined 26 |
| stage90 | [experiment-473](experiment-473-the-exec-succeeds-and-the-abort-storm-after-it.md) | **the exec succeeds, and the frontier is the abort storm after it** — the two calls in `mach_loader.c`'s `in_exec` block get bodies (`workqueue_mark_exiting`, word 6, and `workqueue_exit`, word 5), retiring both in one step because they are two calls in one basic block with only real code (`task_complete_halt`) between them: retiring one would stop at the other next run having measured nothing new. **This is the run in which the OS's exec path returns success**: `xnu_live_pth_wqmark_seq = 1` / `xnu_live_pth_wqexit_seq = 1` with both `_p = 0xc0535e38` and both **`_tbl = 0x804b3b40`**, matching `nm`'s `stage90_pthread_functions`; and **`xnu_live_lmf_ret = 0x00000000` = `LOAD_SUCCESS`** with `xnu_live_lmf_caller = 0x80285434` and `xnu_live_lmf_header = 0xc8146000`. **`lmf_ret = 0` is a reading this time and the caller key is what makes it one**: `__wrap_load_machfile` is at `0x8047304c` and the only `bl` to it is at `0x80285430` in `exec_mach_imgact`, so the caller is the instruction after it — `cmp r0, #0`, then `beq 0x80285480`, taken exactly when the return is zero. **There is no `stub_hit` anywhere in the log**, and `/sbin/launchd` — the RAM disk's own Mach-O — is accepted, activated and installed as process 1. **The stop after it is a data abort repeated into a storm, and the live channel names the address but not the instruction**: `xnu_live_sleh_seq = 4`, `_type = 4`, `_dfsr = 0x00000805` (translation fault, section, read), `_dfar = 0x00101f28` — a *user* address in the first megabyte, and records 1-3 are the three every run has (`0x1000`, then `0xc8105000`/`0xc8146000`), where 472 had exactly those three and `sleh_storm = 0` — then `xnu_live_sleh_storm = 0x00000005` (the `SLEH_LIVE_MAX` cap: at least five) and `xnu_live_block_seq` reaching `0x46` with `xnu_live_block_enter = 0x800f6efc` = `Call_continuation + 0x1c`. **The run hung, measured rather than assumed**: 77 `xnu_entry_` keys against a completed run's 385, and not one of them `undef`/`panic`/`trap`/`abort`, because the storm burned the live slots before the epilogue could write; the device returned by itself (`No errors detected` after the payload's output) so the watchdog recovered it, and the OS console ends on `load_init_program: attempting to load /sbin/launchd` with **no** `failed loading` line, which is what a successful exec prints. **The instrument for the frontier is already in the call path and does not yet read its argument**: `__wrap_sleh_abort(void *regs, int type)` reads `DFSR`/`DFAR`/`TPIDRPRW` from the coprocessors while `regs` points at Apple's `struct arm_saved_state` (`r[13]`, `sp`, `lr`, `pc`, `cpsr`, `fsr`, `far`, `exception`), so the faulting instruction is `regs->pc` — and the offset is **checkable rather than transcribed**, since `regs->fsr`/`regs->far` are the same two numbers the wrapper already reads from `p14`. **`tools/check_pthread_table_slots.py` makes the table's layout structural, because this step is the third time a slot was retired by hand**: it reads the 40 named words out of the *linked image* and compares each against the symbol its member name implies, taking the member names, their order, `_pad[87]` and `PTHREAD_FUNCTIONS_TABLE_VERSION` from Apple's own `bsd/sys/pthread_shims.h` so there is no second definition of the layout to drift from — the hazard being a swapped pair of designators, which is two non-NULL words that pass the constructor's NULL scan and a body that names itself correctly and just names the other slot; measured with `--selftest` refusing all four mutations. **And one capture was not the payload's output at all**, which is why `stages/stage90/run_and_capture.sh` now counts the payload's own lines (`grep -a -c '^MI4IOS6_STAGE90'`, at least 8) and re-reads up to three times — the bad capture was 455596 bytes, *the same length* as the valid one, with the banner's first 41 characters intact and 455525 binary bytes behind them; `0` payload lines for it, `3930` for the valid one. **Measured:** 455596 bytes, exit 0, device back on its own, `.text` 5223808 (+288), image 5438068, `.bss` unchanged, undefined 26 |
| stage90 | [experiment-474](experiment-474-the-udf-happens-and-the-storm-is-its-report.md) | **the `udf` happens, and the storm is its own report** — the abort record gains six words of Apple's `struct arm_saved_state` (the `regs` `__wrap_sleh_abort` already receives; offsets in a new `entry_saved_state.h`, checked against Apple's `thread_status.h`, this configuration's generated `assym.s`, the hand-written `xnu_arm_boot/assym.s` that `start.s` assembles against and the PCB's own `+PC`/`+R0` by `tools/check_saved_state_offsets.py`, whose `--selftest` refuses every mutation), and eight per-entry live keys. **The control is what makes the frame a reading**: `regs->fsr`/`regs->far` are filled by the same `mrc p15` pair the wrapper reads, so the four aborts whose pair was already known must read back equal — all four do, `frame_ok = 0x00000001`, on this run's pairs (`0x805/0x1000`, `0x807/0xc80fd000`, `0x807/0xc813e000`, `0x805/0x00101f28`; the addresses move between runs, the *agreement inside one run* does not) with `cpsr` = SVC and `pc`/`lr` = `Lcopyout_wordwise_loop`/`load_init_program_at_path`, `L_64loop`/`exec_save_path`, `L64loop`/`copypv`, `Lcopyout_wordwise_loop`/`exec_copyout_strings`. **`pc` is the faulting instruction because `dataabt_from_kernel` stores `lr` twice** — into `SS_LR` in SVC mode (the context's `lr`, hence those callers) and into `SS_PC` after `cpsid i, #23` has switched to ABT mode, where `lr` is `lr_abt` = the faulting instruction; the falsifier is that no `pc` is `0x800147b8`, the `bl vfp_save` return address the straight line suggests. **And entry 5, the one 473 could not name, is the trap's own handler**: `pc = 0x80002088` = `entry_word_at`'s first `ldrb r2, [r0]`, `far = 0x00000000` (so the pointer was NULL), `cpsr = 0xa000009b` (UND, I set), `sp = 0xc045ec50` (a kernel stack, not `sp_usr`), `frame_ok = 1` — and `lr = 0x000010e0` is **the answer to 473's prediction from the other side**: the kernel path stores the SVC `lr`, and `return_to_user_now` loads the user's `pc` into `lr` before `movs pc, lr` (`0x80015038`/`0x80015044`, the `ldm …^` writes `lr_usr` and leaves `lr_svc`), so `lr_svc` **is** the user `pc` = `0x10e0` = the RAM disk Mach-O's `udf #0`. **So process 1 did enter user mode and execute its first instruction; what failed is the report.** The instrument's `fleh_undef` is every `udf`'s vector entry but its argument walk was written for `panic`'s: `r_args` is the trapped context's `r8` (callee-saved, so the C body reads the interrupted register — `mov r7, r8` at `0x800066ec`), 0 for a *user* `udf`, and the guard tests **shape, not mapping**: `tst r7,#3` passes, `(0+31)^0` passes the page test, `cmp r6,r7` (`entry_image_ptr`) says no, `mov r0,r7`, `bl entry_word_at` — the read at address 0. **The defect is a sentence in the guard's own comment** (`entry_stubs.c:4580-4583`): "zero there means the reads below did not happen" — an *accepted* NULL publishes the same `0 & ~0xFFF` as a refusal, so the key cannot tell them apart, while the guard's own justification ("the CPU is executing on that stack") is falsified by a pointer that came from user mode. **Then Apple's `sleh_abort` recurses on a NULL map**: `fsr 7` → the `COPYIN(regs->pc, &ins, 4)` arm (`lr = 0x8044ad80` is the instruction after `bl copyin_kern` at `0x8044ad7c`) → `cmn r9,#0x10000` / `addge r0, r1, #692` (`ACT_MAP`) → `ldr r5,[r0]` = 0 → `ldr r0, [r5, #40]` (`MAP_PMAP`) faults at **`0x28`** (entries 6-8, `sp` descending exactly 0x250 per level, `0xc045ea08` → `0xc045e7b8` → `0xc045e568`), so **`current_thread()->map` is 0** for the thread that ran the exec (`0xc0495480`, the same thread as entries 1-4) — recorded, not explained (`thread.c:1280` sets it from the parent task, `thread.c:333` leaves the template at `VM_MAP_NULL`), and 475's second question. **The recursion is what the run died of**: entries 5-8 have no `xnu_live_sleh_back` at all (1-4 have 1..4), `sleh_seen` counts 9 → 0x40 and stops, and the log has 77 `xnu_entry_` keys against a completed run's 385, none of them from the `undef`/`panic`/`trap`/`abort` groups. **Measured:** gate passed, exit 0, device back on Android by itself, log 459628 bytes / 5018 lines / 3930 payload lines, console byte-for-byte 473's shape (`BSD root: md0`, `failed loading …launchd.development: errno 2`, `attempting to load /sbin/launchd` and no failure line and no panic), entry image 5438068, payload `stage90-qcdt.img` 8458240 sha256 `badbca2e…00eb6f` |
| stage90 | [experiment-475](experiment-475-the-udf-reaches-the-kernel-and-the-next-stop-loses-its-record.md) | **the `udf` reaches the kernel, and the next stop loses its own record** — one branch, one refusal, one check, and the run in which **the kernel's own trap path names process 1's first instruction**: `xnu_live_undef_user_seq = 1` with `xnu_live_undef_pc = 0x000010e0` (the RAM disk Mach-O's `udf #0`), `_lr = 0x000010e4` (`LR_und` = pc + 4) and `_spsr = 0x00000010` (user mode), and the same three keys again in the report the epilogue printed (`xnu_entry_undef_pc/_lr/_spsr`) with `xnu_entry_undef_user = 1`. **The branch is derived, not a heuristic**: `panic`'s `udf` is executed by `DebuggerTrapWithState` and every route to it is kernel code (`sleh_undef`'s kernel arm ends in `panic_context`, its user arm in `exception_triage(EXC_BAD_INSTRUCTION, …)`, `NOTREACHED`), and Apple's own `locore_fleh_undef` makes the same test as its first two instructions — so a `udf` arriving with a user SPSR cannot be `panic`'s, and **the user case is forwarded to `locore_fleh_undef` instead of walking `panic`'s arguments**, with the record written to the *live* channel because the epilogue is not reached on that path. **The measurement that says the branch did the work is the absence of the walk**: `xnu_entry_panic_len = 0x000000a4` with `_dropped = 0` (the four user keys and nothing else), no `xnu_entry_panic_args_ok`/`arg0`/`dt_root` anywhere — 474's read of address 0 was `entry_word_at(r_args = 0)`, and the keys that would have carried it are not in the run. **And 474's storm is gone in the same channel that showed it**: `xnu_live_sleh_seq` 1..4 each with `_back` 1..4, `xnu_live_sleh_seen` stopping at 4 instead of 0x40, no `_storm`, and the live order is four `sleh` groups then the four `undef` keys with nothing after. **The forward did not return** — the run ends with the *prefetch* handler's message, not the defensive `entry_epilogue` this step added for "Apple's handler returned" — so the `udf` reached Apple's body and the boot went on to a different fault. **The kernel case keeps the walk with `args == 0` refused**, and the decision is its own key (`xnu_entry_panic_args_ok`) because 474 showed the old page key published the same `0` for a refusal and an accepted NULL. **`tools/check_undef_handler.py` makes both promises build failures**: `--guard` compiles the two guard functions *out of the source* and runs an 18-row case table on the host (the AES check's shape; a NULL is refused, the window that fits a page is accepted, the one that straddles it is not), `--forward` reads the *linked* image and requires `fleh_undef` (0x80006638) and `locore_fleh_undef` (0x80014024) to be distinct symbols with a `bl` between them whose target is computed from its encoding — `--selftest` mutates both and requires the refusal (the second `--forward` arm, a `bl` to itself, is an assertion that has never fired, said so in the doc). **The new stop is a prefetch abort whose record was refused**: slot 3 is still this image's handler (467 gave slot 4 to Apple's body and left slot 3), it reports through the shared `entry_kv` channel — `xnu_entry_kv_written = 8170` of 8192 with `xnu_entry_kv_dropped = 33292` — so the nine `xnu_entry_prefetch_abort_*` keys are **not in the log**: 461's defect on another handler, and 476's object (a buffer of its own, then the choice between forwarding to `locore_fleh_prefabt` and fixing the pointer). The rest of the run is the most this boot has done — `xnu_entry_block_count = 0x46` with 13 returns and 7 `sleep` calls over five threads, the `t268_*` tracer run to its end, and the OS console (measured this time, `xnu_entry_ostext_chars = 0x50b` / `_lines = 0x19`) ending at `load_init_program: attempting to load /sbin/launchd` with no failure line and no panic text. The exec's four aborts are at `pc` 0x80011a20, 0x80007a28, 0x800076f4, 0x80011a20 — 0x60 and 0x64 past 474's, non-uniformly, because this step's code grew and the linker's fills take the difference. **Measured:** gate passed, exit 0, device back on Android by itself, log 481894 bytes / 5534 lines / 3933 payload lines, `.text` 5224384 (+192), image 5438068 (unchanged — inside `.data`'s 16 KB alignment slack), `.bss` unchanged, undefined 26, payload 8458240 sha256 `a5fb7aec…72566810` |
| stage90 | [experiment-476](experiment-476-the-prefetch-slot-joins-the-data-slot.md) | **the prefetch slot joins the data slot, and the price of a call is measured** — 475 ended on a fault whose nine keys the full 8 KB trace channel had refused (8170 of 8192, 33292 refusals), so this step gives slot 3 to Apple's `locore_fleh_prefabt` and the record moves to the **live** channel — which 467's `sleh_abort` wrapper already writes and which the payload captures whether or not the epilogue runs, so the "buffer of its own" 475's agenda asked for was already there and the run is what proves it survives a fault that never returns: **seq 5 has no `xnu_live_sleh_back` at all** (1-4 have 1..4) and is in the log complete. **The other half is one `if`**: the fault pair is class-dependent — data abort DFSR `c5,c0,0`/DFAR `c6,c0,0`, prefetch abort IFSR `c5,c0,1`/IFAR `c6,c0,2` (`osfmk/arm/trap.h`) — and `__wrap_sleh_abort` was reading the *data* pair on both classes; it now selects by `type`, and **474's `frame_ok` control validates that selection on the very entry that motivated it**, because the vector fills `SS_STATUS`/`SS_VADDR` from the same coprocessor pair: seq 5 reads `frame_ok = 1`, where a data-pair wrapper would have reported seq 4's stale `0x805/0x00101f28` against a frame saying `0x0000000d/0x80006d04`. **The fifth abort is the instrument's, and the arithmetic is Apple's**: `pc = 0x80006d04` is inside this image's `fleh_undef` (476's link puts that function at 0x80006638) at the `bl locore_fleh_undef` 475 added — 475's own `--forward` check had computed that `bl`'s target from its encoding — and `locore_fleh_undef` derives `SS_PC` from `lr` (`mrs sp, SPSR`; `tst sp, #32`; `subeq lr, lr, #4` at 0x8001402c), which is right on a vector entry (`lr_und = pc + 4`) and wrong on a call from C: the kernel resumed process 1 **at this image's own instruction, in user mode** (`cpsr = 0x10`, `lr = 0`, `sp = 0x00101efc` the user stack, `IFSR = 0xd` a permission fault on a section), where the fetch faulted. **What slot 3 gives up is named**: `fleh_prefabt` reported nine keys and the live record carries five of them plus the thread and the mode — TTBR0, TTBR1, TTBCR and SCTLR are not in it, so a later step needing the mapping state has to add them to the wrapper — and the build asserts neither `fleh_prefabt` nor `fleh_dataabt` is in the vector table. **The stop is a kernel panic and the record holds two of its entries**: `entry_kv_into` *appends*, so the trap record has two four-key blocks — the first is 475's forward arriving at this handler (`lr` 0x10e4 / `pc` 0x10e0 / spsr 0x10 / user 1), the second is `panic`'s (`lr` 0x800364c4 / `pc` 0x800364c0 = `DebuggerTrapWithState+0x28`, the `udf #0`, from SVC mode 0x93, user 0), with `xnu_entry_panic_entered = 2` as the tell; reading the pair with a per-key `sort -u` keeps the first and yields "a panic at the user `udf`", which the record itself contradicts. The message is `xnu_entry_trap_r9_fmt = 0x8048bb31` = `"ipc_mqueue_receive_results: strange wait_result"` (`osfmk/ipc/ipc_mqueue.c:871`, the `default:` arm of the switch over `wait_result_t`), and the live channel's own order is the last mile: `thread_block` entered from `0x800e9f40` (inside `ipc_mqueue_receive`) **returned**, and the next event is the panic `udf`. **Measured:** gate passed, exit 0, device back on Android by itself, log 487314 bytes / 5689 lines, `.text` 5224416, image bytes 5438068, `.bss` 0x8052fa80..0x80587500, 43 `--wrap`ped symbols (40 reached), undefined 26, console byte-identical to 475's (`xnu_entry_ostext_chars = 0x50b` / `_lines = 0x19`, ending at `load_init_program: attempting to load /sbin/launchd` with no failure line and no panic text), payload `stage90-qcdt.img` 8458240 sha256 `eefbb474…845fdfe2f` |
| stage90 | [experiment-477](experiment-477-the-split-moves-into-the-vector-page.md) | **the split moves into the vector page, and the panic is the one left** — 476 measured what a call costs, so `vec_tramp_1` is hand-written and tests the interrupted mode **before any C code runs**: `mrs sp, spsr` (the banked register, and the first instruction of Apple's own first-level bodies, so nothing of the interrupted context is touched), `and sp, sp, #0x1f`, `cmp sp, #0x10`, `beq` — user mode to `locore_fleh_undef`, kernel mode to this image's `fleh_undef`, with the stack load kept only for the C branch (`undef_from_user` re-derives `sp` from `TPIDRPRW` and the thread's `kstackptr` and never reads this one). The reading the forward produced now comes from `--wrap=sleh_undef`, the function every undefined instruction reaches, and its `lr` is a *different quantity*: `SS_LR` out of the frame, the thread's own (`xnu_live_undef_lr = 0`, which `entry_ramdisk.s`'s thread state writes at +212), not 475's `LR_und = pc + 4`. **The run is the first in which a user `udf` and `panic`'s `udf` reach different handlers**: `xnu_live_undef_pc = 0x000010e0`, `_spsr = 0x00000010`, `_user = 1`, and the falsifier written into the source before the build reads `xnu_entry_undef_user = 0x00000000` with `xnu_entry_panic_entered = 0x00000001` (476: 2) and exactly **one** block in the trap record — that block being `pc = 0x800364c0` = `DebuggerTrapWithState+0x28` from SVC mode (`spsr = 0x60000093`) — so the only `udf` this image's handler saw was `panic`'s. **The spurious fault is gone, in the channel that showed it**: four `sleh` entries, every one `type = 4` with a `_back` and `frame_ok = 1`, **no `type = 3` anywhere** where 476 had a fifth entry that faulted in user mode; and the stop is the same stop 476 reached behind that fault — `xnu_entry_trap_r9_fmt = 0x8048bc59` reads `"ipc_mqueue_receive_results: strange wait_result"` out of this image (476's 0x8048bb31 is the same text read out of that image, 0x128 away), with `thread_block` entered from `0x800e9f40` (inside `ipc_mqueue_receive`) and **returning** immediately before it. **The two middle aborts are named rather than guessed** (`bzero.s:104` is `L_64loop`, `bcopy.s:88` is `L64loop`, so seq 2 is `bzero` from `exec_save_path` and seq 3 is `bcopy` from `copypv`), and 474's prose — which listed those two pairs one position apart from its own table — is corrected in that doc and in this index. **And the build refused this image for the wrong reason**: `--split`'s source half searched the handler's *definition* for `fleh_undef(`, because `extract_function` returns a definition *including its signature* — so the build exited 1 **after** writing the `.bin`, and the payload build and the device run used that image; the ELF half (every `bl` target computed from its encoding over the handler's range) passed, and that half is the one that answers the question. The fix is a depth rule (`calls_at_body_depth`) with two new `--selftest` controls — the unmutated source must pass, and a call inserted into the body must be refused — plus the repair of a control that had never worked: `handler_range` took "the next symbol above" as the size of a symbol `nm` gives none, and for Apple's `locore_fleh_dataabt` that is a label *inside* its body, so the range came out 16 bytes and the scan read zero `bl`s; it now refuses a range it cannot read, and the control points at `fleh_irq` (size 36) with `entry_epilogue` forbidden and at least one `bl` required. The entry build was re-run with the fixed check (exit 0, `xnu_entry_477: slot 1 splits on the interrupted mode in the vector page - user -> locore_fleh_undef (0x80014024), kernel -> fleh_undef (0x80006650)`) and the three artifacts are **byte-identical** to the run's (`xnu_arm_entry.bin` sha256 `efbda6ac…415144856`), which is what makes this run's reading valid despite the failed build. **Measured:** gate passed, exit 0, device back on Android by itself, log 487099 bytes / 5681 lines, `.text` 5224832 (+416), image bytes 5438068 (unchanged — inside `.data`'s 16 KB alignment slack), `.bss` 0x8052fa80..0x80587508, 44 `--wrap`ped symbols (41 reached), undefined 26, the trace channel still full (8170 written / 33287 refused), payload `stage90-qcdt.img` 8458240 sha256 `c9b3dd18…138718` |
| stage90 | [experiment-478](experiment-478-the-return-value-was-the-instruments-void.md) | **the return value was the instrument's, and the panic it caused** — 477's agenda was to name the `wait_result`, and the number that came back says the panic 476 and 477 both stopped on **was this image's own doing**: from 447 the wrapper was `void __wrap_thread_block(void *)` against `extern wait_result_t thread_block(...)`, so the `r0` `ipc_mqueue_receive` compares with 3 and indexes a jump table with (`0x800e9f44`'s `cmp r0,#3` / `bhi panic`, the `default:` arm inlined) was whatever the wrapper's **tail call to a `void` function** left there — which is why the build could not see it: the caller's prototype comes from Apple's header and the callee's from this image, and `ld --wrap` is what joins them, so no check on *values* can catch a wrong *declaration* (both shapes are disassembled side by side from this image's own flags; the 477 source is quoted from `git show 1649447`). **With the ABI fixed the boot does not stop there at all**: 15 of 15 returns are `0` (`THREAD_AWAKENED`), `_results_k1..k6` are all zero, `xnu_live_block_odd_*` does not exist — so 477's two candidates (10 and -1) were neither, and the wake always came — and the run goes on to **kill process 1 and panic because init died**: the OS console gains exactly one line over 476's (counters 0x50b/0x19 → **0x552/0x1a**, and 71 characters is that line plus its CR and LF), `pid 1 exited -- exit reason namespace 2 subcode 0x4, description none` (`OS_REASON_SIGNAL` = namespace 2, `0x4` = **SIGILL**, i.e. the RAM disk Mach-O's `udf #0` at `0x10e0` reaching its consequence), and then `launchd_crashed_panic` — `proc_prepareexit` is `if (p == initproc) launchd_crashed_panic(p, rv); /* NOTREACHED */`, so no init exit can be survived — identified from **two independent sources**: `r9` resolves to `"%s %s -- exit reason namespace %d subcode 0x%llx description: %.800s"`, which occurs once in the tree at `kern_exit.c:574`, and the argument walk yields exactly that format's first two `%s` arguments, `arg0 = 0x804f637b` = the **empty string** (`(p_csflags & CS_KILLED) ? "CS_KILLED" : ""`) and `arg1 = 0x804b78f8` = `"initproc exited"`. `tools/check_block_result_slots.py` makes the six transcribed members structural — the `THREAD_*` values read out of Apple's `kern_types.h`, compared against each `case` value **and the member name in its comment**, with the four accepted members required to be slots 0..3, `ENTRY_BLOCK_RESULT_SLOTS` the member count plus the default, the seven key names written once, and the two histogram writes required to be loops over that macro — `--selftest` refusing **all 14 mutations** plus a mutated header. 476's and 477's read of the panic is retracted in both docs (the site was right, the cause was not) and their other measurements stand. 479's object is the same stop's own name: **init must not die**, so slot 2 (`fleh_swi`) goes to Apple's own first-level handler the way 467 moved slots 3-4 and 477 moved slot 1, and the fixture's first instruction becomes a `svc` the kernel can service — a user-mode busy loop is *not* worth a step, because `xnu_entry_block_count = 0x48` against `_returned = 0x0f` already says 57 of 72 blocks wait for a wake nothing can send, which is the timer's job (`ml_init_timebase` + an MSM8974 `tbd_ops_t` over the GPT at `0xf9020000`, 19.2 MHz, IRQ 19, and `IOCPUInterruptController`). **Measured:** gate passed, exit 0, device back on Android by itself, log 488475 bytes / 5721 lines / 3933 payload lines, `.text` 5225568, image 5438068 (unchanged — inside `.data`'s 16 KB alignment slack), `.bss` 0x8052fa80..0x80587548, 44 `--wrap`ped symbols (41 reached), undefined 26, `sleh` seq 4 with back 4, all `type 4`, `frame_ok 1`, `panic_entered 1`, payload `stage90-qcdt.img` 8458240 sha256 `dfa2aabd…e37ff9f5` |
| stage90 | [experiment-479](experiment-479-the-kernel-answered-process-1.md) | **the kernel answered process 1, and nothing stopped** — 478's agenda was to retire `launchd_crashed_panic` by not killing process 1, and it named the right route (the vector page's slot 2, Apple's own `locore_fleh_swi`) with the wrong syscall in it: the `thread_switch` the 478-era draft carried is a *mach* trap whose only option that does anything ends in `assert_wait_timeout((event_t)assert_wait_timeout, THREAD_ABORTSAFE, 0, NSEC_PER_MSEC)` — interval 0, the event being the function's own address, and no timer in this image to fire the deadline — so that run would have parked process 1 in a wait nothing can signal. What went in is **`getpid`, Unix syscall 20, `narg` 0 — a syscall that returns a value**, and the run answers: `xnu_live_getpid_value=0x00000001` (`EXPECTED_PID` read out of `bsd_init.c`'s own `initproc = proc_find(1)`), `_error=0x00000000`, `_caller=0x8027e248` = `unix_syscall+0x100` (the `mov r4, r0` after `blx r3`, where `r3 = [fp]` is the *sysent slot*), counts climbing by powers of two to `0x00800000` (**8 388 608 calls**) with `_last=0x00000001` on every one and **no `xnu_live_getpid_change_*` key at all** — a floor, since the last power of two is a milestone and the log ends when the SoC's own watchdog bites (`0xdffac`/`0x7ffd` ≈ 28.0 s, armed at entry, never disarmed because the disarm lives in the payload's exit path). The console is 478's text **byte for byte except the `md0` address, with one line removed**: 1289 bytes / 24 CRs through `load_init_program: attempting to load /sbin/launchd` in both runs, no `pid 1 exited` (478: 1), no `xnu_live_undef_*` (478: 6), no `xnu_entry_panic_*` (478: 14) and **no ` xnu_entry_*` report key at all** (478: 473) — the report is written on the trap path and this run never trapped, so every number comes from the live channel and the console tank. `0x804FD000 → 0x80501000` agrees with `.bss` start and the image growing by 0x4000 (5438068 → 5454452), and `xnu_live_prop_w0` reads the same new address out of `/chosen/memory-map`. The instrument had to sit in the *table* (`--wrap` rewrites the initialiser, so `sysent[20]` holds `__wrap_getpid`; the census counts `getpid` **by address only**, like `vcputc`), and `tools/check_sysent_table.py` makes it structural — eight witnesses 0..128 against `syscalls.master` (so the 16-byte stride is a reading, not an assumption) plus the wrapper's slot as a two-sided fact (`got == real` is its own failure message, because a slot holding `getpid` produces a run indistinguishable from an absent instrument), with 7 refused mutations in `--selftest`. The step's own checks caught three defects and all three were in the checks: the sysent check **called the image wrong for being right** (comparing the slot with `getpid`, the 478 `--wrap`-is-an-ABI-substitution lesson not applied to itself), the fixture check's mask `0xFFF00000` **forgot `Rn`** so `cmp r1, #1` survived its selftest (now `0xFFFFF000`, immediate read `0xFF`), and `ok: notes[-1]` named `sysent[128]` on a correct build. Frontier: **no longer a stop, but the absence of one** — 70 blocks entered / 13 returned (57 parked on a timer that does not exist), all four `xnu_live_sleh` records `_user = 0`, and 480 owes process 1 a syscall whose effect is on the machine (`mmap` + a store, the first user-mode fault and the first fixture with a stack, since a Unix syscall's arguments come from `sp`) while the timer (`ml_init_timebase` + an MSM8974 `tbd_ops_t` over the GPT at `0xf9020000`, 19.2 MHz, IRQ 19) and `IOCPUInterruptController` become the plan of record. `xnu_arm_entry.bin` rebuilt byte-identical after the check-message fix; payload `stage90-qcdt.img` 8474624 sha256 `51d1e7b4def68509ed42c5a221bc0b6f1405f182f1877a72dd39483b9f8ac970` |
| stage90 | [experiment-480](experiment-480-process-1-got-a-page-of-its-own.md) | **process 1 got a page of its own, and the first two user-mode aborts this walk has taken** — 479's agenda ("give process 1 a syscall whose effect is on the machine") went in as `mmap(0, 0x1000, R|W, MAP_PRIVATE|MAP_ANON, -1, 0)` followed by a read and a write of the page, so the fixture is 27 instructions and the ring's own order says they ran in order: the exec's four `_user = 0` aborts, `getpid_seq=1` `value=1`, `mmap_seq=1` `error=0` `value=0x00102000`, then `sleh_seq=5` (`_user=1`, `fsr 0x7` = `FSR_PFAULT`, `far 0x00102000`, `pc 0x1118` = `ldr r3,[r0]`) and `sleh_seq=6` (`_user=1`, `fsr 0x80f` = `DFSR_WRITE|FSR_PPERM`, `pc 0x1124` = `str r0,[r0]`), and then the loop to `getpid_count=0x00800000`. **The argument path was measured for the first time**: `arm_get_syscall_args` is the munging variant on this target, `sysent[197].sy_arg_munge32 = 0x8027d7f0 = munge_wwwwwl` (read out of the linked image by the new check), kDirect (`r12 != 0`) is `munge_wlll` -> `munge_wll` -> `memcpy(args, regs, 24)` + word 6 = `r6`, word 7 = `r8`, and all eight words are in the log with the fixture's `0x5a5a` marker landing in **word 5, the padding between `fd` and `off_t pos`** — words 4/5 together being the kDirect-vs-kIndirect discriminator, since the indirect arm puts `r5` in word 4; `bcs` (not `cmp`) is the errno convention. **The two FSRs are the zero-fill reading**: a translation fault serviced by installing a translation, then a permission+write fault on it — and the fixture's own `cmp r3,#0` passing (the next record is the `str`, not a `udf`) says the page it got read as zero. `seq 5/6` have **no `xnu_live_sleh_back`** where 479's four do, and `trap.c` says why: the kernel path ends `exit: … return` (the wrapper runs) while the user path ends `goto exception_return` -> `thread_exception_return()`, which never returns — so the absence is the shape of a *serviced user* fault, with the still-climbing loop and the absent `pid 1 exited`/trap record as its complement. **474's zero is answered for this path and only for it**: `xnu_live_mmap_thread = 0xc047a270`, `_map = 0xc0001340`, `_pmap = 0xc046e900` (read one syscall before the faults, on the same thread), plus a second independent reading — `seq 4`'s user-address fault (`far 0x00101f28`) took `trap.c:443`'s `thread->map` arm and returned (`_back = 4`). **And three addresses turned into a reading about the image**: `sp 0x00101efc`, the exec's strings at `0x00101f28`, `mmap`'s answer `0x00102000` — `USRSTACK` is `0x27E00000` (measured in the built `thread_userstackdefault`) yet `create_unix_stack`'s `VM_FLAGS_FIXED` allocation at `0x26E00000` fails, so Apple's own "try anywhere" branch runs, `p->user_stack = addr + size` is `0x102000`, and `size = MAXSSIZ` pins `addr = 0x2000`: process 1's stack is the first free megabyte after `__TEXT`, and `mmap`'s first free page is therefore the page above it. Console **byte-identical to 479's** (25 lines, 24 CRs, 1290 bytes), `md0` still `0x80501000` — `.text` +864 fits the gap below 0x80500000 and the 15 new initialised words fit `.data`'s first page, so image 5454452, `__bss_start` 0x80533a80 and the RAM disk offset are all unmoved — and **479's "no ` xnu_entry_*` report key at all" needs its qualifier**: the payload's own boot report is 24 keys under the same prefix in every run, against 473 trap keys in 478 and 0 in 479 and 480. Block set unchanged (70 entered / 13 returned / **57 parked**), `ostext` keys identical. Four defects, all in the checks: `decode_arm` mis-masking `udf` and failing a *correct* fixture, a `describe()` that raised `TypeError` while reporting an unrecognised word, a selftest that *accepted* word 8's `^1` (the marker is a property, now four property mutations instead), and a mutation that left the checked span and raised `struct.error` instead of being refused. 12 sysent mutations, 61 fixture mutations, 11 offset mutations, all refused. Next: the timer (`ml_init_timebase` + an MSM8974 `tbd_ops_t` over the GPT at `0xf9020000`, 19.2 MHz, IRQ 19) and `IOCPUInterruptController`, plus three narrowed owed items (`arm_fast_fault` vs `vm_fault` for the write fault, why the FIXED stack allocation is refused, 474's map-zero *moment*). Log 460167 bytes / 5031 lines / 1072 `xnu_live` records, exit 0, device back on its own, watchdog the only reset source; 26 undefined, 46 `--wrap`ped symbols (41 / 1 / 1 / 3 by address: `vcputc getpid mmap`); entry image rebuilt byte-identical (`c8ca5c0f…`); payload 8474624 sha256 `379b67175428eba6df35fb96877b67d592240b3caea181ada224d2deaaa94a99` |
| stage90 | [experiment-481](experiment-481-the-decrementer-gets-an-owner.md) | **the decrementer gets an owner, at Apple's own point in `arm_init`** - 480's first item (`ml_init_timebase` plus an MSM8974 `tbd_ops_t`) went in where Apple's own armv7 kernel registers a timebase: the hook is `__wrap_PE_init_platform` firing on the second (`vm_initialized = TRUE`) call, the one `arm_init_cpu` makes at `osfmk/arm/arm_init.c:383`, one call **before** `cpu_timebase_init` (`:384`, whose struct copy is what has to see it) and two before `fiq_context_init` (`:385`) - the fourth site, `arm_init_cpu`'s `(TRUE, NULL)` at `:463`, is the secondary-CPU path this boot never runs and its `args` is not `BootCpuData`. The call that fills `rtclock_timebase_func` has been absent from every image of this walk because **no `ARM_BOARD_CONFIG_*` is set, so no `ARM_BOARD_CLASS_*` is defined**, the seven-way `strcmp` chain is preprocessed away and `return 0;` becomes the function's first statement - `nm -u out/xnu_obj/pexpert_arm_pe_identify_machine.o` has no `ml_init_timebase`, which the new `tools/check_timebase_registration.py` re-reads before every link (five claims plus two sub-checks, 12 mutations refused, three of them aimed at the linked image's own symbol table, and the registration located **by shape** - `__wrap_PE_init_platform` -> `cpu_timebase_init` -> `__wrap_fiq_context_init` at `0x80008124` with `r0=1, r1=BootCpuData(0x8050a000)`). Measured: `xnu_live_timebase_registered = 0x1`, `_args = 0x8050a000` = `BootCpuData`, `_seq = 0x2`, the three pointers `0x80007840`/`0x80007868`/`0x80007848`; the four `cpu_data` words read back at the first `fiq_context_init` are those same three plus `0x7fffffff` = `STAGE90_CPU_DECREMENTER_INITIAL`; the hardware counted (`CNTV_TVAL` **down 813** while `CNTVCT` went **up 813** in one bounded spin, `dec_ctl = 0x3` = ENABLE|IMASK); and the chain closed end to end (`setpop_deadline = 2^64-1`, `setpop_returned = dec_written = 0x7fffffff`, `dec_readback` six counts lower, `dec_count == setpop_count` at 2/4/8/0x10, so every `setPop` reached the decrementer and nothing else wrote it). The mask is this step's decision and 482's job: 143's no-FIQ-to-non-secure-PL1 measurement still holds (no `xnu_live_fiq_handler_entered`) and `fleh_irq` would branch to a NULL `interrupt_handler` through `cpu_data`, so `ml_init_timebase`'s acknowledge pair is the honest `0u, 0u` until the routing is measured. Three defects are recorded: the header's premise (corrected by the `nm -u` measurement), pass 1 running without the `--wrap` flags so the generator's ordinary `__real_*` definitions beat the linker's rewrite and the wrapper called `entry_stub_hit` (fixed with `PASS1_LDFLAGS` plus a build step that stops the build if any wrapped name or its stand-in is in pass 1's undefined set), and `check_saved_state_offsets.py` defaulting to `RELEASE`, where `CPU_DECREMENTER` is 88 rather than 104 - running it by hand printed a four-number FAIL about another configuration's `struct cpu_data` with the file named nowhere (fixed: `--config` defaults to `STAGE90_XNU` and the FAIL names the file; all 17 mutations still refused). Console byte-identical to 480's (1290 bytes, 24 CR, 25 LF) with its `ostext_tank` 610 -> 0 and `ostext_at` `0x49cd0` -> `0x48367` explained by the block-reserve ordering (481's first live record is the new `PE_init_platform` wrapper's, where 474-480's was `xnu_live_unser_caller`); blocks unchanged at 70 entered / 13 returned / **57 parked**; log 461190 bytes / 5060 lines / 1101 `xnu_live` records; 49 wraps (44 / 1 / 1 / 3 by address: `vcputc getpid mmap`), undefined 26, headroom 1526328; entry image rebuilt byte-identical (`95e91147...`); payload 8474624 sha256 `0a3e17476890696273b452825700950baacefde180cc90358300a2aaf33c06e5`. Next: the interrupt routing decision - which physical line the virtual timer appears on for MSM8974 and whether Apple's decrementer handler or `fleh_irq` owns the slot - plus `IOCPUInterruptController` and the owed list. |
| stage90 | [experiment-482](experiment-482-which-line-the-virtual-timer-asserts.md) | **which line the virtual timer asserts, measured with delivery still impossible** - 481 left the decrementer armed and masked on every write, and the routing decision it handed on rested on a quotation (143's no-FIQ-to-non-secure-PL1) rather than on the line itself, so 482 measures the line without enabling anything: `entry_gic.{h,c}` (new) installs one 1 MB section at `0xf9000000` through L1 index `0xF90` with the live channel's own attribute (`slot_before = 0x00000000` - XNU mapped no GIC, 308's fact measured again on hardware; `desc = 0xf901040e`), reads the pre-state read-only, and then runs the payload's two candidate PPIs (18 and 19, re-read out of this repository's `gic.c`) twice each - once with `CNTV_CTL.IMASK` set (the negative control) and once with it clear - inside `cpsid if`, with the CPU interface cleared and read back (`GICC_CTLR` 0, `GICC_PMR` 0) and both candidates disabled and cleared before the interface is restored; `GICC_IAR` is never read. **The answer is INTID 20** - `xnu_live_gic_pend_added18 = _added19 = 0x00100000`, `_timer_intid = 0x14`, `_hits = 0` - measured with the same countdown (`0x1000` ticks) and the same spin (400000 iterations) in both windows, so the only variable that differed is the mask, and with `ISTATUS` set in both post-spin reads (`ctl_masked = 0x7`, `ctl_unmasked = 0x5`) so the countdown really did reach zero in the control as well as in the measurement. The pair the payload's own device tree names (`interrupts = <1 2 0 1 3 0>`) is therefore wrong for the virtual timer: 18 never went pending across two countdowns, and 19 is the **physical** timer's line, taken by the payload's own IRQ path earlier in the same boot (`gic_timer_last_iar = 0x13` with `gic_timer_last_timer_ctl = 0x5`). The distributor is left exactly as found (`restore_ispendr0 = 0x20400000 == ispendr0`, `_restore_cpu_pmr = 0xf0`, `_restore_cpu_ctlr = 1`), and the probe's mapping is visible in the kernel's own page tables - all three pmap checksums moved (`post_pe_bootstrap` `0xfd71acd9` -> `0xfd1d0164`, `full_pmap` `0x7461ee72` -> `0x740d43cf`, `high_va_code_exec` `0x6543dd7b` -> `0x652f70c6`) with the four carriers of each value agreeing inside the run. **The first run's own numbers found two probe defects and both are fixed and re-measured**: the masked `CNTV_CTL` read was taken before its spin and the unmasked one after it (so `0x3`/`0x5` read as "IMASK clears ISTATUS"), and each candidate cleared its own INTID rather than the line its countdown raised (so bit 20 stayed pending into the second candidate's control - `id19_pend_masked = 0x20500000` - and into the machine - `restore_ispendr0 = 0x20500000`); the corrected run's `id19_pend_masked` and `restore_ispendr0` are both back at `0x20400000` and the two logs differ by exactly the six keys the fix added (1139 -> 1145 `xnu_live` records, 462631 -> 462862 bytes). The run is unperturbed: OS console **byte-identical** to 481's (26 lines, 131085 bytes), blocks unchanged at 70 entered / 13 returned / **57 parked**, `dec_written = setpop_returned = 0x7fffffff` with `dec_readback = 0x7ffffff9` and `dec_count == setpop_count` at 2/4/8/0x10 (481's chain still closed), 0 `stub_hit=`, 0 `xnu_live_undef_*`, no panic, `No errors detected`, device back on its own. `tools/check_gic_routing.py` is new (six claims - offsets against the payload's own copies including the `STAGE90_GICD_ITARGETSR0`/`GICD_ITARGETS0` spelling difference, both bases against the payload's constants *and* its own assertions, the two candidates, slots 6 and 7 against this image's symbols - `fleh_irq` 0x80007808 / `fleh_decirq` 0x8000782c, with Apple's `locore_fleh_irq` 0x80015954 in **no slot** - the shape of Apple's dispatch, and the probe's three guards in order; **33 mutations refused**, four of them against the linked image's own disassembly, and the check runs from `build_entry.sh` so a broken one stops the build). Nine defects are recorded, all of them this step's own: a define parser whose `\s+` crossed a newline and read the next `#define` as the value; a claim satisfied by `fleh_irq`'s *comment* mentioning `entry_epilogue`; a 0x60 disassembly window that ended one instruction before the `blx r5` it was looking for; a mutation whose `LOAD_RE` selector also matched `str`, so it changed the facts and left the claim standing; a mutation that satisfied the property it was supposed to break; the two probe defects above; a guard fingerprint that embedded a call's closing parenthesis and refused the build for the wrong reason; and an argument in a comment that a measurement contradicts (481's 813 ticks over 20000 iterations is 40.65 per 1000, not the 24 it quoted, and 481's spin counter is a register while this one is `volatile` - ~100 per 1000, measured - while the comment also cited a check that did not exist). Layout: `.text` 5232512 -> 5237824 with `entry_gic.o`'s two inputs `0x5c4` + `0x4be` accounting for 2690 of it and the rest the aligned-fill term (whose largest entries are `0xfe8`/`0xfc4`/`0xf74`), `__wrap_fiq_context_init` 0x800087b0 = 481's + the object's own 0xAC, `.bss` unchanged (`0x80533a80..0x8058b5c8`), image **5454452 unchanged**, headroom 1526328, undefined 26, 49 wraps, entry image reproducible (`d7d210b5...` / `a8f66c64...`) and a comment-only edit afterwards proved byte-identical by rebuild; payload 8474624 sha256 `a3bfeacad02a4f03a3ed2d099c164d6b4d1f42c61b7afcfd413d935b6a71dfe5`, `kernel_size = 5949716`. Next (483): put the handler on **20**, give `cpu_data->interrupt_handler` a real value before slot 6 can hold Apple's dispatcher (`fleh_irq_handler` at 0x80015abc loads `[r4, #180]` into `r5` and `blx r5`, and nothing has ever stored one), read `GICD_ICFGR` for the line, then lift `STAGE90_CNTV_ARM_MASK`. |
| stage90 | [experiment-483](experiment-483-the-interrupt-arrives-and-the-clock-runs.md) | **the interrupt arrives, and the kernel's clock starts running on it** - 482 measured that the virtual timer asserts INTID 20 and that Apple's dispatch (`fleh_irq_handler`, loading `cpu_data`+192/196/184/188/180 into r0..r3 and r5 then `blx r5`) branches through a word nothing had ever written, so this step supplies that word and moves slot 6 in that order and no other: `entry_irq.c` (new) installs `entry_irq_handler` through Apple's own `ml_install_interrupt_handler` (the only writer of those five words in the tree - a hand store here would make the read-back a measurement of this file's own store, which `tools/check_irq_routing.py` refuses by pattern), reads the interrupt stack pointer as a gate before the call (`istackptr = 0x80508000`), records the exception it runs in (`cpsr = 0x60000093`, `I` set - an interrupt cannot be delivered into the middle of the arming whether or not the line is enabled yet), reads the five words before (`handler_before = 0` - 482's premise measured on hardware) and after (`handler_after = 0x800085b0` = `entry_irq_handler`, `target_after = 0x8053b2cc` = `g_irq_target`, `nub_after = 0x8050a000` = `BootCpuData`, `install_ok = 1` only because the read-back agreed on the handler *and* the target), reads `GICD_ICFGR` for the line (`icfgr_word = 0xc04`, `icfgr_shift = 8`, both derived from the intid; **`icfgr = 0xffffffff`** - not a valid GICv2 field, so this PPI bank's configuration bits are not storage on this SoC and the register 482 hoped would settle edge-vs-level does not), reads `IPRIORITY`/`ITARGETSR` (`0xa0a0a0a0` / `0x01010101`), clears the line's pending bit (`pend_cleared = 0x20400000`), and only in a build whose `#define STAGE90_IRQ_ENABLE_LINE` says so writes the one bit at the distributor (`GICC_CTLR` 1 and `GICC_PMR` 0xf0 are read and *not* written - the payload's own IRQ path already took an interrupt in this boot with exactly those); the handler splits three ways - INTID 20 gets `GICC_EOIR` then `rtclock_intr(0)` (EOI first, deliberately: `rtclock_intr` re-arms through `setPop`, so a second expiry during that work must find the line already back in the distributor's hands; Apple's own armv7 EOI is FIQ-bank/private-offset and there is no `GICC_EOIR` in its tree, so the shape is the payload's own `gic.c:246`/`:293`), `0x3ff` is counted with **no** EOI (a spurious read acknowledges nothing), and anything else is acknowledged, counted and stopped - because an unhandled line that returns is an interrupt that arrives again immediately. **The result: `first_iar = 0x14` is 482's measurement arriving as an interrupt, `timer_count` passes 2048 serviced, `late_count = 0` over every sampled call, and the third case never ran** (no `other_count`, no `spurious_count`, no stop, in either enabled run) - so every link of Apple's chain (split on the interrupted mode, save, swap `sp` to `istackptr`, fall into the dispatcher, load five words, `blx`) is exercised by one number, and the four recorded arguments are the four it loaded (`target` is an address this image chose, so it cannot be a register-ordering coincidence). **And the clock is now driven by it: `setpop_count` 16 -> 4096** with `dec_count` agreeing - before this step the last deadline was ~1 ms (`dec_min = 0x4ab9` at call 15) with process 1 then spinning 8.4 M `getpid`s and no way to notice. Two states, three runs, all through the gate and all back with `No errors detected`: run A (`ENABLE_LINE 0`) has the OS console **byte-identical** to 482's and the census unchanged at 70/13/57; runs B and C (`1`) move the census to **72 entered / 13 returned** with exactly one more visit each to the same two sites - `thread_sched_call` and `Call_continuation`, the scheduler spine - on *different threads* in each run. **The minimum bar is still not met and this step says why**: the console is byte-identical in all three runs (27 lines, ending at `load_init_program: attempting to load /sbin/launchd`), `getpid` is still `0x800000`, and the 57-59 parked blocks resolve through their containing symbols to the kernel's own idle workers (`vm_pageout`, `memorystatus_thread`, `zone_replenish_thread`, `mbuf_worker_thread`, `aio_work_thread`, `mapping_replenish`, `ipc_mqueue_receive`, `lck_mtx_sleep`, `-sleep_deadline`, `lck_mtx_lock_contended`) - threads the boot's own progress stopped *before*. `tools/check_irq_routing.py` is new (six claims: the install through Apple's function and the absence of a hand store, the read-back as a gate on handler *and* target, the `ICFGR`/priority/targets derivation from the intid, the handler's three cases and their order with exactly one `entry_epilogue` and exactly two `EOIR` writes neither of them spurious, the switch as one `#define` with one `ISENABLER0` write inside its `#if`, and the order probe -> arm -> the kernel's deadline -> the conditional unmask with the arming after `g_dec_writes++`; **36 mutations refused**), and it deliberately does *not* re-assert `check_gic_routing.py`'s claims - the first draft duplicated three of them and was cut back, because a claim in two files is a claim updated in one. `check_gic_routing.py` grew to **36 mutations** and its slot-6 claim was corrected: it had been written as a *coupling* (Apple's entry when the flag is 1, the reporting stub when 0) and the build refused the image, correctly - **the vector page is not part of the switch**, the switch is one bit at the distributor, and a build that moved both would differ from the measured run in two places at once, so slot 6 is now asserted unconditionally in both states. Seven defects are recorded (225-231), every one this step's own: the coupling claim; a comment in `entry_timebase.c` asserting the arming was after `g_dec_writes++` while the code had it before (the re-entrancy argument was right - `ml_install_interrupt_handler` ends in `initialize_screen`, which can reach `setPop` and so this writer, and a re-entrant call with the count still 0 would run 482's *probe* inside the arming); four `--selftest` mutations **accepted** because each tested a name or an element where the property is a position or a behaviour (a gate disabled with `0 &&` that left the compared expression in place; a removed `return 0u` that a second one satisfied; a moved *gate* tested by the position of the *read*; an `EOIR`/`ISPENDR` swap the ordering claim never covered), plus a claim that asserted the intid constant appears *before* the `IAR` read, which is false by construction; two switch mutations written against the flag's literal `0` and broken the moment it became 1; `g_irq_late_count` collected and never published (run C exists because of it); `build_entry.sh`'s vector-slot table one step behind `entry_vectors.s`; and the duplicated draft. Layout: `.text` 5237824 -> 5238944 (A) / 5238976 (C) with the image **5454452 unchanged** (the added code occupies the aligned-fill term), `.bss` `0x80533a80..0x8058b5c8` -> `..0x8058b608`, headroom 1526264, undefined 26, 49 wraps; entry image `bb2bfb9d...` (A) / `de457aa8...` (B, C); payload 8474624 sha256 `5aa8edfc6f4c44012eafcddc8c30274ddc1d2cc1958d2d1f263873d2b20f7b97`, `kernel_size = 5949716`. One thing left named and unfixed: the fixture's timer node still says `interrupts = <1 2 0 1 3 0>` (PPI 2 and 3 -> INTID 18, 19), which 482 falsified for the virtual timer. Next (484): decide between the two candidate causes of the unchanged console - a clock nothing waits on (4096 calls and `dec_min` never left `0x4ab6`-`0x4ab9`, so the only deadline is the repeating one) or a fixture that never asks to wait (`getpid` 8.4 M times) - which is the first *non-repeating* deadline reaching `ml_set_decrementer`, and a way to see it in the log. |
| stage90 | [experiment-484](experiment-484-the-clock-is-asked-and-the-answer-is-a-countdown.md) | **the clock is asked, and the answer is a countdown** — 483's one-sided `dec_min` could not tell "every arming was `0x4ab8`" from "every arming was *at least* `0x4ab8`", and a maximum cannot repair it (call 1 is handed `DECREMENTER_MAX 0x7fffffff` by construction), so 484's census replaces the bound with **repetition**: call 2 is the reference, later calls are `dec_same`/`dec_other`, the first eight *distinct* values are published with the call they first appeared on, a ninth and beyond are counted. The arm-er is named by wrapping the entry points instead of reading three `cpu_data` offsets that have no `assym.s` spelling — `timer_call_enter` (1), `_with_leeway` (2), `_quantum_timer_enter` (3) — plus `timer_call_setup` for the join table and `thread_quantum_expire` for the callback. **The family's fourth member is a finding of the build, not a choice of this step:** `timer_call_enter1` is called only from `sfi.c` and `dtrace_glue.c`, `nm` over the 708 objects finds the name in `osfmk_kern_timer_call.o` alone, and the first build refused the image ("no branch to their wrapper anywhere in the linked image"), so the wrapper was removed and the sources stay numbered 1/2/3. **The first run did not get to say any of it:** it panicked in `sleh_abort at interrupt context`, `pc = gicd_read+4`, `lr = entry_gic_probe+264`, `far = 0xf9000000`, 17 `MACH Reboot`, and the cause was this step's own instrument — the live channel reads `TTBR0`/`TTBR1` **once**, at its first live write, and `entry_mmio_section` installed every device section into that latch; the latch is sound for the console because its sections go in before `arm_vm_init` copies the boot table into the system table four pages higher (`arm_vm_init.c:370-380`, `bcopy(boot_tte, cpu_tte, ARM_PGBYTES * 4)`), and wrong for a caller that runs after that copy. 483's run was not right, it was lucky: its first live write was `ml_init_timebase`; 484's is `__wrap_timer_call_setup`, earlier in the boot, and the latch names `0x80700000` = `topOfKernelData` = `boot_ttep`. The fix is one reading at the point of use (`entry_mmio_section` re-reads the table, through one shared definition `entry_live_ttb_base`, with the init's own window and alignment guard), one new claim in `tools/check_gic_routing.py` (**43** mutations refused, was 36 — including Apple's `bcopy` read out of `arm_vm_init.c` as the reason), and one new check `tools/check_timer_sources.py` (**7 claims, 42 mutations**). **Measured, run B:** `xnu_live_gic_l1 = 0x80704000` beside `xnu_live_l1 = 0x80700000` with `xnu_live_gic_l1_moved = 0x1` and `gic_ttbr0 = gic_ttbr1 = 0x8070404a`, and the read that faulted answers `gic_dist_ctlr = 1`, `gic_dist_typer = 0x468`, `gic_isenabler0 = 0x7fff`. **The answer to 483's question:** the arm-er is the scheduler's quantum timer — `_src3 = 2034` of the first 2048 arming entry-point calls, against `_src1 = 2`, `_src2 = 12`, `_src4 = 0`, on `BootProcessor+64` whose callback is `thread_quantum_expire` — and the written number is a countdown, not a deadline: **13 distinct deadlines across the first 64 records but 63 distinct deltas, `delta == deadline - now` in 64 of 64, and one deadline (`0x6f6c936`) held for thirty consecutive armings** while `now` walks `0x6f3dc24 → 0x6f46ca0`. The census agrees from the other side — nine distinct values in the first eleven armings (`0x2eda2 0x2ec25 0x2ed67 0x2ed34 0x2ed55 0x2ed7a 0x2ed6a 0x2ed63` at calls 3..10, a ninth `0x2ed71` at call 11) while `dec_seen_value = 0x2ed6a` recurs 8 times — and **at least 5 592 armings** (`dec_same` past 2048 with `dec_other = 0xdd8` at the same sample) against 483's 4 096, with `timer_call_setup` published in full: 24 records naming 25 registrations across the quantum timer (`BootProcessor+64` ← `thread_quantum_expire`), BSD's `alarm_expire_timer` ← `alarm_expire`, `ntp_loop_update`, seven threads' `thread_timer_expire`/`thread_depress_expire` pairs and the `thread_call_groups` delayed/dealloc timers. **The minimum bar is still not met and now says exactly what is missing:** the OS console text is byte-identical to 483's (`ostext_chars = ostext_total = 0x3ee`, block `0x20000`), the abort window is the same 16 records with the same 14/2 kernel/user split and every kernel pc moved by exactly `+0x6E0`, and `xnu_live_getpid_count` reaches `0x00800000` — **the same 8 388 608 calls** — so the boot is stopped by the fixture's own program, not by the clock and not by a driver. Layout: `.text` 5240672 → 5240896, image bytes **5454452 unchanged**, `.bss` `0x80533a80..0x8058b6c8` (+192), headroom 1526072, undefined 26 / 54 wraps (48 reached, 1 same-object, 1 never called, 4 by address — `vcputc getpid mmap thread_quantum_expire`); entry image sha256 run B `734a6fed36884c2930d5079ff68d18534a13472127da9c9598ed0b2839cba95e`, payload `stage90-qcdt.img` 8474624 sha256 `a65b8cddd0f1b5d1f0443ea0a7d13d6b81071fe0a0be1807d9efb3ab323846bd`, `kernel_size = 5949716`; two runs through the gate, both exit 0, log 438482 bytes / 17 panics (run A) and **499465 bytes / 0 panics (run B)**, device back on Android on its own |
| stage90 | [experiment-485](experiment-485-the-boot-thread-finishes-and-the-tree-is-registered.md) | **the kernel's own boot thread runs to the end of its tail, and the tree's 21 nodes are registered** — every run so far was read through what the boot printed (458), what process 1 did (475/480) or what the clock was asked (483/484); this step reads the kernel's own main thread. `kernel_bootstrap_thread`'s closing calls are Apple's (`startup.c:628-646`: `bsd_init`, `OSKextRemoveKextBootstrap`, `kdebug_free_early_buf`, `serial_keyboard_init`, `vm_page_init_local_q`, `thread_bind(PROCESSOR_NULL)`, `vm_pageout`), and **five of the seven are wrapped**, each writing its position **before** the call it wraps because the fifth never returns: `xnu_live_tail_seq` = how many have run, `xnu_live_tail_idx` = which one, `xnu_live_tail_site` = the *real* function's address. `bsd_init` is not wrapped (its completion is audible in the console text) and `thread_bind` is not either — **Apple's own body is the argument**: it calls `thread_bind` twice, the earlier time to `processor` (`startup.c:452`), so a record of the name would fire away from the tail and name no position; the remaining five are called from exactly one place in the whole kernel, which is what makes a record of one of them a position (`tail_seen[4] != 0` is `vm_pageout` *entered*, and `tail_seen[0..3]` beside it is the control). **Measured, both runs:** `tail_seq = 5`, `tail_idx = 4`, fifth site `0x80071ef4` = **`vm_pageout+0x0`**, in Apple's order (`0x8011b9c0 0x80048c50 0x80041588 0x80024310` for the first four) — and `vm_pageout` cannot be entered unless `bsd_init` returned, so **"enter the OS" is a reading rather than an inference**: the kernel then goes on running threads (`xnu_live_block_seq = 0x48`, `xnu_live_kthread_cont` names 30 continuations, threads sleeping with timeouts). **The census runs inside the fifth wrapper**, downstream of every match attempt and upstream of the loop that never ends, and walks the tree the way the OS walks it (`getRegistryRoot()`, then that entry's child in `gIODTPlane` via `getChildEntry`, children through `getChildSetReference` + `getName(plane)`, `__state[0]` through `IOService::getState` — the tree's one implementation, pinned by `build_entry.sh` by disassembly and by counting definitions — and `__state[1]` by the offset 455 read out of `getState`'s own `ldr r0,[r0,#36]`): **21 children, every one named and every one at `__state[0] = 0x1e` = `Registered | Matched | FirstPublish | FirstMatch` (`IOService.h:72-76`), `__state[1] = 0`** — `device-tree`, `iokit-platform-scaffold`, five `msm8974-*`, `iokit-catalog-property-dryrun`, five `stage90-*-personality`, `chosen`, `defaults`, `memory`, `cpus`, `arm-io`, `msm8974-io`, `interrupt-controller`, `timer`, in the fixture's own creation order, one for one (`stage90_main.c`) — a fourth cross-check on top of three readings that keep the table from being self-confirming: **the two child counts agree** (`getChildCount(plane) = 0x15` and `OSArray::getCount` on the set that accessor is written in terms of `= 0x15`), **the table agrees with the OS's own lookup** (census child 13 `= 0xc061d8f0` *is* what `fromPath("/chosen", gIODTPlane)` returns, and `fromPath("/chosen/memory-map")` returns `0xc061ddc0`, the same pointer 461's call from inside the `IODeviceTreeAlloc` wrapper got), and **the registry's own child moved**. **Run A answered the first question and falsified the second instrument's premise**, and the defect it found is the step's own: 461 had read the object `IODeviceTreeAlloc` returns and called it "the tree's root nub" in a comment, so run A walked *that* pointer and reported `dtk_calls = 1`, `dtk_count = 0` — while the same log's 462 walk, nine records earlier, held `walk_count = 0x1` with `walk_kids = 0x15` (the registry root's IODT child, 21 children). Two records answering "how many children does the tree have" differently and **no way to tell from the log which object each was about**: root, plane and set were report-only keys and **the report path never ran — `xnu_entry_dtk_*` counts 0 in both runs**, because the payload writes its report from its own program after the kernel returns control and in this step the kernel never does (459's full-buffer defect was the same lesson one step earlier). The fix is in the live channel: read the root the OS reads, publish **both candidates together** (`recorded`, `root`, `same`) with both child counts and a third record for *no reading at all* (`xnu_live_dtk_none`: 0 = no IODT plane, 1 = the registry root has no child in it), and fail the build if any record stops being written. **Measured, run B:** `plane = 0xc05e6690`, `recorded = 0xc061df78`, `root = 0xc05e8030`, **`same = 0`**, `set = 0xc0625a80`, `kids = count = 0x15` — immediately after `IODeviceTreeAlloc` returns the slot *is* the returned object (`walk_seq = 1`: `walk_first = 0xc061df78 = dtalloc_ret`) and by the first `fromPath` of `bsd_init` it is a different object with the same 21 children and **the same child-set pointer**; 461's recorded root is a different object from the registry's child in *both* runs, at every moment after that `fromPath`. The OS's console now adds the line 461's frontier died on — `Added memory device md0/rmd0 (02000000/0D000000) at 0000000080505000 for 0000000000002000` — then `load_init_program: attempting to load /usr/local/sbin/launchd.development` → `errno 2` → `/sbin/launchd`; the 32 abort records have a new centre of gravity, the copies that build process 1's arguments (`exec_save_path+0x38`, `exec_copyout_strings+0x128`, `load_init_program_at_path+0x30` → `mmu_kvtop_wpreflight+0xc`, the `copyin/copyout` word loops, `copypv+0x84`), each handled and retried (`sleh_back = 1`, `at_back = 1`), plus the same two user-mode aborts (`0x00001118`, `0x00001124`). **The minimum bar is half met, and the half that is met is measured** — "the basic drivers run" is a table of the generic IOKit machinery (nodes registered, published, matched) and **not** of a device driver, and this image still contains no Apple `IOPlatformExpert` subclass (461's reconnaissance). New check `tools/check_boot_completion.py` (**6 claims, 52 mutations refused**, baseline-checked first): the indices and the order of the transfers in the linked `kernel_bootstrap_thread` are compared against Apple's `startup.c`; the exclusivity of the five call sites is read out of the object pool (one defining object, one referencing object, the same object for all five, and it is the one that defines the thread); `thread_bind` must still be referenced by **more than one** object, because that is the argument for leaving it alone; the tail count is one `#define` read by both the counter array and its guard; and the last image claim counts a **tail branch** as well as a `bl` — `vm_pageout` is `noreturn`, so `-O2` emits `b __wrap_vm_pageout` after popping `{r4, lr}`, and a check that looked only for `bl` would report that the fifth wrapper is never reached, which is the one reading this step exists to take. **Three defects of its own session** (237–239): **a root named in a comment, read from a pointer, and never compared with the root the OS uses** (a measurement whose subject is asserted by prose rather than published as a value, 237); **a claim whose precondition was never satisfied, so it never ran** — the index claim built its expected numbering only if *every* name was called exactly once and `thread_bind` is called twice, so `expected` was always empty and a swapped index was **accepted** by `--selftest`, which is what the mutation said (238); and **a mutation that mutated nothing** — `entry_note_boot_tail(0u,` → `(1u,` then `(1u,` → `(0u,` undid the first edit, so the no-op was rightly accepted and both patterns now carry the call they wrap (239). Layout: `.text` 5240896 → **5246784**, image bytes 5454452 → **5470836**, `.bss` `..0x8058f8c8` (360008), headroom 1509176, undefined 26 / **59 wraps**; entry image `xnu_arm_entry.bin` sha256 run B `392abd16eb1fa15e5deba902b0097be8c9471d9f8a17229536f045d9ad9aacf9` (`.elf` `cb11c4b4c340ad81a9296457ab204cd02fee6da3d60e7691b0dad6d12405aa44`), payload `stage90-qcdt.img` 8491008 sha256 run B `ae41f6090a2fa9646c567be9b9612807e500bc50d11b7a23d627e7221ed5deef`, `kernel_size = 5964890`, `dt_size = 2521088`; both runs through the gate, exit 0, device back on Android on its own both times, logs 499322 (A) and 504130 (B) bytes. **486 asks two things this step opened:** *one registry slot, two root objects* — publish `getChildEntry`'s result and the registry root's child-set pointer at both moments, the class name and retain count of each candidate, and wrap the detach path to see which call removes the first root and which builds the second (462's "the delete list was every entry" is the lead), because until then "the tree the OS is handed" is a phrase with two referents and every step that used 461's `g_dtplane_root` as *the* tree root inherited the wrong one; and *whether the exec-path copy faults are serviced demand faults* — wrap `vm_fault`/`arm_fast_fault` (the pair owed since 480) and publish the VA's translation before and after the handler returns |
| stage90 | [experiment-486](experiment-486-the-tree-roots-block-changes-hands.md) | **the tree root's block changes hands — 485's "one registry slot, two root objects" is answered, and the answer is not two trees.** The registry's IODT child is the **`IOPlatformExpertDevice`** `StartIOKit` makes (`new IOPlatformExpertDevice`, `IOStartIOKit.cpp:156-157`) and it adopts the tree root's **21 children** — **the same child-set array, `0xc05d13a0`, that the tree root answered with before the handover**, unchanged at every walk record and at the census, so the children were not re-parented into a new array; the object 461 recorded as "the tree's root nub" is not a second tree either: **its block was released and reused**, and by the census the address `0xc05c9cb8` holds an **`IORootParent`** — the object `IOPMrootDomain` makes at `IOPMrootDomain.cpp:1247` — with 0 children and no child set, because it is a different object that never registered in the IODT plane. Three classes, read off the objects' own vtables and named in one run from one image: `walk_seq = 1` inside the `IODeviceTreeAlloc` wrapper `0xc05c9cb8` = **`IOService`** (`_ZTV9IOService + 8`), every `fromPath` after it `0xc05943b0` = **`IOPlatformExpertDevice`** (`_ZTV22IOPlatformExpertDevice + 8`), the census's `recorded` `0xc05c9cb8` = **`IORootParent`** (`_ZTV12IORootParent + 8`) — the vptrs are the image's own labels plus the ABI's 8-byte preamble, which is exactly the arithmetic run B got wrong, visible in the same log as the labels it was wrong about. Census: `dtk_plane = 0xc0592960`, `recorded = 0xc05c9cb8` (0 kids, no set), `root = 0xc05943b0` (21 kids, set `0xc05d13a0`), `same = 0`, both child counts 21, the 21 children the same ones 485 named at `__state[0] = 0x1e`. **The step's own prediction was falsified by its run**: the `entry_note_dtrec` comment, inheriting 461, said the returned entry would be an `IOService` with zero children and no child set *because `IORegistryEntry::init` removed the child-set key from its own table* — true of the tree root and what the numbers look like — and the run answers `IORootParent`. **The mechanism, as far as a log carries it**: `IOPlatformExpertDevice::initWithArgs` (`IOPlatformExpert.cpp:1558-1566`) is `dt = IODeviceTreeAlloc(dtTop)` then `super::init(dt, gIODTPlane)`, so in `IORegistryEntry::init(old, plane)` (`IORegistryEntry.cpp:333-385`) `old` is the tree root and `this` the platform expert device; `IOREGSPLITTABLES` (`:121`, unconditional) makes `registryTable()` `fRegistryTable`, so `fPropertyTable = old->dictionaryWithProperties()` is a **shallow** copy (each value `taggedRetain`ed) and the **registry table object changes hands** (`fRegistryTable = old->fRegistryTable; old->fRegistryTable = fRegistryTable->copyCollection();`), which is why the two key removals hit `old`'s *copy*; the parents' loop then walks `getParentSetReference(plane)` — now *this*'s table, i.e. old's original, holding the meta root's child array whose only member is old — and `next->breakLink(old, kChildSetIndex, plane)` (`Links->removeObject`, `:1389-1410`) drops its last reference (count 1: `attachToParent` added the array's, `IODeviceTreeAlloc`'s own `parent->release()` left exactly that one), so **the tree root is freed inside that loop and the children's loop then rewrites the links of a released block** (which did not crash — freed block, live mapping, and `dtrec_*` shows what it holds), and `patriarch = new IORootParent` gets the block back. **What is argument rather than reading is stated as such**: nothing here watches the release or the allocation, so 487 reads the class of `g_dtplane_root` at *every* walk to bracket the change and wraps `OSObject::release`/the allocator under it to make the second half a record. **The road to the class is the object's own vtable, and it took four runs to get right — three of them wrong, each producing a log that reads like an answer.** Run A declared and called `OSObject::getMetaClass` **by its mangled name**, and a virtual called by name is the *base* implementation (`{ return &gMetaClass; }`, `OSObject.cpp:57-58`): all nine records say `OSObject`, and the tell is in the same log — its other instrument, the metaclass walk, printed `IOResource` and `IOPlatformExpertDevice` at the same moment (a correct answer to a question nobody asked, 241). Run B indexed the object's first word with the **vtable's** index 9 — but the vptr points *past* the two-word preamble, so index 9 of the object's table is `OSObject::taggedRetain` (`0x80133d38`), a `void` function whose leftover `r0` was **1**, and `getClassName`'s `ldr r0, [r0, #12]` took an unserviceable data abort (`DFSR = 0x5`, `DFAR = 0x0d`) with the handler entered **64** times (`sleh_seen = 0x40`) and `sleh_at_back` written **zero** times against run A's twelve — run B's log is 443681 bytes with no `walk_*`, `dtc_*` or `cls_*` records at all. **The tool that was wrong is the check** (243): `claim_slot` read `_ZTV8OSObject`/`_ZTV9IOService` out of the image, found each class's own `getMetaClass` at index 9 of the *label*, and required the code's `#define` to equal 9 while the code indexed the *object's* table — one index, two tables, nothing comparing them, this repository's oldest defect in the one place the whole step depended on it. Run C then read the "preamble" **at** the vptr — the vtable's first two function slots, i.e. the class's own destructors — so every object was refused (`cls_took = 0` nine times, `dtc_class*`/`dtrec_class*` all zero) **and the refusal still named both classes**, because `pre0`/`pre1` are code addresses `nm` resolves: `0x8013cfb0`/`0x8013cfb4` = `IOService::~IOService()` and its deleting form, `0x801754a8`/`0x801754ac` = `IOPlatformExpertDevice::~IOPlatformExpertDevice()` and its deleting form, with `0x804999ac` = `_ZTV9IOService + 8` and `0x8049f990` = `_ZTV22IOPlatformExpertDevice + 8` (244). Run D is the measurement: `STAGE90_VTABLE_PREAMBLE = 2`, the load is `STAGE90_VTABLE_META_CLASS - STAGE90_VTABLE_PREAMBLE` = index 7 of the object's table, the two tested words are `vptr[-2]`/`vptr[-1]` (0/0 in both vtables, which is what a primary vtable with no RTTI pointer looks like — read from the image, not assumed), and the call is made only when the preamble is zero **and** the slot holds an address in `[0x80000000, &__bss_start)`; `entry_note_class` publishes the object, its vptr, both preamble words, the slot's word, the metaclass and whether the call was made, so an object that is refused is a record rather than a hole — 486's own fault, turned into a precondition. New check `tools/check_registry_adoption.py` (**8 claims, 66 mutations refused**, baseline checked first): the chain `StartIOKit -> initWithArgs -> IODeviceTreeAlloc` then `super::init` -> `IOService::init` -> `IORegistryEntry::init` read out of Apple's sources, the handover's order (shallow copy, both key removals, then the parents' child sets, then the children's parent sets, one link pair at a time), the copy's shallowness, the two records' keys, the vtable slot and preamble arithmetic read from the image, the guard, the report/live writers, and the image's own transfers. **Seven defects of its own session** (240–246): **one call declared twice** (the file already declared `getClassName` for the metaclass walk, 240); **a virtual called by its mangled name is the base implementation** (241); **the source name is not the linked name** — `entry_class_words` is `static` with two callers, so GCC emitted `entry_class_words.part.0.constprop.0` and a claim comparing a transfer target with the bare name **failed the build on an image that was right**, while the selftest's mirror image raised `KeyError` instead of mutating (242); **one index, two tables, and nothing comparing them** (243); **a constant defined and not used as a displacement** — a guard that refuses everything and a guard that reads the wrong words are the same log, and only `nm` on the words it published separated them (244); **a pointer treated as one subject across a lifetime** — the falsified prediction, and every reading of that pointer between the two moments inherited the same assumption (245); and **a count written in prose, twice wrong, compared with nothing** — the `--image` message said "three of the seven claims" when the file has eight claims and two image readers, now measured by `image_claims()` and required to equal `IMAGE_CLAIMS` (246). Layout: `.text` 5246784 -> **5248352**, image bytes **5470836** (unchanged — `.data`'s 16 KB alignment absorbs it), `.bss` `0x80537a80 .. 0x8058f908` (360072), headroom 1509112, undefined 26 / wraps 59; entry image sha256 `7b84318c9d7a69b381923e7483b20c7195c08c393bc206c1dba7b074cec3a6e0` (`.elf` `ebd00a54fdda42f6503004e313b6b949f67e9f39c70e03107d3140cdc47941f3`), payload `stage90-qcdt.img` 8491008 sha256 `c20d8eba56afab30241e11634604ae34354f67f8f1c5a1dce6a6141198cdb597`, `kernel_size = 5966100`, `dt_size = 2521088`; four runs through the gate, exit 0, device back on Android on its own every time (504460 A / 443681 B / 506808 C / 507024 D bytes), 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `checks=5`/`failures=0`, `checksum=0x9071e9b9`. **487:** the release as a reading (the class of `g_dtplane_root` at every walk, plus a wrapper on `OSObject::release`/the allocation under it, since 462's "the delete list was every entry" is the other half), and — unchanged from 485 — whether the copies that build process 1's arguments are serviced demand faults, by wrapping `vm_fault`/`arm_fast_fault` (owed since 480) |
| stage90 | [experiment-487](experiment-487-two-planes-one-state.md) | **two planes, one state - the drivers ran, and the step's prediction was falsified by its own run.** The tree's 21 top-level nodes are no longer the objects `IODeviceTreeAlloc` made: **every one of them is an `IOPlatformDevice`**, the nub `IODTPlatformExpert::createNub` makes (`IOPlatformExpert.cpp:1279-1290`: `new IOPlatformDevice` at `:1283`, then `nub->init( from, gIODTPlane )` at `:1285`, which is the *adoption* 486 found one class up), so Apple's nub pass ran over all 21 - `configure` -> `processTopLevel` (`:1269`,`:1314`) -> `createNubs` (`:1293`, `nub->attach( parent )` at `:1305`) -> `createNub`. The second plane is the same object as the first census's root: `IOService::getServiceRoot()` answers `0xc05590a0` on `gIOServicePlane` (`0xc0557758`), `svc_same = 1` against the ODT census's root, because `attach(0)` sets `gIOServiceRoot` to the object `IOStartIOKit.cpp:153-157` made - and it holds exactly **two** children: `0xc0591740`, class **`MSM8974PlatformExpert`** (vptr `_ZTV21MSM8974PlatformExpert + 8`, metaclass `0x8057e80c`), i.e. *this kernel's* platform expert instance, attached under the root by the match path (`IOService.cpp:3334`); and `0xc058ef20`, class **`IOResources`**, which is `gIOResources` (the earlier walk's `wcls_rsvc` instrument names the same address, and `setPlatform`'s `gIOResources->attachToParent( gIOServiceRoot, gIOServicePlane )` at `IOService.cpp:1187` is why it is there). The 8-byte truncation the instrument publishes cannot separate `IOPlatformDevice` from `IOPlatformExpertDevice` (both read `IOPlatfo`), so the classes are settled by the **vptr**: 21 of 21 child records carry `0x804a0268` = `_ZTV16IOPlatformDevice` (`0x804a0260`) + 8, the root carries `0x8049fed8` = `_ZTV22IOPlatformExpertDevice` + 8, and 485's recorded tree root is by then `IORootParent` (`0x8049e088`) with 0 children and no child set - 486's reading, re-measured in a new build. The two service children's names *equal* their classes because `IORegistryEntry::getName(plane)` falls back to `getMetaClass()->getClassName()` (`IORegistryEntry.cpp:749-764`) for an object adopted into the ODT plane and never named in the service plane. Census: `dtk_plane = 0xc05577f8`, `dtk_root = 0xc05590a0` (21 kids, set `0xc0596520`), `dtk_recorded = 0xc058ef78` (0 kids), `dtk_same = 0`, `svc_set = 0xc05ac600`, `svc_kids = svc_count = svc_shown = svc_named = svc_matched = svc_registered = 2`, `svc_none = 0`, every child's `__state[0] = 0x1e`, `dtcc_calls = dtcc_named = 21`; the class instrument's arithmetic closes exactly - **32** `cls_*` records (`cls_calls = 0x20`) = 7 walk + 2 roots + 21 children + 2 service children, and `live_records = 0x90e` (2318) of the 4096 cap with `refusals = 0`. **The step's prediction was falsified**: its design note said `IOService` for all 21 children and an empty service plane, on the premise that no concrete `IODTPlatformExpert` subclass exists in this kernel - the premise was wrong, since `stages/stage90/xnu_platform/MSM8974PlatformExpert.cpp` is that subclass and the fixture's `/msm8974-platform-driver` personality (`IOProbeScore = 0x650`) makes it the root nub's driver. What is argument rather than reading is stated as such: nothing watches the allocation, so 'these nubs came from *this* `createNubs` pass' rests on the shape around it; 488's record for it is a third census of the platform expert instance's own service children, since `createNubs` attaches the 21 nubs to `this` and `0xc0591740` should therefore have 21. **And the boundary with 462 is sharpened rather than answered**: `StartIOKit` is called from `PE_init_platform` (`pexpert/arm/pe_init.c:279`, reached from `arm_init.c:159`) long before `IOFindBSDRoot` (`bsd_init.c:1099`), so both the adoption and the nub pass are already done at the moment 461 measured `fromPath` on `/chosen` failing at the first component - 'the children had not been made yet' is not the reason. New check `tools/check_driver_plane_census.py` (**8 claims, 46 mutations refused**, baseline checked first): the two plane globals, the nub pass in the image with its caller chain, both `createNubs` calls, `deleteList`/`excludeList` still pure virtual in the header, the one shape of the two censuses, the equal caps, the five distinct sites, every live and report key's writer, the two predicted class names, and the wrapper's order (record, ODT census, service census, the call that never returns). **Four defects of its own session** (247-250): **a mutation that names its neighbour instead of the line it is about** - 485's selftest needle was its two adjacent calls and 487 put a census between them, so `_bump` aborted the build on an image that was right (247); **two mutations that stopped mutating, silently** - one replaced the *first* spelling while the claim is an existence test over the whole file, the other patched a literal `movw r1, #7924` that 487's own code moved, and both were reported `ACCEPTED` and caught only because the selftest reports acceptances at all (248); **the recorded ritual and the build's own cross-check disagreed** - step 2 run without `XNU_KERNEL_CONFIG` assembles the ARM layer against RELEASE's `assym.s`, and `check_assym_cswitch.py` refused the build with the two offsets (249); and **the `assym.s` generator's flag list is a hand-kept copy that has drifted** - 'kept in step by intent rather than by construction', says its own comment, and now `XNU_KERNEL_CONFIG=STAGE90_XNU ./tools/gen_assym.sh` **exits 2** (`btlog.h:88: unknown type name 'leak_site_proc'`, because four of its force-includes reach `kern/debug.h` before the flat list defines `DEVELOPMENT`, so the guard latches; measured by bisecting the list and by moving the include to the front, which compiles), while what it would produce is RELEASE's triplet `1464/1480/1488` - the offsets 468's run died on - against the configuration's `1480/1496/1504`, which the kernel's **own compiled object** confirms (`osfmk_arm_machdep_call.o`'s `thread_get_cthread_self` is `ldr r0, [r0, #1496]`), so the stale artifact is right and the tool that wrote it is not (250). Layout: `.text` 5248352 -> **5254568**, image bytes **5470836** (unchanged), `.bss` 360072 -> 361032, headroom 1508152, undefined 26 / wraps 59; entry image sha256 `43524b4f130ee4e8e90437f820dd9bfb92e9c4148e24452c951ccc2f687dc81a` (`.elf` `4e2efd849c17eba594a088aca239dbeb1add50c34524e50d367879d5ef408130`), payload `stage90-qcdt.img` 8491008 sha256 `897b9dbfce7355e620597d213e340f229af3ea7a70e049a65214b3784971977d`, `kernel_size = 5966100`, `dt_size = 2521088`; one run through the gate, exit 0, device back on Android on its own (534988 bytes, 7082 lines), console byte-identical to 486's at the same lines, `No errors detected`, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `entry_checks`/`entry_failures` 0, `entry_checksum = 0x9071ec79`, `panic_entered = 0`, four data aborts all handled (`sleh_storm = 0`). **488:** the generator's flags derived from `build_xnu_arm_kernel.sh`'s rather than copied, with one `TH_*` offset pinned against a **kernel object**'s disassembly (`thread_get_cthread_self`'s `ldr r0, [r0, #1496]` is the pattern), since `assym.s` is currently checked against the assembler that reads it and against no C compiler; the platform expert instance's own service children, as the record that the nubs came from its `createNubs`; the release as a reading (`OSObject::release`/the allocator under the tree root's block); and - unchanged - whether the copies that build process 1's arguments are serviced demand faults, by wrapping `vm_fault` (`osfmk/arm/trap.c:443` user, `:563` kernel) rather than `arm_fast_fault`, which handles only pmap reference/modify faults (owed since 480) |
| stage90 | [experiment-551](experiment-551-exit-2-measured-adb-alone.md) | **exit 2 measured adb alone, so a return the SoC made and adb missed was read as a hang** — found by the peer session reading `run_and_capture.sh`, verified here: the bounded wait polled `sudo adb devices` and nothing else, so a run whose device came back while Android failed to come up would exit 2 and be recorded as a non-return — and that is the one reading the frozen arm cannot afford to get wrong. Today makes it concrete: `2717:0368` with serial `4a2fe00b` on `usb 3-10` for **18 s** at 19:22:45, dropped without reaching `18d1:4ee7`, plus two earlier `2717:0368` windows that ended in `f006`. The criterion is now two readings — `adb devices` says where to capture from, the **host's own USB log** says whether the device came back — and the host-log count is keyed on `SerialNumber: 4a2fe00b` and **not the port**, because `usb 3-10` also carries the serial-less `05c6:f006` occupant (which today appeared on its own after **2 h 38 m** of an empty port), so a port-only test would read that as a return. New contract: **exit 2 only when adb is empty *and* the host log shows no new enumeration; exit 3 when the host log shows one** ("REFUSING to call this a non-return" — a capture failure is not a hang, with the re-read command); **UNREAD** (still exit 2, saying it is not claiming what exit 2 asserts) when the counts cannot be compared. Verified in four states with the decision block extracted verbatim and `serial_enum_count` stubbed, and **the fourth state was a defect in my own first version**: a *falling* count (`9 -> 4`, the `dmesg` ring rotating) made the comparison meaningless while the script announced "the device did not come back" — a number whose shape was not checked before it was compared, the project's oldest class, written while quoting the rule about it. A missing-failing `serial_enum_count` takes the UNREAD path, which is the conservative direction. **Leaves one contract drift, reported not patched:** 550's gate clause prints "its exit 2 is adb's reading alone", which this change makes false, and the new exit 3 is in no gate prose. Host-side only: no device action, no build, frozen pair untouched, gate still exits 0. |
| stage90 | [experiment-554](experiment-554-the-reader-called-the-prediction-a-failure.md) (superseded in part by [555](experiment-555-the-death-is-read-from-the-key-and-the-criterion-is-derived.md)) | **the reader called 533's pre-registered prediction a failure** — a host-side repair to `run_and_capture.sh`'s verdict block (no device, no build), the same class as 551 one step further in: 551 repaired what *exit 2* measured, this repairs what `summarise_log`'s idle-window block measures. Clause (1) (`:204-213`, 540/`ec95e2f`) printed `FAIL  N 'panic ... sleh_abort' record(s) - the death 519 and 520 died is back` and stated **"The arm's verdict is the panic's ABSENCE"**; clause (3)'s `DIED IN THE EXIT` set `verdict_ok=0` unconditionally and said "That is this arm's prediction failed"; the close said "the arm's prediction is the panic's absence and nothing else". **547 §4 (`9675e82`, committed before the run) predicts the opposite for the enable-off cell** — "the prefetch abort panics at `pc = the popped value & ~1`, and the device comes back on XNU's `MACH Reboot`" — and 533 §5.2/§5.4 read the same three-note pattern as "**inside `platform_cache_idle_exit`** - the `pop`" and list "whether Apple's own panic path runs" as an *item to read*. So on a returning 533 the reader would have printed two FAILs for the outcome both documents name as the prediction: **a reader that scores the pre-registration as the arm's failure cannot test it**, and it is clause (2)'s already-repaired defect (a check whose shape was copied from the previous arm) five lines up and forty lines down. What separates the shapes is one extraction and the only new mechanism — `grep -a -c 'lr: *0x800462dc'`, the literal off 520's own dump (`/tmp/cancro-last_kmsg.txt:4006`, `r12: 0xde58b701 sp: 0x8054fed0 lr: 0x800462dc pc: 0x04b79074`), the return address the exit's `bl FlushPoU_Dcache` pushed at `0x800462d8` whose two possible overwriters (`InvalidatePoU_Icache` `0x80046304`, `flush_core_tlb` `0x80046308`) 549 showed are skipped — exactly once in that log and only at the fatal dump. Clause (1) is now **three-way** (no panic → PASS; that `lr` → **PREDICTED**, `verdict_ok` untouched; any other shape → FAIL), clause (3) scores no failure when clause (1) attributed the death, and the close is three-way with a `case` putting 547 §4's prediction beside the arm clause (2) read (a log whose pair says 522 is the *surprise*, since 547 §3 has both enable-on rows dying with no log). **The pin's failure direction is noisy, not silent** — a moved exit stops matching and lands on FAIL, so the comment forbids loosening it to `lr:` alone. Tested in five states (`PREDICTED`+533; moved `lr` → FAIL+disagreement; 522's pair → the caveat; no panic + `post` → PASS×3; unreadable pair → the `*` branch), plus `bash -n`, `--summarise` on 520's unmodified log (no block at all) and the gate read-only — **exit 0**, its three exit sites re-derived at gate time (`:499`/`:511`/`:521`, moved by this edit, which is why 552 made it compute them). **The prediction is not changed**: 547 §4 stays the criterion and the reader was the stale side. Residual: the arm caveat is prose and not a check, and clause (1) still greps only `sleh_abort`. **Leaves drift reported not patched** — the gate's 526-era narration still says "the arm to run is 526" and describes the `_cwe_*` pair as `_set` with C *set* (conditionally right, but it describes a spent arm). No device action, no build, frozen pair `1daaf44e…`/`f202f246…` untouched, device off the bus awaiting a power press. |
| stage90 | [experiment-555](experiment-555-the-death-is-read-from-the-key-and-the-criterion-is-derived.md) | **the death is read from the key, and the criterion is derived** — 554's finding stands (the idle-window block printed FAIL for the death 547 §4 predicts) and this is the same file after the peer session reviewed the *criteria*; host-side, no device, no build. Four of their five points are defects in 554's first version. **(1) The abort's registers are keys, not dump text**: 520's log publishes the fatal episode as `xnu_live_sleh_{storm,seen,pc,lr,sp,cpsr,fsr_frame,far_frame}` (`:8341-8350`, identical to the dump at `:4006`) **and holds an earlier unrelated episode with a different `lr`** (`:7651`, `0x8029231c`) — so a whole-file match on the dump text can be satisfied by the wrong episode; `keyval`'s `tail -1` binds to the fatal one ("the episode that panics is the last one that happens"), and the dump line's two-space padding makes a single-space pattern miss *ambiguously*. **(2) The criterion is derived, not pinned**: `exit_pop_lr_addr()` reads `platform_cache_idle_exit`'s start/size from the ELF's symbol table, disassembles that body, and takes the address after the `bl <FlushPoU_Dcache>` — where the instruction is `movw r0,#4516 / movt r0,#32853`, the load of `up_style_idle_exit` at `0x805511a4`, so the address identifies itself; asserted 4-byte aligned, inside `pmap_kernel_va`, not the function entry. **It failed on its first run** because the rule assumed a *symbol line* for the address after the `bl`, which `llvm-objdump` prints and `arm-none-eabi-objdump` does not — it now accepts either, colon stripped. The literal survives only as a **labelled** fallback (`*** PINNED LITERAL … NOT derived ***`); whole summarise 0.16 s. **(3) The `lr` test's scope is asserted from the log**: `0x800462dc` is the pop's `lr` only where `InvalidatePoU_Icache`/`flush_core_tlb` are skipped, which needs `up_style_idle_exit != 0` **and** `ncpu < 2` — published by name as `xnu_live_pce_up`/`_ncpu` (549), so `other` and `unread` are UNREAD with the reason printed, never FAIL. **(4) 547 §4's third row is a `FALSIFIER` line of its own, not a clause inside a PASS sentence** — for the enable-off pair it is the reading that forbids 535, and the close points back at it; the return axis stays the runner's exit code (551), since the summariser has none, and `pc` is printed as corroboration and never compared (it is the popped word, `pc = r11 & ~1`). **(5) Conceded**: clause (3)'s exemption is conditioned on an attributed death **and** a panic — already true by construction, now printed. **Tested against nine states**, three of them the new criteria's own refusal cases: the normal 533 shape; the fatal key moved → FAIL; the 522 pair → the caveat; no panic + `post` → PASS + `FALSIFIER`; unreadable pair; `pce_up=0` (the other cache arm) → UNREAD, criterion does not apply to this log at all; both `pce` keys unreadable; **an earlier episode's `lr` rewritten to the criterion with the fatal one moved → still FAIL**, the binding demonstrated rather than argued; and `OBJDUMP=/bin/false` → the labelled literal. Plus `bash -n`, `--summarise` on 520's unmodified log (no block — its image published no `slot_cwe_` keys), the derived address equalling the literal in this build (a cross-check, not the criterion), and the gate read-only, **exit 0**, its three exit sites re-derived at gate time (`:629`/`:641`/`:651`). The gate's 526-era narration is still drift and is still reported rather than patched. No device action, no build, frozen pair untouched, device off the bus awaiting a power press. |
| stage90 | [experiment-556](experiment-556-one-address-three-definitions-and-the-gate-binds-the-readers.md) | **one address, three definitions, and the gate binds the reader's to the image** — a host-side change to `stages/stage90/preflight_boot_check.sh`, no device and no build, in the same cluster as 551/554/555: a reading in this phase's path measured against the wrong thing. Two parts, one defect. **(a) The 526-era narration told the reader what the ending would mean, when the arm is what decides it.** Its checklist item (4) said *"the same `sleh_storm 9` at the same `pc` as 520 would say the mechanism is neither the capture nor the re-enable, and any later ending is progress"* — written **before** 547 §4 (committed ahead of the run) **pre-registers that death for the enable-off arm** (the pass reaches the exit, the push runs with `C` clear, the pop loads the stale words, the prefetch abort panics). So the narration would have read the prediction as the negative control, on the very arm this gate is about to run. Replaced with the arm-decides-the-meaning text, which states the prediction, states the **falsifier** — *a return with no pop death at all*, the one reading that forbids 535 as designed — and hands the judgement to `--summarise`, which is what actually derives the address and separates the cells by the log's published `xnu_live_sleh_lr`; the narration then restates no rule, so a death read there by eye is read *that way* and not by that sentence. **(a2) The same item gains what the arm costs and which key localises the death**, both read off 520's own log and re-verified here before being written into a check (557's reading, folded in by agreement since this file owns the narration). The per-pass publishers have **exactly one record each** — `xnu_live_slot_pre_calls`, `xnu_live_slot_rtcpre_calls`, `xnu_live_sip_seq` and the `xnu_live_pce_seq`/`_after_seq` pair — and each publishes on `n <= 4u || (n & (n-1)) == 0`, so **one record means `n = 1`, one call** (a second call would have published `n = 2`); the door counter's last published record is `32768`, so the bypass ran at least that many times while the **cache window itself was entered once, and that one entry is the death**. Two consequences now in the gate: a run of this arm gets **one pass at the exit**, so a non-return costs a power press and nothing else — which is why this phase is expensive in **boots**, not in passes — and the diagnostic item is the **bracket**, not the ending: `pre` and `rtcpre` published with `xnu_live_slot_post_calls` absent is what puts the death inside `platform_cache_idle_exit`, with no second pass to confuse it. **Plus a trap named rather than inherited:** the ram console has **two writers in two blocks** (the records append at the buffer's size field, the console text takes `ENTRY_OS_BLOCK` reserved once), so a line's order is chronological within its own block and meaningless across blocks — 520's log holds the fatal death twice, as text and as records, and reading one as "before" the other is a wrong first reading the peer actually made; any line number quoted from that log now says which block it is in. **(b) The one address the reader keys on was defined in three places and compared in none** — `0x800462dc`, the return address of the exit's own `bl FlushPoU_Dcache` at `0x800462d8`: the image's own bytes, `run_and_capture.sh:82`'s `EXIT_POP_LR_LITERAL=0x800462dc` (the fallback `exit_pop_lr_addr()` uses when the ELF cannot be read), and the prose. The reader **derives** it from `out/stage90/xnu_arm_entry.elf` at run time, so the literal is consulted on exactly the runs where the ELF is absent (a cleaned `out/`, a `--summarise` in another tree) — and on those a log is attributed by a number that is not in the image, which lands on 554's FAIL branch for a **correct** run. The fallback is *labelled*, but a label is not a check, and at summarise time the ELF is precisely what was not readable: **this gate is the one place the two can be compared**, so the comparison happens here and a disagreement stops the gate instead of a run. Five branches: no ELF (UNREAD, with the build-product note); no `EXIT_POP_LR_LITERAL` line (UNREAD **plus a stray-text note**, because the address also lives in that file as the literal inside 554 §2's old `grep` pattern, which in the present shape is a *comment* — a check that read a comment would be reading prose, and it prints at all so that a renamed fallback cannot make the clause go quiet); an unparsable disassembly (UNREAD); a derived value that is not a 4-byte-aligned kernel address (UNREAD — the parse asserting a line's *shape* before comparing its value, 520's rule); and disagreement (`fail`, exit 1, naming the branch of 554 the fallback would corrupt, that this gate cannot say which value is stale, and that the repair is a one-line change to a *variable* and not to the artifact — the frozen pair embeds this entry image, so nothing is rebuilt). **The parse accepts both disassemblers' shapes and that is not defensive style**: GNU `arm-none-eabi-objdump` prints `800462dc:` + the instruction right after the `bl`, while `llvm-objdump` prints a **symbol** line first (`800462dc <platform_cache_idle_exit+0x8>:`) and `0x`-prefixes the target, so a rule requiring a colon would find nothing under the second spelling and report that as an absence — the failure shape 549 records. The address is therefore the first line after the call that begins with one, colon optional, **with the function's own entry address excluded** (a parse returning the entry would compare the criterion against the top of the function). The test is **scoped to the function, not the callee**: the image reaches `FlushPoU_Dcache` from **four** `bl` sites (`0x80045d08`, `0x80046284`, `0x800462d8`, `0x800463bc`), and only the caller separates this seam from the other three — and the count is **printed from the image rather than asserted**, naming which callee it counted, because this project's notes carried **six** for it, having listed the two `FlushPoC_Dcache` references (a *different* callee, `0x80045828`, at `0x80046100`/`0x80046194`) in the same breath. **One zero is deliberately not printed as a count**: if the address was read from a line the census then failed to match, both cannot be true, so the second branch says the count *did not fit this decoder's spelling and is NOT reported* — "no site found" and "the pattern did not match" are different readings. **Tested against seven states** before it is spent (A the tree as it stands → `ok` rc 0, `0x800462dc`, `4 site(s)`; B the literal moved to `0x8004c0ff` → `REFUSING` rc 1; C the literal's line deleted → UNREAD + the stray-comment note, which found the address as text; D no ELF → UNREAD; E a misaligned derived value → UNREAD; F a faux `llvm-objdump` printing the symbol-line shape → accepted, same value; G a decoder whose `bl` line the parse matches but the census's spelling does not → `ok` on the address and the count reported as *not reported*), all host-side fixtures under the job's tmp dir; plus `bash -n`, the whole gate **exit 0**, and a `diff` against the pre-change output whose only differences are the three intended passages (item (4); the re-derived exit-site line numbers `:418/:430/:440` → `:629/:641/:651`, which moved because the peer edited the runner, **which is why 552 made the gate compute them**; and the new six-line section). **The first run of the clause ended the gate at 141 with a correct value**: the `awk` stops at the instruction it wants (`print; exit`), the `objdump` feeding it is still writing, `pipefail` turns that SIGPIPE into the pipeline's status, and `set -e` reads it as a failure of the *gate* — fixed with `DERIVED=$(… || true)` and a comment saying why the `|| true` is load-bearing, the same shape as the reader's own `keyval || true`. **Not changed:** no artifact, no `./build.sh`, and `run_and_capture.sh` read but not edited (it is the peer's lane) — the `six`-for-four count is reported to that file's owner with the measurement rather than patched across the boundary, the same discipline as 554 §5/551 §4. **Limit, recorded:** the clause proves the reader's fallback and the image agree *today*; it cannot prove the reader derives at run time, which is only observable on a host without the ELF — which is why the UNREAD branch names the human step instead of passing quietly. Safety: non-persistent `fastboot boot` remains the only device verb and this change does not run it; the worst case is a refusal to proceed |
| stage90 | [experiment-557](experiment-557-the-cache-window-is-entered-exactly-once.md) | **the cache window is entered exactly once, and that first entry is the death** — a host-side reading of 520's own log and of `cpu_idle` in the frozen tree (no device, no build). `cpu_idle` has **two** ways out to `Idle_load_context`, and only one is inside 546's window: the source's `if ((!idle_enable) \|\| (cpu_data_ptr->cpu_signal & SIGPdisabled)) Idle_load_context(); if (!SetIdlePop()) Idle_load_context();` compiles to a **bypass at `0x8000d978`** (`mov lr, pc` / `b __wrap_Idle_load_context`) that runs **no `WFI` and no cache window at all**, against the full path at `0x8000da28`→`:da3c` (enter → wfi → exit). Both call the same wrapper, so the door record counts both; **the log is what separates them, and four independent per-pass publishers agree**: `xnu_live_door_seq` has **16 records ending at `0x8000`** (≥32768 completions of the loop's tail) while `xnu_live_sip_seq` (`:8295`, `ret=1`), `xnu_live_pce_seq`/`_after_seq` (`:8300`/`:8312`) and `xnu_live_slot_pre_calls`/`rtcpre_calls` (`:8329`/`:8337`) have **one record each** — and since every site publishes on `n <= 4 \|\| (n & (n-1)) == 0`, *one record means one call*. So the window was entered **once**, on the first pass where the door was open and `SetIdlePop` returned true, and the exit of that single pass is where it died. **And the chronology is readable because `entry_live_write` appends at the buffer's size field, which is its cursor** - so the tail is: aborts 1-6 (5-7 user-mode `pc`s) → ≥32768 **bypass** passes (`:7895`-`:8115`) → aborts 7-8 → `SetIdlePop` for the first time → the enter/exit wrappers' first calls → **the fatal episode at `:8341`** (`storm=9`, `pc=0x04b79074`, `lr=0x800462dc`). **Whereas the console text is a different block** (`ENTRY_OS_BLOCK`, 128 KB, reserved once), so the file's line order across the two is not time: the panic at `:4002` and the keys at `:8341` are the *same* death carried twice, and reading `:4002` as "before" `:7895` is a reading of the file's layout — the mistake this session made first. Settles: 547's mechanism is exercised on **one pass per boot**, which is why the enable-off arm panics and reboots rather than storming and why 521/522/526 take the log with them. Does not settle the mechanism itself; `door_now`'s span (`0x04a00a8a`→`0x04a91f52`) is recorded without deriving a per-pass cost, because the counter's rate is not established. No device action, no build, frozen pair untouched, device off the bus awaiting a power press. |
| stage90 | [experiment-558](experiment-558-the-wrappers-second-publisher-is-rtcpre.md) | **the wrapper's second publisher is the rtcpre note, not the abort's rtcab** — a host-side repair (no device, no build) to `run_and_capture.sh`'s clause (3) and to two committed docs; the project's oldest class — a key that names the wrong site — found by the peer session reading the same claim against the source and verified at both call sites before the change. The exit wrapper's three bracket publishers are `g_slot_pre` (`entry_trace.c:1968`), **`g_slot_rtcpre`** (`:1973`, `entry_slot_rtc_note(&g_slot_rtcpre, entry_tpidrprw())`) and `g_slot_post` (`:1978`); `g_slot_rtcab` belongs to the **abort** path (`entry_stubs.c:1792`, inside `entry_note_sleh`), and the two structs are separate nine-key objects (`:6403-6415`), which is why a grep for `entry_slot_rtc_note` cannot tell them apart. **The log already said so**: `rtcab_calls` has 5 records (`1,2,3,4,8` = the publish schedule over 9 abort episodes) against **one** each for `rtcpre_calls`/`pre_calls` — and 557's table already listed rtcab as "not per-pass" without going back to correct the reader keyed on it. **The wrong key can only subtract** (the conjunction also requires `pre_calls >= 1`), and its failure mode is a **false localization**: state J (520's log with the one `rtcpre_calls` line removed, `rtcab` still present) makes the pre-change file, recovered with `git show f33345e:…`, print `DIED IN THE EXIT … rtcab_calls=0x00000008` while the file as changed prints `DIED BEFORE THE EXIT … (slot_rtcpre_calls) did not` — **the number was right and the question was wrong**, and the log carries `xnu_live_slot_rtcpre_calls=0x1`, the key the `_rtcpre` struct is named for. The other eight states re-run unchanged. Both documents carry the key and are corrected in place with a `> Correction (558, before the run)` note — **533 §5.2's table and caveat** (the frozen arm's own instructions to its reader) and **540's** sentence and four-row table — because the key name is an instrument fact and the *prediction* (same three sites, same order) is unchanged. **Reported not patched**: the gate's own bracket paragraph still prints `xnu_live_slot_rtcab_calls` and calls rtcpre "the same site's rtcpre spelling … the other key set this image carries tables for" — which is exactly backwards and presents the error as a supported alternative, while ~270 lines below the gate's derived narration already says `pre` and `rtcpre` — so the gate now contradicts itself; that file is the peer's lane and dirty in the worktree. Peer notes recorded, not acted on: the `rtcpre_sp` drift is the `_rin` refusal rather than `_rej` (`entry_stubs.c:6395`), and `FlushPoU_Dcache`'s four call sites plus `FlushPoC_Dcache`'s two cannot reach `exit_pop_lr_addr`, which disassembles only the exit's own address range and names the callee. No device action, no build, frozen pair untouched, device off the bus awaiting a power press. |
| stage90 | [experiment-559](experiment-559-the-brackets-middle-key-was-named-wrong-in-the-one-block-that-governs.md) | **the bracket's middle key was named wrong in the one block that governs** — a host-side change to `stages/stage90/preflight_boot_check.sh`, no device and no build; the **second half of 558's fix, in the other file**. The gate's `== which arm the entry image in out/ is, in words ==` block states the bracket the next run's log is read through, and it named **`xnu_live_slot_rtcab_calls`** as the exit wrapper's rtc spelling — three things wrong with that, the third the one that matters: (1) `rtcab` comes from `entry_stubs.c:1792`, `entry_slot_rtc_note(&g_slot_rtcab, thread)`, inside **`entry_note_sleh`**, the *abort* path, while the wrapper publishes `g_slot_pre` (`entry_trace.c:1971`; `entry_slot_null_note` at `:1968` in this arm), `g_slot_post` (`:1981`) and, beside them at **`:1973`**, `g_slot_rtcpre` — so the entry-side companion of `pre` is `rtcpre`; (2) `rtcab` is therefore a **storm** count and not an entry count — read out of 520's own capture rather than recalled, `xnu_live_slot_rtcab_calls` has **five** records (`1, 2, 3, 4, 8`, i.e. at least eight aborts, with three distinct `rtcab_pop` and two distinct `rtcab_thr` values among them) while `xnu_live_slot_rtcpre_calls` has **exactly one** and `slot_rtcab_rej = 0`; (3) **a bracket read off `rtcab` inverts the reading the arm exists to buy** — the item the next run turns on is whether the pass died *inside* `platform_cache_idle_exit`, answered by "`pre` and `rtcpre` published, `post` absent", and substituting a key that counts aborts means the criterion is satisfied by the death itself on every run that dies anywhere: it would read the window as entered eight times and put the death in the exit even when the notes say otherwise. The name is corrected and the block now names `rtcab` as *not* part of the bracket, with its publisher, its real meaning and 520's count, so a later reader cannot re-derive the wrong pairing from the same proximity. **One key name with two definitions, N = 2** — the runner and this gate — the runner's fixed first in 558 (`e362a28`), and neither reader could see the other; the gate's copy outlived it only because that block is narration and nothing asserted it, the same asymmetry 556 repaired for the reader's fallback address one key name over. **Also removed: an orphan sentence 556's own edit left behind** — 556 replaced item (4) of the 526-era narration but not its trailing clause, so the block read "… is read that way and not by this sentence. Any later ending than 520's is progress." The survivor is worse than redundant: 556's text says a death at the exit's own `bl` return address **is 547 §4's prediction for this arm**, and the orphan states the criterion the arm decides as if the ending decided it. The same sentence still stands in the 522 narrative block (`:1133`) and is **deliberately left**: 522 is a spent arm, it is the enable-**on** arm, and 547 §4 pre-registers the death only for the enable-off arm, so for 522 the sentence is not the error it is here — reported rather than swept. **Tested host-side only**: `bash -n` clean; the whole gate with `--allow-xnu-entry` **exit 0**, GREEN, with the corrected bracket printed above the arm narration; and a `diff` against the pre-change output of exactly three hunks, all intended — the bracket passage, the orphan's removal, and the **exit-site line numbers** (`:641/:653/:663` → `:644/:656/:666`), which is not a defect: the reader computes those at gate time (552) and they moved because `run_and_capture.sh` was edited in the same window, where a pinned number would have had to be edited here too. **Not changed:** no artifact (`1daaf44e…` image / `f202f246…` entry, the frozen pair, unchanged; `./build.sh` not run); `run_and_capture.sh` read, not edited from here (it is the peer's lane and 558 is already its fix) — the gate is brought to the same reading, not the reverse. **Only my own file is committed**, `git add` by explicit path after checking `git branch --show-current`, so nothing else in the shared worktree could be swept in. Safety: non-persistent `fastboot boot` remains the only device verb this phase uses and this change does not run it; the worst case is a refusal to proceed |
| stage90 | [experiment-560](experiment-560-the-same-wrong-key-in-four-more-files.md) | **the same wrong key in four more files** — a host-side correction to four committed documents (`experiment-546`, `-547`, `-554`, `-557`) and to two README cells, no device and no build; the **third iteration of one sentence**, and the reason it needed its own step is that the first two each stopped at the wrong boundary. 557's own *table* already said `rtcab` is "**not** per-pass - it tracks the nine abort episodes" and 557's §4 then used it as the per-pass key anyway; 558 fixed `run_and_capture.sh`'s clause (3) plus 533 and 540 - **the files the phase had edited**; the four corrected here were all committed before 558 and were never in that set, because the sweep was the phase's recent edits rather than a grep for the name. **The four are pre-registrations, not commentary** - 546 §5 ("`pre_calls` and `rtcab_calls` published with `post_calls` absent"), 547 §4 ("`slot_pre_calls` and `slot_rtcab_calls` published with `slot_post_calls` absent" - the section the gate *cites by name* as the enable-off arm's pre-registration), 554 §1's row quoting 533 §5.2, and 557 §4c - so the wrong key sat in the two documents that decide how the next run is read. **The name is not cosmetic in any of them**: the wrapper's second publisher is `g_slot_rtcpre` (`entry_trace.c:1973`), while `g_slot_rtcab` comes from `entry_note_sleh` (`entry_stubs.c:1792`), the *abort* path - a **storm** count, so 520's capture has **five** `rtcab` records (`1,2,3,4,8`) against **one** each for `pre`/`rtcpre` in the same pass, and a per-pass counter cannot have five records while its siblings have one. **A criterion on `rtcab` is satisfied by any pass that took an abort, which every dying pass does** - so "`rtcab` present, `post` absent" reads as *inside the exit* for a death that is anywhere, and the pre-registered prediction would have been checkable only by the wrong question. **557 §4c is the sharpest because it contradicted its own table ninety lines above** - one value with two definitions inside one file, the project's oldest class - and it is corrected in place with the contradiction named rather than silently overwritten. **The two README cells were a description of a description**: the index's summaries of 546 and 540 restated the key one level out from the files they index, which is a second definition of it - found by grepping the whole tree for the name, not by reading the rows. **Nothing about a prediction changes** (same three sites, same order, same localization), and the census `grep -rn rtcab docs/ stages/stage90/` now returns only correct usages and the correction notes: 521's symbol table (which lists both 52-byte objects separately - the fact the trap rests on), `build_entry.sh`'s deliberate alternation, and the runner/gate mentions whose whole purpose is to say it is **not** the bracket. **Reported, not patched across the boundary**: 558 §5's quotation of the gate's bracket block is a quotation of a file that has since changed, so its "still has it" is true of `e362a28` and false of the tree - 559 is that patch, and the two documents are to be read together. No device action; frozen pair `1daaf44e…`/`f202f246…` untouched, `./build.sh` not run, `stages/stage90/` not edited by this step, device off the bus awaiting a power press. |
| stage90 | [experiment-562](experiment-562-the-log-is-already-there-and-it-is-the-previous-runs.md) | **the log the gate tells the reader to read is already there, and it is the previous run's** — a host-side change to `stages/stage90/preflight_boot_check.sh`, no device and no build, closing a **silent false positive on the one reading the next run exists to produce**. Both branches of the gate's exit-status clause end by naming `$LOG` and saying "read it first" — but the runner only writes that file in **step 5**, which it reaches **only if the device returned**; the exit-3 path ("REFUSING to call this a non-return") and both `exit 2` paths leave it **untouched**, so on every non-return the file the gate has just told the reader to consult is the **last** run's. **And this pair of runs is a false positive, not a general caution**: measured before the change, `/tmp/cancro-last_kmsg.txt` (mtime `2026-09-22 02:55:16`, sha `f0285b0f…`, 596665 bytes) carries `pre_calls` **1**, `rtcpre_calls` **1**, `post_calls` **0** — which is **547 §4's prediction verbatim**, because it is the death that prediction was *written from*; a run whose capture failed, read through it, would confirm 547 §4 **with the data 547 §4 was derived from**. Three things make it worse than an ordinary stale file: the gate's own sentence invites the read with no test attached; exit 3's message asks for a **hand re-capture**, which is the step that gets skipped (if the phone does not settle into Android, or if the human reads the file instead of re-capturing, the old file stays and context now labels it as the new run's); and the log's own layout **cannot separate the two runs**, because the ram console's two blocks make line order chronological only within a block, and 520's log already holds the keys *and* the console-text copy of the same death. **The change is two things the preflight can do and not a third it cannot**: it **records what the file is at the only moment a "before" exists** (path, size, mtime, sha256, and per bracket key the record count *and* the last value — the reader's own rule is `tail -1`, so a count alone cannot say which record a reader would take), and it **names the bracket already in it** rather than describing the hazard, with the test in one command: that sha256 must have changed, and unchanged means no capture happened, which is exit 3's state. Three states are printed and none is a failure: absent ("after the run, **existence itself is the first check**", since step 5 removes then creates it), present-but-unreadable (`UNREAD`, because a sudo hand re-capture leaves it root-owned, and a reading that could not be taken must not look like one that was), and present and readable — where the trap is named **only if pre ≥ 1, rtcpre ≥ 1, post = 0**, the prediction's own shape. **It does not refuse**, deliberately: a log carrying the predicted bracket is the *normal* state of this tree today, so a refusal would stop a legitimate armed boot to prevent something that threatens only a *failed* capture, and 533 is the arm owed since the run. It is a record and a name, not a proof, and that limit is stated in the block. **Tested in five states**, the fourth the one that matters: present with an injected `xnu_live_slot_post_calls` prints the fingerprint and **does not** name the trap — without that negative, a condition that always fired would look identical to one that discriminates. **The first state found a real bug in the edit**: the "absent" sentence was written with backticks inside a double-quoted `echo`, which is **command substitution**, so the gate executed `rm` with no operand on every run and printed `rm: missing operand` into its own narration — exit status still 0, the sentence still reading as prose, so nothing but running that branch would have caught it (fixed by quoting `'rm'`). **Measured**: `bash -n` clean; the whole gate `--allow-xnu-entry` **exit 0** in the default state; `diff` against the pre-change output of **one hunk, twelve added lines, no removals**. The three non-default states used `LOGFILE=` overrides and copies under the job's `tmp/`, and **the real `/tmp/cancro-last_kmsg.txt` was read and never written** — it is the phase's only capture and is not reproducible. No build, nothing under `out/` touched, frozen pair `1daaf44e…`/`f202f246…` unchanged, `flash` not used. |
| stage90 | [experiment-563](experiment-563-the-gates-count-was-a-sections-and-its-fingerprint-is-not-the-test.md) | **the gate's count was a section's, and its fingerprint is not the test** — host-side sentences in `preflight_boot_check.sh` that described a `run_and_capture.sh` which changed underneath them in the same window, and 562's block demoted from a test to a record; frozen pair untouched (`1daaf44e624563694e…` / `f202f2465886aba6…`), gate `--allow-xnu-entry` exits **0**, no device, no build. (1) **The count was one region's and read like the file's**: `3 exit site(s)` was true of **section 4** and 564's step 5 added a second `exit 3` (the returned run whose capture failed) in **section 5**, so the sentence became false of the file — repaired by printing **both** censuses and saying which is which (`5 exit site(s) in total, code(s) 0 2 3`), *not* by widening `READ_CODES`, because every sentence under that census narrates a **wait** and section 5's code fires after a wait has already succeeded; a number widened so a sentence stays true is the same defect moving. (2) **Three stale claims, re-measured**: "the runner writes it with an '`rm`' then a redirect from adb (step 5)" — step 2b now **parks** it and step 5 reads into `$LOGFILE.new` and renames only on a non-empty read, and the absent branch now says that and points at step 2b's own sentence for the same state; "as it stands **before the run**" — a preflight cannot see whether a run follows, so the header reads "as it stands **at gate time**" with the assumption and its breakers printed; and "(that file's own **563**)" — the runner's change is **564** (the peer renumbered, 556/548 precedent), a stale *number* naming a change that moved while it was being written about, caught by grepping rather than by reading. (3) **The fingerprint is no longer the test, and the block says so rather than being deleted**: step 2b vacates the name before the boot, so afterwards `$LOG` is this run's or absent, and **a hash cannot separate a byte-identical reproduction from a fresh capture while existence can** — the post-run test is `[[ -e $LOG ]]` plus non-empty (the unchanged-sha reading is reachable only when the park failed, 511/512, which 564's own message says). The block stays because 564's exit-3 fallback points back at it as the operator's *independent* record and as the thing that identifies a **parked** file as the one this gate saw; deleting it would break that hand retry. (4) **A copy of the log was built, passed six states, and withdrawn** — content-addressed `$LOG.before` (absent; unreadable; no prior copy with `cmp` identical; re-run with **no churn**; a *different* prior copy preserved as `log.before.f0285b0f6e22` and verified, with 547 §4's trap correctly **not** named because of the injected `post`; default **exit 0**) — withdrawn on reading 564 because the **park dominates it** (a state, not a comparison, immune to the byte-identical case), it would have been a **second definition** of one value beside `$LOGFILE.prev`, and it made the gate write a file; recorded because "built it and did not ship it" is otherwise indistinguishable from "never tried". Also removed 562's dead `LOG_BYTES` (`stat` already prints the size). Measured: `bash -n` clean, gate **exit 0** in the default and absent states (the absent branch exercised explicitly with a `LOGFILE=` override, because a branch a default run never reaches is a sentence nobody has read), a `diff` of **four hunks** against the 562 output, and `/tmp/cancro-last_kmsg.txt` **read and never written** — `596665 bytes`, sha `f0285b0f…` identical before and after, still carrying 547 §4's bracket. `run_and_capture.sh` **not touched** and no new code value. **One claim survived the first pass and was found only when the block was re-swept *by predicate* rather than by provenance (560's lesson): on a non-return 562 said `$LOG` is "untouched", but step 2b moves it aside before step 3 boots, so the name is **absent** and the bytes are at **`$LOGFILE.prev`** - aimed at exactly the reader of one of 547 §4's two pre-registered states, and "untouched" is the opposite of what they find; the gate now prints the parked path in **every** state (the `echo` sits above the branch) and says to read it for the earlier death, never as this run's (`563b`, a second commit: three added lines, exit 0, `bash -n` clean - recorded as its own commit because the fourth claim was found by the method that should have been used for the first three). Device off the bus; 533 owed and needs a power press. |
| stage90 | [experiment-564](experiment-564-the-capture-is-staged-and-a-returned-runs-capture-failure-is-not-exit-1.md) | **the capture is staged, and a returned run's capture failure is not exit 1** — three host-side defects in `run_and_capture.sh`'s **step 5**, the one step that runs after a boot is already spent, plus the change 562 §5 asked for; frozen pair untouched (`1daaf44e624563694e…` / `f202f2465886aba6…`) and the gate `--allow-xnu-entry` exits **0**. (1) **Exit 3 already meant "the device returned and no log was captured", and step 5 said exit 1** — `die "could not read /proc/last_kmsg"` where `die()` is exit 1, whose header meaning is "the gate refused, or the device was not found": one state, two definitions, *in the file that defines them*, never seen because the failure path had never run. (2) **The single attempt was made in a window in which it cannot succeed**: section 4 is satisfied when the *host log* shows the enumeration, while adbd is 17 s (542's fastboot→`2717:0368`) + 20 s (522's `2717:0368`→`18d1:4ee7`) behind it — so on the normal path, 547 §4's row (i), step 5 failed into `die`; the repair is `CAPTURE_WAIT` (90 s), a **second** window, because a read cannot spend a run. (3) **Found by writing (2) down: a failed capture destroyed the previous log** — `rm` then redirect, and that file is the ONLY copy of the 520 death whose bracket 547 §4 was derived from; the capture is now staged through `$LOGFILE.new` and a non-empty result is moved into place, so the destination is removed only once a replacement exists, and the re-read loop no longer truncates the good bytes it was sent to **repair** (a repair that can destroy the thing it repairs); its closing "after three reads" was a literal the loop's own early `break` falsifies (550's shape) and now prints the iteration it stopped on. **(4) The peer's 562 §5 request, done structurally**: new **step 2b** *parks* the previous log (`$LOGFILE.prev`, then `.prev.2`, … — nothing is ever overwritten) **before the boot**, so afterwards the name is this run's or absent and the previous bracket cannot be read as this run's *even if the bytes are identical* — which a sha comparison cannot separate; if the park fails (sticky `/tmp`, 511/512) the fall-through is said out loud rather than silent. **Tested with the real block**, extracted from the file and sourced (not a copy): states A–H — A `exit 3` with the previous log byte-identical; B/C capture after retries; D keeps its 38 bytes instead of reading into the hole, and prints **2** reads; E park→failed capture→`exit 3` + absent name + unchanged parked sha; F/G/H park collisions, both logs surviving. Recorded: the wait section's exit sites moved `:644`/`:656`/`:666` → **`:700`/`:712`/`:722`** because step 2b is 46 lines above them — **no new code value** (3 is already in the gate's `READ_CODES`; section 5's new `exit 3` is outside the region that clause reads), and the gate recomputes them at gate time (552), so only the *doc* needed the numbers. Also recorded, so it is not misread as damage: `--summarise` prints **no clause block** on the real 520 log — by design, the block is gated on the `xnu_live_slot_cwe_*` pair (27 `slot_` keys in that log, none of them `cwe_*`; `entry_window_note` post-dates it) — **corrected by 598: the gate is now `xnu_live_door_seq`, which that log does carry, so a 520-shaped log gets the clause block today (112 lines → 151)** — and it runs correctly on a nine-key 533-shaped log, which also confirms `tail -1` of `xnu_live_sleh_lr` = `0x800462dc`. No device action, no build, `out/` untouched, `/tmp/cancro-last_kmsg.txt` read and never written, `flash` not used, device off the bus awaiting a power press. |
| stage90 | [experiment-565](experiment-565-535-is-designed-and-the-rebuild-cannot-wait.md) | **535 is designed, and deliberately not built - with the reason a rebuild cannot wait** — a **pre-registration** for the arm after 533, written while the device is off the bus; nothing built, nothing edited under `xnu_arm_boot/`, frozen pair untouched. **(1) The constraint is measured, not a preference**: the gate's entry-sources clause recomputes the manifest over `stages/stage90/xnu_arm_boot/` in both directions (`preflight_boot_check.sh:549`) and refuses on any change there, and for a *compiler input* change its own remedy text (`:601-620`, 539's repair) is that "`./build.sh` is owed ... it does not reproduce (408) and would spend the frozen boot image" - so the sequence *edit → rebuild entry → gate* produces a different `xnu_arm_entry.bin` and satisfying the blob clause then destroys `1daaf44e624563694e…`. **There is no way to prepare 535 in this tree that does not spend 533's arm**: the order is forced, run → read → build. Same shape as 540 §1's trap (a refusal whose prescribed remedy costs the freeze). **(2) `STAGE90_XNU_EXIT_POC_FLUSH` is not 535 and looks exactly like it**: 517's built, switched, off-in-533 `FlushPoC_Dcache` really does invalidate the L2 of pre-window copies (`entry_trace.c:1885-1888`), but its site is the **wrapper's top**, before `mov %0, sp` and before `__real_platform_cache_idle_exit()` - i.e. **before the push** - while 547 §5's seam is **inside** the real exit at its own `bl FlushPoU_Dcache` (`0x800462d8`, return `0x800462dc`), after the push and before the pop, because **the push is what leaves the line the pop reads**; flipping the flag and calling it 535 would test a different hypothesis wearing this one's name (one value, two definitions - and 517's flag-on run did not come back at all). **(3) What 535 is, conditioned on 547 §4's three rows, stated now so it cannot be fitted afterwards**: log+return -> the inner-seam PoC invalidate; no log/no return -> 535 waits and the bisection is 520's publisher shape; **returns with no pop death -> 546's mechanism is wrong and 535 must not be built as designed**; and from 547 §5/§3, regardless of row: a **PoC invalidate of the L2** that must **not** re-enable allocation outside the coherency domain. Not a build, not an edit, not a device action, not a second prediction about 533. |
| stage90 | [experiment-566](experiment-566-the-runners-whole-path-is-rehearsed.md) | **the runner's whole path is rehearsed, and both producers of exit 3 are exercised** — a host-side rehearsal of `run_and_capture.sh` against a **stubbed `sudo`** in `PATH` (the runner calls `sudo` bare, so PATH decides; the stub's trace proves every device call was intercepted), no device and no build. 564 verified step 5 by sourcing the extracted block, and `--dry-run` cannot: every branch under `DRY_RUN=1` is a `say`. **Safe by construction, not by care**: `LOGFILE` is redirected to a scratch path (the real capture is `f0285b0f…` before and after), nothing calls `flash`, and a stub that failed to intercept could not have booted the phone anyway — the script refuses to reach `fastboot boot` unless the serial is found first, and the device is off the bus. **Measured**: the success path exits **0** with a staged capture and no leftover `.new`; and exit 3's **two producers** were each rehearsed in a state where the other cannot fire — variants 1-2 (adb alive / dead but enumeration prompt) exit **3 from step 5** with the name absent and `$LOGFILE.prev` carrying the previous sha256 unchanged, and variant 3 (the enumeration appearing only *after* the bounded wait expires — the state the phone actually entered on 2026-09-22, `2717:0368` for 18 s) exits **3 from section 4**, with step 5's message count **0**. **A test that only makes the device fail tests the wrong producer.** Also confirmed by running both files together: the gate's new "if the name is gone after the run, the PREVIOUS log was parked rather than lost: step 2b" matches the runner's own behaviour in all three variants, which is the only way that pair can be checked since neither file sees the other's path. Not covered, stated so it is not read as coverage: real `dmesg` parsing, the sticky-`/tmp` failed-park branch (511/512), and anything about the device. Frozen pair untouched, gate `--allow-xnu-entry` exit 0, nothing written outside `/tmp/e2e` and `docs/`. **Correction (566c)**: §3 attributed variant 3's producer to the 2026-09-22 state, but it is a property of the rehearsal's *shortened* `RETURN_TIMEOUT` - the default is **180 s** and this phone's handover is **~17 s** (542), so at the real timeout that enumeration lands *inside* the wait, section 4 returns, and the producer is **step 5**; reaching section 4 needs silence past the whole wait. The code was right (551 added **3** for exactly that event); only the attribution was, and it is the class we keep paying for - **a conclusion about the device drawn from a harness parameter**. The runner header said the same thing wrongly (it separated the producers by whether adb came up, a condition section 4 satisfies on *both* paths) and now separates them by *when the enumeration was seen*; section 4's own exit-3 message now also names `$PREV_LOG`, which only step 5's did. Both re-rehearsed after the change: variant 3 still exits 3 from section 4, variant 1 still from step 5. |
| stage90 | [experiment-568](experiment-568-533-ran-and-came-back.md) | **533 ran and came back — 547 §4's first row, measured** — the armed arm ran on hardware 2026-09-23 00:41 (fastboot by keypress; `Sending boot.img OKAY` / `Booting OKAY`), gate **exit 0**, runner **exit 0**: the boot dies at the idle exit's `pop {fp, pc}`, **the log is present, and the device returns** — which is 547 §4's row (i) verbatim, whose reading is that *the enable is confirmed as the difference between a recovered death and a hang and 546's mechanism stands for the enable-off cells*. **So 535 is to be built as designed** (547 §5: a PoC invalidate of the L2 in the window, after the push and before the pop), and the third row — "returns with no pop death at all", the one that would have stopped it — did not happen. **It is also the phase's first returning configuration**: the SoC reached pid 1 (`/sbin/launchd`, "the OS starts the process at 0x10e0") and came back on XNU's own `MACH Reboot`, so this arm can be re-run and its log read without a power press. **Fresh-run proof, checked three ways**: `xnu_live_slot_cwe_*` has **3 records here and 0 in the parked log** (that pair is published by `entry_window_note`, which post-dates 520's image) · the run-specific keys differ · and only 36 of 3931 payload lines differ in total, which is the payload being deterministic rather than a stale read. **Arm confirmed out of the log**: `pre_calls=1`, `rtcpre_calls=1`, `post_calls` **absent** (the death is inside `platform_cache_idle_exit`), `cwe_calls=1`, `_win = _set = 0x30c57879` with C **clear**. **The death**: `panic(cpu 0 caller 0x80454584): sleh_abort: prefetch abort in kernel mode: fault_addr=0x33f1c1b4`, `lr=0x800462dc` (the exit's own `bl FlushPoU_Dcache` return address, *derived* from the entry ELF at read time), and **547 §1's `pc = r11 & ~1` confirmed on real registers** (`r11=0x33f1c1b5` → `pc=0x33f1c1b4`, with `far` the same). **Step 2b's first run with a device attached worked**: `/tmp/cancro-last_kmsg.txt.prev` holds the 2026-09-22 death byte-identical (`f0285b0f…`, 596665 B) while the new log (`734062592d6f6a4e…` / 597641 B) occupies the name. **What did not change, plainly**: the boot still does not survive the idle pass — the frontier line is the same `real XNU entry: 47` and the death is at the same instruction, so this run answered a question about the *mechanism*, not about the boot — and TWRP stays withheld (「如果os已经能进去了的话」 unmet). **The cost, named**: `out/stage90/stage90-qcdt.img` and `xnu_arm_entry.bin` are **not tracked by git** and the payload link does not reproduce (408), so 533's arm was a one-shot artifact and it has now been spent — building 535 means `./build.sh`, payable only because the run returned and was read, i.e. 565 §1's ordering (run, read, then build) has been satisfied rather than skipped. No build run, nothing flashed, nothing written to storage, frozen pair still `1daaf44e…` / `f202f246…`. |
| stage90 | [experiment-567](experiment-567-the-two-keys-the-stop-is-named-by-are-the-two-that-cannot-be-read.md) | **the two keys the stop is named by are the two that cannot be read** — a host-side change to `stages/stage90/preflight_boot_check.sh`, no device and no build, written because of **a mistake of mine**: I had reported that the death's reason is *absent* from the phase's only capture, and it is not — it is there **twice**, in the keys the fixture publishes for exactly this case (`xnu_entry_why` = `0x00000000`, `xnu_entry_why_byte` = `0x00000034` at `:8378`/`:8379`), which the gate's reader clauses had never named. **§1 the block in full**: the header prints `real XNU entry: 47` (once — the fixture writes that literal at **two** sites, so the second heading is provably missing), `xnu_entry_kv_written=0x00001fea` (8164) against `xnu_entry_kv_in_dram=0x00000000` and `_dropped=0x000092b3` (37555 refused) — and the pair's own note is the test: `_written` "is what the probes recorded, read from a register, so it is right even if the transfer to DRAM was not" while `_in_dram` "is what the transfer actually produced". **They differ here**, so this report's small-`.bss` family is the failure and its register-held fields are the readings — 272's account, and 272's rule ("a field is only as good as its second road"). **§2 the two keys are two roads to one bad read**: `why_byte` = `0x34` is `'4'`, the first character of the `47` the header printed, so they agree with each other and both disagree with 340's telegraph rule — and `47` is the **fixture's own known artifact**, recorded in its note on `g_why` long before this run ("the line comes out as … `: ` with nothing after it … **and in the storm run the same line read `47`**. Both are what a clobbered stack slot looks like, and neither is what the string says"), which is *why* the `.bss` copy exists; a report in which even the copy is wrong is the case 272 describes, not a new one. **The first bytes of this image's `why` strings are `5f 61 65 70`** (`_start…`, `a symbol…`, `exception:…`, `panic()…`) and the gate now **derives** that set from the fixture rather than pinning it: `0x34` is in none of them, so the byte classifies nothing and the honest reading is **"the reason is unreadable"** — a different statement from "there is no reason in the log", which is the one a grep over the reason *strings* produced. **§3 why it matters**: the pair is published **unconditionally** in the same function as the header, so their absence means the lines never reached DRAM and their presence with a bad value means the read did not — the phase was answering 「the stop's reason」 from a grep while the answer sat in the log in the form the fixture built for it ([[mi4-a-claim-in-a-comment-is-not-a-check]]). **§4 "not dumped" is not "empty"**: the results buffer's heading is guarded on `if (g_kv_len != 0u)` — **the same `.bss` global `_in_dram` publishes** — so a failed transfer suppresses the buffer *and* the evidence of the failure with it; the register-held `_written` is what survives, and it says the probes recorded 8164 bytes. **§5 the clause** prints one block and **adds no refusal** (the state it describes is this tree's normal state, and a refusal would stop an armed boot over a *reading*), deriving every number — each key's family from the fixture's own `entry_write_kv("<key>", <arg>)` call and whether `<arg>` names a `g_` global, the why-byte set from its `entry_epilogue("<c>` literals, the heading count from its `entry_write(...)` sites against the file's, with `NOT FOUND`/`UNREAD` states that say so out loud. **Four defects: the draft's three, all found by running the branches rather than the default** (563's rule): (a) the completeness test fired on a file carrying **no report at all** and printed the loud "the buffer was not dumped" over it — 543/552's defect exactly, a count whose scope is unstated (repaired with a `_hasreport` guard); (b) the absent-pair branch said "nothing below is classified by family" while the two lines under it classified the *fixture's* sources (a different claim); (c) `s/ /-/` without `g`, so the byte set displayed as `5f-61 65 70` — nearly right, which is how a wrong set survives; and (d) **the landing of this row itself** — an edit anchored on the *prefix* of the line above it in this file (one record = one physical line) deleted everything after the anchor, so the 549 row survived only as an orphan beginning ` — a host-side reading…` while the diffstat (`3 ++-`) still read like a clean landing; repaired by rebuilding the file as `HEAD` + exactly one inserted line and **proving** it (`diff HEAD → new` = 1 added, 0 removed, the added line byte-identical to the row the tree already carried), which is the shape the fix for this class takes: an edit on a long-line record must re-emit its anchor, and a pure insertion is only proved by a `--numstat` with 0 deletions. **Measured**: `bash -n` clean; gate `--allow-xnu-entry` **exit 0**; **seven states** exercised (five by `LOGFILE=` override: no report; `why` but no pair; pair agreeing with a byte in the set; agreeing with one outside it; both sides zero; a complete report as the **negative control**, 2 = 2 → "the results buffer is in this file"; and the real capture, the `THE PAIR DISAGREES` branch); a comment-only edit after the first run proved by `diff` of the gate's output being **identical**; the working tree's tracked diff **one file, +154 lines, no deletions**. `/tmp/cancro-last_kmsg.txt` **read and never written** (`596665 bytes`, sha `f0285b0f…` identical before and after). `run_and_capture.sh`, `xnu_arm_boot/` and `out/` untouched; no criterion changed, no new code value, frozen pair `1daaf44e…`/`f202f246…` unchanged. **Device off the bus: 533 remains the frozen, UNRUN arm and still needs the power press**, and TWRP-to-storage stays withheld. |
| stage90 | [experiment-570](experiment-570-the-key-the-build-side-added-before-the-record-carried-it.md) | **the key the build side added before the record carried it** — 535's switch `STAGE90_XNU_SEAM_POC` is written by `build_entry.sh` and selects `--wrap=FlushPoU_Dcache` in the trace link, and this gate had never heard of it: the moment the record carried the key, the **converse clause** (`_unshown`, `:387`) would have refused the run — "a switch recorded on the build side and not shown here is a switch the next run would go out with unread" — upstream of the device (safe) but **blocking**, on the one boot window 557 measured. Ten coordinated sites land the key (the two counts in the `ENTRY_CFG_KEYS` header comment *and* their history sentence, a new commented paragraph "The thirteenth is 535's, and it arrived from the build side", the array, the refusal message naming SEAM_POC as the sixth variant key, the `:397` read comment, the `for _vk` list, the derivation `case` arm, "these **six** are switches") plus a `=1`/`=0` narration block at `:495-519` that prints the peer's own semantics — the interception is the *presence* of a wrapper, it acts at exactly the call whose return address is `0x800462dc` (the `bl FlushPoU_Dcache` **inside** the real `platform_cache_idle_exit`, after its `push {fp, lr}`), every other site is handed straight through, and the readings are the `xnu_live_seam_*` keys (`calls`/`lr`, slot `b0`,`b1`,`a0`,`a1`, and `other`/`other_lr`) — and that says what the arm is **not**, since `EXIT_POC_FLUSH`'s flush is the first statement of the exit *wrapper*, one call earlier (569). Four stale prose counts were caught only on diff review (no check stops a build on a comment), and the proof is five record states rehearsed against lines `367-519` extracted from the edited file: no key → refusal naming it, `=0` → the `=0` narration, `=1` → the `=1` narration, twice → "one value with two definitions", `=2` → "not 0 or 1"; a first extraction cut mid-block exited **2** with no narration (a truncated shell extract parses far enough to run every clause before it) and a second dropped `actual_sha` and failed for its own reason. **The whole-gate `EXIT=0` was then taken on the settled build**: the first attempt returned `GATE EXIT=1` at the *entry-blob* check ("the image does not carry the arm … byte for byte" — the peer's mid-build state, with `bash ./build_entry.sh` observed running) and a second refused at **`== image freshness ==`** (`:93`, *long before* `:313`'s entry-switch section, which is why it printed nothing of this change), while the whole `ENTRY_CFG_KEYS`/`_unshown` region passed both times; once the record carried the thirteenth key (`01:05:32`, entry image `12684433…`, not 533's `f202f246…`) and `stage90.bin` was re-embedded (`01:06:05`), the run at `01:06:5x` returned **`GATE EXIT=0`** and printed the `=1` narration in that same run (payload `5867d4ed…`/`6015356` B, entry `12684433…`/`5519996` B, `up_style_idle_exit=1` at exactly 2 occurrences each). **A red gate has to be read by *which section* refused** - neither red was a clause of this change. Gate-only: no image, no `out/`, no device. |
| stage90 | [experiment-571](experiment-571-the-seam-is-hooked-and-the-arm-is-a-pure-addition.md) | **the seam is hooked, and the arm is a pure addition — plus the two derivations its own `--wrap` blinded** — 535 is built and pre-registered before the one `fastboot boot` it costs: `STAGE90_XNU_SEAM_POC=1` adds `--wrap=FlushPoU_Dcache` and a `naked` trampoline (`mov r0, sp` / `mov r1, lr` / `b entry_seam_flush`) that keeps only the call returning to `0x800462dc`, then reads the exit's frame slot **as memory holds it**, calls Apple's own `FlushPoU_Dcache`, does a **PoC clean-and-invalidate of the slot's eight bytes** (Apple's `FlushPoC_DcacheRegion`, `0x8004589c`, read out of the image as 1 × `cr7,cr14,{1}`), reads the same two words again, and **restores** them — the restore being what makes a *clean* half harmless where the window's store is newer than the cache's copy, and why the operation is MVA rather than the whole-cache sweep 546 §4 named. Built `12684433…` (5519996 B), parked with 533's and 520's. **The 589,959-byte diff against 533's image is decomposed rather than accepted**: 484 B of new code at `0x8047c9c4` (+0x2A0 of `.text` once re-padding is counted), Apple's own symbols byte-identical, `.data`/`.sysctl_set`/`.init_array` at the same addresses, and the 23,747 differing words **below** the insertion are 22,648 `movw` halves + 351 `movt` halves + 748 literal-pool address words, every one an absolute address into the moved region. **The arm's own redirection blinded two derivations of the address the run is read against** — the runner's `exit_pop_lr_addr` and the gate's `_pop_lr_from_elf` both matched `/<FlushPoU_Dcache>/`, which `bl … <__wrap_FlushPoU_Dcache>` does not contain — so the reader fell back to its pinned literal and the gate printed `UNREAD … the comparison below did NOT happen` **while still exiting 0**: the runner's pattern is fixed here (`/<[^<>]*FlushPoU_Dcache>/`, the routine and not the spelling), the gate's is owed to the peer session, and **the boot is held until it derives**, because 556's binding is the property this arm's death is read against. Clause (5) is new in the reader (self-selecting on `xnu_live_seam_calls`), names the arm's six readings with PASS/FAIL/UNREAD separated, and the SCTLR pair now says which *image* a log is from rather than only which *cell* — rehearsed on 568's real log in six variants, including the two where it must not print a verdict. TWRP stays withheld (「如果os已经能进去了的话」 unmet). |
| stage90 | [experiment-578](experiment-578-the-operations-two-halves-and-the-routine-whose-name-lies.md) | **the operation's two halves, and the routine whose name lies** — a pre-registration for the arm after 574, written while the device is off the bus so the design round is off the critical path. **The opcode census of the linked image** (sites, not names): DCCMVAC ×5, **DCCIMVAC ×2**, DCCMVAU ×1, whole-cache invalidate ×1, and **`cr7,cr6,{1}` (invalidate-by-MVA, no clean) × 0 — absent from this image**. **The second and the last row are the finding**: Apple's `invalidate_mmu_dcache_region` (`0x800456fc`) is a **clean-and-invalidate** — same `cr7, cr14, {1}` loop body and same 64-byte `and r2,r0,#63`/`bic`/`lsr r1,r1,#6` walk as `FlushPoC_DcacheRegion` — so an arm that reached for it **by name** to make the `pop` miss without writing back gets the exact clean half it is avoiding, while every surface says "invalidate" (the name-versus-body class, measured rather than suspected). Why one DCCIMVAC is the wrong shape, in the arm's own arithmetic: its clean writes the cache's copy of **all sixteen** words of the line to memory (the restore covers two; the other fourteen are the idle thread's live frame on 546 §1), and its invalidate is the half that is needed **because the `pop`'s load is cacheable** — Apple re-enables `SCTLR.C` at `0x8004631c-24`, 24 bytes before the `pop` at `0x8004633c`, so the line must be *gone* for the pop to read memory. **The shape it would take**: split the halves and put each where it is safe — **clean before the push** (the exit wrapper's top, where the line's cache copy is the pre-window authority and after it nothing on the line is dirty) and **invalidate-only at the seam** (574's own hook, clean removed, so the pop misses and reads the push's own values while the neighbours lose nothing). Step 2 needs an instruction this image lacks, so it opens `mcr p15,0,rX,cr7,cr6,{1}` — the **first** definition of an operation rather than a second, but the build would have to assert the opcode's absence in every routine it did **not** call, or an arm calling `invalidate_mmu_dcache_region` by name would look identical from every surface while quietly cleaning. Falsifiers pre-registered: 574 returning with an **unchanged** pair means the line was not dirty, step 1 is unnecessary, and the candidate moves to the L2-domain questions (572 §5(b)); 574 not returning means the interception itself is implicated first. Host-side only — one disassembly census of a parked image and reads of parked logs; the arm is unchanged, the gate green, and the boot still waits on the power press. |
| stage90 | [experiment-577](experiment-577-the-arm-cannot-write-the-slot-and-now-the-build-says-so.md) | **the arm cannot write the slot, and now the build says so instead of the argument** — 574 §2 named the one part of its safety claim it could *not* assert ("a store is not distinguishable from the frame's own pushes and the live-channel writes by opcode alone"), and a store to the slot writes the very two words the exit's `pop` is about to read — the object 546's mechanism is about. Closed: the slot's registers are identified **from the body itself** (a load base used at both offset 0 and offset 4, closed under `mov rX, rY`; `sp` excluded, which is why the naive "no store via `sp`" form finds nothing on either arm — the stores name a *copy* of the address), and no store may use one. **Measured on both parked bodies, same registers, opposite verdicts**: 574 = `r0, r5` with **0** stores (the claim, now a reading); 535 = the same registers with **2** (`8047ca74: str r7, [r5]` and `8047ca78: str r6, [r5, #4]`, the restore) — **the falsification, and it cost nothing but the awk because the image that fails it was already parked**. The clause refuses three ways, including *no slot register identified at all*, because a body whose reads cannot be located is a body whose stores cannot be checked and the store check would then pass by finding nothing. The arm's safety now rests on four properties read out of the image: `--wrap` with 4 redirected / 0 direct, **no coprocessor write (`mcr`) and no `FlushPoC_DcacheRegion` call** in the body (580 corrected the wording: the body holds one `mrc`, the compiler's SCTLR read, in **both** arms — the census names its opcodes and the discriminating property is the callee), the same four publications, and no store to the slot. The peer's gate copy (in the tree as this was written) is the same finding from the other side and **not a duplicate**: that one is what a person reads before touching the device, this one is what **stops the build**, and the peer's own paragraph has already been corrected from the false "no store of any kind" to "no store **to the slot**". Host-side only: the artifact is byte-identical (`151425c4…`) and the gate is green (`EXIT=0`, 0 UNREAD). |
| stage90 | [experiment-576](experiment-576-the-arms-sp-is-the-slot-and-the-test-was-one-push-out.md) | **the arm's `sp` is the slot, and the reader's test for it was one push out** — 574's clause (5) compared `xnu_live_seam_sp` against the abort's `sleh_sp` with `==`, and the two are **8 bytes apart** in the same frame: `platform_cache_idle_exit`'s `push {fp, lr}` at `0x800462d4` puts the slot at the seam's `sp`, while the dump's `sp` is that same sp *after* the `pop` freed those 8 bytes (546 §1: push and pop are 8/8 with no `sp` change between them), and the entry wrapper's own `mov %0, sp` runs **after** its prologue (`8047c964: str r4,[sp,#-8]!`) so `xnu_live_slot_pre_sp` is slot+8 too — 520's log carries both under different names with the **same** value (`slot_pre_sp=0x8054fed0` beside the abort's `sp: 0x8054fed0`), so the slot is `0x8054fec8` and **the equality would have printed FAIL for a correct arm**. Fixed host-side (`run_and_capture.sh` only, not a build input, so the frozen 574 arm survives and the gate stays green `EXIT=0` 0 UNREAD): the test is now the relation **against both publishers** — `seam_sp+8 == sleh_sp` and `seam_sp+8 == slot_pre_sp` — two relations rather than one because a check consuming both would let one silent key hide the other, and their disagreement is itself a reading; the FAIL texts print hex on both sides (the first version printed the expected address in **decimal** beside hex ones, 515's defect). Rehearsed in three states on 568's real log (OK → both PASS; **OLDEQ → both FAIL, i.e. the old criterion failing a good arm**; BADWRAP → the two publishers disagreeing is caught). **And the log already carried the slot's *contents* under a name nobody had joined up**: `slot_pre_m8`/`_m4` are the words at the two addresses the push is about to write, so 520's log holds **both sides of the mechanism on one pass** — `{0x80553520, 0x8047c974}` written, `{0x33f1c1b5, 0x33f1c1b5}` = `rtcpre_pop` read. One premise of 546 §1 is *not* what this pair shows (the second word is inside the exit wrapper's own extent, not in `cpu_idle`) and is recorded as a question the next log answers rather than resolved here. No device action; **the boot still waits only on the user's power press.** |
| stage90 | [experiment-574](experiment-574-the-measurement-arm-and-the-interception-with-nothing-behind-it.md) | **the measurement arm is built — the same seam with nothing behind it, and the gate refuses the new switch exactly as 569 §5 predicted** — the arm 572 §6 chose: `STAGE90_XNU_SEAM_MEASURE=1` keeps 535's whole interception (`--wrap=FlushPoU_Dcache`, 4 sites redirected / 0 direct, the `naked` trampoline, the `lr == 0x800462dc` filter, the same two reads and the same publications) and removes the operation entirely — body `0x1CC` vs 535's `0x1D8`, **14 loads, 0 `mcr` and 1 `mrc` (the compiler's SCTLR read, present in both arms — see 580), 0 calls to `FlushPoC_DcacheRegion`**, so with `SCTLR.C` clear the two loads are non-cacheable reads of the caller's own frame and the arm cannot move a line or change memory: it is the first arm this phase can boot without risking the device, which is deliberate after 535. **Two switches, one seam**: the build and an `#error` refuse both at once, both keys are recorded always (`0` included), and `SEAM_ON=$((POC|MEASURE))` is the single variable the link and every clause read. **The gate refused the new key in the intended direction** (`carries key(s) this gate does not print: STAGE90_XNU_SEAM_MEASURE … a switch the next run would go out with unread`), recorded verbatim; the peer's `575` (`b3121a9`) then registered it and the gate is **green, `EXIT=0`, 0 UNREAD**, with the 556 binding clause deriving `0x800462dc` and now printing both spellings (`4 through the wrapper, 1 the wrapper's own body`) — the count the peer measured and I had got wrong by inference (mine said 2, the disassembly says 1). The 535→574 image diff is decomposed: same size, **`.text` identical in both**, Apple's symbols byte-identical, **nothing below the seam objects moved at all**, and the 1,216 changed symbols are the relaid-out entry objects (`-0xC`/`-0x10`/`+0x10` = re-padding). The reader's clause (5) now dispatches **three ways** on `xnu_live_seam_op` — 535's rule, 574's rule (an unequal pair is Apple's own L1 flush writing the line back; an equal pair is the arm working as designed), and **no interpretation at all** when the key is absent — and the rehearsal caught that third branch, because the first version printed the UNREAD and then fell through to 535's rule anyway (M1 `AS DESIGNED` / M2 `THE L1 FLUSH WRITES IT BACK` / M3 `NOT interpreted` / M4 535's `STALE LINE, WRITTEN OUT`; 568's unmodified log still 0 FAIL/UNREAD). Nothing run: **the device is still hung from 535 and needs the user's power press**, and the one boot waits for that. TWRP stays withheld. |
| stage90 | [experiment-572](experiment-572-535-ran-and-did-not-come-back.md) | **535 ran and did not come back — the arm moved a returning death into a non-return** — the one boot, on 2026-09-23 ~01:16: gate `exit 0`, runner **`exit 2`** with *both* halves of the criterion (551) — `serial 4a2fe00b not listed` **and** `host log: 1530 -> 1530` enumerations — and the host's own USB log shows the shape: `18d1:d00d` at `2194582.646`, disconnect `2.75 s` later when `fastboot boot` took over, and **nothing since** — so the SoC did not return in any form *and was not reset by its own watchdog* (a reset would have re-enumerated within seconds; 520's runs carried `hw_watchdog_counter_running=0x00000001`). **No log exists and none is recoverable** (ram console in DRAM, cold boot clears it): the live name is **absent**, `.prev.2` holds 568's log intact, `.prev` the 2026-09-22 one. The reading is the *state*, not a log: 533's image **returned** from the same cell (568), the only functional difference is the arm (the payload's re-embed is the new entry image, sources unchanged), so **the arm turned "dies at the pop and returns on XNU's `MACH Reboot`" into "does not return"**. **This is not 547 §4's row (ii)** — same shape, different cell, and that row is about 533's image: one value with two definitions in the place where it costs a boot. **The pattern is now three of**: every arm that put a **clean to the Point of Coherency** into that window has cost the return (517's whole-cache flag ×2, 521's, 535's region) against 533's no-operation cell which returns with a log. Candidates named and **unmeasured**: (a) `FlushPoC_DcacheRegion(slot, 8)` walks **64-byte lines** (`and r2,r0,#63` / `lsr r1,r1,#6`), so the clean writes back all sixteen words of the line while the arm restores only the two it read; (b) the PoC clean reaching a level whose configuration 544/545 leave open; (c) the interception itself (cheap to exclude, so it is tested *first*). **The next arm is the interception with no operation** — same filter, same reads, same publications, `bl __real_FlushPoU_Dcache` and return, no clean/invalidate/store — harmless by construction and decisive either way *without* a log. Safety: one non-persistent `fastboot boot`, never flash, nothing to storage, no brick possible; **the device is hung and needs a power press** (the user's hand). TWRP stays withheld. |
| stage90 | [experiment-569](experiment-569-the-exit-flush-fires-one-call-before-the-seam.md) | **the exit-side flush fires one call before the seam, and the gate now says so** — a narration-only change to `preflight_boot_check.sh`'s `V_EXIT_POC_FLUSH` block, in **both** branches: `=1` now names the site (the flag is the **first statement** of `entry_trace.c`'s `__wrap_platform_cache_idle_exit`, so it runs **before** that wrapper's own `mov %0, sp` and before the real exit's `push {fp, lr}`), and `=0` records that an absent flag is not an absent seam. 565 section 2 measured the difference that matters: 535's seam is the `bl FlushPoU_Dcache` *inside* the real exit (return `0x800462dc`), **one call later** than 517's flag, and a flush before the push cannot cover a line the push has not written - so a run of this flag is not 535's run, and calling it one is one value with two definitions. Proof: `bash -n` clean, `git diff --numstat` 13/2 (a clean replacement, no suffix loss), a full gate run **`GATE EXIT=0`** with the changed block printed by that same run, and both branches rehearsed independently from lines 467-483's own bytes. The backstop is the gate's own converse clause (`:377-380`), which `fail`s on any `STAGE90_*` key the gate does not print - so if 535 lands as a new switch the next run stops **before the device is touched**. No image, no `out/`, no device, no arm: the 568 pair stays spent and parked |
| stage90 | [experiment-573](experiment-573-the-clause-that-was-quiet-on-the-image-it-binds.md) | **the clause that was quiet on the image it exists to bind** — 556's binding clause derived the exit's `bl FlushPoU_Dcache` return address with `/<FlushPoU_Dcache>/`, and `--wrap=FlushPoU_Dcache` means that string is not in the call's line: on 535's image the derivation came out **empty**, the clause printed `UNREAD`, and the gate **exited 0** — a green run in which the one clause whose job is to bind the reader's criterion to this image had compared nothing (measured: the old pattern matched **1** line, and that line is the *wrapper's own body*, not the four callers, which spell `<__wrap_FlushPoU_Dcache>`). The pattern is now about the **routine** and accepts any wrapper prefix, one `FLUSH_N` is split into `FLUSH_SITES` / `FLUSH_WRAP` (so `--wrap` follows from a measurement), the narration says which of the two mechanisms this image uses, and the branch that *was* a wrapped arm's resting state is now a hard stop when the decoder did run — no decoder on `PATH` and a decoder that printed nothing stay `UNREAD`, but a full disassembly with no such call in the function is `fail`. Gate-side only, no rebuild, no device; all four cases of the empty-derivation branch rehearsed from the file's own bytes, and the narration's unwrapped side then observed on 533's **parked** image (`FLUSH_WRAP=0`, `FLUSH_SITES=4`, same `0x800462dc`), so both sides of that narration are measured rather than asserted. |
| stage90 | [experiment-575](experiment-575-the-second-seam-switch-and-the-record-about-no-image.md) | **the second seam switch, and the record about no image** — 572's arm is `STAGE90_XNU_SEAM_MEASURE`, the *same* interception 535 built (`--wrap=FlushPoU_Dcache`, one wrapper, the same return-address filter `lr == 0x800462dc`) with 535's operation taken out of the body, and this gate had never heard of it: **measured** by extracting the pre-change region out of `HEAD` and running it against the record `out/` actually carries, which returns `REFUSING: … carries key(s) this gate does not print: STAGE90_XNU_SEAM_MEASURE` — 570 §1's prediction one arm later, a switch arriving as a complaint about the gate's own bookkeeping, upstream of the device (safe) but blocking, on the one boot window 557 measured. **(1) What is new is not the key, it is that the two switches are one seam's two arms**: `SEAM_ON = SEAM_POC or SEAM_MEASURE` (`build_entry.sh:429`) is what puts `--wrap` in the link, both-at-1 is refused by the build (`:421`) and `#error`ed by `entry_trace.c:437-438`, so a record naming both describes **an image that cannot exist** — and the gate is the only place that pair is visible at gate time, so it now refuses it (`[[ ! ( $V_SEAM_POC -eq 1 && $V_SEAM_MEASURE -eq 1 ) ]]`), naming the test rather than guessing which of the two values is wrong and saying plainly that nothing is rebuilt by the refusal and nothing should be (the frozen pair embeds this entry image). Every sentence that read `SEAM_POC=0` as "no interception" is now conditional, because on this very record (`SEAM_POC=0`, `SEAM_MEASURE=1`) it is false. **(2) The arm's own key cannot name the arm**: `entry_trace.c:2254` publishes `xnu_live_seam_op` as `(uint32_t)(STAGE90_XNU_SEAM_POC)`, so 572's run and a no-seam run both show **0** — told apart by whether the `xnu_live_seam_*` keys were written at all (`xnu_live_seam_calls` present), absent is not zero (569, 526) — and the new narration says so in the arm's own words. **(3) Sixteen sites**, seven of them prose counts (the "seven" that *are* the variant, the list's history sentence "nine, then twelve, and is **fourteen**", "drops all seven", "these seven are switches", two read comments, the required-key message) — the class 570 §2 caught four of drifting, with no check to stop a build on a comment — plus the array, the `for _vk` list, the derivation `case` arm, a new commented paragraph for the pair, the **pair clause**, and a three-way narration whose `SEAM_MEASURE=1` branch carries the one reading rule a run of this arm is worth: with nothing behind the interception an UNEQUAL `b`/`a` pair is *Apple's own flush writing the line back* and an equal pair is the arm working as designed — **the opposite** of what the same keys mean under 535. **(4) Proof**: `bash -n` clean, `git diff --numstat` **62 / 18** (the deletions are the old `else` sentence and the rewritten comment lines), and six record states rehearsed against lines `328-578` extracted from the edited file (the extract re-`cmp`ed against a fresh `sed` first) — A the real record → exit 0 with the `SEAM_MEASURE=1` narration, B `SEAM_POC=1` → 535's, C both 0 → the `else`, D both 1 → the pair refusal, E no key → "has no STAGE90_XNU_SEAM_MEASURE line", F `=2` → "not 0 or 1"; and the *before* taken the same way, the `HEAD` extract refusing case A and case D with the `_unshown` message. One defect caught only because the rehearsal ran: case D first exited 1 with **`ENTRY_ELF: unbound variable` and no refusal** — the message named a variable assigned at `:1736`, below the clause at `:464`; repaired by putting the literal path in it, the same seed problem 570 §4 and 573 §4 each recorded, with the same tell (**an exit 1 with no refusal printed is not this clause refusing**). **(5) The live gate, read by section**: `GATE EXIT=1` at `== the entry image's own sources ==` (`:581`, the peer's mid-edit `entry_trace.c`), a clause **downstream** of everything here — while `== which arm the entry image in out/ is, in words ==` cleared and printed both of this change's keys and the new narration in that same run. Gate-only: no image, no `out/`, no rebuild, no device. |
| stage90 | [experiment-579](experiment-579-the-seams-own-body-read-out-of-the-image.md) | **the seam's own body, read out of the image and not out of the record** — which arm the boot is about to run was narrated (`:519-546`) and the impossible pair refused (`:464-465`) both out of `xnu_arm_entry-config.txt`, a text file the *build* writes, and the gate's only artifact-facing clause bound the record to the entry image **by hash** — and a hash says nothing about what is **in** the artifact, so a record can be internally consistent and describe an image that is not the one in `out/`. The property that actually licenses this boot (535's operation absent from the measurement arm, present in 535's) is now read out of the ELF: a new last section disassembles `entry_seam_flush` and compares the **callee** — `bl FlushPoC_DcacheRegion`, present iff `SEAM_POC=1` — against the record's two keys, keyed to the **OR** the same way `build_entry.sh:429` is (`SEAM_POC=0` no longer means "no interception", 575). **Why the stop is a call and not a census, measured**: the two arms share every mnemonic in either body — `out/`'s is **115 instruction lines / 8 stores / 24 loads** (`0x1CC = 115 x 4`) and 535's **118 / 10 / 24** (`0x1D8`), differing only by the operation's 2 `str`, 2 `dsb` and 3 instructions — and 574 §2's published "**14 loads, 0 coprocessor instructions**" is the answer to the test printed beside it, `$3 ~ /^mcr/`, while the body carries one **`mrc`** (`entry_sctlr`'s `15, 0, sl, cr1, cr0, {0}`, a *read* of SCTLR and not a cache operation): `^mcr` finds 0 and `^mrc` finds 1 on **both** parked bodies, and the 14 is the `ldr` subset of 24. `_n_call` counts `bl` only, because a call the seam does not divert leaves by a **tail branch** (`8047ca2c: b 80045874 <FlushPoU_Dcache>` on 535's). **Four findings** (record names a seam the image has no symbol for; record names none and the image carries one — the "NO interception" paragraph printed over an image that has one; `SEAM_MEASURE=1` over an operation-carrying body, which would **invert the meaning of the very pair the arm exists to produce**; `SEAM_POC=1` over a body with no operation, which would spend the window on the already-spent arm) and **three UNREAD** (no ELF; no decoder on PATH; a decoder that printed nothing = 549), kept apart from a fourth that is a *parse that did not fit* (named in a full disassembly, no body extracted) — and the trust boundary is stated rather than papered over: **a decoder that answers wrongly is covered by none of them**, so the record-to-bin hash binding stays. Every refusal names the test rather than presuming which side is stale, and carries 575's sentence: nothing is rebuilt by it, and nothing should be (the frozen pair embeds this entry image). **Proof**: `bash -n` clean, `git diff --numstat` **125 / 3**, region re-`cmp`ed against a fresh `sed` after every edit, **ten harness states M1-M10** (M1/M2 the two ok, M3/M4/M10 the three seam findings, M5-M8 the UNREAD four, M9 the both-zero reading) each observed where the others cannot fire, and **four whole-gate states** on a frozen instant of `out/` (record `01:54:18`, manifest `01:54:19`, all 20 sources verified, bin `151425c4…`, ELF `3bc72605…`): A the real record → **EXIT=0**, sources clause green, this section last; B/C → the new refusals; D → **575's pair clause upstream** of it, as designed. Two defects were caught by running: a first M8 written as a well-formed fake body was read as **2 instructions and said ok** (the hole is now stated, and the reworked M8 names the symbol only as a call target), and the first whole-gate rehearsal was invalidated by the **peer's build finishing** — repaired by rehearsing against one **snapshot instant** instead of a pinned commit plus a live `out/`. Complementary to **577** (the build-side slot store) on purpose: the build stops such an image existing, this stops a record that does not describe the image in `out/`. Live gate on the live tree: **EXIT=0, 0 UNREAD**. Gate-only: no image, no `out/`, no rebuild, no device; 535's arm unchanged and unrun; TWRP-to-storage withheld. |
| stage90 | [experiment-581](experiment-581-the-run-reads-the-log-it-just-captured.md) | **the run reads the log it just captured** — the runner opened by summarising the **previous** run's log (`step "payload output"`) and, after spending a boot to capture a new one, ended at `done. Full log: <name>`: **the reading of the log the run had just paid for was a command the operator had to know to type.** Every failure path in the wait section prints its reading in full, and the one path that *spends a run and produces a reading* printed least — on a phase whose boot is a single chance (the payload's log lives in the top of DRAM and a second boot overwrites it). `summarise_log "$LOGFILE"` is now called inside the capture arm, **after** the "fewer than 8 payload lines" warning so a suspect file is read *under* that caveat rather than instead of it, and it is a read of a file already on disk: no device call, no exit code, no capture logic changed — one definition of the reading, printed where it is produced. **Why it was safe to land one step before the expensive run**: `bash -n` clean; the function's last statement is an `if` with no `else`, i.e. status 0 on both branches (the property that was itself a defect once — a trailing `[[ ... ]] && say ...` returned 1 on an abort-free log and, under `set -e`, the caller exited 1 **before booting anything**, which is how four exp-267 "runs" re-summarised a stale log and reported nothing); **the success path still exits 0** and now prints the reading; **all six reachable outcomes of section 4 / step 5 are unchanged, exit code for exit code, with `new-block=0` in every one** (`1 / 1 / 2 / 2 / 3-from-section-4 / 3-from-step-5`, as 580 measured) — which is the check that the block did not leak into a path that must not have it; `--dry-run` exits 0 without the block; and the **gate is green after the change** (`EXIT=0`, 0 UNREAD, section 4 still `3 exit site(s), code(s) 2 3`). **And the path exercised on a 574-shaped log**: a stub returning 520/568's real capture with the seam keys, `sp` corrected to the slot (`0x8054fec8`) and `xnu_live_seam_op=0` added, printed the whole reading at **exit 0**, taking the `op=0` branch (`seam_lr=0x800462dc` PASS, `seam_sp=0x8054fec8 is the exit's frame slot: it is sleh_sp-8` PASS, `THE L1 FLUSH WRITES IT BACK`). That log is **synthetic** and parked under a name that says so — a file that already carries the expected shape is the artifact-as-prediction defect (562, 563), and the branch it took is what that input produces, **not** a prediction of the run: with `op=0` an unequal pair is Apple's own L1 flush writing the line back and an equal pair is the arm working as designed, and **574's run is the measurement that decides which**. No device action, no arm changed (frozen `151425c4…` / `3bc72605…`), phone still off the bus; the boot waits only on the user's power press, with a session-only watcher armed to detect the return, gate, fire one boot and report the exit code against the contract. TWRP withheld. |
| stage90 | [experiment-582](experiment-582-the-return-site-is-in-the-wrapper-and-b1-has-a-derived-value.md) | **the return site is in the wrapper, and `b1` now has a derived value to be measured against** — 546 §1's premise ("the `lr` this frame pushed names `cpu_idle`, and the `pop` is the return into it") is true of the **unwrapped** link and false of every image built since 517's `--wrap=platform_cache_idle_exit`: the caller is `__wrap_platform_cache_idle_exit`, so the exit's `pop` returns **into the wrapper**. Measured from the frozen 574 arm: the wrapper's `bl platform_cache_idle_exit` is at `8047c98c` and returns to **`0x8047c990`** (535's parked arm: the same two instructions at the same addresses) — an address inside the wrapper's own extent `0x8047c964..0x8047c9c4` that **appears nowhere in this repository's prose**, because until now nothing compared against it. **What the reader did instead was a range, and a range is not a value**: `b1` was tested only for `^0x80…` and `< 0x80600000`, and 520's own log shows what that admits — `pre_m8 = 0x80553520`, a stale stack word, passes it, as would any address the idle stack happens to hold, which is exactly the wrong-value case the arm exists to detect. A new `exit_caller_lr_addr()` derives the one address from the ELF (symbol table → body → the `bl` found **by name tail**, excluding `<__wrap_…>`, the rule `exit_pop_lr_addr` had to learn in 535 → the answer asserted **inside the wrapper's own extent**, so a wrong `bl` fails rather than returning a number). Four reachable states, each measured: `0x8047c990` → **PASS**; `0x80553520` → **READING** (kernel text but not this image's return site — the reading the old test got wrong); `0x800462dc` → **READING**; `0x33f1c1b5` → **READING** (not kernel text — 520 died on this value); no decoder → **UNREAD**. **And the UNREAD branch was unreachable in the first version of the edit** — with no decoder `caller_lr` is empty and a kernel-text-looking `b1` fell into the comparison arm and printed a comparison whose address was empty; caught by rehearsing that branch, not by reasoning, and the "could not derive" test now runs first. **The prediction this puts on the coming run**: `b0`/`b1` are uncached reads of memory taken after the push, so `b1` is predicted to be **`0x8047c990`** in *both* of clause (5)'s branches (the L1 flush changes memory *after* `b` was read) — giving the run's reading two independent answers instead of one; `b0` has no derived expectation, so this is one predicted word, stated as one. Regression: 520's real log still `0 FAIL`/`0 UNREAD`/exit 0, the full success path on the 574-shaped synthetic input still exits 0 with the line firing inside the run, gate green. Reader-only: no device, no build input, no arm changed. TWRP withheld. |
| stage90 | [experiment-583](experiment-583-the-same-predicted-word-in-a-second-key.md) | **the same predicted word in a second key, and the exit has exactly one caller** — 582 derived the address a correct idle frame holds at `slot+4` (`0x8047c990`, the return site of the wrapper's `bl platform_cache_idle_exit`) and attached it to the seam's `b1`. Two consequences follow from the frame's own arithmetic and a call census, both read off the image. **(1) `b1` and `xnu_live_slot_pre_m4` name the same word.** The wrapper's prologue leaves `sp = X-8`; the capture reads `spv-16/-12/-8/-4` = `X-24/X-20/X-16/X-12` as `m16/m12/m8/m4`, and the real exit is entered with `sp = X-8`, so its `push {fp, lr}` writes `X-16 = slot = pre_m8` and `X-12 = slot+4 = pre_m4`. The capture happens **before** the push, so `pre_m4` is the *previous* pass's pushed `lr` on a frame that has not moved — **582's prediction therefore lands in a second key the older instrument already publishes**, independently of the seam, and the old "did the frame move" reading becomes **three distinguishable states** instead of one: both equal the derived site (in memory, frame stable → **PASS**); `b1` right and `pre_m4` not (**the frame MOVED** — a fact about the idle loop, now distinguishable *because* `b1` is right); `b1` wrong (the push's store did not reach the word the `pop` reads). All four states rehearsed and printed as their own sentence (`583-pair-four-states.txt`) — **but 586 corrected this section: the frozen 574 arm is `STAGE90_XNU_SLOT_NULL=1`, so it publishes `xnu_live_slot_pre_calls` and none of the four words, and this second key is not reachable on the boot that is about to run. The arithmetic and the states are right for a `SLOT_NULL=0` arm; the prediction the coming run can answer is `b1` alone.** **(2) The exit has exactly one caller over the whole image** — `8047c98c: bl 800462d4` in `__wrap_platform_cache_idle_exit` — so `b1 = 0x8047c990` holds for *every* pass through the seam and not only one call site's worth; with a second caller the prediction would have to be a set keyed by caller, and the seam's `lr == 0x800462dc` filter would not have separated them, because it names the *flush* call's return address and not the exit's caller. **And 576 §3's open question is decided by a header already in the tree**: 520's `pre_m4 = 0x8047c974` is neither of the two explanations 576 offered — it is the *old instrument's own* `entry_slot_note` saved `lr` (`entry_slot_capture.h`'s opening paragraph; the defect 521 was written to fix), **so 520's log cannot be used to falsify 582's prediction in either direction**. Regression with the final reader: 520's real log `0 FAIL`/`0 UNREAD`, the full success path exits 0, all six section-4/step-5 states unchanged (`1 1 2 2 3 3`), gate green. Reader-only: no device, no build input, no arm changed. TWRP withheld. |
| stage90 | [experiment-585](experiment-585-one-value-two-runs-and-the-pair-the-pop-actually-read.md) | **one value, two runs, and the pair the pop actually read** — 576 §3 quoted 520's log as saying the pop read `{0x33f1c1b5, 0x33f1c1b5}`; **`0x33f1c1b5` occurs zero times in 520's capture.** It is **533's** run (its dump: `r4: 0x33f1c1b5` / `r11: 0x33f1c1b5` / `pc: 0x33f1c1b4` / `rtcpre_pop=0x33f1c1b5`, and its abort-time slot capture `ab_m8 = ab_m4 = 0x33f1c1b5` — the "both words equal" fact 576 §3 described, true of 533 and false of 520). 520's own dump reads `fp = 0x04b79075`, `pc = 0x04b79074` — **two different values, consecutive** — and the source already had the right pair (`entry_stubs.c:6491`). **What the corrected pair shows is stronger than what was claimed**: in **both** runs, four hours and two builds apart, the pop loaded `xnu_live_slot_rtcpre_pop` into `fp` and that value **minus one** into `pc` — and the code reads `rtcpre_pop` from `cpu_data + CPU_RTCPOP`, the idle loop's own deadline, **not a stack word**. So in both runs the pop's two words are `cpu_data`-derived values one apart, while `lr` still holds `0x800462dc` — **the very address the `push` saved into the frame's second word**. The death is therefore measurable **without a cache theory**: the push's word is a value the machine still holds in `lr`, memory received it, and the pop into `pc` got something else, twice, deterministically. (Where the other value came *from* is left open — two candidates fit the numbers — but what the pop read is settled, and it was never stale stack content: no stack address appears.) Consequences: 582's prediction is unaffected and better supported (`b1` reads memory, and memory demonstrably received the push's `lr`); the reader's 535 `STALE LINE` branch is a hypothesis wearing a comparison's clothes — the comparison is now *supported* by both real dumps, but its sentence called a `cpu_data` field "the slot's `lr` word", which is the name-versus-body mistake, and the sentence now says which of the two it measures; and the **synthetic 574-shaped rehearsal input carried 533's value** — 520's image predates the seam, so no capture could have produced those keys at all — so every copy is relabelled and renamed `…-SYNTHETIC…` with a banner saying the seam keys are invented. Also corrected in place: 576 §3 now carries the correction rather than the false sentence. Host-side only: two greps and two seds over parked logs, prose edits and a file name; arm byte-identical and unrun, device off the bus. TWRP withheld. |
| stage90 | [experiment-586](experiment-586-the-four-states-this-arm-cannot-be-in.md) | **the four states this arm cannot be in, and the silence that read as agreement** — 583 built a four-row table on `xnu_live_slot_pre_m4` and promised the coming run a second independent key. **The frozen 574 arm is `STAGE90_XNU_SLOT_NULL=1`** (as are 533's and 535's), and an arm in that state calls `entry_slot_null_note`, which publishes the pass count and **neither `sp` nor the four words** — five ways: the config line, the function's own comment, the wrapper's `bl <entry_slot_null_note>` at `0x8047c978` in the artifact, 533's real capture (`xnu_live_slot_pre_calls` present, `slot_pre_sp`/`_pre_m16`/`_pre_m12`/`_pre_m8`/`_pre_m4` all absent), and 520's for contrast (all seven present — that arm is `SLOT_NULL=0`). So the reader's `slot_pre_sp` comparison and its whole `m8`/`m4` block printed **nothing** on the coming boot — and nothing, in a block where every neighbouring absence prints a line, reads exactly like agreement; measured on a 533-shaped input carrying the seam keys, the section went `PASS seam_sp=…` straight to `PASS b1=…` with nothing between, one line under an UNREAD *for an absent key*. **Repaired in two places**: both comparisons gain an absence branch naming `xnu_live_slot_pre_calls` (present with the words absent = the site ran and published no words; both absent = the site did not run), and the pair gains a fourth state — `b1` wrong and `pre_m4` right = **the word was right before the push and the push's store did not leave it there**, 546 §1's premise failing in the window, which 583's three-row table could not name. **And the block had the mirror of 582's own defect**: it tested the derived comparison first, so with no readable entry ELF the empty `caller_lr` fell through to a fallback that printed `equal to b0/b1 when the frame did not move between two passes` one line above the `b1` line's own UNREAD — the empty-derivation test now runs first, as it does in the line below. Six states rehearsed (`586-pair-six-states.txt`), the frozen arm's shape built from **533's real capture unmodified** as the body with only the eleven `xnu_live_seam_*` lines invented. **The gate's `SLOT_NULL=1` branch now says the run will print those two lines, and derives that by grepping the runner for the key rather than asserting it** (563); both branches exercised, the negative one on a symlink farm, because the gate sets `STAGE_DIR=$PWD`. **583 §1 corrected in place**: what survives is the arithmetic and the four states, which are correct for any `SLOT_NULL=0` arm — **582's `b1 = 0x8047c990` is now the only prediction the coming run can answer**, and the exit's one-caller census is unaffected. Regression: the six section-4/step-5 states `1 1 2 2 3 3` unchanged, 520's log `0 FAIL`/`0 UNREAD`, gate `EXIT=0`, 23 sections, 0 UNREAD. Reader-and-gate only; the frozen arm is byte-identical and unrun. TWRP withheld. |
| stage90 | [experiment-587](experiment-587-the-guard-the-clause-needed.md) | **the guard the clause needed, and the branch that told the operator to fix a reader that was not broken** — 586 gave the gate a clause that reads `run_and_capture.sh` and tells the operator whether the coming run's summary will print the two `UNREAD` lines naming `xnu_live_slot_pre_calls`. This is the guard for that clause's own read, and it was needed because `grep -q` returns **2** for a file it cannot open: with no guard, an absent, directory-shaped or mode-000 runner took the `else` and the gate asserted `does not name xnu_live_slot_pre_calls anywhere` **about a file it had never read** - 584 section 2's R7/R8 shape (a property asserted of an artifact by a comparison that never ran), arriving one commit later inside the clause 586 added for the opposite reason, and costly in the same direction: that branch tells the operator to go and fix a reader that may not be broken. Five states rehearsed in a symlink farm: S1 absent / S2 a directory of that name / S3 mode 000 each took the false branch (`grep`'s own `No such file or directory`, `Is a directory`, `Permission denied` printed immediately above it) and now each refuses through `fail` with a `REFUSING:` line, the `-f` branch being needed because `_readable`'s `-e`/`-r` pair is **true of a directory**; S4 (readable, key renamed) and S5 (readable, key present) are unchanged. Repair is the ninth guarded read in the idiom of the other eight, `14 0` pure insertion, `bash -n` clean; the gate's whole stdout on the frozen 574 arm is **byte-identical** to 586's run and stays `EXIT=0` / 23 sections / 0 `UNREAD` / 0 stderr; no new exit-code producer (the refusal goes through `fail`). Frozen artifacts untouched (`151425c4…` / `bcacf065…` / `aeb7862a…`); no device, no build, no arm changed. TWRP withheld. |
| stage90 | [experiment-588](experiment-588-one-empty-string-three-states.md) | **one empty string, three states, and the sentence that tells the operator what is on disk** — `$PREV_LOG` is empty for three reasons (no file existed, the park failed, `--dry-run`), and **both** of its consumers tested `[[ -n $PREV_LOG ]]` and so read all three as "the park failed". On the state the real `/tmp/cancro-last_kmsg.txt` is in as this is written — **absent**, the previous logs parked at `.prev`/`.prev.2` — the exit-3 message told the operator the file *"is untouched, so it is still the run before this one's"* and to compare a sha256, **about a file that does not exist**: a wrong sentence about what is on disk in the one place where being wrong costs the run, which is why 562 and 564 exist. **Found by running the success path end to end, not by reading it** — and the first version of that rehearsal scored a successful path as **exit 3**, because its own stub wrote `*"exec-out")` and `*"boot")` with **no trailing `*`**, so neither could match a string that merely *contains* those words; both fell through to a bare `exit 0` with no output and the capture read zero bytes nineteen times. A `case` pattern is anchored at both ends unless it says otherwise — the "the test did not run and the failure looked like the artifact's" shape, one layer out in the harness. Fixed, the rehearsal returns exit 0 — **and the wrong run had printed the false sentence**, which is the defect, reachable only because the stub was broken. **The repair names the state once**: step 2b sets `PARK` = `parked`/`nopark`/`nofile`/`dry`, both consumers `case` on it, and `nofile` gains its own sentence (*"does not exist and did not before this boot either … a file present afterwards is new"*) while `nopark` gains the half it lacked (*"it is also the name the hand retry above writes to, so a successful retry replaces it"*). The default branch is not a state of the log: the first version made it a dry-run sentence, an **unreachable branch claiming a state** (both blocks are inside `DRY_RUN -eq 0`), which is 582 §2's and 586 §2's defect twice over; it now says `$PARK` was never set, i.e. a defect in this file. **Seven states rehearsed on the paths that reach them** — success path (0), `nofile`/`parked`/`nopark`/`unknown` in step 5 (3), `nofile`/`parked` in section 4 (3), the `unknown` one on a symlink farm since it is unreachable through the file as written, and `nopark` via a mode-555 directory (`mv` refused — the effect 511/512 describe, said as the effect rather than the cause). Regression: the six section-4/step-5 states **1 1 2 2 3 3** unchanged, 520's log `0 FAIL`/`0 UNREAD`, gate `EXIT=0` 23 sections 0 UNREAD. Reader-only; the frozen arm is byte-identical and unrun. TWRP withheld. |
| stage90 | [experiment-589](experiment-589-the-sentence-the-gate-printed-and-its-own-comment-did-not.md) | **the sentence the gate printed and its own comment did not, in the state where the log is in DRAM** — the gate's closing prose before the state test asserted, unconditionally, that a missing log after the run means the *previous* log **was parked rather than lost**. `mv` in step 2b sits behind `elif [[ -e $LOGFILE ]]`, so with nothing at the name at gate time there is nothing to move and `.prev` is not written by that run — **which is the state `/tmp/cancro-last_kmsg.txt` is in** (absent; `.prev` = the 2026-09-22 log, `.prev.2` = 533's; the last several runs did not return). So on the coming run it asserted a `mv` that will not run, and framed the very observation the next paragraph reads as *the capture failed* as **parked, not lost** — 588's shape one file over, in the one place where a wrong sentence about what is on disk costs the run, and a prediction in a block whose first line disclaims predicting the run. **Second gap:** the comment block has described since 564 the state where the sha *is* the test (a failed park, 511/512 sticky `/tmp`), and the **output never said it** — and that state is not gate-visible, so it cannot be made conditional on a reading. Repaired as printed text only: `26 4`, and every added or removed line is a comment or an `echo` (checked mechanically) — no branch, no read, no new exit code, the park still the runner's. The `.prev` paragraph is rephrased from asserting a cause to **conditioning** one ("whenever there is one to park"). **Not byte-identical and said so:** unlike 587's inert guard, these lines are in the branch that is taken, so the output differs by design — and the whole diff against 588's run is **one hunk**, lines 439–450, inside `== the log those instructions name ==`; `EXIT=0`, 23 sections (the same 23), 0 real `UNREAD`, 0 stderr. Three states rehearsed — absent (the live one), present-and-readable, present-and-mode-000 — stubs under the job's `tmp/` and removed, and **nothing created at the log's name**, which is 562's own trap. The five claims the gate makes about `run_and_capture.sh` were re-read against it at `e1551a4` rather than trusted, and all five hold, so no claim moved with 588 — what moved is what the gate says about itself. Frozen arm untouched (`151425c4…` / `bcacf065…` / `aeb7862a…`); no device, no build. TWRP withheld. |
| stage90 | [experiment-590](experiment-590-our-own-wrapper-writes-the-word-the-pop-reads.md) | **our own wrapper writes the word the `pop` reads, and the `pc` in the dump is a fetch address** — two corrections and one new lead, all from the frozen entry image's own disassembly (`151425c4…`) plus the archived 520 capture; no device, no build, no arm. **(1) The three idle wrappers share one `sp`.** `cpu_idle` (`0x8000d934`) calls `__wrap_platform_cache_idle_enter` (`0x8000da28`), `__wrap_cpu_idle_wfi` (`0x8000da38`) and `__wrap_platform_cache_idle_exit` (`0x8000da3c`), and the four instructions between them are `movw`/`movt`/`ldr` — none writes `sp`, and `bl` does not either — so all three are entered with one `sp` (`S`). **(2) `r4` is `cpu_idle`'s `rtcPop` at both idle wrappers, at the instruction level**: `0x8000d98c` is `add r6, r5, #224` / `ldm r6, {r4, r7}` with `r5 = cpu_data`, and `224 = 0xe0` is `STAGE90_CPU_RTCPOP` (`entry_stubs.c:6220`), the offset the rtc note reads the deadline from; nothing between there and the `bl` to the `wfi` wrapper writes `r4`, and the calls are AAPCS (`r4` callee-saved — which `0x8000da14` itself relies on, comparing the freshly read pair against `r4`/`r7`). **(3) Both wrappers then store that doubleword at `S-12`** — `strd r4, [sp, #-12]!` at `0x8047c8d4` and `0x8047c884` — **and `S-12` is exactly the word the exit's `push {fp, lr}` writes `lr` into and the `pop` loads as `pc`**, twice per pass, before Apple's push overwrites it. There are four `strd r4,[sp,#-12]!` sites in the image; these two are the ones whose `sp` is `S`. **So a *writer* at the slot is named by address.** **(4) The correction to 585**: `pc` in a prefetch-abort dump is the **fetch address**, and `far` is the same — so 520's `pc = 0x04b79074` is `[S-12] & ~1`, not `[S-12]`; the word is `0x04b79074` **or** `0x04b79075`, and the second is `rtcPop`. 519 had already named the relationship ("the Thumb-address relationship read off its own `r4`/`far` pair"); 585 turned one value in two spellings into two values in two words. What is a real memory reading is `fp = 0x04b79075 = rtcPop`. **(5) And the new lead, marked as a hypothesis**: `strd` to `S-12` from an 8-byte-aligned `S` is an **unaligned doubleword store**, which ARMv7 makes **UNPREDICTABLE** (`LDRD`/`STRD` are not in the list of instructions that may be unaligned). Storing at the offset as written puts `r4` at `S-12` (546's cache story needed to explain the `pc` word); **aligning down to `S-16` puts `r4` at the word the `pop` loads as `fp`, and 520 measured `fp = rtcPop`** — one of the two measured words explained with no cache theory at all. That is decidable on the bench with a two-instruction arm that issues the same store and reads both words back. **What it does to the next arm**: 535's candidate was cache maintenance at the seam (and its run did not come back) while this is the *writer* — repairs are our own code, each smaller (keep the deadline out of the exit's `{fp, lr}` slot, or **invalidate** — not clean — that one line after the push). Nothing is built: the phase runs one arm at a time and 574's `b1` is what chooses the axis; this exists so that if `b1` comes back as anything but `0x8047c990`, the next arm is already designed against addresses. Also records a claim I made to the user that was stronger than the evidence (`"that wrong value is not Apple's, it is our own wrapper's prologue spilling r4"`) — the mechanism is supported, the *identification* is a hypothesis 585 left open. **Addendum 590a, checked before it could reach an arm: the unaligned/align-down reading is largely dead.** `strd r4, [sp, #-12]!` stores **`r4` and `r5`** - STRD takes consecutive registers - and in `cpu_idle` **`r5` is the `cpu_data` pointer** (`0x8000d944: ldr r5, [r1, #1484]`), not a second deadline word. So under align-down the `pop` would load `pc = cpu_data` = `0x8051a000` in 520's run, a valid kernel address, and would have returned there instead of aborting at `0x04b79074`. **It aborted** - so align-down cannot explain the `pc` word, and "one of the two measured words explained with no cache theory" is withdrawn for it. **The aligned-as-written reading is the one the measurement fits**: it puts `r4 = rtcPop` at `S-12`, exactly the word the `pop` loads as `pc`, whose measured value is `rtcPop-1`/`rtcPop` - and 546's cache story is then what the push's `lr` store has to lose to. **And the abort's register file carries the instrument's own values**: 520's log publishes `rtcpre_datap=0x8051a000`, `rtcpre_thr=0xc0573df0`, `rtcpre_pop=0x04b79075`, and the dump reads `r5`, `r9`, `r11` as exactly those three, with `r6 = cpu_data+0xe0` = `cpu_idle`'s own `r6` - so `r11 = rtcPop` is consistent with the `pop` having committed its `fp` load (`LDM sp!, {r11, pc}` writes `r11` before `pc`), which is 585's `[S-16] = rtcPop` reading and it stands. **What does not fit is `r4`**: `cpu_idle` sets it to `rtcPop` and nothing writes it again, yet the abort shows `r4 = 0x8054fed0` = `sp` - recorded as **open**, not resolved, because 519's chain is a statement about `r4` at the *store site* (which the disassembly confirms) and not about `r4` at the *abort* (which it does not). Portions of this entry are summary restatements of the underlying document. TWRP withheld. |
| stage90 | [experiment-591](experiment-591-the-second-premise-with-no-guard.md) | **the second premise with no guard, and the `ok` a doctored image earns** — the seam-body section's comment had said since 579 that **four** record-versus-body disagreements stop the gate, but only **two** `fail` calls guard the arm pair — both reading `FlushPoC_DcacheRegion` (`_POC_N`) — and the **measurement arm's second premise had no guard at all**: that the body calls `FlushPoU_Dcache`, i.e. `_POU_N >= 1`. That premise is the whole meaning of the arm's two readings, because what the arm is worth is what sits **between** them — Apple's own L1 flush, the seam's own `bl FlushPoU_Dcache` at `0x8047ca58`, the call the `ok` line names and the arm's one rule is about (unequal `b`/`a` = that flush writing the line back, equal = the arm working as designed). A body whose interception **passes the call through by a tail branch** leaves `_POC_N == 0`, so the branch's only test passes and the gate printed `ok` over a run in which **nothing ran between the two reads**, where the pair carries nothing and a *return* would be read as the arm's own reading — 584 §2's shape and 587's, one branch over from the guard the other arm has carried since 579. Proved by a **four-cell rehearsal in a mirrored `repo/` under the job's `tmp/`** with the gate pair and `run_and_capture.sh` **pinned** (`38c9c39a…` / `1ad26147…` / `a45f6cf1…`, the runner pinned because the gate prints its exit-site **line numbers** and the peer was moving them live) against a **one-line doctoring** of the real disassembly (`1,253,983` → `1,253,982` lines, the only deleted line being `8047ca58: bl 80045874 <FlushPoU_Dcache>` — pure deletion, nothing written under `out/`): **A vs C byte-identical** (`cmp` clean over 36,285 B, the change inert on the frozen arm since `_POU_N` is 1 there), **B the defect reproduced** — HEAD's gate stays `EXIT=0` on the doctored image and prints `ok` with "Its **0** bl into FlushPoU_Dcache is the wrapper's own call into the real routine", asserting in one breath the call its own number refutes (four lines differ from A, all four a single digit changing value, which is why B is the same *length* as A and still differs — byte 34,429) — and **D the refusal**: `EXIT=1`, one `REFUSING:` line (1,190 B), naming `FlushPoU_Dcache=0`, stating what it costs, and stating what the clause cannot tell ("that the record and the body disagree, not which is stale"). D's stdout is 8 lines shorter than C's because the seam section is the gate's **last** and `fail` prints and exits (`:50`), the convention all 65 `fail` calls follow; **all 23 section headers are still present**. The repair is `18 1` — fourteen lines of comment plus one guarded `if`, **no new read** (`_POU_N` was already computed at `:2124` and already printed) — and the guard was chosen over rewording the `ok` echo into silence because the clause's job is to **stop the boot** when the premise it narrates is false. Unchanged: `EXIT=0`, 23 sections, 0 real `UNREAD`, 0 stderr bytes on the frozen 574 arm, the seam section's output byte-identical. Frozen artifacts untouched (`151425c4…`, `bcacf065…`, `aeb7862a…`). No device, no build, no arm, no write under `out/`. TWRP withheld |
| stage90 | [experiment-592](experiment-592-a-pass-is-not-a-clearance.md) | **a PASS on `b1` is not a clearance of the `pop`, and the pairing it is read by** — the peer session's item 4 on 590 held in substance and was inverted in its pairing, and checking it is what this is. **What holds:** the seam's four words are read with the caches off while the `pop` runs with them on. `entry_seam_flush` (`0x8047c9c4`) reads `b0`/`b1` before its `bl FlushPoU_Dcache` and `a0`/`a1` after it, and contains exactly one coprocessor instruction — `mrc p15, 0, sl, c1, c0, 0` at `0x8047ca64`, the `SCTLR` read, taken after the `a` pair — and no `mcr`, so that one reading is `C`'s value for all four words; Apple turns `C` back on in `platform_cache_idle_exit` itself at `0x8004631c`-`0x80046328` (`mrc`/`orr #4`/`mcr` on `SCTLR`, then `isb`), sixteen bytes before the `pop` at `0x8004633c`. So `b1 == 0x8047c990` says *DRAM* held the frame's word while the caches were off; it does not say the `pop`'s own lookup, with `C` on, will see it, and a stale line answering it is exactly 546 section 3's mechanism. **What was inverted:** the word the `pop` takes as `pc` is `b1`, not `b0` — `entry_seam_flush`'s base is `mov r0, sp` in `__wrap_FlushPoU_Dcache` (`0x8047cb90`), and a `bl` does not move `sp`, so it is the exit's `sp` as its `push {fp, lr}` left it: `b0 = [seam_sp]` is the pushed `fp`, `b1 = [seam_sp+4]` the pushed `lr`, and `pop {fp, pc}`'s ascending register list loads `pc` from `[seam_sp+4]`. The inverted form follows from counting the two *words* instead of the two *registers*, and it sends a failure to the wrong word; the reader's header has said "`b1` is therefore a return address" since 582 and its `STALE LINE, WRITTEN OUT` arm compares `a1` against `xnu_live_slot_rtcpre_pop`, so nothing moved on account of it except a paragraph stating it once. **The repair reads rather than asserts:** a new clause takes the `C` bit out of `xnu_live_seam_sctlr` (a key the frozen image's own ELF carries, so the clause is a guard and not the expected state), with three rehearsed states — `C=0` a `PASS` that says in its own words that the `pop` is outside what it can see, `C=1` a `FINDING` (546 section 1's premise for this cell failing) plus the `UNREAD` naming what it costs, absent an `UNREAD` — and both non-`C=0` states set `verdict_ok=0`, so a block whose comparisons were all made against cache contents cannot be summarised as passing. The `b1` PASS line now states the pairing and the limit, and the header carries the derivation and the asymmetry together. **No prediction changed:** `b1` is still `0x8047c990`, the arm is still the frozen 574 (`914f45ac…` / `151425c4…` / `3bc72605…`, all three re-measured after the edit), and the gate is green — `EXIT=0`, the same 23 sections, its output differing from 588's capture in exactly two hunks: the exit census's line numbers (`1214/1246/1256 -> 1278/1310/1320`), printed at gate time because the census reads the runner rather than a copy of it (563), and the `.prev` paragraph 589 landed after that capture was taken. Regression: 582's four `b1` states re-run and each still on its own arm, 520's real log still exit 0 / 0 FAIL / 0 UNREAD (the seam block gated out by design on that image), and the four synthetic `sctlr` states rehearsed. No device, no build, no arm. TWRP withheld |
| stage90 | [experiment-593](experiment-593-the-door-opens-once-and-the-first-arrival-is-the-last.md) | **the window opens once because 514's repair opens the door once, and the first arrival is the last** — the one host-side question aimed at the *goal* rather than at a reader, answered from two archived captures and the instruments' own partition; no device, no build, no arm. **The reading:** 520's and 533's logs agree exactly — `door_seq` 16 records to `0x8000` (⇒ ≥ 32768 entries into `__wrap_Idle_load_context`), against `sip_seq=1` with `sip_ret=1`, `pce_seq=1`, `wfi_seq=1`. Since the schedule is powers of two, one record *is* the count one, so the window ran **once**. 513's own partition does the rest: `door 1 = machine_idle − SetIdlePop entries`, `door 2 = SetIdlePop FALSE`, `door 3 = SetIdlePop TRUE` ⇒ **door 3 = 1** (the window) and door 1 ≥ 32767 — and `door_en=1`, so by 513's own words door 1's disjunction "can only be true because of the `SIGPdisabled` bit" (the disassembly agrees: `0x8000d95c: beq` is `!idle_enable`, `0x8000d968: ble` is `cpu_signal`, both landing on one `mov lr, pc; b __wrap_Idle_load_context`). 514 supplies the link: `SIGPdisabled` is never cleared because a uniprocessor port receives no IPI, and 514's repair is the kernel's own `cpu_signal_handler_internal(FALSE)`, called **once** — guarded on `park_printed == 0u` (`entry_trace.c:1221`, set at `:1288`) and, checked rather than assumed, **not wrapped in any `#if`**. So: no IPI ⇒ 32767+ passes leave by door 1 ⇒ 514 clears the bit once ⇒ **the very next pass** reaches `SetIdlePop` for the first time in the boot, gets TRUE, enters the window for the first time — and dies at `platform_cache_idle_exit`'s `pop {fp, pc}`. **"Once" is a property of the repair, not of the window, and the first arrival is the last.** **A flagged discrepancy resolved:** 520's `sip_site=0x8000d810` / `door_lr=0x8000d820` / `pce_caller=0x8000d8cc` were recorded as resolving "inside `L_64ormorealigned` (a bzero loop), contradicting 538's site filter". They do not: resolved against **533's own ELF**, 533's own log's three site keys are exact (`0x8000d980` / `0x8000d970` / `0x8000da2c`), and 520's three are each **exactly `0x160` lower** — one key off by an arbitrary amount would be a stale stack word, three off by the same amount with independently-known sites is a **version offset**, and 520's image is not archived. The rule: a recorded site is only resolvable against the image that produced it, and the check that it is the right image is that the independently-known sites move together. **What it buys:** the death is reachable *only* through 514's one-shot repair, so there is a lever that does not have to win the `pop` — a switch that skips the repair, so `cpu_idle` leaves by door 1 every pass, the three idle wrappers are never called and `pop {fp, pc}` **is never executed**. Pre-registered as one arm with its falsifier (the death moves earlier, or the machine stops returning, ⇒ the death is not in the sleep path) and its honest limit (the pre-514 spin is known *alive* from 512's 33-million-entry count, but that count predates the full path into userland, so this is a candidate, not a fix). Not built. Frozen 574 untouched and unrun. TWRP withheld |
| stage90 | [experiment-594](experiment-594-the-arm-that-never-sleeps.md) | **the arm that never sleeps, and a guard whose comment counted two of three statements** — 593 section 4's lever built: the switch, its plumbing into the arm record, the clause that refuses a record and a body that disagree, and the **reader**, which had to exist before the run (564's rule). **The switch:** `STAGE90_XNU_IDLE_NO_SLEEP`, 0 or 1, default **0** — a build that forgets it is the arm every run has used, not the one under test. At 1 the `#if` around 514's repair in `entry_trace.c` compiles out `rb = entry_counter(); cpu_signal_handler_internal(0); entry_note_repair(caller, rb, entry_counter());`, so `SIGPdisabled` stays set and `pop {fp, pc}` is never executed. **The plumbing, and the half the first draft missed:** the key went to `ENTRY_ARM_KEYS` (twelfth → **thirteenth**), to `STUB_DEFINES` and to the `-D` list — and **not** to `xnu_arm_entry-config.txt`'s writer, which is the only thing the gate reads; the peer session caught it, and without it the switch would have reached no record and the gate could not have gone loud (591's defect one layer out, and this one not even *recordable*). 514's own clause also pinned three numbers to `1`; they are now `$(( 1 - IDLE_NO_SLEEP ))` or the switch's own set, because a criterion pinned as a literal is 554/555 one layer down — and pinning them would have refused the arm this step exists to build. **The defect this step produced:** the guard's note said "the declaration has to move with the **two** statements that use it", and the set was **three** — moving them left `rb = entry_counter();` behind, inside the guard, where it was deleted. **The arm's own build could not see it**: on `IDLE_NO_SLEEP=1` the whole block is `#if 0`, so the image under test was green and it was the **switch-off restore build** — whose only purpose was to reproduce the baseline — that refused with `'rb' may be used uninitialized [-Werror=maybe-uninitialized]`. A defect on the `#if 0` side of an arm switch is invisible to the arm that runs, which is why the restore build is not bookkeeping. **The reader, and why the gate had to be widened first:** the clause block was `if grep -a -q 'xnu_live_slot_cwe_'`, and on this arm *no* publisher it scores exists (the enter wrapper's note, the exit's three brackets, the seam, `sip`/`pce`/`wfi`), so it would have printed nothing for the one log that matters — a reader silent for a reason and one that broke look identical from outside (564). The arm is now decided before the gate from keys the arm does not silence (`door_seq` present ⇒ `cpu_idle` ran; `repair_seq`/`sip_seq`/`pce_seq`/`wfi_seq` absent), and the clause reads a **ladder** rather than an absence: (1) `xnu_live_poll_seq > 2` — `entry_note_poll` publishes only *after* `__real_poll` returns and the park is the third poll, so a published 3 **is** the park having returned, and the frozen arm's two archived runs both stop at 2 (5 ms and 40 ms, neither a park); (1b) the largest `poll_timeout_ms` ≥ this tree's `ENTRY_PARK_MIN_MS`, **read out of `entry_trace.c`** rather than written down again; (2) the park's console group present, whose count reads 0; (3) `poll_over`; (4) more than 3 `sleh_user` records against 520's 3. **And the obvious marker is the wrong one:** the group reads like the arm's own statement (`called %d time(s)`, 1 on the baseline, 0 here), but those four `mini4:` lines are printed *after* `__real_poll` returns and on the baseline the park's poll never returns — `grep -c 'mini4: the repair --'` is **0 on 520 and 0 on 533**, only three `mini4:` lines of any kind are in each — so a marker the baseline cannot produce cannot separate the two arms, and it stays as the strongest witness instead. **The reader's own defect, found by running it:** the new clause printed `the park is the third poll and  runs only *after*  returns` — the two symbol names were in backticks, so bash *ran* them. Now structural: `grep -E '^[[:space:]]*(say\|step\|die)[[:space:]]+"'` over its own source, with the escaped form stripped first (without that the guard refuses the script it guards — its own first draft) and shown to fire by planting a line and watching it exit 1, then restoring byte-identically. **What it does not do:** the arm is **not bootable** — the payload embeds `xnu_arm_entry.bin` and a `build.sh` run cannot be undone (408), so that is a payload rebuild which spends the freeze, and the user's decision. Rung 1 passing reads *progress past the death point*, not "the OS is up". Frozen 574 untouched and unrun; the switch-off restore reproduces `151425c4…` and the arm-on image is `696a0f39…` (unchanged by the `rb` fix, which is the proof the fix is inert on the arm). TWRP withheld |
| stage90 | [experiment-595](experiment-595-the-next-boot-sends-the-sleepless-arm.md) | **the next boot sends the sleepless arm, and the frozen arm is parked rather than spent** — 594 built the arm that might reach the OS and left it unbootable on the grounds that a payload rebuild spends the freeze; this takes that decision the way the goal asks, and makes it cheap. **Why this arm gets the press:** the frozen 574 arm *provably cannot* reach the OS (547 §4 pre-registers a death at the `pop`, which is exactly the first arrival 594's arm never makes), while 594's reader does **not** depend on the frozen run — its baseline is 520's and 533's archived logs — so the order is free and the power press is the only scarce thing. **The rebuild is the gate's own sanctioned branch, not a spent freeze:** `preflight_boot_check.sh:877` is a two-branch test on one hash — same hash ⇒ `./build.sh` must NOT be run (it does not reproduce, 408); **hash differs ⇒ "the arm really is a new one and `./build.sh` is owed, with `STAGE90_EXTRA_CFLAGS='-DSTAGE90_XNU_ENTRY=1'`"**. It differs (`696a0f39…` vs `151425c4…`). The park is **bytes** and not a rebuild for the measured reason that `build_entry.sh` *is* reproducible (tonight's switch-off build reproduced the frozen `151425c4…` byte-for-byte) while the payload link is **not**. **Parked first, re-hashed, under `/tmp/r594/frozen-payload/`:** `stage90-qcdt.img` `914f45ac…`, `stage90.bin` `0f108392…`, `stage90.img` `8274b1c4…`, the ELF/fixture/`SHA256SUMS.txt`/build-config, and the frozen entry pair `151425c4…` / `3bc72605…` with its record — **revert is two `cp`s and no build**, which is the point. **A pre-flight isolated the one clause at issue, before spending the build:** with the arm-on entry image in and the payload still frozen, the gate refused at EXIT=1 with **exactly one line** (`does not carry the arm … byte for byte`) while build configuration, `sha256sum -c`, the freshness scan and the header/blob-size readings all passed — so no *other* clause objects to this arm. **The build's self-check that it changed one thing:** `stage90-build-config.txt` is **byte-identical** to the frozen payload's `-dM` dump (the same switch set), the new `stage90.bin` (`ce02edfe…`) contains the 594 arm **once** and the frozen arm **zero** times, `kernel_size` 6015356 unchanged. **The gate on the rebuilt pair: EXIT=0 / 518 lines / 39,142 B / 0 stderr**, blob at image offset **496148** exactly as the gate's own comment predicts for a `STAGE90_XNU_ENTRY=1` payload, with the `IDLE_NO_SLEEP=1` narration printing — including the sentence naming `xnu_live_repair_*` as absent *by construction* (594 §5). New payload `stage90-qcdt.img` = `60063c47…`, which is what `fastboot boot` will send. **Pre-registered:** witness `xnu_live_poll_seq` reaching **3** (both parked logs stop at 2, 5 ms and 40 ms) corroborated by a timeout ≥ `ENTRY_PARK_MIN_MS` read out of `entry_trace.c`; second witness the park's console group with `called 0 time(s)`, which the baseline cannot print at all; signature = the window family **absent** with `door_seq`/`idle_seq` still climbing to `0x8000`; **falsifier `poll_seq` still 2** — the live spin, alive and never progressing, which the runner's exit code cannot distinguish from success. **And the return path is bounded:** the payload arms the MSM8974 hardware watchdog once before the handoff (25 s + 3 s gap) and **nothing pets it** — one `stage90_hw_watchdog_arm` call site, no pet function, and no MSM watchdog *driver* in the XNU side — so a spinning XNU still resets at ~28 s and the DRAM log is readable; expected runner exit 0 or 3, never 2. **What it does not buy:** a PASS reads *progress past the death point*, not "the OS is up" — the CPU spins hot, and 「能进入操作系统」 is decided by the readings after the park. Frozen arm deferred, not answered. Nothing flashed, nothing written to storage, TWRP still withheld |
| stage90 | [experiment-596](experiment-596-the-goals-own-criterion-had-no-reader.md) | **the goal's own criterion had no reader, and `0x8000` was never where the machine died** — two findings that are one shape from opposite sides. **(1) The reader that was missing.** This project has measured 「起码要能进入操作系统，把基础驱动跑起来」 since 504, and `grep -n 'xnu_live_open' run_and_capture.sh` returned **nothing** — a run that survived the park would still have been judged against the death point, with the goal's own evidence unread in the same file. Measured, and identical in shape in 520 and 533 (only thread pointers differ), in the fixture's own record order: `open("/dev/rmd0")` → `error=0 fd=0`; `read(fd,page,4)` → `error=0 ret_lo=4` with `word_before=0x00102000` → `word_after=0xfeedface` (**the driver's `uiomove64` moved this file's own `MH_MAGIC` into the page `mmap` returned, and the pair is self-checking because the page held that address a moment earlier**); the control `open("/dev/nosuch")` → `error=2` (ENOENT); `getpid` → 1; `exit` pid 2 rval 3; `wait` `done_seq=2 status=0x0300`; 2 `ast` records. **The block is placed at the end of `summarise_log`, outside the arm branch**, because the userland phase precedes the `pop` and both arms publish these keys — a block inside a branch would be silent for exactly the logs most likely to matter (564's defect and 594's missing `else`, applied before rather than after it bit). **A third helper was needed and its absence would have been 558's defect a third time:** `keyval` takes the last occurrence and `maxhex` the largest; the fixture's driver reading is a *pair* of opens and 504 states the criterion in its own words ("a pair of opens whose second is also answered positively would mean the first told us nothing"), so `ordered()` returns a key's occurrences in publication order and the block reads `open_error` **as a pair** — `0x00000000` then `0x00000002`. One value would have been the *control's*, reported as the driver's. The insertion order that reader rests on is **measured, not assumed**: 533 runs `poll_seq=1`, `door_seq=0x8000`, `poll_seq=2`, `open` 8151, `read` 8158, control `open` 8170, `wait` 8186, `exit` 8226, `wait_done` 8301, the park's `repair` 8307, `sip` 8319, `pce` 8324, `wfi` 8345 and the fatal `sleh` 8363 — the fixture's own program order (`+96 +116 +140 +156 +176 +188 +212 +232 +276 +300`) followed by 593's causal chain. The magic is read out of `entry_ramdisk.s`'s own `.equ MH_MAGIC` and the read is scored as a *relation* (`ret_lo == nbytes`, `word_after == MH_MAGIC`), so no number is a literal here. **And the PASS text names its own ceiling**: "it is the floor, not this run's progress … Losing it would be a regression; having it says only that the OS still boots to pid 1's syscalls", with a closing banner naming what the block does *not* decide. **The new `say` strings' own defect, found by `bash -n`:** an apostrophe inside a parameter expansion (`${g_magic:-the fixture's magic}`) opens a quoted string *inside* the expansion word, so the closing `}` never closes it and bash reports an unmatched `{`; a default in `${var:-...}` is not inside the double-quoted string the way the rest of the argument is. Same class as 594's backtick defect one operator over — and the backtick half is structural now, so the new lines were checked by it unasked. **(2) `0x8000` is where the publisher last spoke, not where the machine died** — rung 0's narration, 594 §5's correction and 595 §7/§9(a) all said the second, and **the same log that supplied the number falsifies it**: 533's `door_seq=0x8000` is at line 8133 and the capture runs on past it through the whole userland phase, the park's `repair`/`sip`/`pce`/`wfi` and the fatal `sleh` at 8363 — so both baselines lived on past it and died in `[32768, 65536)`, below the next power of two. **The test is unchanged** (`> 32768` still means "reached 65536 passes", which no baseline run did); only what the record is *called* changed, and a reader sent to "where the machine died" was sent to the wrong record. Six sites in three files, `grep -rn 'where the machine died' docs/ stages/stage90/` being the whole enumeration, **swept by predicate and not by provenance** (560's lesson). **Verified on eight states**: 533 and 520 (PASS, and 520's PASS comes from a log that lands in the `else` — which is the point of not gating it), the four earlier synthetics A–D, and two built for this: `syn-E` (control also answers 0 → the pair's FAIL) and `syn-F` (survived, truncated before userland → UNREAD), plus `syn-G` (survived *and* the syscalls → both PASS, the conjunction the report asks for). What it does not buy: no arm, payload, gate or prediction changed — `60063c47…` is still the image the next press sends. Nothing flashed, nothing written to storage, TWRP still withheld |
| stage90 | [experiment-597](experiment-597-the-pop-loads-the-word-our-wrapper-spilled.md) | **the word the `pop` loads is the one our own wrapper spilled, and it is one address** — two facts established by different steps, never joined, and joining them turns the cache account of the death from a plausible story into a measurement. **(1) The addresses are one address, computed from the image rather than argued:** the two idle wrappers spill `r4` at `sp-12` and `platform_cache_idle_exit`'s `push {fp, lr}` saves `lr` — the word the `pop {fp, pc}` loads as `pc` — at the *same* `sp-12`, measured from `cpu_idle`'s own `sp` at its three `bl`s (all three at `+8` from its entry, so a wrapper's entry `sp` *is* the base). New host-side tool `tools/check_idle_frame_slot.py` derives it and **refuses** if the two ever differ; verified on five states — the frozen arm and the sleepless arm both pass, and three doctored `/tmp` copies are refused (the enter wrapper's `strd` moved to `#-16`, the exit wrapper's frame shifted so the pop reads `-4`, and a CSSELR write planted in `CleanPoC_Dcache`). **(2) The value is that register's:** both archived runs' fatal `pc` equals `xnu_live_slot_rtcpre_pop & ~1` exactly — 520 `0x04b79075`→`0x04b79074`, 533 `0x33f1c1b5`→`0x33f1c1b4` — and `r4` is loaded from `cpu_data+0xe0` at `0x8000d98c`. **Together they force the conclusion**: the exit's `push` stores `lr` to that address with `SCTLR.C` clear (`0x80046240-48` clears it, `0x80046320-24` restores it), so DRAM holds the right word; the only other writer of that address in the boot is this project's own wrapper. So **a cache line older than the push answered the lookup** — and its content is not anonymous: it is the wrapper's registration save at exactly the address the death names. **(3) The correction the seam's arm rests on:** `entry_seam_flush`'s note says the enter path "cleaned the whole L1 earlier in this same window", so its clean half "has nothing to write back". Neither half holds — `CleanPoU_Dcache` is called at `0x80046274` **after** `SCTLR.C` was cleared, and no set/way sweep in this image can select a level at all: the image has exactly **two** CSSELR writers (`machine_write_csselr`, `entry_epilogue`), neither a cache routine, and `do_cacheid` selects L1 then L2/L3 and **never selects L1 back**. So the enter wrapper's `strd` at `0x8047c8d4` (its first instruction, `C = 1`) dirties a line **nothing in the window cleans or invalidates**, and the arm faces a **dirty** line — the *invalidate* is what repairs the pop's two words and a *clean* writes the stale spill over the pushed `lr`. **And a second live word the arm does not restore:** the exit wrapper stores its own `lr` at `sp-4` (`C = 0`) and reads it back at `0x8047c9b8` (`C = 1`), in the same line — so a clean writes the **enter** wrapper's `lr` (`0x8000da2c`) over the exit's (`0x8000da40`), and `b entry_note_pcx` would re-enter the `wfi` path: **a window loop that never reaches `ClearIdlePop` and never returns, offered as a candidate for 535's non-return and marked as one** (574's arm reads only the real exit's two words, so its pair is blind to `sp-4`). **(4) 534's conclusion stands, its citation does not:** `xnu_entry_csselr_before=0x2` is an *entry-phase* key and cannot speak to the level at the idle exit; the structural half (`do_cacheid` is the only CSSELR writer and ends on L2) is what carries it, now checked. **Not built and not run:** the two candidate repairs change `entry_trace.c`, a build input, and a rebuild would replace the sleepless arm the next press is for; that file's comment is likewise **not** edited, because it is covered by `xnu_arm_entry-sources.txt` and a comment edit would make the image stale and make the gate refuse — a comment edit would block the boot. Nothing flashed, nothing written to storage, the parked frozen payload re-hashed byte for byte, TWRP still withheld |
| stage90 | [experiment-598](experiment-598-the-baseline-had-no-lane-and-one-register-had-two-publishers.md) | **the baseline had no lane, and the window's SCTLR had two publishers** — the step 594 §5 and 595 §9(b) both named and both deferred, landed and validated against the one artifact it could be: **520**. **The gap:** the clause block is a three-way and the control flow kept two — `if sleeper / elif grep xnu_live_slot_cwe_ / else` — and `xnu_live_slot_cwe_` is `entry_window_note`'s key, i.e. **533's instrument and not the arm's signature**, a fact about the *image*. 520's image predates it (`slot_cwe_*` has 3 occurrences in 533 and **0** in 520, while both carry `door_seq`=16 with `sip_seq`/`pce_seq`/`wfi_seq`/`repair_seq`=1), so half of this project's own baseline satisfied every criterion the five clauses are written for and could not enter the block, and the `else` it landed in named *three* states in prose while the code kept two. **The widening:** the selector is now `xnu_live_door_seq`'s presence — published by `__wrap_Idle_load_context` on the earliest passes of every boot that reaches `cpu_idle`, so it is the arm's signature rather than the newest instrument — and it is a **widening and not a re-keying**, checkable because `slot_cwe_` present implies the window ran implies `door_seq` was published. **Measured on 520's own capture, before and after:** 112 lines → **151**, with clause (1) `PREDICTED` (`xnu_live_sleh_lr=0x800462dc`, `pc=0x04b79074`), clause (3) `DIED IN THE EXIT` from the three bracket publishers, and the banner *the arm's prediction arrived* — the reading that did not exist for half the baseline. **And the second thing the validation found:** the cell clause (2) reads has **two publishers of one register read at one point in one wrapper** — `entry_window_note(win, entry_sctlr())` (`entry_trace.c:1973` → `slot_cwe_set`) and `entry_note_pce_after(..., entry_sctlr())` (`:1976` → `xnu_live_pce_after_sctlr`, `entry_stubs.c:5610`), with no `SCTLR` writer and no cache operation between them, so they must be equal — and the older one is old enough to be in 520's image: **520 carries it, `0x30c57879`, C clear, the cell that 547 §4's prediction is for.** The fallback is named, and **checked**: wherever both keys are present the block compares them and prints the outcome on *both* branches, because a check that succeeds by printing nothing cannot be told from one that never ran, and a disagreement is a `FAIL` (two names for one value is this project's most-paid-for defect class, so the fix is a comparison rather than a substitution). **Three defects the change forced out:** the `UNREAD` text that said *the pair's keys are in this log* was false of exactly the logs the widening adds; the new by-construction branch's first draft named 520 as its example and **520 takes the branch above it** (found by running it — the printed sentence now says "This is not 520's shape"); and clause (1) printed *this* log's registers as *520's* ("520's is pc=$sleh_pc" — on 533 that read `0x33f1c1b4`, a value 520's log does not contain), false the moment a second baseline carried the line. **Verified on eleven states** with the **pre-598 runner itself as the oracle** (the file stashed, both versions run on the same logs, `git stash pop` verified by sha): 520 and 533 are artifacts and the rest are shapes — `syn-A/B/C/E/F/G` and the empty log **`cmp`-identical**, `syn-D` differing only in the `else` paragraph, `syn-H` (neither publisher) firing the by-construction branch, `syn-I` (publishers disagreeing) firing the agreement `FAIL`, `syn-J` the `else`. Gate re-run: **EXIT=0**, 518 lines, 0 stderr, the only difference from the 597 gate run being the exit census's three line numbers (same codes, same count — 584's mechanism). One host-side file, +150/−43; no build, no device, no `fastboot`, nothing written to storage; `60063c47…` is still the image the next press sends. TWRP still withheld |
| stage90 | [experiment-599](experiment-599-the-window-is-unreachable-on-the-arm-that-is-parked.md) | **the window is unreachable on the arm that is parked, and that is a fixed point rather than an intention** — 594 built the sleepless arm, 595 gated and parked it as the image the next press sends, and both justified it with a *story about a control flow*; this turns that into a property of the image, before the one scarce resource (a power press) is spent on it. **The gate, read out of Apple's own source and matched instruction for instruction** (`cpu.c:118`): `if ((!idle_enable) || (cpu_signal & SIGPdisabled)) Idle_load_context();` then `if (!SetIdlePop()) Idle_load_context();` then the window — and in the image `0x8000d960 ldr r0,[r5,#0x28]` / `0x8000d964 cmn r0,#1` / `ble -> door` (which **is** `cpu_signal & 0x80000000`, `cpu_internal.h:68`) / `0x8000d96c bl __wrap_SetIdlePop` (**the gate**) / `0x8000d974 bne -> the window` / `0x8000d97c b __wrap_Idle_load_context` (the fall-through door). **And `cpu_idle` is `noreturn`** — both exits are *tail branches* (`0x8000d97c`, `0x8000dad4`), so "leaving by door 1" is **the healthy idle** (`Idle_load_context` is the routine that saves the idle context and dispatches another thread), not a spin — which corrects the natural reading of 594 §1's "bypass". **The gate dominates the window**: each of the three wrappers has exactly **one** `bl` site in the whole image, all in `cpu_idle` (188/204/208 bytes after the gate), and the region `[0x8000d980, 0x8000da50)` has **no branch into it from outside** — every branch targeting inside starts inside, except the gate's own. **And the gate can never open, because `SIGPdisabled` is a closed fixed point of this port**: set at init (`cpu.c:365`), clearable only by `cpu_signal_handler_internal(FALSE)` (`cpu_common.c:401-402`, the `mvn r1,#0x80000000` / `bl hw_atomic_and` pair), whose **only caller** is `cpu_signal_handler()` — registered as **the IPI handler** (`machine_routines.c:605`, in `ml_processor_register`) — while the IPI *sender* is guarded by the same bit (`cpu_common.c:343`/`:374`) and `real_ncpus == 1` (549). Set ⇒ no IPI ⇒ no handler ⇒ never cleared. So 514's repair is not a convenience: **it is the IPI, simulated** — and 593's observation is derived rather than inferred. **The differential, which is the one number that separates the arms:** the frozen 574/535 image has **1** clearer (`0x8047be64`, in `__wrap_poll`) ⇒ *reachable EXACTLY ONCE*; the sleepless image has **0** ⇒ *UNREACHABLE*. **And the ceiling is not "time stops"**: three of the image's four `bl rtclock_intr` sites are outside the IPI handler (`entry_irq_handler` and the two `locore.s:1617`/`:1858` shims, the first inside `fleh_decirq_handler`), so the tick has a route that does not need the IPI and the park's timed poll can still expire — which is also why the baseline's ≥32768 door-1 passes coexisted with its timed userland calls. New tool `tools/check_idle_window_unreachable.py`, **verified on six states**: both real images, and four doctored `/tmp` copies each firing its own predicate — the frozen arm's clearer planted ⇒ the verdict **flips**, `cmn`→`cmp` ⇒ the bit-test refusal, an intruder branch into the region ⇒ the domination refusal, the door's `b`→`bl` ⇒ the noreturn refusal. **Two refusals were the tool correcting its own author**: the first draft assumed one `SetIdlePop` site (there are two) and the second that the door was the instruction right after the gate's branch (it is two), each caught on the real image before any conclusion rested on it. No device, no build, no `fastboot`, nothing written to storage; `60063c47…` is still the image the next press sends and `master`/the work line are where 598 left them. TWRP still withheld |
| stage90 | [experiment-609](experiment-609-the-channel-says-when-it-is-full.md) | **the channel says when it is full, and three clauses were not asking** - the sweep 603 §7 named and 604/606/607/608 each repeated is taken, and it found what it was for: **the live channel publishes a record when it starts dropping records and no clause in the reader had ever read it.** `entry_live_write` (`entry_stubs.c`) publishes `xnu_live_cap` (8192, every boot) and, once `g_live_records` reaches the cap, publishes **`xnu_live_capped` exactly once** - at the first drop - and then drops silently, so a log can stop publishing **in the middle of a boot that is still running**. Three clauses read absence as a fact about the machine: rung 0's "did not get past 32768 passes" (`door_seq`), rung 1's "the park's poll has not come back" (**the falsifier** - and a falsifier that fires on a full channel is not one), and the goal block's "what is missing here is a **POSITION**". **Measured so the risk is a number**: the archived captures carry **4,258-4,449** records against the 8,192 cap (52-54%) and none is capped, which is why no run has shown it. The reader now prints the channel's state on **every** log in one of three states (`not full - N of 0x00002000` / `**TRUNCATED**` / `not in this log`), triggered by **two** signals - the `capped` record **or** the count reaching the cap the log itself published, because a boot that filled the channel exactly and then stopped carries no `capped` record at all - and the three clauses print `UNREAD` on a truncated log, including the summary's own last paragraph (a reader that says UNREAD and then draws the conclusion anyway is worse than either half). **The other eight `FAIL` branches are read and named**, with a second finding of the same class one build out: rung 1b compares the log's timeout against **this tree's** `ENTRY_PARK_MIN_MS`, which would silently re-point every archived log if that constant ever moved (593's class) - measured before calling it theoretical, it has been `1000` at every commit that carries it, and the assumption is now stated in the file. **Validation: 51 archived captures diffed against a same-depth oracle copy of the pre-609 runner - 51 show a change and ZERO of the changed lines is a verdict line.** Eight rehearsal states, a state per side (a `-capped` modifier whose base is what precedes it, so the only difference is the channel's own record) plus a **count-only** state no `capped` record can express. Gate **EXIT=0 / 537 lines / 0 stderr**; rehearsal **7 ok / 0 failed** and **13 ok / 0 failed**. **This does not advance the goal** - no device, no boot, no `fastboot`, nothing written to storage; `60063c47...` is still the image the next press sends. TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-648](experiment-648-the-readers-clause-chain-is-unreachable-on-the-sleeper-arm.md) | **the reader's clauses (1)-(5) are unreachable on the sleeper arm, and the arm table is where a keyless seam gets read** — 640 §5 found clause (5) names one cause for a keyless seam block where there are two, and derived the fix from the file (`idle_no_sleep_arm` is already derived there). Re-read against the reader's **actual control flow**, the repair moved: `:876` `if (( idle_no_sleep_arm == 1 ))` and `:1291` `elif grep -a -q 'xnu_live_door_seq='` are **one chain**, so a log that takes the sleeper ladder never evaluates clauses (1)-(5) at all — and a sleeper log *is* such a log by the arm table's own test (`:826-829`). **Measured on four archived captures** (markers: the ladder's verdict line, a line that exists only inside the clause chain, and the arm table's `door_seq` row): 513 run 1/2 (sleeper) **ladder 1, clause region 0, arm table 1**; 520 and 533 (baseline) **ladder 0, clause region 2, arm table 1**. **513's two captures are the shape the owed press can produce** — 513 is the record's own worked model of the no-repair regime, which 594's switch reinstates. Two mechanisms produce the same silence about the seam pair and they are different mechanisms: the `seam_calls >= 1` gate (`:1752`, what a *baseline*-shaped keyless log hits) and **the arm branch at `:876`** (what the *owed* log hits, one level up, skipping the whole death-shape block). The ladder already narrates the second (`:989`, *"the whole family clauses (1)-(5) score is absent"*, with 513 named as the counterexample); what it does not say is 640 §4's point — that the pair **cannot exist there**, so 638 §3's `a1`/`b1` table and 642's `sleh_pc` join are UNREAD on such a log. **So the reachable repair is the arm table's row, not the clause.** **And the first draft of the patch had a branch that could not be reached** — it re-tested `idle_no_sleep_arm` inside clause (5)'s new `else`, whose entry *is* that test; caught by running the reader over the four logs, where the arm-shaped narration printed on **no input at all** (the "cell that cannot reach the branch its label names" shape). **Staged, not landed**: `$CLAUDE_JOB_DIR/tmp/646-runner-repair/646-runner-repair-p0.patch`, base `run_and_capture.sh` `9b2b5ba0…`, product `e393ac6f…` + `bash -n` clean, re-derived from scratch in `/tmp` and hashed; it waits for the owed log because `press-watcher.sh:292`/`:296` fire the runner by path (620). Verified: `--summarise` old vs new on the four logs (513 **+5 lines** and **no clause (5) line**; 520/533 **+8**); on the 581-rehearsal **synthetic** seam-carrying log (invented keys, name says so, 562/563) the seam block's own output is **byte-identical** — 535's and 572's arms read exactly as before; and `tools/rehearse_live_path.sh` with `RUNNER_OVERRIDE` = the mutant is **39 ok / 0 failed** (20 live-path, 15 reading — so the mutated analyser ran 15 times — 4 path argument). The mutant lived at `stages/stage90/.r646-mut.sh`, a name the catcher never fires, and has been **removed** (tree clean, live runner back to `9b2b5ba0…`). Independently verified by `run-experiment-526` that **it cannot change what the press sends** — six hunks, all at line ≥818, all inside `summarise_log()` (`:498-2305`). **Two more measurements**: the runner prints **no self-revision anywhere** (`grep -nE '\$0|BASH_SOURCE|revision|self_sha|VERSION'` → only `:79`'s `cd` and `:307`'s awk field), so `press-watcher.sh:296`'s summary cannot be attributed to a reader revision — one line would close it, recorded and not folded in; and **the battery names no seam state at all** (`grep -n 'seam' tools/rehearse_live_path.sh` empty), so §4's runs are the only coverage the new row and `else` have. **Does not advance 「起码要能进入操作系统，把基础驱动跑起来」**; nothing built, nothing run, no `fastboot`/`adb`/`sudo` against a device, nothing written to storage; arm unchanged (`60063c47…` / `696a0f39…`, still UNRUN); TWRP withheld. |
| stage90 | [experiment-649](experiment-649-no-indirect-route-into-the-idle-window.md) | **no indirect route into the idle window — the address that matters is materialised nowhere in the image** — 599 proved the window unreachable on the sleeper arm with a tool that counts **`bl` sites**, so its coverage boundary is a **branch** census; a route that is not a branch (a function-pointer table, a literal-pool word, a `movw`/`movt` pair reached by `ldr`/`blx`) would be invisible to it. This measures that blind direction on the armed entry ELF, before the press. **The two calls, counted from the linked image** (`arm-none-eabi-nm -n out/stage90/xnu_arm_entry.elf`, then a 1,253,975-line full disassembly): `8000da3c bl 8047c944 <__wrap_platform_cache_idle_exit>` — exactly one site, inside `cpu_idle`, 208 bytes past the gate — and `8047c96c bl 800462d4 <platform_cache_idle_exit>` — exactly one site, inside the wrapper. So `cpu_idle`'s gate → the wrapper → the real routine, with no second entry; the gate region is printed instruction by instruction (`8000d968 ble` on `cmn r0,#1` = `SIGPdisabled` → the door, `8000d96c bl __wrap_SetIdlePop`, `8000d974 bne` → the window at `0x8000d980`, `0x8000da50` the end of `cpu_idle`). **A second `bl __wrap_SetIdlePop` exists at `0x8000da20` and is not a second gate** — it is inside the window region (`if (cpu_data->rtcPop != lastPop) SetIdlePop();`, `cpu.c:150`), so reaching it already means the gate was passed; a site count without the ordering sees two gates where there is one. **The address is materialised nowhere**: a byte scan of the whole 6.7 MB image finds `0x800462d4` **once** (file offset `0x5b7468` = `.symtab+0x61b44`, i.e. the symbol's own table entry) and `0x8047c944` **once** (`0x5a7438` = `.symtab+0x51b14`), with **0** occurrences of either Thumb-tagged form — and `.symtab` is not loaded at run time, so those are definitions, not references. That rules out a function-pointer table, an `.init_array` entry and a literal-pool word, the three forms an indirect call needs. The disassembly agrees on the assembled forms: `0` hits for `.word` naming either address **against 22,139 `.word` literal-pool lines**, `0` for `movw rX, #0x62d4`/`#0xc944`, `0` for the offset forms `#287444`/`#4704580` — each zero paired with a live-pattern baseline (`movt rX, #32772` appears **78** times; the symbol name itself 6 times), because *establish that the extractor could have seen it* is the discipline that makes a zero mean something. **Stated limit, not overclaimed**: this rules out every route that is not a branch and every *absolute* or *offset-form immediate* materialisation; it cannot rule out an address computed at run time from registers, which no scan of an image can — what it does say is that the inputs to such a computation are not in the image as constants. Corroborates 599 from outside its own method; **does not advance 「起码要能进入操作系统，把基础驱动跑起来」**, changes no artifact (no build, no `out/` write, no arm, no park, no gate, no runner), and does not land either staged repair set. Host-side and read-only: `nm`, one `objdump -d` and a Python byte scan on `out/stage90/xnu_arm_entry.elf`; no `fastboot`, no `adb`, no `sudo` against a device, nothing written to storage — `fastboot boot` only, never `flash`. Arm unchanged (`60063c47…` / `696a0f39…`, still UNRUN); TWRP withheld. |
| stage90 | [experiment-650](experiment-650-on-the-armed-arm-the-witness-cannot-mean-the-pop-was-survived.md) | **on the arm that is armed the witness cannot mean "the pop was survived", and the runner had one guarded cell and one unguarded** — two pre-press reader defects. **(A) The pre-registration's own inference.** The sheet's Step 6 reads a satisfied witness as *"The idle exit survived its `pop`"*, but the arm the press sends removes 514's repair, so `cpu_idle`'s gate never opens and `platform_cache_idle_exit` is never entered (599's fixed point, `tools/check_idle_window_unreachable.py` → *the window is UNREACHABLE in this image*, 649's census) — **a `pop` that never executes cannot be survived**. So on this arm a satisfied witness has exactly one available mechanism: the window was never entered. **And that is what the only hardware instance of the witness did** — measured, window-family keys counted per archived capture: 513 run 1 and run 2 each carry `door_seq` 25 with `repair_seq`/`sip_seq`/`pce_seq`/`wfi_seq`/`slot_cwe_` **all 0** and `poll_seq` 1,2,3,**4** with timeouts 5, 40 and **2000** ms — the witness satisfied with the window never entered; 520 and 533 (which *did* enter: `sip`/`pce`/`wfi` 1, `slot_cwe_` 3 in 533) stop at `poll_seq` 2 with no ask above 40 ms. So one observation, two causes, and the record names the one this arm cannot exhibit — [[mi4-one-value-two-definitions]] one level out. **The reader itself already knew**: its rung-1 PASS narration prints *"the same state has already returned from a park on hardware: 513's two captures … **the window never entered**"* — contradicting Step 6's parenthetical on the arm Step 6 is about. **What is not claimed**: this does not make the press worthless — on this arm a satisfied witness is a reproduction of 513 on the current payload (regression evidence) and **not** evidence about the `pop`; the reading that carries the press is the sheet's own second bullet, the *new stopping point*, read off the arm table and the ladder (648). **(B) One cell guarded, its neighbour not.** `:1075` opens `if [[ -n $poll_seq_max ]] && (( poll_seq_max > 2 ))`; rung 1b is a **nested** chain at `:1109`/`:1112`/`:1119`/`else :1128`, and the `$live_trunc` caveat is the **outer** `if`'s `elif` at `:1134` — so the rung-1b `else` (*"the poll that returned is not the park"*) has no truncation caveat although the premise is a property of the **log** and applies to both exits. Reachable by exactly one route: `poll_tmo_max` is a max over all records (`:915`), and the park's `poll_seq` and `poll_timeout_ms` are separate `entry_live_write` calls (`entry_stubs.c:4538` then `:4542`), so a cap landing between them publishes `poll_seq=3` with max timeout 40 ms. **Reproduced on a synthetic log** built from 513 run 1 (park timeouts `0x7d0`→`0x28`, plus ` xnu_live_capped=0x00002000`): the live runner prints *"A poll that is not the park came back"*, the repaired one prints `UNREAD … this log's live channel is TRUNCATED`. **Found by `run-experiment-526`, who reported rather than edited** (the runner is this lane) and then corrected their own account of the parentage — not siblings; the fix is a **third `elif` before the `else`**. **Staged, not landed**, as `650-runner-repair-p0.patch`: base `9b2b5ba0…`, product `5318afda…` + `bash -n` clean; with 646 in **either order** → `bc291893…`, so the two are order-free; both hunks at line ≥1078, inside `summarise_log()` (`:498-2305`), so it **cannot change what the press sends**, only what its read says. Verified old vs new on five logs: 513 r1/r2 **+5/−1**; 520, 533 and the 581-rehearsal synthetic seam-carrying log **byte-identical** (the seam block does not move, so 535's and 572's arms read as before). Mutant at `stages/stage90/.r650-mut.sh` (a name the catcher never fires), **removed** — tree clean. **§3, measured and not repaired**: a **catcher-fired ambiguity refusal spends the automatic cover permanently** — `press-watcher.sh:291` prints the relay's `COMMIT_LINE` (`press-watcher-relay.sh:82`) before `:292` runs the runner, and `spent()` (`:147-148`) then stops the relay (*"will never arm again"*, `:181-192`); only the **gate**'s refusal (`:287-290`) precedes the commitment line. **The first draft of that paragraph generalised one step too far and its scope was corrected the same day** — `press-watcher.sh:274-281` is a branch the catcher **holds** on (`ambiguous()`, `:132-137`) and loops rather than exiting, so with the neighbour in fastboot the commitment line is never printed, `spent()` stays false, and the catcher fires itself when the list settles (`:283`). The refusal-and-cover-lost path is therefore narrower: it needs the neighbour to *enter* fastboot between `:255`'s re-check and the runner's own list check, a race of the gate's runtime plus the runner's startup. *Unplug `33e80afe` first* stands as advice, but not because the cover dies if you don't — an operator told that would be pushed toward pulling hardware or hand-firing in a state that needs neither. **Does not advance 「起码要能进入操作系统，把基础驱动跑起来」**; no device action, no build, no byte of `out/` changed; arm unchanged (`60063c47…` / `696a0f39…`, still UNRUN); TWRP withheld. |
| stage90 | [experiment-652](experiment-652-the-park-ran-and-the-pop-consumed-words-the-seam-never-read.md) | **the park ran, the seam's pair is equal — and the words the `pop` consumed are not the words the seam read** — the owed press fired 2026-09-24 17:24:03 and **returned**: `RUNNER EXIT=0`, gate exit 0, log captured (`708ff83e…`, preserved in `out/stage90/captures/650-owed-run-park-574-*`). It sent the **574 park** (`151425c4…`, `IDLE_NO_SLEEP=0`, `SEAM_MEASURE=1`, `SEAM_POC=0`), by the operator's choice at the one decision point that was theirs. **First run in this project to carry BOTH a `xnu_live_seam_*` pair and a return** — the cell 638 §3 was written for. **The pair:** `seam_calls=1`, `seam_lr=0x800462dc` (the exit's own `bl`'s return address — entered at the seam, not at one of the routine's three other callers), `seam_sp=0x8054fec8`, `seam_sctlr=0x30c57879` (`C` clear ⇒ the four words were read out of DRAM), `b0/b1 = 0x800b2648/0x8047c990` and `a0/a1` the **same** — **`a1 == b1`**, which is the second row of 638 §3's pre-registered table: the flush did **not** write DRAM ⇒ the stale line never left the cache; a restore runs with `C` clear so it writes DRAM and cannot reach a cache line ⇒ **candidate (B) is provably useless** and **597's candidate (A)** is the direction. **So the choice 597 could not make is made by measurement**, and `seam_op=0` makes the arm a clean control (no operation behind the interception, `FlushPoC_DcacheRegion` at 0 calls in this image). **The death, and the join 642 asked for — which now exists and DISAGREES.** `panic … sleh_abort: prefetch abort in kernel mode: fault_addr=0x5006e74`, with **`pc == r4 == r11 == far == 0x05006e74`** (not a kernel address) and **`sp = 0x8054fed0 = seam_sp + 8`** — the abort is at the `pop`, at the slot the seam read. That is 597's signature a third time (fatal `pc` = the spilled `r4`: `0x04b79075→…74`, `0x33f1c1b5→…b4`, here exact). **But neither of the seam's four readings contains `0x05006e74`**, and 642's three-way join (`sleh_pc` against `b1`, `a1`, `rtcpre_pop`) is read for the first time — it matches **none** of them (`rtcpre_pop` is absent from this log). **So the pair the arm published is not the pair the `pop` consumed**, which is a different defect from the one the pair tests: either the slot was **written between the seam and the pop** (`r4` holds the same value — the shape of a `str r4, [sp, …]`) or the `pop` read a **different slot**. This log does not separate them, and it is not guessed at. The negative it does establish: **`a1 == b1` is not a statement that the slot was intact when the `pop` ran** — two moments 10 bytes apart in one function. **The frontier has not moved**: `poll_seq` stops at **2** (5 ms, 40 ms, no ask ≥1000), so 638 §3's premise is not confirmed and the falsifier stands; but this arm **entered the window** (`sip/pce/wfi=1`, `slot_cwe_*=3`) and localizes the death inside `platform_cache_idle_exit` (`slot_pre_calls=1` and `slot_rtcpre_calls=1` published, `slot_post_calls` absent). `progress: 3 user-mode fault record(s)` — 520 had 3 — `sleh_storm=9`, 520's count. The user-mode floor PASSes (driver-answered `open`, `read` → `0xfeedface`, getpid/exit/wait/ast) and is **a floor 520 and 533 also met**. **Two reader defects found by reading this log, left unrepaired:** (a) the same image is named as **three arms** in one run — 533's, then *"`xnu_live_seam_calls` is present, so the arm under test is 535's"*, then *"`seam_op=0x00000000` ARM 572"* — for an image that is none of them, because the measure form was built as 535's interception with the operation removed and so inherits both keys; (b) **the wrong label is the one that recommends the next arm**: the clause (1) FAIL says *"this image carries 535's arm"* and names the seam's two stores and the operation's clean half as the next candidates — on an image where `seam_op=0` and the body check counts `FlushPoC_DcacheRegion` at **0**, so neither exists. The predicate needs the `SEAM_POC`/`SEAM_MEASURE` pair, which the record carries and this reader never reads. **`bc0d92a` landed the four staged repairs in the same window, because the press being spent ended the 620 hold** — the relay printed *"the press has been spent … will never arm again"* at 17:24:31 and exited, so nothing armed is left to be changed under. 637+640 in the gate (`87753103…` → `ad698429…`, 140/40, `bash -n` clean, gate re-run **exit 0 / 578 lines**) and 646+650 in the runner (`9b2b5ba0…` → `bc291893…`, order-free, every hunk ≥818 inside `summarise_log()`); the 640 repair is the one this run validates, since on the arm sent (`=0`) its conditioned sentence is **true**. **Numbering: 651 is `run-experiment-526`'s doc in the export dir (the catcher's ambiguity hold) and `bc0d92a`'s subject line also says 651, so "651" names two things; this record is 652.** Does **not** advance 「起码要能进入操作系统，把基础驱动跑起来」 — the OS is still not observed booting, and the next step is a **build** (candidate (A) is an `entry_trace.c` change), now permitted because no press is armed. `fastboot boot` only, **never `flash`**, nothing written to storage; the phone returned to Android by itself. TWRP-to-storage stays withheld. |
| stage90 | [experiment-653](experiment-653-the-acting-arm-built-on-the-controls-reading.md) | **the acting arm, pre-registered on the control's reading — candidate (A) is one switch pair from the arm that just ran** — the owed run returned `b1 == a1` (638 §3's second row), which selects a repair that acts on the **cache**; this record names the arm that puts the operation back **before** it is built, the way 574 §6 pre-registered the measure arm's rows. **Nothing in `xnu_arm_boot/` is edited**: the operation already exists behind `STAGE90_XNU_SEAM_POC` (`entry_trace.c:2282-2294` — `FlushPoC_DcacheRegion(slot, 8u)`, an **MVA** clean-and-invalidate to PoC over the slot's line, then the restore of the push's two words), and the build sets exactly **two values** against the record of the arm that ran: `SEAM_POC` `0→1`, `SEAM_MEASURE` `1→0`, with `IDLE_NO_SLEEP=0`, `SLOT_NULL=1`, `EXIT_POC_FLUSH=0`, `IDLE_CACHE_ENABLE=0`, `ISTACK_SEPARATE=0`, `IDLE_STACK=1` all unchanged. **The two keys are one arm of one seam**, refused together by `build_entry.sh:441-447` and by `entry_trace.c:437-438`, so the only difference between the two runs is steps 3+4 of one function. **Why this is candidate (A)** and not a guess: `FlushPoC_DcacheRegion`'s one cache instruction is `DCCIMVAC` (clean-and-invalidate **by MVA**, `caches_asm.s:298-310`, `MMU_CLINE` 6 ⇒ 64-byte lines), and an MVA operation is defined to the **point of coherency**, so it reaches whichever level holds the line without XNU's operand-encoded level or a `proc_reg.h` geometry — the two questions 534 left open, and exactly the escape from the measured defect (Apple's own `FlushPoU_Dcache`, the routine the seam intercepts, **did not reach the line**: an equal pair with `C` clear means no write-back, and 534's arithmetic says why — `CSSELR` is left at the L2, so the L1-geometry loop sweeps 256 of the L2's lines and **none of the L1's**). Its clean half has something to write because the line is **dirty** (the enter wrapper's `strd r4, [sp,#-12]!` at **`0x8047c8d4`** — corrected here: 638 §3 spells it `0x8047c8b4`, one digit off; the frozen ELF, 519, 520, 590 and the gate all say `0x8047c8d4`). Step 4 is required as the operation's **consequence**, not as the (B) candidate the pair rejected. **Pre-registered readings, and the reader already has the cells** (`run_and_capture.sh:2069-2088`, the `seam_op == 0x1` branch, quoted): `a1 ≠ b1` ⇒ **`CHANGED`** (*"a dirty line, holding something this block does not name"*) ⇒ **the operation reached the line**, and the control's equal pair beside it makes the difference the operation and nothing else, with `a1` the number to join against the death's `pc`/`r11`; `a1 == b1` ⇒ **`CLEAN LINE`** ⇒ the **falsifier** (*"the falsifier for 546 section 3's mechanism rather than its confirmation"* — neither the level account, nor the dirty-line account, nor the MVA escape survives, and m659's identity question becomes the whole story); `a1 == rtcpre_pop` ⇒ **`STALE LINE, WRITTEN OUT`**, **unreachable on this arm** while `SLOT_NULL=1` (`rtcpre_pop` is absent — recorded so its absence is not read as its refutation, and `SLOT_NULL` is left alone deliberately: an arm that changes the operation *and* the instrument at once attributes nothing). **The independent reading is the frontier itself**: every capture since 520 shows `slot_pre_calls`/`slot_rtcpre_calls` published with `slot_post_calls` **absent**; getting past the `pop` would publish the post site and make the park's witness a *boot* reading (the third `poll` returning on an arm whose window **was** entered) — the first outcome in this phase that is progress rather than a re-reading. **A non-return (exit 2) is 574 §6's third row and 597's window-loop candidate** — 572 §5's first candidate (the clean writing back the line's other fourteen words) is what the pair cannot see — and it carries **no capture**, so it refutes nothing about the pair; the payload's armed watchdog caps it at **28 s**, and the live channel is the instrument that exists for that cell (609/610), not asserted to have drained here. **As built (host-side, no device):** entry `a43304f2…` (5,519,996 B; record `e3af19b4…` reads back `SEAM_POC=1`, `SEAM_MEASURE=0`, `IDLE_NO_SLEEP=0` and the seven unchanged keys; `build_entry.sh` narrated the deliberate change and its only differing tokens are the two seam keys), payload **`stage90-qcdt.img` `a5997bae…`**, gate **exit 0 / 557 lines** with the blob at payload offset 494,100; **the payload's own record stayed `6c2b6038…`, byte-identical to both parks** (the entry switches are not in the payload's dump), so the attribution rests on the entry record. **The first `./build.sh` omitted `STAGE90_EXTRA_CFLAGS='-DSTAGE90_XNU_ENTRY=1'` and the gate refused** (`STAGE90_XNU_ENTRY 0u` out of the build's own `-dM` dump) — 533 §(3)'s defect reappearing, caught by the gate rather than by a press. Parked at `out/stage90/frozen/armed-seam-poc-a43304f2/` **and** `arm-653-seam-poc-a43304f2/` in the export dir (11 files, new named set in `revert-set.txt`, `verify_revert_set.sh` exit 0 on both). **Readiness: 4 of 5 ok, the only FAIL physical** — the neighbour `33e80afe` sits in fastboot, so a run fired now is refused at the ambiguity guard. **And the readiness tool's own arm row named this arm *"574's park"* and pre-registered the *measure* arm's question for it** — two arms answer the reachability sentence identically, the separating switch is the record's seam pair, and the row did not read it; repaired to name three arms from `SEAM_POC`/`SEAM_MEASURE` joined to the reading, refusing a missing, duplicated or impossible pair, exercised in five directions. Does not advance 「起码要能进入操作系统，把基础驱动跑起来」 by itself; **TWRP-to-storage stays withheld**. No device action; `fastboot boot` only, never `flash`; the arm that just ran verifies from `arm-574-idle-no-sleep-0/` (exit 0, 11 files) outside the tree a build rewrites. A press now waits on the neighbour off the bus and a **fresh arming** of the catcher (652 ended the cover; the catcher is the peer's file). |
| stage90 | [experiment-654](experiment-654-the-battery-read-a-trees-motion-as-a-runner-failure.md) | **the live-path battery's two reds were the tree's motion and not a runner defect - and `out/` is now pinned per state, with a third verdict and a third exit code** - `tools/rehearse_live_path.sh` returned **`18 ok, 2 failed`**, both reds the **last two cells of section A** (`adb-plan-line`, `fastboot-plan-line`), both `exit 1, promised 0`, and both **the gate refusing a torn tree**: `STAGE90_XNU_SEAM_MEASURE=1` in the record beside an image whose `entry_seam_flush` calls `FlushPoC_DcacheRegion`, and `stage90.bin` embedding an entry of **the same length and different bytes**. **The timestamps settle it**: an entry rebuild in this session wrote `xnu_arm_entry.elf`/`.bin` at **17:39:41**, the record at **17:45:44** and the payload at **17:47:21**, while those two cells ran **17:39:34 -> 17:39:59** - so `out/` was not self-consistent for **7 m 40 s**, and the only two cells inside that window are the only two that went red (the states before them read the whole, pre-build arm and passed). The second refusal is the gate's **first** clause, not its only one (`preflight_boot_check.sh:431` vs `:2605`), so the two cells read as two different failures without the tree having been consistent for either. **The tell is sharper than the timing**: `fastboot-plan-line` was written (628) because the press's branch could have lost its plan line with no cell noticing, so its red message is *character-for-character* what a genuinely dropped plan line prints - one red, two mechanisms, separated only by the per-state `err.txt` ([[mi4-measurement-defects]] m662). **The repair pins the tree the live path reads**, in `run_state`: the five files the gate reads (`xnu_arm_entry.elf`/`.bin`, `stage90.bin`, `stage90-qcdt.img`, `xnu_arm_entry-config.txt`) hashed with `sha256sum` **and** their mtimes, immediately **before and after each state** - never the build's own `SHA256SUMS.txt` (it agrees with whatever is on disk) and mtime is in because the gate's freshness sweep compares mtimes. A state whose pins moved prints **`TREE`**, is counted in **neither `ok` nor `failed`** (a pass across a write is as unsupported as a fail) and the run ends at **exit 2**, distinct from exit 1's "the runner is wrong". **The detector's firing is measured**, not asserted, with an additive knob (`REH_TREE_EXTRA`, which can only *add* pins): probe written 12 s into `happy-adb` (window 18:09:25-18:09:49) -> `TREE happy-adb` with the before/after line (`e3b0c442…` = the empty file), **`19 ok, 0 failed, 1 with no verdict`, EXIT=2**. **And the knob's first version added nothing while printing that it was armed** - `printf '%s'` writes no trailing newline and `while IFS= read -r` **drops an unterminated last line**, so a single-path `REH_TREE_EXTRA` pinned nothing and the run would have read as *"the detector does not fire"* (m663); repaired to `printf '%s\n'` **plus a count of the pins that landed**, printed when it agrees and **refused (exit 1) when it does not** (1 extra -> 6 pins, 2 with one non-existent -> 7, none -> 5). **The same battery on the quiescent tree: section A 20 ok / 0 failed, B 15 ok / 0 failed, C 4 ok / 0 failed, EXIT=0** - so the two cells are green and the runner has no regression there; this is also the first battery whose reading is pinned to a tree. **Deliberately not done**: a `pgrep` "is a build running?" pre-check (it would read as a guarantee it cannot give), pins in `reader_state`/section C (`--summarise` never reaches the gate or the plan), and the **press-side hardening** - the gate reads five files in sequence and a rebuild can put a *different consistent* pair before it (636's acceptance direction), so pinning the arm's identity around the gate call is an open item, not done here. **The operating rule the pins encode**: do not build while a battery (or a press) is running, in either lane. No device action, no build, no byte of `out/` written (the arm still verifies against the `armed-seam-poc-a43304f2` park: `f4ef57fc…`/`a43304f2…`/`a47bc89f…`/`a5997bae…`/`e3af19b4…`); no catcher is alive; `fastboot boot` only, **never `flash`**; TWRP-to-storage stays withheld. |
| stage90 | [experiment-656](experiment-656-the-joins-third-arm-matches.md) | **642's three-way join matches its THIRD arm exactly, and the "absent" that hid it was a key *name*** - 652 was the first log carrying both sides of it and recorded the join as disagreeing with all three, because *"`rtcpre_pop` is absent from this log"*. **It is present**: `xnu_live_slot_rtcpre_pop=0x05006e74`, line 8358 of the archived capture, and the reader's own extractor resolves it (`rtcpre_pop=$(keyval slot_rtcpre_pop)`, `run_and_capture.sh:1809`). **So `sleh_pc = far = pc = r11 = r4 = 0x05006e74 = rtcpre_pop`** while `b1 = a1 = 0x8047c990`: two arms fail, the third is an exact equality. **The false absence came from grepping the wrong name** - `keyval`'s *argument* is `slot_rtcpre_pop`, the published key is `xnu_live_slot_rtcpre_pop`, and the hand-check `xnu_live_rtcpre_pop` occurs **zero** times and prints the reassuring answer (`run_and_capture.sh:266` spells the full name on the same line as the mistake). **And the switch does not gate the family the name suggests**: `slot_` is a prefix shared by **two instruments** - the SLOT_NULL *null* instrument (`xnu_live_slot_{sp,m16,m12,m8,m4,calls}`, inside `#if STAGE90_XNU_SLOT_NULL`, `entry_stubs.c:6315-6347`) and the **rtcpre** instrument (`g_slot_rtcpre`, a separate key table with **no `#if` guard**, `:6403-6406`). Measured counterexample on the arm that ran: `SLOT_NULL=1` **and** `xnu_live_slot_rtcpre_pop` in the same log; `SLOT_NULL` still stays 1 on the acting arm, but the reason is "it would add an unrelated behavioural difference", not "it would recover a lost reading" ([[mi4-one-value-two-definitions]] m664). **The reader's third cell fires, measured on three one-variable fixtures built from the real capture** (`seam_op` 0->1 plus one `a1` edit each, run through `--summarise`): `a1==b1` -> `CLEAN LINE`; `a1=0x8054fec0` -> `CHANGED` (its own sentence printing `rtcpre_pop=0x05006e74`); `a1==rtcpre_pop` -> **`STALE LINE, WRITTEN OUT`** - so 653's "unreachable" row is withdrawn and the press's pre-registration is **three rows of equal standing**, with the third the strongest and carrying a known candidate value. **And `sp` rules out one of 652's two mechanisms**: a **prefetch** abort means `ldmia sp!, {fp,pc}` had already committed, so `sp_at_pop = 0x8054fed0 - 8 = 0x8054fec8 = seam_sp` and the `pop` loaded `pc` from `[seam_sp+4]` - the word the seam read as `b1` - leaving "the slot changed between the seam's non-cacheable read and the `pop`'s cacheable one", i.e. 597's stale line, whose contents the values now name (a non-cacheable store writes DRAM and leaves the line untouched, so a line left dirty with the loop's deadline before the disable answers the first lookup after it; stated as the reading the values support, not as a measurement). Also anchored from the same dump: `r6 = 0x8051a0e0 = cpu_data + 0xe0` (rtcpre_pop's **address**, 590's `add r6, r5, #224`), `r9 = 0xc0558df0 = rtcpre_thr`. **No runner change and none needed** - the reader is right in both places; the defect is entirely in the record, so the repair is a corrigendum in 652 and 653. Does not move the frontier (`poll_seq` stops at 2, `slot_post_calls` absent) and does not advance the goal; no device action, no build, no byte of `out/`; nothing written to storage, `fastboot boot` only, never `flash`; a press watcher is armed (the neighbour `33e80afe` still in fastboot), which is why no file the press fires was touched. TWRP-to-storage stays withheld. |
| stage90 | [experiment-657](experiment-657-the-acting-arms-cells-re-ordered.md) | **the acting arm's three cells, re-ordered: an unequal pair reads `STALE LINE, WRITTEN OUT`, not `CHANGED` - and that row is the one that predicts the frontier moves** - 653 pre-registered `a1 != b1` -> `CHANGED` as the row selecting 597's candidate (A) and filed `a1 == rtcpre_pop` -> `STALE LINE, WRITTEN OUT` as unreachable; 656 withdrew the unreachability, and this step finds the mapping **inverted**, from the arm's own instruction stream. **(a) The seam's `a`-reads precede its restore** (`entry_trace.c:2254-2300`: `b0/b1` -> Apple's flush -> `FlushPoC_DcacheRegion(slot,8u)` -> `dsb` -> **`a0/a1`** -> **restore** -> `dsb` -> publish), so the published pair reads what the *operation* did to DRAM before anything repairs it. **(b) The operation's own bytes**: `entry_seam_flush` calls `FlushPoU_Dcache` at `0x8047ca58` and `FlushPoC_DcacheRegion` at `0x8047ca64`, which is a 64-byte-line walk (`and r2,r0,#63` / `bic r0,r0,#63` / `lsr r1,r1,#6`) whose maintenance instruction is `fmdr_loop` `0x80045710` **`ee070f3e` = `mcr p15,0,r0,c7,c14,1`** - the **MVA** clean-&-invalidate 638 §1 tabulates - so `(slot, 8u)` covers one whole line and both `slot` and `slot+4`. **(c) The line is dirty and its contents were already measured by the dead run**: `b1` (non-cacheable) `0x8047c990` vs the `pop`'s cacheable load of the same address `0x05006e74` = `rtcpre_pop`. A clean writes the whole line to DRAM, so a working operation puts the deadline at `slot+4` and `a1 == rtcpre_pop`. **(d) The reader's cell order settles which sentence that is** (`run_and_capture.sh:2069-2088`): `a0==b0 && a1==b1` -> `CLEAN LINE`, **elif** `a1 == rtcpre_pop` -> `STALE LINE, WRITTEN OUT`, else `CHANGED` - the first needs *both* words equal, so an unequal pair never reaches it and the exact `a1` test wins before `CHANGED` can. **So the row that fires is the one 653 retired, and `CHANGED` is the least likely of the three** (it needs `a1` to be neither the deadline nor the pushed `lr`). **(e) Why it is the GOOD row**: the restore then rewrites both words in DRAM with `C` clear and the line has just been invalidated, so the `pop` - the first cacheable load after the exit's re-enable (`0x80046320-24`, `isb` `0x80046328`, `pop {fp,pc}` `0x8004633c`) - misses and fetches the repaired DRAM, i.e. **`slot_post_calls` should be published and the frontier move**. `a1 == b1` (`CLEAN LINE`) is the falsifier - the operation inert with `C` clear, which would answer 638 §5's open question "inert" and make candidate (A) as built useless for a reason unrelated to the level arithmetic. And `b1 = 0x8047c990` is re-confirmed as the wrapper's return address from `bl platform_cache_idle_exit` (`8047c98c` -> `8047c990`), 582's prediction, measured a second time. **No runner change and none needed** - the cells and their order are right, only the record's mapping was wrong. Does not advance 「起码要能进入操作系统，把基础驱动跑起来」 by itself (`poll_seq` still stops at 2, `slot_post_calls` still absent); no device action, no build, no byte of `out/`; nothing written to storage, `fastboot boot` only, never `flash`; a press watcher is armed, which is why no file the press fires was touched.  **CORRIGENDUM (657 §4.1/§4.2, 2026-09-25):** the "why it is the GOOD row" reading above was **withdrawn and then narrowed by measurement**, and this row is the last carrier of it to be corrected. **(a)** 571 - the step that built this arm - had already registered the `a1 == rtcpre_pop` cell (measured "as designed" on 568) **and registered the opposite frontier consequence** for it ("the invalidate did not survive the distance to the `pop`"), so the pair does **not** by itself predict that the frontier moves; the honest pre-registration is both readings, and §4.1 says so. **(b)** §4.2 then *measured* that object: the four instructions `0x8004632c`-`0x80046338` are `mrc TPIDRPRW` / `mov r1,#1` / `ldr r0,[r0,#1484]` / `str r1,[r0,#304]` - **no `cr7` write of any kind**, the load at `0xc05593bc` (TPIDRPRW = `0xc0558df0`, this arm's published `xnu_live_pce_tpidrprw`) and the store at `0x8051a130` (the dump's `r0 = 0x8051a000 = cpu_data`, +304), neither in the slot's line `0x8054fec0`-`0x8054feff` - so 571's mechanism is out and the live alternative is **one level below**. And the operation's two halves are **one** instruction: `cfmdr_loop` `0x800458b0` holds a single `mcr p15,0,r0,cr7,cr14,{1}` (DCCIMVAC, clean **and** invalidate) and the built call `entry_seam_flush:0x8047ca64` passes len 8, whose loop arithmetic runs it **once at `0x8054fec0`**, so the cell that shows the write-back shows the invalidate too - **but only for a dirty line**. **(c) `CLEAN LINE` therefore has TWO readings** (the operation inert, or the line already clean with the invalidate still having run), and the frontier reading (`slot_post_calls`/`poll_seq`) is what separates them. The four joint cells are pre-registered in `experiment-658` §1. |
| stage90 | [experiment-658](experiment-658-the-next-arm-and-the-envelope-a-longer-boot-is-bounded-by.md) | **the next arm, designed before the press - and the envelope 「不能变砖」 rests on, measured as the frontier advances** - the owed press is blocked on one physical thing (the neighbour `33e80afe` still parked in fastboot, so a run fired now is refused at the ambiguity guard and boots nothing), and this step does the two things that need no device. **(1) The press's reading to the next arm, pre-registered as a two-dimensional cell**: the pair (657 §4) crossed with the frontier (`slot_post_calls` `run_and_capture.sh:1669`, `poll_seq` `:1087` - **both have a PASS row**, so a success cannot arrive as an `UNREAD`, the 630 rule) gives four cells, and the two that share `dies at the pop` need **different** arms: `STALE LINE, WRITTEN OUT` + dies means the invalidate happened at L1 and the `pop`'s word came from below it (**the level walk**, §2), while `CLEAN LINE` + dies means reach is the suspect (**maintenance with the cache set**, or the same walk with `CSSELR` written). **(2) The next arm designed and deliberately NOT built**: 534's finding is the fix - the tree has **exactly one `CSSELR` writer** (`machine_cpuid.c:88`, reached only from `cpuid.c:214`'s `do_cacheid`, which never selects L1 back) and every set/way sweep assumes the level instead of writing it, while Linux's `cache-v7.S` `v7_flush_dcache_all` writes `CSSELR` before each level's `CCSIDR` read and sweep. So the arm saves `CSSELR`, walks `CLIDR`, selects each level and sweeps its `CCSIDR` geometry - and **restores `CSSELR`**, because a fix that leaves it moved fixes one quantity by redefining another. It is not built because building replaces the payload under an armed watcher. **(3) The envelope, five readings:** the host half is a rule not a check (`fastboot boot` only, never `flash`); the payload's static tripwire re-run by hand clean on both halves of `stage90.elf`; the frames the 574 run ever published are **exactly two** (`gicdrv_phys0=0xf9000000`, `timerdrv_phys0=0xf9020000`); the kernel's identity window is **16 MB** (`xnu_entry_args_memSize=0x01000000`, `physBase == virtBase == 0x80000000`, so `[0x80000000, 0x81000000)`); and storage on this part is the single megabyte `0xf9800000`-`0xf98fffff`, outside both the window and the frames - so a stray store takes a translation fault, `sleh_abort`, panic, `MACH Reboot`, and 652 measured the phone returning to Android by itself. **(4) Two gaps named rather than glossed** (the constraint is absolute and an over-broad guarantee is this project's most expensive defect class): the gate's **symbol** tripwire is scoped to `stage90.elf` and blind to the 6.7 MB entry image it jumps into - and running it there fires **25 false positives** (`ufs` matches `bufsize`/`nkdbufs`/`ubc_upl_maxbufsize`, `partition` matches `IODTNVRAM::getNVRAMPartitions`), so its scope cannot simply be widened: the patterns need anchoring, and that is a gate change that waits for a window with no watcher; and the **mapping** half is checked by nothing - the docstring's own "the mapping is what actually prevents the write" rests on `arm_vm_init.c:381-382` and on `entry_mmio_section` having exactly one call site (`entry_gic.c:377`), and `xnu_entry_jump.c:161-162` hands XNU the **device tree**, so the day IOKit matches a node and calls `ml_io_map` a *new* mapping appears that nothing in this chain would see before the press. **No device action, no build, no edit to the gate, runner or readiness tool** (all three are fired by path by the armed watcher). Does not advance 「起码要能进入操作系统，把基础驱动跑起来」 - `poll_seq` still stops at 2 and `slot_post_calls` is still absent. TWRP-to-storage stays withheld. |
| stage90 | [experiment-659](experiment-659-the-presss-only-automation-falsified-not-assumed.md) | **the press's only automation, falsified rather than assumed - and its success was silent** - a press is owed and the armed watcher (`/tmp/r654/press-on-clear.sh`, pid 1951993) is the **whole** of its automation, so it was measured instead of trusted. **(1) The falsification needs two readings**: four samples 12 s apart show the `sleep` child's pid advancing (2536144 → 2536212 → 2537154 → 2537292) **and** `voluntary_ctxt_switches` climbing 928 → 937 → 945 → 962 (~8 per iteration: the two `sudo` forks). The pid alone is weak - at a random instant the loop is asleep ~99 % of the time, so almost any snapshot has a `sleep` child - and the counter is the reading a lucky sample cannot satisfy; a loop wedged on its first `sudo` would freeze both. **(2) The defect the falsification exposed**: after 19 minutes and ~114 polls the watcher's log held **two** lines, both from its own start, because it prints on a *condition changing* - so `press.log`, the artifact the operator reads while waiting, **cannot distinguish "polling every 10 s" from "wedged since 00:52:49"**. The liveness evidence lived only in `ps`, which nobody runs while waiting - [[mi4-silence-is-a-reading-only-if-success-is-silent]] in the one file the press's automation is judged by. **(3) A monitor, passive by construction**: `/tmp/r654/heartbeat.sh` (pid 2537421, 60 s) appends one line a minute with the verdict (`ALIVE`/`STUCK`/`GONE`), the counter movement, the `sleep` pid movement and the watcher's own last line - **no `sudo`, no `fastboot`, no `adb`, no runner**, because a monitor that could fire would be a second press and exactly one press is the standing rule. Its verdict function is **selftested** so both directions are reachable (`--selftest`: **4 case(s) exercised, 0 failed**, including a frozen counter+pid that must say `STUCK`) - m663's rule, one step after m663 recorded it. Measured against the real target: `ALIVE vol=980->1032 sleeppid=2537413->2538216` over 60 s ≈ 6 iterations x 8. **(4) Named limits**: a *total* `sudo` failure keeps the counter moving but empties both lists, which the watcher prints into its own log every ~5 min; a *partial* one (fastboot fails, adb works) makes the neighbour look absent and would fire against a bus that still holds it - **refused at the runner's own ambiguity guard before anything is booted** (`FB_AMBIG_WAIT`, exit 1), so it costs the attempt and not the phone, and the remedy is a re-arm. No device action, no build, and **no edit to the watcher, the runner, the gate or the readiness tool** (the watcher is a running `bash` script besides - 620's hazard). Does not advance 「起码要能进入操作系统，把基础驱动跑起来」; TWRP-to-storage stays withheld. |
| stage90 | [experiment-660](experiment-660-the-armed-closures-two-directions.md) | **the armed watcher's edit closure: one direction of movement is caught, the other is not - and this session walked into it** - the armed watcher fires three files by path and reads the arm's bytes, so what must not move is a **closure**, and both directions of it are measured here. **§1 the lapse, stated plainly**: the watcher armed at **00:52:49** and `tools/verify_press_ready.sh` - **one of the three files it invokes** - was edited at **01:00:15** (`25c01e7`) and **01:05:43** (`c292607`), contrary to this session's own recorded rule. Measured scope: `git log --since` over the window shows **zero** commits touching `preflight_boot_check.sh`, `run_and_capture.sh` or `out/`; the hazard is bounded because what the watcher does with that file is `| tail -14` + `say "readiness exit=…"` - an **advisory print**, while the bytes come from `out/` - and the committed version was `bash -n`-clean and **executed after the edit** (4 ok / row 4 FAIL). The invariant stays discipline until a window with no watcher. **§2 the caught direction**: the manifest covers **every regular file** in `xnu_arm_boot/` (**20**, dotfiles included) by name+hash, and the gate's two commands re-run verbatim give an **empty `comm -3` on the real tree** (the positive control - the success direction is reachable) and a named diff for all three mutation shapes (comment-only edit → `entry_trace.c`; added file; removed file). **A measurement defect the control caught**: the first run named **`.gitignore`** in every case because `cp "$SRC"/*` drops dotfiles, so the mutant diff carried the *copy's* difference alongside the mutation's - fixed by `cp -r "$SRC/." "$DST/"`. And `run_and_capture.sh` has **no** sources clause, so the gate is this check's **sole** carrier. **§3 the uncaught direction - the rebuild**: `build_entry.sh:30624` hashes the new image and `:30662`/`:30692-30709` rewrite the config **and** the manifest **bound to that hash**, while `./build.sh` writes no entry-side record at all - so a rebuilt tree is **internally consistent by construction**, the gate (which additionally requires the manifest's hash to equal the bin's, `:1071`) passes, and **nothing in the firing chain compares `out/` against the arm the press is owed for**: the watcher would fire the *new* arm, 658 §2's "bytes no pre-registration describes". **And the one tool that would notice is advisory**: readiness row 1 hashes `out/` against `revert-set.txt` and the park, but the watcher captures `${PIPESTATUS[0]}` into a `say` with **no `if` between it and the gate** - a check read by a human afterwards rather than by the thing that decides. **§4 four repairs, all waiting** (test readiness in the firing path; let the gate read the owed arm's identity; anchor the storage-symbol patterns, 658 §3(a); and 654 §6) because every one is an edit to a closure member. **§5 the closure roster** for the next session. No device action, no build, no edit to any closure member, nothing written to storage. Does not advance 「起码要能进入操作系统，把基础驱动跑起来」; TWRP-to-storage stays withheld. |
| stage90 | [experiment-661](experiment-661-the-three-repairs-staged-and-rehearsed.md) | **the three repairs 660 named, staged and rehearsed outside the tree - because the tree is closed** - (R1) **the watcher must TEST readiness, not only print it**: the block is three lines and the load-bearing detail is that `PIPESTATUS` is read on the line *immediately after* the pipeline, before anything else overwrites it; `/tmp/g661/press-on-clear.v2.sh` is the whole watcher with it in place (`bash -n` clean) and the block alone was exercised against **both** exit codes (`0 -> carrying on to the gate`, `1 -> REFUSED`), because a branch that only ever sees one value is 630's problem one level in. (R2) **the gate clause that asks *which arm*, not *is this consistent***: it reads `stages/stage90/revert-set.txt` - **whose own header says it is written by hand and never by the build** (*"a record the build writes agrees with itself and constrains nothing"*, which is 660 §3's finding stated by that record) - and asks whether `out/stage90/` equals, **file for file, one** recorded set. A rebuild of unchanged sources still passes (the entry build is byte-for-byte reproducible, so the hashes are the same); a changed source, a set **mixed** from two arms, or a removed file is refused with the near-miss named (`10/11, differs: xnu_arm_entry.bin`). Measured in four directions: the real tree is **11/11 PASS** while the other two sets read **2/11** (the positive control, so the comparison is discriminating and not a tautology), and three mutants all REFUSE. (R3) **anchoring the storage-symbol patterns**: the rule is a **delimiter**, not a word boundary - `\b` treats `_` as a word char, so `\bpartition` misses `bdev_partition_scan`, and `\bmmc\b` misses `mmc_host_alloc` (which the **current** pattern gets wrong: **8/9** on the positive control). `(^|[^A-Za-z0-9])(sdcc|emmc|nand|mmc|ufs|flash|partition)` catches **9/9** of a synthetic positive control and **0** on both real images, clearing the **25** false positives. Named gap: a **camelCase** storage name (`AppleSDXCController_probe`) is still missed - the tripwire is a heuristic, as the address checker's own docstring already says of itself. **Nothing landed**: every artifact is under `/tmp/g661`, no file in 660 §5's closure was edited, no build, no device action; R4 (654 §6) stays a design note. Does not advance 「起码要能进入操作系统，把基础驱动跑起来」; TWRP-to-storage stays withheld. |
| stage90 | [experiment-662](experiment-662-the-press-fired-and-the-device-did-not-come-back.md) | **the press fired, and the device did not come back - exit 2, no capture, and the only variable that changed was the operation** - readiness **5 ok / 0 fail** (exit 0), gate **exit 0** (557 lines), runner **exit 2**; `Sending boot.img (8340 KB) OKAY` / `Booting OKAY` sent the recorded arm `armed-seam-poc-a43304f2`, and `/tmp/cancro-last_kmsg.txt` **does not exist** (the previous log was parked to `.prev.3` first, sha256 `708ff83e…`, so nothing was lost). The blocker that had held this press since 2026-09-24 19:06 cleared at **03:52:53** when the neighbour `33e80afe` left the bus, and the watcher fired 33 s later. **Timeline read by SERIAL, which is the runner's own instruction** (a port-only test on this host can read a stranger as a return): `2376679` 3-3 disconnects, `2376766` 3-10 leaves adb, `2376772` 3-10 re-enumerates as `idVendor=18d1 idProduct=d00d` (fastboot), `2376773` `SerialNumber: 4a2fe00b`, `2376776` disconnect = `fastboot boot` handoff - and **nothing since**; the counters agree independently (`1537 -> 1537` by serial, `1545 -> 1545` on the port). **The payload's 28 s watchdog did NOT bring it back** - the net that returned the phone on 2026-09-24 17:24 failed here, as memory 517 recorded for another arm; the phone is not bricked, it needs a power press (the documented worst case), but a non-return the watchdog cannot recover is a cost the next arm must be designed against. **No reading**: exit 2 means the pair was never read, so 653's pre-registration is *unanswered*, not falsified, and 657 §4.2's four cells have none filled. **What the run does establish**: from the two arms' own config files, the whole difference between returning (574 park: `SEAM_MEASURE=1 SEAM_POC=0`) and not (acting: `SEAM_POC=1 SEAM_MEASURE=0`) is **the operation behind the interception** - same seam, same interception, same two reads, same `pop`; only `FlushPoC_DcacheRegion(slot,8u)` and the two restore stores differ. Three explanations stay consistent and **the run cannot separate them**: (1) the repair worked, the `pop` was survived and XNU hung later in a state the watchdog cannot reset; (2) the operation hung the machine before the `pop` in a way that defeats the watchdog; (3) the reset happened and Android failed to come up (the runner's own note says that is the same reading from adb alone) - and only a log separates them, which is why the next press is worth spending on the **same arm**. **Explanation 1 is NOT claimed.** **Three repairs landed in the same window** (the press being spent lifted the edit freeze; land then arm, 653's rule): **R1** the watcher now *tests* readiness (`ready=${PIPESTATUS[0]}` read on the line after the pipeline, then `exit 1`; block exercised at both codes), **R2** the gate's new `== the arm the record owes ==` clause asks *which arm* rather than *is this consistent* - `out/` must equal one set recorded by hand in `revert-set.txt`; its first real run prints `11/11` for the acting arm against `2/11` for the other two, and the patched gate is exit 0 with a `diff` showing only the two changed sections - and **R3** the storage-symbol patterns anchored on a delimiter and widened to the entry image (0 on both real images, 9/9 on 661's positive control). R4 (654 §6) still waits. **The phone needs a power press** (hold Power 10-15 s, release, press normally) and **the watcher is re-armed**, so it will reboot into the payload again within seconds of Android coming up - by design, and worth telling the operator. No `flash`, nothing written to storage. TWRP-to-storage stays withheld. |
| stage90 | [experiment-663](experiment-663-the-arm-whose-reading-survives-a-hang.md) | **the arm whose reading survives a hang - designed from the non-return, before the repeat fires** - the consequence the non-return forces: every instrument writes into a **RAM console in the top of DRAM**, and the top of DRAM is reachable only by a run that **comes back**, so **a hang destroys the reading and no amount of instrumentation produces one**. That revises 662 §4's plan to spend the next press on a **repeat of the same arm**: at n = 1 a repeat reproduces an unreadable non-return, so its only verdicts are "back" (which the 574 park already did without the operation) or "no reading again" - a coin flip on information. **The design**: end the run at the seam on purpose, after the pair is published - read `b0`/`b1`, run the operation, read `a0`/`a1` and restore, then **force the reset** and do not return to the exit. If the operation is what hangs the machine the forced store never executes (**and that is a reading**); if it is benign the phone warm-reboots and **the pair comes back**, which 657 §4's four pre-registered cells turn into a **2 x 3 table with five informative cells**. **And the measurement removes the arm's hard part**: the payload's reboot is PS_HOLD **plus** the watchdog bite as an independent net, and the gate notes the entry image carries no address in the watchdog's page - but `MSM8974_WDT_BASE = 0xf9017000` (bite at `+0x14`, so **`0xf9017014`**) is *inside the `0xf9000000` megabyte the entry side already maps every run* for the GIC probe (`entry_gic.c:377`), which `hw_watchdog.c:53` says itself. So **the primary mechanism is two stores into an already-installed section** (`hw_watchdog.c:314-315`'s BITE=1 then RST=1 idiom, no new mapping), with the PS_HOLD store as the fallback; the arm must record which it used, and it must keep the acting arm's four switches otherwise identical. **The arm after it** is stated in §5: if this one returns with `a1 == rtcpre_pop` the frontier arm is the **`CSSELR` level walk** (658 §2); if it never reaches the forced store, the operation is fatal by itself and the next step is to **bisect it one arm at a time** - *and every one of those arms is readable, because each is forced to end at the same step*. **Nothing built** (a watcher is armed, pid 3454466); **the arm must be recorded in `revert-set.txt` before its press**, and that is no longer advice - 662 §5's **R2** clause refuses a fire whose bytes are not a recorded set.  **§3.1, added after measuring the premise**: the design rests on *force the reset and the run comes back*, and that is **in doubt by measurement** — the payload arms the watchdog for 25 s bark / 28 s bite (`0xc7fb5`/`0xdffac` over `WDT_HZ=32765`) and **nothing pets it once XNU runs** (`WDT_REG_RST` is written in exactly two places, `hw_watchdog.c:231` arming and `:315` the forced bite), so the bite was due 28 s into the 03:53:50 hang and the phone was still dark **282 s** later — and the gate has counted the same outcome four times before (517/521/522/526, all needing a power press). So the honest design forces **BOTH** paths in `platform_reboot`'s own order (PS_HOLD first, then the bite), and — more important — **a rehearsal arm goes first**: an arm that does *nothing* at the seam except force both resets and not return, so that its verdict is one unambiguous bit and **both outcomes buy a fact** (the reset path works and §2 is sound; or it is broken and §2 would have been a wasted press). Same discipline as 661's R1-R3 and m668: rehearse the scaffolding before trusting what is built on it.  **§3.2, added after measuring the reset itself**: §2's step 4 says *"through the payload's own reboot path"*, and this tree holds **two** things a reader could call a reboot path — only one can reset anything, and the other **hangs**. Measured read-only on the frozen images: (a) **`PE_halt_restart` is an unfilled `.bss` slot** at `0x805858d8`, `= 0` at `IOPlatformExpert.cpp:288` and **assigned nowhere** in the tree, and **both** `haltRestart` overrides take `mvn r0,#0` with it zero — `IOPlatformExpert::haltRestart` (`0x801762c0`, read at `+0x14`) and **`IODTPlatformExpert::haltRestart`** (`0x801781f0`, read at `+0x34`, the override a real device tree uses), the latter reached virtually through `gIOPlatform` (`0x805858d4`, vtable offset 892) from `PEHaltRestart` (`0x80177010`); (b) **`halt_all_cpus` (`0x80011e84`) does not return — it ends in `b 80011eb4`, an infinite spin** after its `bl PEHaltRestart`, and neither it nor `host_reboot` (`0x800fc3e8`) touches PS_HOLD or the watchdog, so **there is no hardware write anywhere in the OS's own reboot path** — an arm that entered it would hang with `SCTLR.C` wherever the seam left it and with nothing to bring the phone back; (c) it is **unreachable from the seam anyway**: `reboot_kernel` (`0x8029f230`) has **exactly one** `bl` in the image (`kern_xxx.c:140`, inside the `reboot(2)` syscall) and **none** of `reboot`/`halt`/`shutdown`/`pei` is among the **77** names the entry image wraps, so a `--wrap=reboot_kernel` hook would be entered by nothing and would move `.text` for nothing. **So the reset must be the explicit hardware writes** — §3's two primitives — and the rehearsal arm's step 4 is the writes, not a call. Both literals are new on the **bite** side (the entry image carries no `f9017` in any of the four encodings an instruction can put an address in), **but not on the PS_HOLD side - corrected by [experiment-664](experiment-664-the-entry-image-already-owns-the-forced-reset.md)**, which found the entry image's own `entry_epilogue` already doing the forced `PSHOLD = 0` reset and the flat grep that missed it. No device action, no build, no closure edit; TWRP-to-storage stays withheld. |
| stage90 | [experiment-664](experiment-664-the-entry-image-already-owns-the-forced-reset.md) | **the entry image already owns the forced reset - a false absence, the encoding it hid behind, and what it changes about the next arm.** 663 §3.2 closed with *"the entry image contains 0 occurrences of `f9017` and 0 of `fc4ab000`, so both literals are new"*. **The second half is false**, and this step corrects it in place. The address is in the image at `80005794`: `movw r3,#0xbfff` + `movt r3,#0xfc4a` + `str r2,[r3,#-4095]` = **`MSM8974_PSHOLD`**, assembled from two 16-bit immediates so a flat-string grep cannot see it. **And the method was already in the tree, twice**: `tools/check_storage_refs.py:24` says of its own instrument *"PS_HOLD as `movt ..., #64586` (`0xfc4a`)"* - that is the instruction - and the gate prints a row for exactly this page with a `movt_pattern` helper, whose comment states the rule (*"before concluding a value is absent, establish that the extractor could have seen it"*) and the trap (a pre-ARMv7 decoder prints `.word` for every `movw`/`movt`). **The real fact**: `entry_stubs.c`'s `entry_epilogue` (`:3531`, `__attribute__((noreturn, noinline))`) ends (`:4227-4234`) with `RESTART_REASON ← 0x78665501`, `dsb`, **`PSHOLD ← 0`**, `dsb`, `for(;;) wfe` - and **`fleh_dabort` ends in it on both of its exits** (`:9395` *"exception: data abort"*, `:9401` *"a data abort inside the data-abort handler"*). So **this store is how a run comes back**: the frontier's `pop` faults, the handler records it, the report lands in the RAM console, PS_HOLD resets the phone, Android comes up, the catcher captures `/tmp/cancro-last_kmsg.txt`. **Three consequences**: (1) **the PS_HOLD half of 663's step 4 already exists and is exercised on every returning run** - four archived captures are the proof - so the **rehearsal arm should force the bite alone**; (2) an abort cannot leave the phone dark by itself (both `fleh_dabort` exits reach the store), which narrows 662 §4's explanations to the one needing no extra assumption - a hang that takes *no* fault, so no handler runs; (3) **the bite half survives a proper check**: `f9017` is absent in all **four** encodings an instruction can carry an address in (flat text, `movw`/`movt` halves, offset immediates from the GIC's base, literal-pool words), and the watchdog's section is mapped strongly-ordered - `mmu_entry_gic=0xf9010c02` shares section index `0xf90` with `0xf9017000`, and `mmu_entry_pshold=0xfc410c02` / `mmu_entry_imem=0x0fa10c02` cover the epilogue's own two stores (base = bits 31:20; the `1` is bit 16, the section's shareable bit). **One question opened and not settled**: the entry image installs exactly **one** section into XNU's live tables (`entry_gic.c:377`), and the epilogue's two stores are in **different** sections that only the payload's table established - they work (the returns are the evidence) but nothing checks that they still do. No device action, no build, no closure edit; TWRP-to-storage stays withheld. |
| stage90 | [experiment-666](experiment-666-the-bounded-spin-was-not-bounded.md) | **the bounded spin was not bounded — an unsatisfiable guard found by building the arm that needed it, and the payload build that does reproduce.** 664 §4 concluded the next press should force the **watchdog bite alone** (`STAGE90_HW_WATCHDOG_SELFTEST=1`: arm the watchdog, do NOT arm the software dead-man, spin, come back on the bite). This step built that arm, and building it is what found that the arm was not safe. **§1 the defect, measured three ways**: `timebase_elapsed_us` (`timebase.c:39-43` at HEAD) returned `(delta * 5u) / 96u` — a **32-bit** product that wraps at 858,993,459 ticks (44.74s at 19.2MHz), so its largest possible return is **44,739,242us** while `STAGE90_SELFTEST_DEADLINE_US` is **90,000,000** ⇒ `elapsed >= 90000000u` **cannot be true**, the spin never breaks, and the two `log_kv32`s and `platform_reboot()` behind it are **dead code**. So the one arm whose own comment promises *"the device comes back without being touched"* was the one arm that could not — with the bite dead, the only net left is the software dead-man this arm deliberately does not arm. (The fallback is stronger than that comment says: `platform_reboot()` is **PS_HOLD *and* a forced bite** — `stage90_main.c:1067-1071` then `hw_watchdog.c:314-315` — but only if the branch is reached.) Seen as (a) the object's `add r0, r0, r0, lsl #2` 32-bit \*5 plus a `umull` reciprocal; (b) the guard itself, `movw r6, #0x4a80` + `movt r6, #0x55d` then `cmp r0, r6` / `bcc` — an unsigned `>=` against a ceiling it cannot reach; (c) a host program on the exact types, exhaustively over 2^32 inputs, where the old function returns **521,514us** at the 90s delta (the wrapped value — which is why a broken guard of this shape does not look broken). The old comment's *"for short intervals used here, low 32-bit deltas are enough"* was true of every caller **except the one that mattered** (20ms/50ms GIC windows, ~8ms preflight, 1.9ms) — *a claim in a comment is not a check*, and here the claim was the reason the wrong arithmetic was chosen. **§2 the repair, exact and helper-free**: the obvious widening `(uint64_t)delta * 5u / 96u` **does not link** — measured — because GCC 10.3 emits `__aeabi_uldivmod` and this payload is freestanding (`undefined reference` in five places), so the old comment's worry about a 64-bit helper was right and it was the *product* that never needed the width. Instead the dividend is split at the divisor: `delta = 96q + r` ⇒ `delta*5 = 480q + 5r`, and `480q/96 = 5q` exactly, so `q*5u + (r*5u)/96u` is exact in 32 bits with both terms small. **Host-verified exhaustively over all 2^32 inputs, 0 mismatches**; ceiling **223,696,213us**; `elapsed_us(1,728,000,000 ticks) = 90,000,000` exactly, so the guard is now reachable *at* the deadline. The same decomposition fixes the inverse, and **a duplicate definition was removed rather than a second copy fixed**: `gic.c`'s `timer_usec_to_ticks` and `timebase.c`'s `usec_to_ticks` were byte-identical; they are now one `timebase_usec_to_ticks`. The linked image has **no undefined symbols**. **§3 the structural half**: the deadline's reachability is now a compile-time `#error` in `stage90.h` — with the `#define` **first**, because an `#if` written above it would read the macro as 0, pass for every input, and guard nothing (m663's shape, and this block's own first draft); **falsified** by raising the deadline to `300000000u` and watching the build refuse; and a second check on the `uint32_t` return was written and then **deliberately removed**, because ticks are 19.2× microseconds so it could never fire — one check that can fail beats two where one is a restatement. **§4 the arm, and the two things building it found**: built with `STAGE90_EXTRA_CFLAGS='-DSTAGE90_XNU_ENTRY=1 -DSTAGE90_HW_WATCHDOG_SELFTEST=1'`. (1) **The placement is load-bearing and the gate is what said so**: the first build put the spin before `build_stage90_apple_dt()`, which makes that `static` function unreachable, so the compiler drops it and its literal — and the image then carries **one** copy of `up_style_idle_exit=1` where 515's repair needs two, which the gate refused *after* the bytes had been parked and recorded (`1 occurrence(s)` before, **`2` and `2`** after). The repair is a placement: the self-test now sits **after** the DT build — nothing lost, the watchdog is still armed first, so the builder runs under the hardware net — and the arm becomes the same payload as every other arm of this stage. (2) **The `build.sh` clause this step had briefly added to skip the 515 leg for a self-test build was deleted**: the gate re-runs that property from the image, so a skip could only ever have let through an image the gate refuses — a build-time check that disagrees with a gate that also runs is not a convenience, it is a hole. With the placement fixed the leg passes with no clause at all. **The arm**: `stage90-qcdt.img` **`ef0361a2…`** (8,540,160 B), `.bin` `fdcea7ee…` (6,015,252), `.elf` `20e2c0fd…`, parked at `out/stage90/frozen/armed-selftest-wdog-ef0361a2/` and recorded as the set **`armed-selftest-wdog-ef0361a2`** (11 files, `verify_revert_set.sh` exit 0 against *both* the live tree and the park; the superseded first build is left in place, named, and is **not** a set — a withdrawn artifact is recorded, not tidied). **It is identifiable from the payload's own record**: `stage90-build-config.txt` is byte-identical (`6c2b6038…`) across the 574, sleeper and acting arms because those differ in *entry* switches, while this arm sets a *payload* switch — its dump reads `STAGE90_HW_WATCHDOG_SELFTEST 1` at line 16 where all three read `0u` (682 → 681 bytes). **Gate exit 0 / 546 lines / 0 stderr** with `--allow-xnu-entry --allow-hw-watchdog-selftest`; without the second flag it refuses at its own clause, which names the three outcomes — ~33s the bite fired · ~90s it did not and PS_HOLD returned the device · *never*, the one outcome with no log. **§5 the payload build DOES reproduce, and it corrects 408**: plain twice ⇒ the same bytes both times (the control); pre-fix sources with `-DSTAGE90_XNU_ENTRY=1` ⇒ **all seven members identical** to the parked acting arm (`936c2741…`/`a47bc89f…`/`a5997bae…`, and `stage90.img`, the fixture, the manifest and the config too), with the entry image `a43304f2…` byte-for-byte. The 48 bytes that started 408 are **the switch**: `kernel_size` 6,015,356 with `STAGE90_XNU_ENTRY=1` versus 6,015,308 without. Scope stated as narrowly as measured — one arm, this tree, this host, that switch set; it does not explain 526's `7819cddb…`/`76bf4ea7…`, and a payload embeds `xnu_arm_entry.bin`, so an *older* arm should not be expected to reproduce once the entry has moved. **What it establishes is what the project needs: an arm's bytes are a function of the tree and its switch set, so a park's provenance can be tested by rebuilding.** **§6 the arm's reading is content, not timing** (665 §5's requirement): the spin line prints before the spin, so a return at ~28s with **no** deadline line is the bite firing, and the deadline line is the bite not firing — both directions print. **§7 what blocks the press**, and two of the three failing readiness rows are the tool's own staleness: `tools/verify_press_ready.sh:86-87` **defaults `PARK`/`SET` to the sleeper arm** so `live arm is the recorded arm` FAILs against an arm from two steps ago (a default that names a historical arm is a stale pin — 653's repaired shape); the tool runs the gate **without** `--allow-hw-watchdog-selftest`, so `the gate accepts this tree` reports the gate's flag refusal as a verdict about the tree; and `the arm is named by a reading` name-checks the **ACTING** arm from the entry ELF and the seam pair — correct about the entry, wrong about the press, **653's defect recurring one arm later**. The fourth row is physical: neither list names `4a2fe00b` and **no catcher is armed**. **The cost of §5's fix is real**: the acting arm's payload can no longer be rebuilt from the tree (its park is unaffected), so a fixed acting arm would be a *new* arm with a new name. Does not advance 「起码要能进入操作系统，把基础驱动跑起来」; no device action, nothing sent, nothing written to storage, no watcher armed; no edit to `preflight_boot_check.sh` (the peer lane's file); **TWRP-to-storage stays withheld**. |
| stage90 | [experiment-667](experiment-667-the-park-is-found-and-the-arm-is-joined.md) | **the park is found from the bytes and the arm is joined to the payload record — the three stale rows 666 section 7 recorded, repaired, each measured before and after.** 666 section 7 named three stale rows in `tools/verify_press_ready.sh` and recorded them without repair, because two of the three were *refusals of a good tree* and one was **green on the wrong arm**. **(a) the default park named a historical arm**: `PARK`/`SET` defaulted to `armed-sleepless-696a0f39` — live two steps before this file's last edit — so with no arguments row 1 listed **ten members** as *"the LIVE file(s) are not the recorded bytes"* (`stage90-qcdt.img` live `ef0361a2…` vs record `60063c47…`) while every one of them WAS the recorded bytes of the arm in `out/` (the gate itself printed the park agreement file for file). The park is now **FOUND**: the live `stage90-qcdt.img` — the one file `fastboot boot` sends — is hashed and the record is asked which set records it, with zero matches and two-or-more both refusals that name what they found. The alternatives are worse and measured: a default is a name nobody typed, and the record's own `role=` text cannot answer it because **three sets** claim in their role to be *"the arm the next press sends"* (`armed-sleepless-696a0f39` line 126, `armed-seam-poc-a43304f2` 137, `armed-selftest-wdog-ef0361a2` 200). An explicit `--set` survives as the way to ask about another arm, and a WRONG one does not make a green row: `--set armed-seam-poc-a43304f2` refuses on the bytes (`a5997bae…` vs live `ef0361a2…`). **(b) row 3 could not be made green, and its refusal text was a line that did not contain the refusal**: it invoked the gate as `"$GATE" --allow-xnu-entry`, which on this arm is refused (`REFUSING: the hardware-watchdog SELFTEST skips the normal boot path and spins until a net reboots it; needs --allow-hw-watchdog-selftest`, `:1570`) — and the row's *detail* was built by a grep that matched the gate's **passing** line `no source file is newer than the image`, so the FAIL row narrated a line that was not the refusal. The flag set is now **DERIVED from the arm's own switches** (one switch-to-flag table, copied from the gate's conditions at `:1570`/`:1592`/`:1605`/`:1649`/`:1680`/`:1684`/`:1948`/`:1954`/`:1975`/`:1985`) and **printed as the two commands to type** — `./preflight_boot_check.sh --allow-xnu-entry --allow-hw-watchdog-selftest` then the same for `run_and_capture.sh` — so the flags the gate is checked with here and the flags the run is given there are one value with one definition (the runner hands unrecognised arguments straight to the gate, `run_and_capture.sh:142`). Two checks bound the copy: the gate's own flag vocabulary (usage line plus every `needs --allow-` / `without --allow-`) must be derivable, and anything it cannot produce is a **refusal that names the flag** rather than a silently under-supplied set; and `--gate-flags` falsifies the derivation, which it did — set to the old constant it reproduces the defect on demand (`exit 1 … REFUSING: … needs --allow-hw-watchdog-selftest`) and set to the derived set the row is green, so the seam is not a red-only knob. **(c) the arm row was green on the wrong arm**: its two inputs — the extractor's reachability sentence and the *entry* record — are identical between 653's acting arm and 666's self-test arm (the self-test arm carries the acting arm's entry byte for byte, `a43304f2…`), so it printed *"the ACTING arm (653 …)"* with the acting arm's whole pre-registered reading attached. What separates them is **one line of the payload's record** — `#define STAGE90_HW_WATCHDOG_SELFTEST 0u` → `1`, `diff`'d against `frozen/armed-seam-poc-a43304f2/`, while the sleeper and acting payload records remain byte-identical (re-measured) — so the arm is now the ELF reading **joined to the payload's own switch record**, and the join is required: a record that cannot be read, or a switch not named exactly once with a value, **refuses** instead of falling back to the entry reading, because the fallback IS the wrong answer. On a self-test arm the row now says what that means — the payload spins before the handoff (`stage90_main.c:1224-1249`), nothing pets the watchdog, the spin returns only by rebooting, so no `xnu_live_*` key is in the log at all, the seam pair and 638 section 3's `a1`/`b1` table and 642's `sleh_pc` join are UNREAD, this arm's own keys are the `hw_watchdog_*` block logged *before* the spin (the precondition, not the question — `hw_watchdog_enabled=0` says the press tested no hardware net), and the answer is the **time to return**: 25.0 s bark / 28.0 s bite against 90.0 s for `STAGE90_SELFTEST_DEADLINE_US`, with the deadline line's absence NOT the verdict (it can mean the log was read before 90 s elapsed). **Every new branch was falsified, not just exercised**: a scratch tree with a **truncated copy** of the qcdt (built by `head -c` *reading* the original, never an in-place write) makes row 1 name all four recorded sets; the record removed makes rows 3 and 4 name it; a duplicate `STAGE90_HW_WATCHDOG_SELFTEST` line plus a bare `#define STAGE90_CACHE_MODE` makes both rows name both; `--gate-flags '--allow-bogus'` reports the gate's own `unknown argument` at exit 2; a fake gate whose usage names `--allow-newthing` makes the vocabulary check name it. **One defect this step made itself, kept because the shape is the lesson**: a backtick inside the new double-quoted `conseq=` strings is a command substitution — twelve `… : command not found` lines and a detail line printed with the identifiers eaten (`there is no  key in it at all`) — and the older strings had none for exactly that reason, so a string that is *printed* is still *parsed*. A second one: `(set … - )`, the found case having no reason string, now filled. **Live run: 4 of 5 green** — row 4 naming *"the HARDWARE-WATCHDOG SELFTEST arm (666: `STAGE90_HW_WATCHDOG_SELFTEST=1`), and the entry it carries is the ACTING arm (653 …)"* — and row 5 red and true (neither list names `4a2fe00b`; no catcher is armed). Also one **finding in the peer's file, reported and not edited**: the gate's dead-man guard is `if [[ $SELFTEST == "1u" ]]` (`:1605`) while the payload's record is the preprocessor's own output (`build.sh:395-397`, `-E -dM`) and prints a command-line definition verbatim — the live record reads `STAGE90_HW_WATCHDOG_SELFTEST 1`, no `u` — and every documented form for the dead-man arm is `-DSTAGE90_DEADMAN_SELFTEST=1` (`README.md:162`, `docs/status/roadmap.md:424`, experiments 93/94, `docs/reference/recovery-and-rollback.md:277`), paired with `-DSTAGE90_HW_WATCHDOG=0`; so a record reading `1` would not trip that guard and the gate would pass an arm that hangs on purpose without requiring `--allow-selftest`. Sent to `run-experiment-526` (that file's lane); my side cannot be silent, since the tool refuses an unrecognised spelling of any switch it reads. Host-only: no device action beyond row 5's two device *list* reads, no byte under `out/` written, the falsification trees live outside the repository under `/mnt/data/scratch-verify-press-ready/`, `fastboot boot` only and never `flash`. Does not advance 「起码要能进入操作系统，把基础驱动跑起来」; the press still waits on the neighbour `33e80afe` off the bus, a fresh arming and the operator's press; **TWRP-to-storage stays withheld**. |
| stage90 | [experiment-668](experiment-668-the-arm-the-press-sends-is-pinned-by-the-runner.md) | **the arm the press sends is pinned by the runner (654 section 6's R4), and three instruments' flag sets come from one derivation — one missing definition, three symptoms, and one of them costs a press.** 667's lesson was that the gate's `--allow-*` set is a function of the arm's own switches; this step found the same hard-coded set in **two more instruments**. **(a) `tools/rehearse_live_path.sh`** passed `--allow-xnu-entry` by hand, so on the arm 666 parked **all twenty of its live-path cells failed** — measured on the unmodified file, with `REFUSING: the hardware-watchdog SELFTEST ... needs --allow-hw-watchdog-selftest` in every cell's `err.txt` — and they had been failing since that arm was built, unread. **(b) the armed press launcher** (`/tmp/r654/press-on-clear.sh`, a scratch file) fires both commands with `--allow-xnu-entry` alone **and** pins its readiness call to the superseded acting arm (`--set armed-seam-poc-a43304f2`), so a press from it is spent on a gate refusal and its readiness row blocks the owed arm entirely. **R4 landed in `stages/stage90/run_and_capture.sh`**: a new `== the arm this press is for ==` step before the gate resolves the recorded set from the `stage90-qcdt.img` hash, **requires** `--expect-arm=<set>` for a run that would send an image (**no default** — a default is a name nobody typed, 667), refuses on a mismatch naming both, and pins the hash — re-checked **immediately before the send** (a refusal: up to `FB_AMBIG_WAIT` seconds of device checks have elapsed since the gate ran, and nothing is sent yet) and **once after it** (a note: a state whose pinned inputs moved gets no verdict, 662). The pin is the image and nothing else on purpose: any rebuild that could change what the press does changes its hash. **The consequence is the point — the launcher's `./run_and_capture.sh --allow-xnu-entry` no longer reaches the gate, so a stale caller can no longer spend a press.** Two silences were found in the argument loop and fixed: `set -euo pipefail` made `--summarise` with no value end the script at `EXIT=1` with **no output at all** (pre-existing, measured), and an empty `--expect-arm=` is now refused separately from an absent flag (632's shape). **One question, one definition, twice**: `tools/resolve_arm_set.sh DIR` (which recorded set are these bytes — by **hash**, because the caller does not know the name, and the record's `role=` text cannot answer it: three sets claim to be next) and `tools/gate_flags_for_arm.sh DIR [--gate PATH]` (which flags this arm's record demands — still a copy of the gate's conditions, bounded by two checks: every switch named exactly once **with a value**, and every flag the gate's own usage line and refusals name must be producible). What deliberately did not merge, and why, is in section 3: the readiness tool keeps its reader for the **values** of two switches (row 4's join) and its `kv_of` for enumerating a **named** set's members — two different questions from the resolver's identify-an-unnamed-set-by-hash. **Falsified:** the resolver four ways (a truncated copy names its hash and all four sets), the flags tool five ways (including a fake gate naming `--allow-newthing`), `--expect-arm` absent on a live run (exit 1 before the gate, naming the argument to pass), naming the superseded arm, every `--expect*` spelling, both empty-value cases, and the seven-from-667 readiness suite re-run against the refactor (all seven still fire, now with the tool's wording). **The battery went 0 ok/20 failed -> 20 ok/0 failed**, with its other two sections green (15 and 4) and exit 0. Host-only: no device action beyond row 5's two device-*list* reads, **no byte under `out/` written**, `fastboot boot` only and never `flash`. **Does not advance 「起码要能进入操作系统」** — the frontier is where 652 left it; it makes the next press attributable and a press on the wrong arm impossible. **TWRP-to-storage stays withheld.** |
| stage90 | [experiment-669](experiment-669-the-presss-reading-is-a-time.md) | **the next press's reading, taken seriously — the owed press is for the hardware-watchdog self-test arm, and its whole question is a TIME; nothing in the tree computed it.** 668 made a press *attributable*; this step asked what its **log** would be read with, and measured three gaps. **(a) `stages/stage90/run_and_capture.sh` had no elapsed-time computation at all** — the interval between `fastboot boot` and the device's return is the arm's answer (`stage90_main.c:1244-1254` states both candidates, ~28 s if the SoC's countdown fired and ~90 s if the bounded spin did), and it was left as a subtraction in the operator's head on the one run whose log is the only copy of the measurement. It is now `BOOT_SENT_AT` immediately before the send plus `mark_return HOW`, one definition used by **all three** tests that can see a return (adb, the host log's serial counter, its port counter), printed in section 4 beside the exit code; the empty string means *not measured* and never 0, so `--dry-run` prints no number. The clock includes the transfer and the payload's start-up (633 measured 1.1805 s of prelude for the park arm — the print says so), and the candidates are 3.2x apart so the prelude cannot change the verdict. **(b) `summarise_log`'s marker table carried two of the arm's five `hw_watchdog_*` keys and neither of its two SELFTEST sentences** — the net's own evidence (`readback_ok`, `countdown_plausible`, `checksum`) was not counted, so the run whose purpose is to test the recovery net printed a table that could not show the net's evidence. All five and both sentences are in it now, plus a `reading:` clause that **joins the arm's two numbers to the run's one** — neither site has both: the arm's are in the log, the measured one is in the run. Its selector is the arm's own sentence (`hw_watchdog SELFTEST: spinning`) and **not** a switch in the build record, because `--summarise` is used on logs captured in other sessions where no record is being read. **(c) the arm's numbers arrive as hex** (`~0x0000001c`, `~0x0000005a`) — m672's shape, a value that is present in an encoding a reader may not decode — so both spellings (`ram_console.c:37`'s `0x%08x` and `xnu_log.c:29`'s raw eight digits) are accepted, and one number found is a reading that says so rather than a crash. **Falsified:** four synthetic logs through `--summarise` (the 0x spelling, the raw spelling giving the same 28/90, a one-number line, and a log with the five keys but no spinner line, where the clause prints nothing), and the whole live-path battery re-run after the change. **And the launcher is repaired and rehearsed, NOT installed**: `/tmp/g668/press-on-clear.v3.sh` declares the arm it is armed for as `$1` with **no default** and refuses a mismatch, checks that declaration **twice with the wait in between** (R4's pin one file over), takes its flags from `tools/gate_flags_for_arm.sh`, **tests** readiness instead of `say`ing it (661 R1) and asks for it with no `--set`/`--park` so the set is found from the bytes (667) — **25 rehearsal rows, 0 failed**, with **six mutants of the launcher each turning the rows it should**, and one case on the real tree with real tools and real bytes where the only stub is `sudo` so the flow stops in the wait, before any gate or runner. **The harness's own first draft is the finding worth keeping**: its mutation driver wrote each mutant to a file and ran the harness, which hard-coded the unmutated path — so it printed the same `25 ok, 0 failed` for all six, a green table for a file nobody had changed. The check that caught it was the driver's own *did the row I expected go red*; the fix is that the script under test is a parameter and every run prints its sha256. Host-side only: no `fastboot`, no `adb`, no boot, no byte under `out/` written, `fastboot boot` only and never `flash`, the neighbour `33e80afe` untouched. **Does not advance 「起码要能进入操作系统，把基础驱动跑起来」** — the frontier is where 652 left it; it makes the *next* press's answer a number the tooling computes. **TWRP-to-storage stays withheld.** |
| stage90 | [experiment-684](experiment-684-the-ending-faults.md) | **the press was spent, the run came back — and the ending it was built on faults** — the press 683 unblocked was given and **the device returned with the log**, so the arm's question is answered, but not with the answer it was designed to give. Gate `EXIT=0 / 550 lines`, **exactly one** `run_and_capture.sh`, one `fastboot boot`, **`RUNNER EXIT=0`**; the runner's own measurement is **18 s** and the bytes sent were unchanged across the send (`94c95342…`); capture 612,441 B, sha `64725596…`, archived at `out/stage90/captures/684-endrun-pressed-2026-09-25-last_kmsg.txt`. **The seam published exactly as designed** — `seam_calls=1`, `seam_op=1`, `seam_end_run=1`, entered at the exit's own call site (`lr=0x800462dc`, so not one of the routine's three other callers) — and the pair came back **unchanged** (`b=0x800b2648/0x8047c9c0`, `a=` the same) ⇒ the cell is **`CLEAN LINE`**, whose one reading on this arm is *the line was not dirty and the clean had nothing to write out*: **546 section 3's mechanism was not exercised at all**, and `b1=0x8047c9c0` **is** the real exit's return site for this image, so the frame the exit's `push` wrote was already correct in memory. **Then the first instruction of the deliberate ending faulted**: `panic(cpu 0 caller 0x80454668): kernel abort type 4: fault_type=0x3, fault_addr=0xfa0065c` with `r2=0x0fa00000 r3=0x78665501 sp=0x8054fea8 lr=0x8047cb08 pc=0x8047b488 fsr=0x805 far=0x0fa0065c`, and the disassembly confirms the instruction: `8047b47c <entry_seam_end_run>` = `movw r3,#0x5501` / `mov r2,#0xfa00000` / `movt r3,#0x7866` / **`8047b488: str r3,[r2,#0x65c]`**, reached by `8047cb04: bl` from the seam (return address `0x8047cb08`). `fsr` status 5 is **a section translation fault**, so **the word this project calls `RESTART_REASON` is not mapped in the context the seam runs in and the store could not happen at all.** **THAT FALSIFIES 674 section 1 — measured, not argued.** 674 said both registers are already mapped because the payload installs `0x0fa00000`/`0xfc400000` into `stage90_candidate_l1`, the table `xnu_handoff.c:318` installs into TTBR0; 681 read that off the source, found the Phase-5 line unconditional, checked two other statements of the same fact, and reported **the claim holds**. It does not: the seam runs while XNU runs, and by then the live TTBR0 is not the payload's table — `0x0fa00000` is below XNU's kernel window (`[0x80000000,0xFFFEFFFF]`), so XNU's pmap has no section for it. *A source reading can see which table the payload installs; only a run can see which table is installed by the time the code that matters executes.* **The same argument condemns the second store** (`0xfc4ab000`, in the payload's Phase-4 megabyte) — **implied, not measured**, because the run died at the first. **So 663 section 2 step 4 has the requirement 674 removed**, and the entry image already owns the tool: `entry_mmio_section` (`entry_stubs.c:2230`) installs one section into the live table and is used for exactly this for the GIC (`entry_gic.c:377`). **The run came back because the fallback worked** — the entry image's own abort path took the fault (`xnu_live_sleh_pc=0x8047b488`, `_lr=0x8047cb08`, `_far_frame=0x0fa0065c`, `xnu_entry_panic_entered=1`) and its `entry_epilogue` reset the phone — so this press **did not test `PS_HOLD`** as the frontier's first net; and **the arm 663 designed has still never actually ended a run**. **One reader sentence was right for the wrong reason** (owed): the `DIED IN THE EXIT` block attributes the non-return to 520's `pop {fp, pc}` when on an ending arm a non-return through the seam is **by design** and here the pc is the ending's store. **The frontier is unmoved** (the run never reached the pop; the boot got as far as 520 — 3 user-mode fault records, `storm=9`; the goal block passes but is **the floor**, since the userland phase happens before the death). Three facts no source reading had: the slot was not stale; `RESTART_REASON` is unreachable from the seam by section translation fault; the ending has never run. One press, **nothing flashed, nothing written to storage**, neighbour `33e80afe` off the bus, device back in adb; **TWRP-to-storage stays withheld**. |
| stage90 | [experiment-683](experiment-683-one-name-in-the-gates-key-list.md) | **one name in the gate's key list, and the name row 4 owed** — two instruments stood between 678's parked arm and a press and **both were names, not checks**. (1) **The gate refused the arm over one missing key**: 678's record carries `STAGE90_XNU_SEAM_END_RUN=1` and `ENTRY_ARM_KEYS` (`build_entry.sh:543`) writes it, but the gate's own copy `ENTRY_CFG_KEYS` (`preflight_boot_check.sh:564`) had not — and the converse clause (`:585-588`) refuses any record key it does not print, deliberately ("a switch recorded on the build side and not shown here is a switch the next run would go out with unread"), so **every press of 678's arm would have been refused before the device was touched** and the launcher, which waits on a refusal rather than exiting (672), would have waited forever. Measured **exit 1 / 69 lines without the name, exit 0 / 550 with it**. That file is the **peer lane's**; the change was made here because its owner had been unreachable for two days with the press waiting on exactly this, **under the operator's authorisation**, and it adds one name to a list this gate *prints* — no safety clause moves (manifest, storage tripwire, identity comparison and the exit census are byte-unchanged; census still **5 exit site(s) / codes `1 2 3`**). The two prose pieces counting the variant set as eight are corrected in the same edit; the `9 in, 4 out` measurement above them is **left as the historical record it is**. **The lesson is 675 section 4's**: a key list written down three times goes stale in the copy nobody edits, and its refusal looks exactly like a safety refusal while being a bookkeeping one. (2) **Row 4 named 678's arm as 653's** — it printed *the ACTING arm (653 …)* and then 535's pop-based consequences, when **on 678's arm the pop is never executed**; that is **680's defect one file over**, and readiness had no reader for the ending switch (`grep -c SEAM_END_RUN` = **0**). The entry reading alone cannot fix it (653's arm and 678's are the same sentence — the operation is byte-identical, only the ending is new), so the name has to be the reading **joined to the record**, the rule 667 wrote the row for. The repair appends **once**, after the existing block — when `SEAM_END_RUN=1` it names the arm as 678's ending arm and says the FACTS hold while their CONSEQUENCES do not, what `STALE LINE, WRITTEN OUT` reads on this arm, that `seam_calls>=1` with no pair is *the operation did not return* (680's sentence, said before the press instead of after it), and that **a hang at the pop is not a possible failure of this press**; when the record does not name the switch it says the image predates 678 and its seam RETURNS — *a fact about which image this is, never a zero*. **Verified both directions**: the live arm names the ending; `armed-seam-poc-a43304f2` read as a live tree keeps the ACTING name and gains the "predates" clause and **does not** claim the ending; `armed-selftest-wdog-ef0361a2` is unchanged. **Found and not repaired, named**: `ARM_CFG` is `$LIVE/…` (`:567`), so under `--park`/`--set` row 4 still describes the **live** arm while rows 1–2 describe the park — 679 section 3.2's defect one flag over, pre-existing, and it must not be read as a verdict about the park. **No device was touched in this step**; readiness **5/5 ok exit 0**, gate **exit 0 / 550 lines**, arm/park/record unmoved, **TWRP-to-storage stays withheld**. |
| stage90 | [experiment-682](experiment-682-the-reset-reason-word-is-at-another-address.md) | **the reset-reason word is at a different address than the tree says — `0x0fa00000` is the shared RAM, not the IMEM** — 681 corrected a comment on the strength of a claim, and **the claim was the wrong part**: this step went to the SoC's own bring-up code and to the phone's own device tree and found **both** addresses wrong. The offset is right — `restart.c:48 RESTART_REASON_ADDR 0x65C`, `:371 restart_reason = MSM_IMEM_BASE + RESTART_REASON_ADDR`, `:284-299` the word table (`0x77665500` bootloader / `0x77665502` recovery / `0x77665503` rtc / `0x6f656d00|code` oem- / `0x77665501` default) — **but the base is not**: `MSM_IMEM_BASE` is `IOMEM(0xFA00A000)` (`msm_iomap.h:72`), a **virtual** address that `board-dt.c:82 msm_scan_dt_map_imem` maps onto the DT's `qcom,msm-imem` page (the same header's `MSM_DEBUG_UART_BASE`/`_PHYS` pair proves the convention). **Measured on the phone over adb (`od` of the DT): `soc/qcom,msm-imem@fe805000 reg = 0xFE805000, 0x1000`** ⇒ **the word is `0xFE80565C`** (v1's `0xfc42b000` excluded on the device; either way not `0x0fa0065c`). **And `0x0fa00000` is the shared RAM**: `msm_iomap-8974.h:26 MSM8974_MSM_SHARED_RAM_PHYS 0x0FA00000`, consumed at `io.c:317`, and the phone's DT declares the region `soc/qcom,smem@fa00000` for `0x200000` with `soc/qcom,ipc-spinlock@fa00000` at the same base and size — so **`RESTART_REASON` is SMEM + `0x65c`**. **`entry_reset.h`'s claim fails on two independent grounds**: it is not the IMEM word, and `bootinfo.c` never reads one — `powerup_reason_details` is the **bootloader's** DT property `pureason` (`drivers/of/fdt.c:587` → `setup.c:686` → `bootinfo.c:126`). **And it is refuted by measurement that does not involve this project at all**: at 08:05:11 the phone's own Android took `adb reboot bootloader`, writing `0x77665500` to its real IMEM word, and the next boot reports `0x191` (`pu_reason >> 16 == 0`, `unknown reboot`) — a word we know was written was not in the report. (Second, independent argument: `0x7?66…`'s high half has bit 1 set, so `find_first_bit` would print `reset_reasons[1]` = **`kpanic`** and `is_abnormal_powerup()` true on *every* bootloader reboot; it does not.) **The reset is untouched and measured**: every returning run's reset is `PS_HOLD ← 0` (`MSM8974_MPM2_PSHOLD_PHYS 0xFC4AB000`, `msm_iomap-8974.h:37` — the project's constant confirmed against the SoC's own header for the first time), so **674's "step 4 needs no new mapping" survives intact** and 681's "the line the reset path depends on" was wrong. **The design consequence, named not taken**: the IMEM is the one region that **persists across a reset by design** (that is why the reboot reason lives there) — a breadcrumb aimed at `0xFE80565C` would survive a **hang** and be readable by the next run's payload, which is exactly 663's gap; owed with 676 §6's and 677 §6's items for one build. **Two comment-only edits, verified byte-inert** (rebuild with the arm's own flags: all six artifacts **SAME** against `out/stage90/frozen/armed-seam-endrun-88972ba9/`, `resolve_arm_set.sh` → `armed-seam-endrun-88972ba9 / 94c95342… / 8540160`); `entry_reset.h` and `stage90.h`'s *value* are owed, not changed (the first is inside the gate's closure; the second moves the payload's bytes). **Safety**: the write lands in the live SMEM region and the SMEM header/heap layout is the bootloader's and **not in this tree**, so whether `0x65C` is header or heap is named, not guessed — it has never been observed to matter (every returning run has done it and Android has come up, this reading's own run included), but **the constraint holds by measurement, not by design**. No boot, no flash, nothing to storage, no press armed; **TWRP-to-storage stays withheld**. |
| stage90 | [experiment-681](experiment-681-the-line-carrying-the-reset-reason-word-was-labelled-with-the-other-address.md) | **the line carrying the reset-reason word was labelled with the other address** — 674 named a mapping as load-bearing for the arm the next press sends: `stage90_candidate_l1` already maps `0x0fa00000`, so the entry image's seam reaches `RESTART_REASON` with **no install of its own** and 663 §3's "one more section install" was unnecessary. That claim is what makes 678's arm correct, and it had been read off the doc. **This step reads it off the source; the claim holds** (`xnu_arm_vm_init_full_pmap.c:453`, `map_l1_section_mmio(stage90_candidate_l1, 0x0fa00000u, 0x0fa00000u)`, **unconditional**, landing in the table `xnu_handoff.c:318` installs into TTBR0 — so the byte is reachable from the seam's own context; `MSM_IMEM_BASE_PHYS` `0x0fa00000` + `0x65c` = `RESTART_REASON`, the word `platform_reboot` and `entry_epilogue` both write). **And the line that carries it was the one line in the region whose comment named a different megabyte**: Phase 4's `/* MSM IMEM */` sat on `map_l1_section_mmio(..., 0xfa000000u, ...)`, which has no leading `f` and is not the IMEM — the name sat on the line **above** the one that matters, so an operator looking for where the reset path's word becomes reachable found it on the wrong line and the right line unlabelled — **one value, two definitions** (m699) in its cheapest form, prose only, no instruction. The same fact is stated twice more and both were checked: `mmu.c:5156` maps the base into the TTBR0 round-trip table, `entry_reset.h:33` spells the literal `0x0fa0065c` (the entry image cannot include `stage90.h`, so the pair of definitions is structural — the defect is one of them being *wrong*, not there being two). **What `0xfa000000` is for is not established and the replacement comment says so rather than guessing** (the entry image's GIC probe maps `0xf9000000` live; this VA has no reader in either image). **The edit is provably inert**: rebuilt with the arm's own flags, all six artifacts **SAME** against `out/stage90/frozen/armed-seam-endrun-88972ba9/`, and `resolve_arm_set.sh` still answers `armed-seam-endrun-88972ba9 / 94c95342… / 8540160`. **The mistake this step made, and the park doing its job**: the first rebuild ran `./build.sh` **without** `STAGE90_EXTRA_CFLAGS='-DSTAGE90_XNU_ENTRY=1'` — not a rebuild of this arm but `STAGE90_XNU_ENTRY 0u`, **a different arm** — and `build.sh` writes `out/stage90/` in place, so the live tree silently became that other arm; recovered with `cp -r` from the park, `verify_revert_set.sh` exit 0, record and park unmoved, **no device touched**. **So the payload build IS reproducible** when the arm's own flags are passed, and 408's "does not reproduce" is a property of running `build.sh` without them — reported to the peer lane, because the gate's narration is where it matters. **No device was touched, nothing was flashed, no press is armed**, and **TWRP to storage stays withheld**. |
| stage90 | [experiment-680](experiment-680-the-runner-could-not-read-the-arm-that-ends-the-run.md) | **the runner could not read the arm that ends the run — a key it never looked for, and three sentences about a `pop` that does not run** — 678's arm publishes the seam pair and then ends the run on purpose, and the runner's seam block turns that pair into a cell whose every sentence is about a `pop {fp, pc}` that ran with the word the pair describes. On this arm **the pop never runs** (the seam never returns to the exit that owns it), so the log 678 is waiting to fire would have been read with the wrong arm's rules — a `CLEAN LINE` printed as *"a pop that still died on a stale value"* when nothing died at the pop — the same designed-absence-read-as-a-finding shape 677 was spent on, one step earlier and one file over. `grep -c seam_end_run run_and_capture.sh` was **0** before and after 678: the key the entry image publishes at `entry_trace.c:2442` had no reader, so the arm's own reading (*the pair was published and the machine was then reset on purpose*) was invisible, and the one cell **only this arm can produce** — `seam_calls>=1` with **no pair at all** — printed `CHANGED … a dirty line, holding something this block does not name` from **two absent keys**, where on the arms that return an absent pair and a hang are the same silence. **And that branch has never been read from a real log**: measured across every capture in `out/stage90/captures/`, **no archived log carries `seam_op=0x00000001`** (650's is the 574 park, `op=0`; 653's arm is the one that ended EXIT 2 with no capture) — so this is a path the project has never read through. **The change**: `xnu_live_seam_end_run` is read, printed, and used to select which reading a cell gets — `0x1` = ARM 678, publishes then ends, the pop never runs; `0x0` = the seam returns and the pair is read against the pop's death; absent = UNREAD with **both** causes named. The three cells keep 535's **facts** and replace only the **consequence**, because the consequence is exactly what a pop makes true or false — on this arm `STALE LINE, WRITTEN OUT` reads *the operation put the loop's own datum where the frame's word belongs, the pair was published, and the run then ended where this project told it to*, which is 663 §2's whole purpose stated by the reader rather than left to the operator. **The cell conditions are written ONCE** (one `if/elif` assigning `pair_cell`, two arms `case`-ing off it) because a second copy under the ending arm would be one value with two definitions; and a **fourth cell** `pair_cell=absent` gets its own sentence on the ending arm while the returning arm falls through to today's text **unchanged**, deliberately — every archived capture must read exactly as it did, and the returning arm's own absent-pair sentence is **owed** rather than invented here. The absence clause is reported as 677's tick clause is — a fact about which image ran, **never** as a zero — and its discriminator is a fact about the instrument, measured: `STAGE90_SEAM_LIVE_MAX` is `4u` and `entry_seam_publish(1)` returns `1`, so **the first call always publishes when the live channel is up**, making *a pair with no ending key* the first cause and *`seam_calls>=1` with no pair* the second. **Verified**: all **22 archived captures, old runner vs new, byte for byte — 18 identical**, and the 4 that differ differ by **exactly the seven-line absence clause**, and they are precisely the four logs carrying a seam block (`576-rehearsal-OK`/`-OLDEQ`/`-BADWRAP` and 650's real 574-park capture); the baseline taken by **stashing** the change so it is `HEAD`'s own runner and not a copy. **The four end-run cells exercised, all exit 0** (clean / stale / changed / absent), plus the same four at `0x0` and the keyless variants, where `0x0` prints the returning arm's own 535 sentence. **The live-path battery, old vs new: 20 cells identical cell for cell** (`0 ok, 20 failed` both ways) — its red is pre-existing and is **the same single blocker**, every cell stopping at the gate whose last printed line is the entry record's `STAGE90_XNU_IDLE_NO_SLEEP=0` before the converse clause refuses. **The gate with the peer lane's one line in a `.sh` copy: exit 0 / 550 lines**, census still **5 exit site(s) / codes `1 2 3`** — no new `exit`/`die`, so the exit contract is untouched. `bash -n` clean, no `say` string carries an unescaped backtick (18 escaped ones is the convention — 679's own slip, checked here), and **every hunk is inside `summarise_log`**, a function reached only by `--summarise` or after a capture, so the live run's control flow is not touched. **This does not unblock the press** — still the peer's one line — and **does not advance 「起码要能进入操作系统，把基础驱动跑起来」**; no device, no build, no `fastboot`, nothing written to storage, **no press armed**, and **TWRP to storage stays withheld** |
| stage90 | [experiment-679](experiment-679-readiness-printed-nothing-on-every-arm-since-666.md) | **readiness printed nothing at all on every arm but a self-test one — and it had been that way since 667** — 678 parked its arm and the next thing to do is read the tool the safety gate tells you to run first. It did not print a verdict: `tools/verify_press_ready.sh` **exited 1 after its header and before its first row**, `line 640: conseq: unbound variable`. Not a refusal and not a reading — the one instrument that decides whether a press is worth spending, producing **nothing**, on the arm the next press would send. **Two defects, one rename, in opposite directions**: 667 split one variable so the arm row could carry two consequences — the entry-level prose became `entry_conseq` and a new `conseq` took over the sub-arm (self-test) sentence — and it **updated the reader above and not the one below**, leaving *one variable with no reader and one reader with no variable*. `set -u` makes the second fatal, in the branch taken by every arm whose payload has **neither** self-test switch on; and the row that dies is row 4, so **row 5 — `a press now would be caught`, the one row that decides whether the press is worth spending — is never reached**, while a launcher that waits on readiness (672) waits **forever** instead of being refused. **Measured both ways**: before, **0 verdict rows** / exit 1 / `conseq: unbound variable`; after, **5 rows, 4 ok and 1 FAIL**, no stderr — reproduced by a copy of the fixed file with the one line reverted (`tools/.r679-pre.sh`, same directory so `REPO_ROOT` resolves, deleted afterwards). **And it was invisible in the step that introduced it, for a nameable reason**: 667 measured its own five rows against **666's arm**, a self-test arm where `sub_arm` is non-empty and the `elif` reader runs — **a refactor validated on one arm whose own switch selects exactly the branch that hides the break**; `stage90-build-config.txt` is byte-identical (`6c2b6038...`) across the 653 seam arm, the sleeper and 678's arm, so **every non-self-test arm since 666 has aborted here**, including the one in `out/` when the owed press was spent. **The repair is the reader 646 wrote** (`conseq=$entry_conseq`), and it is also what makes that prose reachable at all — 667 left `entry_conseq` read by nothing, so the entry-level reading rule for all four arms **has not been printed since 667**. **One address in it had already gone stale and the repair removed it**: the `poc` sentence quoted the operation's call site as `entry_seam_flush:0x8047ca64`, a site **inside the function 678 edited**, and it is `0x8047cab4` in 678's image — printing it would have put a false statement about *this* arm into the output of the tool that gates the press; the gate prints the reading that matters (the seam's call count and the eight bytes) and the site address is not the reading. **The other addresses were re-measured rather than assumed** and are correct in 678's image (`0x800458b0` is `mcr 15,0,r0,cr7,cr14,{1}` under its own `cfmdr_loop` symbol; `0x8004632c` is `platform_cache_idle_exit+0x58` and decodes to the four mnemonics 657 measured) — they sit **before** `entry_seam_flush` in `.text`, which is why the one address that moved is the one inside the function that changed, a rule and not a coincidence. **A second flag honoured in one place and not another, said out loud**: validating the fix turned up the same class one flag over — `--live` moves rows 1, 2, 4 and 5 and **cannot move row 3**, because the gate resolves its own tree (`preflight_boot_check.sh:21`) with no seam; measured, `--live out/stage90/frozen/armed-selftest-wdog-ef0361a2` printed rows about the self-test park and a row 3 whose refusal named `out/stage90/xnu_arm_entry-config.txt`. Not repaired by teaching the gate a seam (that would change what row 3 means for every caller, including 646's documented `--live` cells), so it is **said in the reading**, and the clause is empty on the live run so the ordinary reading is unchanged. **Also recorded because it is this file's own class**: the first edit used **unescaped backticks** in a double-quoted string, so bash ran them and the printed sentence came out hollowed (`0x800458b0 is  under its own  symbol`) — the file's convention is `\`` (lines 690/694) and the runner has a structural check for exactly this, which readiness **does not**; that check is **owed** and named rather than invented at the end of a repair step. **Verified**: the repair by before/after; **no regression on the branch the rename was written for** (`--live`/`--park`/`--set` at `armed-selftest-wdog-ef0361a2` still names the HARDWARE-WATCHDOG SELFTEST arm and still prints `THIS PAYLOAD NEVER REACHES THE ENTRY`); `bash -n` clean; no `command not found`/`unbound` in the output. **State**: the arm in `out/` is unmoved, no device touched, nothing flashed, nothing written to storage, **no press armed** — and **the press is now blocked by exactly one thing and it is not in this lane**: the single `STAGE90_XNU_SEAM_END_RUN` name the peer lane must add to `ENTRY_CFG_KEYS`. Everything this lane can check is green (row 5 is ok: `adb` lists `4a2fe00b` as `device`, so no keypress is owed), and **this does not advance 「起码要能进入操作系统，把基础驱动跑起来」**, so **TWRP to storage stays withheld** |
| stage90 | [experiment-678](experiment-678-the-seam-publishes-and-then-ends-the-run.md) | **the seam publishes its pair and then ends the run on purpose — the reading stops depending on a return** — 677 spent the owed press, the device came back, and the payload lived **at most 22 s**: below the watchdog's 28 s bite and far below the self-test's 90 s bounded spin, so the arm's own two candidate times were never reached and its question came back unanswered a second time — not because the machine hung but because **the run ended early**. The consequence is sharper than the missing answer: the pair `entry_seam_flush` publishes is the diagnosis of the `pop {fp, pc}`, every seam arm so far has gone back to the idle exit to publish it, and **that is the step that dies** — the log lives in DRAM and only a return can reach it, so an arm that must return to report *cannot report about a hang*. This step builds the arm that does not return: **step 4 of 663 §2's five-step table**, `STAGE90_XNU_SEAM_END_RUN=1` on top of the operation arm, publishing the pair and then writing `RESTART_REASON (0x0fa0065c) <- 0x78665501`, `dsb sy`, `PSHOLD (0xfc4ab000) <- 0`, `dsb sy`, `for (;;) wfe` — `entry_epilogue`'s own tail, the one reset this project has measured to work, proven by **every returning run it has ever had**. **It is a MODIFIER, not a fourth interception**: refused without `SEAM_POC` and refused with `SEAM_MEASURE`, both as `#error`s (`entry_trace.c:514`/`:517`) and both as `die`s in `build_entry.sh`, and both refusals were falsified by firing them. **And it is a smaller change than 663 specified, because two measurements removed half of it**: 674 §1 showed the payload already maps both megabytes into `stage90_candidate_l1` at handoff (`xnu_arm_vm_init_full_pmap.c:410-416`), so there is **no new literal and no new `entry_mmio_section` install** — the disassembly confirms both stores are `movw`/`movt` pairs into mapped megabytes — and 673 §3 showed no existing switch ends a run at the seam, so the ending is ten lines behind a new switch. Three constants now have **one definition** in new `xnu_arm_boot/entry_reset.h` (the entry source manifest went 25 -> 26 files, `xnu_arm_entry-sources.txt` 2104 -> 2184 B). **The ending is 12 instructions: 2 `str`, 1 `wfe`, 2 `dsb`** — the shape 674 §3 required, asserted by the build's own clause. **And the publish path is intact, checked by the key set rather than by a size**: the gate reads `entry_seam_flush` as **114 instructions / 14 loads** against the 653 arm's **122 / 24**, and that 32-byte *shrink* on an edit whose only in-function addition is a call is the **collapse of two epilogues into one** (the 653 arm's publish path returns, so it needs the callee-saved restore twice; this one never does) — read out of the image, this arm publishes **exactly the 653 arm's twelve keys plus one**, `xnu_live_seam_end_run`, published *with* the pair because on this arm an absent pair and no return means the operation hung the machine between the two reads, which the config record cannot say — [[mi4-an-edit-on-a-prefix-deletes-the-suffix]] is the rule that paragraph exists to obey. **A defect the build's new clause caught**: the first build refused `entry_seam_end_run contains 29 str instruction(s) and not two` because the clause bounded the disassembly with the next **global** symbol and `entry_seam_end_run` is `static`, so the window ran into `entry_str8`; the fix caps it at `+256` and stops at objdump's own label lines. **And a second class had already fired in this project**: the record writer is the one site a name-keyed edit does not visit, so the *second* build exited 0 with the new key **missing from `xnu_arm_entry-config.txt`** (594's defect); the writer now has the `echo` line and an after-the-fact `comm` comparison of the record against `ENTRY_ARM_KEYS` **in both directions**, `layout_fail` on either. **Two payload-source edits ride in this commit that change zero bytes of this arm, and that is measured**: with the three payload sources stashed the rebuild is **byte-identical** (`9fd85093...` / `94c95342...` both ways) — the L2 comment correction (676: `0xf0ffe03b` is 2048 sets x 8 ways x 128 B = 2 MiB, not "4096-set, 4 MB") and a one-second tick in the bounded spin, which is behind `STAGE90_HW_WATCHDOG_SELFTEST` and that switch is 0 here. The 136-byte `stage90.bin` growth against the 653 arm and the moved symbol sizes are **commit 666's `timebase_elapsed_us` repair**, not this step, and that was measured by a controlled compile (`timebase.c` against current vs HEAD `stage90.h` is byte-identical) after the same suspicion about `stage90.h` was checked and ruled out. **Parked, recorded, verified, three copies, 11 files each** (`out/stage90/frozen/armed-seam-endrun-88972ba9/`, `/mnt/data/mi4-ios6-export/`, and the live tree; `verify_revert_set.sh` exit 0 on all three, 6 manifest-member checks agree), and `resolve_arm_set.sh out/stage90` resolves it for the runner's `--expect-arm=`; nine of eleven files differ from `armed-seam-poc-a43304f2` and the two that agree are the payload's own switch dump and the fixture — the token is the **entry** image, per the rule the three earlier sets follow and not the exception 666 broke. **The gate REFUSES this arm at exit 1 / 69 lines on one clause** — `carries key(s) this gate does not print: STAGE90_XNU_SEAM_END_RUN` — which is the cross-lane key-set pair; **and the cost of the peer's half was measured rather than estimated**: a `.sh` copy of `preflight_boot_check.sh` at `stages/stage90/.g678-gate.sh` with **only** that name added to `ENTRY_CFG_KEYS` and nothing else gives **exit 0 / 550 lines**, so the four changes the first message asked for reduce to **one** (the copy deleted before anything else read the tree). Also recorded so it is not read as this step's: `rehearse_revert_set.sh` ends **36 ok / 2 failed** on `the record does not cover: captures`, and that cell is **pre-existing** — `captures` is a *directory* the gate reads a park log out of (`:735`) and criterion A is a raw grep that cannot tell it from a file; the gate source is byte-identical to `HEAD` and re-running the rehearsal with `HEAD`'s own record reproduces the cell word for word. **No device, no build beyond the arm, no `fastboot`, nothing written to storage; no press is armed, because arming one on an ungated arm is what the standing rule forbids.** The next step is 673 §3's last item before a press — the peer's one line, then the gate green on `armed-seam-endrun-88972ba9`, then a fresh arming. **This does not advance 「起码要能进入操作系统，把基础驱动跑起来」** — the OS is not observed entering — so **TWRP to storage stays withheld** |
| stage90 | [experiment-677](experiment-677-the-press-fired-and-the-reset-came-early.md) | **the press fired and the device came back — the net's question is answered by the clock, and the reset landed earlier than the arm's own number.** The owed press was given and the armed launcher fired: readiness `exit=0`, `GATE EXIT=0` (546 lines), **exactly one** `run_and_capture.sh` (**`RUNNER EXIT=0`**, returned+captured), one non-persistent `fastboot boot`, image `ef0361a2…` unchanged across the send. Capture **2062 B / 33 payload lines**, sha `542edb59…`, archived. The log is complete and clean — the net armed (`en_after=1`, bite `0xdffac`, `counter_running=1`, `readback_ok=1`), and **nothing in the payload ended the run**: the last line is "spinning", the deadline path's own two keys (`selftest_deadline_us`, `selftest_elapsed_us`) are absent, `platform_reboot entered=0`, `exception=0`, dead-man not armed, and ramoops says **`No errors detected`**. **The decisive measurement is the clock, and it excludes BOTH candidate times**: host USB on `3-10`/`4a2fe00b` gives the jump at **08:05:19** (dev 77 `18d1:d00d` out) and Android's `2717:0368` at **08:06:03**, and the phone's kernel start is **08:05:41** by `host wall − /proc/uptime` (three samples 08:05:40/41/41) — so the payload lived **≤22 s**, minus the bootloader. The arm's own candidates are **28 s** (countdown) and **90 s** (bounded spin), so **the spin's deadline is excluded by the clock (~4×), not by the missing sentence** — that half is settled — **and the countdown's 28 s is above the measurement by ≥6 s, outside its ~1 s error. So the pre-registration's two cells do not contain this outcome: a THIRD one, "reset, by something, earlier than the net's own interval".** Two explanations, both unmeasured: (a) the countdown fired with a shorter interval, **weakened** by the tree's own driver (`msm_watchdog_v2.c:82` has `WDT_HZ = 32765` on this SoC and `:437`'s `timeout + 3*WDT_HZ` is where this project's 3 s gap came from — a wrong constant would make Android's own timeouts wrong too); (b) something outside the payload reset it inside 22 s, named and not guessed. **The device's own record, decoded out of the tree**: `powerup_reason = "unknown reboot"`, `details = 0x191` → `WARMRST` (bit 8) set, restart-reason field `0x191>>16 = 0`, so the driver's `find_first_bit(0,32)=32 >= RS_REASON_MAX` prints its last line (`bootinfo.c:117`); `is_abnormal_powerup()` = `0x191 & 0xE0000` = **0**, so **no watchdog and no panic reason was recorded** — a warm reset with no software reason, consistent with a hardware countdown and inconsistent with a panic, but **not readable as a watchdog reset**, and confounded by the `adb reboot bootloader` warm reset 8 s earlier. **The instrument this arm lacked is one line**: the payload already has a 19.2 MHz timebase and already prints `selftest_elapsed_us` on the *deadline* path only, so a periodic elapsed print inside the spin would make the device state its own session length and the RAM console survives the warm reset — a **fifth item** for the next build. **674 §2's PS_HOLD-first order looks right, not wrong** (the bite's interval is now in question, PS_HOLD is proven on every returning run), the window has closed and **673 §3's order applies: edit → build → park → record → gate → re-arm**, and the device is up in adb so the next run needs no keypress. **The frontier is unmoved and the storage write stays withheld** (this capture has neither of 605's two clauses — the arm spins before the entry) |
| stage90 | [experiment-676](experiment-676-the-measured-l2-geometry-has-two-decodes.md) | **the measured L2 geometry has two decodes in this tree, and the arm that follows has an in-tree reference.** Host-side, read-only, and it goes for the numbers 658 §2's L2 arm would sweep with. The three that `entry_epilogue` publishes (`entry_stubs.c:4202-4204`) are **identical in all three runs that carry them** (520, 533, 650): `xnu_entry_csselr_before=0x00000002`, `xnu_entry_ccsidr_before=0xf0ffe03b`, `xnu_entry_ccsidr_l1=0xa007e01a`. Decoded by the ARMv7 `CCSIDR` layout: the L1 row is **64 sets / 4 ways / 64 B = 16 KiB** (a coherent L1, which is what fixes where the field boundaries are) and the first row is therefore **2048 sets / 8 ways / 128 B = 2 MiB**. **But three places in this tree say that same value is "4096 sets of 8 ways of 128-byte lines, four megabytes"** - `entry_stubs.c:3566` (in the frozen image), `cache_ops.c:51`, and 195's doc `:159`, which is their source. **544 §2 has it right and names the cause**: 4096 is the **compile-time** L2 geometry (`__ARM_L2CACHE_SIZE_LOG__=21`, `L2_CLINE=6` → 4096 sets of 64 B), so `entry_stubs.c` presents a compile-time number as what *this register* "decodes to" - one value with two definitions, one file apart. Corroborated twice: `cpuid.c:275`'s own comment says **"capri has a 2MB L2 cache"** for exactly this SoC, and `cpuid.c:289`'s `(NumSets+1)*linesz/PAGE_SIZE` = **64** is an integer for 2048 sets and 128 for 4096. **No behaviour changes** - `cache_dcache_geometry` reads the register at run time; the comments are wrong and the code is right. **And 534's claim is now measured, not read**: `CSSELR = 0x2` **is** `CSSELR_L2` (`cpuid_internal.h:44`, and `machine_write_csselr` writes `level|type`), whose writer is `cpuid.c:265` with no L1 write-back before `do_cacheid` ends - so the seam arm's save/restore has a **predicted value**, `0x2`. **The arm's reference is in-tree and was read verbatim**: `external/android_kernel_xiaomi_cancro/arch/arm/mm/cache-v7.S:62-115` - CLIDR (`:64`) and **LoC** as the walk's bound (`:65-67`), skip Ctype<2 (`:73-74`), **CSSELR written per level** (`:78`) then CCSIDR (`:80`), the two field masks **without `+1`** (`:86-90`, so it does *not* over-iterate), `DCCISW` (`:100`), and **switch back to level 0** (`:110-111`). So of 658 §2's three absent cells, **two are already in the log** (CSSELR, and L2 geometry) and only **CLIDR** is missing - measured with the path named: it occurs in `xnu_arm_boot/` once, as a word in a comment (`build_entry.sh:1127`), with `CCSIDR` as the positive control. **And one thing is named open rather than confirmed**: 544's "(over-iterating sets is harmless)" is a claim about ARMv7 `DCCISW`, unmeasured here, and the only sanctioned implementation in the tree is the one that does not over-iterate - so an arm must not inherit it. Both comments are inside the closure (660 §5) and are **recorded, not edited**; they join 674 §3 and 675 §1 as a four-item list for the next build |
| stage90 | [experiment-675](experiment-675-the-next-arms-switch-is-a-cross-lane-pair.md) | **the next arm's switch is a coordinated edit, and half of it is the other lane's file.** 673 said the arm after the press is "~10 lines behind a new switch"; that is right about the C and wrong about the step, because a switch here is **a key in a record** and the record's key set is compared for **equality** against a list in `preflight_boot_check.sh` - **measured: `ENTRY_ARM_KEYS` (build side, 13) + `STAGE90_XNU_ENTRY_SHA256`/`_BYTES` = the record's 15 keys = the gate's `ENTRY_CFG_KEYS` (15), identical as sets**, so one key added on the build side and not on the gate side makes the gate refuse (`carries key(s) this gate does not print`), and the gate is `run-experiment-526`'s lane: **the pair has to land together and this lane cannot close it alone** (594 §b's constraint, one switch later). The roster is **six sites in `build_entry.sh`** (`:418-422` the variable + 0/1 case, `:436-448` the mutuality refusal a third arm joins, `:450-451` `STUB_DEFINES`, `:500-504` `ENTRY_ARM_KEYS`, `:551-565` the record loop's `case`, `:827-836` the `entry_trace.c` compile line), **one in `entry_trace.c`** (`:437-439` the `#error`), and **three in the gate** (`:564-569` `ENTRY_CFG_KEYS`, `:615-634` the variant loop, `:513`/`:574` the two prose counts). **The sharp one is #6, and it has 517 behind it**: `STUB_DEFINES` reaches `entry_stubs.c`/`entry_timebase.c` and **not** `entry_trace.c` (`build_entry.sh:820-826`'s own comment), so a switch whose block lives in `entry_seam_flush` and is passed only to `STUB_DEFINES` produces **the record saying 1, the gate printing 1, every surface naming that arm, and the image being the other one** - for 663 §2's arm, a press spent reproducing the acting arm and a death at the `pop` under a story that says otherwise. Also: the two prose counts (`:513` "nine, then twelve, and is now fifteen"; `:574` "The eight variant keys (…)") both go stale on the same commit and **nothing can fail on them**, and the arm must record *which* net it forced (663 §2) - which is a second key, or one key with two values, the class `build_entry.sh:432` names as the one this project has paid for most often. Read-only throughout: no build, no `out/` byte, no edit to `xnu_arm_boot/**` or to the gate, nothing sent, neighbour untouched |
| stage90 | [experiment-674](experiment-674-step-4-needs-no-new-mapping.md) | **step 4 needs no new mapping at all, and what the owed press therefore buys.** One level down into 663 §2 step 4 (force the reset) and it **corrects a published claim**: 663 §3 bullet 2 says the PS_HOLD half *costs one more section install* - it does not. The payload builds the L1 the handed-off kernel runs under and its Phase-4 device list already carries both registers step 4 needs (`xnu_arm_vm_init_full_pmap.c:410-416`, into `stage90_candidate_l1`, whose own comment calls it *the one a handed-off kernel would run under*): `0xf9000000` (the GIC megabyte, which holds the bite at `0xf9017014`) and `0xfc400000` (PS_HOLD, `0xfc4ab000`). The seam is entered from XNU `cpu_idle`, so both are live before the first XNU instruction; and PS_HOLD writability there is **not an inference** - `entry_epilogue` (`entry_stubs.c:4229`) stores 0 to it from the entry image own code and **every run that has ever returned a log proves that store lands**, through this same section. So `entry_mmio_section` is needed for **neither half**, the bite literal (664: `f9017` occurs 0 times in the entry image in all four encodings) and the two stores are the only new bytes, and 663 §3 *no new mapping* argument extends to the half that was claimed to need one. **What the owed press buys, re-scoped**: step 4 order is PS_HOLD first, and PS_HOLD is on measured ground, so the press measures the **second (PMIC-independent) net only** - a bite-fired result records a redundancy, a bite-did-not result says the bite is unavailable here and **not** that the frontier arm will hang, and only a no-return says anything against PS_HOLD-first (the bounded spin own PS_HOLD path did not land). 663 §3.1 *the reset path is broken here, and §2 would have been a wasted press* is therefore too strong; the press stays owed and the order stays as pre-registered. **And step 4 gains a requirement**: 663 §2 step 5 *do not return to the exit* is intent, not control flow - if the first store does not land, execution falls back into `cpu_idle`, reaches the `pop` and hangs, i.e. becomes the run it was built to avoid - so it must end in `platform_reboot` own shape (`stage90_main.c:1068-1080`: store, `dsb sy`, then `for (;;) wfe`), and record which net it used (574 rule). Read-only: no build, no `out/` byte, no `xnu_arm_boot/**` edit (the closure 673 measured), no gate or runner call, no device addressed, neighbour untouched |
| stage90 | [experiment-673](experiment-673-the-next-arms-edit-is-inside-the-closure.md) | **the arm after the press is one edit away, and that edit is inside the closure the gate recomputes.** Asked now because the answer decides whether the next arm can be built the moment the press's reading lands: is 663 §2's arm (publish the pair, force the reset, never return) a build-only step? **No switch exists for it.** `entry_seam_flush` (`xnu_arm_boot/entry_trace.c:2250`) publishes the pair and **falls off the end at `:2316`**, and the `naked` wrapper's `b` (`:2324-2328`) sends the return back to the call site - no watchdog write, no loop, no reboot call anywhere in the body; the seam's three arms (POC / MEASURE / neither, with `:437`'s `#error` asserting the first two exclusive) **all return to the idle exit**, which is what dies at the `pop`. The payload side has none either: `STAGE90_HW_WATCHDOG_SELFTEST` and `STAGE90_DEADMAN_SELFTEST` end a run by *waiting*, and `platform_reboot()`'s bite sits inside the **failure** path (`#if STAGE90_HW_WATCHDOG == ARMED`), not the seam. So the edit is ~10 lines behind a new switch - small, because the bite `0xf9017014` is inside the megabyte the entry side already maps (658) and needs no new mapping and no payload call, and its verdict is the **time** 669 built the runner to measure. **And the edit is inside `xnu_arm_entry-sources.txt`** - *every regular file in `xnu_arm_boot/`, by content*, recomputed by the gate and **refused when it differs** (`preflight_boot_check.sh:1041/:1063/:1171`, and the manifest is written *by* `build_entry.sh` at the build moment, so it agrees with the image and not the tree). So the ordering is a derivation and not a preference: **the edit cannot be made now** (it would make the gate refuse the owed press, and 660's closure forbids moving the entry sources while a firer is armed), **it need not be made now** (only the arm after the press needs it), and **the press releases the constraint by itself** - one runner, then the launcher exits: **press → window closes → edit, build, park, record, gate → re-arm.** **And the step's own first probe was a false absence**: `grep -rn 'STAGE90_XNU_SEAM' ... xnu_arm_boot/` used a **top-level** path that does not exist (the sources are under `stages/stage90/`), so its silence proved nothing and *"no switch ends the run"* was two sentences from being recorded on it - caught only because the next probe returned `ugrep: warning: xnu_arm_boot/: No such file or directory`. That is **m671's shape in the hour after the rule was written**; the redone grep against the real path printed the switch block, `#error`, publish calls and wrapper, which is the control that makes the absence a reading. Read-only throughout: no build, no `out/` byte, no `xnu_arm_boot/**` edit, no gate or runner call, no device addressed, neighbour untouched |
| stage90 | [experiment-672](experiment-672-the-wait-asked-a-question-of-its-own.md) | **the wait asked a question of its own, and a refusal ended the armed window - the fifth copy of one definition, and the first that is copied from a *state* rather than an arm.** 671 cleaned the record; this asked what ends the window. The launcher's wait loop broke on a predicate of its own (`33e80afe gone from the fastboot list` and `4a2fe00b in one of the two lists`) and only then consulted `tools/verify_press_ready.sh`'s row 5 - the verdict that authorises the fire. **Measured with the REAL tool on the real tree, device lists supplied by a stub (read-only; the tool writes nothing)**: row 5 returns **0** for `fastboot lists 4a2fe00b ALONE` and for `adb ... device`, and **1** for `adb ... unauthorized` ("Accept the 'Allow USB debugging' prompt") and for `fastboot ... plus a THIRD device` (the ambiguity guard would refuse and boot nothing) - while v4's trigger **broke the wait on all four**. So on two states the launcher's answer was "go" and the authority's was "no", and v4's response to a refusal was `exit 1`: **the armed window ended with no press spent, and one of those states is transient** - it clears by the very action the tool's message names. Second defect in the same loop: `GIVING UP: 33e80afe is still in fastboot` was printed without reading the neighbour, so when the phone simply never appeared the operator was told to unplug a phone that was never plugged in (670 §1/m679's family - a record naming a cause it did not measure). **Fix** (`press-on-clear.v5.sh`, `7f23cae5…`): the cheap poll decides only *when to ask*, readiness decides *whether to fire*, **a refusal ends the fire and not the window** (it says so in those words), the verdict is cached on the device lists (row 5 is a function of them and the tree is frozen by the closure, so re-asking an unchanged state cannot change the answer) and re-asked when they change or every 180 polls; the give-up message measures **both** halves; and the start block prints the **launcher's own sha256** (669 §4 one file over) - the swap's log is the first to carry it. **Falsified**: 27-row harness **29 ok / 0 failed** for v5 vs **26 / 3** for v4 (the three `A6` rows), and a new five-device-state harness **10 ok / 0** for v5 vs **8 ok / 0** in its v4 record mode where the two rows that differ are `window=no` on S2/S4 and the false neighbour claim on S5. **And the harness was wrong first**: the launcher appends to its log (`tee -a`), so v4's first run was read with v5's lines still in it and printed `window=yes` for the launcher that has the defect - a row taken across a write, fixed by one `rm -f` per drive. **The swap, host-side, both lists empty**: v3 (pid 3744476) killed **by explicit pid**, v5 armed pid **426955** at 07:21:30 UTC, deadline **13:21:30 UTC**. **A near miss worth the paragraph**: `pkill -f 'rehearse-v3.sh'` matched the shell running it (its own command line carries the pattern), so the shell died mid-script and the armed launcher was never signalled - it survived by signal ordering, not by design; **kill by explicit pid**. Host-side only: no `out/` byte, gate and runner unedited, `xnu_arm_boot/**` untouched, no device addressed, neighbour untouched |
| stage90 | [experiment-671](experiment-671-the-rehearsal-wrote-the-presss-record.md) | **the rehearsal wrote the press's own record — a stub's nine-byte line at the production path, and a pin that a hash could not have made.** Asked in the wait, because the launcher is armed (670 §4): what is at the paths the press will write its record to? `/tmp/r654/gate.log` held **9 bytes**, `gate ran\n`, and `run.log` **11**, `runner ran\n` — the scratch STUBS' lines (`rehearse-v3.sh`'s `mkstate`), at the paths the real gate and runner are redirected to, mtime **06:13:38**, i.e. **42 minutes before the 06:55:41 arming**. The mechanism is that the launcher named **one** of its three log paths by env (`PRESS_LOG`, line 52) and hard-coded the other two at **five** sites (173/175/177, 186/190), while the harness overrode exactly the overridable one — so **all 20 scratch states wrote the press's production record**, and a rehearsal was able to write the thing it is a rehearsal *of*. `>` truncates, so the press is never at risk; **the reader is**: a 9-byte file answers *"did the gate run?"* with **yes**, and a real gate's answer is 39 000 bytes — m679's shape (a record that names the wrong producer) one artifact over. **Fix**: `press-on-clear.v4.sh` (sha256 `0981d8f6…`, staged; v3 `254c69b1…` untouched) names all three by env with the same defaults and **prints all three in the start block**; the diff is 9 code lines, and it caught a defect in itself — a whole-file `str.replace` rewrote the two defaults it had just inserted into `GATE_LOG=${GATE_LOG:-"$GATE_LOG"}`, an unbound self-reference under `set -u`, caught by reading the diff (`bash -n` passed). **The pin, measured rather than argued**: on the first run against v3 the two paths were rewritten with **byte-identical** content — same sha `c44e985d…`/`bafea56c…`, only the ns mtime moved 06:13:38 → 07:07:22 — so **a hash-only pin would have certified the write as "no change"** (m662's second instance), and the signature is ns-mtime **and** sha because the row measures *"was this file written"*, which is the claim. `Z2` (`A1-wrote-its-own-siblings`: `gate ran;runner ran;` in the scratch dir) exists so `Z1` green is not a silence. **Falsification, one row moving**: v3 → **25 ok / 2 failed** (`Z1` red, paths `ABSENT` → created; `Z2` red), v4 → **27 ok / 0 failed**, exit 0, `Z1` green with both `ABSENT` before and after. The two stale files were `rm -f`'d — absence is *"no gate has run"*, a nine-byte line is not — with the evidence kept in `/tmp/g668/evidence-671/`. Host-side only: no `out/` byte, gate and runner unedited, `xnu_arm_boot/**` untouched, both device lists empty throughout |
| stage90 | [experiment-670](experiment-670-the-catcher-cannot-fire-the-owed-arm.md) | **the program that fires the press cannot fire the arm the press is owed for — the fourth copy of one missing definition, and the first that costs the press.** 668 pinned the arm at the runner and 669 made the next press's answer a number; both were about *my* side. This step asked what fires the press. `press-watcher.sh` (`run-experiment-526`'s lane, read and not edited) fires `:287` `./preflight_boot_check.sh --allow-xnu-entry` and `:292` `./run_and_capture.sh --allow-xnu-entry`, and on the arm in `out/` (`armed-selftest-wdog-ef0361a2`) **both were run as written, host-side, on the live tree**: the gate answers `GATE EXIT=1` / `REFUSING: the hardware-watchdog SELFTEST … needs --allow-hw-watchdog-selftest`, which takes `:288`'s `exit 1` — so **the watcher exits and the owed arm can never be fired by it**. That is not a hold: `ambiguous()` and the adb-state arm hold and re-fire, the gate arm exits (651/m658's distinction, in the watcher rather than the relay). The runner line answers `RUNNER EXIT=1` refused **before the gate** for lacking `--expect-arm`, and because it sits after `say "gate green; booting"` at `:291` it is the one ambiguity class that *would* spend the cover. **The family, with the cost of each copy**: `verify_press_ready.sh` (repaired 667 — red on a good tree, quoting the gate's *passing* line), `tools/rehearse_live_path.sh` (repaired 668 — **0 ok / 20 failed**, and uniformly red so it read as stale and stopped being read, which is what let the third survive), the armed scratch launcher (repaired, not installed), and now the catcher. One definition, four copies; the repair is not a new constant but two calls — `tools/gate_flags_for_arm.sh out/stage90` and `tools/resolve_arm_set.sh out/stage90 \| cut -f1` — which expand to `--allow-xnu-entry --allow-hw-watchdog-selftest` and `--expect-arm=armed-selftest-wdog-ef0361a2` for this arm. **A sweep so this is the last one rather than the next**: every file on the host that could fire the gate or runner was classified by whether it *invokes* or merely *mentions* them — the five superseded `press-watcher.v{5,6}*`/`loose`/`parked-ops` copies carry the same two lines and are fired by nothing (named here so a later reader does not patch the wrong one), eight other scripts mention the runner in prose or in a `--summarise` instruction, `apply-gate-repairs.sh:52` is a staging applier and not the press path, and `~/.claude/scheduled_tasks.json` is **absent** (the live job is held by the session's scheduler). So `press-watcher.sh` is the only firer, and the only one still carrying a constant. **What this changes about the press**: nothing about the arm, the gate or the payload — but the blocker list gains its only invisible item, because the three known blockers (the neighbour off the bus, a fresh arming, the operator's press) all presupposed that the arming *would work*. Host-side only: no `fastboot`, no `adb` beyond the list reads readiness has always made, no byte under `out/` written, `fastboot boot` only and never `flash`, the neighbour untouched. **Does not advance 「起码要能进入操作系统」** — it removes the reason the next press could not have happened, which is a prerequisite for the reading and not the reading. **TWRP-to-storage stays withheld.** |
| stage90 | [experiment-665](experiment-665-the-returning-run-spun-in-xnus-reboot-path.md) | **the returning run entered XNU's reboot path, spun in it, and came back anyway - and the frontier's fault was a *prefetch* abort, not a data abort.** Reading the one returning run's own log (`out/stage90/captures/650-owed-run-park-574-2026-09-24-last_kmsg.txt`) against `experiment-664` §2's account. **§1**: the log's `panic(cpu 0 caller 0x80454584): sleh_abort: prefetch abort in kernel mode: fault_addr=0x5006e74` — and `0x05006e74` **is `rtcpre_pop`**, the word the `pop {fp, pc}` loaded into `pc`, so the *fetch* faulted. The entry handler is **`fleh_prefabt`** (`0x8000ae34`, `entry_stubs.c:9298-9318`), not `fleh_dabort`; both end in `entry_epilogue`, so 664's conclusion survives and its handler name is corrected. **§2**: `Attempting system restart...` and `MACH Reboot` have exactly **one** print site each — `kdp_machine_reboot_type` (`osfmk/kern/debug.c:664`) and `halt_all_cpus(TRUE)` (`osfmk/arm/machine_routines.c:349`), whose tail is `while (1)` — and `PEHaltRestart` is the unfilled `.bss` slot 663 §3.2 measured. So the returning run **reached a reboot path that does nothing, entered a one-instruction spin, and the phone came back anyway**: 663 §3.2 confirmed on hardware, and the gate's own inference at `preflight_boot_check.sh:1319` (*"so XNU reset the machine itself"*) is refuted — reported to the peer lane by message, **no edit to that file**. **§3**: exactly two candidates remain for the reset (the entry side's `entry_epilogue` `PSHOLD <- 0`, or the payload's armed watchdog bite) and **the log does not say which**, because the field that names the caller — `xnu_entry_why` — **read `0x00000000`** (NULL), so the report's header printed two bytes of garbage from physical address 0 (its third known-bad outing; `entry_stubs.c:1870-1880` records the other two). The prefetch handler's own keys are **absent** while `xnu_entry_kv_dropped=0x92b3`, and a dropped key reads identically to a handler that never ran. **§5**: this adds a **requirement** to 663 §3.1's rehearsal arm — it must publish a marker that survives, or write through a field that is not `g_why` — because a returning run cannot be *attributed* to the bite from outside. No device action, no build, no closure edit; TWRP-to-storage stays withheld. |
| stage90 | [experiment-646](experiment-646-the-presss-verdict-names-which-arm-it-sends.md) | **the press's verdict names which arm it sends, and the record cannot** — `tools/verify_press_ready.sh` said the press would not be *wasted* (bytes = park = record, gate green, a press caught) and never said **which** of the project's two gate-clean arms `out/` holds, although the reading order of the run depends on it: the sleeper's log carries **no** `xnu_live_seam_*` key at all (640 §2), while 574's park is the only arm that can answer 638 §3's `a1`/`b1` pair and 642's `sleh_pc` join. From `run-experiment-526`'s proposal; one row added, which reads the arm out of the ELF: `tools/check_idle_window_unreachable.py` on the live entry ELF plus `STAGE90_XNU_IDLE_NO_SLEEP` out of the live entry record, and **refuses when the two disagree**. **Why the record cannot answer it, measured on both arms**: the separating switch is an *entry* switch, so `stage90-build-config.txt` — the payload's switch record, and the one the gate prints — is **byte-identical** on both arms (`6c2b6038…`), while `xnu_arm_entry-config.txt` differs (live `6636e2d1…` `IDLE_NO_SLEEP=1`, 574 `5073b0c4…` `=0`); entry bins `696a0f39…` vs `151425c4…`, ELFs `7f80c2cb…` vs `3bc72605…`. **The row reads the sentence and not the exit code**, because both of the extractor's verdicts exit 0 (`check_idle_window_unreachable.py:381`, `:388`) and only its refusals exit 1 — a status-keyed row would be green on either arm. Falsified in five directions, green in two: 574 ELF beside the sleeper record → *"one quantity with two readings, and they disagree"*; 4 KB of urandom as the ELF → *"the reachability check exited 1 … REFUSING: command failed: arm-none-eabi-nm"*; a stub printing an unknown verdict sentence → *"a sentence this row has no reading for … so the arm is not named rather than named wrongly"*; a stub printing no `VERDICT:` line → *"which arm it is cannot be inferred from either direction"*; the record with the switch line removed → *"carries 0 … line(s)"*. Green: `out/` → *the SLEEPLESS arm (594/595) … `IDLE_NO_SLEEP=1` agrees, so this press's log carries NO `xnu_live_seam_*` key*; `--live` at the 574 park → the other arm named, with the consequence that follows from it. **And the fifth row that was refuted before it was written**: a row keyed on the witness (`poll_seq` 3 with `poll_timeout_ms >= 1000`) would be green **whichever arm is sent** — `xnu_live_poll_seq` has one publisher, `entry_note_poll` (`entry_stubs.c:4538`), whose only call site is `entry_trace.c:1303`, *after* both `#if !STAGE90_XNU_IDLE_NO_SLEEP` guards (`:1253-1264`, `:1283-1296`) — so the witness stays the *goal* of this press and is not an arm identity. **A symlink hazard checked here and not found**: the peer's six `xnu_arm_entry.bin` hits under its scratch trees are symlinks into the live tree, which would make row 1's park-vs-live `cmp` vacuous; `find out/stage90 -type l` returns nothing, the park is **11 regular files and 0 symlinks**, so is the export copy, and the one tool in this lane naming the job dir (`check_ambiguity_predicates.sh:50`) hashes nothing inside it. Nothing built, nothing run, no `fastboot`, no `adb`, no `sudo`, nothing written to storage; the arm is unchanged (`60063c47…` / `696a0f39…`, still UNRUN); `tools/rehearse_live_path.sh` is green on the tree as it stands (20 ok / 0 failed live-path states, 4 ok / 0 failed path argument, plus the reading table). The press is still the operator's and still blocked by the neighbour — `fastboot devices`/`adb devices` currently name nothing, which is the state a press is taken *from*, and `33e80afe` returns to the list on its own, so it must be **unplugged**, not merely absent. **`check 4` named two different rows inside the one file, and 647 repaired it** (from `run-experiment-526`'s read-only measurement of this lane): 646 numbered its new row **4** in the body section comments (which is the order the rows RUN and PRINT — `CHECKS[]` is filled live arm, park, gate, arm, device — and matches `70a66f2`'s own message) and **5** in the header list, which `--help` also prints. The header's own sentence *"nothing in checks 1-4 says which one `out/` holds"* is true under the header's numbering and **false** under the body's (4 *is* the arm row), so each list was self-consistent and neither was authoritative. Repaired in the header, the direction reality picks: the two ordinal-bearing sentences became statements (*"none of the other rows says which one `out/` holds"*; *"the arm row ... took the fourth position ... because the rows are ordered by what they read"*), and the file now states the rule as its own: **cite a row by the label the table prints, never by its position.** Comment-only, and the tool proves it — it prints no ordinal anywhere (`row()` keys on the `CHECKS[]` name strings; both summaries are `%d of %d check(s)`), and the two revisions give identical output, **4 ok / 1 FAIL, exit 1**, same five rows. Safe to edit with the catch armed: the catcher fires `preflight_boot_check.sh` (`press-watcher.sh:287`) and `run_and_capture.sh` (`:292`/`:296`) and names neither this tool nor `tools/` at all (measured) — so this is not the same kind of change as the two owed repairs behind 620. **Does not advance 「起码要能进入操作系统，把基础驱动跑起来」**; TWRP withheld. |
| stage90 | [experiment-644](experiment-644-the-arm-the-press-sends-had-no-copy-outside-the-tree.md) | **the arm the press sends had no copy outside `out/`, and the two records a reader would check cannot tell the arms apart** — the press sends one command and the payload build is not byte-reproducible (408), so `out/stage90/` holds the only copy of the armed arm that has ever existed; 636 made that a gate row for *what the tree accepts*, the 574 park rescued the arm that *enters* the window, and **the live arm (`696a0f39…`) had neither** — a `find` of `/tmp`, `/home/lvyufeng` and `/mnt/data` at depth 8 puts its 5519996-byte entry bin at exactly five places, of which **two are inside `out/`** (the live file, and the park itself), two are `/tmp/r594/…`, and one is the job temp dir that is cleaned when the job is deleted, i.e. **no copy outside `out/` and the job dir** → copied out 2026-09-24 01:21 with an ordinary `cp -r` (fresh mtimes; **never `cp -a`**) to `/mnt/data/mi4-ios6-export/armed-sleepless-696a0f39/`, verified the way the record says — `tools/verify_revert_set.sh … --set=armed-sleepless-696a0f39` → **exit 0**, `11 file(s)`, 6 manifest-member checks (**never `sha256sum -c`**: the copied manifest is absolute-pathed and would hash the live tree) — and that directory's new `README.md` carries the file table, the record verbatim, and the differential. **The record's own sentence "the two sets differ in 9 of their 11 files" is now measured rather than quoted**: 9 differ, 2 agree, and the two that agree are the ones it names byte-identical (`stage90-build-config.txt` `6c2b6038…`, `stage90_fixture.macho` `52bc9c35…`). **Two readers a person would reach for cannot tell the arms apart, and one can**: `strings -n 6` gives 13234 unique strings each with `comm` empty in both directions; the payload's own switch record is byte-identical, because the switch that separates the arms is an *entry* switch; `cmp -l` on the entry bins gives **531553 differing bytes in 69717 runs** (first offset 8645, last 5487365); and the entry record differs in exactly two lines, `STAGE90_XNU_ENTRY_SHA256` and `STAGE90_XNU_IDLE_NO_SLEEP`. The reader that can is `tools/check_idle_window_unreachable.py` — `7f80c2cb…` → `VERDICT: the window is UNREACHABLE in this image`, `3bc72605…` → `reachable EXACTLY ONCE`, **both outcomes seen** — and it reads the code rather than the build's own record. **The check that would not have been a check**: the first idea (assert the arm being sent can publish the owed run's witness) is refuted by printing the publisher — `entry_note_poll` (`entry_stubs.c:4538`/`:4542`) is called from `__wrap_poll` at `entry_trace.c:1303`, **outside both** `#if !STAGE90_XNU_IDLE_NO_SLEEP` guards (`:1253`-`:1264` and `:1283`-`:1296`, which hold only 514's repair), so `poll_seq=3` with `poll_timeout_ms >= 1000` is a green row **on either arm** and such a check could not fail in the direction it was written for. What the arms differ in is **which question the press's log can answer** — the witness, or 638 §3's `a1`/`b1` pair table together with 642's `sleh_pc` join — so one press answers the question the arm it sends was built for, and neither arm is broken. The same census found six scratch trees under the job temp dir whose `xnu_arm_entry.bin` is a **symlink to the live file** (`gate3`, `mirror594`, `mirror594b`, `reh574/cases/A`-`F`, `shadow-526`): `sha256sum` follows a symlink, so a hash taken in such a tree describes the target and not the tree — `mi4-stand-in-size-is-not-value`'s third form reached by path. The tool is `mi4-ios6-1a`'s lane, so the fifth-row proposal went as a `SendMessage` and **no tool was edited**. Nothing built, booted, edited, re-armed or run; nothing written under `out/` or `xnu_arm_boot/`; `fastboot boot` only, never `flash` |
| stage90 | [experiment-642](experiment-642-the-pops-pc-is-published-and-never-joined.md) | **the `pop`'s own pc is published in this log and joined to nothing** — clause (5) reads the seam pair and the file's own text says which word the pop takes (`b1`, `:264-267`/`:1984-1986`), while clause (1) reads `xnu_live_sleh_pc`, which the same file also calls the popped word (547 §1, `:1503`); both are locals of one function (`summarise_log()`, opened `:498`) and **no line compares them** (measured: an `awk` over `:1200-2100` for both patterns returns nothing), the two places the comparison is wanted asking for it in prose instead — "corroborate the shape **by eye**" (`:1501`), "**read it against** this death's registers" (`:1527-1528`). **The one join that does exist is in the branch the arm under test cannot take**: `$seam_a1 == "$rtcpre_pop"` lives at `:2033` inside `elif [[ $seam_op == "0x00000001" ]]` (`:2026`, 535's arm), and the frozen 574 park publishes `xnu_live_seam_op=0x00000000` → 572's branch (`:2011`), where `$rtcpre_pop` is read at `:1766` and consumed by nothing — which is also the comparison the header cites at `:264-267` as "the second, independent statement of the same fact", so on the arm the pair was pre-registered for that statement is not made (three files agree on the park's `seam_op=0`: its own record `SEAM_POC=0`/`SEAM_MEASURE=1`, `entry_trace.c:2307`'s unconditional `entry_live_write` inside `#if POC \|\| MEASURE`, and the gate's narration at `574-gate-green.txt:90`). The missing join prints which of three words the pop took (`sleh_pc` against `b1`, `a1`, or the deadline), i.e. **638 §3's question answered on the run instead of inferred** — both of 638's leaves are statements about memory, and the pop runs with `C` back at 1 after the L1 was invalidated, which is 546 §3's mechanism. **Retro-validated on both archived fatal logs**: `520` has `rtcpre_pop=0x04b79075` against fatal `sleh_pc=0x04b79074`, `533` has `0x33f1c1b5` against `0x33f1c1b4`, both `& ~1` ✓ — and **neither log carries a single `xnu_live_seam_*` key** (`grep -c` = 0 each), which is why the join has never been made: no run has carried both sides. Nothing built, booted, edited or run; the runner is another session's lane, so this is a `SendMessage` and a measurement, not a patch. **§8 re-measured every line anchor the same day and corrected five** — each had landed on a nearby, self-consistent line about the same subject, two of them on the branch's own descriptive comment one line below its `if`/`elif` |
| stage90 | [experiment-641](experiment-641-the-arm-that-enters-the-window-is-built.md) | **the arm that enters the window is already built, and it is one switch from the armed one** — 638 pre-registered the seam's `_b1`/`_a1` pair for "an arm that enters the window (`IDLE_NO_SLEEP=0` with `SEAM_MEASURE=1`), **which no build has carried**", and 640 repeated it as "The only seam arm ever built was 535's, which was `SEAM_POC=1`". **Both sentences are wrong about builds.** `/tmp/r594/frozen-payload/` holds exactly that arm, recorded 2026-09-23 and hash-bound (`151425c4…` / `3bc72605…`), and `experiment-594` §1's own word for it is **unrun** — so what no build has carried is a *run*. **The distinction has teeth:** the false absence prescribes `build_entry.sh`, and a build **with the catcher armed silently swaps the armed image** (636), so a reader who follows the sentence literally spends the owed press on nothing. **Discriminated rather than argued:** `tools/check_idle_window_unreachable.py` run on both entry ELFs, both outcomes seen — armed (`7f80c2cb…`) 46 lines, `(D) the clearer: (none)`, `VERDICT: the window is UNREACHABLE in this image`; the parked one 44 lines, `(D) 0x8047be64  in __wrap_poll` (514's repair), `VERDICT: the window is reachable EXACTLY ONCE in this image`; the whole diff is the image path, section (D) and the verdict paragraph. **And the two payloads differ in the entry and nothing else:** in each 6015356-byte `stage90.bin` the entry begins at offset **494100** and ends at **6014096** (`= STAGE90_XNU_ENTRY_BYTES`), leaving a **1260-byte** device-tree-shaped suffix (`/defaults`, `debug`, `/chosen`, …); prefix and suffix are byte-identical between the parks and each entry region hashes to the entry its own record names. **The seam's hook is a tail branch, not a `bl`** — `__wrap_FlushPoU_Dcache` is `mov r0, sp; mov r1, lr; b entry_seam_flush`, and `entry_seam_flush` is the same 115 instructions in both arms — so `SEAM_MEASURE` is not what the two arms differ in; **reachability** is. **Two of my own measurement defects, both caught by a control:** "the entry is the payload's last `ENTRY_BYTES`" is false (total − `ENTRY_BYTES` = prefix **+ suffix**, and the armed park whose answer I already knew is what caught the extractor), and a `bl`-only census of the seam symbol reports **0 sites** while the code is live in both arms. The arm is parked outside version control at `/mnt/data/mi4-ios6-export/arm-574-idle-no-sleep-0/` with a park-relative manifest verified in place (**never** verify a park with `sha256sum -c`: the copied manifest names absolute `out/stage90/` paths and reports four `FAILED` about a *different* directory), and its `xnu_arm_entry-sources.txt` still matches `xnu_arm_boot/` as the tree stands (0 differences over 20 files, measured 2026-09-24, by the gate's own two pipelines) — **and it has now been through the gate too**, run on a scratch copy of the tree (§9): control 626 lines exit 0, the park **536 lines exit 0** with the sources clause reading 20 of 20 against `151425c4…` and the arm block printing 638 §3's pre-registered *"the window … IS ENTERED … its keys are expected PRESENT"* (18 lines, against 113 for the armed arm). The one refusal it meets is the **freshness sweep** naming `macho_fixture.c` — an mtime of a different build session, with the fixture byte-identical on both sides — and the manifest substitution is **per-site and inverts** — `SHA256SUMS.park.txt` in a copy, this directory's own absolute-pathed `SHA256SUMS.txt` in `out/` (which is what the record binds) — while fresh mtimes are needed in both cases. It is not an unrecorded artifact: it **is** `set=frozen-574` in `stages/stage90/revert-set.txt`, and `tools/verify_revert_set.sh <park> --set=frozen-574` returns `VERIFIED: 11 file(s) … 6 manifest-member check(s) agree` (measured 2026-09-24, read-only, exit 0), so a real arming has its own pre-press gate: `tools/verify_press_ready.sh --set frozen-574 --park <park>`. No build, no device, no `fastboot`, nothing written to storage; `60063c47…` is still **UNRUN** and TWRP-to-storage stays withheld |
| stage90 | [experiment-640](experiment-640-the-seams-pair-is-unreachable-on-the-arm-that-is-armed.md) | **the seam's pair is unreachable on the arm that is armed, and the arm's own table does not carry it** — 638 pre-registered the seam's `_b1`/`_a1` pair as *the* decision between 597's two candidate repairs, to be read off the owed run. This step asks the one question 638 never asked — **can the armed image produce that pair at all** — and the answer is no, from the image: the seam's only acting site is `bl __wrap_FlushPoU_Dcache` at `0x800462d8`, whose `+4` is `STAGE90_XNU_SEAM_LR`, inside `platform_cache_idle_exit` (`0x800462d4`, `pop {fp, pc}` at `0x8004633c`); that function has exactly **one** caller in the whole link (`0x8047c96c` in `__wrap_platform_cache_idle_exit`), which has exactly one (`0x8000da3c` in `cpu_idle`) — **behind the `SIGPdisabled` gate 599 section 5 proved never opens on this arm**. Re-derived, not cited: `check_idle_window_unreachable.py` on the armed ELF still exits 0 with `(D) the clearer: (none)` / `the window is UNREACHABLE`. **The four sites, enumerated:** `0x80045d08` in `cache_xcall` and `0x800463bc` in `cache_xcall_handler` take the *other* branch (they publish `xnu_live_seam_other`, and the handler needs an IPI no arm delivers); `0x80046284` (enter) and the acting `0x800462d8` are both inside the window. So the owed log's expected seam reading is **no `xnu_live_seam_*` key** — exactly what the gate's own idle narration says (`:576-580`), and not what 638 section 3's table scores. **The correction:** the arm is right (`IDLE_NO_SLEEP=1`, `SEAM_MEASURE=1`, `SEAM_POC=0` off the record — one arm with two names in this tree, the runner prints `SLEEPER ARM` where 594's title says never-sleeps), the physics stands, and the pair stays **pre-registered for an arm that enters the window** (`IDLE_NO_SLEEP=0` + `SEAM_MEASURE=1`), which no build has carried — 535's seam run was `SEAM_POC=1` and left no log. **(Corrected 2026-09-24 by [experiment-641](experiment-641-the-arm-that-enters-the-window-is-built.md) and by 640 section 9: a build *did* carry exactly that combination on 2026-09-23 — entry `151425c4…`, hash-bound, never run, never through the gate, and parked outside version control; what no build has carried is a **run** of it. It is not to be rebuilt to get one: `build_entry.sh` with the catcher armed silently swaps the armed image and the owed press boots nothing.)** **And two readers state that absence with one cause:** the gate prints both "those keys are an expected ABSENCE on this arm" (`:576-580`) and "for this arm `xnu_live_seam_calls` is present" (`:882-883`) in one run, and the runner's clause (5) preamble calls a keyless log "a log from an image that does not carry the arm" — while its own `arm:` table (`:833-841`) exists to separate exactly these two kinds, and the seam's family is missing from it. Neither file was edited (620: both are read by the armed catch; the gate is `run-experiment-526`'s lane, so the clause is sent to them as 637's was, and they reproduced the finding in their lane and staged a repair on a copy — moving the two claims under `if [[ $V_IDLE_NO_SLEEP -eq 1 ]]` so the blocks cannot disagree again). **And the table's own instrument:** bare `objdump` cannot read this ELF — it prints no disassembly and its grep returns **zero** seam sites, a reading identical to "the seam is not in the image" — while `arm-none-eabi-objdump` reproduces the four sites by address alone; the blindness (this project's first-ranked shape) was found by the peer while re-deriving the table. `stage90-qcdt.img` is still `60063c47…`, still **UNRUN**; TWRP-to-storage stays withheld |
| stage90 | [experiment-638](experiment-638-the-seams-flush-is-a-clean-and-an-invalidate.md) | **the seam's flush is a clean AND an invalidate, and the arm's pair is read through the cache the `pop` reads through** — the owed press sends the sleeper arm, which carries the seam in its **measure** form (`SEAM_MEASURE=1`, `SEAM_POC=0`). **Correction first**: `FlushPoU_Dcache` — the seam's call and the enter path's — is **not a clean**. Apple's own source says so: `caches_asm.s:279` `mcr p15,0,r0,c7,c14,2  // cleanflush dcache line by way/set`, and the frozen ELF carries it at `0x80045878`. So the three neighbours separate by encoding *and* by Apple's comment: `CleanPoU_Dcache` `c7,c10,2` “clean dcache line by way/set” (lines stay **valid**), `FlushPoU_Dcache` `c7,c14,2` “**cleanflush** … by way/set” (clean **and invalidate**), `FlushPoC_DcacheRegion` `c7,c14,1` “**Clean & invalidate** dcache line”. The seam's comment and 597 §4 both inherited “clean” from the **name** rather than the encoding. **Then**: `slot` is the exit's `sp` **after** its `push {fp, lr}`; `b0`/`a0` = `[slot]` = the saved **`fp`**; **`b1`/`a1` = `[slot+4]` = the saved `lr` — the word `pop {fp, pc}` loads as `pc`**. `platform_cache_idle_enter` clears `SCTLR.C` at `0x80046240-44` and the exit sets it at `0x80046320-24`, with the seam's `bl` at `0x800462d8` between them — so **all four of the arm's loads are non-cacheable and the `pop` at `0x8004633c` is the first cacheable load after the re-enable, of the same address** (published as `xnu_live_seam_sctlr`, bit 2 must be 0). That makes the pair and the `pc` **two readings of one word through two different cache states** — `[[mi4-one-value-two-definitions]]` — which is **why an equal pair is not a clearance**, as the arm's own comment already says (“an *equal* pair there is the arm working as designed and not ‘the line was clean’”): an equal pair has two causes it cannot separate (the line was clean **or** the routine is inert with `C` clear). **The pre-registered reading**, available whether or not the arm survives the `pop`: **`a1 ≠ b1`** ⇒ the flush wrote DRAM with `C` clear ⇒ the clean half acted and the seam's own `bl` writes the pre-push line over the pushed `lr` ⇒ **597's candidate (B) works** (restore the two words); **`a1 == b1`** ⇒ the flush did not write DRAM ⇒ the stale line never left the cache, and a restore runs with `C` clear so it **cannot reach a cache line** ⇒ **candidate (B) is provably useless** and only a repair acting with the cache **set**, or one that does not leave the line stale across the disable (the enter wrapper's `strd` at `0x8047c8b4` is the last cacheable writer), can work ⇒ **candidate (A)**. **And a self-check the owed run gives for the first time**: 597 showed the fatal `pc` equals the **spilled `r4`** with bit 0 masked (`0x04b79075 → 0x04b79074`; `0x33f1c1b5 → 0x33f1c1b4`), and no run has had both the pair and a log (535 left none; the arm is UNRUN) — so if it dies at the `pop`, `min(b1, a1)` masked to bit 0 should equal the log's fatal `pc`, and **which one matches says which side of the flush the `pop` read**. Does not predict the branch (pre-registration, not prophesy), does not settle whether a set/way op is defined with `C` clear (a property of this part, left to the run), does not build, run or edit anything — arm still `60063c47…`, still UNRUN; **TWRP stays withheld**. |
| stage90 | [experiment-637](experiment-637-the-gate-refuses-a-missing-key-in-the-entry-record-and-narrates-one-in-the-payload-record.md) | **the gate refuses a missing key in the entry record and narrates one in the payload record** — `preflight_boot_check.sh` reads two *generated* records and treats a missing key in them in **two opposite ways**, a consistency gap inside one file rather than a missing idea. For `out/stage90/xnu_arm_entry-config.txt` it enumerates **15 keys** (`ENTRY_CFG_KEYS`), refuses on any the file lacks (`[[ -n $_v ]] || fail "… has no $_k line … a record without that key would let a run go out with a switch nobody recorded"`) **and** on the converse (`comm -23`, a key the list does not name). For `out/stage90/stage90-build-config.txt` there is **no array, no per-key assertion and no converse**: **13 keys are read** by `value_of() { sed -n "s/^#define $1 //p" }` and **only `MODE` is asserted present** (line 134) — two records read by one gate 200 lines apart with opposite disciplines. **What an unread value becomes is a *value***: five `case` blocks group `""` with `0|0u` (lines 1474, 1490, 1746, 1752, 1759), and driving the `BOOT_ARGS` block **verbatim** with a stub `value_of` shows **`0u` and empty print the same sentence, character for character** — 632’s class (an empty value with two causes printed as one reading) and 615’s, one record over from where 632 repaired it. **And for `FAULT_INJECT` the state is not prose**: the off-branch is the one that *skips* the `--allow-fault-inject` refusal, so an unread key means the gate exits 0 for a build whose record never said what the handoff targets. The extractor is brittle **in both directions**, measured against six spellings: `#define KEY  0u` (value column aligned) returns **`" 0u"`** → the case takes `*)` → **flips an OFF switch into fault-injection mode and refuses without the flag**, while two spaces after `#define` / a tab / no value return empty → **off, indistinguishable from not read**. The same pattern guards the two watchdog constants in `stage90.h` (1322-1323) but **those call `fail` on empty** — and a `stage90.h` reformat refuses earlier at the freshness clause — which is exactly the per-record guard whose absence is this step’s subject. **Latent: all 13 keys are present today and `build.sh` emits one space**, which is stated so the size of the finding is not overstated; it is worth fixing because the triggers are ordinary (a key stops being emitted → a switch narrated unread; the emitter's spacing changes → a mode flipped and a refusal) and **the fix is already in the same file** — the `ENTRY_CFG_KEYS` pattern, both directions. The key that matters most is `STAGE90_XNU_ENTRY`, whose off-branch is the sentence that decides whether a press is worth taking. **Sent to the lane’s owner, not edited**: `preflight_boot_check.sh` is `run-experiment-526`’s lane and a file the armed catcher fires, so a change now would change what the press runs; **they re-derived every figure off the source (13 keys enumerated, not taken from this count), reproduced both directions on their own four synthetic records, and confirmed all 13 keys read correctly in the live record** — and added two extensions measured here: `#define K` with **no value at all** is swallowed by the same emptiness (a syntax error `build.sh` would catch, while the gate prints “off”), and the converse gap is wider than a missing key (a key **present in the record and absent from the gate’s thirteen** is invisible — the half `comm -23` already guards for the entry record). Agreed by both sessions: **the repair does not land before the press**; the intended shape is the `ENTRY_CFG_KEYS` discipline applied to `$CONFIG` plus a whitespace-tolerant extractor (`s/^#define $1[[:space:]]*//p`). Read-only otherwise — no device, no boot, no build; arm still `60063c47…`, UNRUN; `verify_press_ready.sh` still 3 ok / 1 FAIL, the FAIL being the neighbour. **TWRP stays withheld.** |
| stage90 | [experiment-636](experiment-636-the-gate-accepts-a-tree-in-which-the-armed-image-has-been-replaced.md) | **the gate accepts a tree in which the armed image has been replaced** — the press sends one command (`fastboot boot out/stage90/stage90-qcdt.img`) and the payload build is **not byte-reproducible** (408), so `out/stage90/` holds the only copy of the armed arm that ever existed. Four things must be true at the press, and the first was checked by **nothing**: the live bytes are the parked bytes. Measured: a copy of the armed set with **one byte changed in the qcdt** (as a rebuild would leave it) gives **check 1 FAIL** (*“the LIVE file(s) are not the recorded bytes: live 8bcb5df6…, record 60063c47…”*) while **the gate still exits 0** — its four clauses chain image ↔ entry bin ↔ config ↔ entry-source manifest, and a rebuild that rewrites them together is *self-consistent*, so nothing in the chain is anchored to the parked bytes. The two ways the armed window breaks, and who sees them: **`build_entry.sh`/`build.sh` → the live arm becomes a different arm → only this check**; **an edit under `xnu_arm_boot/` → the entry clause compares content, refuses → the gate (a press then boots NOTHING)**, which is why the armed-window rule is *read the tree, do not edit it* (a `git checkout` only moves mtimes, and 533 removed the mtime sweep for exactly that false refusal). Added `tools/verify_press_ready.sh` — four checks, one verdict (**five** since 646, which added the row that names *which* arm `out/` holds; see that row): (1) live vs record by hash **and** live vs park by `cmp` (two independent fields of the same bytes), (2) park vs record via `verify_revert_set.sh` (never `sha256sum -c`), (3) gate exit 0, (4) `fastboot devices`/`adb devices` — refusing on any list that is not `4a2fe00b` alone, on a state the catcher’s `usable()` is false for, and on **a list it could not read**. **On this host now: 3 ok, 1 FAIL** — the FAIL is the true blocker (`33e80afe` alone in fastboot) and nothing else, so **the tree and the arm are press-ready** (live = park = record, 11 files each way). Falsified eleven ways: park doctored / unknown set name / absent live dir / **live bytes replaced** (the four directions) / stub gate exit 1 → *“exit 1 - REFUSING: stub gate”* / `sudo` fails → *“could not be READ … not a statement that the phone is absent”* / `adb unauthorized` → FAIL not ok / `adb device` → ok / fastboot naming the phone with no state column → FAIL (**634’s divergence from the live side**) / phone alone pinned → **4 of 4 ok** / anchor 3 ok 1 FAIL. **Two defects of the tool’s own, recorded**: it first parsed the record **positionally** and `file=` is the *fourth* token, so every member was reported absent with a path ending in `manifest_members=…` (an assumed output shape — repaired by token-by-name); and `--gate PATH` was honoured for the executability test and **ignored in the invocation**, so falsification D reported `exit 127` as if it were the gate’s verdict (repaired by invoking `"$GATE"`). Does not fire anything, does not fix the neighbour, does not edit the live catcher (635’s false header is still owed), does not change the arm — `60063c47…`, still UNRUN. Does not advance the goal; it removes a way for the one press that can advance it to be spent on nothing. |
| stage90 | [experiment-635](experiment-635-the-catchers-header-says-a-press-is-spent-its-loop-says-held.md) | **the live catcher's header says a press is spent, its own loop says the press is held** — the owed press has one unmet precondition, and the operator’s decision (press now, or unplug `33e80afe` first) turns on a question the armed waiter answers two ways **in its own two places**. The blocker, measured 2026-09-23 ~21:52: `fastboot devices` lists **`33e80afe` alone**, `adb devices` nothing, and from its `usb 3-3` enumeration (`18d1:d00d`) against `/proc/uptime` the neighbour has been in **fastboot ~12,931 s = 3 h 35 m continuously** — against a host-log population of thirteen windows, **eleven under 14 s** and the longest 152.9 s (plus one still open at 47 min). A 3 h 35 m dwell is **4.6× the longest ever recorded and still open**, so the rule the guard’s comment is written around (“usually in fastboot for seconds”) is true of the population and false of the state right now, and **waiting for it to clear is not a plan**. The phone is off the bus (`usb 3-10`’s last event ~32,169 s = 8 h 56 m ago), so PRECONDITION 2 is met. The contradiction: the loop’s branch (`elif ambiguous; then … HELD and usable, but NOT firing … The phone is held and nothing has been spent; this watcher fires the moment the list settles`) re-evaluates `ambiguous()` every 30 s poll and fires when the list settles — `_was_amb` is narration only, part of no condition — while the **header**, in v4’s rationale for putting the preconditions first, asserts an operator pressing with the ambiguity live has “**the press is spent** on a run the guard refuses”. Both sentences are about the same file’s own behaviour and only one can be true; the header describes the state v3’s arm-time text left the operator in and was left behind when the hold branch made it false (604 with the comment’s subject being the file it heads; 626’s shape one layer in). The resolution is a **code reading, recorded as one**: the press is **held, not spent**, and the operator-facing arm-time text agrees with the loop, so the false sentence is in the rationale only a source reader sees. **It was not tested, and § 3 is why**: `STAGE` is a hard assignment (not `${STAGE:-…}`), so a copy fires the real gate and runner; and the `sudo` stub is not a net on its own, because the real runner **writes `/tmp/cancro-last_kmsg.txt`** — the exact marker `press-watcher-relay.sh`’s `spent()` reads, so a test run for a comment would make the relay exit 0 and **never arm another catcher**, disarming the automatic catch. The repair (one deleted clause) is deferred: 620 keeps the live catcher unedited until the press is spent. **The advice is independent of the resolution — unplug `33e80afe`, then Vol-Down + Power**: unnecessary-and-harmless under the loop’s reading, required under the header’s. The catcher will not do the unplug’s job for you: its own bound is **6 h** (`seq 1 720` × `sleep 30`) from 19:51:24 → **2026-09-24 01:51:24**, ~4 days with `REARM_MAX=16`. Arm unchanged `60063c47…`, still UNRUN, and the record chain re-read (`STAGE90_XNU_IDLE_NO_SLEEP=1`, entry `696a0f39…`, gate exit 0); newest commit touching `entry_trace.c` is `6153ed4` (594), so the armed bytes carry every repair in the tree, and 597’s two candidates stay unbuilt because a rebuild now would replace the arm the press is for. Host-side only — enumerations, `dmesg`, `/proc/uptime`; no boot, no build; **TWRP stays withheld**. |
| stage90 | [experiment-634](experiment-634-two-files-decide-one-question-about-the-fastboot-list.md) | **two files decide one question about the fastboot list, and nothing checked that they agree** — the owed press runs one command that can leave the phone parked in fastboot having booted nothing, and two files decide whether it may act: `run_and_capture.sh`’s `fastboot_pinned_only()` (the guard: exactly one non-empty line, and it must begin `4a2fe00b<TAB>`) and `press-watcher.sh`’s `ambiguous()` (the catcher’s pre-check and the sentence the operator is shown: no line that fails to begin `4a2fe00b` may hold a non-space). One counts lines, the other counts offending lines, and the catcher’s own narration **states** that they agree (“the runner refuses at its FB_AMBIG_WAIT guard”) — a claim about a **different file**, in a comment, next to no check (604, and 626’s shape) arriving on the press path where the cost is a spent press. Both predicates were extracted from the real files (awk brace-counting — the watcher’s `fastboot_has()` is one line, so a `sed` range swallows the file) and evaluated over ten `fastboot devices` strings with `sudo` stubbed. **They agree on all four shapes this host produces and diverge on six it cannot**, and five of the six are the expensive direction (`usable()` true, `ambiguous()` false): the catcher fires, the runner refuses, and because the commitment line `gate green; booting` is written **before** the run, the relay’s stop condition latches and no successor catcher is armed. Two of the six are 619’s shape one field over — `fastboot_has()` is a **prefix** test, so `4a2fe00bX` alone would be called *this phone*, and a bare serial with no state column passes the runner’s own earlier `grep -q "^$SERIAL"` checks while its guard refuses it. **None of the six is reachable here today**, which is the honest size: a latent divergence in two definitions of one value, not a hole that has been firing. Added `tools/check_ambiguity_predicates.sh` — asserts the whole matrix, with the divergence count **counted, not written down** (the first draft hard-coded it: this document’s own subject, in the tool), and **refuses rather than passes** when it cannot see (unreadable runner/catcher, a function it cannot extract, an extractor that overran, and an **empty stub record**, since a green table from a stub that never answered is not a reading). Falsified five ways plus an anchor: **runner widened past the tab → 2 FAIL, exit 1**; watcher’s `ambiguous()` deleted → refuse; watcher path absent → refuse; **watcher widened → 1 FAIL, exit 1**; a watcher reaching no `sudo` → refuse; **the real files → 10 cells, exit 0** — sensitive on **both** sides, so it is not a test of the runner wearing a comment about the watcher. The press cycle was re-read while this was done, nothing touched: the gate **accepts (exit 0)** and is **inert** — stubbed and unstubbed runs are **byte-identical** (49,345 bytes) with the stub **never called**, so “never touches the device” is measured and not a comment; the relay (pid 4067419) is on **catch 1 of 16** armed 19:51:24 as pid 1344846, whose own bound is 6 h (`seq 1 720` × `sleep 30`) to **2026-09-24 01:51:24**, ~4 days with `REARM_MAX=16`; both running revisions match the sha the log recorded at arming; `/tmp/cancro-last_kmsg.txt` is **absent**, so the press is unspent; and **the blocker is measured**: `fastboot devices` lists **`33e80afe` alone**, so PRECONDITION 1 is unmet and a press now produces no boot. Also found and recorded, deliberately **not** fixed: `tools/check_experiment_index.py` is **red on master** — six unimodality violations at `README.md:504-513`, byte-identical at `f24bc64` and at every one of the last thirty commits touching the file, invoked by nothing, and last recorded green at 517; the fix means permuting historical rows, which the next reader could not tell from a retcon. Does not touch the device, the arm, the payload or any parked byte — `60063c47…` is still the sleeper arm, still UNRUN — and does not advance the goal: the one action is the user’s, **unplug `33e80afe`, then Vol-Down + Power**. |
| stage90 | [experiment-632](experiment-632-the-clause-that-decides-the-witness-printed-a-false-sentence.md) | **the clause that decides the witness printed a false sentence when its own extractor missed** — rung 1b is half the witness the owed press is pre-registered against (`poll_timeout_ms >= ENTRY_PARK_MIN_MS`, "the poll that came back is the park and not a stray ask"), and its threshold is read out of `entry_trace.c` rather than written into the runner twice (correct — a second copy of that constant is this project's most-repeated defect). The pattern required **exactly one space** in each of two gaps, and measured verbatim against six spellings of one line, **four emptied `park_min` on a perfectly readable file**: two spaces after the name (a value column aligned), a tab, two spaces after `#define`, a parenthesised value. So a reformat would have turned rung 1b from PASS/FAIL into UNREAD on the morning of a press, indistinguishably from an absent definition. **And the sentence was false in that case**: `park_min` is empty in two different states and both printed `ENTRY_PARK_MIN_MS could not be read from <path>` — naming a failure of *reading* about a file the operator can open with the define plainly in it (615's class, and the same shape 629 found in the write gate's `--help`). Repaired with `[[:space:]][[:space:]]*` in both gaps (**five of six now read `1000`**; `(1000u)` still refused, correctly) and **two sentences where there was one**, each true of the state that prints it, the second saying in as many words that it is a statement about the definition's FORM and not the file's presence. Both branches still set `verdict_ok=0`, so what changed is what the operator is *told*, not what the clause decides. Falsified with one-hunk mutants, runner asserted identical (`9b2b5ba0…`) before and after: mutant A (extraction forced empty) takes section B to **11 ok / 4 failed** and `REFUSING`, exit 1 — and **the guard is four cells wide, wider than this step predicted**: `predicted`, `no-poll-over` and `slot-cwe-only` lose the arm clause (because `verdict_ok=0`) and `small-timeout` loses the FAIL branch it asserts, which is unreachable when there is no threshold to be below; mutant B (source unreadable) prints the "could not be **READ** at all" sentence. The anchor is **20 / 15 / 4 ok, 0 failed, exit 0**. So the structural check 604 asks for already existed and the tempting `tools/verify_park_min.sh` was not needed. Also verified in the same session: the live arm is `cmp`-identical to its park, both parks verify (11 files each), and `IMAGE` resolves to that file — the bytes the press boots are the recorded bytes. Two pre-press verifications closed the step, one of them wrong first. **(a)** The four archived captures the ladder's premises are recorded over (`520`, `533`, `513` run1/run2) were re-read by the pre-632 runner (`cce24b2`, `8aad5d12…`) and the post-632 runner: **byte-identical output on all four** (exit 0/0, 0 differing lines), with 513's pair still reaching rung 1b and passing it in both — so 632 moves no archived reading. **(b) And the first attempt at (a) measured my own harness**: the copies were made in `mktemp -d`, where `$(dirname $0)/../..` is `/`, so the threshold path became `//stages/…` and `park_min` was empty in all four runs *pre and post alike* — the diff's "8 differing lines" on 513 was wording, and the tell was the same `//stages` path inside it. Redone in-tree and the table above is what came out; this is 629's out-of-tree-mutant defect arriving in a *regression* test, and the same reason the parks are verified with `verify_revert_set.sh` and never `sha256sum -c`. **(c)** The press path dry-runs clean — `--dry-run` with `sudo`/`adb`/`fastboot` stubbed on `PATH` gives exit 0 and a stub record of `sudo dmesg` ×4 and nothing else — **and its limit is worth stating**: the plan line printed is the **adb** arm, because with no device visible mode detection cannot select the fastboot arm, and the fastboot arm is the branch the press takes (628's gap, found a second time). This does not advance the goal — no device, no boot, no `fastboot`, nothing written to storage; `60063c47…` is still the sleeper arm, still UNRUN, and **TWRP stays withheld** |
| stage90 | [experiment-631](experiment-631-the-rule-was-stated-in-three-sections-and-enforced-in-none-of-the-sections-it-named.md) | **the rule was stated in three sections and enforced in none of the sections it named** — all three of the harness's cell kinds let a cell assert an **absence** (`forbid:PHRASE` / `MUST_NOT_SAY`), and an absence is the weakest expectation there is because **a command that failed to run at all also "did not say" the thing**. 626 put the refusal into `reader_state` and its comment named the other sections — "the guard **the path cells below carry** for the same reason" — **and the path cells did not carry it**; `path_state`'s comment made the same claim a third time ("`MUST_NOT_SAY` is only ever asserted together with a positive expectation", a sentence about every caller that nothing checked), and 628 recorded the gap from the other side. So the rule lived in three places, was enforced in one, and the two where it was merely written down each named the *other* as already guarded — 604's class in its most obstructive form, a claim that gives the next reader a reason not to look. **Measured**: `run_state PROBE-ABSENCE-ONLY 0 "" 'forbid:The device did not come back'` printed `ok    PROBE-ABSENCE-ONLY           exit=0` on a green row — a runner that died before its first `say` satisfies it exactly. Repaired by the same refusal in all three, with **one wording and three call sites** rather than three rules that happen to agree (`run_state` and `path_state` each refuse when the absence is present and the positive argument empty, before running anything). **Falsified three times**, each probe the first cell of its own section: `run_state` **exit 1**, `reader_state` **exit 1**, `path_state` **exit 1**, each naming its own guard. And the guard stays honest: **0 of the 39 real cells trips any of the three** (re-derived from the source), and the anchor run is **20 / 15 / 4 ok, 0 failed, exit 0**. One of my own probes measured the harness's *schedule* rather than the guard first — a 30 s `timeout` returned `124` because section A's twenty cells each invoke the peer's gate and cost 30-55 s, so the probe was ~10 minutes from being reached; that `124` is quoted nowhere as a verdict, being `[[mi4-a-status-is-a-verdict-only-if-its-producer-delivered-one]]`'s shape arriving in a falsification harness. An absence is now non-vacuous, not sound: a `forbid:` phrase no runner could print is still satisfied by everything, which 630's hand audit found clean on all ten and nothing enforces. This does not advance the goal — no device, no boot, no `fastboot`, nothing written to storage; `60063c47…` is still the sleeper arm, still UNRUN, and **TWRP stays withheld** |
| stage90 | [experiment-630](experiment-630-every-cell-asserted-a-way-the-ladder-could-fail.md) | **every cell asserted a way the ladder could fail, and none asserted the way it passes** — the sleeper clause's rungs are the reading the owed run is pre-registered against (`poll_seq` past the second with the largest ask at or above the park's threshold = rung 1's PASS and rung 1b's PASS), and the rehearsal asserted only the **failure** direction: `door-max-0x8000` ("stops at or below 0x8000"), `poll-seq-2` ("no poll record past the second"), `small-timeout` (the 40 ms ask) and the UNREAD forms. The two rows that could have caught a PASS changing assert the **arm clause**, which is gated on `verdict_ok` and therefore a **proxy spanning exactly rungs 1 and 1b** — measured with one-comparison mutants: raising `poll_seq_max > 2` to `> 99` and raising the `poll_tmo_max >= park_min` test each suppress the arm clause, while raising `door_max > 32768` and making `[[ -n $park_over ]]` unsatisfiable leaves the clause **and all fifteen reading rows green** (measured cell by cell on the harness's own fixture logs), with **section A 20 ok / 0 failed under the same mutant** — the run-path cells cannot see the ladder either. Rungs 0, 2, 3 and 4 were read by no cell; 613's shape one level up from 628 (there a branch nothing asserted, here a whole *direction* of a ladder's verdict). Repaired by five expectations on the `predicted` row — rungs 0, 1, 1b and 3 PASS plus **rung 2's NOTE**, a branch (`verdict_ok == 1 && park_group == 0`) no row reached — eight expectations where there were three, printed as `(+7 more)`. The counted pair: the repaired harness is **14 ok / 1 failed** on that mutant, the one FAIL being `predicted  did not say: went past the last record every previous boot published` then `REFUSING`, and **20 / 15 / 4 ok, 0 failed, exit 0** on the real `8aad5d12…`. Two of my own measurements were wrong first and are written down: the forbid/partner audit's **first run reported two false gaps because its own glob (`*/*.err`) missed every `err.txt`**, so it searched **35 artifacts instead of 55** and could not see the `die` on stderr — corrected over 70 artifacts on 10 forbids: **zero gaps**, recorded as a negative finding (`[[mi4-measurement-defects]]`'s rule arriving in its own audit); and the static grep that found the five PASS phrases in the harness **zero times** was true about the text and **wrong about the coverage**, because the proxy exists. This does not advance the goal — no device, no boot, no `fastboot`, nothing written to storage; `60063c47…` is still the sleeper arm, still UNRUN, and **TWRP stays withheld** |
| stage90 | [experiment-629](experiment-629-the-gate-that-guards-the-only-irreversible-action-stopped-at-its-first-refusal.md) | **the gate on the only irreversible action stopped at its first refusal** — `preflight_storage_write.sh` is where the "a brick is impossible by construction" property ends, and it was written as a chain of six `refuse` calls, so **the checks after the first failure never ran**: in the state the phone is actually in the failing check is the third (the evidence that the OS stayed up), which means checks 4/5/6 — the image exists and is hashed, the rollback verifies against its manifest, the tool image is *the* image this tree records — **had never been evaluated on this host**, and would first run on the day the OS stays up, holding a booted phone and a spent press. Restructured to six named checks that all run, a printed table, and a refusal that is the **conjunction**; the permit does not weaken (plan and `CLEARED` only when all pass, exit 1 otherwise). Three defects, measured on **one frozen revision** (`88088457…`, 30,191 bytes, asserted unchanged before and after): **(a)** `--help` printed *nothing* on the relative invocation — `cd "$(dirname "$0")"` with `$0` still relative made the read look for `stages/stage90/stages/stage90/…` (615's class, in the file whose first reader's first move is `--help`), and even when readable it stopped at line 20, past both bounds of `sed -n '2,50p' … \| head -20`, dropping the `Usage:` block and the exit contract (613's class) — now `readlink -f "${BASH_SOURCE[0]}"` before the `cd` and a `usage()` bounded by the comment block, **62 identical lines from all three invocation forms**; **(b)** a `skip` counted for nothing in the exit, so a run with zero refusals and one skip printed `all 6 check(s) passed` and exited **0 = a cleared persistent write** — falsified with **one** mutated skip site, the same mutant under the old and the new condition giving **0 vs 1** and the old one printing a sentence its own table carries a `skipped` row in; **(c)** a limit recorded rather than fixed: check 5 is **consistency, not authenticity** — against a faithful copy of the backup, one moved byte refuses and *a manifest re-written to match* passes, 6/6, exit 0, because a self-written record is not a constraint. Per-check falsifications (the real capture **520** fires check 3; an unrecorded tool image and `--boot-image == --image` fire 6; `modem`/`aboot` fire 1; each missing flag fires its own, `--target` alone producing a `not-reached` row), and the gate is **inert**: with `sudo`/`fastboot`/`adb`/`dd`/`sh` stubbed on `PATH`, the positive path prints the plan and `CLEARED` and the recording file is **empty — 0 device commands executed**. Three of my own falsification rows were wrong first (a same-file case that hit the identity branch so its label named a branch it could not reach; two mutants made outside the tree where `../..` is `/`; a copy whose manifest still named the original, so `sha256sum -c` was hashing the live backup — 611's rule, one step before the same property became finding (c)). This does not advance the goal — no device, no boot, no `fastboot`, nothing written to storage; `60063c47…` is still the sleeper arm, still UNRUN, and **TWRP stays withheld**: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-628](experiment-628-the-plan-line-on-the-presss-branch-had-no-cell.md) | **the plan line on the branch the press takes was asserted by nothing** — 620 made the boot step's plan line mode-selected and added a cell for the adb half; the `fastboot)` half — the arm Vol-Down + Power actually lands on, since every live-path state starts in fastboot with adb unlisted — was named only by `boot-call-fails`'s **forbid** of the adb line, and **a forbidding expectation passes on a `case` that prints nothing** (613: to assert a phrase's absence the detector must be seen to see it when present). Measured: with the fastboot arm replaced by `: ;;`, sections A and B stayed **19 ok / 0** and **15 ok / 0**. The new `fastboot-plan-line` row carries no marker (the section default *is* the press's state) and its `forbid:` makes it the converse of `adb-plan-line`; with it the same mutant is **19 ok / 1 failed**, that one cell, while the real runner is **20 / 0 + 15 / 0 + 4 / 0**, exit 0. The same run found two defects in 620's `RUNNER_OVERRIDE`: a **relative** override breaks all four of section C's cells (that section `cd`s on purpose, so the relative path resolves elsewhere — 615 in the harness that exists to catch it), now resolved with `readlink -f` once; and an **unrecognised state marker was silently ignored**, so a typo ran the cell in a different state than its label names while still printing `ok` — m639's shape in the harness's own wiring, now a refusal that names the marker and the cell.
| stage90 | [experiment-627](experiment-627-the-rungs-measured-premises-have-a-record.md) | **the rungs' measured premises were a claim in a comment, on logs no copy can be checked against** — the sleeper clause's ladder prints thresholds measured on four archived captures (rung 0's `door_max > 32768` rests on `0x8000` being the last door record of both baselines; rung 1 on their `poll_seq` stopping at 2 with asks of 5 ms and 40 ms; rung 1b on the park being the ask above `ENTRY_PARK_MIN_MS`), and **nothing could check any of them** — `out/` is gitignored so no capture is in version control, and 513's pair lived only in `/tmp` (604's rule: a claim in a comment is not a check). `stages/stage90/baseline-readings.txt` now records **38 readings over 4 logs** with the runner's own two primitives (`maxhex`/`keyval`) as the derivation kinds, plus pinned `expect_logs=`/`expect_readings=`; `tools/verify_baseline_readings.sh` re-derives every one and refuses on a missing log, a wrong hash, a wrong value, an absent key (empty ≠ zero), or a set smaller than the record claims. **The audit found no error** — every premise is exact, including the strict inequality. The verifier had three defects, all found by running it: an unstripped `kind=` prefix that failed all 38 readings on a correct record, a backslash inside a single-quoted `printf`, and — found by falsifying it **green** — a record with every reading line deleted verified at exit 0, which is why the record now pins its own size (613/612: a check that finds nothing, and a completeness criterion derived from the thing it bounds). Eight falsifications, and the pair that separates "the bytes moved" from "the value moved".
| stage90 | [experiment-626](experiment-626-the-arm-was-decided-by-four-behavioural-keys.md) | **the arm was decided by four keys that only say the window was not reached** — the six-term conjunction over the log's own keys scored four *behavioural* absences (`slot_cwe_`, `sip_seq`, `pce_seq`, `wfi_seq`) as one test with the one *structural* key (`repair_seq`, whose publisher is inside `#if !STAGE90_XNU_IDLE_NO_SLEEP`, `entry_trace.c:1283`), so a log that reached the window was read as the baseline arm and the sleeper arm's reading printed as the other arm's ordinary death — measured on the gate session's own 533-derived fixture: ONE appended `xnu_live_slot_cwe_win=` line turned a rung-1/1b PASS log into a death block (622, `b0c1c02`), and the old conjunction had answered the 0 arm by printing nothing. The decision is now the structural key plus the reach guard (`door_seq`), every state prints a table naming each key's **kind** before the branch, and a behavioural key with no `repair_seq` is a named DISAGREEMENT that keeps the ladder running and withholds the death block. Validated on every file on this host that mentions a live key (183 files, **181 same branch**; the two that differ are this reader's own output saved to a file, not logs) and on the six archived captures. A rider: the rehearsal's fixtures prefixed their live records with `MI4IOS6_STAGE90_XNU loader_`, which the payload's own writer does not (`entry_stubs.c:2419`/`:2499`) — harmless while every pattern is a loose substring, and exactly what would have hidden this step's first draft's stricter form, which would have misread every real capture.
| stage90 | [experiment-620](experiment-620-the-boot-call-status-was-inherited.md) | **the call that spends the press had no guard, and the status it passed through was invisible** — 616 pinned `fastboot boot` to the serial, 618 guarded the list it acts on, 619 guarded the `adb` call a few lines above it, and the one line that boots the payload was left bare. Under `set -euo pipefail` a non-zero status ends the script with **fastboot's own code** — no sentence from the file, and because the gate's census counts literal `exit N` in command position, a code the gate could not see either: 584's defect arriving in the runner, on the call that matters most. **The repair is deliberately not 619's, and that is the finding**: a failed `adb reboot bootloader` means the boot did not happen, but `fastboot boot` **sends the image and then waits for the acknowledge**, so a non-zero status can come from either side of the send and **which side is unmeasured**. Aborting is a *claim* ("nothing was sent"); if it is wrong the abort discards sections 4 and 5 — the only things that produce a reading — and the payload's log then dies at the phone's next power cycle: **the press is spent and the reading lost**. So the status becomes a value (`FB_BOOT_RC`), the wait and capture run either way, and the **non-return verdict is conditioned on it** — a boot call that did not report success cannot support exit 2's claim that the payload ran, so that state is **exit 1** and does **not** say "press power" (it says check `sudo fastboot devices` first: if the phone is listed, the press was not spent). Companion on the same path: the `== boot ==` plan line was unconditional, so the fastboot path — **the press's path, always** — recorded an `adb reboot bootloader` the run never issues. **And the gate's exit-code census could not see `die`**: `grep -oE '^[[:space:]]*exit [0-9]+'` misses it, `run_and_capture.sh:137` defines `die() { …; exit 1; }`, and section 4 has held a `die` since 2026-09-22 — measured, the printed line is **identical before and after the section gains a state** (`3 exit site(s), code(s) 2 3` both ways) while the narration named only the code that owes a press. This step's first spelling (`exit 1`) was refused by the gate exactly as designed; changed to `die` the gate went **green because the census had stopped seeing it**, so the half was reported to the gate's owner rather than left as a passing run — their 621 counts both spellings, reads `die`'s code out of its own definition, and puts 1 in `READ_CODES`. **Cells 33 → 36** (19 + 13 + 4), exit 0: `boot_fail_rc:N` (send side) and `boot_sent_rc:N` (acknowledge side, where the payload **is** running) as two arms of one stub, `FORBID` an array because `boot-call-fails` forbids two sentences at once, and `adb-plan-line` as the presence half of the plan-line pair. Falsified one run each, on **copies** with the live runner verified `e742ba4d…` throughout: bare boot call → 17 ok / **2 failed** (both new cells); `case` reverted → 18 / **1** (*said what this state must NOT say: `sudo adb -s … reboot bootloader`*, `adb-plan-line` green); block deleted → 18 / **1** (*exit 2, promised 1* **and** *said what this state must NOT say: It needs a power press* — the false hang in the operator's own words). **The falsification harness had the defect too, and it is m636 one layer out**: rename-never-in-place protects a process *reading* a file, and nothing protected a file a live process **fires by path** — `press-watcher.sh:292` runs `./run_and_capture.sh`, no lock in either file — so the first batch mutated the live runner three times, and "restored byte-identically afterwards" described the *after* rather than the *during* a press lands in. Fixed structurally: `RUNNER_OVERRIDE` **refuses to resolve to the live runner**, the mutation script refuses any path ending in `/run_and_capture.sh`, and the copy must sit at `stages/stage90/.r620-mutN.sh` because the runner derives `STAGE_DIR`/`REPO_ROOT`/`OUT` from `$0` (verified by the copy's own dry run printing the real `out/stage90/stage90-qcdt.img`). **Does not advance the goal** — no device, no boot, no `fastboot`, nothing written to storage; `60063c47…` is still the arm by two routes, the phone is off the bus, and the press is still the user's. TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-619](experiment-619-adb-listed-the-device-so-it-assumed-it-could-command-it.md) | **adb listed the device, so the runner assumed it could command it** — mode detection asked `sudo adb devices | grep -q "^$SERIAL"`, and `adb devices` prints `SERIAL<TAB>STATE`, so that grep matches field 1 and therefore **every state column**: measured on this host, `device`, `unauthorized`, `offline`, `no permissions`, `sideload`, `recovery` and `bootloader` **all match**. The branch that needs a device it can *command* was entered on a listing that only proved the device is *there*. The cost lands on the press: from `unauthorized` (the host's key not accepted — after a wipe, or a cleared `/data/misc/adb/adb_keys`) the `adb -s $SERIAL reboot bootloader` that follows **fails**, the 30-poll wait for fastboot expires, and the run ends at `die "device did not appear in fastboot"` — **exit 1, sixty seconds later, on a message about fastboot, with nothing sent**, while the device sits there waiting for an RSA prompt. `offline` is the same failure as a **race** (adb reports it for minutes while Android is up and adbd is wedged). **The finding is the peer session's**; I reproduced the predicate independently before changing anything. Fix: an `adb_state` reader with the host-log readers' UNREAD convention, and **four questions in an order with no unreachable branch** — adb usable (`device`/`recovery`) → fastboot lists it → adb lists it in a state adbd cannot be commanded through (**a message naming the state and what to do about it**) → dark device. **fastboot is asked before the bad-state message on purpose**: the usual argument that asking adb first cannot mask a fastboot listing rests on the two modes being exclusive, a fact about USB rather than about this script, and a branch that survives only because of a fact it does not check is this file's recurring shape. **Two adjacent sites deliberately diverge**: the `reboot bootloader` call gains `|| die` (without it a failing `adb` aborts under `set -e` and the operator sees **adb's own status**, for which the contract has no clause), while the **return read keeps the plain `grep`** — the question there is *did it come back*, and a device listed `unauthorized`/`offline` **has** come back, so tightening it would turn a real return into exit 2 and a power press. Two call sites, the same `grep`, opposite correct answers. The stub now prints the **state column** from a marker, because a stub coarser than the reading leaves the new branch untestable (616's `boot *` glob, one file over). Battery **31 → 33 ok / 0 failed** (16 + 13 + 4) on the committed bytes; **two cells for one branch** because the message interpolates `$ADB_STATE`, so `adb-offline` fails a version that hard-codes `unauthorized`. **Falsified**: mode detection back on the serial alone → **14 ok / 2 failed**, exactly the two new cells, both *"did not say `its state is …`"* **and** *"said what this state must NOT say: `device did not appear in fastboot`"* — the original defect reproduced, with the other 31 green. Gate **EXIT=0 / 549 lines / 0 stderr**; `--dry-run` **exit 0 / 0 stderr**; both parks **exit 0 / 11 files**. Reachability is low (the adb key survives a boot) and that is why this is small rather than urgent. **Does not advance the goal** — no device, no boot, no `fastboot`, nothing written to storage; the 12:51:37 Android bring-up ran 14 s and **fell into `05c6:f006` instead of `18d1:4ee7`**, so that press produced no run and no log; last `usb 3-10` event 12:53:09; `60063c47…` still the arm. TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-618](experiment-618-the-guard-refused-a-state-that-clears-itself.md) | **the ambiguity guard refused a state that clears itself in seconds** — 616 added the guard because `fastboot boot` with two devices listed **picks one**, so a second phone could take the boot. This step measured what it refuses: the neighbour `33e80afe…` on `usb 3-3` has **13 fastboot windows** in the host log — 12.9, 1.6, 13.0, 3.3, 3.2, 3.1, **1946**, 3.1, 11.9, 1.9, 9.8, 152.9 s, and one begun 2026-09-23 10:59:41 still open **47 min** later. **Eleven of the thirteen are under 14 s**, so the guard as written refused on its first look a state that would have cleared before a human finished reading the message — and the cost lands on the press the phase is waiting for: with the neighbour parked, Vol-Down + Power produces a refusal and no run. Two edits in the boot step. **(1) The condition is now "exactly one device, and it is `$SERIAL`"**, in one helper that takes the list as an argument and counts it itself (no `FB_COUNT`/`FB_LIST` pair that can drift). At entry it is equivalent to 616's `count != 1` — the presence check above has already established `$SERIAL` is there — and that equivalence is why 616's separate wrong-serial branch was unreachable; it breaks **once a wait is in between**, where the list can lose this phone and keep the stranger and a count test would read `1` and pass. What it buys is a **refusal instead of a raw `fastboot` error**, whose status this file's exit contract has no clause for. **(2) The guard waits before it refuses** — `FB_AMBIG_WAIT=${FB_AMBIG_WAIT:-60}`, the list re-tested by the same test, so the wait cannot turn a refusal into a wrong boot, and nothing has been sent so it cannot cost a press either. The refusal's wording moved off `fastboot lists 2 device(s)` — true of one of the three states the condition now catches — to `did not settle to $SERIAL alone within ${FB_AMBIG_WAIT}s (it lists $N device(s))`, and exit 1's clause names the new producer. **And the stub was made faithful**: it now refuses `boot -s 4a2fe00b` when that serial is not in the last list it printed, because a stub that booted anyway would make `-s` look like a guarantee in a state where it is a no-op. Battery **29 → 31 ok / 0 failed** (14 + 13 + 4); `leave_after:N` drives the wait off the runner's own call count (mode detection, presence check, guard read, then one per poll). Falsified one run each, runner restored byte-identically: wait removed → `neighbour-leaves-fastboot` **exit 1, promised 0** *and* the forbidden `cannot act on a`; condition cut to the count → `fastboot-other-after-wait` **did not say `did not settle to`**, i.e. it exits 1 for the wrong reason. Gate **EXIT=0 / 549 lines / 0 stderr**; `--dry-run` **exit 0 / 0 stderr**; revert battery **38 ok / 0 failed**; both parks **exit 0 / 11 files**. **This does not make the current press work** — the neighbour is the *parked* case, not the transient one (a press now waits 60 s then refuses), so unplugging it is still the recommended path; what changed is the common case. **Does not advance the goal** — no device, no boot, no `fastboot`, nothing written to storage; `60063c47…` is still the arm and the phone has been off the bus since 2026-09-23 01:18:06 (10.5 h). TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-617](experiment-617-the-one-exit-code-reserved-for-a-hang.md) | **the return criterion could not see the phone, and reported it as a hang** — `run_and_capture.sh` decides whether the device came back from one host-log thing, `grep -c "SerialNumber: $SERIAL"`, and 616's own comment calls the port's serial-less occupant a *hazard to that test*. It is that, and it is also **the phone itself**: on `usb 3-10` (every one of the phone's 1530 records) the ring holds **1536** `New USB device found`, **1530** with `SerialNumber: 4a2fe00b` and **6** with **all three descriptors empty** (`idVendor=05c6, idProduct=f006`, `Mfr=0, Product=0, SerialNumber=0`) — and they are **consecutive device numbers on one port four to five seconds apart**, 74 → 75 → 76, alternating with that same device's Android mode where the serial *is* printed. So a return in that mode advanced neither test, fell through every branch, and was reported as **exit 2, "The device did not come back"** — the one code the contract reserves for a hang and the one that sends the operator to press power. Fix: a **second, independent reading** on the port the phone is on, **any id** — `phone_port` **derives** the port from the log's own `SerialNumber: 4a2fe00b` lines (measured `3-10`), `port_enum_count` counts enumerations on it, `port_enum_rollup` prints `05c6:f006=6 18d1:4ee7=508 18d1:d00d=510 2717:0368=512` (sum 1536) — with **identity left to the operator**, because `05c6` is also the neighbour's vendor on `usb 3-3`. **The defect inside the fix**: the port branch printed the refusal, set `ENUM_ADVANCED=port`, and had **no `exit`** — so it fell past the two tests below (neither can fire) into `exit 2`, i.e. **the right message and the wrong code**, found by tracing the flow; and `ENUM_ADVANCED` was **never initialized**, which under `set -euo pipefail` makes `[[ -n $ENUM_ADVANCED ]]` an `unbound variable` error rather than false (measured). The park note also moved out of the serial branch, where it lived only because that branch used to be the only one. **And the two branches' ordering was a claim with no cell behind it**: putting the port branch back first left the battery **green**, because the only state reaching that block advanced the port alone — so `return-after-last-poll` was added (a normal return, both counts, timed into the last `sleep 3` of the window), and reverting the order now turns **it** red while `qdl-return` stays green. Three instrument findings, all the same kind: the stub keyed the return on the **number of `dmesg` reads** (so 617's second baseline swallowed it — a model of the *runner* dressed as a model of the device, now elapsed seconds), `no_serial_ever` emitted an **empty log** (which the runner correctly reads as UNREAD — 609's distinction — so it now emits a readable log that never mentions the phone), and `RETURN_TIMEOUT=3` gave the loop **one poll**, hiding the in-wait paths. Battery **28 → 29 ok / 0 failed** (12 + 13 + 4), with a new `forbid:` marker so a state can assert what the runner must **not** say; falsified in one run each — narrowing the `exit 3` → `qdl-return` **exit 2, promised 3** *and* the forbidden line; swapping the order back → `return-after-last-poll` red, `qdl-return` green; pointing a forbidden phrase at a printed line → that cell red. Gate **EXIT=0 / 549 lines / 0 stderr**; `--dry-run` **exit 0 / 0 stderr**; both parks **exit 0 / 11 files**. **This does not advance the goal** — no device, no boot, no `fastboot`, nothing written to storage; `60063c47…` is still the arm and the phone has been off the bus since 2026-09-23 01:18:06 (9.7 h). TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-616](experiment-616-every-call-pinned-the-serial-except-the-one-that-acts.md) | **every device call pinned the serial except the one that acts** — the one press this phase gets is spent by a single line, `sudo fastboot boot "$IMAGE"`, and it was the only device-touching call in the file with no `-s` (the `adb -s "$SERIAL"` beside it and three `fastboot devices` checks grepping for the serial all pin it). `fastboot` with more than one device listed **does not refuse — it picks one**, so a second phone in fastboot redirects the boot to it and the run waits for a `usb 3-10`/`4a2fe00b` return that cannot come: a **false non-return, on the press the phase exists for**. And the second device is real, measured out of the host log: `usb 3-3` carries a phone-class device (serial `33e80afe…`, 218 × `18d1:d001`, **12 × `18d1:d00d` = fastboot**) while the Mi 4 has 1530 records, every one on 3-10 — separable, but only if the call says which one it means. Fix: read the list once, refuse an ambiguous **count**, and pin the call with `-s "$SERIAL"`. **The count is deliberately the only new branch, and that is the third version of it** — the first also refused "the one device is not `$SERIAL`", a branch that is **unreachable** because the `grep` two lines above already refuses that list, and the rehearsal's own cell is what showed it. The three guards are now a partition; the first two ask whether `$SERIAL` is *present*, and **neither asks whether it is alone**, which is what `fastboot boot` without `-s` silently picks from. Both halves falsified in one run each: removing `-s` → `happy-adb` **exit 64** (the stub refuses a bare boot, which makes the pin structural rather than asserted), removing the guard → `two-devices-in-fastboot` **exit 0, promised 1** — **it booted with two devices in fastboot**, the hazard reproduced rather than described. The rehearsal's third cell was **wrong twice** (asserted against the unreachable branch, then against the boot step's check, when mode detection is earlier still) — the same shape as 614's hand-written count. And the new `say` text's first version used an **unescaped backtick**, which 594's guard caught on the first `--dry-run` before anything else ran. `rehearse_live_path.sh` **26 ok / 0 failed**; gate **EXIT=0 / 549 lines / 0 stderr**; runner `--dry-run` **exit 0 / 0 stderr**; both parks **exit 0**. **This does not advance the goal** — no device, no boot, no `fastboot`, nothing written to storage; `60063c47…` is still the arm and the phone has been off the bus since 2026-09-23 01:18:06. TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-615](experiment-615-the-path-argument-was-not-the-callers.md) | **the path the caller names was resolved against this script's own directory** — the step after the one gated boot is to **read that run's capture**, and `run_and_capture.sh:58` is `cd "$(dirname "$0")"`, which runs *before* the argument loop. So a relative `--summarise` path was joined to `stages/stage90/`, and the tool refused with `no such log`, **naming a file that was sitting right there** (597,641 bytes, readable) — measured three ways: from the repo root, and from the capture's own directory with the bare filename. That is a refusal naming the artifact for a fact about the harness, on the one reader whose whole product is a reading *of* a log, and `no such log:` is indistinguishable from *"the capture step failed"* — after a single gated boot that is the difference between rebuilding and re-reading. Fixed by capturing `INVOKE_PWD` before the `cd` and resolving both path arguments (`--summarise`, and `LOGFILE` from the environment) against it, with the re-basing **narrated** rather than silent and the refusal printing the absolute path it looked at. **A second defect inside the first**: `resolve_path` `printf`ed its result and was called as `$(resolve_path …)` — a command substitution runs in a **subshell**, so the `REBASED` append died and the note printed nothing while the resolution worked; found by running the fix and noticing the missing line, which is the project's own silence rule turned on its author. `rehearse_live_path.sh` gains a fourth section, **4 ok / 0 failed** (24 total), and its falsification is stated rather than implied: **removing the resolution turns cells 1–2 red with the original symptom and leaves cells 3–4 green**, so those two greens are exactly what a broken version also satisfies — the absence assertion (`absolute-path-no-note`) carries its evidence in a sibling, the second place in the tree that does. Gate **EXIT=0 / 549 lines / 0 stderr**; `rehearse_revert_set.sh` **38 ok / 0 failed**; both parks **exit 0**. **This does not advance the goal** — no device, no boot, no `fastboot`, nothing written to storage; `60063c47…` is still the arm and the phone has been off the bus since 2026-09-23 01:18:06. TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-614](experiment-614-the-arm-the-press-sends-had-one-copy.md) | **the arm the press sends had one copy and no record, and the record could only ever refuse** — 611-613 built a record, a verifier and a rehearsal, all pointed at the arm to fall **back** to. This step points them at the arm the next press **sends**: `out/stage90/` holds the sleepless arm (qcdt `60063c47…`, entry `696a0f39…`), built, gated, **never run** — and protected by nothing. `out/` is gitignored, no park existed, and the payload build is **not byte-reproducible** (408), so `out/stage90/` was the **only copy in the world** of the one arm the next device action depends on: losing it costs the press, not just a rebuild. Now **parked** at `out/stage90/frozen/armed-sleepless-696a0f39/` (11 files, hash-compared against the live tree in the same run that measured them) and **recorded** as a second `set=` with `manifest_members=` pinned per 612. The two sets differ in **9** of 11 files — measured by the tool, after the header sentence I wrote first said **8**. **The second gap**: the record could only ever be demonstrated to *refuse*, and a verifier whose only shown behaviour is refusal cannot be told from one that is wrong about every directory. With the armed set recorded the same directory gets **both** verdicts — `live-tree-matches-armed-set` **exit 0**, `live-tree-vs-the-other-set` **exit 1** — the property that **a verdict about a directory that does not name its set is not a verdict about a set**. **And the cost landed on a documented command**: `verify_revert_set.sh /tmp/r594/frozen-payload` now exits **1** with a wall of hash failures that reads as **rot**, so the tool keeps **per-set** counts and prints `note this directory matches frozen-574 exactly, and fails armed-sleepless-696a0f39 … --set=frozen-574`, and the reference's park commands now name their set. Battery **35 → 38 ok / 0 failed**; the note cell is load-bearing (deleting the note makes it the only failure, tool restored byte-identically). Both parks verify **exit 0 / 11 files / 6 manifest checks**; gate **EXIT=0 / 549 lines / 0 stderr**. **This does not advance the goal** — no device, no boot, no `fastboot`, nothing written to storage; `60063c47…` is still the arm and the phone has been off the bus since 2026-09-23 01:18:06. TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-613](experiment-613-which-check-covers-which-direction.md) | **which of the three checks covers which direction, and the help text a header edit truncated** — 612 left the manifest covered twice, by check **5** (its bytes) and by **6a/6b** (its member list), and named neither one's direction. Measured, the partition is not the one the names suggest: **5** and **6a** both read the **record**, so the shrinkage direction — the one 611's record was blind to and 612 exists to fix — is carried **not by either member check but by pinning the manifest as bytes**. **5** catches a manifest FILE doctored either way; **6a** catches the record naming a member it does not carry; **6b** is the only check that reads the **target**, so it is the only one that catches a manifest that has **GROWN**. Evidence is two runs, not the code: a fixture manifest shrunk 10 → 9 members against the unmodified record gives `FAIL SHA256SUMS.txt is 769 bytes, the record says 847` and **nothing else fires** (6a reads the record's own field, which the shrink does not touch; a shrunk manifest has no *extra* names for 6b), while the un-shrunk twin prints `ok every one of the 10 member(s)… is in the set` and `VERIFIED`. The partition is now four cells (**30 → 35 ok / 0 failed**), two of which assert **silence** — the one property a command that does nothing has — so both get their evidence from a single capture behind two guards that were **falsified rather than argued**: replacing the shrink with a `cp` fails `shrunk-fixture` (*"10 members before, 10 after"*) and `shrunk-refused-by-bytes` (*"a silence asserting a silence"*) **while the two silence cells stay green on a run that never happened**, which is exactly why they may not be read alone. The detector was checked from the other side too (a stray in `manifest_members=` does put `not a file= line of set` into the output), so the cells can see the phrase whose absence they assert. **Second defect, same file, this project's oldest shape**: the new table pushed text past line 45 and `--help` was `sed -n '2,45p'` piped through `head -24`, so it silently stopped **at check 5** — dropping the 6a/6b description, the absolute-path trap and the `Usage:` line — **and kept exiting 1**, so nothing went red. The range is now derived (`awk` over the leading comments; **76 lines**, ending on the exit contract) and the cell asserts the **last** line of the header: restoring the old line makes it the battery's only failure while the exit code stays 1 in both versions. Park **11/11 / exit 0**; gate **EXIT=0 / 549 lines / 0 stderr**. **This does not advance the goal** — no device, no boot, no `fastboot`, nothing written to storage; `60063c47…` is still the image the next press sends, and the phone has been off the bus for **7.74 h**. TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-612](experiment-612-the-record-was-two-files-short.md) | **the record was two files short of its own criterion** — 611 stated the criterion in one line (*"the set is the files the gate reads"*) and the peer holding the gate took it literally. The criterion was right; the **derivation** was not. `preflight_boot_check.sh:139` is `( cd "$OUT" && sha256sum -c SHA256SUMS.txt )`, so the gate reads **A** the nine paths it names in its text **and B the members of the manifest it verifies** — and B lives in a file that is *itself a member of the set*, so **no grep of the gate can return it**. B is five names, three already in A and **two not**: `stage90.img` and `stage90_fixture.macho`. **Measured**: a nine-file revert, manifest restored, prints `stage90.img: FAILED` and `rc=1` — **the gate red, on a record whose whole purpose is that a revert cannot leave the gate red** — while the eleven-file revert prints five `OK` / `rc=0`. `stage90.img` is live `28ee2c22…` vs parked `8274b1c4…`; the fixture is `52bc9c35…` in **both** arms, which is why "it happens not to matter today" is the reasoning a record exists to replace. **B is pinned in the record**, not read from `out/stage90/SHA256SUMS.txt` — that is the one file whose contents the next build changes, and the live and parked manifests matching today is a coincidence: new field `manifest_members=`, plus checks **6a** (a member must be a `file=` line — the check that would have refused 611) and **6b** (the target manifest must not name anything outside the set — the direction that catches a growing manifest). **A is not amended**: `ef6aa65` is pushed and wrong, and the record's own comment says so. Rehearsal 25 → **30 cells / 0 failed**, and **two of the new cells were themselves wrong first**: one **passed by doing nothing** (its `bash -c` always `echo`ed exit 0 and the phrase matched text inside that sentence), the other **derived nothing** (6b's basename pattern required a directory, and the fixture manifest is relative → empty list → green). Park **11/11**; gate **EXIT=0 / 549 lines / 0 stderr**. **This does not advance the goal** — no device, no boot, no `fastboot`, nothing written to storage; `60063c47...` is still the image the next press sends. TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-611](experiment-611-the-revert-set-has-a-record-now.md) | **the revert set has a record now, and the one manifest it had verified elsewhere** - the user's hardest condition is not the goal but 「一定要保证不要让设备彻底死机或者变砖」, and the mechanism behind it is that the arm in `out/` can always be put back - the frozen 574 pair, "revert is two `cp`s and no build" since 595. That sentence is a claim about **a directory of files**, and it had **no record**: the hashes live in **22 documents** as prose, and the files are **outside version control** (`out/` is gitignored). Now `stages/stage90/revert-set.txt` (sha256 + bytes + **role** per file, hand-written, `tool-images.txt`'s shape) + `tools/verify_revert_set.sh DIR`. **What makes a set complete is a derivation, not a list**: the set is **the files the gate reads**, and that is **nine** - `SHA256SUMS.txt`, `stage90.bin`, `stage90-build-config.txt`, `stage90.elf`, `stage90-qcdt.img`, `xnu_arm_entry.bin`, `xnu_arm_entry-config.txt`, `xnu_arm_entry.elf`, `xnu_arm_entry-sources.txt` - derived from the gate's own source, and **both spellings are needed** (`$OUT/name` for eight, `out/stage90/name` for one), which is how the record's first draft shipped **eight** lines against a gate that reads nine. **The finding**: every build writes `SHA256SUMS.txt` with **absolute paths**, so `sha256sum -c` *inside a park* hashes the **live tree** - it printed 1 `OK` and 4 `FAILED` while every parked byte was right, a manifest of absolute paths being a record of a **location** and not of a **directory** (595's session was bitten by this once already). The verifier reads its own record, never that manifest, and prints where those paths point as narration. The park's manifest is **not rewritten** - it is a frozen artifact with its own recorded hash (591). `tools/rehearse_revert_set.sh` = **25 cells / 0 failed**, incl. the real record pointed at the **live tree** (must refuse - it holds the sleeper) and a directory whose own manifest passes where the record refuses. **Four defects found, all in the step's own instruments**: a cell comparing a directory with itself, a `sed` anchored `s/^sha256=/` over a **mid-line** field so the "malformed" record was well-formed, a manifest listing itself, and the missing `stage90.elf` - now a structural check deriving the gate's read list. Park **9/9 verified**; gate **EXIT=0 / 549 lines / 0 stderr**. **This does not advance the goal** - no device, no boot, no `fastboot`, nothing written to storage; `60063c47...` is still the image the next press sends. TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-610](experiment-610-the-references-other-gap-measured.md) | **the reference's other open gap, measured** - 608 §6 named it (*"it does not document an EDL path for this phone"*) and 605's gate had named it from the other side: the rollback it prints is a **fastboot** rollback, so it covers everything except the case where fastboot is what broke. EDL is that route, and this step turns the sentence into facts, measured 2026-09-23. **Nine of nine** EDL host tools absent from `PATH` (`qdl`, `edl`, `firehose`, `QSaharaServer`, `fh_loader`, `sahara`, `emmcdl`, `QFIL`); **the backup is not an EDL kit** - partition images, `SHA256SUMS.txt`, two unpacked trees and **no programmer**, with `*.mbn` / `rawprogram*.xml` / `patch*.xml` returning nothing across backup and repo, and EDL needing a signed `prog_emmc_firehose_8974.mbn` plus a `rawprogram*.xml`; **`stages/stage90/firehose/` is not that programmer despite the name** - it is Apple's libdispatch `firehose_buffer.c` ported so `bsd/kern/subr_log.c` links (experiment 255), XNU's log-ring code, so the obvious search finds a directory that looks like an answer; and **this tree does not know the phone's secure-boot state** (`getvar secure` / `unlocked` both returned **empty**), which decides whether a programmer must be signed. So the gap is *"begins by obtaining a `cancro`/MSM8974 programmer and provenance-recording it - `tool-images.txt`'s shape - and cannot be verified without writing to storage"*, left undocumented rather than half-documented. **One fact, one statement**: the emergency section now points at the list instead of restating the claim without the measurement (this project's most-repeated defect class). **This step claims nothing about the gate half of 594/609** - that is the peer's **`8aea551`**, which landed in `preflight_boot_check.sh` while this step was open; §1 measures it independently instead (gate **EXIT=0 / 549 lines / 0 stderr**, `HEAD~1` **537**, `diff` = `77a78,89`, one insertion, nothing removed) and records the peer's correction that `696b45c`'s *"executes no new line"* was a claim about `out/`, not the code. **Three numbers and one citation were written from memory and the tree refuted all four** - the secure-boot sentence cited the very file that records `secure:` as **empty**, and the manifest's path, role and counts were wrong (`out/stage90/xnu_arm_entry-sources.txt` is a **build output**; the gate is its *reader*, not an entry; **20** entries, header on line **5**). Plus the `/tmp` oracle trap a third time (a gate copy in `/tmp` gives **EXIT=1 / 0 lines / 104 B stderr** and reads as "the committed gate fails") and an index-row insert that **deleted 608's row prefix** - found by counting pipes per row, not by reading the diff. **This does not advance the goal** - no device, no boot, no `fastboot`, nothing written to storage; `60063c47...` is still the image the next press sends. TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-608](experiment-608-the-tool-image-has-a-record-and-the-gate-reads-it.md) | **the tool image has a record now, and the gate reads it** - 605 built the persistent-write gate and named its own largest gap ("No TWRP image exists in this tree ... before `--boot-image` means anything"), and until now the gate checked `--boot-image` by **parsing** it - a claim about a *format*, not a *file*, so any Android boot image would have been cleared to boot and the stock recovery was accepted as a mechanics stand-in. **Both halves are closed.** The image is **TWRP 3.7.0_9-0 for `cancro`**, fetched 2026-09-23 through TWRP's own device page (16,240,640 bytes, sha256 `a2f4b9037946ededf9f06f28dff922decebe2e7a0bf1bfff24b6e34327e02443`, md5 `525f8796...`, `ANDROID!` page 2048, kernel 9,066,373 B @ `0x8000`, ramdisk 7,171,052 B @ `0x1000000`, `dt_size=0`, `androidboot.hardware=qcom ... androidboot.bootdevice=msm_sdcc.1`); **two independent channels agree about the one file** - both hashes equal the values TWRP publishes, and the detached PGP signature verifies against key `9570 7D42 307C 9D41 D09B F709 1D85 97D7 891A 43DF` (uid `TeamWin <admin@teamw.in>`, signed 2022-10-11, key and signature committed under `stages/stage90/tool-images/` so re-verification needs no network). **The first fetch succeeded and produced the wrong file**: a direct `curl` of the image URL answers **HTTP 200 with a 6,817-byte interstitial**, and the mirror host it names answers **404 with a 5,800-byte error page** - both written to the `.img` name, which is why the record keys on the hash and not the name. The record is `tool-images.txt` (hash, md5, bytes, name, **role**, source page, fetch date, signer) and the check is `tools/verify_tool_image.sh`, which the gate runs with `--require-role=twrp` before printing a command: **eight refusals shown to fire in the verifier's own battery, plus two more found by building it** (the signature refusal of §4 and the missing-tool guard of §5), including the row that retires 605's stand-in (the stock `recovery.img` is refused by hash) and one case no hash constraint catches - **an image edited *and* re-recorded to match hashes is refused by the signature**, which is the argument for keeping corroboration beside the constraint (a *bad* signature refuses; a *missing* one or a host without `gpg` prints `NOT CHECKED` and verifies the rest). **And a defect of my own, found by doing it**: the first attempt at simulating a host without `gpg` built its `PATH` symlinks from `command -v`, which resolved to a shell function, so the links were self-referential, `grep` was missing, and the script refused with "hashes to ... which is NOT in the record" - **a false statement about the image in place of a true one about the host** (595's class); fixed with a guard that names the tools before any of them speaks, now a demonstrated refusal. Gate re-run **EXIT=0 / 537 lines / 0 stderr**; the gate's cleared path was exercised on a **synthetic** log, which is a test of the gate and not evidence the criterion has been met. **This does not advance the goal** - no device, no boot, no `fastboot`, nothing written to storage; `60063c47...` is still the image the next press sends. TWRP still withheld: 「如果os已经能进去了的话」 unmet |
| stage90 | [experiment-607](experiment-607-the-ceiling-is-non-use-not-unreachability.md) | **the ceiling is non-use, not unreachability - and a pet is one store** - 606 drew the right consequence from the right number and stated **one leg the wrong way round**. The gate's scan measures that no instruction in the entry image materialises an address in `[0xf9010000, 0xf901ffff]`; that is true, and it is **not** the claim "the registers are unreachable" - which 584's own paragraph, immediately above, warns against. The reachability question has a **measured** answer and it is the opposite one: **this image maps the watchdog's registers on every boot.** `entry_gic_probe`'s first act is `entry_mmio_section(0xf9000000, 0xf9000000, ...)` - `entry_gic.c:109` says what that is, "one 1 MB section descriptor into XNU's own L1" - and the watchdog is at `0xf9017000`, inside that megabyte. Both archived captures carry it: `xnu_live_gic_map=0x1` with **`xnu_live_gic_desc=0xf901040e`** (bits[1:0]=`0b10` -> section; bits[31:20]=`0xf90` -> base **`0xf9000000`**, covering `0xf9000000-0xf90fffff`) and `xnu_live_attr=0xc` = `ARM_TTE_BLOCK_ATTRINDX(3)`, the PRRR field that is 0, which `SCTLR.TRE` remaps to **Strongly-ordered** - so the mapping is the *right kind* for MMIO and a store through it lands uncached. `hw_watchdog.c`'s own header says the same from the payload's side ("needs no new mapping"). **What the scan does establish**: a store to `0xf9017004` would need the address materialised, and there are zero in all three encodings with the payload as a positive control - so *nothing stores to the registers* follows, and **"cannot" does not**. The honest sentence is **reachable and unused**, and the ceiling rests on the second. **And that corrects the cost 606 gave for the only alternative**: it is not "a new driver ... a new build" - **no new mapping** (the section is installed on every boot by the probe) and **no new driver** (a pet is one store to `0xf9017004`, `WDT0_RST`, from a wrapper this image already has: `__wrap_thread_quantum_expire`, `__wrap_machine_idle`, `__wrap_thread_block`). What remains true is the cost that matters: **any new entry build replaces the parked arm**, so the owed press would answer a different question - a decision about *when*, not about feasibility, and the user's. **And the pet is self-limiting in exactly the way a real kernel watchdog is**: a pet driven by the tick stops when the tick stops, so a hang that kills interrupts also kills the pet and the net still fires - which is how Android's own `msm_watchdog_v2` works; the design question is therefore **placement, not mechanism**, and a pet on a path that runs while the machine is wedged would defeat the net and turn a recoverable hang into a power press that eats its own log (a subset property checkable at build time). Gate re-run **EXIT=0 / 537 lines / 0 stderr** (531 before; +6 `echo`, no new `exit`/`fail`/`die`). **This does not advance the goal** - no boot, no build, no device, no `fastboot`, nothing written to storage; `60063c47...` is still the image the next press sends. TWRP still withheld |
| stage90 | [experiment-606](experiment-606-the-run-has-a-ceiling-and-it-is-the-net.md) | **the run has a ceiling, and it is the net - 28 s, on a successful run too** - 594's arm exists to answer "does the machine get past the `pop {fp, pc}`"; nobody asked the prior question, "how long can a run of this arm last at all". It is not open-ended: the payload arms the MSM8974 hardware watchdog before the jump (`stage90_main.c:1205`) and **nothing in the image that runs afterwards carries that page**, so the SoC resets the machine on its own clock - true of a run that goes *well* as much as of a hung one. The gate has measured the middle of that argument since 584 (entry image: pool word 0, `movt` 0, `mov` 0 in `[0xf9010000, 0xf901ffff]`, with the payload as positive control - it arms the net, so it must carry the page, and it does, 10 `movt`); what it did not do is draw the consequence, and an operator reading a green gate would reasonably assume a good run simply keeps running. **The ceiling is a number read out of the tree, not prose** (rung 1b's rule): `STAGE90_HW_WATCHDOG_TIMEOUT_S` 25 s + `STAGE90_HW_WATCHDOG_BITE_GAP_S` 3 s = **28 s**, refused if either cannot be read, with 603's 1.1805 s of boot consumed before the park. **And what it looks like is measured**: 513's two captures (2026-09-21, an image with **no repair** - the same regime the sleeper switch restores) end their payload records at `xnu_live_poll_over` with `panic` **0**, `Attempting system restart` **0**, `MACH Reboot` **0**, `pc_sample_watchdog_fired` **0** and `platform_reboot entered` **0** - so **no software path ended either run**, where 533's capture *does* carry XNU's panic restart (`MACH Reboot` 1, `panic` 4, because it died at the `pop`). The shape a good run makes is therefore: `poll_seq=3`, `poll_over`, then the records stop **with no fault text** - "the log ends without a fault" is the ceiling, not a missing reading. **It does NOT make 「起码要能进入操作系统，把基础驱动跑起来」 unreachable**: that condition is about *entering* the OS with a driver answering, and 596 measured exactly that in the archived baselines inside the first seconds - a 28 s ceiling does not stand between this project and it. What it does mean is that **"stays up" is not a state this arm can be in, and no arm can be, until the net is addressed**: either not arm it (removes the only net - and the reference's own record is that a net-less hang costs a power press *and* its log, because a power press reinitialises DRAM) or pet it from XNU (a new driver = a new build = a new arm). Both are the **user's** call and this step takes neither. **The claim is a ceiling, not a proof of the reset path**, and the printed text says so: the watchdog shares its 1 MB section with the GIC and `entry_gic.c:377` maps it, so the GIC route is real and unmeasured. Gate re-run **EXIT=0 / 531 lines / 0 stderr** (518 before; +13 `echo`, no new `exit`/`fail`/`die`). **This does not advance the goal** - no boot, no build, no device, no `fastboot`, nothing written to storage; `60063c47...` is still the image the next press sends. TWRP still withheld |
| stage90 | [experiment-605](experiment-605-the-write-path-exists-now-and-it-refuses.md) | **the write path exists now, and it refuses** - the user's condition has two halves and only the first had any machinery: every device touch in this tree has been `fastboot boot`, which writes nothing to storage (the reason a brick has been impossible by construction), and 「如果os已经能进去了的话，可以twrp写入到存储里了」 had a 311-line *procedure* and no **gate** - a document cannot refuse. New `stages/stage90/preflight_storage_write.sh` refuses by default, runs no `fastboot`, touches no device, and clears exactly one form of the write. **The condition is made checkable**: 「如果os已经能进去了的话」 read by feel is not a precondition, so it is the **runner's own two clauses in one capture** - the goal block's PASS (user mode reached, a driver answering) **and** the arm's clause PASS (stayed up past the park). The second is what makes it a criterion: **the first alone is a floor 520 and 533 also meet** - both reached pid 1, both ran the whole userland fixture, both died at the idle exit's `pop {fp, pc}` - so a gate accepting the floor would clear a persistent write on a boot that died (the runner's own text already says "the run that matters is the one whose log has both"). **And the phrases are checked against the runner before use**: a gate grepping for a sentence is making a claim about what that file prints, and if either phrase is gone from `run_and_capture.sh` the gate refuses rather than clearing a write on a phrase nothing prints. **The four preconditions**: the criterion from a capture; the whole backup manifest verifies AND the target's stock image is present (a rollback whose hashes moved is a promise, not a path); the target is `boot` or `recovery` (the forbidden partition list refused by name); and **TWRP is booted, never flashed** - the tempting reading of the user's sentence is "flash TWRP", and it is not needed: `fastboot boot twrp.img` writes nothing and the target is then written from inside via `adb shell dd`, leaving **exactly one** persistent change instead of two and keeping `fastboot flash` out of the picture for the tool (the only `fastboot flash` in the output is the **rollback**, labelled as a recovery action). **A contradiction the first draft had**: it printed the direct `fastboot flash` form as a fallback and said `CLEARED` anyway - one verdict covering two actions, with "NOT cleared here" four lines above `CLEARED`; the fix requires `--boot-image` and prints no bootloader flash at all. **Nine refusal states, each shown to fire with exit 1**: no flag; `--target=modem` (forbidden list by name); no `--evidence`; evidence at a path with no file; **`--evidence=/tmp/533-A.log` - a real archived capture from a boot that reached pid 1 and ran the fixture, refused on the ceiling, which exercises the difference between the two clauses**; an empty file; no `--image`; `--boot-image` == `--image` ("a gate that clears booting the payload in order to write the payload has cleared the wrong thing"); and a non-boot-image (`tools/parse_android_bootimg.py` refuses it). **The structural check fires too**: one word of the arm clause doctored in the runner made the gate refuse at the phrase check before looking at any evidence, the runner restored from a copy and verified by sha256. The cleared path was exercised end to end (exit 0, printing the payload's `60063c47...` and the rollback's `b2119252...` as the record the reference asks for) **on a synthetic log** - a test of the gate, and not evidence that the criterion has ever been met. **Still missing, stated so it is not assumed**: no TWRP image exists in the tree (the backup's `recovery.img` is the *stock* recovery, verified, and is the rollback target), the gate cannot tell what an image *is*, and no EDL path is documented so the rollback assumes fastboot works. Gate re-run **EXIT=0 / 518 lines / 0 stderr**, census still 3 sites / codes `2 3`. **This does not advance 「起码要能进入操作系统，把基础驱动跑起来」** - no boot, no build, no device, no `fastboot`, nothing written to storage; `60063c47...` is still the image the next press sends. TWRP still withheld |
| stage90 | [experiment-604](experiment-604-the-goals-fail-named-three-faults-and-the-state-is-a-position.md) | **the goal's FAIL named three faults, and the state it will actually meet is a fourth** - 596 added the block the user's own condition asks for (`user mode reached, and a driver answering`), outside the arm branch so it runs on every log with payload output. Its `FAIL` named **three** causes - the driver's open answered non-zero, the control answered 0 too, the read's word did not change - and all three are *wrong values*. But the fixture calls in a fixed order (open(driver), open(control), read, getpid, exit, wait), so a log whose records simply **stop** is a boot that stopped there: a **position**, which on an arm whose whole purpose is to find where the boot stops is the reading the run exists to produce. **Found by building the state, not by reading the branch**: a log ending right after the driver's open printed "the driver's open did not answer 0" one line under `open 1 call(s), error in call order: 0x00000000` and two lines above `read 0 call(s)` - **all three named faults contradicted by the block's own table**, and the operator's next action is decided by the sentence and not by the table. The realistic shape is worse than the synthetic one: a boot dying *between* the two opens has `open 1 call(s)` with read/getpid/exit/wait all absent, so the block reports a driver fault while the numbers say the driver answered **0**, the one thing 504's reading requires. **The fix asks the sequence question first and the values question only if it is complete**: `g_stop` names the first call in the fixture's own order whose record is absent, built from `-z` tests and counts only - **no arithmetic on a value that may not be a number** (the neighbouring `g_pair`/`g_driver` use `16#${g_open1#0x}`, and a base conversion on a non-numeric word is stderr noise under `set -e`, the class 601 spent a step on), so the presence side is safe by construction rather than by the log being well-formed. With a stop named the `FAIL` says it is a **POSITION and not a driver fault**; with every call present the three faults are printed **with the values that decided them** (`the driver's open answered 0x00000005 (must be 0)`), so sentence and table can be read against each other. **Verified by comparison**: the pre-step runner staged at the *same depth* (a copy one directory up is the trap 598 §5 / 600 §7 / 601 §4 all paid for, deleted before the gate ran) is **byte-identical on 11 of 14 states** - 533-A /-D/-J, 513 run 1/2, 516-run2/3, 520's two logs, the sleeper `predicted` state and the falsifier state - and the 3 that differ differ **only inside the FAIL branch**, all still exit 0 with 0 bytes of stderr and the `UNREAD` branch unchanged. **The rehearsal gained the two sides of the split**: `mk_sleeper_log` now varies the fixture, with `goal-truncated` (stops after the first call) and `goal-bad-values` (every call present, driver's open `0x00000005`), each a row with **two** expectations; **shown to fire** by doctoring one word (`driver fault` -> `driver error`), which failed exactly that state, named it, and quoted the line it did not say with **no `exit N, promised N` part** - so the text check caught it - the runner restored from a copy and verified by **sha256**, not by `git checkout` which would have reverted this step's own edit. Pass **7 ok / 0 failed** and **9 ok / 0 failed**, exit 0; gate **EXIT=0 / 518 lines / 0 stderr**, census still **3 sites / codes `2 3`** (no new `exit`/`die`). **This does not advance the goal** - no boot, no build, no device, no `fastboot`, nothing written to storage; `60063c47...` is still the image the next press sends. TWRP still withheld |
| stage90 | [experiment-603](experiment-603-the-parks-fail-had-two-causes-and-the-soc-reset-is-excluded.md) | **the park's FAIL had two causes, and the SoC's own reset is excluded by measurement** - 594 pre-registered the witness (`xnu_live_poll_seq=3` with `poll_timeout_ms >= 1000`) and its **falsifier** (`poll_seq` still 2), and a falsifier is worth pre-registering only if reading it tells you something. This one has **two causes**: the arm's failure mode (the park's poll does not return because the idle spin never progresses) and **the SoC being reset out from under the park** (the payload arms the MSM8974 hardware watchdog at `stage90_main.c:1205` before the jump and nothing in the image pets it). Only the first is about the arm, and the second would make a `FAIL` a statement about the watchdog wearing the arm's name. **The instrument is one clock and it is checked, not assumed**: `timebase_cntfrq=0x0124f800` = 19,200,000, calibrated twice in 533's own capture (`xnu_live_tmr_dl_delta=0x024a3a28` is a **2000 ms** deadline and reads 2.00099 s; the 5 ms and 40 ms asks read 6.88 ms and 45.57 ms), and the fact that the payload's records and XNU's share it is **measured by monotonicity in line order across the payload/XNU boundary** - 156: `0x035499b4` (2.9102 s), 279: `0x0355ded6` (2.9145 s), 4689: `0x049827e7`, 8093: `0x04a205dd`, 8319: `xnu_live_repair_before` `0x04afba8b` (4.0951 s), 8356: `xnu_live_wfi_after` `0x04b79097` (4.1218 s) - strictly increasing over 8,400 lines with no wrap, so the log is in write order and the two writers are on one clock. (The check's first run flagged 3 "inversions" and all 3 were `poll_ticks`/`wait_ticks` - keys I had put in knowing they are *durations*: a check that flags the wrong thing is a check nobody reads, so the predicate was fixed and not the log.) **The reset's interval is stated twice in the log and the two agree exactly**: `bark_ticks_written` `0x000c7fb5` / `hw_watchdog_hz` `0x7ffd` = **25.0000 s** against `hw_watchdog_timeout_s=0x19` = 25, and `bite_ticks_written` `0x000dffac` = **28.0000 s**; the arm precedes the timebase sample by log order, so the reset is at sample + 28 s. **The budget**: sample #2 2.9145 s -> the park's entry snapshot 4.0951 s = **1.1805 s of boot**, the park returns at 6.0951 s, the reset is at <= 30.9145 s, **margin >= 24.8 s** - a 2000 ms ask is not racing a 28 s reset. **And 533 gives the falsifier its own measured shape from the other direction**: the park's entry is at 4.0951 s and the last record the boot ever wrote is that window's `wfi_after` at 4.1218 s, so on the frozen arm the park lasted **26.7 ms of its 2000** - that is what `poll_seq` stopping at 2 looks like when it is the arm. **The contradiction the first draft created, and the guard that removes it**: written unconditionally the block said "the watchdog is armed, 28 s" twelve lines under the function's own `hardware watchdog: NOT confirmed armed` - one value with two definitions in one output, introduced by a step whose whole subject is excluding a second cause; it is now guarded on the same count that other line reads (`watchdog > 0`), so with the arm record the reader may say the reset is excluded and without it, must say the interval is **cited and not read from this log**. **The derivation is left owed on purpose**: the key that would mark the park's start on the *sleeper* arm is the one it cannot publish (`xnu_live_repair_before` is inside 594's guard), so a derivation keyed on it would be `UNREAD` in exactly the state it exists for (586). **The rehearsal changed so the new branch is a test and not a paragraph**: a row may now carry N expectations (one per row could only reach the first line of whichever guard ran - 602's own lesson, one step later), the `ok` line says how many it carried, and a new state `poll-seq-2-no-arm` removes the arm record so the *else* branch is exercised while the six pre-existing states take the *then* branch. **Shown to fire**: one word doctored (`measured, not argued` -> `assumed`) made exactly one state fail, named it and quoted the line it did not say, with **no `exit N, promised N` part** - so the text check caught it and not the code check - and the runner was restored from a copy and verified by sha256 (not by `git checkout`, which would have reverted this step's own edit). Full pass **7 ok / 0 failed** and **7 ok / 0 failed**, exit 0; gate **EXIT=0 / 518 lines / 0 stderr**, census still 3 sites / codes `2 3` (no new `exit`/`die`). **This does not advance the goal** - no boot, no build, no device, no `fastboot`, nothing written to storage; `60063c47...` is still the image the next press sends and its witness is unchanged. TWRP still withheld |
| stage90 | [experiment-602](experiment-602-the-live-path-has-a-reader-now.md) | **the live path has a reader now, and the two halves of "will the press produce one" are tests** - the press is the scarcest thing this phase has and the next one is owed, so 601 asked whether the runner gets through its own first step and found it would not, cleanly, on the state the machine is *actually* in; that defect was reachable only by **running** the live path, and 601 left the rehearsal in `/tmp`. New `tools/rehearse_live_path.sh` closes it, in two halves, because both can waste the press: the **runner** must reach the capture and the **reader** must be able to read it. **Safety by construction**: `sudo`/`adb`/`fastboot` are replaced by stubs first on `PATH` (`fastboot boot` is a `touch`) with a bare `adb`/`fastboot` stub too, everything unmodelled is **refused by name** and a refusal fails the state, and before any state runs the harness **asserts all three resolve into its own stub directory** - a stub that failed to `chmod` would otherwise make the next line a real `fastboot boot` (600 section 7's defect one layer down). **Section A, seven states, all passing**: `happy-adb` (0), `host-log-return-no-adb` (3), `no-return` (2), `host-log-unreadable` (1), `no-fastboot-after-reboot` (1), `happy-reads-the-log` (0), `exit3-log-was-parked` (3) - all five codes exercised, each asserting the line its own wording promises, with the stub reproducing the *sequencing* the return criterion turns on (count 1 on the baseline taken right after `fastboot boot`, the return on the **second** `dmesg` read after it). **Section B, six states of the reader, all passing**: `predicted`, `poll-seq-2`, `small-timeout`, `door-max-0x8000`, `no-door-seq`, `no-poll-over` - every state the coming run can produce prints its own line, and two readings are named rather than left in the tool (`door-max-0x8000` fails rung 0 *and* passes rungs 1/1b, which rung 0's own text explains; `no-poll-over` is the one rung whose absence is silent **by design**, because `poll_over` publishes from the fifth poll and a parked idle is healthy). **The first draft reported 3 ok / 3 failed, and all three were the harness's, not the runner's**: an expectation met on **stderr** while the check read stdout (two states end in `die`), a marker that meant the opposite of the state its own comment named, and an expectation spanning a **line wrap** (`below the` / `park's own threshold` are printed on two lines) - items 1 and 3 being the project's standing defect reproduced inside the check written to detect it. **Shown to fire, twice, and the first attempt found a different check**: doctoring an **exit code** made the **gate** refuse first - `run_and_capture.sh's wait section can return 7, which is not a code this gate's reading explains (2 3)` - the first measurement of 584's cross-file census check firing, and a better finding than the one sought; doctoring a **message** then produced the rehearsal's own refusal (one state, named, the line it did not say, with the exit code untouched). Runner restored after each and verified by **sha256** (`7da957d4…`), not by reading the diff. **This does not advance the goal** - no boot, no build, no device, no `fastboot`, nothing written to storage; `60063c47…` is still the image the next press sends. TWRP still withheld |
| stage90 | [experiment-601](experiment-601-the-runners-first-step-read-a-missing-log-as-an-empty-one.md) | **the runner's first step read a missing log as an empty one, and leaked eleven grep errors** — the press is the scarcest thing this phase has, so the question is not what the log will say but **whether the runner gets through its own first step to produce one**, on the state the machine is actually in. It would not have, cleanly: `$LOGFILE` is absent at this moment, and `summarise_log` — called at step 1 before anything is booted, and again at step 5 on the log it just captured — ran **eleven `grep -c` invocations against a path with no file behind it**. `grep -c` writes its error to **stderr** and *nothing* to stdout, and the `|| true` that swallows its exit status does not swallow the message; `n` came back as the **empty string**; and **`[[ "" -eq 0 ]]` is true in bash**, so the "no payload output" branch was reached by a *value that was never produced* rather than by a *count of zero*. Not cosmetic: it printed `MI4IOS6_STAGE90 lines: ` with **no number**, seven blank marker rows, and `hardware watchdog: NOT confirmed armed` — a claim about the watchdog inferred from an absent file, which is the misattribution the `-eq 0` branch twelve lines below says in as many words that it must not make. **Found by rehearsing the live path**, not by reading: a `sudo` stub on `PATH` (which never reaches the real `sudo`/`adb`/`fastboot` and refuses anything it does not model) drove steps 1-5 with no device. **`--summarise` cannot reach the state at all** — the CLI refuses a missing path with `die "no such log"` — which is exactly why it survived: the state is reachable only on the live path, and the live path was the one with no reader. Every `--summarise` reading this phase has taken (600's 22, 598's 11, 599's 6) ran on files that existed. **The fix**: two states named separately at the top of `summarise_log` (`! -e` = "no file at PATH", `! -r` = the 511/512 ownership state), both `return 0` — deliberately, because this function's last statement was once `[[ ... ]] && say ...`, whose status-1-on-false made the *caller* exit under `set -e` before booting anything. **Pre-existing, measured not assumed**: HEAD's own runner through the same stub gives the identical 11 stderr lines and the same blank count. **Verified both ways**: live path with the file absent, stderr **715 B / 11 lines -> 0 B**, same exit 0, step 1 now naming the path, later steps unchanged; and 24 states against HEAD's runner with stdout `cmp`-ed *and* stderr byte-counted — **23 byte-identical** (including an **empty file**, which must keep the old branch) with the single difference the target state (unreadable: HEAD 550 B of stderr and a blank count -> 0 B, named). Gate re-run **EXIT=0 / 518 lines / 0 stderr**, census still 3 sites / codes `2 3` (now `:1921`/`:1953`/`:1963`), +37/-0, no new `exit`/`die`; `60063c47…` still the image the next press sends. **This does not advance the goal** — the OS is not observed booting, the phone is off the bus since 2026-09-23 01:18:06 (and the `05c6:9008` on `usb 3-3` is the neighbouring device, serial `33e80afe-v63-usbd-disabled-rndis`, not the phone), and the press remains the user's. No device, no build, no `fastboot`, nothing written to storage. TWRP still withheld |
| stage90 | [experiment-600](experiment-600-the-sleeper-clause-asserted-an-image-the-log-cannot-see.md) | **the sleeper clause asserted an image the log cannot see, and 513's own captures are the counterexample** — 594's arm needed a reader before it could be run, and the reader opened by claiming *the image* skipped 514's repair, while the branch is entered by six `grep`s over the **log** (`run_and_capture.sh:459-465`) and no log carries a build marker (549). The six tests are behavioural (`door_seq` present; `repair_seq`/`sip_seq`/`pce_seq`/`wfi_seq`/`slot_cwe_` absent) — "the window did not run" is what they measure, and on 594's arm that is *caused by* the switch, which is not the same as being *tested by* it. **The counterexample is this project's own**: 513's two captures (2026-09-21, **two days before the arm existed**, an image with no repair to skip and no `entry_window_note` to publish) satisfy all six tests and take the branch — 25 `door_seq` records to `0x01000000`, `poll_seq` 4 with timeouts 5/40/2000/2000 ms and `error=0`/`retval=0` on every one, and **zero** occurrences of `repair`/`sip`/`pce`/`wfi`/`slot_cwe`. Three printed lines and one comment are corrected to read the *machine* rather than the image, and the fix is the same one 598 §4.3 made one clause over (a claim about another artifact written from one log). **Rung 0 is a progress test and not an identifier**: 513 reaches `0x01000000` = **512×** the `> 32768` threshold, so passing it says the machine kept going, not that the arm ran. **Rung 1 is a measured outcome and not an extrapolation**: 513's two 2000 ms parks *returned on hardware* with `SIGPdisabled` set and the window never entered — a stronger witness than 598's two short asks (5 ms / 40 ms, both before the repair) — and it is the hardware half of **599 §6**, whose derivation said the tick cannot need the IPI. **The guard caught its own author**: two backticked identifiers in new `say` strings made the runner refuse at start (`a printed string contains a backtick, so bash will RUN it`, exit 1, 0 output lines) while `bash -n` stayed clean — 594 §4's structural check firing on a step about readers being wrong, and the fix is wording, not escaping. **And an attribution defect inside the sentence written to fix attribution**: the first draft said "513's two captures … (their `poll_ticks` 0x24c072a and 0x24c0c95 …)" — *both values are run 2's*; run 1's are `0x024c37ca`/`0x024c3e14`, and the four now appear with their capture named. The rounding moved with it: measured 38,537,002–38,551,060 ticks ⇒ **~38.5 M**, not "~38.6 M". **Verified on 22 states against HEAD's own runner** (a same-depth copy, because the script does `cd "$(dirname "$0")"` — 598 §5's one-directory trap): **14 byte-identical**, and the 8 that differ are the sleeper branch only, where the replaced lines are narrative (0 of them carry a verdict token) and the *set* of verdict lines is character-for-character identical. **The harness had two defects and both would have inverted the result**: the HEAD copy was not executable so every "before" run exited **126** with empty output (caught by asserting exit codes rather than letting `cmp` on an empty file speak — a producer that delivered nothing is not a verdict), and the first verdict-line filter included a bare `^  ` alternative that matched nearly every line, labelling 7 of 8 commentary-only diffs `VERDICT-CHANGED`. Gate re-run **EXIT=0 / 518 lines / 0 stderr**, census still 3 sites / codes `2 3` (now `:1884`/`:1916`/`:1926`), `+45/-5`, no new `exit`/`die`; `60063c47…` still the image the next press sends. No device, no build, no `fastboot`, nothing written to storage. TWRP still withheld |
| stage90 | [experiment-584](experiment-584-the-exit-code-with-two-producers.md) | **the exit code with two producers, and the verdict a read that never happened** — a gate-only change (`preflight_boot_check.sh`, `63 0`, pure insertion) that makes the file's own exit-code vocabulary true: `0` green, `1` a refusal printed above, `2` the argument parser and nothing else. Under `set -e` every **unguarded** command that failed ended the script with **its own** status and no literal `exit` involved, so `awk`'s status 2 on the entry record was a second producer of the usage error, and `cat`'s status 1 on the build config was indistinguishable by code from a refusal. Nine states rehearsed in a symlink scratch tree against the committed gate gave **four different answers** to an unreadable input, only one of which carried a reading — and two of them (`R7` entry bin mode 000, `R8`/`R9` checksum list absent/mode 000) printed a **finding about the artifact** ("the image does not carry the arm … byte for byte", "image does not match SHA256SUMS.txt") produced by a read that never happened. The repair is two layers: an `ERR` trap that restates `$LINENO`/`$BASH_COMMAND` as this file's refusal, covering every command including ones added later, and `_readable` at the eight sites where existence is already checked — `$CONFIG`, `$IMAGE`, `SHA256SUMS.txt`, `$ENTRY_BIN`, `$PAYLOAD_BIN`, `$PAYLOAD_ELF`, `$ENTRY_CFG`, `$ENTRY_SRC_MANIFEST` — ordering **absent > nonregular-or-directory > unreadable**. Two deliberate omissions: the entry ELF stays 579's non-fatal four-way `UNREAD` (that clause runs last by design), and the checksum list is the one `-e`-with-no-`-f` site because `sha256sum -c` used to be the thing that reported it. Two bugs fixed inside `_readable` after being measured: `stat` without `-L` reported the **symlink's** mode (777 for a mode-000 file), and `%a` printed `0` not `0000`. `ERR` semantics measured in isolation before writing it in: `fail` and the parser's `exit 2` raise nothing (exit 2 keeps exactly one producer), `cmd \|\| true` and `if`-conditions are outside `ERR`, so refusals are not doubled. Regression: whole-gate A–D byte-identical, M1–M11 byte-identical but for the region extractor's line numbers (`1915-2024` → `1978-2087`, same 110 lines, re-`cmp` clean), live gate `EXIT=0` / 23 sections / 0 `UNREAD` / 0 stderr on the frozen 574 arm. Frozen artifacts untouched (`151425c4…` / `bcacf065…` / `aeb7862a…`); no device, no build, no arm changed. TWRP withheld. |
| stage90 | [experiment-580](experiment-580-the-census-that-names-its-opcodes-and-the-four-outcomes-of-section-4.md) | **the census that names its opcodes, and the four outcomes of section 4** — two host-side defects found before any boot, each the class this project keeps meeting: **a check right about the machine and wrong about what it said**, and **a reader that could not tell "I read a zero" from "I could not read"**. (1) **The measurement arm is not coprocessor-free.** 574 §2 listed "0 coprocessor instructions in the body (`$3 ~ /^mcr/`)" and 577 §2 restated it without the parenthetical as "no coprocessor instruction of any kind"; both are false of the body, and the parenthetical is how it went unnoticed — the *pattern* is `mcr` and the body holds a **`mrc`**: `8047ca64: mrc 15, 0, sl, cr1, cr0, {0}`, the compiler's inline SCTLR read, present in **535's parked arm too** (`8047ca80`). Measured, `mcr=0 / mrc=1` in **both** arms — **the counts do not separate them, the callee does** (`bl FlushPoC_DcacheRegion`: 0 here, 1 there). The clause now counts four numbers (`mcr`, `mcr`→`cr7`, `mrc`, `mrc`→`cr7`), prints the census in the arm's narration (`0 mcr (coprocessor write) / 1 mrc (coprocessor read, of which 0 name cr7)`), keeps the refusal on any `mcr` — a read cannot change cache state, a write can — and adds a refusal on an `mrc` naming `cr7`, by **target** and not by count. **Both refusals can fire**: the negative control is a routine in the same image, `mmu_kvtop_wpreflight` (`0x80017594`) → `mcr=2 mcr7=1 mrc=1 mrc7=1`. Comment-and-clause edit only: rebuilt, bin `151425c4…` / elf `3bc72605…` **byte-identical**, gate `EXIT=0`, 0 UNREAD (the arm-change guard caught the first attempt — `STAGE90_XNU_ISTACK_SEPARATE` left at its default 1 against a record saying 0). (2) **578 §1's table read back against `nm`**: its "where" column mixed instruction addresses with function entries (`FlushPoC_DcacheRegion` `0x800458b0` is `cfmdr_loop`'s `mcr`; the entry is `0x8004589c`), the alias is **`CleanPoC_DcacheRegion_Force`** and no symbol named `_Force` exists in either parked image, and "every data-cache maintenance site" scoped to the by-MVA `{1}` forms (the image also holds 9 set/way `{2}`, 5 `cr7,cr5` and 3 `cr7,cr8` sites — none aimable at one line, which *is* the argument for the scope). Counts, the two `cr7,cr14,{1}` sites and the absent `cr7,cr6,{1}` unchanged; the sharpened row: **four of the five `cr7,cr10,{1}` sites are the entry's own code**. (3) **`serial_enum_count` could not say "I could not read it": `grep -c` prints `0` for empty input**, so a *failed* `dmesg` read as a **zero count** — measured, a stub whose `dmesg` fails returns `[0]`, and the caller's `${...:-UNREAD}` never fired. The consequence was the strongest reading in the file from no reading at all: `0 -> 0` falls through to `exit 2` "The device did not come back ... It needs a power press", which on the coming run tells the operator to spend the device while the result sits in DRAM. Repaired by testing **the read**: a `dmesg` that fails or prints nothing returns no value, and a trailing `return 0` keeps `set -e` from turning it into exit 1 at the assignment. (4) **The unreadable state is not exit 2**: it is a host-side failure that says nothing about the device, which is what exit 1 already is, so the branch `die`s. The first attempt made it **exit 4** and **the gate refused the runner's change** ("can return 4, which is not a code this gate's reading explains (2 3) ... before spending a boot on a run whose status it cannot narrate") — the check working, and it moved the choice onto the right ground; named rather than hidden, the census counts literal `exit N` sites, so a `die` inside section 4 is a second producer of exit 1 the census cannot see. The **fall** case (ring buffer rotating, `2 -> 1`) was split out of the UNREAD branch, whose text called a compared-and-fallen count "could not be compared". (5) **Six states exercised** through one stub whose device side is identical in all of them (`RETURN_TIMEOUT=6`, `CAPTURE_WAIT=5`), each printing exactly one message: `dmesg` fails → **1**; `dmesg` empty → **1** (and the pre-boot hint "the host log is unreadable here" fires for the first time); count falls `2 -> 1` → **2**; no rise `1 -> 1` → **2**; rise **at** the after-read `1 -> 2` → **3 from section 4**; rise **inside** the wait → **3 from step 5**. (6) **566 §3's variant 3 was right; the re-run of its `/tmp` stubs was not** — all four landed on step 5 because they ran at the **default 180 s** (where a rise inside the wait is what happens) and `bin_s4`'s `adb` clause never consulted its boot marker: 566c's subject re-derived from the other side, plus a stand-in with the right name and the wrong body. No device action, no arm changed, frozen 574 arm untouched and unrun; **the boot still waits only on the user's power press**; TWRP withheld. |
| stage90 | [experiment-549](experiment-549-which-arm-runs-is-a-boot-arg.md) | **which arm the window runs is a boot argument, not a property of the code** — a host-side reading (no device, no build) that corrects one table in 546 and turns 533's arm question from an inference into a reading. The enter and the exit each begin with the same two-way test on `up_style_idle_exit` (`0x8004625c`, `0x800462ec`) and `real_ncpus` (`0x80046270`, `0x80046300`), and the two selectors are not equalities of the code: `up_style_idle_exit` is `.bss` `0x805511a4` (**0**) written only by `PE_parse_boot_argn` in the real `arm_init` (`0x8000d180`-`0x8000d198`, the string at `0x80487b93`), and `real_ncpus` is `.data` `0x80520378` = **1** (read out of the image). **This image sets the boot argument**: the string `boot_args.c:48`/`stage90_main.c:797` builds ends in `up_style_idle_exit=1` (present twice in `stage90.bin`, guarded by a `_Static_assert` because `cmd_len` silently drops the *tail* at 256 bytes — and the tail is this token). **So the enter takes the single-CPU `CleanPoU_Dcache` arm and the exit skips `InvalidatePoU_Icache`/`flush_core_tlb`; the window's whole cache inventory is two L1 D-side operations.** 546's §2 table listed six calls as executed; four are on branches this image does not take and two are mutually exclusive alternates — corrected in place with a "runs in this image?" column, and the conclusion is stronger (the enter's arm is a **clean**, so the exit's is the window's only invalidation). Also: `0x800457fc` is reached as `CleanPoC_DcacheRegion`, and `CleanPoC_DcacheRegion`/`_Force` are the **same symbol** (`caches_asm.s:216-219`); its immediates in the image are `#63`/`#6`, not `#31`/`#5` (told to the peer who supplied the correction). **The payoff**: `__wrap_platform_cache_idle_enter` loads **both** selectors (`0x8047c8f4`/`0x8047c8f8`) into `entry_note_pce`, and again (`0x8047c948`/`0x8047c94c`) into `entry_note_pce_after`, publishing them **by name** as `xnu_live_pce_up`/`_ncpu` and `xnu_live_pce_after_up`/`_ncpu` — **and 520's own archived log already carries them**: `_up=1`, `_ncpu=1` on both sides of the enter (log lines 8300-8317), so the returning run's arm is now **measured, not inferred** from the boot-arg string, and it is the state 546's mechanism assumes. `_after_sctlr=0x30c57879` is the same in-window value 548 decoded. `_seq` publishes on `n == 1 || (n & (n-1)) == 0`, so a printed `1` means the first enter and a `4` means at least four (406's tell). **And it removes a candidate remedy**: flipping the boot-arg buys I-cache + TLB, neither a D-side L2 op, so the L2 stays unmaintained either way and 535's PoC *flush* remains the fix. Instrument note: bare `llvm-objdump` decodes this ELF as pre-ARMv7 (`e_flags` empty, BuildAttributes empty), printing every `movw`/`movt`/`ubfx`/`dmb` as `.word` — 118,621 of 1.38 M words, `movw`/`movt` count 0; use `--triple=armv7-none-eabi`. No device action; frozen pair untouched. |
| stage90 | [experiment-548](experiment-548-the-kernel-memory-is-mapped-cacheable.md) | **(the *second* commit labelled 548 — the first, `3448f9f` "the gate measures the net's reach in the entry image", is gate-only and has no document; it is an ancestor of this one, so the label is disambiguated here rather than by rewriting history)** **the kernel's memory is mapped cacheable, so 546's one checkable premise holds** — a host-side check (no device, no build) of the single premise 546 §6 listed as a pmap property rather than a function property, and which 547's mechanism rests on. XNU maps kernel memory with memory attribute index **0** (`arm_vm_init.c:175-176` and `pmap.c:2641`/`:5416`/`:6232`/`:6240`, both `CACHE_ATTRINDX_DEFAULT` = `CACHE_ATTRINDX_WRITEBACK` = `0x0`, `proc_reg.h:630-636`), whose own comment is *"cache enabled, buffer enabled"* — index 3 (`CACHE_ATTRINDX_DISABLE`) is the non-caching one and is not what kernel memory uses. The remap tables `PRRR_SETUP = 0x1F08022A` (`TR0 = 0b10`) and `NMRR_SETUP = 0x01210121` (`IR0 = 1 = NMRR_WRITEBACK`) give **cacheable under either reading of `TR0`** (remap applies → write-back; fixed normal type → write-through), so the premise does not turn on that ambiguity. And `SCTLR.TRE` is **set** in the state the pop runs in — two regimes in the captured log: the payload-era `xnu_entry_stub_sctlr_after = 0x00c5487b` has bit 28 clear, while `xnu_live_sctlr = 0x30c5787d` and the in-window `xnu_live_pce_after_sctlr = 0x30c57879` (C clear, TRE 1) have it set, so the attributes in force at the frontier are XNU's, not the payload's `STAGE90_PMAP_ATTR_MODE_SO_ONLY` bootstrap table. **Closes** 546 §6 bullet 1 — a valid stale line for the stack can exist, so the mechanism is not impossible by construction — and adds that *every* kernel mapping in this boot is cacheable, which is why the window's missing L2 maintenance matters at all. **Does not close** which level holds the line, why the enable-on cells produce no log, or whether the death is a cache matter. Residual, stated: write-back vs write-through (immaterial — with `C` = 0 the push bypasses the cache either way) and the bit positions of the 3-bit index under `TRE` = 1 (which `ARM_PTE_ATTRINDX` places unconventionally, `proc_reg.h:932`). No device action; frozen pair untouched. |
| stage90 | [experiment-547](experiment-547-the-load-succeeded-and-the-value-is-wrong.md) | **the load succeeded and the value is wrong, and the ledger says the wrapper's write kills the recovery** — a host-side reading of 520's own register dump (`/tmp/cancro-last_kmsg.txt`) against 546's mechanism and the four-cell ledger. New from the dump: `lr = 0x800462dc` (the exit's post-`bl FlushPoU_Dcache` return, so execution is at or after the final `pop`), the abort is a **prefetch** abort at `far = pc = 0x04b79074` with `fsr = 5` (translation fault, level 1) and `cpsr` bit 5 set (Thumb) — i.e. **both loads of the pop completed and the value brought back was not an address**; `r11 = 0x04b79075` is the same counter the instrument captured as `slot_rtcpre_pop`, and the fetch is at `r11 & ~1`. **The address-arithmetic family is eliminated without invoking caches**: the same function's `push {fp, lr}` and `pop {fp, pc}` address the same location by construction (nothing touches `sp` between them), so a wrong stack pointer, a frame mismatch, an unmapped stack or a skipped push would each predict a failure the dump does not show. **And the reconciliation the ledger needed**: the two cells carrying the enter wrapper's `SCTLR.C` write (522, 526) are exactly the cells in which 546's precondition — a store the cache did not see — is *absent* (their push is cacheable), and they are the two that produce **no log at all**; so the ledger's real discriminator is "dies recoverably (log + `MACH Reboot`)" versus "dies with nothing", not "dies at the pop" versus "does not" — every cell that logs dies at the pop. The enable-on cells' shared feature is a write that re-enables allocation while the core is **outside the coherency domain** (Apple's enter cleared `ACTLR` bit 6 at `0x800462b8`; the wrapper's write follows its return), offered as a hypothesis. **533's prediction sharpened**: die at the pop, log it, and **come back** — with 547 §4 giving the three outcomes as a falsifier table, the third of which ("returns with no pop death") would stop 535 being built as designed. For 535: a PoC invalidate of the L2 in the window, by return address, **and not a wrapper `SCTLR.C` write**. No device action; frozen pair untouched. |
| stage90 | [experiment-546](experiment-546-the-pop-is-cacheable.md) | **the pop is cacheable, and nothing in the window invalidates the L2** — a host-side reading of the bytes the run dies in (no device, no build): the whole of `platform_cache_idle_enter`/`_exit` read instruction by instruction out of the frozen image, plus the source they came from. The window is bounded by Apple's own two writes — `SCTLR.C` cleared by `platform_cache_disable()` at `0x80046240` and set by `/* Enable dcache allocation. */` at `0x80046324` — and both the push (`0x800462d4`) and the fatal `pop` (`0x8004633c`) are inside it, **the push with the cache off and the pop 24 bytes after the cache comes back on**. All six calls in the window invalidate **L1 lines only**: two PoU flushes, one PoC *clean by VA* of the per-CPU struct, the I-cache invalidate and the TLB flush — and Apple's own comment on the first is `/* Flush L1 caches and TLB before rejoining the coherency domain */` (`caches.c:459`). **No instruction here invalidates an L2 line, and none can** — that would take a PoC flush at level 2. Hence the reading's hypothesis, one sentence: the stack lines were cached earlier, the push with `C`=0 wrote memory and left the L2 copy **stale but valid**, the exit's PoU flush killed the L1 copy, and the pop — cacheable — hits the stale L2 line instead. **It explains the two arms that were built against this seam and failed without being designed to**: 516's `CleanPoC_Dcache` *cleaned* the stale copy out rather than removing it, and 521's `FlushPoC_Dcache` invalidated at the right level at the wrong **time** — at the enter, before the wrapper's own cacheable activity can re-victimise a copy down into the L2. For 535: the seam is still the exit's `bl FlushPoU_Dcache` (`0x800462d8`, the last `bl` with cache work in the span), it must be told apart from the routine's other three callers by the return address (`0x800462dc`, what `xnu_entry_stub_caller` exists for), and it needs a **PoC** flush; the stretch from the re-enable to the pop has **no `bl` at all**, so "flush just before the pop" is not buildable. **And a prediction for 533, stated before the run**: it changes only the enter-side enable, so it should still die at the same `pop`, with `pre`+`rtcpre` published and `post` absent (`rtcab` as written here was the abort path's counter, not the wrapper's - corrected under 560). Also notes that the exit's re-enable is Apple's own and unconditional, so every arm including 520's returning cell executes it. No device action; frozen pair `1daaf44e…`/`f202f246…` untouched. |
| stage90 | [experiment-545](experiment-545-the-level-is-in-the-operand.md) | **the level is in the operand, so the L1 loop is the L1** — a host-side reading (no device, no build) that **corrects 544 where it mattered**: the cache level for a set/way maintenance operation does not come from `CSSELR`, it travels **in the operand's own low bits**, encoded exactly as `CSSELR` encodes it (0 = L1, 2 = L2) — and Apple writes it, two instructions before the second loop of each PoC routine (`CleanPoC_Dcache` `mov r0, #2` @ `0x80045780`, `FlushPoC_Dcache` @ `0x8004584c`; both PoU routines have `#0` and no second loop). Three witnesses: the linked image's bytes, the source in `caches_asm.s` (`:132`/`:145`, `:169`, `:242`/`:254`, `:277` — the split is exactly PoC vs PoU), and the vendor kernel, whose `v7_flush_dcache_all` writes `CSSELR` **and** ORs the same level register into the operand (`cache-v7.S:78`/`:94`). It cannot be a set number — `2` as a start would skip sets 0 and 1 of every way. **So 544 §2's consequence, both branches of §3, and §4's "no sequence the project has run can invalidate an L1 line" are dead**: every PoC routine sweeps the L1 (level 0) and the L2 (level 2), the PoU routines sweep the L1 only, and the exit's `FlushPoU_Dcache` is weak not because it misses the L1 but **because a PoU flush is defined to stop there** — so an arm at that seam needs a **PoC** flush (coverage), not a repaired geometry. What survives is 544's `CCSIDR`-not-`proc_reg.h` rule (the geometries really are the compile-time ones: 64 sets/4 ways/64 B captured against 128 swept; 2048/8/128 B against 4096 swept) and the choice of seam. **And the correction makes one thing visible that no level fix can repair, recorded as a hypothesis rather than a finding**: there is **no barrier between `push {fp, lr}` and the sweep** (`0x800462d4`/`0x800462d8`), Apple's routines put their `dsb` at the *end* and contain no `dmb` at all, while the vendor kernel's flusher opens with `dmb @ ensure ordering with previous memory accesses` — if prior stores must be visible to the sweep, the push can be missed and left dirty in an L1 the window then turns off. Not settled: which level holds the stale line, whether the death is a cache matter at all, and whether *Krait* honours the operand's level bits (the captures prove only that `CSSELR` selects the `CCSIDR` read). No device action; frozen pair `1daaf44e…`/`f202f246…` untouched. |
| stage90 | [experiment-544](experiment-544-no-cache-routine-selects-a-level.md) | **no cache routine in this image selects a level, so one of every pair of loops is always on the wrong cache** — a host-side reading (no device, no build) that settles what 534's `CSSELR` finding means for the flush the idle exit actually calls, in a form that does not depend on how well anyone remembers the ARM ARM. **The image contains two `CSSELR` writes in total** (`mcr p15,2,Rt,c0,c0,0`): `machine_write_csselr` at `0x80022eb0`, reached only from `do_cacheid` (`cpuid.c:224/262/265`), and `entry_epilogue` at `0x8000497c`, this project's terminal report path writing `#0` **at the end of a run**. Neither is in a cache routine. `do_cacheid` writes L1 first, then **L2/L3, and never selects L1 back** — confirmed a third time by the live capture `xnu_entry_csselr_before=0x00000002` read at the top of the epilogue, with nothing between the two writing `CSSELR`. And Apple's routines are written in **pairs of loops whose two geometries can only matter if a level is selected between them**: `FlushPoU_Dcache` `@0x80045874` and `CleanPoU_Dcache` `@0x800457a8` each have **one** loop (set inc `#64`, overflow `#0x2000` → 128 sets, way inc `1<<30` → 4 ways = the `ARMA7` `MMU_*` **L1** geometry), while `CleanPoC_Dcache` `@0x8004575c` and `FlushPoC_Dcache` `@0x80045828` have a second (overflow `#0x40000` → 4096 sets, way inc `1<<29` → 8 ways = `L2_*`, `__ARM_L2CACHE_SIZE_LOG__=21`). The claim that holds **either way**: if the level follows `CSSELR` both loops run on the L2 — loop A sweeping **512 of its 16384 (way,set) pairs, 3.1%**, loop B covering it because the compile-time geometry over-iterates, and **no set/way op in the image can reach the L1 at all**; if it does not, loop B is dead code and the **L2** is never maintained. Either way a pair cannot be doing what it was written for. So `FlushPoU_Dcache` — the one call inside the symmetric push/pop, at `0x800462d8` — is confirmed to be the weak one 534 named (the only single-loop routine, L1 geometry pointed at the L2; I count 512 pairs against 534's 256, same two conclusions), **but the L1 is not the gap in that one call, it is the gap in every cache operation this image makes**: 516's `CleanPoC_Dcache` before the window and 521's `FlushPoC_Dcache` are both two-loop routines and neither touches an L1 line either, so the hot stack line the fatal `pop` reads over survives every sequence the project has run. **What it changes:** the seam must be one from which a level can be selected and the sweep must read `CCSIDR` rather than `proc_reg.h` — the compile-time geometry is the hardware's in one of three dimensions on each level (`ccsidr_l1=0xa007e01a` = 64 sets/4 ways/64 B = 16 KB against `MMU_CSIZE`'s 32 KB and 128 swept sets; `ccsidr_before=0xf0ffe03b` = 2048/8/128 B = 2 MB against 4096 sets of 64 B), which is why loop B covers the L2 anyway and why an arm that sweeps "the same shape, more of it" would be reading a layout instead of the expression. Nothing is built: **533 is frozen and unrun and decides the arm**. |
| stage90 | [experiment-542](experiment-542-the-net-has-not-fired-in-this-arm.md) | **the net has not fired in this arm, and the gate now says so** — a host-side reading (no device, no build): `== recovery net ==` prints on every run that the hardware watchdog makes a hang safe, and that claim is about the *mechanism*; what it does not say is whether it has worked in the configuration about to be booted. For this arm the last three hangs **517's first run, 521, 522 and 526** did not come back and each needed a power press — and 526's is measured on this host rather than taken from a ledger: its `usb 3-10` fastboot device (`18d1:d00d`, serial `4a2fe00b`) disconnected at **16:35:47** and the port has been silent since, over two hours against a bite due 28 s after the arming. **And it cannot have been disarmed**, which is the part that makes it a finding: measured over every `mov`/`movw`+`movt` immediate pair in `out/stage90/xnu_arm_entry.elf`, **no address in `[0xf9010000, 0xf901ffff]` is materialised anywhere**, so nothing running after the jump can reach the watchdog at `0xf9017000` — the `0xf9000000`/`0xf9002000` that do appear are the **GIC**, one nibble away from the region that matters, which is why the first pass of the search looked like a hit. So the three hangs are not "the net was switched off": a hang in this state does not come back from it. The three returns (518/519/520) came back with XNU's own panic and its own `Attempting system restart...MACH Reboot` in their logs, so XNU reset the machine itself and that return path needs no net; whether the net was *also* due around then is deliberately **not claimed** - the payload's log carries no timestamps, so the logs cannot order the arming against the return, and the host timeline that can (`02:00:59` jump -> `02:01:16` Android, 17 s) is recorded with its condition rather than as a finding, because the log captured at 02:55 is not provably the 02:00 run's. The 506-515 ledger claim is neither overturned nor leaned on, for the same reason. The change is one paragraph, printed **only under `--allow-xnu-entry`** (verified both ways: exit 0 with the flag and the lines present, absent without), and it **adds no net** — a second net would change the frozen, unrun arm. What it changes is the expectation: a hang here owes a power press, the device is never at risk of a brick (no `flash`, nothing written to storage), and the log goes with the cold boot. |
| stage90 | [experiment-540](experiment-540-the-arm-cannot-change-by-accident.md) | **the arm cannot change by accident, and the log says where the pass died** — the two things that had to be true before 533's run could be trusted *after* it happens, and neither was, without touching the arm: **the boot image is still `1daaf44e624563694e…` and the entry image still `f202f2465886aba6…`**. **(1) The gate refused a correct tree.** 533's own commit broke `preflight_boot_check.sh` on the tree it committed — measured at 4787622 with `git status` empty, `xnu_arm_boot/build_entry.sh` 17:53:35.476837508 against the image that same file had produced at 17:53:15.533031244, 19.94 s apart, exit 1 — because the freshness sweep listed that directory against the *payload image's* mtime and **`build.sh` never opens any of those files**: it consumes exactly one thing from there, `out/stage90/xnu_arm_entry.bin`, byte-embedded at `build.sh:93` (`build_entry.sh` appears only in prose, `:70`/`:85`/`:309`). The prescribed remedy — rebuild — is the one thing this phase cannot spend (408: the payload link does not reproduce), so the repair asks the question the refusal stood in for: `build_entry.sh` writes `out/xnu_arm_entry-sources.txt` (every regular file in `xnu_arm_boot/`, 20 of them, hashed, bound to the bin's own sha256 on its second line) and the gate recomputes the same list by the same rule and compares **both directions** — a changed file, a file added, a file removed; a checkout or a commit that rewrote an unchanged file is no difference and no refusal. **It is also the first check that ever looked at the five lowercase `.s` sources** (`entry_vectors.s`, `entry_ramdisk.s`, `entry_macho.s`, `entry_arm_rtabi.s`, `assym.s`): `-name '*.S'` is case-sensitive and matched none of them. **(2) The build could silently produce a different arm, and now cannot.** The record, the manifest and `SHA256SUMS.txt` are all written *by* the build, so a wrong build rewrites them in agreement with itself and the gate, which reads them, is satisfied by an arm nobody chose — and the wrong invocation is the documented one: four of the six switches are off their defaults (`TRACE` 0→1, `REAL_ARM_INIT` 0→1, `SLOT_NULL` 0→1, `ISTACK_SEPARATE` 1→0), so the blind `./build_entry.sh` that both this file's header and `build.sh`'s missing-bin message prescribe rebuilds 526's capture sites where 533's null instrument is. `build_entry.sh` now refuses a switch-set change **before anything in `$OUT` is written**, naming both sets and the one variable that makes the change deliberate (`STAGE90_ENTRY_ARM_CHANGE=1`), verified in six states including the two that would otherwise build; and **enumerating the script's own environment reads** (`grep -o '${STAGE90_[A-Z0-9_]*:-'`) found **three more image-shaping switches in no record anywhere** — `STAGE90_ENTRY_CHECKPOINT`, `_SKIP`, `_AFTER` (`:218`/`:224`/`:228`, `--wrap` at `:235-236`, `xnu_arm_entry_checkpoint.o` in the link at `:27218`) — so the arm is ten keys now, recorded as `(unset)` rather than omitted, and the gate prints twelve. **(3) The reader still had 522's criteria.** `run_and_capture.sh` asserted `_set` with `C` **set** — 522's re-enable — which is the one thing 533 removes; measured against the copy at HEAD, 533's own expected pair printed `FAIL … so the re-enable did not happen`, naming an instruction that is not in the image. The pair is now reported as a reading with each arm's shape named beside it and FAIL reserved for the shape **neither** arm can produce (`_win` with `C` set), and item (3) became the run's actual prediction: the exit wrapper's three bracket publishers (`g_slot_pre` → `g_slot_rtcpre` → the real exit → `g_slot_post`, one schedule and one live gate; the middle one was written `rtcab`, the abort path's counter, and is corrected under 558 (the reader) and 560 (this row's spelling), so a publisher that ran proves the later ones were reachable) turn a missing `post_calls` into a **localization** — pre+rtcab published and post absent means the death is inside `platform_cache_idle_exit`, 520's `pop {fp, pc}`. Both changes were run against the states they are meant to refuse (six for the build stop, four synthetic logs for the reader). **Device still off the bus** — the run is owed a power press. Peer commits 536/537/539 are this step's gate half and carry their own measurements. |
| stage90 | [experiment-533](experiment-533-the-idle-windows-near-end-is-apples.md) | **the idle window's near end is left exactly as Apple left it** — the phase's first arm whose *treatment* is the window's cache state and whose apparatus is 526's, already shown not to be the cost: **526's image with one thing taken out, the enter-side `SCTLR.C` re-enable**. Nothing else changes — 526's null exit wrapper, 517's flush still out of the image, 516's `CleanPoC_Dcache`, 519's `rtcPop` reading and the enter wrapper's own `entry_window_note` all stay. It exists because 526's run came back negative, and the four configurations the phase has now measured stand as: **518/519/520** (flush off, enable off, 520's own publisher) returned, dying at the `pop`; **521** (flush **on**, enable off, 521's repair) did not; **522** (flush off, enable **on**, 521's repair) did not; **526** (flush off, enable **on**, **no capture**) did not — so 522 and 526 differ by exactly the capture's memory traffic, 526 says that traffic is innocent, and what is left is the one deliberate change 521, 522 and 526 share and 520's returning cell does not: **the write that switches the D-cache back on at the window's near end**. The change is a switch, `STAGE90_XNU_IDLE_CACHE_ENABLE` (`entry_trace.c:422-428`, validated at `build_entry.sh:349-353`) guarding the call in `__wrap_platform_cache_idle_enter` (`entry_trace.c:1849-1855`), **and its default is 0 — the cell that came back**: this phase has paid twice for the other choice (520's gate printed 519's hash out of a comment literal; 522's first build silently carried 521's flush because the clause asserted the flag rather than the image), so a build that forgets the variable must be the *baseline* arm and not the arm under test. **The switch's on-position is not close to 526 — it is 526, byte for byte**: two builds of one tree with the sources hash-checked before and after give `STAGE90_XNU_IDLE_CACHE_ENABLE=1` → `05596cc1daefd99f66f7a040f32c133b22740ace41b680a7008fc50766aad672` = 526's frozen entry image exactly, and `=0` → `f202f2465886aba6daa357e110c6bf50e8208c6673a5c4119a8d069065eab28e`, 0 `FAIL`s each; the invocation is 526's with one variable more (`STAGE90_ENTRY_TRACE=1 STAGE90_ENTRY_REAL_ARM_INIT=1 STAGE90_XNU_SLOT_NULL=1 STAGE90_XNU_EXIT_POC_FLUSH=0 STAGE90_XNU_ISTACK_SEPARATE=0 STAGE90_XNU_IDLE_CACHE_ENABLE=0`). The two entry images differ by **528,901 bytes and are the same size** (5,519,996) — the call that goes away is four bytes, every address after it in `.text` moves by four so hundreds of thousands of relocated immediates change, and the length is pinned by `__bss_start` and the fill absorbs the four (`mi4-linker-fill-term`, from the other direction). **Three defects, all in this step's own checks.** (1) *An extractor field that was only right because of the call this arm removes*: the `awk` that reads the enter wrapper's `SCTLR` accesses (`build_entry.sh:29100-29111`) took the first read with `if (en == 0) b = a`, which is "the first read" only because the enable call later in the body makes `en` nonzero — with the flag off `en` never does and `b` is overwritten by every read, so `cwebefore` came out as `0x8047c940` (516's read, 28 bytes past the window's opening) and the arm was refused naming the wrong read; the fix is `if (en == 0) { if (b == 0) b = a }` plus a flag-off clause that asserts the two reads are **adjacent** (`cwepm == cwebefore + 4`), so an extractor that drifts fails the build instead of publishing a plausible number. (2) *The clauses about the write were unconditional*: 522's assertions that the re-enable follows the call that opens the window, that the pair brackets the write and that the note follows the second read evaluated `0 > realaddr` and `0 < 0` with the write gone and refused a correct image; they are now the arm's own question (`build_entry.sh:29112-29147`), the flag-on arm keeping 522's four assertions **including its adjacency test** (`cwebefore == realaddr + 4`, `cweaddr == realaddr + 8`) and the flag-off arm asserting what is really in the window — the call at `realaddr`, the first `SCTLR` read as the instruction after it, the second as the instruction after *that*, the note after the second, 516's cache-off reading after the note — read out of addresses rather than counted; and the 526 say line's sentence about the enter wrapper ("the enter wrapper is untouched, so 522's clause above still asserts the SCTLR.C enable *n* time", whatever the flag was) is now flag-selected (`:29197`, `:29199`), because a say line describing an image that does not exist is 520's gate defect. **The image is `1daaf44e624563694e9f5306276dd4b09e743f8a80ada5882aa2c52e8d12fef3`** (8,540,160 bytes, frozen as `out/stage90/frozen/533-stage90-qcdt.img` with `533-xnu_arm_entry.bin` — and the *first* frozen pair, a ladder image at `ad458ecd…`, was replaced rather than kept: see the third defect below), (3) *The first frozen artifact was the wrong kind of image*: it was built with a plain `./build.sh`, and the canonical run build is `STAGE90_EXTRA_CFLAGS='-DSTAGE90_XNU_ENTRY=1' ./build.sh` — without it `STAGE90_XNU_ENTRY` is `0u`, the `bl stage90_xnu_entry_run` is compiled out and the image runs the payload's own ladder and reboots, so it **never jumps into XNU and cannot produce the log this arm exists for**, while the gate printed `off: the payload runs its own ladder and reboots` and exited 0 (the same class as (1) and (2) one level up: prose describing the artifact instead of a check refusing it). It was caught by the peer session's read-only audit (`stage90-build-config.txt` line 7 `STAGE90_XNU_ENTRY 0u`; no `bl` to `stage90_xnu_entry_run` anywhere in the payload), the gate now refuses `--allow-xnu-entry` with the switch off, and the rebuilt image is a good test of the offset clause: the 48 bytes move `kernel_size` 6,015,308 → 6,015,356 and the blob 496,100 → **496,148**, which the gate recomputes rather than reads. **The image is `1daaf44e…` and the gate is green** — with a second finding landed in this commit, from the peer session's read-only audit: **the gate could not see which arm the image carried**. `build.sh` generates `out/stage90/xnu_arm_entry_blob.c` from `xnu_arm_entry.bin` and the payload's `.rodata` holds it, so the image *is* a function of the entry image — but `preflight_boot_check.sh` never mentioned `xnu_arm_boot/`, its freshness scan was `find "$STAGE_DIR" -maxdepth 1` (one directory too shallow), nothing tied image to bin in either direction, and `stage90-build-config.txt` carries the **payload's** switches only, so the gate's printed switch list cannot say which arm the image holds: rebuild the entry image and forget the payload and the image, `SHA256SUMS.txt` and every check stay in perfect agreement while the gate describes one arm and the image carries another — **520's defect one level down**, benign here only because the prose still described 526 and 526 was what the image carried. The fix is `preflight_boot_check.sh:103-125` (freshness covers `xnu_arm_boot/*.{c,h,S,ld,sh}`, `*.sh` only in that subdirectory because `build_entry.sh` is as much a source of the image as any `.c` in it) and `:127-245`, asserting **the image carries the arm `xnu_arm_entry.bin` holds, byte for byte**, out of four readings no two of which share a source: the payload ELF's own symbols (`stage90_xnu_entry_blob` va `0x000809e4` size 5,519,996; `stage90_xnu_entry_blob_size` va `0x000809e0`) with the offset recomputed at gate time as `blob_va - kernel_addr`; the image header (`page_size` 2048, `kernel_size` 6,015,356, `kernel_addr` 0x00008000), which first proves the image's kernel section **is** `stage90.bin` byte for byte; a byte-content search requiring exactly one match; and the payload's compiled-in size word read out of the image. **One measured number belongs here because the natural guess is wrong**: the blob is *not* at `2048 + size(stage90.bin)` (6,017,356, past the kernel region's end) — it is **inside** the payload, at payload offset **494,100** = image offset **496,148** (494,052 / 496,100 in the ladder build), which is why the payload is 6,015,356 bytes and not 494 KB; the offset is not a size. Both directions were run before the clause was accepted: against the tree as it stood (image carrying `05596cc1…`, bin holding `f202f246…`) it **fails on the bytes and not on the lengths** — both entry images are 5,519,996 bytes, so a size check alone would have called that image correct — and against the image built above it passes with *the linker's symbol says the blob is 5519996 bytes at 494100, which agrees*; a third direction refuses a shrunken entry bin (a 5,000,000-byte prefix of the arm the image carries), which a prefix match could not see. **The run's read order, written before it happens**: (1) **does the device come back at all** — a return is `18d1:d00d` → `2717:0368` → `18d1:4ee7` in ~20 s on `usb 3-10` serial `4a2fe00b` and a hang is one dead second in fastboot with nothing after it, which is what 521, 522 and 526 each produced and the fourth run in a row the armed hardware watchdog has not recovered; (2) if it returns, **where it dies** — 520's `sleh_storm 9` at the same `pc` is the `pop {fp, pc}`, anything later is progress; (3) `xnu_live_slot_cwe_win` and `_set` must **agree**, both with `C` clear — the note is kept on purpose even with the write gone, because an absent key and a key that says "no change" are different facts, and a `_set` with `C` set would be an image still carrying 522's write; (4) the ending shape and whether Apple's own panic path runs (520's log carries it), because a returning arm with the instrument intact is what 523's console item and the storage arm both need before they can be developed at all. **The two outcomes are not symmetric, and that is stated before the run**: a return indicts the enable cleanly (526 and 533 differ by nothing else); a non-return does **not** put the cost on one thing, because 533 is not 520's cell — 520's image has neither the enter wrapper's `entry_window_note` (a store *inside* Apple's cache-off window, 522's addition) nor 526's null instrument — so a non-return would say the enable is not the cost either and leave that note's store and the null wrapper's own shape as the next bisection, in that order. **Not decided**: the storage arm (530-532 stand where they were); the volume (TWRP withheld — the goal's precondition 「如果os已经能进去了的话」 is unmet, XNU reaching user mode and then dying at the idle exit's `pop`); and **boot-image reproducibility**, where the peer session's reading is that the *entry* image reproduces byte for byte from the tree while the **boot image does not** (526's frozen `7819cddb…` versus a same-tree rebuild's `76bf4ea7…`, 48 bytes apart, 44 inside `stage90_xnu_pmap_bootstrap_contract_selftest` and `stage90_xnu_pmap_table_dryrun_contract_selftest` and 4 immediately before `stage90_embedded_macho_size`/`stage90_embedded_macho`, the embedded Mach-O fixture byte-identical once the offset delta is applied first) — so the payload link consumes something beyond the committed sources, arms are compared **through the entry image**, and this doc asserts no boot-image reproducibility. A build and a gate: **no device action in this step**, one non-persistent `fastboot boot` to follow through `preflight_boot_check.sh --allow-xnu-entry` then `run_and_capture.sh --allow-xnu-entry`, nothing ever written to storage, and the worst case remains a phone that needs a power press — which the device already owes, having been off the bus since 526's `18d1:d00d` at 16:35:37. |
| stage90 | [experiment-532](experiment-532-the-storage-arms-first-act-is-one-section.md) | **the storage arm's first act is one section, and both windows are in it** — a host-side reading (no device, no build, no switch) that opens the storage line at the level *below* 531: not the controller's register file but **the address translation that has to answer for `0xf9824900` before a single one of its registers can be touched**. **XNU's page tables do not map the eMMC controller**, and the payload has exactly three L1 tables of its own, none of which holds it: `mmu.c`'s identity table (`build_identity_table` `:5404`, installed at `:5495`) maps `STAGE90_GIC_ALIAS_BASE`, `0x0fa00000`, `RAM_CONSOLE_BASE`(+1 MB), `0xf9000000` (`:5483`) and `0xfc400000` (`:5486`) and nothing else; `xnu_arm_vm_init_full_pmap.c`'s candidate table maps the same devices in Phase 4 (`:414-416`) and Phase 5 (`:452-453`) — **and it is a probe, not the live table**, because it switches itself in (`:501-502`), verifies, and switches back (`:565`), which the log agrees with rather than the comment that calls it "the one a handed-off kernel would run under" (`stage90_xnu_arm_vm_init_full_pmap_live_ttbr0=0x00654000`, `restored_ttbr0=0x00600000` = the identity table again) — so a reader who took that comment literally and edited Phase 4 would be editing a table nothing runs on. **The mechanism that puts a device section into XNU's own L1 already exists and is already proved**: `entry_mmio_section(va, pa, &slot_before, &desc)` (`entry_stubs.c:2227`), whose first user is the GIC probe (`entry_gic.c:377`) after its own header measured it — *"XNU's page tables do not map the GIC … `fleh_irq` runs with XNU's tables live, which map `[physBase, physBase + memSize)` and nothing at `0xf9002000`"* (`entry_gic.c:107-118`) — and it is **the same function and the same descriptor recipe the console's live channel uses** (482 split it out: the attribute `g_live_attr` is `0xc`, read out of `PRRR`/`SCTLR.TRE` rather than guessed, so the mapper deliberately takes no attribute argument). Three of its properties decide how the arm must be written: **the table is a reading, not a latch** — `entry_live_ttb_base` (`:2166`) re-reads `TTBR0`/`TTBR1`/`TTBCR` every install, picks `TTBR1` when `TTBCR.N != 0`, and masks the low 14 walk-attribute bits; the measured `xnu_live_ttbcr=0x00000002` means **every** device address above `0x40000000`, which is all of them, is answered by `TTBR1` (`xnu_live_ttbr0` and `xnu_live_ttbr1` both `0x8070004a`, base `0x80700000`) — 484's whole correction, after a run in which the write landed, the read-back agreed, and the next load from `0xf9000000` was `sleh_abort` at `pc = gicd_read+4`, `far = 0xf9000000`; **it refuses four ways** (`g_live_state != 1` `:2236`; `l1 < 0x80000000` or `l1 & 0x3fff` `:2242`; `index >= 4096` `:2114`; slot occupied `:2119`) and an occupied slot is **skipped, not clobbered**; and **it is one 1 MB section per call**, `SECTION_INDEX(addr) = addr >> 20` (`mmu.c:19`), `L1_SECTION_SIZE = 0x00100000` (`mmu.c:5`). **Which is the finding: 531's two windows are one L1 index.** `0xf9824000 >> 20 == 0xf9824900 >> 20 == 0xF98` (3992) — the two are `0x900` bytes apart, **0.22 %** of one section, and the section `0xf9800000`-`0xf98fffff` also carries the legacy node's third region (`dml_mem` `0xf9824800`) — so the arm's first act is **one** `entry_mmio_section(0xf9824000, 0xf9824000, …)` whose descriptor is `0xf981040e` (the `0xfff00000`-aligned PA, plus SH `0x10000`, AF `0x400`, the attr `0xc` and the block type `0x2`; the console's own is the log's `xnu_live_desc=0xde51040e` with `xnu_live_slot_before=0`), and **a two-call arm copied from 531's two-window table refuses its own second call**: after the first install the slot holds a block descriptor, `(before & 3) != 0`, and `entry_section_install` returns 0 — the map's granularity is three orders of magnitude coarser than the register map's, and the mistake fails loudly rather than silently. **The other two front-end addresses are in two *different* sections, and neither is mapped either**: the console's sink `0xF991E000` (528 §1) is index `0xF99` = 3993 and 528 §5's pinmux `0xFD510000` is `0xFD5` = 4053 — so of the five blocks 528 listed as "a read if the payload only reads it", the three in `0xFC4` (the UART core gate `0xFC400704`, the six BLSP1 CBCRs `0xFC400684 + 0x80n`) and the one in `0xF90` (`0xF9012484`, the BLSP1 AHB vote) are reachable and **the two that would actually put characters out are not** — the addresses are right and the dereference is not, which is the same observation from the other side; and the watchdog is the precedent stated in the same words one file over: *"`0xf9017000` is inside the 1 MB section `0xf9000000`-`0xf90fffff`, which both the identity table and the candidate L1 already map as MMIO"* (`hw_watchdog.c:53`). **"The GIC works, so device addresses work" is not evidence** — the GIC's section is in the identity table *and* installed by the GIC probe itself, which is exactly the precedent the storage arm has to follow — and the failure mode of skipping it is measured: 484's `far = 0xf9000000`, which for the arm is `far = 0xf9824900` arriving *inside* the mode sequence. Two descriptors spell "a device section" here and the difference is recorded rather than unified (`STAGE90_PMAP_DESC_SECTION_SO = 0x00010c02`, AP full access, `DACR = 0x3` manager, versus the live channel's `AP[0]`-only `0x400` with `DACR = 0x1` client) **because the other half changed with it** — a manager domain ignores `AP` and a client domain enforces it, so the two are correct as they are and "unifying" them is the defect. The arm's order is forced and every step of it is a reading: live channel up (else there is no proved attribute to map with), one install with `_l1`/`_l1_moved`/status published, and only then the register file — with 531 §10's free reading (`CORE_POWER 0x0`, `CORE_HC_MODE 0x78`, `CORE_MCI_VERSION 0x050`) taken *before* any write to it, which is what turns "prerequisite" versus "re-do" into a measurement. Open: which side of the jump the arm runs on (the identity table needs `map_section_mmio` in `mmu.c`, a caller inside XNU needs `entry_mmio_section` — different files, tables, attributes and lifetimes, one device, one of them per run); the PMIC/power question (531 §6, unchanged — the mapping is required and not sufficient); whether mapping the whole megabyte is safe (it is this image's only MMIO granularity and every other device takes it, but `0xf98xxxxx` is not the controller alone); HFS+ versus a purpose-built FS; and where the volume comes from (TWRP, withheld). No file outside `docs/` was written; **526 stays built, gated, frozen and unrun** — `out/stage90/stage90-qcdt.img` recomputed `7819cddb50d5a341…` — and the phone is off the bus and owes a power press. |
| stage90 | [experiment-531](experiment-531-the-emmc-controller-is-two-windows-and-a-mode-bit.md) | **the eMMC controller is two windows and a mode bit** — a host-side reading (no device, no build, no switch) that opens the storage line at the register level, out of the vendor's own Linux 3.4 driver at `external/android_kernel_xiaomi_cancro/` (the source 524 used for the console's polled putc). **The register file is at `0xf9824900` and it is not in SDHCI mode until a bit in a second, lower window says so**: `hc_mem` `0xf9824900` holds the standard block `0x00`–`0x3F` **and** the vendor block `0x100`–`0x188`, while `core_mem` `0xf9824000` holds the legacy SDCC words the probe needs first, and the split is checkable rather than conventional — enumerating the `CORE_*` names gives **nine** used only as `core_mem +` (`POWER` `0x0`, `HC_MODE` `0x78`, `MCI_VERSION` `0x050`, `MCI_DATA_CTRL` `0x2C`, `PWRCTL_STATUS/MASK/CLEAR/CTL` `0xDC`–`0xE8`, `TESTBUS_CONFIG` `0x0CC`) and **fifteen** used only as `host->ioaddr +` (the `DLL_*`/`VENDOR_SPEC`/`CSR_CDC_*`/`DDR_200_CFG 0x184` family), with an **empty intersection** — so **the reading a grep invites and the hardware refuses is "the vendor registers are in the second window"** (they are in the *first*, at `+0x10C`; the second is the *lower* address, which is why the guess goes the wrong way). **`core_mem` is also a name two nodes in the same file both use, for two different windows**: the neighbouring driverless `sdcc1: qcom,sdcc@f9824000` (`msm8974.dtsi:313-319`) calls its own *whole* window `core_mem` at the *same address*, and an alphabetic grep reaches `msm_sdcc.c` first with a *different* addressing scheme for the *same* silicon (`core_memres->start + MMCIFIFO`, `msm_sdcc.c:547`, region resolved by the same `platform_get_resource_byname(…, "core_mem")` at `:5901-5902`) — the two drivers agree on the register they both name (`MCI_VERSION 0x050` `msm_sdcc.h:175` = `CORE_MCI_VERSION 0x050` `sdhci-msm.c:139`, same word, same address, reached as `core_mem + 0x50` by both), and the legacy driver has **no Kconfig entry in this tree** (`drivers/mmc/host/Kconfig:461` declares `MMC_SDHCI_MSM` and nothing for `msm_sdcc`; neither cancro defconfig selects one), so **a node with no driver is still a document about the hardware**. **The mode bit, and it is not optional** (`sdhci-msm.c:2832-2868`, the probe's own `/* Reset the core and Enable SDHC mode */`): write `0` to `CORE_HC_MODE` (`:2845`), set `CORE_SW_RST` `(1<<7)` in `CORE_POWER` (`:2848`), poll it clear — 10 µs × 1000, the driver's own comment deriving **~40 µs** from 10 HCLK + 15 MCLK at hclk 27 MHz / mclk 400 kHz (`:2856`, `:2850-2855`) — then `HC_MODE_EN = 1` (`:2864`) and `FF_CLK_SW_RST_DIS` `(1<<13)` (`:2867`); the pending power interrupt is cleared *before* any interrupt is enabled (`:2877-2884`) and the mask only after the handler is registered (`:2946`) — so a first attempt that reads `CAPABILITIES 0x40` or `HOST_VERSION 0xFE` before this is **not** looking at a dead controller, it is looking at a block that is in legacy SDCC mode. **The window size is the top vendor register**: the Pro override widens `0x11c` → `0x1a0` (`msm8974pro.dtsi:1763-1769`), which is not a round number but `CORE_DDR_200_CFG 0x184` exclusive — `0x11c` covers `0x00`–`0x11B` (standard block, `DLL_CONFIG` `0x100`, `DLL_STATUS` `0x108`, `VENDOR_SPEC` `0x10C`, the two ADMA error words, **nothing from `0x130` up**) while `0x1a0` reaches the CDC block only HS400 tuning needs, matching `qcom,bus-speed-mode` ending at `DDR_1p8v` in the base node and beginning at `HS400_1p8v` in the override, with the clock table's top moving 200 MHz → 384 MHz; `0x11c` is not unique to 8974 (`msm8226.dtsi:795`, `msm8610.dtsi:350`). **Neither SDCC1 gate is a vote, and the discriminator is the clock's own struct *type*** (the type *is* the register set): `gcc_prng_ahb_clk` is a `local_vote_clk` carrying a `vote_reg` and an `en_mask` to spend (`clock-8974.c:2327-2337`; `clock-local2.h:126-134`), a `branch_clk` has neither field and cannot express a vote at all (`:99-109`), and both SDCC1 clocks are `branch_clk` + `clk_ops_branch` (`:2339-2348`, `:2350-2359`) — so the whole enable is two reads and one write on `SDCC1_APPS_CBCR 0x04C4` / `SDCC1_AHB_CBCR 0x04C8`, `\|= CBCR_BRANCH_ENABLE_BIT` `BIT(0)` then the halt check on **bits 31:28 of the same register** (`clock-local2.c:373-390`, `:333-371`, `:325-328`, ≤500 µs at `:36`); `SDCC1_BCR 0x04C0` is **defined twice and referenced nowhere** (`clock-8974.c:244`, `clock-8610.c:75`) — it is the value `branch_clk`'s real `bcr_reg` field ("block reset register", `clock-local2.h:92`, consumed by `__branch_clk_reset` `clock-local2.c:511-530`) *would* hold, and neither SDCC1 entry sets it; and `has_sibling = 1` is load-bearing for an **unrelated** reason (the `!has_sibling` fall-through at `:504-506` is why `list_rate` answers `-ENXIO`). **Two of the driver's quirks are statements about where a value lives, and both move it out of the obvious register**: `host->max_clk` is *not* `CAPABILITIES 0x40` — `sdhci.c:3270-3283` reads the base-clock field out of caps and **discards** it under `QUIRK_CAP_CLOCK_BASE_BROKEN` for `ops->get_max_clock`, which on msm is the device tree's `qcom,clk-rates` (`sdhci-msm.c:2236-2243`), so the identification frequency is the board's declared `400000` and the top is `200000000`/`384000000` (a register that **lies** is worse to program against than one that is zero); and the card-clock divider is *not* `CLOCK_CONTROL 0x2C` — `QUIRK2_ALWAYS_USE_BASE_CLOCK` writes `div = 0` into the divider fields (`sdhci.c:1282-1283`) because the rate comes from `clk_set_rate` on `sdcc1_apps_clk_src` (`sdhci-msm.c:2519-2522`), whose divider is `SDCC1_APPS_CMD_RCGR 0x04D0` — the spec's register, programmed zero, with the real value in the GCC, which is 530's **one value, two candidate definitions, and the register that looks right is the one that is not it**. **Bus power is a PMIC request, and the driver's quirk is a safety rule**: the deviations are the driver's own comment (`:2891-2895`, "Bus power control is handled by interacting with PMIC"), the mechanism is a second interrupt (`pwr_irq`, IRQ 138, requested at `:2929-2943`) and a request/acknowledge pair in `CORE_PWRCTL_CTL 0xE8` (`BUS_ON/BUS_OFF/IO_LOW/IO_HIGH`, `:67-75`) — and `SDHCI_QUIRK_SINGLE_POWER_WRITE` (`:2897`) makes `sdhci.c:1345-1357` skip the spec's clear-then-set, whose skipped arm writes `0` to `POWER_CONTROL 0x29` **and calls `check_power_status(host, REQ_BUS_OFF)`**, so on this SoC "clear power then set power" is a **request to the PMIC to power the eMMC down and back up mid-boot**; a hand-written reader writes that register exactly once and never zeroes it. **One register, two version fields**: `HOST_VERSION 0xFE` carries spec version in bits 7:0 and vendor in bits 15:8 — `sdhci.c:3177-3178` takes the spec field for every 3.00-only and preset-value path (`:1225`, `:1973`, `:2075`, `:2294`), while the msm driver takes the **other** field against `SDHCI_VER_100 0x2B` (`sdhci-msm.c:2909-2914`) and a match **adds two quirks** (`QUIRK2_SLOW_INT_CLR`, `QUIRK2_RDWR_TX_ACTIVE_EOT`, `:2915-2925`) — so one 16-bit read at `0xFE`, taken after the mode sequence, decides both. **The data path is the good news: PIO is reached by not asking for DMA.** `TRANSFER_MODE 0x0C` bit 0 clear (`SDHCI_TRNS_DMA 0x01`) transfers through `BUFFER 0x20` with readiness in `PRESENT_STATE 0x24` (`DATA_AVAILABLE 0x800` / `SPACE_AVAILABLE 0x400`) or `INT_STATUS 0x30` (`DATA_AVAIL 0x20` / `SPACE_AVAIL 0x10` / `DATA_END 0x02`), readable and clearable **without enabling a single interrupt** — the driver never takes this path, and not because anything requires DMA: `SDHCI_REQ_USE_DMA` is set from `host->flags` (`sdhci.c:843-844`) and those come from what `CAPABILITIES` **advertises** (`SDHCI_CAN_DO_ADMA2 0x00080000`), and **advertising is not an obligation** — the whole register set for a polled single-block read is `ARGUMENT 0x08`, `BLOCK_SIZE 0x04`, `BLOCK_COUNT 0x06`, `TRANSFER_MODE 0x0C`, `COMMAND 0x0E`, `RESPONSE 0x10`, `BUFFER 0x20`, `PRESENT_STATE 0x24`, `INT_STATUS 0x30`, with no ADMA list, no `ADMA_ADDRESS 0x58`, no IRQ and no GIC — i.e. 524's `while (!(read(status) & READY)); read/write(data);` shape again. **The first reading is self-verifying**: the cancro kernel declares GPT support (`cancro_user_defconfig:208` `CONFIG_MSDOS_PARTITION=y`, `:218` `CONFIG_EFI_PARTITION=y`, `lineageos_cancro_defconfig:37`), so the expected signature at **LBA 1** is the 8 bytes `EFI PART` with the MBR fallback at LBA 0 ending `55 AA` — one block read whose *value* proves the path, and whose partition-entry LBA/count/CRC fields are what the next step needs (530 decides *how* the payload presents a device; this decides *what* it presents). Open, and named rather than assumed: **the PMIC/power question** (if the rail can only be switched through the PMIC path, a reader polling `POWER_CONTROL` waits on the wrong register — but the bootloader read the payload off *something*, so the card may already be enumerated at handoff, which is what bounds the line); whether the mode sequence is a prerequisite or a re-do (the initial `CORE_POWER` bit 7, `CORE_HC_MODE` bits 0/13 and the CBCRs should be **read and logged**, a free reading); whether XNU needs a real SDHCI driver or the polled reader plus 530's `bdevsw` registration suffices; HFS+ vs a purpose-built read-only FS (529/530's trade, unchanged); and where the volume comes from (TWRP, withheld — the goal's precondition 「如果os已经能进去了的话」 is still unmet). The one hazard named: `POWER_CONTROL 0x29` written as 0 is a PMIC bus-off request. **No file outside `docs/` was written**; 526 stays built, gated, frozen and unrun, and the phone is off the bus and owes a power press. |
| stage90 | [experiment-530](experiment-530-the-root-device-is-already-wrapped-and-the-hfs-row-is-a-re-derivation.md) | **the payload can already supply the root device, and the HFS+ row is a re-derivation rather than a copy** — 529 §8's two open questions, answered from the built image. **The seam between the number and its use is nine instructions wide and contains no call at all**: `bsd_init` inlines `setconf()`, so the whole retry loop is twenty instructions at `0x80049b68`–`0x80049bac` (`mov r0,r4` / `r4 = 0x8055aed4` `rootdevice`, `mov r2,r5` / `r5 = 0x8055af2c` `&rootdev`, `sl = 0x8059ebec` `&mountroot`, `r8 = makedev(6,0)`, loop top `:b68`, `bl IOFindBSDRoot`, `beq` **success jumps the whole error arm**, `str r8,[r5]` `0x80049ba0` = `rootdev = makedev(6,0)` **error edge only**, `str fp,[sl]` `0x80049ba8` = `mountroot = NULL` **both edges**, `bl vfs_mountroot` `:bac`) — so 529 §8's second candidate point ("between `:940` and `:946`") is **not reachable by any wrapper**, because reaching it means *inserting* an instruction into Apple's `bsd_init`; the back-edge to `0x80049b68` is *after* the register setup, so `r4`/`r5`/`sl`/`r8` are loop-invariant and a retry re-enters `IOFindBSDRoot` with the same arguments; **and the supplier of `rootdev` is already wrapped** — `nm` gives `__wrap_mdevadd` `0x8047daac` and `__wrap_mdevlookup` `0x8047db1c` in the shipped image, and `*root = mdevlookup(xchar)` (`IOKitBSDInit.cpp:467`) *is* the `dev_t`, so a wrapper returning `makedev(payload_major, unit)` roots the system on a payload-owned block device with **no edit to `bsd_init` and no I/O Kit** — at the price of two readings that mislead: the console prints **`BSD root: md0`** while printing the *payload's* major (`rootName` is built from `rdBootVar[2]`, the boot variable, and the numbers from the `dev_t`), and **`rd=` is a precondition** whose absence is silent while its presence with a missing device **panics** (`:489`, no fall-through; the shape is `md<hexdigit>\0`, `:457-465`) — plus one hypothesis that dies: the retry loop does **not** leak memory devices, because the RAMDisk branch's `mdevadd(-1, …)` is behind `static int didRam` (`:346`, **file-local, unreachable from the payload**), four lines above the call. **The row is not copyable**: `struct vfstable` (`mount_internal.h:299` 2050 / `:303` 4570 — *not* `mount.h`, the "declared in the other file" shape) grew one **trailing** field (`vfc_sysctl`), 2050's HFS row has **12** initializers where 4570 wants **13**, and zero is *correct* for the new field because `vfs_init` writes it (`vfs_init.c:454`/`:456`) — so the trailing field is a reading hazard, not a port cost; **`sizeof(struct vfsops)` is unchanged** (2050's 14 named + `vfs_reserved[7]` = 21; 4570's 16 named + five named reservations = 21), 2050's positional table lands every named field correctly and leaves exactly the two reinterpreted slots (`vfs_ioctl` 15, `vfs_vget_snapdir` 16) NULL, and NULL there is a **refusal on an unreachable path** (`ENOTSUP` at `kpi_vfs.c:374`/`:385`; the only callers are the three snapshots at `vfs_syscalls.c:955`/`:11698`/`:11953` and `vfs_cprotect.c:296`); the flag word is where the copy **breaks loudly** — `VFS_THREAD_SAFE_FLAG` is a `#define` *inside 2050's own* `vfs_conf.c:76`/`:78` and appears **nowhere in 4570** (4570's NFS row is 2050's minus exactly that token), so a copied row is a **compile error**, not a silent difference; and the `0x020` collision is the latent one in the *dangerous* direction (`VFC_VFSDIRLINKS` 2050 → `VFC_VFSCANMOUNTROOT` 4570 — a copied row would silently become a mountroot candidate; 2050's HFS row does not set it, any *other* copied row does); **ordering is the array, not a comment** (`vfs_init.c:430` `break`s at the first NULL `vfc_vfsops`, `:432` `vfsconf[i-1].vfc_next = vfsp` — so a row after the two `{NULL, "<unassigned>", …}` sentinels is **never registered at all**, and `:459` calls `(*vfs_init)(&vfsc)` on **every** registered row, dereferencing the ops vector, which `hfs_init`'s `hfs_chashinit`/`hfs_converterinit`/`BTReserveSetup` + five lock groups must survive); **the port's size is the file list, not the directory** — 62 files / 36 `.c` / 25 `.h` / **68,285 LOC**, and `grep 'optional hfs$' bsd/conf/files` gives **36 lines selecting exactly those 36 `.c`** (21 top level, 7 BTree, 2 Catalog, 4 Misc, 1 Unicode, + `1 OPTIONS/hfs`) with `hfs_quota.c` the **one** conditional (`optional quota`, absent from `out/device_table.txt`) — and `hfs_quotactl` is in `hfs_vfsops.c:2349`, not in it; **the `#if HFS` guard answers to `OPTIONS/hfs`** (`SETUP/config/mkmakefile.c:342` is the only `OPTIONS/` recogniser, `:377-404` uppercases the name and sets `tdev.d_slave = 1` when matched → `mkheaders` emits **1**, not matched emits **0**; verified against `out/xnu_options/RELEASE/devfs.h` = `#define DEVFS 1`) so a port **adds `OPTIONS/hfs … optional hfs` to 4570's `bsd/conf/files`** or the row and all 36 files vanish **without a diagnostic** — and `config/MASTER` has no `hfs` line at all (the `OPTIONS/` line *is* the declaration, the 440 lesson with the roles swapped); the manifest this project links is generated **from those `*/conf/files` lists** (`tools/build_xnu_arm_kernel.sh:162` → `list_sources.py --write out/xnu_arm_manifest.txt`), so the port's file list is load-bearing here and not only in Apple's build; and the numbers punish the plausible tidy-up — **`VT_HFS` is 16 in *both* trees** (4570 `vnode.h:100-113`, under Apple's own `/* 16 - 20 */` marker) while the row's `vfc_typenum` is the **historic `17`** (`/* historic filesystem type number */`, confirmed by 4570's own `enum fs_type_num` carrying `FT_NFS = 2`/`FT_DEVFS = 19` rather than the `VT_*` tags; 2050's row says 19 for devfs while `VT_DEVFS` is 18, and the mismatch exists *inside* 4570 too), so the port **keeps 17** and adds `FT_HFS = 17` for free. Not decided: wrap vs row; the eMMC driver's shape (only its attachment is measured — `bdevsw_add(-1, &…)` `bsd_stubs.c:161` with its `index = 1` comment at `:125`, `cdevsw_add_with_bdev`, `devfs_make_node(…, DEVFS_BLOCK, …)` `memdev.c:607`, `makedev`, `NB_MAX_MDEVICES` **16** at `:125`); and whether `hfs_init` survives here. A host-side reading — **no device, no build, no switch** — written while **526 stays built, gated, frozen and unrun** and the phone is off the bus (owes a power press). |
| stage90 | [experiment-529](experiment-529-hfs-is-in-this-repository-three-times-over.md) | **HFS+ is in this repository three times over, and the missing link is not the filesystem** — 523 §3's "there is no HFS in this tree" is true of `external/xnu-4570.1.46` and false of the repository, which holds three more XNU trees (`xnu-2050.18.24`, `apple-xnu-rel-2050`, `xnu-upstream`), each with a `bsd/hfs` of 61 files / 68,285 LOC and a `vfstbllist[]:119` row `{ &hfs_vfsops, "hfs", 17, 0, …, hfs_mountroot, … }` — a real on-disk root filesystem where 4570 has no source, no row, no `FT_HFS` and no `OPTIONS/hfs`; what is genuinely absent is `IOMedia`/`IOStorageFamily`, present in neither the tree nor the repository (`find` for it and `grep -rl` for the class names both return nothing; `IOKitBSDInit.cpp:872-874` names them only as `serviceMatching` strings) — **and it is not on the required path**: `vfs_mountroot` (`bsd/vfs/vfs_subr.c:1040`) builds root's device vnode from the plain global `rootdev` (`bsd_init.c:237`, `systm.h:138`; set by `setconf` at `:940`/`:1111`, mount at `:946`) through `bdevvp` (`:1059`) with no I/O Kit input, `IOFindBSDRoot`'s other three exits being only one way to supply that number (`rd=mdX` unconfigured **panics**, `IOKitBSDInit.cpp:489`; the default exit is the unbounded `IOMedia`+`Apple_HFS` wait at `:530-566`); HFS+ is a BSD-level vnode filesystem — `#include <IOKit` appears nowhere in `bsd/hfs/`, only one comment at `hfs_vfsops.c:4834` mentions `IOMedia` — and its mount contract is three load-bearing ioctls (`DKIOCGETBLOCKSIZE` `:1326`, `DKIOCSETBLOCKSIZE` `:1364`/`:1413`, `DKIOCGETBLOCKCOUNT` `:1373`/`:1421`; `DKIOCGETPHYSICALBLOCKSIZE` tolerates `ENOTSUP`/`ENOTTY` at `:1343`) **all three already answered by the tree's only bdevsw** (`bsd/dev/memdev.c:391`, `:395`, `:407`, registered by `bdevsw_add` `:591` + `devfs_make_node` `:607`), with 9 of the 10 `DKIOC*` it uses already defined in 4570's `bsd/sys/disk.h` (only `DKIOCCSSETLVNAME` missing, one call site at `hfs_vfsutils.c:685`) — so the chain is **three** links (filesystem: in-repo, unported; eMMC block driver at SDHCI `0xf9824900` + core `0xf9824000`, IRQ 123/138: absent, 3,359 lines of vendored `sdhci-msm.c` before any XNU `bdevsw`; `IOMedia`: absent and unnecessary), not four; plus a hazard and a **fourth** link: `mockfs_mountroot` accepts **any** rvp (its own TODO at `mockfs_vfsops.c:75` names the `EBADMACHO` panic that costs, so "root is mockfs" is not evidence HFS+ was refused), and 4570's ordering comment "mockfs should always be the last standard entry" is printed **twice** (`vfs_conf.c:146` above mockfs, `:151` above **routefs**, which sits after mockfs and cannot mount root) so the marker a reader would place a new row by points at the wrong row — while **there is no volume at all** (no `*.dmg`/`*.ipsw`/rootfs anywhere; `find` returns only the phone's own firmware dumps and `out/stage*/stage*.img`), which the goal answers itself by creating one with TWRP (still withheld, precondition unmet). A reading of files already in the repository, **no device and no build**, written while **526 stays frozen, gated and unrun**. |
| stage90 | [experiment-528](experiment-528-the-console-uart-is-f991e000-and-its-gate-is-a-vote.md) | **the console's UART is `0xF991E000`, and its clock gate is a vote rather than the CBCR.** A host-side reading (no device, no build, no switch) that closes 524 section 5's open address and 527 section 6's named limit as far as the tree can, and names the half only the device can answer. **The base, from four witnesses**: `msm_iomap-8974.h:41-42` defines `MSM_DEBUG_UART_PHYS 0xF991E000` (and the sibling `msm_iomap-8084.h:39` says the same), `serial0 = &blsp1_uart1` in all four 8974 board dtsi files, `msm8974-mtp.dtsi:24-26` turns `serial@f991e000` to `status = "ok"` in the one board dtsi carrying `Copyright (C) 2015 XiaoMi, Inc.` (the three Xiaomi board files carry no uart line and inherit it), and the node itself (`msm8974.dtsi:253-265`, `reg = <0xf991e000 0x1000>`, `interrupts = <0 108 0>`) - so the winner among 524's three `qcom,msm-lsuart-v14` instances is chosen by the SoC's own header, not by documentation. **The label that must not be used to find the gate**: the node is labelled `blsp1_uart1` and it is BLSP1's **UART2**, said three ways inside this tree - the node's own bus property `qcom,msm-bus,name = "serial_uart2"` (`:259`), the clock lookup keyed by **address** (`CLK_LOOKUP("iface_clk", gcc_blsp1_ahb_clk.c, "f991e000.serial")` `clock-8974.c:4962` and `("core_clk", gcc_blsp1_uart2_apps_clk.c, "f991e000.serial")` `:4982` - the driver's device name is built from `reg`, so the label plays no part), and four other SoC clock files pairing the same way (`clock-9625.c:1791`, `clock-krypton.c:1848`, `clock-samarium.c:3459-3460`, `clock-8226.c:3293`: `f991d000`<->uart1, `f991e000`<->uart2, `f991f000`<->uart3) - with the two number series agreeing, the address map stepping 0x1000 per instance and the GCC 0x80 (`0x0684`/`0x0704`/`0x0784`), and `CLK_LOOKUP("core_clk", gcc_blsp1_uart1_apps_clk.c, "")` (`:4981`) naming no device because this dtsi instantiates no node at `0xF991D000`. So the core gate for `0xF991E000` is **`BLSP1_UART2_APPS_CBCR = 0x0704`** (`:356`), and a reader who takes the label writes `0x0684` and the console stays dark. **The two gates**: `iface_clk` is `gcc_blsp1_ahb_clk`, a **`local_vote_clk`** (`:1766-1775`: `.cbcr_reg = BLSP1_AHB_CBCR 0x05C4`, `.vote_reg = APCS_CLOCK_BRANCH_ENA_VOTE 0x1484`, `.en_mask = BIT(17)`, `.ops = &clk_ops_vote`) whose enable is `local_vote_clk_enable` (`clock-local2.c:600` = read `VOTE_REG`, OR the mask, write, then halt-check the CBCR; `VOTE_REG(x)` is `*base + vote_reg`, `:56`) - i.e. **set bit 17 of `0xF9011000 + 0x1484 = 0xF9012484`** (`APCS_GCC_CC_PHYS 0xF9011000` `:5776`), and the "obvious" write to bit 0 of `0xFC4005C4` **is not the enable for this clock**: its CBCR is a status the vote drives; `core_clk` is `gcc_blsp1_uart2_apps_clk` (`:1921-1930`, `.cbcr_reg = BLSP1_UART2_APPS_CBCR`, `.ops = &clk_ops_branch`) whose enable is `readl; |= BIT(0); writel` (`clock-local2.c:374`, `CBCR_BRANCH_ENABLE_BIT` `:62`) at **`0xFC400704`**, parent RCG `0x070C` (`+0x4` CFG div/src/mnd-mode, `+0x8` M, `+0xC` N, `+0x10` D, `clock-local2.c:50-53`/`:68-70`, `set_rate_mnd` `:127`, freq table `F(7372800, gpll0, 1, 192, 15625)` `:1066`) - and **the rate is the one thing the payload must not touch**: a baud rate is the pair (RCG rate, the UART's own divisor, which the bootloader set), so enabling a branch is safe and reprogramming the source without knowing the divisor is not. A third CBCR per instance (`BLSP1_UART2_SIM_CBCR 0x0708`) is defined for all twelve and bound to no clock struct in the file. **What the tree cannot answer**: the pins - `find arch/arm/boot/dts -name '*pinctrl*'` returns nothing and no dts defines a uart pin group, so the pinmux is the bootloader's and unstated; the tree gives only where it would be read (`MSM8974_TLMM_PHYS 0xFD510000`, `msm_iomap-8974.h:34`, and `GPIO_CONFIG(gpio) = MSM_TLMM_BASE + 0x1000 + 0x10*gpio`, `gpio-msm-v2.c:89`, over the **virtual** base - the offset is what transfers). **And cancro's own kernel does not use that UART as a console**: `cancro_user_defconfig` has `# CONFIG_MSM_SERIAL_DEBUGGER is not set` (`:402`) and `# CONFIG_SERIAL_MSM_HSL_CONSOLE is not set` (`:1774`) with `CONFIG_ANDROID_RAM_CONSOLE=y` (`:3109`), while the reference `msm8974_defconfig` compiles the HSL console in (`:311`) - so 524's vendored `debug_putc` is a *reference* for the register sequence, not a driver this phone boots, and the physical console is a channel *added* for 527 section 8's reason (it outlives a hang), not one restored. The instrument this specifies, and it is a **read**: the six BLSP1 UART CBCRs (`0xFC400684 + 0x80*n`), the AHB CBCR (`0xFC4005C4`) and the APCS vote word (`0xF9012484`), and the TLMM config words (`0xFD510000 + 0x1000 + 0x10*g`) - all in always-on blocks, so it can wedge nothing, and it decides whether the bootloader left the console branch running at handoff (the write of 527 section 6 can then be tried first, bounded) or the payload must make the two writes above, in that order, before its first UART access. Specified here, **not scheduled ahead of 526**, which is frozen, gated and unrun and whose ledger a different run would leave unread. |
| stage90 | [experiment-527](experiment-527-the-console-text-is-already-captured.md) | **the console's text is already captured - the sink that is off is the DCC.** A host-side reading that retires 524 section 4 and recategorises the console item 523 section 2 put first: 458/459's taps sit **in front of** both sinks, so the characters the sinks discard are written into a reserved 128 KB block of the RAM console - 520's log carries XNU's own boot output verbatim (`Darwin Kernel Version`, `BSD root: md0, major 2, minor 0`, `load_init_program: attempting to load /sbin/launchd`, and the register dump and `MACH Reboot` that ended the run) at byte offset **296022**, which is exactly the published block base `xnu_live_ostext_at=0x00048456`, the host dump beginning at the RAM console's data area, with the instrument's own `\n[os-console-459]\n` marker in front of it. The counters close arithmetically: `_chars = _total = 0x3ee` (1006), `_heals = 1`, `_tank = 0`, `_block = 0x20000`, and the marker is **18** bytes, so 18 + 1006 = **1024** = `ENTRY_OS_HEAL` exactly - the value is a **snapshot at the heal**, not a total, and the block's own 1995 bytes are the total (a whole boot to `MACH Reboot` prints ~2 KB, 64x under the block). The route is read, not assumed: `xnu_live_console_kputc = 0x800ad3cc` is **`cnputc`**, not `serial_putc` (`0x8003d978`), so **`serial_init()` returned 0** in this boot, and `noserial = 1` (`disable_serial_output`, still `TRUE` by default), `noconout = 0`, `opsidx = 0`, `dbgcnt = 0` (**no panic in the run**); the text went `cnputc -> _cnputs -> vcputc -> __wrap_vcputc -> entry_os_console_char(c, 1)`, and both sinks' gates (`uart_initted`, `gc_initialized` - `b` locals) are read with a `cmp`/`bne` out, so the capture is gate-independent **by construction**. The sink that is off is Apple's ARM **DCC**, devirtualized: `uart_putc` (48 bytes) is `if (uart_initted) { while (arm_debug_read_dscr() & 0x20000000); mcr p14, 0, r4, c0, c5; }` - `0x20000000` is `ARM_DBGDSCR_TXFULL` (`proc_reg.h:950`), `mcr p14,0,..,c0,c5` is `write_dtr` (`pe_serial.c:162`) - and `nm` on `pexpert_arm_pe_serial.o` lists **only** `serial_init`, `uart_putc`, `uart_getc`, `uart_initted` (`text 421, data 0, bss 1`, sole sink reference `U arm_debug_read_dscr`): **there is no `gPESF`** - `pe_serial.c:39`'s static pointer and its table were constant-folded away, which `serial_init`'s own disassembly confirms (`strb r5, [r0]` into `uart_initted` with no table store beside it). So 524 section 4's "two global writes" are refused **three times**: no `gPESF` exists to write; `uart_initted` is a `b` local; and setting it would arm a **CP14 DCC** sink - `while (arm_debug_read_dscr() & TXFULL)` on a read no run has ever reached (unmeasured), so a clear bit 29 spins forever inside `kprintf`. The seam for a real console is already occupied: `__wrap_uart_putc` (`0x8047d808`) calls `entry_os_console_char` with the character in `r4` and then **tail-calls Apple's inert sink**, and `serial_putc` (`pe_kprintf.c:131`) is a four-byte `b 8047d808` - so the physical console is one polled write in the payload's own wrapper (524's vendored `debug_putc`, `SR 0x0008`/`TF 0x000C`), leaving Apple's sink alone. Two smaller readings: a counter published on a schedule is not a total (1006 vs 1995), and `entry_stubs.c:2550`'s citation of `serial_putc` at `pe_serial.c:813` names a blank line - the definition is `pe_kprintf.c:131`. Nothing touched: no build, no switch, no device; the phone is off the bus and owes a power press. |
| stage90 | [experiment-526](experiment-526-the-null-instrument.md) | **the idle window's readings, replaced by a counter - a metrology null.** 522's run produced a ledger rather than a reading, and its third column is the reason this arm exists: 521 and 522 both carry 521's repair of the exit wrapper's capture (the four words of the idle exit's `{fp, lr}` slot read by the caller at each of two sites - 8 loads and 8 stores a pass into `g_slot_pre`/`g_slot_post`'s `pend_*` words, at word offsets 32/36/40/44) and **neither came back**, while 520 - which carries neither that capture's shape nor any state change - did, and 521 and 522 differ from each other only in *which* state change they carry (the exit-side `FlushPoC_Dcache` and the enter-side `SCTLR.C` re-enable). So the apparatus and the treatment are confounded and 521 section 4's reading ('flag-off runs come back, flag-on runs do not') is already falsified by 522; the way to separate them is to hold the treatment and remove the apparatus, which is what a null instrument is. **`STAGE90_XNU_SLOT_NULL=1`**, off by default, is the arm: the two capture sites become two calls to `entry_slot_null_note`, which publishes the pass count on the same `<= 4`-then-powers-of-two schedule and behind the same `entry_live_ready()` gate and nothing else - so a null run's log has `xnu_live_slot_pre_calls`/`_post_calls` present with the four word keys and `_sp` **absent**, which says the site ran and published no words, and is exactly the distinction 519 section 11's decision rule rests on (a null that called the real publisher against a stale table would republish the previous pass's words as this pass's). **Nothing else moved**, and that was checked rather than intended: the same tree built with `STAGE90_XNU_SLOT_NULL=0` reproduces 522's frozen image `13d771938336fe65...` **byte for byte**, so the two images differ by the capture and by nothing else - the check `mi4-off-option-two-spellings` asks for, done the only way that cannot be fooled. The image read off the disassembly: wrapper `0x8047c974` (522's own address), `str r4, [sp, #-8]!` + `str lr, [sp, #4]` (the frame that *is* the slot), **one** `mov r3, sp` at `0x8047c97c` kept deliberately so the two arms can be compared instruction by instruction, `bl entry_slot_null_note` at `0x8047c988` and `0x8047c9a8`, 519's `entry_slot_rtc_note` at `0x8047c998`, the real `platform_cache_idle_exit` at `0x8047c99c`, `b entry_note_pcx` at `0x8047c9d0`, and between them **0** negative-offset loads and **0** slot stores where 522 has eight of each - image `7819cddb50d5a341332aa89d4b6f0479e098e1a19f7d46cc9a2df3a1875ff3e4`, entry bin 5,519,996 bytes (521's and 522's size), payload `1c9491195e94c091...`, a second build of the same switch reproducing the hash. The clause was **refusal-tested against the state it exists to refuse**: its own two `awk` programs, run verbatim against 522's capture-bearing wrapper, count `entry_slot_note` **2** / `entry_slot_null_note` **0**, **8** negative-offset loads at `16 12 8 4` twice, and **8** stores at `32 36 40 44` twice - so every null predicate fires on the image it must reject and none fires on the one it must accept. Two defects came out of that, both in the check and both stopped by the build: the null count was computed by an `awk` forty lines *below* the assertion that reads it (so the first build refused a correct image on `${sxw_null:-0}`), and the pre/post straddle assertion was still 520's unconditional `first < real < last`, which with no `entry_slot_note` calls read `0 < x < 0` - it is now the arm's own question, two null sites straddling the call, because one site or two on one side would be a different instrument whose count could not say whether a pass reached the call. **The run is owed, not done**: the device has been off the bus since 522 and needs a power press. What to read, written before it happens: (1) **does it come back** - a return says the readings cost something and the next arm bisects them (the two loads before the call, or the two after it), a third non-return says the cost is the state change 521 and 522 do not share; (2) the counts present with the words absent; (3) 522's `--summarise` block still fires on this image's `xnu_live_slot_cwe_*`, so `_win` `C` clear / `_set` set / `_calls >= 1` are read from the same log; (4) 520's `sleh_storm 9` at the same `pc` would say the mechanism is neither the capture nor the re-enable. Gate green (`7819cddb50d5a341...` computed from the file). Unchanged by this step: the OS's root is still an 8 KB Mach-O stub with a mockfs fixture behind it, so 「挂载存储」 has not begun, and a non-return still takes the log with it - which is the phase's real cost and 523's console item's real reason. |
| stage90 | [experiment-525](experiment-525-pid-1-runs-and-the-entry-pc-is-the-program.md) | **pid 1 runs - the entry pc is the program's first instruction, and its faults are its own page's first touches** - a reading, not a step: no image, no switch, no device. It re-reads 520's own log (`/tmp/cancro-last_kmsg.txt`, 596 665 bytes / 8 919 lines, ending `No errors detected`; provenance checked - `xnu_live_slot_rtcpre_pop=0x04b79075` with `_calls=1` and `_thr=0xc0573df0` is 520's, and there is no `_cwe_*`, no `_post_*` and no `_rin`, so it cannot be the 522 build) and **retires a claim 523 §4 and the phase notes both carry**: *"`__TEXT` has fileoff 0, so pid 1 executes its own header and faults at `0x1118`/`0x1124`/`0x11a4`"*. **`pc = 0x10e0` is `entry_code`** - `entry_ramdisk.s:1276-1277` defines `sizeofcmds_value = (load_commands_end - g_stage90_ramdisk) - 28` (asserted `== 0xC4` at `:1284`) and `entry_pc_value = TEXT_VMADDR + (entry_code - g_stage90_ramdisk)` with `TEXT_VMADDR = 0x1000` (`:938`), so 28 + 0xC4 = 224 = 0xE0 puts the code exactly where the commands end - and the log agrees from the other side: `xnu_live_getpid_seq=1`, `value=1`, `error=0` at the listing's `+0 svc #0x80`, because **an instruction inside the load commands cannot make a syscall**. What pid 1 actually did is the finding: **the whole 79-instruction program ran to completion** - `getpid -> 1`; `mmap(0, 0x1000, 3, 0x1002, -1, 0x5a5a, 0)` `error 0`, `value 0x00102000`; `poll(NULL,0,5)` then `poll(NULL,0,40)` (`retval 0`, `ticks 0x000d5976`); `open("/dev/rmd0")` -> **`fd 0`**; `read(0, 0x102000, 4)` -> `ret 4` with **`word_before 0x00102000` -> `word_after 0xfeedface`** and `copy_before=0`/`copy_after=0`; `open("/dev/nosuch")` -> **`error 2`** (the control); `fork` -> pid 2; the child `exit(3)`; `wait4(2, 0x102000, 0, NULL)`; a SIGCHLD 0->1; then the second `wait4`. The two user faults are **the mmap'd page's own demand faults, serviced**: `far = 0x102000 = xnu_live_mmap_value` for both, `pc 0x1118 = entry_code+56 = ldr r3,[r0]` and `pc 0x1124 = +68 = str r0,[r0]`, and the tree's own `proc_reg.h:263-283` decodes them as one access's two halves - `fsr 0x007` = `FSR_PFAULT` (read) and `fsr 0x80f` = `DFSR_WRITE \| FSR_PPERM` (write) - so the fault register and the disassembly agree independently of the two `pc` values. That they were retried is proved twice: the program continues to `poll_seq=1`, and `word_before` is the value `str r0,[r0]` left behind, read back through the kernel's own copy path. **The tree's linker wall is also mis-stated**: `tools/build_xnu_arm_macho.sh:203-218` refuses with *"no Mach-O linker on this host"* and *"`/usr/lib/llvm-14/bin` ... has no `ld64.lld`"*, but `/usr/bin/ld64.lld-14 -> ../lib/llvm-14/bin/ld64.lld` (Ubuntu LLD 14.0.0), and the script's own discovery loop (`:56`, run verbatim) resolves `MACHO_LD` to it - so `exit 3` at `:217` is dead code. But **no linker is missing and armv7 still will not link**: lld links a one-instruction armv7 Mach-O (exit 0, `Mach-O armv7 executable`, `MH_EXECUTE`, `ARM/V7`) and **refuses the moment the object has a reference in it** - `VANILLA relocation must be extern at offset ... of __TEXT,__text`, `width 1 bytes, but must be 0 bytes`, exit 74 on **both** 14.0.0 and 15.0.7 - which **reproduces experiment 150** (*"`ld64.lld` ... CANNOT link armv7 Mach-O"*; 150 names the message `unhandled relocation type`, the message differs, the conclusion does not, so 150 stands). The second wall is independent and decides the pid-1 question: **lld's Mach-O driver cannot emit a *static* executable** (`-static` = *not yet implemented*; output always carries `DYLDLINK TWOLEVEL PIE` + `LC_LOAD_DYLINKER`), which is why `entry_ramdisk.s` hand-builds its header - so 523 §4's *"a real static ARMv7 Mach-O"* arm is **already done and already runs**. Left open on purpose: **where pid 1's stack came from** - the Mach-O writes `sp = 0` (`:1014`) so `thread_userstack` (`osfmk/arm/status.c:585-593`) returns `USRSTACK = 0x27E00000` (`bsd/arm/vmparam.h:10`), yet the log's user `sp` is `0x00101efc` and the kernel's stack `copyout` lands at `far 0x00101f28` with the page above at `0x102000 = 0x2000 + 0x100000`, i.e. `create_unix_stack`'s `VM_FLAGS_ANYWHERE` fallback (`kern_exec.c:4938-4952`) with exactly `MAXSSIZ` - and nothing in the 596 KB log records `user_stack_alloc_size`, the map's bounds or that call's `kern_return_t` (no `0x27D...`/`0x27E...` address appears at all). `seq 7` (the run's only user-mode prefetch abort, `pc = far = 0x11a4 = fork`'s resume point, thread `0xc05ae480`) is named but **not decided**. The ninth entry is the unchanged death at the idle exit's `pop`. Does not change 522 (built, gated, frozen, unrun; the verdict is still the panic's absence and the boot going *past* the idle loop) and does not reorder the phase - the console (523 §2, 524) is still the drivers milestone's first item. |
| stage90 | [experiment-524](experiment-524-the-polled-putc-the-console-needs-is-already-in-the-tree.md) | **the polled putc the console needs is already in the tree** - a reading, not a step: no image, no switch, no device. It answers the question 523 §2.3 left open (*"whether the `ln2410` accessors can be bent to drive \[the UARTDM\] is an open question"*) and the answer is that nothing needs bending, because **this repo has vendored the MSM8974 UART register map and a polled writer for exactly this chip** in `external/android_kernel_xiaomi_cancro/`. The correction that matters: there are **two** Qualcomm windows, not one - the **low-speed** one the debug console uses (`UART_SR 0x0008` / `UART_TF 0x000C` / `UART_CR 0x0010`, with `0x0008` and `0x000C` each being *two registers decided by direction*: `CSR`/`SR` and `TF`/`RF`) and the **UARTDM** one 523 described (`UARTDM_TF 0x0070`). And `msm_serial_debugger.c:103-112` already contains the table: `debug_putc` = `while (!(read(SR) & TX_READY)); write(c, TF);` is `pe_serial_functions.td0`, `debug_flush` = `while (!(read(SR) & TX_EMPTY));`, `debug_port_init` (`:57-91`, `CR` resets + the four divisor registers `MREG/NREG/DREG/MNDREG` + `CSR = 0xFF` + `CR = 0x05`) is `uart_init`, `debug_getc` (`:93-99`) is `rr0`/`rd0` - and `ln2410_uart_init`'s own comment, *"No interrupts, No DMA - just polled"*, is the same mode. **`serial_init` re-disassembled with its literals read out of the ELF by address, not by string search**: `0x80492399`="dcc", `0x8049239d`="boot-console", `0x804923b5`="uart0", `0x804923bb`="uart1", `0x804923aa`="compatible", `0x804c7c59`="reg", `0x804b1e3e`="name" - 523's table confirmed - and between the `compatible` fetch at `0x8003dba4` and `uart_putc` at `0x8003dbbc` there is **no `strcmp`, no `gPESF` store and no `uart_init` call**, so the `#ifdef S3CUART` block is compiled out by disassembly as well as by `nm` (`pclk`, `ubrdiv`, `uart,16550` all absent). Two consequences: **`uart_putc` needs no change** - its body (`pe_serial.c:813-819`) is already `if (uart_initted) { while (!gPESF->tr0()); gPESF->td0(c); }`, so "the driver is running" is exactly two global writes, `gPESF = &table` and `uart_initted = 1`; and the **`dcc` branch is proof the shape works** (`0x8003dae0`-`0x8003daf0` sets `uart_initted` and returns 1, with **no `gPESF` store**, so an arm could get *the console* by pointing `gPESF` at a table whose `td0` is the payload's own sink - an instrument, not the milestone). The address is in-tree too: `msm8974.dtsi`'s `serial@f991f000` (`:239`), `blsp1_uart1: serial@f991e000` (`:253`), `uart7: uart@f995d000` (`:2394`), `qcom,msm-lsuart-v14` (`:240`) / `qcom,msm-hsuart-v14` (`:2395`) - though **not** as a `compatible` the image could match, since the `strcmp` chain is Apple's and compiled out; the node needs a string a *new* `#if` compares. Its own first pass was a defect it caught: `strings \| grep -F dcc` returned 0 because **`strings` defaults to a minimum length of 4**, so a 3-character literal is invisible - `mi4-measurement-defects` gains the rule **before concluding a value is absent, establish that the extractor could have seen it.** Does not decide that the low-speed window is wired to the Mi 4's debug connector (a measurement needing the device), and does not reorder 523 §6's static-pid-1-vs-filesystem question. |
| stage90 | [experiment-523](experiment-523-the-console-and-the-root-are-wired-and-inert.md) | **the console and the root are wired and inert** - host-side scope for the drivers milestone, written while 522's run is owed. Two "supply, not subsystem" findings, both read out of 522's image and the tree rather than argued. **The console chain is complete and the last link is off:** `kprintf` -> `PE_kputc` (`0x8055a0d4`) -> either `serial_putc` (`0x8003d978`, `b __wrap_uart_putc`) or `cnputc`, both of which end in the payload's `entry_os_console_char` (`0x80004560`), and the one call that would reach a wire is Apple's `uart_putc` (`0x8003dbbc`), whose whole body is `if (uart_initted) { wait; write; }` with `uart_initted` (`0x8055a104`) zero and nothing in the boot setting it. `PE_init_kprintf` (`pe_kprintf.c:38-41`) is the fork, and `serial_init` (`0x8003daac`) has exactly two ways to return 1: the **`dcc` boot arg** - which in this image is `mov r5, #1; strb r5, [uart_initted]; return 1`, the only path that can succeed here - or a **device-tree node**, which `serial_init` looks for as `boot-console`, else `name = "uart0"`, else `uart1`, then `reg` -> `ml_io_map(soc_base + reg[0], ...)` and `compatible`. **The payload's device tree declares no such node at all** (`xnu_real_dt.c` has no `serial`/`uart`/`compatible` text). And a node alone would not be enough, because the strings that select a driver are **absent from the image**: `uart,16550`, `uart-16550`, `uart,s5i3000`, `uart-1,samsung` (-> `ln2410_serial_functions`) and `uart16x50,mmio` all grep zero, so the `strcmp` chain and both tables are compiled out and `pe_serial.c`'s `#else return 0` is what remains - a file whose own header says *"Polled-mode UART0 driver for **S3c2410 and PL011**"*, and MSM8974 is neither (it is the Qualcomm UARTDM, a command/data register split, not a 16550 window and not a PL011 one). The I/O Kit side is **not** the blocker: `/dev/console` already exists as `makedev(0,0)`, `0622` (`devfs_vfsops.c:113-115`) with `tty`/`null`/`zero`, and `bsd_init` mounts devfs at `/dev` (`bsd_init.c:1019-1026`). What is missing is a `pe_serial_functions` for the UARTDM (or a measurement that `ln2410`'s accessors drive it), the `compatible`, the `#ifdef` that puts the branch in the image, and the `boot-console` node with its offset - after which `PE_kputc = serial_putc`, the payload's two wrappers stop being load-bearing, and the driver is *running* by the only definition that matters here (the output is not the payload's buffer). **The root, and the storage clause:** `vfs_conf.c:123-158`'s `vfstbllist` has a non-NULL mountroot field for exactly one entry - `mockfs_mountroot` - and devfs/routefs/nullfs carry NULL with `NFSCLIENT` off, which is why the OS mounts at all and why it mounts a fixture; and **there is no HFS in this tree** (no source under `external/xnu-4570.1.46`, no entry in `bsd/conf/files` - Apple's HFS is a separate component). So **a storage driver is not sufficient** for「挂载存储」: the phone's userdata is ext4/f2fs, neither is in XNU, and mounting it needs an MMC/BlockStorage path **and** a filesystem - the second being the larger. **What pid 1 can run:** `mach_loader.c:623-636` refuses a static `MH_EXECUTE` unless `DEVELOPMENT || DEBUG` and a dynamic one needs `CS_DYLD_PLATFORM` (`:1173`), i.e. Apple's signature - and `STAGE90_XNU = [ RELEASE mockfs development ]` already takes the first branch deliberately, so the infrastructure for a real pid 1 is in the image and unused while the RAM disk holds an 8 KB **header stub**. The small testable arm is a real static ARMv7 Mach-O as `/sbin/launchd` that writes to the console from the first section. Two obligations the cache walk leaves for this phase: `ml_arm_sleep` **stores with the cache off** (`bcopy(suspend_signature, IOS_STATE, 8)` at `0x8000e090`) and becomes reachable as soon as a suspend-capable driver exists, and 522 re-enables the D-cache **outside the ACTLR coherence domain** - free on one CPU with no DMA, a correctness property the moment a second exists. |
| stage90 | [experiment-522](experiment-522-the-idle-window-gets-its-cache-back.md) | **the idle window gets its cache back.** 521's non-return left one state change standing - the full L1 *and* L2 clean-and-invalidate executed *inside* the window with `SCTLR.C = 0`, which is the only operation in the image this hardware has never survived - so 522 stops needing it: `STAGE90_XNU_EXIT_POC_FLUSH` is **0** and the D-cache is switched back on at the window's *near* end, `entry_idle_cache_enable` (24 bytes: read `SCTLR`, `orr #4`, write, `dsb`, `isb`) called from `__wrap_platform_cache_idle_enter` the moment the real `platform_cache_idle_enter` returns (`0x8047c924` -> call at `0x8047c92c`), before the WFI. That makes the exit path a coherence property rather than a maintenance operation: the `push {fp, lr}` write-allocates and stores the dirty line, Apple's own L1 `DCCISW` writes it back to the Point of Unification - which on this CPU **is** the L2 - so the `pop {fp, pc}` that 519 and 520 died in misses L1 and hits an L2 holding the *pushed* word, and `caches.c:490`'s `SCTLR.C` write becomes a no-op instead of a race. This differs from what 521's section 5 predicted (the enter side `FlushPoC_Dcache` in place of `CleanPoC_Dcache`), and section 1 records why: repairing the copy still leaves the wrapper's `strd` and the window's cache-off store arguing about which one the `pop` reads. Two defects, both in the checking: objdump's `orr` immediate is operand field **6** (the mnemonic takes a field, which `ldr` does not have), and - the one that matters - **the first 522 image was not 522**: `/tmp/stage90-521-resume.sh` still defaulted `STAGE90_XNU_EXIT_POC_FLUSH` to 1, so the build was green, the 517 clause passed (it asserts the image agrees with the *flag*, which a flag-on image does), and the image carried the flush that had just failed to come back *plus* the enable - two state changes, unattributable. It was caught by reading the wrapper's disassembly, where `bl 80045828 <FlushPoC_Dcache>` sits at `0x8047c97c` as the exit wrapper's first call. Rebuilt green at flag 0: image `13d771938336fe65...`, entry bin 5,519,996, `.text` +96, `.bss` byte-for-byte 519's again, 44 keys over 7 tables with `entry_window_note:3`. Gate green (it printed `13d771938336fe65...`, computed from the file), **run on 2026-09-22 after 521's owed power press brought the phone back as Android (`2717:0368` -> `18d1:4ee7`, `adb devices` = `4a2fe00b device`): one non-persistent `fastboot boot`, `Sending 'boot.img' (8340 KB) OKAY`, `Booting OKAY`, nothing flashed - and the device did not return within 180 s and has not returned since. The host's own USB log carries 521's signature to the second (`usb 3-10`: dev 88 = `18d1:d00d` at 14:14:45, disconnect at 14:14:46, nothing after), so the payload took the core and never came back; the log is lost with the top of DRAM and a power press is owed. **This is the project's third non-return and it falsifies 521's ledger reading**: 521 section 4 had it as *flag-off runs come back, flag-on runs do not*, and 522 is flag-**off** by construction - so the flag was never the discriminator, and what 521 and 522 share and 520 does not is the capture/publish path 521 introduced (8 loads and 8 stores per pass into `.data`, inside the window). All four of this arm's readings are therefore absent - `xnu_live_slot_cwe_win`, `_set` `_calls`, and `post_calls` have no value - and the next arm is the **null instrument** that 521 section 5 and this log's section 3 both named in advance: the same wrapper with the readings replaced by a counter, to separate the measurements' cost from the state change's. The design was then read off the image rather than argued (section 1.1): `platform_cache_idle_exit` is `push {fp, lr}` -> `bl FlushPoU_Dcache` -> ... -> `orr #4`/`mcr` at `0x80046324` -> `pop {fp, pc}` at `0x8004633c`, so **520's `pop` did not run with the cache off - it ran with the cache ON**, three instructions after the window's clear was undone, which is exactly why it *hit* the stale line instead of missing it; and this image's only job is therefore the exit's `push`, whose store then lands in the L1 line the flush cleans into the L2 before the pop. Reading `caches.c` against the image then pinned two things the arm's safety rests on (section 1.2): Apple's exit **rejoins the coherence domain (ACTLR bit 0x40) before re-enabling the cache**, so 522 does the reverse order *inside* the window - unobservable on this uniprocessor (the only agent that could miss a dirty L1 line outside the domain is the core that made it, and Apple's own `DCCISW` cleans it to the PoU one call later) but **a real hazard the moment a second CPU or a live DMA master exists, i.e. the drivers milestone**, recorded as that step's obligation; and `cpu_CLW_active` is *not* cleared in this window at all, because `caches.c:421` is in the else branch of `caches.c:414` and `up_style_idle_exit && real_ncpus == 1` takes the if branch - which is also why the enter's own clean is `CleanPoU_Dcache()` (L1 only) and the pre-window line survives **valid in the L2** for 520's trap to read. The exit's `InvalidatePoU_Icache`/`flush_core_tlb` pair are likewise **not executed** here (the `if (!up_style_idle_exit || (real_ncpus > 1))` guard, visible in the image as the two-global test with the `bcc` skipping both calls at `0x80046300`), so the only D-cache operation between the `push` and the `pop` is Apple's own `FlushPoU_Dcache`. The arm's central claim - *this image has no store of its own inside the cache-off window* - was then made structural rather than a remark (section 2.1): `xnu_entry_522` now asserts the `_win` SCTLR read is the instruction immediately after the call that opens the window and the enable's call the instruction after that (`0x8047c928`/`0x8047c92c` against `realaddr 0x8047c924`), and that `entry_idle_cache_enable` holds **0** stores, so the image's whole presence inside the window is one `mrc` that writes no memory plus six bytes of control-register write; the say line now *names* the window at 140 bytes of Apple's own tail (`0x80046244`..`0x800462d0`) where before this arm it spanned essentially the entire idle path. Both assertions were refusal-tested before the build was trusted (a spliced store counts 1 and fails; every displacement of the two addresses is refused) and the rebuild is **byte-identical** in all three artifacts - which is what a clause-only edit should be, and is the evidence that the image the gate approved is the image the clause describes. 520's own captured log then settled what is on the far side of the `pop` (section 2.2), and it matters for how the run is read: that image booted the **whole** path - `BSD root: md0`, MD device attached, `launchd` attempted twice, pid 1's thread staged at user pc `0x10e0` with user sp `0x101efc` (`ast_pid = 1`) - and then **pid 1 executed in user mode**, with aborts taken in user mode at user addresses `0x1118`, `0x1124` and `0x11a4` (`sleh_user = 1`) on that user stack, before `sleh_storm 9` killed the machine at the idle exit's `pop`. So the frontier is not "reach the OS" but "survive the first idle pass the OS reaches *after* its first user-mode seconds" - the distance to a running boot is the 140-byte window this arm closes. A census then closed the remaining question - *does anything read the bit this arm sets early?* - and it is short enough to state in full (section 1.3): **Apple's ARM layer never tests `SCTLR.C`**. The bit is written exactly twice (cleared at `caches.c:393`, set at `caches.c:490`) and read only for modification; the one accessor that hands the register out, `get_mmu_control`, has six callers and all of them are in `arm_init` manipulating `SCTLR_PAN_UNCHANGED`/`SCTLR_PREDIC`; and `cpu_idle_wfi` itself (`0x800172dc`..`0x80017330`) is `dsb` + `wfi` with arithmetic on registers only, touching **no memory**, so what can run under the halt is an interrupt handler - which reads Device memory (uncached by *attribute*, not by `SCTLR.C`) and Normal kernel memory, and the only Normal-cacheable locations any other agent writes here are the `cpu_data` fields of a cross-CPU protocol that a uniprocessor never runs. So 522's early write cannot alter a decision anywhere in Apple's code, including in a handler taken during the halt; what it can alter is timing and the coherence of this image's own stores, which is what the two readings measure. A census of *who opens the window* then turned up a second opener (section 1.4): `platform_cache_disable` has **three** call sites, not one - the idle enter's, plus **two inside `ml_arm_sleep`** (`0x8000dfc0`), where the boot-CPU branch disables the cache and then **stores with it off** (`bcopy(suspend_signature, IOS_STATE, 8)` at `0x8000e090`, then an infinite spin). That is Apple depending on exactly the semantics this walk has assumed, and the same shape of store 520 died in. It is **dead code today**, checked rather than assumed: no `bl`/`b` to `0x8000dfc0`, no pointer to it in `.data`/`.rodata`, and its name lives only in the ELF `.strtab`, which the payload does not copy - so the arm's claim holds as written, while `ml_arm_sleep` becomes reachable the moment a platform driver that can suspend exists, i.e. at the drivers milestone, where it carries the same question in a path whose answer cannot be "the cache is off for 140 bytes and all of them are Apple's". The four criteria are now read **mechanically** rather than by eye (section 3.1): `run_and_capture.sh --summarise` prints 522's verdict, and the block is *self-selecting* - it fires only when the log carries `xnu_live_slot_cwe_*`, so it appears for this image's run and never for another step's - with **PASS / FAIL / UNREAD** as three separate states because "the key is absent" and "the value is zero" are readings about different things. It was run against four logs before being trusted (520's real capture -> the block is absent; a pass-shaped log -> all three PASS; a death-shaped log -> verdict FAIL; a sparse log -> UNREAD, no crash), and two defects came out of that exercise, both in the reading and neither able to reach a hardware run: an extraction whose `grep` status ends the caller under `set -e`/`pipefail` when a key is absent (`|| true` is load-bearing - the same shape as a defect four lines above it in the same file), and shape tests written in decimal against values the live channel writes as `0x%08x`, which made every present key read as UNREAD. A third piece of host-side work while the device is off the bus asks what "the OS" actually is on the far side of the `pop` (section 3.2), and the answer reframes the goal: the root device is **an 8 KB 32-bit Mach-O stub** (`g_stage90_ramdisk`, `0x80511000`, `.data`, parsed out of the image: `MH_EXECUTE`, `__PAGEZERO` + `__TEXT` vmaddr `0x1000`/vmsize `0x1000` with `fileoff 0`, and an `LC_UNIXTHREAD` whose **`pc = 0x10e0` is exactly pid 1's user pc in 520's console line**) - so pid 1 begins executing *inside its own Mach-O header*, which is why 520's user-mode faults are at `0x1118`/`0x1124`/`0x11a4` and end at `far = 0x102000`, the page past `__TEXT`. And the filesystem serving it is **mockfs**, whose own comment says it builds *three nodes* - a directory, a devfs mountpoint, and one file assumed to be an executable - so `load_init_program`'s second candidate `/sbin/launchd` *succeeds* against a fixture. The OS therefore boots, mounts, execs and schedules pid 1 for real, with **nothing to run and nowhere to run it from**: no init, no userland, and no on-disk filesystem in this configuration (only `devfs`, `mockfs` and `routefs` have ops vectors; none can read a real partition). So "mount the storage" is a phase that has not begun, and after 522 the next work is a decision about what the OS's root is going to be - not another cache fix. The same work also named the first item of the drivers milestone (section 3.2.1): 520's boot output carries exactly one console and it is **not an I/O Kit one** - `[os-console-459]`, which is the payload's own cmdline token, so every kernel `printf` travels through this stage's shim, and `bsd_init` completed with no `IOConsole`/UART/`AppleARMPL011` service, no `IOService` match producing `/dev/console`; the I/O Kit the image carries (`IOService`, `IOBSD`, the catalogue and registry machinery) is a **framework, not a driver**. The two things the kernel did *not* need a device for are the two it did: `md0` comes from the platform shim's `RAMDisk` path and pid 1 from a fixture. |
| stage90 | [experiment-521](experiment-521-the-repair-ran-and-the-device-did-not-come-back.md) | **the repair, run - and the device did not come back.** 521 is 520's section 11 as one arm: `STAGE90_XNU_EXIT_POC_FLUSH=1` (517's exit-side `FlushPoC_Dcache`, added at this site and never run on its own) plus the four instrument repairs 520's run exposed. Built green on image `00c630e9639bc18b...` (entry bin 5,519,996, `.text` +4,840, `.bss` byte-for-byte 519's again because the five key tables are initialised and live in `.data`), with the wrapper's frame asserted at **8 bytes** - the capture is staged in the table's own words (`entry_slot_capture.h`) rather than passed in registers, because a probe compiled ahead of the build showed gcc emitting a 16-byte frame for the register form and a 16-byte frame moves the slot out from under the enter wrapper's `strd r4, [sp, #-12]!`, i.e. it would have stopped testing 520's mechanism while every surface stayed green. The clause also asserts 8 loads at `[sp-16/-12/-8/-4]` with the first group *before* the first publication and the second *after* the real exit, and 8 stores at word offsets 32/36/40/44 - its own first version said 4 and refused an image that was right (the shape asserted was one site's, and the macro runs at two), the build catching a defect in the check rather than the code. Gate green (it printed `00c630e9639bc18b...`, computed from the file), one non-persistent `fastboot boot`, nothing flashed - and **the device did not return within 180 s and has not returned since**: `adb` lists nothing, `lsusb` still shows the bootloader's fastboot gadget, and `fastboot getvar` does not answer, so a power press is needed and the log is lost. **This is the project's second non-return and the second flag-on image**: every flag-off run since 506 came back (518's, 519's and 520's each ending in XNU's own `MACH Reboot`), the hardware watchdog recovered 506-515 and recovered neither flag-on run, and 517's first run - which also carried this flush - needed a power press too. So the state change attributes where 517's could not: the prime suspect is now **a full L1 *and* L2 clean-and-invalidate executed inside the idle window with `SCTLR.C = 0`** - the one operation in the image that has never run on this hardware, and the piece 516 never exercised (the two-loop body it verified is the **enter** side's, cache on). The mechanism does not need maintenance in the window at all: the stale valid line is created before it, by the enter wrapper's `strd` plus 516's `CleanPoC_Dcache` (`DCCSW` - clean only, leaves the line valid). **522 is therefore the enter side cleaning *and invalidating* (`FlushPoC_Dcache` where the enter wrapper calls `CleanPoC_Dcache` today) with the exit-side flush back at 0** - one state change, none of it inside the window - and if that also fails the next arm is a null instrument, to separate the cost of the readings from the cost of the flush. |
| stage90 | [experiment-520](experiment-520-the-two-words-of-the-idle-exits-slot.md) | **the two words of the idle exit's `{fp, lr}` slot, watched - and the run says what the `pop` read.** 519's §10 proved by arithmetic that no exception frame can write above the `sp` it interrupted, so the writer had to be *observed*; 520 is 519's §11 as an arm and nothing else (no state change, seven live-channel keys per site, published at `<= 4` then the powers of two). Switch set is 519's unchanged. Run on `4a2fe00b`: one non-persistent `fastboot boot`, the device back on its own, `25c4bc7d...`, 139 `xnu_live_slot_*` records. **The death reproduced at the same instruction** - `sleh_storm = 9`, `sp = 0x8054fed0`, `lr = 0x800462dc`, `user = 0`, `frame_ok = 1`, numerically identical to 519's. **And the identity is now a number:** `xnu_live_slot_rtcpre_pop = 0x04b79075` (read in the exit wrapper immediately before the call that never returned - the post reading is absent, `_post_calls = 0`) while `pc = 0x04b79074 = rtcPop - 1`, the Thumb-address relationship 519 read off its own `r4`/`far` pair. `lr = 0x800462dc` is `platform_cache_idle_exit`'s `bl FlushPoU_Dcache` (`0x800462d8`) return address and `bcc` at `0x80046300` is taken, so the faulting instruction is that function's own `pop {fp, pc}` at `0x8004633c` (a `pop` does not touch `lr`, which is why the panel reads the way it does). The only store of `cpu_idle`'s `r4` (`lastPop` = `rtcPop`) into that word is `strd r4, [sp, #-12]!` in `__wrap_platform_cache_idle_enter` (`0x8047c8d4`) and `__wrap_cpu_idle_wfi` (`0x8047c888`): pre-indexed by 12 from the same entry `sp` the exit wrapper's 8-byte frame lands on, so `r4` goes to `E-4`, **the slot's upper word**. **The exit's own `push {fp, lr}` writes that word and does not win, because the window is open:** the store runs with `SCTLR.C = 0` and reaches DRAM, while the line is still **valid** in the L1/L2 from the `strd` (516's enter-side `CleanPoC_Dcache` is `DCCSW` - clean only, c7,c10,2 - which writes the deadline to DRAM and leaves the line valid), and `caches.c:490` sets `SCTLR.C` again before the `pop`, whose load **hits** the stale line. Apple's own `FlushPoU_Dcache` is `DCCISW` but **L1 only**. So the repair is `FlushPoC_Dcache` (`DCCISW`, L1 **and** L2) - **517's exit-side call, which is in the image behind `STAGE90_XNU_EXIT_POC_FLUSH` and has never been run on its own.** Reading 4 answers its own question: `ml_get_timebase` was called >= 1024 times, **517's instrument published no `xnu_live_tb_*` record at all** (its early return on `SCTLR.C` set), and its own pair spans **exactly 24 ticks** with the return strictly between - so it is not the source of a 1-tick pair. **Three defects in this arm's own instrument, all measurement-class:** the two slot readings were read *through `entry_slot_note`'s own frame* (`pre_m4 = 0x8047c974` is the note's own saved `lr`, the instruction after its `bl` at `0x8047c970` - defect 394 from the other side); the sampler **skipped the fatal abort** (`seq 9` is neither `<= 4` nor a power of two, and its `SS_SP = 0x8054fed0` was inside the guard); and the two pinned read windows refuse this run's real stacks (`0xc820bf14`), with the rtc note's inner refusals uncounted so `sp = 0`/`lr = 0` at every abort cannot be told from "never read". Also fixed before the run: **the gate's XNU-entry prose carried 519's hash as a literal** and went on reporting it for 520's image - the gate now computes and prints the file's own `sha256`. |
| stage90 | [experiment-519](experiment-519-the-idle-thread-gets-its-own-stack.md) | **the idle thread gets a stack of its own, and the collision stops being invariant.** 518's run showed the arm could not be a value of `cpu_data->istackptr` - one field, two readers (`locore.s:1361-1362` for the handler, `cswitch.s`'s `Idle_context` for the idle body), so `SS_SP == istackptr - 16` moved with it. 519 changes the *stack* instead, in the one place the choice is made: `tools/patch_idle_stack.py` rewrites `Idle_context`'s `ldr sp, [r12, CPU_ISTACKPTR]` into a hand-written non-slidable `ldr sp, [pc]` + `.word stage90_idle_stack + 16384` (`LOAD_ADDR(sp, EXT(x))` is rejected by LLVM as the variant `L_EXT(x)`), bounded to `Idle_context` - `Shutdown_context` keeps `ldr sp, [ip, #4]`, because it wants the interrupt stack on purpose - and the idle body runs on a 16 KB `.bss` array of this image's own (`0x8054bee0..0x8054fee0`) while the handler keeps the whole interrupt stack. **The run that failed to falsify 518 identifies the instruction**: `SS_SP = istackptr - 16` is exactly `Idle_context`'s `sp = istackptr`, then `cpu_idle`'s `sub sp, sp, #8` and the exit wrapper's 8-byte frame, and no other path produces it (the boot's `sp` is `intstack_top - 80` from `start.s:311`, and `machine_idle` runs on the idle *thread's* stack) - and the exit side is untouched, because `Idle_load_context` restores `sp` from `TH_KSTACKPTR`, which is the correction to 518's own §5 proposal of wrapping `__wrap_Idle_load_context`. **The second effect is a property of two listings**: `ml_at_interrupt_context()` reads the *current* `sp` (`machine_routines.c:669`) and `prefabt_from_kernel` (`locore.s:900-963`) builds its frame on the interrupted stack and calls `sleh_abort` without switching, so for a kernel-mode abort the predicate is a test of the *faulting* code's stack - on the array it answers false, which turns 516/517/518's `panic: sleh_abort at interrupt context` into the diagnosed `sleh_abort: prefetch abort in kernel mode: fault_addr=0x...` with the register panel. The reading is `entry_idle_stack_note` (six `xnu_live_idlestack_*` keys: `sp`, the array's top, both fields the kernel's predicate could be about, the predicate itself, and the call count), taken from `__wrap_platform_cache_idle_enter` - the first C on the stack `Idle_context` chose, once per pass, before the D-cache goes off - and **the switch is the control**: at `=0` the same keys report 516's arrangement, `sp` in the interrupt stack with `inwin` equal to `calls`. Two defects were found on the way and both are in the *checks*: the patch tool's first run wrote XNU's tree (`assemble_arm_layer.sh`'s mirror is files that are **symlinks** into it; writes now go through `os.replace`), and **518's flag-off image had never been built and did not build** - `STAGE90_XNU_ISTACK_SEPARATE=0` left `want` unused under `-Werror` and 518's own clause then refused a correct body, so 518b's "`=0` restores 517b's arrangement" was a claim no build had tested. Image `40bf8a0bc8cccc33f7e1bd765304364d99807319c006ce774f9394ebc34b66bc`, `.text` 5,305,256, `.bss` 379,696, entry image 5,519,996 (unchanged - the array lands above the pinned `__bss_start` the copy span ends at). **The run (2026-09-22, one non-persistent `fastboot boot` through the gate, nothing flashed, 596,958-byte log / 4,264 `xnu_live_*` records, device back on its own) obtained both predictions, and prediction 2 in its second branch**: `xnu_live_idlestack_sp = 0x8054fea8` is inside the array with `_inwin = 0` and `_calls = 1`, so the idle body left the interrupt stack; and the fatal abort is now the *diagnosed* `panic(cpu 0 caller 0x80454584): sleh_abort: prefetch abort in kernel mode: fault_addr=0x7152a6c` - `ml_at_interrupt_context()` answering false, which 516/517/518 could never reach - with `frame_ok = 1`, `fsr_frame = 5`, `sp 0x8054fed0`, `lr 0x800462dc`, `pc = far = 0x07152a6c`. **The live chain and the panic's `sp` agree from two sides** (array top 0x8054fee0, `cpu_idle`'s `sub sp, sp, #8`, the exit wrapper's 8-byte frame ⇒ the exit's entry `sp` is 0x8054fed0), and **the panel is the idle path's own context one instruction before the `pop`**: `r0 = 0x8051a000`/`r1 = 1` are the exit's `str r1, [r0, #0x130]` operands, `r5 = cpu_data` and `r4 = 0x07152a6d` are `cpu_idle`'s (`ldm r6, {r4, r7}` with `r6 = cpu_data + 0xe0`, i.e. `rtcPop`), `lr` is the exit's post-`bl` return address reachable only through the `bcc` that skips both later `bl`s - so the two words the `pop` read are two counter readings one tick apart, the later one equal to the deadline the idle loop held. **§10 makes 518's mechanism unreachable by arithmetic rather than by arm**: `EXC_CTX_SIZE = 80 + 264 + 16 = 360` (`genassym.c:188`, and the image's own `sub sp, sp, #360`), the VFP area is placed at `align16(sp+80)+16` and is 264 bytes, so it never ends above the interrupted `sp` - no exception frame writes above the `sp` it interrupted, and the slot is 8 bytes of *slack* the frame reserves and leaves alone, which is why the panel's fields identify as the idle code's own. The step's conclusion is therefore a measurement and not a mechanism: the writers the image's idle path contains (`push {fp, lr}`; two `strd r4, [sp, #-12]!` prologues that store `cpu_idle`'s r4/r5 at the slot's *upper* word; `entry_note_pcx`'s `strd r8, [sp, #16]` on the slot; the exit wrapper's single-word `str`) are enumerable and none writes two successive counter readings - so the next arm *watches the slot* (the two words before and after the exit's call, the same two at the abort, `__wrap_ml_get_timebase`'s own readings, and `rtcPop` with the idle thread's saved pcb `sp`/`lr`) and the decision rule is written into it. |
| stage90 | [experiment-518](experiment-518-the-arm-moved-both-stacks-so-the-collision-was-invariant.md) | **the arm moved both stacks, so the collision came back unchanged - and the frame it was meant to convict was authentic.** 518's `STAGE90_XNU_ISTACK_SEPARATE` ran on hardware (`32a513bc...`, gate green, device returned on its own inside the capture window, 599,995 bytes of log, 3,932 `MI4IOS6_STAGE90` lines, 4,263 `xnu_live_*` records) and **could not test its hypothesis**: `xnu_live_irq_istackptr` moved to `0x80516000` and every stack address in the panic moved down by exactly `0x2000` (`saved state` `0x80517e88`->`0x80515e88`, `sp` `0x80517ff0`->`0x80515ff0`) - because the idle code's stack top and the handler's stack top are the *same field*: `istackptr` is read by the vector to place the handler's stack (`locore.s:1361-1362`) **and** by the context switch to place the idle thread (`cswitch.s:182, 205`), so `SS_SP == istackptr - 16` before and after the change and the geometry is invariant. The identical panic that returned is therefore *predicted by the invariance*, not evidence about the clobber. **And the frame is authentic, which falsifies 516's reading of it**: the step's own abort instrument (`xnu_live_sleh_*`, real hardware values, not a spill) shows `pc == far == 0x04e1ae60`, `sp == frame + 360` (the identity the vector writes), `fsr == 5`, `frame_ok == 1`, and the panic's live panel agrees - so `SS_PC`/`SS_STATUS`/`SS_VADDR` were the vector's own words, not the handler's spills. **The damage is one word higher**: `SS_LR = 0x800462dc` places the interrupted code in `platform_cache_idle_exit` whose only indirect transfer in its 0x6c bytes is the closing `pop {fp, pc}` at `0x8004633c`, and the `pop` pins its own slot by `sp` arithmetic (`SS_SP` is the post-pop value) to `[istackptr-24, istackptr-16)` - **exactly the handler's 5th and 6th pushed words**, holding two counter readings one tick apart (`0x04e1ae61` and `0x04e1ae60`, and the panic's `r11` is the first of them), where the function had pushed `{fp, 0x8047c8dc}`. So the collision is real and lands on the idle code's saved return address, one word above the frame's top word - which is why the frame looked clean. The value is *fresh*: the run's own `xnu_live_wfi_after` is `0x04e1ae7b`, 27 ticks above the fetch address. Two of the step's readings were unpublishable by construction - `xnu_live_tb_*` never appears at all (`entry_note_timebase_call` returns early whenever `SCTLR.C` is set, and the totals are printed only by the console epilogue, which a fatal run never reaches) and `xnu_live_istack_before` is written only at `g_istack_moved == 1`, i.e. before the channel is up - the same class as 461's: *a reading that exists only on a path the death never reaches*. The store itself did take effect twice, with `A`/`I`/`F` clear in SVC mode (`_cpsr1`/`_cpsr2`), so 518b's ordering question is answered: not early enough to precede every interrupt. **The next arm cannot be another value of `istackptr`** - it has to make the two stacks different memory at once, and the hook already exists (`__wrap_Idle_load_context`, `0x8047c794`, called from `cpu_idle` at `0x8000d46c`): point the field at a separate region while the real context load runs and restore it after, which both separates the stacks and takes the idle code **outside** `[intstack_top - 16384, intstack_top)` so that `ml_at_interrupt_context()` answers false and a recurrence is a recoverable abort instead of Apple's `panic: sleh_abort at interrupt context`. |
| stage90 | [experiment-517](experiment-517-the-step-was-two-changes-and-its-first-run-did-not-come-back.md) | **the step was two changes, and its first run did not come back** — 516's two owed halves (the interrupt frame measured from inside `ml_get_timebase`, via a `--wrap`; the exit's `FlushPoC_Dcache()` before the L1-only `FlushPoU_Dcache` at `caches.c:460`) went into one image, the device booted it non-persistently and **never re-enumerated** — no `MACH Reboot`, no panic, no watchdog bite, no log, ~15 minutes of empty `adb`/`fastboot`, a power press needed and the ram_console lost with the cold boot. So the run is unattributable and the step is split: the state change is now `STAGE90_XNU_EXIT_POC_FLUSH`, **default 0**, asserted against the wrapper's own flush count in the build. The instrument is complete and green host-side (20 `xnu_live_tb_*` keys, `_datap` beside `_frame` so 516's `_pcx_datap` is a comparison, the stir's store address computed from `EntropyData` so `_hit` can say whether it lands in the frame) — **and reviewing it turned up a defect that would have cost the run: the guard admitted a frame only inside `intstack_top − INTSTACK_SIZE`, and `locore.s` builds the saved state in one of two places neither of which is the interrupt stack (the thread's PCB at `TPIDRPRW + ACT_PCBDATA` from user mode, the interrupted SVC stack carrying the identity `frame[SS_SP] == frame + 360` from kernel mode), so every ordinary call would have published `_frame = 0`. The guard is now those two facts (`xnu_live_tb_kind`/`_cand` say which one and what was refused), and the same host-side reading decoded 516's panic: its abort was inside `FlushPoU_Dcache` called from `platform_cache_idle_exit` — the call this step's flag-on arm duplicates — with the fetch faulting at a timebase (`0x0723c1f8`) 29 ticks below the value the log's own post-`WFI` record reads, and `sp` 16 bytes below `intstack_top`, which is also what `ml_at_interrupt_context()` tests for — the panic's "at interrupt context" is a statement about `sp`, and this kernel's boot and idle loops run on the interrupt stack (`start.s:310` is the image's only `ldr sp, <intstack_top>`), so a boot-time abort is labelled an interrupt-context one) — and reviewing it found a real defect before it cost a run: `ml_get_timebase` is the first wrapper here on a per-**interrupt** path, `entry_live_write` spends one of 8192 records for the whole run and 516's log already holds 4258, so the writes are now bounded by construction (8 per-call records, then one refresh per 256 calls). Two of the three build failures were the check rather than the image (an operand class written `r[0-9]+` where objdump spells r12 `ip` per instruction — a latent defect 516 also shipped; and a stray `)` riding into a number the build published, which shape guards now prevent). **And the step's real finding is about the net**: the hardware watchdog recovered 506–515 (including `WFI`-parked and abort-storm runs) and 516 returned on XNU's own `MACH Reboot`, and this run is the first it did not — so `preflight_boot_check.sh` and `docs/reference/recovery-and-rollback.md` §4a no longer claim it always brings the phone back. Wrap census 76, `.text` 5,298,816, `.bss` 363,248, entry image 5,519,996, 129 fixture mutations refused, `xnu_entry_failures = 0`. **518 makes the collision an arm**: `fleh_irq_kernel` builds its frame below the interrupted `sp` and then takes the handler's stack from `cpu_data->istackptr`, which the kernel writes `= intstack_top` and never adjusts — and this boot runs the OS *on* that stack (`start.s:310-311`), so the handler's first spills land in the frame's own fields (frame+52..72 is 304–324 bytes down from `istack_top`, and 516's `SS_PC`/`SS_STATUS`/`SS_VADDR` are exactly the words a handler that deep would have overwritten, with a *timebase* in them — the handler calls `ml_get_timebase`). `STAGE90_XNU_ISTACK_SEPARATE` (default 1) moves the handler's start to the middle of the 16 KB stack, which is both the candidate fix and the safer arm, since the instrument that would measure the collision adds stack use to the very handler that is deep enough to reach the frame; flag-on `32a513bc…`, with the store landing from `__wrap_PE_init_platform` *before* `PE_init_platform(TRUE)` brings the GIC up and both stores' `CPSR` in the log, because `arm_init.c:226` overwrites the store the first call makes; flag-off `=0` restores 517b's arrangement. |
| stage90 | [experiment-516](experiment-516-the-write-back-reaches-the-point-of-coherency-and-the-halt-is-reached.md) | **the write-back reaches the Point of Coherency, and the halt is reached** — 515's discriminator answered the good way, and a whole layer of the idle crossed: `xnu_live_pce_after_datap = 0x8051a000` read with `SCTLR.C = 0` (`xnu_live_pce_after_sctlr = 0x30c57879`, bit 2 clear) **is the value the cache-on read gave**, with `xnu_live_pce_tpidrprw == xnu_live_pce_after_tpidrprw` (0xc04750d0 / 0xc05feb10 by run), so neither half of `getCpuDatap()` changed under the window. The build check found why the first attempt could not work: **on this CPU the Point of Unification is the L2**, `CleanPoU_Dcache` (`0x800457a8`, one `DCCSW` loop) reaches only that far and Apple's own up-arm clean at `caches.c:415` is that function, so the wrapper calls **`CleanPoC_Dcache`** (`0x8004575c`, the same loop plus a second over the L2's geometry — 2 `DCCSW` loops against 1, which is the whole of this step, counted in the image via a `next_global()` that must skip local labels and require a *strictly* greater boundary because `clean_mmu_dcache` shares `0x8004575c`). Run 1 with the PoU form is kept as the discarded arm: `sleh_pc = sleh_far = 0x20400000`, `sleh_lr = 0x80046278` = the `b` after the enter's own `CleanPoU_Dcache`, i.e. the return-address slot itself written. With the PoC form the enter **returns**, **the halt is reached for the first time in this walk** (`xnu_live_wfi_seq = 1`, `_inst = 0xe320f003`, `_ticks = 0x0005aefc` / `0x0006eded`, i.e. 19.4 ms and 23.4 ms of real sleep), and the run then ends in Apple's own panic instead of 515's 64-record storm: `panic(cpu 0 caller 0x804542dc): sleh_abort at interrupt context (saved state:0x80517e88)` — `ml_at_interrupt_context()` is `sp ∈ (intstack_top − INTSTACK_SIZE, intstack_top)` (`machine_routines.c:669-677`) and `trap.c:312-313` is what refuses a fault taken there. `lr = 0x800462dc` = **`platform_cache_idle_exit + 8`**, the instruction after that function's first act `bl FlushPoU_Dcache`, **identical in both runs**; `sp = 0x80517ff0` (the istack, whose top the image declares `L_intstack_top = 0x80518000`) and `cpsr = 0x80000093` also identical; and `pc = far` is **the CPU's own timebase** — 28 and 29 ticks below that same run's own `xnu_live_wfi_after`, so it scales with uptime where a code address could not. The register dump is the interrupt frame's own `r0..r12`, because `return_from_irq` (`0x8001ab10`) → `load_and_go_sys` (`0x8001a860`) ends `ldr lr, [sp, #60]` (SS_PC) / `ldm sp, {r0-r12}` / `movs pc, lr` (`0x8001a944-0x8001a94c`) — so the pc restored is whatever that slot held, while the dump's `lr`/`sp` are the SVC *banks* the return switched into. `r4 = r11 = pc + 2` in both runs and is owed an explanation. Image `.text` 5,296,832 → 5,297,664, entry image 5,519,996 unchanged, wrap census 75, 129 mutations refused, `xnu_entry_failures = 0`; the discarded PoU arm, the two objdump spelling defects (`cr1, cr0`, and `--wrap` being a linker-side fiction) and the `awk exit`/`head` SIGPIPE are recorded. 517 is owed the frame-slot measurement (`[cpu_data + 0xb0]` is the frame pointer between `locore.s:1403` and `:1433`, so a `ml_get_timebase` wrapper can read `SS_PC` and compare it with the timebase it is returning) and the exit-side mirror repair. |
| stage90 | [experiment-515](experiment-515-the-branch-moved-and-the-interrupt-inside-the-window-reads-the-same-null-cpu-data.md) | **the branch moved, and the interrupt taken inside the window reads the same null `cpu_data`** — 514's wall, repaired and *counted*: **`up_style_idle_exit=1`** in the payload's `CommandLine` and in the tree's `/chosen/boot-args` copy (`arm_init.c:287-289` parses it into the `boolean_t` at `:103` that `caches.c:414` tests), spelled `name=value` because a bare token is matched only with a leading `-` (`pexpert/gen/bootargs.c:98-115`), and **the field had 255 of its 256 bytes**, so three tokens whose only readers are stage 85-88 sources (`live-pmap`, `tlb-live`, `pmap-restore`) went rather than truncating the flag off the end - and `_Static_assert(sizeof(cmd) <= BOOT_LINE_LENGTH)` now turns the silent tail-drop `build_boot_args` performs into a build failure. `--wrap`s at **both ends** of `platform_cache_idle_enter` (**75** wraps: 63 by a branch, 1 same-object, 1 never called, 10 by address), each reading with the cache **on** - the enter's before the real call, the exit's after it re-enables `SCTLR.C` (`caches.c:490-495`) - so the branch is counted and not inferred. **The repair took, and the log says it twice**: `xnu_live_pce_seq=1` from `_caller=0x8000cf24` = `cpu_idle+248` (the `bl` at `0x8000cf20`) with `_up=1`, `_ncpu=1`, `_datap=0x8051a000` (BootCpuData) - and **`sleh_far=0x130` and `sleh_pc=0x80045290` occur in neither run**, because the `else` branch does not run: the kernel's *own* cache-off read of the test takes the up arm, which is the witness that counts. **And the boot stops one layer deeper**: `xnu_live_pcx_seq` and `xnu_live_wfi_seq` are absent in both runs, so the fault is *inside* the window - an interrupt taken with `SCTLR.C = 0`, whose handler is Apple's own IRQ prologue, which reloads TPIDRPRW and calls `ml_get_timebase()` to stir the entropy pool (`locore.s:1421-1423`), faulting on `ldr r2, [r3, #88]` with `r3 = 0` (`machine_routines_asm.s:988-989`): `sleh_pc=0x80017058` = `ml_get_timebase+36`, `sleh_far=0x58`, `sleh_lr=0x8001aae8` inside the entry image's `locore_fleh_irq`, `sleh_cpsr=0x60000093`, `sleh_thr=0xc056c480`, `xnu_live_irq_spurious_count=1` - the **same NULL `getCpuDatap()`** (`cpu_data.h:79`), now read asynchronously, after which the abort handler's own first load faults at `0x8` and `sleh_seen` runs to its cap of 64. **And the window is measured by the payload's own counters, which recede**: `g_sleh_seq` (`0x80544afc`, one writer, `entry_stubs.c:1641`) runs `1..8` then **`4`**..64 in both of this image's runs where 514's ran `1..8` then `1..64` - a read-modify-write against the memory copy is the only way that number can go down, so the DRAM copy held **3** where the channel was publishing **8**, and since the up arm's `CleanPoU_Dcache()` (`caches.c:415`) would have written the 8 back (making the first in-window abort publish 9), the interrupt arrived **between `platform_cache_disable()` and that clean** - 514's instrument-inside-the-window hazard, now a measurement rather than a caution. Both runs carry the same readings to the byte (logs 572179/571845), **device back on Android on its own both times** (`MI 4LTE`), nothing flashed; `.text` 5292096 -> **5296832**, image 5519996 (unchanged: the copied span ends at the pinned `__bss_start` 0x80543a80), `.bss` 363120 -> **363184**, `up_style_idle_exit` **0x8054d104**, `real_ncpus` 0x80520378 (1 in the image), payload sha256 `5b24486a...`, 129 fixture mutations refused. The halt is **still not reached** - `poll_seq` 1 and 2, three `mini4:` lines, no `xnu_live_poll_over` - so the frontier moved *into* the window the `wfi` sits in; it did not cross it |
| stage90 | [experiment-514](experiment-514-the-repair-opens-the-door-and-the-idle-s-own-cache-path-reads-a-null-cpu-data.md) | **the repair opens the door, and the idle's own cache path reads a null `cpu_data`** — 513's three candidate repairs, the first taken: **one call to the kernel's own `cpu_signal_handler_internal(FALSE)`** (`cpu_common.c:386`, the `hw_atomic_and(&cpu_signal, ~SIGPdisabled)` at `:402`), made from `__wrap_poll` on pid 1's thread *before* the park's real call so the kernel's own `getCpuDatap()` answers and not this file's guess, with the counter read either side (12 and 13 ticks in the two runs); a `--wrap=cpu_idle_wfi` (0x8047b4b4) turning 513's *bound* on the halt into a count, whose record is written **after** the call and not around it; and the park's counters snapshotted at its start so the two new console lines report a window instead of a cumulative total. **The repair took, and the census is the witness**: the idle/door exit series stops at `0x8000` (32768) where 513's ran to `0x01000000`, and `SetIdlePop` — entered **0 times** in every 512/513 run, in the whole boot — is entered **once** at `0x8000cce0` (this step's own call site, `bl` at `0x8000ccdc`) and answers **TRUE** with `_en = 1`, so the decrementer was armed. The door's `_lr` is `0x8000ccf0` on all 16 records in both runs because inserting the second wrap made gcc's SetIdlePop-FALSE path *fall through* into the same `mov lr, pc`, so **in this image one exit site is three doors** and the door comes from `SetIdlePop`'s census, not `_lr` (513's `branch + 8` rule still reads right: `0x8000cce8 + 8`). **And the boot then stops four instructions short of the halt**: `pc 0x80045290` = `platform_cache_idle_enter+0x58` = `str r0, [r4, #304]` = `cpu_data_ptr->cpu_CLW_active = 0` (`caches.c:421`) with `fsr 0x805` (write, translation fault, section) and `far 0x130`, i.e. **`r4 = 0` — `getCpuDatap()` returned NULL**; the same expression read 300 instructions earlier in `cpu_idle` (`0x8000ccb4`) was non-NULL, proved by absence (no fault at `0x8000ccd0`/`0x8000cd0c`/`0x8000cd10`, addresses `0x28`/`0x11c`/`0x18`, and a door taken 32768 times is a real `cpu_signal`), and the only thing that changed is upstream of the read: `platform_cache_idle_enter` clears `SCTLR.C` (`proc_reg.h:498`) first and *then* reads `getCpuDatap()`, with `FlushPoU_Dcache()` **after** it, so the load reads DRAM where the field is still `machine_thread_create`'s 0 (`pcb.c:130`) while the switch's write (`pcb.c:103`) is still dirty in the cache — every other writer is excluded (`pcb.c:272`, `cpu.c:441`, `arm_init.c:250`; TPIDRPRW only by a switch). The handler then faults on its own first load: `ml_at_interrupt_context` (`machine_routines.c:669-677`) does `getCpuDatap()->intstack_top` and `ldr r1, [r1, #8]` faults at **address 8**, and because the CPU is already in abort mode the second abort re-enters the handler — `sleh_seen` to its cap **64**, `sleh_storm 9`, `pc 0x80011664` × N, `sp` descending `0x248` a record, the log ending there and the device back on its own both times. **The wall is named**: `real_ncpus` is 1 (`cpu_common.c:66`; `cpu_data_register` is called only for a non-boot CPU, `machine_routines.c:570`) and `up_style_idle_exit` is 0 (`arm_init.c:103`, set only by the boot argument of that name at `:287-289`, absent from the payload's `CommandLine`), so `caches.c:414` takes the **else** branch on a uniprocessor — the branch whose first act is that read — where the up-style branch (`CleanPoU_Dcache()`) reads nothing. Owed: 515 passes `up_style_idle_exit=1` and wraps `platform_cache_idle_enter` to publish `up_style_idle_exit`, `real_ncpus` and the cache-on `getCpuDatap()`, with `CleanPoU_Dcache`/`FlushPoU_Dcache` gated on that window so the branch is counted, and the falsifier is the halt (`xnu_live_wfi_seq`, `_inst 0xe320f003`, `_ticks > 0`) with the park's `poll` returning. Image: `.text` 5,290,560 → **5,292,096**, entry image **5,519,996** (`0x00543a7c`), `.bss` `0x80543a80`…`0x8059c4f0` (363,120), wrap census **73** (61 reached / 1 same-object / 1 never called / 10 by address). The `mini4:` block holds **3 lines (250 chars)** against 513's 6 (821) — the same first three, and the five missing ones are all printed after `__real_poll` (`entry_trace.c:1025-1048`), which is the park not returning said on the console as well as in `xnu_live_poll_seq` (1, 2 and no 3, `xnu_live_poll_over` absent) — and `xnu_live_ostext_at` is `0x00048410` in all three logs, with the OS's own text differing from 513's in one word (the RAM disk at `0x8050D000` → `0x80511000`). 129 fixture mutations refused; `xnu_entry_failures = 0`, no `trap record:`, no `pid 1 exited`, no panic; two runs, nothing flashed, both logs ending in `No errors detected` |
| stage90 | [experiment-513](experiment-513-the-idle-s-door-is-the-signal-bit-and-nothing-on-this-cpu-clears-it.md) | **the idle's door is the signal bit, and nothing on this CPU clears it** — 512's question (which of `cpu_idle`'s three exits makes the idle a spin) answered by instrumenting the *exits* rather than the state, because every field the three doors test is `static` in the OSS source and this image's `assym.s` carries `CPU_*` placeholders: `--wrap=Idle_load_context` and `--wrap=SetIdlePop`, the two functions `cpu_idle`'s doors call, each publishing the site it was called from (`entry_note_door`, `entry_note_setidlepop`). **The trap the design had to survive first: gcc merged the first two doors' exits into one call site** — a single `mov lr, pc` / `b Idle_load_context` at `cpu_idle`'s offset `0xa4`, reached by the `beq` at `0x84` (`!idle_enable`) and the `ble` at `0x90` (`SIGPdisabled`) — so the exit census cannot separate doors 1 and 2, and the partition needs a second instrument: `SetIdlePop`, which door 2's *test* calls and door 1's never reaches (`cpu.c:124`; the clause counts 2 sites in `cpu_idle` because `cpu.c:148`'s idle-timer re-arm is the second, and both are below the first test). The OS's own console block gains two lines and they name the door: `mini4: the idle's doors -- pid 1, first test 2198110 time(s) with idle_enable=1, SetIdlePop refusing 0, through the wfi 0; SetIdlePop entered 0 time(s) (TRUE 0, FALSE 0)` and `mini4: the idle's exits by site -- 2198110 exit(s) at 1 site(s): 0x8000cb3c x2198110, 0x0 x0, 0x0 x0, 0x0 x0 (overflow 0); first 0x8000cb3c with idle_enable=1; SetIdlePop's first site 0x0 answered 0 with idle_enable=0`. **The reading: `SetIdlePop` was entered 0 times in the whole boot, so door 2 was never evaluated; the first test therefore returned early, and with `idle_enable` TRUE (`xnu_live_idle_en`/`xnu_live_door_en` = 1 on all 25 records) its right operand must be the one that did it: `cpu_data->cpu_signal & SIGPdisabled`**, the bit `cpu_init` sets unconditionally (`cpu.c:365`) and which the tree's only clearing path — `cpu_signal_handler_internal(FALSE)` (`cpu_common.c:386`, the `hw_atomic_and` at `:402`) — reaches only through `cpu_signal_handler` (`cpu_common.c:380`), whose one caller is the platform's IPI handler slot (`machine_routines.c:605`), which this port never fills: **the loop is pure — no deadline consulted, no CPU halted.** Three witnesses agree: the door arithmetic, 25 `xnu_live_door_*` records at powers of two (every `_lr = 0x8000cb3c`, every `_en = 1`, no `xnu_live_sip_*` record of any kind), and 483's decrementer census at ≤64 (`_setpop_count` ≤64, `_irq_timer_count` ≤16). **Two defects of this step's own reasoning are recorded and fixed.** The site reader printed the `lr` a `mov lr, pc` passes as `branch + 8`; the run recorded `branch + 4` (`0x8000cb3c` for the branch at `0x8000cb38`, the ARM-state `pc`-as-this+8 rule), and the fix is in `build_entry.sh`'s awk, `entry_stubs.c`'s docstring and `entry_trace.c`'s comment — the clause now prints `0x8000cb38(lr 0x8000cb3c)` and `(lr 0x8000cc94)`, agreeing with the run on both sites. And 512's own console wording was corrected: the count read at the park's return is *cumulative* from the boot's first `cpu_idle` entry (130.8 µs after the first ask began), so the park's share is a **bound** — the 32768th entry precedes the park's start and the 65536th follows it, so of 2,198,110 between **2,132,574 and 2,165,342 (97.0%–98.5%)** are the park's — and "while it did" is now "by that time" (a same-length rewrite, proved by `cmp`: the shipped entry image differs from the run's by **exactly 12 bytes**, the format string, so no address or site moved; the payload by 31 = those 12 plus the 19-byte digest that covers them). **The cost reading moved, and the instrument is the reason**: 18.00 ticks a pass (0.9375 µs, 1.0667 M/s) flat from 2^20 onward and **18.00 in both runs of this image**, against 512's **12.0** — 513's image adds a wrapper that runs on every door-1 exit, which is consistent with the difference and is as far as two runs can separate it. The captured OS block grew 1723 → **2113 characters** and a character diff says the +390 is *exactly* these two lines (everything before offset 1675 byte-identical), so `xnu_live_ostext_chars`/`_total` publish twice (1006, then 2022) against 512's once, the second heal firing 91 characters before the block's end — 512's census question advanced but not closed (both counters read 2022 while the block holds 2113). Image: `.text` 5,289,568 → **5,290,560**, image **5503612** bytes, `.bss` `0x8053fa80` +**363024** to `0x80598490`, 27 undefined, wrap census **72** (60 reached / 1 same-object / 1 never called / 10 by address), wrappers `0x8047b16c`/`0x8047b190`, writers `0x80006d04`/`0x80006ddc`, `idle_enable` at `0x80549810` (one 4-byte B, loaded inside `cpu_idle` and stored inside `cpu_machine_idle_init` out of `cpu.o`'s own relocations), 129 fixture mutations refused; payload sha256 `fb7ea486…`/`9fa52aef…`/`b698a5b8…`, the same sizes as 512's. The park itself is unchanged and correct — 2000 ms asked, 38,537,002 ticks (2007.136 ms) held — and the falsifiers are all negative (`xnu_entry_failures = 0`, no trap record, no panic, `No errors detected`). **Next (514):** the repair, now a located choice (clear `SIGPdisabled` via `cpu_signal_handler_internal(FALSE)` once the IPI slot is installed, or install the handler at `machine_routines.c:605`, or accept the spin), each a behaviour change with the falsifier that `SetIdlePop` must start being entered and the decrementer census must rise above 127; a wrapper *on* `cpu_idle_wfi` to turn the wfi bound into a count; the telemetry copy loop, `_cpsr = 0x10`, `_entry_hi`, `p->p_xstat`, the watchdog as an ending. |
| stage90 | [experiment-512](experiment-512-the-process-parks-and-the-kernel-s-idle-path-runs.md) | **the process parks, and the kernel's own idle path runs** — the fixture's `getpid` loop becomes `poll(NULL, 0, PARK_MS)` (the kernel's own idiom: `bsd/kern/sys_generic.c:1788` calls exactly this "an extremely inefficient sleep"), so pid 1 *waits* instead of asking, and `--wrap=machine_idle` — the call `processor_idle` makes (`osfmk/kern/sched_prim.c:4532`) only when every run queue is empty — is wrapped, so the run can see the OS in the state no earlier run could reach: nothing to do. The console block 509-511 made four lines long ends with `mini4: the OS has nothing to run -- pid 1 parked in poll for 2000 ms (caller 0x80285898), and the kernel's own idle path was entered 3325332 time(s) while it did (ticks 0x24b80d2)`. **The signal is the kernel's, not the fixture's**: 26 `xnu_live_idle_*` records at powers of two, `_caller = 0x800b69e8` = `processor_idle+0x114` (the `bl` is at `+0x110`, and the build clause requires all 1 branch(es) to the wrapper to be inside `processor_idle`'s 0x800b68d4..0x800b6d8c), `_thread = 0xc05b1480` the idle thread, `_pid = 0` (`kernproc`), `_cpsr = 0x60000093` — SVC mode with I masked, which is what `machine_idle`'s own `cpsid if` anticipates. **`_now` goes `0x06ced6b1` to `0x1ecfe458`: 402,722,215 ticks for 2^25 − 1 entries = 12.00 ticks each, 0.625 µs at 19.2 MHz, 1.6 million entries a second — and the first entry is 131 µs after the fixture's first `poll` began**, which is the first time this machine idled at all. **The reading the step was not looking for: this idle does not sleep.** `cpu_idle` (`osfmk/arm/cpu.c:118`) has two early returns and exactly one `wfi`, its loop is `processor_idle`'s, so 33,554,432 `machine_idle` entries are 33,554,432 trips round that path — and 483's decrementer census, on the only route the kernel can program the countdown through (arm32's `ml_set_decrementer` `bxne`s `cpu_set_decrementer_func`, set at `cpu.c:461`), reads **at most 127 writes for the whole boot** (`_dec_count` published at 2, 4, 8, 16, 32, 64 and never at 128), so the `wfi` was reached at most ~127 times: **one pass in 264,000 gets past the early returns.** `_setpop_count` (≤127) and `_irq_timer_count` (16..31) agree. The run is not damaged by it — the parks return, the timebase is right to 0.27%, the child, the reap and the `SIGCHLD` all still happen — but both short asks come back 14-15 ms late (5 ms → 18.810 ms, 40 ms → 55.228 ms) while the 2000 ms park lands at 2005.344 ms, and which of the three early exits is taken (`idle_enable` **FALSE** from `cpu_common.c:67` unless `cpu_machine_idle_init(TRUE)` ran at `arm_init.c:376` with no `jtag`; `cpu_signal & SIGPdisabled`, set by `cpu_init` at `cpu.c:365` and cleared only by `cpu_signal_handler`, whose only caller is the platform's IPI slot; `SetIdlePop` refusing because the last deadline expired and `timer_resync_deadlines` does not call `setPop` when nothing is queued) is 513's object, five readable words and a return. **Two owed items close and one correction is made.** The first AST's thread, `_pid = 0` with a zeroed state since 511, is on this run `0xc05afa40`, and the same pointer in `xnu_live_sleep_*` parked with `_site = ux_handler_init+0x70` and `_wmsg` resolving in the image to **`ux_handler_wait`** — the only maker of that string is `bsd/uxkern/ux_exception.c:200` inside `ux_handler_init`, called by `bsdinit_task` (`bsd_init.c:1059`) on its way to `load_init_program(p)` at `:1079`, so the thread that carries the first AST is the thread running `bsdinit_task`; the *ordering* claim behind it is now same-stream (`_ast_seq = 1` at byte 546006 < `_exec_seq = 1` at 546809 < `_exec_done_seq = 1` at 550043 < `_ast_done_seq = 1` at 550152), because the OS's own console text is *captured* into a reserved 128 KB block (`_ostext_block = 0x20000`, opened by the payload's `[os-console-459]` marker) and its position in the file says nothing about when it was printed. And the tool's old claim that the two asks' "property is an 8:1 ratio" is corrected: the words are 8:1 and **no run has ever produced it** (511 measured 5.6:1, this one 2.9:1) because the published tick count is the deadline *plus* the wake, so the property is the ordering and the ratio is a reading of the run. **One flag, one wrapper, one writer, one print, one added word in the fixture** (78 → 79 words, 312 → 316 bytes; the `b` at word 71 re-pointed at `park`, `udf #1` moves from word 77 to 78): `.text` 5,288,480 (510) → 5,289,056 (511) → **5,289,568**, entry image **5503612** bytes, `.bss` `0x8053fa80` + **362896** to `0x80598410` — the *same extent 511 had* even though two `bss` words are added (`g_idle_calls` at `0x80541c48`, `park_printed.2` at `0x805983f0`, both inside the section's existing tail) — 27 undefined, wrap census **70** (58 reached / 1 same-object / 1 never called / 10 by address; `machine_idle` is in the first), wrapper at `0x8047afc4`, delivered from `processor_idle+0x110`; and **511's own image row is corrected with it**: its `.text` after-value is 5,289,056 and the delta +576, not 5288960/+480 — its own build log says so and the row was 96 low. The payload's sha256 are `bf9f42d1…` / `110471d1…` / `efad670f…`. Host side: `PROGRAM_WORDS = 79`, five new words pinned in the table, `park_timeout`/`park_threshold` reading the `#define` out of `entry_trace.c` so the fixture and the wrapper cannot drift, and three new clauses (the park's back-branch must close on its own first word, the timeout must be non-zero, and the two asks must stay below the wrapper's threshold) — 129 fixture mutations refused. **The capture script had the mirror image of 511's failure and it is fixed**: 512 could not *remove* a log owned by root (`rm: cannot remove …: Operation not permitted`, exit 1, *after* the boot), so the removal is now `rm -f \"$LOGFILE\" 2>/dev/null || sudo rm -f \"$LOGFILE\" || die …`, verified against root-owned, user-owned and missing files; this run's log was recovered by hand while `/proc/last_kmsg` still held the boot. **Next (513):** which of the three early exits makes the idle a spin, read as five words; the captured console's own census (1006 characters counted against 1722 in the block); and 511's carried items — the telemetry copy loop, `_cpsr = 0x10`, `_entry_hi`, `p->p_xstat`, the watchdog as an ending. |
| stage90 | [experiment-511](experiment-511-the-kernel-s-own-way-out-to-user-mode.md) | **the kernel's own way out to user mode** — `--wrap=bsd_ast`, and the console block 509 and 510 made three lines long ends with a fourth: after `mini4: the OS's own init load returned, so pid 1 has the init image (caller 0x80048eb8)` comes `mini4: the AST is done -- pid 1's thread is at 0x10e0 for user mode (sp 0x101efc)`. **`thread_bootstrap_return` cannot be wrapped and the step says why before it says anything else**: it is a label at the *head* of `load_and_go_user` (`osfmk/arm/locore.s:1897-1912`, `LEXT(thread_bootstrap_return)` falling straight through into `LEXT(thread_exception_return)`) and nothing calls it — it is a `thread_continue_t`, an *address* set into a PCB and jumped to by machinery that never returns, so a C wrapper there would put a prologue and a `bx lr` in front of a thread with no return address. `bsd_ast` is one frame in and is a call: `activate_exec_state` → `execve` → `load_init_program` → `bsdinit_task` → **`bsd_ast`** → `ast_taken_user` → `load_and_go_user`, and its before/after pair on `thread + ACT_PCBDATA + SS_PC` is the same word 510 read, one frame out, bracketing the whole AST — which for the first call is the exec itself. **The run answers with two calls and closes the route with one thread pointer crossing four records written by three instruments**: `_seq = 1` on thread `0xc0485d20` with `_before = _after = _sp = 0`, `_cpsr = 0x10` and **`_pid = 0`** (the bootstrap thread, which has never been in user mode — and *inside* it the exec runs and writes 0x10e0 into a **different** thread), then `_seq = 2` on thread **`0xc04c4be0`** with `_before = _after = 0x000010e0`, `_sp = 0x00101efc`, `_cpsr = 0x10` and **`_pid = 1`** — the thread `xnu_live_entrypt_thread` names (510) and the thread the process's first two faults are taken on (`sleh_seq = 6`, `_user = 1`, `_pc = 0x00001118`, `_sp = 0x00101efc`, `_fsr = 0x07` a read on the page `mmap` returned; `_seq = 7`, `_pc = 0x00001124`, `_fsr = 0x080f` a write; and `_seq = 8`, `_type = 3`, `_thr = 0xc04c31a0` is 507's forked child at `0x11a4`). **`xnu_live_ast_calls = 2` is what makes the two calls visible as two, and `_pid` on each side of them is what separates the thread the AST is delivered on from the process the work is done for.** One prediction was wrong and is recorded: the wrapper predicted the zeroed state would be the *cloned* thread's, and the run puts the zeroed state on the **bootstrap** thread while pid 1's thread already holds 0x10e0 — because the exec runs inside the first AST. **And the first build's console line named the wrong call** (`seq == 1`, printed when it was done and not pid 1's thread) and read `mini4: the AST is done -- pid 0's thread is at 0x0 for user mode`, which is 509's durable-artifact defect arriving from the other side: the print condition is now a reading (`after != 0u`) rather than a position in a count, the image was rebuilt, and the step re-run. **Four build clauses**, the last of which is this step's own defect: membership (`bsd_ast` is not in pass 1's undefined set), the branch to the wrapper inside `ast_taken_user` (0x8011643c..0x80116664, 455's hazard), **every reference to `bsd_ast` in the pool is a call** — the relocation set of all 708 objects must be exactly `R_ARM_CALL`, which is what makes the choice over `thread_exception_return` a fact about the tree rather than an argument, since `--wrap` rewrites address-taken references too — and **the reader has no pipe in it**, because `set -o pipefail` turns `objdump | grep -q` into a pipeline that can report a successful match as exit 141 (the failure this file already carries notes about twice, and the sentence the first build printed while the disassembly plainly contained the branch; the identical command re-run by hand matched 200 times out of 200, so the *cause is not established* and is recorded as such). **No fixture change and no kernel change** — one flag, one wrapper, one writer pair, one print. `.text` 5288480 → **5288960** (+480), entry image **5503612** again, `__bss_start 0x8053fa80` with **362896** bytes — **exactly +8 on 510's 362888**, the two words this step adds (`g_ast_calls` and the wrapper's `ast_printed` static), the one row of this build that closes to the byte — 27 undefined, wrap census **69** (57 reached / 1 same-object / 1 never called / 10 by address; `bsd_ast` is in the first), wrapper at `0x8047b324`, delivered from `ast_taken_user + 0xc4`; run 2's payload sha256 `faf96e9b…` / `d10a9114…` / `a9bf7a4a…`, run 1's `dc56fbee…` / `87252f8e…` / `0e3947de…`. Run 2 is unperturbed (no `stub_hit`, `undef`, `osr`, panic, trap record or report, `pid 1 exited` absent, the fixture's whole program runs, `_wait_status = 0x300`, `_wait_error = 0x0a`), log 573837 bytes / 8392 lines, `No errors detected`, device back on its own. **Two operational findings recorded with the step**: two *background* builds of this same tree died silently at two different checks while every one of those commands passes by hand and the foreground build completes twice (cause **not established**; the practice is a foreground build), and `run_and_capture.sh` could not write `/tmp/cancro-last_kmsg.txt` **after** run 1's boot because root's `O_CREAT` on an existing file in a sticky `/tmp` is refused (root's `rm -f` on it succeeds and root's create of a non-existent file succeeds — the `fs.protected_regular` signature with no `CAP_FOWNER`), so the script now removes the log first and run 1's readings are in the document only because the log was re-captured by hand before the device took another boot. **Next:** the route is measured from the kernel's side and the restore itself is not instrumentable — the faithful successors are the first AST's unidentified thread (`_pid = 0`, zeroed state, no name) and the telemetry copy loop, which this run puts on **this step's own thread** (`_thr = 0xc04c4be0`, `_pc = 0x80016574` = `Lcopyin_wordwise_loop`, `_lr = 0x800aad34` = `telemetry_take_sample + 0x158`, `_far_frame = 0`, `_seen = 0x22`, `_redirected = 0x1a`). |
| stage90 | [experiment-510](experiment-510-the-os-starts-the-process-at-a-pc-it-had-already-written.md) | **the OS starts the process at a PC it had already written** — `--wrap=thread_setentrypoint`, and the console block 509 made one line longer is now two: after `mini4: the OS's own init load returned, so pid 1 has the init image (caller 0x80048eb8)` comes `mini4: the OS starts the process at 0x10e0 (the thread's user pc was 0x10e0)`. The function is four instructions — `sv = get_user_regs(thread); sv->pc = entry;` (`osfmk/arm/status.c:662-675`) — so a wrapper around it holds two different facts, the **argument** (what the exec decided) and the **word** (what the thread now holds), and the wrapper publishes both: it reads `thread + STAGE90_ACT_PCBDATA + STAGE90_SS_PC` before the call and again after it, because an instrument that published only the argument could not tell a correct store from no store at all. Beside the print are nine live records: `xnu_live_entrypt_seq = 1`, `_caller = 0x80291cb0` = `activate_exec_state + 0xe8` (the OS's own call site, read out of the linked instructions by the build), `_thread = 0xc05c81a0`, `_entry = 0x000010e0`, `_entry_hi = 0` (a 32-bit address has a zero top half, so a non-zero one would say the entry is not an address at all), **`_before = 0x000010e0`**, then `_done_seq = 1`, `_done_entry = 0x000010e0`, `_after = 0x000010e0`. **`_before` is the one prediction this step got wrong and the run's finding**: the writer predicted a cleaned state, on `activate_exec_state`'s `thread_state_initialize(thread)` immediately before the call (`kern_exec.c:730-760`), and the answer is the opposite — `thread_setstatus(thread, ARM_THREAD_STATE, ...)` (`kern_exec.c:743-758`) had already copied the image's whole `LC_UNIXTHREAD` state, `pc` included, into exactly this word, and `load_threadentry` → `thread_entrypoint` reads `state->pc` back out of it (`status.c:683-705`) and hands it over. So the boot holds **three sources of one number**: the kernel's decision, the kernel's state from a different write, and the RAM disk's own `pc` word, which `tools/host_ramdisk_macho_check.py` reads out of the blob and asserts is inside `__TEXT` — and `_before == _after` is the reading rather than a disappointing one. **The first run measured the wrapper instead of the kernel, and the ABI is why**: the declaration is `void thread_setentrypoint(thread_t, mach_vm_offset_t)` (`osfmk/kern/thread.h:980`) and `mach_vm_offset_t` is 64-bit, so the entry travels in **r2:r3 with r1 unused** (visible in the caller's own object: `ldr r2, [r8, #4]` / `mov r3, #0` / `bl`) — the first build declared the parameter `uint32_t`, read r1, and passed r2/r3 through untouched, so the real function stored the junk: `_entry = 0x00000008`, `_after = 0xde500000`, console `mini4: the OS starts the process at 0x8`, `pid 1 exited -- exit reason namespace 2 subcode 0xb`, `panic_entered = 1` (`panic_len = 0x1320`), one `stub_hit`, four `osr`, and every fixture key zero. **The build clause that catches it took three attempts, and all three failures are the same defect — a check that could not see what it was checking**: `sites == 1` for the real function refused an image where gcc had **tail-duplicated** the `bl` (`from 2 site(s)`); the rewritten reader refused itself because it printed the three lines *preceding* a match and split on `---`, so the `bl` line — the only one carrying the operands — never survived (`found no call … so it measured nothing`); and the negative side was then exercised on purpose by narrowing the parameter back to `uint32_t` (`of 510's 2 call(s) … 0 write r2 first and 0 write r3`). The clause that remains is *every call to the real `thread_setentrypoint` is inside the wrapper, and every one writes r2 and r3 first*. **No fixture change and no kernel change** — one flag, one wrapper, one writer pair, one print. `.text` 5287968 → **5288480** (+512), the entry image is **5503612 bytes** again, `__bss_start 0x8053fa80` with 362888 bytes, **27 symbols undefined**, wrap census **68** (56 reached / 1 same-object / 1 never called / 10 by address; `thread_setentrypoint` is in the first category, which is the census's own confirmation of the call-site clause), the wrapper at **`0x8047b270`** with the one branch to it at `0x80291cac` inside `activate_exec_state`; `stage90.bin` sha256 `0d81aef6…`, `stage90.img` `e1115196…`, `stage90-qcdt.img` `92994a59…`, and a rebuild after the comment edits reproduced all three byte for byte. Run 2 is unperturbed: no `stub_hit`, `undef`, `osr`, panic, trap record or report, `sleh_seq = 8`, the fixture's whole program still runs (`getpid` = 1, fork, exit, `SIGCHLD = 0x14`, reap, `_wait_status = 0x300`, `_wait_error = 0x0a`), log 572646 bytes / 8355 lines, `No errors detected`, device back on its own — and the copy-loop fault 508 named is in this run's records with both ends: `_pc = 0x80016454` = `Lcopyin_wordwise_loop`, `_lr = 0x800aad34` = `telemetry_take_sample + 0x158`, `_far_frame = 0`, `_redirected = 1`. **Next:** `thread_bootstrap_return` — the number is measured and the route is not: what turns a thread with an entry point and no user-mode history into a running process is the mechanism 509 promoted and this step only half answered. |
| stage90 | [experiment-509](experiment-509-the-os-console-says-its-init-image-loaded.md) | **the OS's own console says its init image loaded** — `--wrap=load_init_program`, and the console capture every run in this walk has ended with now reads one line longer: after `load_init_program: attempting to load /sbin/launchd` comes `mini4: the OS's own init load returned, so pid 1 has the init image (caller 0x80048eb8)`. **The wrapper is a reading rather than a trace because of the function's own shape**: `void load_init_program(proc_t p)` returns on every arm that loaded an image (`if (!error) return;`) and its only other exit is `panic("Process 1 exec of %s failed, errno %d")` (`kern_exec.c:5168`), so code after the call runs *iff* the OS loaded its init image — the property 489 could only argue from the file. Beside the print are three records: `xnu_live_exec_seq = 1`, `_caller = 0x80048eb8` (read out of the image as `bsdinit_task + 0x88`), `_who = 1`, `_proc = 0xc06acb80`, and after it `_done_seq = 1`, `_done_who = 1`, **`_done_current = 0`** — the step's one wrong prediction and its finding: the loader runs on the **kernel** task, so `current_proc()` inside it is `kernproc` (pid 0) while the process the image is loaded *for* is `initproc` (pid 1), the mechanism 507 measured for `psignal`'s sender arriving from the other direction. **Two wrappers were designed and one would have failed**: `__mac_execve` is called *only* from `bsd_kern_kern_exec.o`, the object that defines it, which is 455's same-object case — a wrapper that links, defines its symbol and never runs — while `load_init_program`'s single call site is `bsdinit_task` in `bsd_kern_bsd_init.o`, and the build now reads that branch out of the linked instructions instead of trusting the flag list. The build's third clause reads `bl panic` inside the loader's own range (a loader that fell through to a return on failure would make `xnu_live_exec_done_seq` a record that says nothing), its negative side exercised by pointing the same grep at `bsd_ast`/`bsd_init` (zero branches against one in `bsdinit_task`); `load_init_program` and `printf` are checked out of pass 1's undefined set, the sink being the OS's own `printf` (`bl 800399b4` immediately before each of the loader's four `load_init_program_at_path` calls) and deliberately not `IOLog`, whose `_IOLogv` opens with an `assertf` that would panic. **A second run, and the first was thrown away on purpose**: run 1's line said "pid 1 is in user mode", which is true of the run (the same log has pid 1 faulting in user mode at `0x1118`/`0x1124`) but not of the instant the wrapper measures, so the wording was narrowed to the reading, the image rebuilt and the step run again. Nothing else moved: no `stub_hit`, `undef`, `osr`, panic, trap record or report, the fixture's whole program still runs (`getpid` = 1, fork, exit, `SIGCHLD = 0x14`, reap, `_wait_status = 0x300`, `_wait_error = 0x0a`), log 573221 bytes / 8371 lines, `No errors detected`, device back on its own. `.text` 5287616 → **5287968** (+352), entry image **5503612** again, `__bss_start 0x8053fa80` with 362888 bytes, 27 undefined, wrap census **67** (55 reached / 1 same-object / 1 never called / 10 by address); `stage90.bin` sha256 `7c94e35e…`, `stage90.img` `41dcef79…`, `stage90-qcdt.img` `92a1bdf4…` (run 1's are `8580b680…`/`175d3f4c…`/`e1c833c9…`); three files. **Next:** `thread_bootstrap_return` — the image is loaded for process 1 by a thread running in `kernproc`, and what starts pid 1's own thread at a user PC is the item this reading promotes from a name on the list to the frontier. |
| stage90 | [experiment-508](experiment-508-the-reap-and-the-status-the-kernel-composes.md) | **the reap, and the status the kernel composes** — `wait4` (`sysent[7]`) is wrapped — the third of the three calls a Unix process's life is, and the only one whose *answer* the kernel builds out of things the program never sees (`exit` puts `W_EXITCODE(uap->rval, 0)` into `p->p_xstat` at `kern_exit.c:682`/`:823`, `wait4` masks it at `:1782` and copies it out, and `reap_child_locked` frees the process whose pid it then returns) — and the run reads all of it: the parent's first call **blocks** (`xnu_live_sleep_site = 0x802950ac` = `wait4_nocancel + 0x19c`, the instruction after `bl __wrap_msleep0`, `_pri = 0x520` = `PWAIT | PCATCH | PDROP`, `_chan` the parent's own proc, `_tmo = 0`, `_thr = 0xc04a70d0` ≠ the child's `0xc04a7df0`), the child's whole death (`exit_seq = 1 _pid = 2 _rval = 3`, `pth_delete_seq = 1`, `sigchld_seq = 1 _signal = 0x14`) lands **between** the call's two records, and the kernel's `copyout` of the status then takes a **kernel-mode write permission fault on the fixture's own page** — `sleh_seq = 8`, `_fsr = 0x0000080f`, `_far = 0x00102000`, `_pc = 0x80016360` = `Lcopyout_bytewise`'s store, `_lr = 0x80295114` = the instruction after `bl copyout` in `wait4_nocancel`, `_user = 0`, `_recover = 0x8001644c` = `copyio_error`, **`_redirect = 0`**, which this instrument's own rule (490) reads as *armed and retried* rather than converted to `EFAULT` — so the copyout returned 0, the parent's `ldr`/`cmp` against `0x300` passed, and the **second** call on the same pid answered **`ECHILD`** (`_error = 0x0a`, `_ret = 0`) with **`_copy_error = 0`, `_status = 0x00000300`, `_ticks = 0x0e`** read back out of the page. **The status is the kernel's, and the proof does not need the record that is missing**: the page held `0xfeedface` at 7818 (504's `read` pair, same run), the second call returns `ECHILD` before any copyout, and the fixture reached its loop taking no `udf` — so both of its checks passed. One prediction was wrong and is recorded: **`xnu_live_wait_done_seq = 1` does not exist** — six keys the writer emits unconditionally, absent, with `xnu_live_capped` absent and 4409 of 8192 live records used, so it is neither the writer nor the cap; the console's two-writer exposure is the remaining candidate (a `xnu_live_tmr_src0..4` group is the five lines sitting exactly where the six would be) and the honest fix is owed rather than guessed. The fixture grew to **78 words / 312 bytes** (17 new instructions: `wait4(2, &status, 0, NULL)` twice, with the status checked against `EXIT_STATUS` and the *second* call deliberately not branched on, 503's rule) and `host_ramdisk_macho_check.py` reads every one of them, with two new structural clauses (the parent's branch must not land in the loop; no conditional branch between the second `svc` and the loop) and 128 mutations in its selftest (was 111); `check_sysent_table.py` gained an `ARGUMENT_WORDS` clause asserting `sy_narg 4` / `sy_arg_bytes 16` / `munge_wwww` for slot 7, and `build_entry.sh`'s wrap census needed `wait4` added to its `by_address` list because the only reference to the wrapper is the table word itself. `.text` 5287040 → **5287616** (+576), the entry image is 5503612 again, `__bss_start 0x8053fa80` with 362888 bytes, 27 undefined, wrap census 66 (54 reached / 1 same-object / 1 never called / **10 by address**), `sysent[7] = 0x8047b0f0 = __wrap_wait4` (`wait4` at `0x802952f0`); `stage90.bin` 5999040 sha256 `eed2c6b68f7009855fc923ad2bff5bf63b44ec14bb112f58f82be82041057682`, `stage90.img` 6002688 sha256 `6f6ad93fe11b307fa0d90c8f764197b52f44e715550c198d0d06321539f12b49`, `stage90-qcdt.img` 8523776 sha256 `537de92b642c2bac1953f03efc7fa416e543132ca4514afcda0bed61f9930fac`; checks 128/48/20/30/57/131/7 refused; one run ended on the hardware watchdog (25 s) with the device back on Android by itself — and **neither process is a zombie any more**, which is the one thing this log says that 507's could not. The fixture's program is complete (make a process, lose it, reap it), so the next frontier is what `load_init_program` is still trying to do at line 3994: `/sbin/launchd`'s own Mach-O. |
| stage90 | [experiment-507](experiment-507-the-child-is-dead-and-the-os-tells-its-parent.md) | **the child is dead, and the OS tells its parent** — `pth_proc_hashdelete`, the slot 506's run stopped in, got a body (the fifth hand-written slot after 433's `pthread_init`, 465's `pth_proc_hashinit` and 473's two workqueue slots: record the call, do not dereference `p`, do not reach into the callbacks table — and 465 left `p->p_pthhash` NULL, so a delete has nothing to free), and the run's readings are the two that have never appeared in any log in this walk: `xnu_live_pth_delete_seq = 1` for the child's proc `0xc05b3e38` (entered from `proc_exit + 0x188`, `tbl = 0x804c1168`) and then **`psignal(pp, SIGCHLD)`** — `xnu_live_sigchld_seq = 1`, **`_signal = 0x14` = 20**, `_to = 1`, `xnu_live_psignal_calls = 1`, `_returned_from`/`_to` after the real call — **the first inter-process event in this walk**, the OS telling process 1 that process 2 is gone. **There is no `stub_hit` in the run, no `undef`, no `osr`, no `abort`, no `panic`**: the child became a zombie, its parent never left its loop (`getpid_count` 23 records to `0x800000`, `_last = 1` at every one; `tmr_qexp_seq` to `0x0b`, `timerdrv_fires = 2`), and **this is the first run in the walk that ends by the hardware watchdog rather than by a stop** — nothing is left to stop it, so there is no report at all (`real XNU entry` and `trap record` both absent), the log is 571870 bytes over 8328 lines, and the device returned on its own. Two of the step's own predictions were corrected by the run and both are readings: `xnu_live_sigchld_from` is **0**, not the dying child's pid, because `set_bsdtask_info(task, NULL)` (`kern_exit.c:1371`) clears the task 72 lines above the call so `current_proc()` answers `kernproc` (pid 0, `bsd_stubs.c:104`, "Never returns a NULL") — the wrapper's null guard firing for a real reason on its first use; and the distance from the retired slot to the `psignal` is **338** source lines, not the 275 four documents said (all four agreed with each other and none with the file — corrected here and in 506's). The check had to grow for this step: `check_pthread_table_slots.py`'s comparison of the table's 40 words with the symbol each member implies **cannot see a body-versus-stand-in at all** (`stage90_pthread_slot_<member>` is the name both kinds are given), so a third clause reads the **object's own literals** — a stand-in is defined by the name it hands to `entry_stub_hit`, a body by the live key it publishes — and requires the stand-in name literals to be *exactly* the members with no body, with a `BODIES` table of five member/key pairs as the only thing it is told; 7 mutations in its selftest now (four table, three object). Two defects recorded beside it: that clause's **first failure was a Python traceback**, because the line that reports a problem had been `say("FAIL: %s" % problem, file=sys.stderr)` since 473 and `say` takes no `file=` — it never ran because the check had never failed, so a *report* that never executes is 500/501's lesson one level further in; and the 275/338 number. `.text` 5286944 → **5287040** (+96: the body's four calls and three key literals), the entry image's file is 5503612 again, `__bss_start 0x8053fa80` with 362888 bytes to `0x80598408`, wrap census still 65 (54 reached / 1 same-object / 1 never called / 9 by address), the table at `0x804c1168` with 5 of its 39 members bodies and 34 stand-ins; `stage90.bin` 5999040 sha256 `4d869e7c3edf6279b9e5cadea7f1c25c2ccf9cf7bdfa65f3696b3ef329d9e398`, `stage90.img` 6002688 sha256 `eb714d649b90d40d8283ac5adc38b50202168b6ac9ba0d34387a4b5492f56f06`, `stage90-qcdt.img` 8523776 sha256 `481e2d7782ef94dbac9190bfdaf4b6759fb58cec0a8a89c75286969aa333626d`; three files (+ 506's correction); checks 7/111/48/20/30/57/131 refused; one run, `No errors detected`. The next step is `wait`: the child is a zombie nothing has reaped, `wait4(2, &status, 0, NULL)` is `sysent[7]`, and its `status` is the `W_EXITCODE(3, 0)` = `0x300` that 506's prediction named and this image publishes nowhere. |
| stage90 | [experiment-506](experiment-506-the-childs-own-arm-and-the-slot-the-exit-path-stops-in.md) | **the child's own arm, and the slot the exit path stops in** — `cmp r0, #0` became `cmp r1, #CHILD_FLAG` with the branch **kept at `beq entry_child`** (`cmp r1, #1` when `r1` is 1 is *equal*, so the equal arm is the child's; the register 505 retired is the parent's), the program stayed 61 words, and the whole edit is two words of the blob — word 49 `0xe3500000` → `0xe3510001`, word 50 `0x1a000000` → `0x0a000000`, decoded out of the **booted image** at `0x5863f0` of `stage90.img`, not only out of the object. **Run 1 inverted the condition** (`bne`) and is kept as a reading: the device's console printed `pid 1 exited -- no exit reason available -- (signal 0, exit 3)`, `xnu_live_exit_seq = 1 _caller = 0x80285848 _pid = 1 _rval = 3`, no `xnu_live_sigchld_*`, `xnu_entry_panic_entered = 1` with `_len = 0x13fa` (478's `launchd_crashed_panic`, reached from `proc_prepareexit`, `kern_exit.c:849`), log 579953 bytes, device back on its own — and **the check could not refuse it**, because the decode table asserted the same `bne` and the catching mutation had been written *from* that table: one derivation, two documents, and only the device disagreed. The fix is a second opinion that does not restate the table — `want = COND["ne"] if immediate == 0 else COND["eq"]`, a relation between the test's immediate and the branch's condition — exercised on the built artifact: word 50 of `out/stage90/xnu_arm_entry.elf` patched to `0x1A000000` is refused twice, by the table (`word 50 at 0x11a8 is bne to word 52`) and by the clause, and all 111 mutations are refused on the real one. **Run 2 is the step**: the child's first dispatch is `xnu_live_sleh_seq = 8` (type 3 prefetch abort at `far = pc = 0x000011a4`, `thr = 0xc04f11a0`, `sp = 0x00101efc`, `cpsr = 0x40000010`, `user = 1` — 505's record, regrown) and one record later `xnu_live_exit_seq = 1 _caller = 0x80285848 (unix_syscall+0x100) **`_pid = 2`** _rval = 3`, while the parent's `getpid_count` reached `0x4000` over 14 records with `_last = 1` every time, the console ends at `load_init_program: attempting to load /sbin/launchd` (line 3994) with **no `pid 1 exited` anywhere**, and `xnu_live_undef_*`, `xnu_live_osr_*` and `xnu_live_getpid_change_*` are all absent — the child took its own arm and nothing faulted. **And the run's last live record is the only stub hit in it**: `xnu_live_stub_hit_seq = 1`, `name_ptr = 0x804c157a` = `stage90_pthread_functions.pth_proc_hashdelete` (`w0 = 0x67617473` / `w1 = 0x5f303965`), `caller = 0x80294150` = **`proc_exit + 0x188`** — `proc_exit` calls `pth_proc_hashdelete(p)` (`kern_exit.c:1105`, under `#if PSYNCH`), the shim is Apple's `pthread_functions->pth_proc_hashdelete(p)` (`pthread_shims.c:364`, the `ldr r1, [r1, #36]` / `bx r1` at `0x80205d0c`), and the slot is one this image fills with a stand-in — **a stub hit is terminal** (`entry_stub_hit` ends in `entry_epilogue`, `entry_stubs.c:6107`), so the exit arm this step added is **338** source lines long (`:1105` to `:1443` - 275 when this row was written, an estimate; 507 computed it and corrected it) and the boot stopped all of them short of its `psignal(pp, SIGCHLD)` at `:1443`: the parent was never told for a second and unrelated reason than 505's. The prediction was also one number wrong and the run corrected it — `xnu_live_exit_rval` is the *argument* (3) as the munger marshalled it, while `W_EXITCODE(3, 0)` is computed inside the kernel (`exit`, `kern_exit.c:684`) into `p->p_xstat` (`:826`), a field nothing here publishes; the fixture's header and the checker's note had named `0x300` and now say this. 465's and 473's slots are measured working in the same run (`xnu_live_pth_hashinit_seq = 2`, `p = 0xc058d3a8`; `xnu_live_pth_wqmark_seq`/`_wqexit_seq = 2` with the same `p`), and the next step is the same shape for `pth_proc_hashdelete`. `.text` is **5286944, unchanged from 505**, and so is the entry image's 5503612 — the step adds nothing to the image, only two words inside the RAM disk blob — `__bss_start 0x8053fa80`, 362888 bytes to `0x80598408`, wrap census still 65 (54 reached / 1 same-object / 1 never called / 9 by address); `stage90.bin` 5999040 sha256 `b07fb6f610f3c0ff5cd404fc130d0f7a292c2074ce103bb73ea08261e7b53f5e`, `stage90.img` 6002688 sha256 `516b77f6e98e0a420754f731180ed1d3ad35b2e4cebfd102e94e1483f671efb5`, `stage90-qcdt.img` 8523776 sha256 `cd3cb6ac24e589b0193a34018ae5be9ab99825985f2b0729ba5475a801f7586a`; two files, +200/−68; checks 111/48/20/30/57/131 refused; run 2's log 591886 bytes and 8781 lines with `No errors detected`; and two new defects recorded — the report cannot name its own reason (`xnu_entry_why = 0x1034e1a0`, an empty string printed although every `entry_epilogue` caller passes a literal, with both reads taken after the epilogue turns the caches and MMU off, `entry_stubs.c:3728-3746`), which is the step's new debt alongside `p->p_xstat`. |
| stage90 | [experiment-505](experiment-505-the-first-process-the-os-makes-and-the-child-that-came-back-with-the-pid.md) | **the first process the OS makes, and the child that came back with the pid** — `fork` was made, `forkproc`'s `pth_proc_hashinit` ran a second time for `p = 0xc06a68c8`, the parent was told `ret_lo = 2` with `error = 0` (`xnu_live_fork_caller = 0x80285848` = `unix_syscall+0x100`, `uap0 = 0x000011e0` = the ENOENT `open`'s path, i.e. the word the argument buffer kept), and **the child ran** — but down the *parent's* arm, because on ARM the child's `r0` is the **pid** too. The parent's `save_r0` is `arm_prepare_u32_syscall_return`'s (`systemcalls.c:293`); the child's is `thread_set_child`'s, which `fork1` calls *after* `thread_dup` (`kern_fork.c:590`, `:636`, "this is what gives the child process its 'return' value from a fork() call") and whose ARM body is three instructions — `mov r2, #1` / `str r1, [r0, #848]` / `str r2, [r0, #852]` (`osfmk/arm/status.c:722`; the ARM64 port writes the same pair, `arm64/status.c:1253`, so it is the platform's ABI) — so the halves are told apart by **`r1`** (1 in the child, 0 in the parent from `uu_rval[1]`) and not by the zero the fixture wrote. The run says so three ways: the child's **first** `getpid` returned **2** at call `0x5e8b` = 24107 (`xnu_live_getpid_change_seq`, while `xnu_live_getpid_last` stayed 1 for the parent), the child's first dispatch is `sleh_seq = 8` (`type 3` prefetch abort, `far = pc = 0x000011a4` = the word after the `svc`, `thr = 0xc060a480`, `sp = 0x00101efc`, `cpsr = 0x40000010` — the parent's own pc, stack and flags, which is `machine_thread_dup`'s 80-byte `bcopy` of `machine.PcbData` visible from the far side), and the child's own `xnu_live_undef_pc = 0x11d0` = `entry_failed: udf #1`, reached only by `cmp r0, #EXPECTED_PID` failing (`undef_spsr = 0x20000010`, `Z` clear / `C` set = 2−1). **That is the first death this walk survived**: `threadsignal+0x198` created an exit reason (`xnu_live_osr_ns = 2`, `_code = 4`), the task became a corpse (`xnu_live_stub_hit_caller = 0x802933b4` = `gather_populate_corpse_crashinfo+0x5a0` through a pthread slot), the parent's spin kept climbing past the fork's record, and **no panic** — `xnu_entry_abort_entries = 0`, `No errors detected`, device back on its own. The parent's thread is `0xc060bec0` (the `mmap` record's and the six `tmr_qexp` records') and its quantum expired six times before the child was ever dispatched, so the child's 24105-spin delay is an order and a duration is owed. Absences that are the step's readings: no `xnu_live_exit_*` (the `exit` arm was not taken, so 506's readings — `exit_pid` 2, `rval` composed into `0x300`, `psignal(pp, SIGCHLD)`=20 to pid 1 — have never been seen here), no `xnu_live_sigchld_*`, no `psignal` record even though the death went through `threadsignal`. The header's "`save_r0` has exactly one writer in this tree" was the claim the run refuted, and the same file holds both writers 250 lines apart; the fixture's `mov r0, #0` is a word with no reader at all. Fixture 52 → **61 words**; `.text` 5285824 → 5286944, **entry image 5503612 bytes again** (502–505), `__bss_start 0x8053fa80` + 362888, headroom 1473528, wrap census unchanged at 65/54/1/1/9, `stage90.bin` md5 `2c289e187fe361cf8d6a4536ed29682a`, `stage90.img` 6002688 sha256 `48cd7060f240a0e9e231aeca6b5154e7e06719045a3d516c45e18c21862c41a3`, payload 851 insertions / 53 deletions across six files, all checks green (108/48/20/30/57/131 refused, `check_sysent_table.py` reading `fork`'s munger 0 and `sy_narg` 0), console block unchanged and still ending at `attempting to load /sbin/launchd` with no failure line, one run, `No errors detected`, watchdog armed at `0xf9017000` and not needed |
| stage90 | [experiment-504](experiment-504-the-first-value-a-driver-hands-back-and-the-load-that-had-to-become-a-copy.md) | **the first value a driver hands back, and the load that had to become a copy - process 1 opens `/dev/rmd0` and the `read` after it returns 4 bytes whose first word is `0xfeedface`, and getting there took two runs because the first one's instrument faulted on the very buffer it was measuring.** 503 left the ceiling in one sentence - the fixture "has no way to open a file, and there is nothing for it to open" - and this step removes both halves: the program grows from thirty-seven instructions to **fifty-two** (208 bytes, `.if`-asserted and word-by-word checked), and what it opens is the node the boot's own memory device printed four lines into the OS console (`Added memory device md0/rmd0 (02000000/0D000000) at 000000008050D000 for 0000000000002000`). `mdevadd` makes **two** nodes per device (`bsd/dev/memdev.c:607` `DEVFS_BLOCK "md%d"`, `:616` `DEVFS_CHAR "rmd%d"`) and they are not two spellings of one thing: the block node's read is `spec_read` and last-resort `mdevstrategy`, with a `panic("mdevstrategy: source address %016llX not mapped")` at `:322` if the address it computed is not the one it meant, while the character node's read is `mdevrw` (`:204`) whose body from the driver's side is one `uiomove64` (`:232`). Three properties make it the safe one and all three are readings: `mdevopen` (`:191`) cannot refuse an `O_RDONLY` open (it returns `ENXIO` for a bad minor, `EACCES` only for `flags & FWRITE`), the copy goes to the process's own *virtual* address because `mdevrw` re-writes `uio_segflg` to a `UIO_PHYS_*` form only when `mdFlags & mdPhys` is set and the payload's `mdevadd` passed `phys = 0` (`xnu_live_mdevadd_phys` 0), and the address is one 480's store already wrote into the page. The two paths are `adr`-loaded, so each address is a value the image *computes* once and the host check reads out of the instruction stream and cross-checks against the strings - the alternative, two `.word` literals, is this project's oldest defect in its plainest form - and the second call is the control: the *same* syscall with the *same* three arguments and a name no driver can have made a node for, so the two records differ in one word (the path, `0x000011b0` and `0x000011bc`) and the two errors have to differ, `0` and `ENOENT` = 2 - not a number chosen here, but the one XNU's own loader printed for its own missing path. **The readings, run B**: `open` path `0x000011b0` flags 0 mode 0 error 0 fd 0, then `read` fd 0 buf `0x00102000` nbytes 4 error 0 **ret_lo 4** ret_hi 0 with **`word_before` `0x00102000`** (the address `mmap` returned, in three records at once) and **`word_after` `0xfeedface`** (the RAM disk's own `MH_MAGIC`: `mdBase << 12` is `0x8050D000`, `uio_offset` is 0, the file's first four bytes are `ce fa ed fe`), then the control with **error 2**; the spin after it runs `getpid_count` to `0x800000`, and the two asks reproduce 503's shape a run later (152554 and 891390 ticks, ratio 5.84 against an 8:1 ask). **Run A is the step's other half and it is a defect with a location**: the wrapper read the buffer with a plain kernel load, `word_before = out[0]`, and the kernel took a data abort at exactly that instruction - `pc = __wrap_read+0xb8` = `ldr fp, [r8]`, `far = 0x00102000`, `DFSR = 0x5` (a *section translation* fault, i.e. no translation at all, not a permission fault), `recover = 0` - so `sleh_abort` had no recovery address to point `pc` at and the record ran to its cap of **64 entries at one repeated `pc`**; `xnu_live_sleh_seen` reached `0x40` while `_armed` was **2 against 28** and `_redirected` **0 against 26**, because the thread had stopped taking those faults at all. The fault is two instructions before `bl __real_read`, so run A's log holds **no `xnu_live_read_*` key**, no second `open` and no `xnu_live_getpid_count`: one instrument fault cost the boot's own tail as well as its own records. The mechanism is Apple's and the linked image states it: `COPYIO_MAP_USER`/`COPYIO_UNMAP_USER` (`osfmk/arm/machine_routines_asm.s:548-561`, `:601-610`) write the thread's user page-table walk table and ASID into `TTBR0`/`CONTEXTIDR` around the copy and put the kernel's back, and the four `mcr`s are in this build's `copyout` at `0x80015d8c-0x80015da0` and `0x80015df0-0x80015e00` - so while the kernel runs, `TTBR0` holds the *kernel's* tables, and a wrapper that skips that dance is not copying user memory, it is dereferencing an address that happens to belong to a process. The fix is the function that already does it, `copyin_word` (`:719-752`, declared `kern/misc_protos.h:101`, the same call `kern_event.c:2095` and `sys_ulock.c:455` use), and the wrapper now publishes **`copy_before`/`copy_after` beside the two words** - `0` means the read is a read, non-zero is the `EFAULT`/`EINVAL` returned instead - which is the key run A could not print. `build_entry.sh` gains a `xnu_entry_504` clause that the name is not in pass 1's undefined set, 471's rule for 504's reason: a name the generator stood in for would make the two words facts about the stand-in. The step's own tools: `host_ramdisk_macho_check.py` **73 -> 93 mutations refused** (the program is 52 words; the decode gains `mov rd, rm` - masked with `Rn` *included*, because `mov` is `add` with `Rn = 0` and a mask that leaves `Rn` free reports a correct instruction as a wrong one - and `adr`, whose target is read out of the `add`/`sub` of `pc`; six mutations are new, three of them byte-valued strings); `check_sysent_table.py` **14 -> 18 refused**, 10 witnesses, `WRAPPED`/`WITNESSES`/`MUNGERS` gaining 3, 5 and `munge_www` for both (all three slots take three 4-byte arguments), with the real `read` and the real `open` in their slots as two mutations - either would leave a fixture that reads a driver and publishes *nothing*, which is run A's symptom arrived at from the other side; `check_os_entry.py` 30 refused (its two fault sites are in 480's block and did not move, the wording went from thirty-seven words to fifty-two); the wrap census **60 -> 62** with `open` and `read` in `by_address` beside `getpid`, `mmap` and `poll`, because their only reference in the whole image is the word in `sysent[3]`/`sysent[5]`; `check_timer_line.py` 44/20, `check_irq_routing.py` 9/57, `check_driver_catalogue.py` 18/131 unchanged and re-run, index 481 rows. Entry image `.text` 5280960 -> **5285824** (+4864: the program, two wrappers, two note functions, the list entry) -> **5285984** (+160 for the fix: the two `copyin_word` calls, two spill words, two more keys), `.bss` 362824 -> **362888**, `bss_end` `0x805983c8` -> `0x80598408`, `xnu_entry_checksum` `0x90702371` -> `0x907024b1` - and **the image's file is 5503612 bytes in all four builds**, 503's band lesson read again: `image_bytes` is `__bss_start` = `0x8053fa7c`, which is `.init_array`'s end, and `.data` begins at the 16 KB-aligned `0x8050c000`, so `.text` can grow by kilobytes without moving one byte of what the payload copies. The rest of the boot is unmoved and this time *across four runs*: the OS console block is byte-identical in 502's, 503's and both of 504's (25 text lines, 1289 bytes, sha256 prefix `424e1b747e8bafb2`, same pad line), the frontier is still `load_init_program: attempting to load /sbin/launchd`, run B's abort record is 502's and 503a's entry for entry (entries 1..4 the boot's own demand faults, 5 and 6 the fixture's own page at `pc = 0x00001118`/`0x00001124`, 7 and 8 `Lcopyin_wordwise_loop + 0x0` at `far = 0` with `recover = 0x80015eec = copyio_error` - **the kernel's own copy path faulting on a null user address and being redirected to `EFAULT`, which is not a failure but the mechanism working**, 28 armed and 26 redirected, the same four numbers), `_irq_timer_count` 0x800 with `_irq_late_count` 0 and `_isr_calls` 1/2/3 on intid 0x28, logs 567625 and 570316 bytes, both `No errors detected`, device back on its own in both runs. Two runs, non-persistent `fastboot boot` only, nothing flashed. |
| stage90 | [experiment-503](experiment-503-the-first-block-and-the-leeway-the-kernel-adds-to-every-deadline.md) | **the first block, and the leeway the kernel adds to every deadline - a user process makes the kernel park a thread for the first time on this machine, and the two intervals it measures are not the two it asked for.** 502 left the frontier at `load_init_program: attempting to load /sbin/launchd`, and the walk's ledger has said for a while that this is not a stop: the exec returns 0 (that function prints on failure and on nothing else, `kern_exec.c:5079-5173`) and `/sbin/launchd` *is* the 37-instruction fixture, which had only ever asked for things that answer immediately. The third syscall is the one that waits, and it is the smallest wait there is: `poll(NULL, 0, ms)`, Apple's own zero-descriptor sleep, twice, at an 8:1 ratio - because a wake that came from anywhere but the countdown would return after about the same number of ticks whichever timeout was asked for, and one sample cannot tell that from a clock. The fixture therefore grows to **thirty-seven instructions** (148 bytes, `.if`-asserted in the assembly and word-by-word in `host_ramdisk_macho_check.py`), and **neither ask branches on the answer**: this program is `initproc`, so a fault here is 478's `launchd_crashed_panic`, and a `poll` that returned an errno is a reading rather than a reason to end the boot. The munger is the ABI claim: `sysent[230].sy_arg_munge32` is `munge_www` - `munge_wwwwwl` minus exactly the word `off_t`'s 8-byte alignment inserts into `mmap`'s struct - so the fixture's three register writes are the three arguments and **there is no padding word**, which is the one place this call differs in kind from 480's; `check_sysent_table.py` now names both mungers, gains `(230, "poll")` as its tenth witness and two mutations (the real `poll` in the slot, and the six-argument munger in 230's), 12 -> **14 refused**. The readings: both calls returned **0** with the caller `0x80284848` = `unix_syscall+0x100`, and the blocks are **164047** ticks (8.544 ms) for the 5 ms ask and **900814** (46.917 ms) for the 40 ms one - a ratio of **5.49, not 8** - timed by `entry_counter()` (`mrrc p15, 0`, the counter `mach_absolute_time` reads). The kernel's own deadline-family records name both armings (`source 2`, `flags 0x10` = `TIMEOUT_URGENCY_USER_NORMAL` and *not* the delayed-call timers' `0x21` = `TIMER_CALL_SYS_CRITICAL\|TIMER_CALL_LEEWAY`, on a heap `thread->wait_timer`), and their `now` is 910 and 342 ticks after the respective wrapper's `before`: **requested 129841 and 803662 ticks**, i.e. **33841 / 35662 above the ask** (1.763 / 1.857 ms), with the wake arriving **33360 / 96810 later still** (1.738 / 5.042 ms). The second number is Apple's own arithmetic: `timer_call_enter_internal` computes `slop = timer_call_slop(...)` and does `deadline += slop` (`timer_call.c:620`, `:1784-1830`), where the slop is `MIN((deadline - now) >> tcs_shift, tcs_max_abstime)` scaled by `timer_user_idle_level` and picked from `arm_timer.c:256-274` by urgency and priority - and the caller's `TIMEOUT_NO_LEEWAY` is only a *floor* (`if (flags & TIMER_CALL_LEEWAY) && leeway > slop`), so `poll` cannot decline it. That is why the lateness grows with the ask (a fraction of the time-to-deadline) and then stops (a cap); which row and what idle level are owed, with `timer_call_slop` itself as the instrument. The first number has no such source and is a defect with a location: `poll_nocancel` makes the timeout absolute on `getmicrouptime`/`microuptime`/`clock_get_system_microtime` (`sys_generic.c:1722`) and `kqueue_scan` subtracts `clock_get_uptime`/`mach_absolute_time` (`kern_event.c:6054-6063`), so the two clocks' disagreement lands in the deadline unremarked - and here they disagree by 1.76 and 1.86 ms. The step's own instrument needed a fix first: 481's `tmr_enter` window publishes the first 64 armings of *any* source, which in this boot are all the quantum metronome (source 3), so the deadline family a parked thread is made of was published by **no rule** (`xnu_live_tmr_src2` was 1 in 502's run) - `entry_timebase_note_timer_enter` now branches on `source == 2u` into its own keys and its own bound, and `check_timer_sources.py` gained six claims and six mutations for it (the branch, its eight keys, its bound, its counted overflow, and that the two rules share no key), 42 -> **48 refused**. The bound is **24** and says why: the run's own count is 18 - 8 would have published the boot's delayed-call timers and dropped calls 9 and 10, the polls, which is the same defect one level down. `check_timer_line.py` 44 citations/20 refused, `check_os_entry.py` 30 (its two fault sites at `entry+0x38`/`+0x44` are in 480's block and did not move), `check_irq_routing.py` 9/57, `check_driver_catalogue.py` 18/131, the index 479 rows, and the wrap census now **60** with `poll` in `by_address` beside `getpid` and `mmap` because its only reference in the image is the word `sysent[230].sy_call`. Entry image `.text` 5280384 -> **5280960** (+576) with the file at **5503612 both times** - and `STAGE90_TMR_DL_SHOWN` 8 -> 24 rebuilds to the same `.text`, because it is the same instruction with a different immediate - so the payload does not move at all (`kernel_size` 5999040, `stage90.img` 6002688, `stage90-qcdt.img` 8523776, RAM disk still `0x8050d000 + 0x2000`). The rest of the boot is unmoved and *byte-identically* so: the OS console block is the same 131083 bytes with the same `a6222524d5fa6a9b` prefix - stronger than 502's one-line diff, because this step did not move `STAGE90_XNU_RAMDISK_VA` - the frontier is still `load_init_program: attempting to load /sbin/launchd`, `_ostext_chars` = `_ostext_total` = 0x3ee with `_ostext_heals` 1, `getpid_count` still `0x800000`, `sleh_storm` 9, `_isr_calls` 1/2/3 on intid 0x28, `_irq_timer_count` 0x800 with `_irq_late_count` 0, log 569236 bytes, `No errors detected`, device back on its own. Two runs, non-persistent `fastboot boot` only, nothing flashed. |
| stage90 | [experiment-502](experiment-502-the-compare-values-and-the-counter-that-is-not-the-cores.md) | **the compare values, and the counter that is not the core's - 501's unread register is a countdown to an *unprogrammed* compare value, and 501's "the two routes agree" was two different counters agreeing inside a slack wider than their offset.** A generic-timer countdown is a view of a compare value (`TVAL = CVAL - counter`), so the invariant `TVAL + counter` is a prediction with no unknown in it, and the armed side is the control: `_cv_p_cval_lo - _cv_p_arm_ct_lo` is `0x0002ee00` = 192000 = `_arm_ticks` to the tick in **both** runs, while the virtual side's sum is 5 and 4 (then 4 and 6) - **zero** - and its control word at the derived `0x03C` reads 0 where the core's `CNTV_CTL` reads 1. So `0x038` is a countdown to CVAL = 0 and the frame's virtual timer is **stopped**: with a compare value the counter passed long ago, an enabled unmasked timer would read `ISTATUS`, and nothing will assert intid 39 (501's owed item) until `0x038` and `0x03C` are programmed. The second half answers 501's other half. Its bracket required the core's reading between two frame readings and measured `_cv_inside` **0 of 8** with `_cv_d0` 70 against `_cv_wmax` 13 - outside past the **upper** end by `0x39`, a case its own comment had not named - and one order cannot separate two counters from a late-returning path, so the step reads every pair **both ways round**: with `a` the reading spacing, `K` the phase and `dL` the two paths' latency difference, order X gives `+a+K+dL` and order Y `-a+K+dL`, so the sum is `2K+2dL` and the difference is `2a`. Three pairs close it: `_cv_xp_sum` is **9** over sixteen samples (0.6 ticks, mirror bracket 8 of 8) so the frame's `0x000` **is** the core's counter and 501's "7 ticks" was the read cost; `_cv_win_sum` is **-1011** - the frame's own `0x008` against its own `0x000`, one read path at both ends so no latency can sit in it - i.e. 63.2 ticks per sample with the two orders agreeing to 2 in 1011; and `_cv_xv_sum` **1022** with `_cv_xv_sum - _cv_xp_sum` = 1013 against `_cv_win_sum` 1011, **the same quantity by two routes one of which is path-free, agreeing to 0.2%**, which is what closes the escape hatch quantitatively. The frame carries two counters 63 ticks apart; 501's `_rt_cntv_*` comparisons carry that bias and 63 < 104 is why it did not show. The rate is measured, not assumed: `_cv_r_fd` 32782 against `_cv_r_cd` 32781, `_cv_r_ppm` **1000030** (30 ppm = one tick of the interval), with the loop bounded by the frame's counter *and* by `mach_absolute_time` so a stopped counter ends it. `check_driver_catalogue.py` **18 claims/120 mutations -> 18/131**; **two clauses went blind on the step's own new code** (the bracket clause searched "the region before the first key" for a two-ended test and the new loops' `msm8974_cpu_cntvct()` calls satisfied it - refused now by reading the loop's own braces and *counting* brackets), which is the third step running where a check failed on its object's new neighbour; `check_timer_line.py` **44 citations checked** and 20 mutations refused, with one new clause - the frame's virtual control word, the one offset the driver *derives* rather than shares with the device's kernel, is refused as a literal - and the draft's "501: 42" corrected by measurement: **501's own tree says 37** (re-run in a worktree at `4767904`), so the step added seven citations rather than two; `check_irq_routing.py` 9/57. Entry image `.text` 5275520 -> 5277600 -> **5280384** (+4864) with the file at 5503612 both halves - the image is padded to 16 KB and only the first half crossed a boundary - and **501's published `.text` was `.text + .sysctl_set`**: its own log says 5275520, so its `+8168` delta was right and its absolute was not, which is 461's defect with both ends inflated by the same 4776. The boot is unmoved and the step found that **the console fingerprint this project quotes is a property of the build**: the 1288-character block differs from 501's by exactly one line, XNU's own `Added memory device ... at <RAMDISK_VA>`, and with that line normalised both runs hash `47c53d7558e1b2a6` - the frontier is still `load_init_program: attempting to load /sbin/launchd`, `_isr_calls` 1/2/3 on intid 0x28, `_irq_timer_count` 0x800, `sleh_storm` 9, log 564354 bytes, `No errors detected`, device back on its own. |
| stage90 | [experiment-501](experiment-501-the-other-route-agrees-and-the-offset-nobody-read-is-not-the-register.md) | **the other route agrees, and the offset nobody read is not the register - the frame's counters are read a second way for the first time, and the coprocessor confirms every offset except the one that file defines and never uses.** 500 ended with a driver that owns the frame's line and programs its device, and with two things it had never done: it had read the frame *only* through the frame, and it had never looked at the region the frame's `reg` calls its second view (`reg = <0xf9021000 0x1000>, <0xf9022000 0x1000>`; the binding's own words are "the second view base address is optional", `arch_timer.txt:43-44`). Both are now readings. The second view **maps and reads zero everywhere** (`_v2_phys` 0xf9022000, `_v2_va` 0xc218d000, `_v2_len` 0x1000, `_v2_objkind` 2; `_v2_freq` 0, `_v2_cntv_lo` 0, `_v2_cntp_lo` 0, `_v2_tval` 0, so `_v2_cntv_d` is 0x06ed5bca - the frame's whole counter, not a read's worth, `_v2_cntv_ok`/`_v2_cntp_ok` 0), so it is **not** a second window onto this frame. The point of the step is the other half: that kernel holds **two routes to every counter** (`counter_get_cntpct_mem` `:297` / `counter_get_cntpct_cp15` `:310`, `counter_get_cntvct_mem` `:318` / `counter_get_cntvct_cp15` `:331`) and picks one at boot (`:607` `if (!has_cp15)`, with `has_cp15` true on this CPU), so the loser is never read again - **two definitions of one value in one file, the choice made and never compared**, this project's oldest defect family sitting inside the evidence for the frame rather than in our own code. Reading the frame's registers through `mrrc p15` makes the comparison that kernel cannot, and it came out as the driver's own table: `CNTPCT` 7 ticks apart, `CNTVCT` 0x47 apart, both inside a *measured* `_rt_slack` of 0x68, and `CNTP_CTL` **equal exactly** (`_rt_ctl_fr` 2, `_rt_ctl_cpu` 2, `_rt_ctl_agree` 1, with `_rt_cntv_ctl_cpu` 1 the independent reading that XNU's decrementer was left enabled and unmasked). **The one disagreement is the finding**: the frame's `0x038` reads `0xf912a3cc` where the coprocessor reads `0x0002d8cb` (`_rt_tval_d` 0x06f034ff, `_rt_tval_ok` 0), and `0x038` is `QTIMER_CNTV_TVAL_REG` (`arch_timer.c:67`) - **the only one of that file's eight timer offsets with no reader anywhere in it**, the frame route that kernel does use writing and reading `QTIMER_CNTP_TVAL_REG` instead. No amount of re-reading that kernel would have found it; only a reading of the register could, and the step says what the register is *not* and stops, because what it is can only be answered by a *write* and the register in question is the deadline XNU's own `entry_timebase.c` sets (`c14, c3, 0` / `c14, c3, 1`). The step is read-only by construction, and structurally so: `check_driver_catalogue.py` goes **16 claims/99 mutations -> 17/110**, claim 17 ("a driver that reads a device reads every way the device can be read, and holds them together") requiring that the region between the second view's mapping and the last coprocessor read **stores to nothing**, and its eleven mutations were joined by three *accepted* ones that are all one defect - a reader that matched something other than its object (a needle that found `mrrc p15, 1` in the *comment* naming the device's kernel's instruction, so encodings are now searched in the source with its comments stripped; a `0x000u` literal the offset pattern's missing `u` could not see; a pair-clause that listed only the *frame* side's words, so the coprocessor's half could have gone missing silently). `check_timer_line.py` gains a third anchor list for that kernel's counter functions and the switch between them - **37 citations checked** - and it failed **our own correct file** twice before any mutation found it: a prose line carrying two names and two citations is read as a cross-pairing, so the rule is one citation and one name per line, which is the same lesson 500 learned from the other end. `check_irq_routing.py` unchanged at 9/57, entry image `.text` 5272128 -> **5280296** (+8168), `.bss` 362824 **unchanged** (no file-scope storage: locals published where they are read), 27 undefined and 59 wraps unchanged, artifacts `0352455a`/`d4915fcf`/`75af3797`/`38ba7e27`, and the post-run comment corrections are proved comment-only by rebuilding all four byte-identical. The rest of the boot is unmoved (OS console block byte-identical, 1288 chars, sha256 `a0593af0...`; `_irq_timer_count` 0x800, `_irq_late_count` 0, 500's three deliveries in shape), the run is 561874 bytes with `No errors detected` and the device home on its own. Owed: what the frame's `0x038` actually *is* (needs a write, so it needs a design that cannot move the kernel's deadline), the frame's second line intid 39 - now a question with a datum attached, since the second *view* is empty - the `AckC`/EOI question for a level line, and the registry's capacity stop, still never taken.
| stage90 | [experiment-500](experiment-500-the-piece-that-was-missing-was-the-deadline.md) | **the piece that was missing was the deadline, not the line - and the run says the distributor was already armed.** 500 set out to arm two things for the driver that owns the timer's line: the frame's countdown, and the distributor's per-line state on intid 40 (the GICv2 SPI blind spot: `ITARGETSR` 0 at reset means *no CPU targeted*, so the line is dropped whatever else is set). The run read `_line_isen_before = 0x00000100`, `_line_target_before = 0x01010101`, `_line_group_before = 0` and `_line_pend_before = 0` - **every distributor write this step makes was a write of a value that was already there** - because the boot before this image was Android's own kernel, which takes *this frame's* deadline on *this SPI*, so the line is one Android enables at its own boot. What had been missing in 498 was the **device**: nothing in the boot had put a deadline in the frame, and a line with no device behind it asserts nothing. The mechanism is now a driver's: `start` derives the count from the frame's own frequency (`_arm_ticks = 0x2ee00` = 192000 at 19.2 MHz, never written down), writes `CNTP_TVAL` then `CNTP_CTL` (`_arm_ctl_before` 2 -> `_arm_ctl_after` 1, with `_arm_tval_after` 27 ticks down), reads the enable back (`_arm_rc` 1), and enables the line (`_arm_line_rc` 1) - and the dispatcher called the driver's handler **three times** (`_isr_calls` 1, 2, 3), each with `_isr_intid` 0x28 and `_isr_agree` 1: calls 1 and 2 reload the deadline from the file-scope record (`_isr_rearmed` 1 then 2) and read `_isr_ctl_after` 0x1 - the reload cleared `IT_STAT`, which is the acknowledgement that *makes* the re-arm branch a legal handler - and call 3 masks (`_isr_masked` 1, `_isr_ctl_after` 0x7, byte for byte 498's end state), with no fourth call. `_isr_tval` is *negative* (-141, -82, -90 ticks = 7.3, 4.3, 4.7 us of delivery latency): `CNTP_TVAL` keeps counting below zero. **The order the claim enforces is discipline, not the safety argument** - the draft said the frame must be programmed while the line is still disabled, and the run shows that window was already open before the driver ran and that the *mask* is what makes it harmless (`_arm_ctl_before` 2, `_isr_ctl_after` 0x7: the frame is masked whenever no deadline is being serviced). `check_driver_catalogue.py` 15 claims/81 mutations -> **16/99**, claim 16 being the arming chain, with two reader defects fixed on the way (a **declaration read as a call**, so an order clause compared the wrong two offsets; and a **comparison read as an assignment** - `arm_ticks == want` matched `\s*=\s*` and the capture ran through a comment to the next statement's `;`, so a literal deadline was *accepted* by the clause that exists to refuse it; `=(?!=)`). **Claim 15 was stating something false**: its reader required the `refCon` to be spelled `(uint32_t)(uintptr_t)this`, so 498's own registration was invisible to it and it printed "`MSM8974Timer.cpp` owns no interrupt line" about the one file that had just been given one - the reader now takes the argument whatever its shape and the claim states its subject (the driver whose own device is the distributor) instead of choosing it by a reader's narrowness. `check_timer_line.py`'s claim 5 needed the same repair: it read the handler's device write by looking 120 characters past the store for `IT_MASK`, a window measured on 498's *one-store* handler, and 500's two paths put the mask outside it - **the check failed a handler that still masks on its last call**; the question is now asked of every store in the body. `ENTRY_LIVE_CAP` 4096 -> **8192** (498 wrote 3967, 97%, and 500 adds 69; this run wrote 4036 and published `xnu_live_cap`) - a buffer whose occupancy is not published is one nobody can see filling. Artifacts: `xnu_arm_entry.bin` `c0a466cd...`, `stage90.bin` `8c91ce7d...`, `stage90.img` `f3e988b8...`, `stage90-qcdt.img` `2f39e8e5...`, `.text` 5272128 (+1568), `.bss` 362824 (+32), 27 undefined, 59 wraps; the post-run comment corrections are proved comment-only by rebuilding to **byte-identical** artifacts. The rest of the boot is unmoved - the OS console block is byte-identical to 497's and 498's (1288 chars, sha256 `a0593af0...`), `_irq_timer_count` reached `0x800`, `_irq_late_count` 0, `_irq_other_count` never taken, `No errors detected`, device back on its own |
| stage90 | [experiment-499](experiment-499-the-clause-that-reads-a-citation.md) | **the clause that reads a citation asked for the right number and never the wrong one - and 498's committed check exits 0 on the tree that has the defect.** 498's claim 1 ends by deriving the two calls' line numbers from the device's kernel and requiring `arch/arm/kernel/arch_timer.c:NNN` or `` `:NNN` `` to appear in each of our two sources; that is a search of the *file* for one string, so a second, stale citation of the same call is invisible to it, and eleven of the thirteen numbers in the block that grounds the frame's offsets were never read at all. Six citations and a seventh number are wrong: `QTIMER_FREQ_REG` cited at `:66` is at `:65`, the three `ARCH_TIMER_CTRL_*` defines are cited one line *above* each — so `ENABLE`'s citation points at a **blank line** — and the two call citations are the `:631`/`:637` pair 498's own commit message says it corrected, still standing in the block they were written in because the correction landed in a second comment six hundred lines below, which satisfied the clause on its own. The new clause binds each citation to the name written beside it on the same line and requires that name to be on the line the citation names (**17 citations read**, and the count is published as a note because a clause that can match nothing must say how many things it matched); a citation with a *path* before its number belongs to the file the path names, which is the scope that stops it reading `msm8974.dtsi:161-167` as a line of the kernel; and a bare `:NNN` with no name on its line — the shape the wrong number actually had — must name one of the two calls the block quotes. Two defects of the clause were found while writing it: the block finder looked at the whole line rather than the text after the comment marker, so the leading `*` counted as a space, the block was never found, and the bare-citation half was **dead code that read as a passing clause** (the count exposed it, and a guard now fails when a source cites the kernel by path with no block opening that way); and the bare rule fired on another file's citation. `check_timer_line.py` 8 claims/16 mutations → **8/18**, `the_grounding_block_cites_the_line_before_the_call` (restoring `:631`, which 498's own script was measured *accepting*) and `the_grounding_block_cites_another_define_line` (restoring `:66`, the kind that reads as plausible because it names a real neighbouring define). The edit to the driver is comments only and that is proved by rebuilding: the four-step build is green with 498's exact four hashes (`xnu_arm_entry.bin` `7c3dbe7c…`, `stage90.bin` `13a1ee83…`, `stage90.img` `c0545590…`, `stage90-qcdt.img` `39e0d1c6…`), 498's `.text` 5270560 and `.bss` 362792 — **no device run, because the image is the one 498 ran twice, byte for byte** |
| stage90 | [experiment-498](experiment-498-the-frame-is-the-device.md) | **the frame is the device, and its line is a cell - and the first run aborted on the *spelling* of one property, which is the measurement.** The timer node's `reg[0]` (`0xf9020000`) is a container, which is why 494 read a stored zero out of it: the device's own tree (`msm8974.dtsi:153-167`) and the device's own kernel (`of_iomap(frame, 0)` `arch_timer.c:623`, `irq_of_parse_and_map(frame, 0)` `:629`) both say the timer *is* the `frame@f9021000` one `reg` entry later, and that the physical timer's line is the frame's *first* `interrupts` specifier - `<0 8 0x4>`, SPI 8, which the controller's `#interrupt-cells = <3>` and the GIC rule (`type 0` => `32 + num`) turn into **intid 40**. Run 1 booted a tree whose controller carried `phandle`, drove XNU's own mapping path for the first time (adding `interrupts` to `/timer` made the plane's publication walk map it) and **aborted**: `AddPHandle` registers the property named `gIODTPHandleKey` = **`AAPL,phandle`**, not `phandle`, so `gIODTPHandles` stayed empty, `FindPHandle(1)` answered 0 and `IODTGetICellCounts(0, ...)` loaded a vtable from address 0 - `panic(cpu 0 caller 0x80450e88): kernel abort type 4: fault_type=0x1, fault_addr=0x0`, `pc 0x80179428` = `IODTGetICellCounts+8`, `lr 0x80177a24` = `IODTMapInterruptsSharing`, followed by `MACH Reboot` and a device that came back on its own. **The step's own check had passed that tree**: claim 3 compared two *values* (both 1) and never the key, and the comment beside the property asserted the right pair with the wrong name - one decision, two spellings, and the half that was right got checked. So the step has a **second run on a byte-identical entry image** (`7c3dbe7c...`), with only the payload's tree changed (`kernel_size` 5982648 -> 5982656, eight bytes for `AAPL,`), and every number is in it: `_frame_phys = 0xf9021000`, `_frame_len = 0x1000`, `_frame_va = 0xc202d000`, `_frame_freq = 0x0124f800` = 19200000 with `_frame_freq_agree = 1` (the third definition of that frequency, read as a register), and the rate **measured** rather than declared - `_frame_cnt_d = 0x00037f8a` against `_frame_mac_d = 0x00037fa7`, a difference of 29 inside a `_frame_slack` of 112 (four frame reads, derived not chosen), so `_frame_rate_ok = 1`; and the line - `_line_type = 0`, `_line_num = 8`, `_line_trig = 4`, `_line_rule = 1` (the rule is published beside the number it produced, because 8 and 40 are both defensible readings and only one is this machine's), `_line_intid = 0x28`, and **the OS's own reading of the same property**, `_osmap_ok = 1`, `_osmap_n = 1`, `_osmap_same = 1`, `_osmap_ph = 1`, `_osmap_ph_agree = 1` - the *only* group run 1 could not produce, and the reading that says the repair reached the right place. The registration is 496's registry, not `IOService::registerInterrupt` (`_cli_rc = 1`, a fourth client at intid 40), because `IOPlatformExpert::lookUpInterruptController` (`:379-397`) is an unbounded sleep whose only waker's only caller is `IOCPU.cpp:783`, which this image never reaches - **no source in the image may call it**, and a new scan over `xnu_platform/` and `xnu_arm_boot/` refuses the build if one does (that scan's first version reopened the path the mutation edited, and reported `ok` about a file the mutation had changed - a report true of the wrong object, third shape). The handler masks the frame (`CTRL |= IT_MASK`) before it publishes and before it returns, because a level-sensitive line the device does not drop is re-entered forever; `_isr_calls = 0` because **nothing in this step enables the line** - arming it, the distributor's enable/target/priority, and the `AckC` question are the next step's. `tools/check_timer_line.py` is new: **8 claims / 16 mutations**, all refused, in `build_entry.sh` in both modes, and its claims compare this repository's two halves of one fact - our driver's offsets against the device's own kernel's `QTIMER_*`, our `/timer`'s line and the frame's *address* against the device's own tree's `frame@` block - because Apple's XNU has no driver for this block and the device's kernel is the only authority in the repository. Two of its clauses came from defects: the **citations** (both line numbers in the prose were wrong - 631/637 for 623/629 - for as long as the code and the check agreed with each other, and nothing could have said so; the number is now derived from the kernel's text in both directions), and the **key names** (derived from Apple's `OSSymbol::withCStringNoCopy` declarations, which is the clause run 1 added). `check_irq_routing.py` is now 9/57, `check_driver_catalogue.py` 81 (its object-kind needle was `xnu_live_\w+_objkind`, which the new `xnu_live_timerdrv_frame_objkind` satisfied - so deleting the *entry's* kind stopped refusing the mutation while the check printed `ok`). The rest of the boot is unmoved: the OS console block is byte-identical to 497's (1288 characters, sha256 `a0593af0...`), `sleh_seen = 0x20`, `sleh_armed = 0x1c`, `sleh_redirected = 0x1a`, `sleh_storm = 9`. Found on the way and not by the device: `assemble_arm_layer.sh` writes every configuration's objects to `out/xnu_asm_obj`, so a run without `XNU_KERNEL_CONFIG` rebuilds the layer from RELEASE's defines over this one - the image came out with `machine_load_context` reading `TH_CTH_SELF` at 1480 where the configuration's `assym.s` says 1496, and `check_asm_config.py` is the only thing between that and a boot. |
| stage90 | [experiment-497](experiment-497-a-record-the-image-does-not-have.md) | **a record the image does not have — and the artifact is byte-identical, which is the measurement.** 496 ended with a list whose first item was a plan ("replace each `entry_live_write("key", local)` with the variable it is supposed to be the record of"), and 497 applied it and measured the result: **the plan works for four of the six records and fails for two** — `g_irq_cli_last` and `g_irq_icfgr_word` were each given a reader on the very next line (`IRQ_LIVE(..., g_irq_cli_last)`) and were removed by the compiler **anyway**, because the key beside the store *is* a reader and store-to-load forwarding across one line is free; the four that survived (`g_irq_first_iar`, `g_irq_last_iar`, `g_gic_isr_last`, `g_irq_icfgr_shift`) survived because other calls happen to sit between store and key, which is a fact about inlining and interruption and not about the source. **So the rule is not "give the record a reader" but "a file-scope record earns its storage by being read where its value is not already known"**, and the seven declarations are **subtracted**: each key publishes the value the function holds at that moment (a parameter, a local, an expression) — `xnu_live_irq_first_iar` from `iar`, `xnu_live_irq_other_iar` from `iar` on the stop path, `xnu_live_irq_icfgr_word` from `word`, `xnu_live_irq_icfgr_shift` from `shift`, `xnu_live_irq_cli_last` from `intid`, `xnu_live_gicdrv_isr_last` from `intid` — with the reason written where each declaration was. The seventh needed a different argument rather than a different reader: `g_gic_isr_done` was written `1u` and read on the next line, so its value is defined by the **program point** and not by the machine, and a value that cannot vary has no business in `.bss`; the key is now a **literal**, and what keeps it honest is *where* it is published, which became a clause of the claim (inside the handler, after the withdraw block, so an early return or a kept line cannot reach it). **The proof that nothing behavioural moved is the image**: the 497 entry image hashes to `0934bbe9d268fd09e2920cc1484bcce4f005bd6daba9075620b6f807e82486d0` both built from 497's sources and with 496's two files put back (`cmp` IDENTICAL), `.text` 5266528, image 5487228, `.bss` `0x8053ba80..0x80594388` 362760, 27 undefined, 59 wraps — 496's six numbers — and the payload carries **496's exact three hashes** (`77fd1327…`/`53cd8c7e…`/`e8da7170…` at 5982532/5986304/8507392), so every store this step removes was *already absent from the artifact* and 496's two runs are runs of exactly this binary. The one hardware run is therefore narrow by design (exit 0, `/tmp/stage90-497-run1.log` 555319 bytes, `No errors detected`, device back on Android on its own): it is the reading that no **key** was lost with the variables — `_first_iar = 0x14`, `_first_icfgr_field = 1`, `_icfgr_word = 0xc04`, `_icfgr = 0xffffffff`, `_icfgr_shift = 8` (20 = the timer line), `_irq_timer_intid = 0x14`, `_cli_last = 0`×3, `_isr_last = 0`×3, `_isr_done` 0 then 1 — and `xnu_live_irq_other_iar` is ABSENT because the stop path has never been taken (`_other_count` absent again, no `stub_hit`, no `sleh` stop), so "the key is still there" is a claim about the source for that one and about the log for the other six. The key **set** is otherwise 496's; the only difference vs 496 run1 is `xnu_live_dec_same`/`xnu_live_dec_other`, which 496's own run2 also lacked (published only when a decrementer write matches the reference for the 2nd time inside a window the boot can close early), so it is a boot-to-boot timing property and not a regression — and no extra run was needed because 496's two runs already bracket it. Everything the boot decides is unmoved: console block byte-identical to 496's (1288 from the byte after the marker's newline, sha256 `a0593af0…`; 1289 by the other endpoint pair used here), `sleh_seen = 0x20`, `_armed = 0x1c`, `_redirected = 0x1a`, `_storm = 9`, `_drive_compiled = 1`, `_own = 0`, `_cli_rc = 1`, `_en_before = _en_after = 0x00107fff`, `_pend_before = 0x20400000`, `_isr_calls` 1→2→3, `_isr_pends` 1→2, `_isr_unregs = _isr_unreg_rc = 1`, `_cli_slot = 0`, `_cli_handler = 0x8027facc`, `_cli_capacity = 4`, `_cli_calls_total` 1→2→3, `irq_late_count = 0` with `irq_timer_count` to `0x800`, watchdog `_enabled = 1`/`_timeout_s = 0x19`/`_bite_truncated = 0`, and 495's three timer latencies for the third boot running — `_fire_lat_ns` **101.040729 / 101.044687 / 101.101250 ms** with `_gap_ns` **101.046979 / 101.051770 ms**. Checks: `check_irq_routing.py` 8/50 → **9 claims / 56 mutations**, all refused, and the new claim 8 is the only one here that **cannot be made from the source at all** — every non-`const` file-scope `static` object either file declares must be in the linked image, with C++ internal linkage looked up under the name the linker gives it (`_ZL<len><name>`; `nm` prints `_ZL14g_gic_isr_last`, so a lookup by the C spelling silently reports a present variable absent — this session's own first defect), **29 records checked** (20 in `entry_irq.c`, 9 in the driver), the driver's nine correctly filtered through an evaluator of the file's own `#if`/`#define`s because with `MSM8974_GIC_DRIVE_SGI 0` they are not compiled and demanding them would be wrong. The check was shown to refuse a **real** artifact and not only its mutations: against 496's sources it fails naming all seven, with the thesis in one message ("the record is written and never read, or read only where its value is already known, so the optimizer removed it — the source describes storage the machine does not have, every key that names it publishes something else, and the run still reads correctly"). Six mutations ship; a seventh (`the_driver_record_is_declared_outside_its_switch`) was **written and refused nothing** — a selftest can change the source but the symbols come from one real build, so removing a record from the compiled set leaves it present in that image — and was deleted rather than shipped as a mutation that tests nothing, with the conditional instead verified by a direct reading of the filter (`file_scope_statics(compiled_text(driver, switch=0))` → `['g_gic_starts']`). Owed: **a line owned by a peripheral** (still the top of the list, with the distributor's enable, the `AckC` question and the return path into `rtclock_intr`'s world), the conditional clause of claim 8 having no mutation of its own until two images are built from two sources (which the existing switch already makes possible), the unregister guard's asymmetry, a timeout that outlives its asker, `/timer`'s second definition, the other device nodes (`/arm-io`, `/cpus`, …), the two services the catalogue answers with nothing, `MSM8974RootResource`'s `state0 = 0`, a name/class reader wider than eight characters, the release as a reading, `vm_fault`, 488's flag-list, 490's frames band and `xnu_live_dec_same` |
| stage90 | [experiment-496](experiment-496-a-driver-owns-a-line-and-writes-its-device.md) | **a driver owns an interrupt line and writes its device — the first interrupt in this boot that a driver asks for and services itself.** 495 made the OS call a driver back on the OS's own timeout; 496 is the driver's own: `MSM8974GIC::start` writes `GICD_ISENABLER0` through the mapping 494 made, hands the entry image a function of its own file through `entry_irq_register_client`, asks for the line by writing `GICD_SGIR` with the target-self encoding, and the kernel calls *that function* from the exception path **three times, the second and third asked for from inside the handler**: `_isr_calls` 1 → 2 → 3 with `_isr_last = 0` each time, `_isr_pends` 1 → 2, `_isr_guard = 1`, `_isr_unregs = _isr_unreg_rc = _isr_done = 1`; the enable reads `_en_before = _en_after = 0x00107fff` (bit 0 was already set, so the driver's write is a proven-idempotent re-assertion and the record says so rather than claiming the driver turned the line on), `_cli_rc = 1`, `_mapvaddr = 0xc2196000` (the address the ISR writes the device through), `_own = 0`, `_isr = 0x8027facc`, `_sgir = 0x02000000`, and every `_isr_pend_after_ack = _isr_pend_final = 0x20400000` — bits 22 and 29 pending, this line not. **The first delivery happened inside the request, and the log's own key order is the evidence**: `start`'s two post-request keys (`_pends = 1`, `_sgir`) come *after* the whole handler sequence in both runs, so the machine took the SGI during `start` — store, `dsb sy`, dispatch, three calls, unregister — before `start` reached its next statement, and the three calls are sequential rather than nested because an SGI pended inside the handler cannot be delivered until the epilogue restores `CPSR`. **The stop is not taken, and the run says so by absence**: `xnu_live_irq_other_count` is ABSENT from both logs, there is no `stub_hit` line and no panic — an intid reaching the dispatcher with *no* client is a stop, so the driver withdraws the registration only when its own pending bit is clear (`_isr_guard`), and that guard is the whole reason the key exists. **The order of three stores is the safety property, and each is now checked**: the registry takes a slot only when its handler is zero and stores the handler **last** (a scan racing the write sees a slot that is not yet live rather than a live slot with the wrong fields), the unregister clears the handler **first** (the mirror), and the acknowledgement is written **before** the client is called. **The route not taken is read out of the tree rather than preferred**: `IOCPUInterruptController::registerInterrupt` (`IOCPU.cpp:840`) fills its vector and then blocks — `if (enabledCPUs != numCPUs) { assert_wait; thread_block; }` (`:875-880`) — and the only writer of `enabledCPUs`, `enableCPUInterrupt` (`:821`), **has no caller anywhere in this tree** (a whole-source grep finds the declaration at `IOCPU.h:133` and the definition, nothing else), while `IOService::registerInterrupt` (`IOService.cpp:6337`) calls that same function after a `lookupInterrupt`, so it either reaches the same block or returns having done nothing. Checks: `check_irq_routing.py` 7/36 → **8/50**, `check_gic_routing.py` 7/43 → **7/48**, `check_driver_catalogue.py` 14/57 → **15/81**, all mutations refused in both modes; claim 4 now pins the scan's *position* (after the timer case, before the spurious case), the per-slot cap and the stop that follows it, and counts `GICC_EOIR` writes (three) and `entry_epilogue` calls (two); the new claim 7 reads the registry's own contract; claim 15 requires **every device store in the driver to lie inside the switch's `#if` blocks**, so `MSM8974_GIC_DRIVE_SGI 0` is a machine that cannot write this device through any name in the file. **Three of the new clauses were first written against the wrong thing** — a needle carrying a trailing blank line where the file has `}` and one using five spaces where the file has six, both matching nothing while "passing"; and claim 15 computing its base from `mapvaddr` while the handler writes through the saved `g_gic_mapvaddr`, so it reported a defect against correct code (the claim now discovers the second base from the body and requires it to be assigned from a published variable, which is a claim with its own mutation). **Found while measuring the `.bss` delta: seven file-scope variables these two files declare and the image does not have** — six written and never read (`g_irq_first_iar`, `g_irq_last_iar`, `g_irq_icfgr_word`, `g_irq_icfgr_shift`, `g_irq_cli_last`, `g_gic_isr_last`), so the stores are dead and the key is published from the parameter or the local holding the same number a line earlier, and one read but constant-folded (`g_gic_isr_done = 1`). Every key is still the right value — the runs do not need re-taking — but the artifact has no variable behind the key, and the claim that would catch it (every non-`const` file-scope `static` a file declares is in the image's symbol table) needs the linked image, so the fix is its own step. `.text` 5264672 → **5266528**, image **5487228** (unchanged), `.bss` 362664 → `0x8053ba80..0x80594388` **362760**, 27 undefined, 59 wraps; `gen_assym.sh` **266 defines**, `assemble_arm_layer.sh` **17 ok / 0 failed**; payload `.bin`/`.img`/`.qcdt.img` the same three sizes as 495 (5982532/5986304/8507392) with new hashes `77fd1327…`/`53cd8c7e…`/`e8da7170…`. The console block is byte-identical to 495's (1288 characters, sha256 `a0593af0…`) and is the same span in all eight logs of 493–496 — with the note that the fingerprint is convention-sensitive by one character at each end (including the marker's newline gives 1289 characters and `609abc39…`, starting at the marker's `[` gives 1305 and `b31113ad…`), and both wrong endpoints have been reported in this project as "the record is wrong" when the record was right: the endpoints are a suspect before the data is (defect 298). 495's readings reproduce to the tick (`_fires` 0 → 1 → 2 → 3, `_fire_lat_ns` 101.102708/101.040677/101.042656 ms, `_gap_ns` 101.047083/101.050104 ms, `_due - _arm = 1 920 036` ticks = 100.002 ms), the fault counters are 495's (`sleh_seen = 0x20`, `_armed = 0x1c`, `_redirected = 0x1a`, `_storm = 9`), `irq_late_count = 0`, and all 60 `gicdrv` keys agree between the two runs except the seven that are heap addresses. Two runs, both through the gate, both exit 0, `No errors detected`, device back on Android on its own. Owed: the seven variables and their claim, **a line owned by a peripheral** (SGI 0 is the driver's own device; no real device asserting a line has yet been routed to a driver and cleared), the unregister guard's asymmetry (a stop and a hang look the same in the log by design, which is why `_isr_guard` and `_cli_unregistered` are separate keys), a timeout that outlives its asker, `/timer`'s second definition, the other device nodes, the two services the catalogue cannot answer for, `MSM8974RootResource`'s `state0 = 0`, a wider name/class reader, the release as a reading, `vm_fault`, 488's flag-list, 490's frames band and `xnu_live_dec_same` |
| stage90 | [experiment-495](experiment-495-the-os-calls-a-driver-back.md) | **the OS calls a driver back — for the first time the boot needs a driver rather than merely tolerating one.** 492–494 made a driver *read* its device, as a number out of a memory descriptor and then as a word through the OS's own mapping; none of it made the machine do anything the driver asked. 495 asks the OS for a timeout — `IOWorkLoop::workLoop()`, `IOTimerEventSource::timerEventSource( this, msm8974_timer_timeout )`, `addEventSource`, `enable()`, `setTimeout( 100 ms )` — and the kernel calls the driver back through four objects it created for it, **three times, on the thread-call daemon, with the work loop's gate held**: `_fires` 0 → 1 → 2 → 3, `_fire_lat_ns` (arm → *this* fire) **101.101 / 101.041 / 101.042 ms**, `_gap_ns` (previous fire → this) **101.048 / 101.049 ms**, `_rearms` 0 → 1 → 2 with `_rearm_rc = 0` twice (so the gate held across the callback and re-arming from inside it is accepted, which is what a real driver does). The arming is a chain of five calls that can each fail silently, and every one is in the record: `_ms = 0x64 = 100` (from one `#define` used by the arming, the deadline and the re-arm), `_wl = 0xc050f640`, `_ts = 0xc0543e40`, `_add = 0`, `_en = 1`, `_to = 0`, `_arm_lo = 0x06de06b6`, `_due_lo = 0x06fb52df`, with `_due - _arm = 1 920 041` ticks = **100.002 ms** for the 100 ms asked for. **The falsifier the step was built around fires in the affirmative, and it is a second, independent record of the same arming**: the entry image already wraps the old `timer_call_*` family, and `thread_call_enter_delayed_with_leeway` reaches `timer_call_enter_with_leeway` from `_arm_delayed_call_timer` (`thread_call.c:618`, flags `TIMER_CALL_SYS_CRITICAL\|TIMER_CALL_LEEWAY` = exactly the `0x21` the wrapper logged), so `xnu_live_tmr_enter_seq = 0x34`, `_src = 2`, `_call = 0x8051f320` (`&group->delayed_timers[TCF_ABSOLUTE]`), `_now = 0x06de07af` = the driver's `_arm` **+249 ticks (13.0 µs)** and `_deadline = 0x06fb530a` = the driver's `_due` **+43 ticks (2.2 µs)** — three readings that agree this is this driver's arming, the 2.2 µs being the separation of the two `clock_interval_to_deadline` calls (`setTimeout(UInt32, UInt32)` is that same function, `IOTimerEventSource.cpp:381-387`, which is what makes `_due` the moment the callback should have run rather than a guess). The fire lands **1.096 ms after the deadline the OS actually armed** (1.098 ms after `_due`), and that residue — decrementer expiry, this image's IRQ handler, `rtclock_intr`, the daemon being made runnable, the gate — is the whole of the dispatch: three latencies agreeing to 60 µs, two intervals to 1 µs, and run 1's independent 1.105 ms and 101.046/101.049 ms. **`_to = 0` is not the arming and the record says which is**: `wakeAtTime` has one early return (`if( !action )`, `:472`) and returns `kIOReturnSuccess` on every path after it *including* the one where nothing was entered, so the reading this step is for is the callback's own keys — `_fires` published as 0 before the arming, `_fire_lat_ns`, and the re-arm. The chain is read out of Apple's files rather than assumed: `IOWorkLoop::init`'s `kernel_thread_start` (`IOWorkLoop.cpp:170`) is why `_wl` non-zero means a thread exists — the run's own instrumentation says so without being asked, `xnu_live_workloop_ret` appearing **twice** in 494's log and **three** in 495's, each preceded by the same `kthread_cont = 0x8018b208` pair the OS's own two work loops use; `timerEventSource(OSObject*, Action)` (`:308-312`) forwards to the `PriorityKernelHigh` form whose `setTimeoutFunc` (`:205-270`) picks `&IOTimerEventSource::timeoutAndRelease` and allocates it with `thread_call_allocate_with_options`; `timeoutAndRelease` (`:146-178`) closes the gate, invokes the action and opens it; `wakeAtTime` (`:470-499`) enters the thread call at `:492`; the daemon is started by `thread_call_initialize()` (`startup.c:445-446`) and the timer queue is expired by `rtclock_intr`, which this image's own handler calls (`entry_irq.c:204`). No key in the log names that thread — the `tmr_enter_*` records are the old `timer_call_*` family and the `tmr_qexp_*` records are the scheduler's quantum timer — so that link is a reading of Apple's file and what the run adds is that it happened. **Run 1 is kept because it found the step's real defect, in the driver's own record**: `_fire_lat_ns` was computed against a timestamp written once before the first arming and never moved by the re-arm, so it was a **sum** that grows one interval per fire — 101.105 / 202.151 / 303.199 ms, three numbers under a name that says "how long the OS took to call this driver back" (`_gap_ns` likewise), and the fix is one line, the callback taking a fresh `mach_absolute_time()` immediately before each re-arm exactly as `start` reads its own arming instant, after which the column is one measurement repeated three times. `tools/check_driver_catalogue.py` 13 claims/46 mutations → **14/57**, all refused, run inside `build_entry.sh` in both modes: claim 14 requires the source created on the work loop `workLoop()` was assigned to, `addEventSource` called on *that* variable, the function handed to `timerEventSource` defined **in this file** (read out of the call, not looked up by name), `enable()` before the arming `setTimeout`, one *name* for the interval across `setTimeout`/`clock_interval_to_deadline`/the re-arm that the file defines as an integer, the callback publishing `_fires` and `_fire_lat_ns` and re-arming, the `_fires` zero before the work loop is created, and the eleven keys that separate the five silent failure modes — with a driver that arms no timeout *noted* rather than failed (`/interrupt-controller`'s is not about time), and at least one in the image required, or the reading would be absent from every run. The eleven new mutations are one per link; **two of them were first written against the file's prose rather than its code and are the check's own defects** — `live_keys()` cut a driver's keys at the *last* underscore, so the ten new keys read as seven prefixes and the claim failed on its own reader, and `driver_timeout()` searched the whole file for the first `setTimeout`, which is the callback's **re-arm** since the callback is defined above `start`, so the "order" it reported was about two different regions of the file (both now searched from the work loop forward with absolute offsets) — and a third replaced the `clock_interval_to_deadline` call **as quoted in a comment** instead of the call itself, because its needle did not carry the statement's `;`, which passed and whose passing is the reason the check exists: a claim about a comment is not a claim about the code. Build, twice (run 1 is the first, run 2 the fix): `.text` **5264672** in both, image **5487228**, `.bss` 362696 → `0x8053ba80..0x80594328` **362664** (four words smaller because `g_timer_first`, the static the cumulative key needed, is gone), 27 undefined, 59 wraps; payload `.bin`/`.img`/`.qcdt.img` **the same three sizes as 494** (5982532/5986304/8507392) with different hashes (`0c93f6da…`, `3dbbd4c9…`, `045d97b6…`), and run 1's `3413982d…`/`2dd18e5f…`/`3c5b3882…`. The console block is byte-identical to 494's (1288 characters, sha256 `a0593af0…`), the fault counters are 493's and 494's (`sleh_seen = 0x20`, `_armed = 0x1c`, `_redirected = 0x1a`, `_storm = 9`, no `stub_hit`, no panic), and **every one of 494's device keys reads the same value** (`_hwok = 1`, `_maptyper = _probetyper = 0x468`, `_rd0 = 0x00000000`, `_resolve = 1` on both nodes) with the register read through a mapping at yet another VA (`0xc2094000`/`0xc2095000`) — so nothing the boot decides depends on the timeout and nothing here claims the boot advanced; the arming happens *after* 494's closing read, `_rd0` being the record two lines above the five zeroes. Two runs, both through the gate, both exit 0, `No errors detected`, device back on Android on its own, every watchdog key 494's except the live counter (`_enabled = 1`, `_timeout_s = 0x19`, `_bite_after = 0x000dffac`, `_readback_ok = 1`, `_counter_running = 1`) so the net was armed and did not bite, and no persistent write (`fastboot boot` only). Owed: `/timer`'s second definition (nothing on the OS side is compared with the payload's read of the GPT), **an interrupt handler a driver installs and a device write** (`ml_install_interrupt_handler` is already used by `entry_irq.c` for nub 0; `IOCPUInterruptController` with `registerInterruptController`, or `IOService::registerInterrupt`), a timeout that outlives its asker, the other device nodes, the two services the catalogue cannot answer for, `MSM8974RootResource`'s `state0 = 0`, a wider name/class reader, the release as a reading, `vm_fault`, 488's flag-list, 490's frames band and `xnu_live_dec_same` |
| stage90 | [experiment-494](experiment-494-the-device-read-through-the-kernels-own-mapping.md) | **the device read through the kernel's own mapping, and the one value in it that has two definitions** — the step that spends 492's rule: 493 left `_phys0` a *number* read out of the memory descriptor the OS built, and 494 maps **that very object** with `IOMemoryDescriptor::map( kIOMapAnywhere )` and reads a register through the address it returns. Both drivers work: `/interrupt-controller` `_map = 0xc0505dc8`, `_mapvaddr = 0xc203d000`, `_mapvlen = 0x1000`, `_mapctlr = 0x1`, `_maptyper = 0x468`; `/timer` `_map = 0xc0505d80`, `_mapvaddr = 0xc203e000`, `_mapvlen = 0x1000`, `_rd0 = 0x00000000` (run 2; run 1 one page lower at `0xc2009000`/`0xc200a000`), the two kernel VAs exactly one page apart in both runs and neither of them the payload's identity-mapped `0xf9000000`/`0xf9020000`. **The falsifier fires in the affirmative**: `GICD_TYPER` read through the OS's mapping equals the payload's own read of the same read-only register through its own 1 MB section — `_maptyper = _probetyper = 0x468`, `_hwok = 1`, with the payload's live key `xnu_live_gic_dist_typer = 0x468` as a third record of one value; the payload's `g_stage90_gic_dist_typer` is stored **before the probe writes any register at all**, so what is compared is the register as the probe found it. `0x468` decodes as 288 interrupt lines, 4 CPU interfaces and the security extensions present — a register whose fields agree with the part. `/timer` gets no such comparison and the doc says so rather than inventing one: nothing in this project has ever read a word out of the GPT at `0xf9020000`, so `_rd0` is a first reading with one definition (owed). The Apple route is read out of the source rather than assumed and every symbol in it is in this image (`device_pager_setup` 0x800a4634, `device_pager_data_request` 0x800a43f8, `IOMemoryDescriptor::map` 0x801606d4, `createMappingInTask` 0x801605ac, `IOMemoryMap::getVirtualAddress` 0x80160204, `IOGeneralMemoryDescriptor::doMap` 0x8015f0e8, `getKernelReserved` 0x8015a0e0). `tools/check_driver_catalogue.py` 11 claims/36 mutations → **13/46**, both modes green in the build log (lines 921-922): the new claims require the map to be called *on the object the OS's entry was cast into*, every read offset to be a named definition that resolves, both guards to precede the read, and the verdict's two sides to be a mapped read and the payload's own symbol. **And 493's two census ordinals are corrected in four records**: the rows' own `xnu_live_pex_seq` keys read `0x19` (`interrup…`, child `0xc04ea8c0` = its driver's `_prov`) and `0x1a` (`timer`, child `0xc04e9738`), where 493's records had the rows' *positions* in the dump, one higher (defect 289). Build: `.text` 5262656 → **5263456** (+800), image **5487228 unchanged**, `.bss` `0x8053ba80..0x80594308` 362632 unchanged, 27 undefined, 59 wraps, `.bin`/`.img`/`.qcdt.img` **the same three sizes as 493** with different hashes (`a91188fa…`, `fabb004c…`, `2adbe1a2…`) because the growth fits inside the slack before the 16 KB-aligned `.data`. The console block is byte-identical to 493's (1288 characters, sha256 `a0593af0…`) and so are the fault counters (`sleh_seen = 0x20`, `_armed = 0x1c`, `_redirected = 0x1a`, `_storm = 9`) — six device reads through two OS-made mappings added nothing to any counter, so they **did** touch the device and the machine did not notice. Two runs, exit 0, `No errors detected`, device back on Android on its own |
| stage90 | [experiment-493](experiment-493-the-os-initialises-the-machines-devices.md) | **the OS initialises the machine's devices — a second device driver, and the first reading of the kernel's own resolution of a node's `reg`, which is where the defect was.** 492 left one driver and one open question (a single driver cannot tell "the table matches nodes" from "this node happened to work"), so 493 states the same three parts again for `/interrupt-controller`, whose names `MSM8974Timer`'s cannot match, and makes `IODTResolveAddressing`'s answer readable: `doServiceMatch` on the nub runs `kIOReturnSuccess == getResources()` (`IOService.cpp:3724`) before `probeCandidates` (`:3726`), a nub's `getResources` is `IODTPlatformExpert::getNubResources` → `IODTResolveAddressing( nub, "reg", 0 )` (`IOPlatformExpert.cpp:1358-1366`, `:1700-1703`), and that files its array under `gIODeviceMemoryKey` (`IODeviceTreeSupport.cpp:1251`), which `IOService::getDeviceMemory()` returns (`IOService.cpp:6046-6049`). **The declaration is on the root and it is the step's first half**: a `reg` entry's width comes from the **parent's** `#address-cells`/`#size-cells` (`IODeviceTreeSupport.cpp:1227-1233`, `cells = sizeCells + addressCells; num = addressProperty->getLength() / (4 * cells)`), `IODTGetCellCounts` (`:1034-1041`) answers Apple's `1`/`2` for a node that declares neither, and a tree of `{address, size}` pairs that declares nothing therefore resolves `num = length / 12` — **two entries for the timer's three pairs and one for the GIC's two**, an array of the wrong count on every nub, invisible because `getNubResources` returns `kIOReturnSuccess` on the path that resolved nothing. The root now declares 1/1 (one declaration reaches every nub, since every nub's parent is the root), and the arm that computes the address is the identity for this tree (`*phys = CellsValue( childAddressCells, cell ); *phys += offset;` with `offset` still 0, `:1090-1094`, `:1065`) — no `ranges` needed anywhere between `/` and `/timer`. **Run 2 says both halves**: `_devcount = 3` and `2` (exactly the pairs), `_phys0`/`_len0` `0xf9020000`/`0x1000` and `0xf9000000`/`0x1000` = each node's own `reg[0]`/`reg[1]`, `_match = 1` on both (the node's `name`), `_resolve = 1` on both — the OS's answer and the tree's agree, on two different nodes. **Run 1 is kept because it found the step's real defect, in the driver's own reader**: `_devcount` right (3 and 2, the pair counts, so the OS *had* resolved both nodes) beside `_phys0` 0, `_len0` 0 and `_resolve` 2, because the entry was cast to `IODeviceMemory` — the type the array is documented to hold — and `IODeviceMemory::withRange` is a **blind cast** of `IOMemoryDescriptor::withAddressRange` (`IODeviceMemory.cpp:34-39`), which builds an `IOGeneralMemoryDescriptor` (`IOMemoryDescriptor.cpp:1162-1181`), a **sibling** of `IODeviceMemory` (`IOMemoryDescriptor.h:986`, `IODeviceMemory.h:45`, both `: public IOMemoryDescriptor`), so `OSDynamicCast( IODeviceMemory, … )` answers 0 for every entry the OS files and the record said the OS had resolved nothing. The entry is read as an `IOMemoryDescriptor` now and `_objkind` (1 = a real `IODeviceMemory`, 2 = an `IOMemoryDescriptor` and not one, 0 = neither) and `_objlen` (its own `getLength()`, which does not go through the segment walk) publish which class it was — run 2 reads `_objkind = 2` on both; 492's `_freqkind` a second time, in the same file, one step later: **the reader's type is a reading too**. `_reg1` joined the record for the same reason (the comparison's right-hand sides are `_reg0`/`_reg1`, its left `_phys0`/`_len0`, and run 1's `_resolve = 2` was a number whose cause was not in the log). **A second driver is what makes the bucket a layer**: over the same 30 `findDrivers` calls as 492, **26 are now answered with two candidates** where 27 were answered with one — `probeCandidates` walks the whole bucket for every nub and the candidate test decides (`_some`/`_none`/`_max` 28/2/2), and the falsification the step was built to see (a GIC driver on `/timer`, or `_match = 3`) did not happen. The census records `pex_seen = _shown = _named = 0x1d` (29 of 29), `_registered = _matchpass = 0x1c`, `_inactive = 0`, and **both device rows have `kids_of = 1`** — `seq 0x19` (`interrup…`) and `seq 0x1a` (`timer`) (**corrected at 494, defect 289**: this row first printed the rows' *positions* in the dump, which are one higher than the keys, because the census writes an unnumbered header row first), each row's child pointer being the `_prov` its driver published (`0xc0485708`, `0xc0485760` in run 2), so census and driver are two readings of one attachment; `MSM8974R…` still reads `state0 = 0`. **Neither driver touches its device**: `_phys0` is a number read out of a memory descriptor, and `/interrupt-controller`'s `0xf9000000` is the GIC 482/483 measured from the payload side — the first register read belongs to the step that proves the kernel's mapping first. `tools/check_driver_catalogue.py` grew from 8 claims/14 mutations to **11 claims/36 mutations**, all refused; the seven old ones now iterate one record per device personality (so a third is checked by being added), and the four new ones are the two readings of one address: the root's declaration against the OS's own arithmetic over each node's array (with Apple's defaults read out of Apple's file and refused if they move, and the declared width refused if it equals the defaults, i.e. a no-op), the driver's record against `getDeviceMemory`/`getDeviceMemoryCount`/`getPhysicalSegment` with the count derived from `regwords` **at the divisor the declaration implies**, Apple's file against what the headers derive (`getNubResources` cannot fail — which is why an empty resolution had to be read by a driver at all), and the class the array holds against the class the driver reads it as, as a *derivation* from `withRange`'s cast, its factory's `new`, and the two headers' `: public` chains. The mutation that matters is **`the_driver_reads_the_entry_as_an_io_device_memory`**, which re-introduces the defect run 1 measured; four others are defects the check had in itself while being written (a `method_body` that anchored on a line start no definition satisfies, then one that followed a **call** into the callee's brace because `.*?\n{` crosses a `);`; `c_int_array` raising `ValueError` on `0xf9000000u`; `resolve_condition` matching from the file's first `else if(`, so its "condition" was most of the driver; and an identifier scan that counted the `u` of `2u` as a number the comparison names). Build: entry `xnu_arm_entry.bin` 5487228 bytes (`.text` 5262656, `.bss` 362632, +16388 on 492), 27 undefined, 59 wraps, 266 assym defines, 17 ok/0 failed; payload `.bin` 5982532, `.qcdt.img` 8507392 sha256 `c2e9e021…`. **The OS's own console block is byte-identical to 492's except one address** (`Added memory device md0/rmd0` `…80505000` → `…80509000`, the payload's layout following the image's `+0x4000`), and the frontier stands at `load_init_program: attempting to load /sbin/launchd`. Two runs, each through the gate, both exit 0, `No errors detected`, device back on Android on its own, `sleh_seen = 0x20`/`_redirected = 0x1a` as in 492, no persistent write (`fastboot boot` only). Owed: the two nodes' registers (the kernel's mapping is proved first), the other device nodes, the two services the catalogue cannot answer for (their class), `MSM8974RootResource`'s `state0 = 0`, `IOPMrootDomain`'s start through `startCandidate`'s vtable slot, a name/class reader wider than eight characters, and the release / `vm_fault` / 488's flag-list / 490's frames band / `xnu_live_dec_same` |
| stage90 | [experiment-492](experiment-492-the-node-has-a-driver.md) | **the node has a driver, and the catalogue was why it had none — 491's empty driver tree was one line of `IOCatalogue`, and the run now shows a service-plane child under a device node for the first time.** 491 measured the symptom (26 nubs under the platform expert, every one registered and matched, not one child under any of them) and this step read the cause rather than the matching code: `IOService.cpp:3688 matches = gIOCatalogue->findDrivers( this, &catalogGeneration );` resolves through `IOCatalogue.cpp:199-231`, which looks the personalities up **by the service's own class chain** (`meta = service->getMetaClass(); … meta = meta->getSuperClass();`) because `addPersonality` (`:124-132`) files each one under its `IOProviderClass` value — and every personality in this kernel was filed under `IOPlatformExpertDevice` or `IOResources` while **every nub of this machine's tree is `IOPlatformDevice`** (`IODTPlatformExpert::createNub` is `nub = new IOPlatformDevice`, `IOPlatformExpert.cpp:1283`), whose chain is `IOPlatformDevice` → `IOService`, so `findDrivers` answered each nub with an empty set and `probeCandidates` was never called for one. The fix is a personality and a driver: `{ 'IOClass' = MSM8974Timer; 'IOProviderClass' = IOPlatformDevice; 'IONameMatch' = (timer, "qcom,msm-timer"); 'IOProbeScore' = 1616:32; }` in `stage90_platform_config_tables.c` and `MSM8974Timer.cpp` (the third `PLATFORM_SOURCES` entry, linked between the resource root and the platform expert). **The run says the whole of it**: `xnu_live_timerdrv_seq = 1`, `_prov = 0xc05d19c8` (the nub that is census row 26, and the same pointer `findDrivers` was asked about as call 28), `_match = 1` (**the node's `name`**, so the personality's two names are separately readable — `kIONameMatchedKey`, `IOService.cpp:5443-5448`), `_have = 0x3`, `_freqkind = 0x2`, `_freq = 0x0124f800`, `_reg0 = 0xf9020000`, `_cntfrq = 0x0124f800`, `_agree = 0x1` — i.e. the driver read its **provider's** description of the device and compared it with the machine's own `CNTFRQ` (`mrc p15, 0, r, c14, c0, 0`) live from the CPU, which is the first time the tree's 19,200,000 and the hardware's are compared on hardware rather than in two different programs. **Run 1 is kept because it found the defect in the driver's own reader**: it came back `_have = 2`, `_freq = 0` in a node that has a `frequency`, because the reader was an `OSDynamicCast( OSNumber, … )` and every property of a device-tree node becomes an **`OSData`** when the node is turned into a registry entry — `data = OSData::withBytes( prop, propSize )` (`IODeviceTreeSupport.cpp:379`), for the node's `name` as much as for its `frequency`; `reg` was read byte-wise and worked, so the failure was never in the tree. The reader now accepts both kinds and publishes which it found (`_freqkind`), and the new check refuses a build that reads a node property without the `OSData` cast. **And the catalogue is now read from the other side of every call**: the `findDrivers` wrapper was opened to *all* services with the generation and an `is this `gIOResources`` bit — 30 calls in run 2, **27 answered with exactly one candidate, 1 (the root nub, `IOPlatformExpertDevice`'s bucket) with two, 2 with nothing** (services whose chain holds no personality; the record publishes their *pointer* and not their class, which is owed), and the 25 nub lookups all answered with one. **491's third census walks its level whole for the first time**: `STAGE90_PEX_DEEP` 24 → 40 (inner room 40 − 8 = 32 against 29 rows) gives `pex_seen = pex_shown = pex_named = 0x1d`, `_registered = _matchpass = 0x1c`, `_inactive = 0`, `_kids = _deep_l1 = 2`, and **row 26 — the `timer` nub — has `kids_of = 1`**, which is this driver; `MSM8974R…` still reads `state0 = 0` (owed since 491), and the boot's own console frontier did not move (`load_init_program: attempting to load /sbin/launchd`). **A pre-build prediction of this step was wrong and the run corrected it**: a driver hung off a *nub* was expected to abort through `IOPlatformDevice::getResources` → `IODTPlatformExpert::getNubResources`, called an unresolved symbol — it is defined, at `0x80175fec`, with `getResources` at `0x80176998`, and the driver started on a nub with nothing arranged. `tools/check_driver_catalogue.py` (new, in `build_entry.sh`) holds the table to the three files that must agree for a driver to start — the bucket against `createNub`'s class read out of Apple's file, each `IOClass` against the `OSDefineMetaClassAndStructors` of the `PLATFORM_SOURCES` the build script names (read, not repeated; and every class compiled must be *named*), the entry's names against the `/timer` node's `name`/`compatible` **and** the two strings the driver compares `IONameMatched` with, the properties it reads against the node's and against the kind the plane produces, the root entry's name against the tree root's `compatible`, the resource entry's against the resource `IOKitBSDInit.cpp` publishes, and the absence of a `CFBundleIdentifier` on every entry (`probeCandidates` stalls on `isModuleLoaded`, `:3253`) — 8 claims, **14 mutations refused**, and two of the mutations are defects the check had in itself (it first parsed the stock table **quoted inside the file's own doc comment**, and first compared `\"qcom,…\"` with its backslashes intact). `check_driver_plane_census.py` needed its two cap mutations re-derived because they spelled the ceiling as the literal `24u` **in their needles** — a needle that is a value stops mutating when the value moves, and `_bump` turns "refused" into a crash; 11 live and 11 report keys added, 58 → 58 mutations with the two now read from the files. **And one defect in a build tool, found by using it**: `tools/build_xnu_arm_kernel.sh --platform-only` *wrote* the pool's `config.stamp` for a pool it does not build, so a run without `XNU_KERNEL_CONFIG` stamped `config RELEASE` over a `STAGE90_XNU` pool whose 708 objects were untouched; `build_entry.sh`'s `verify_root_device` refused the next link (the guard working, the writer wrong), and the mode now reads the stamp, refuses when the pool is not the configuration it would compile the platform blocks with, and says which to set — both directions measured (exit 3 without the environment, `platform-only: the pool in …/out/xnu_kernel_obj is the 'STAGE90_XNU' kernel, and the stamp is left alone` with it). **And the same trap in the documented build**: step 2's two scripts must *both* carry `XNU_KERNEL_CONFIG` (`assemble_arm_layer.sh` defaults `CONFIG=${XNU_KERNEL_CONFIG:-RELEASE}`), and assembling without it gives a `cswitch.o` built at `TH_CTH_SELF 1480` that `check_assym_cswitch.py` compares with `STAGE90_XNU`'s 1496 — the check caught it, twice. Build: entry image `xnu_arm_entry.bin` 5470840 bytes, 26 undefined symbols, 59 wraps, 266 assym defines, `assemble_arm_layer.sh` 17 ok/0 failed; payload `.img` 8491008, `.bin` 5966104. Two runs, each through the gate, both exit 0 with the device back on Android on its own, `No errors detected`, no persistent write (`fastboot boot` only). Owed: which two services the catalogue cannot answer for (their class), the node's registers (`_reg0` recorded, not dereferenced — the mapping is proved first), the other device nodes, `MSM8974RootResource`'s `state0 = 0`, `IOPMrootDomain`'s start through `startCandidate`'s vtable slot, a name/class reader wider than eight characters, and `xnu_live_dec_same` |
| stage90 | [experiment-491](experiment-491-the-bit-registration-sets.md) | **the bit registration sets, and the level the nubs are at — the number two censuses had been reading as “matched” is set by *registering*, and the third walk finds 26 services where this project had only ever looked one level higher.** 485 read `state0 & 0x4` over the tree's children and 487 over the service plane, both naming it from `IOService.h:74` (`kIOServiceMatchedState = 0x4`); 491 read the *setter*: `IOService.cpp:3754 notifiers[0] = copyNotifiers(gIOMatchedNotification, kIOServiceMatchedState, 0xffffffff);`, inside the block that begins `:3748 if( (0 == (__state[0] & kIOServiceInactiveState)) && (0 == (__state[1] & kIOServiceModuleStallState)) ) {` — and that block sits **after** the `while( keepGuessing )` loop whose only consumer of `findDrivers`'s answer is `if (keepGuessing && matches->getCount() && (kIOReturnSuccess == getResources()))` (`:3730`) with `matches->release()` as its else-arm (`:3736`), while `copyNotifiers` writes the bits through `__state[0] = (__state[0] | orNewState) & andNewState;` (`:4782`, guarded at `:4760`). Nothing on that path tests a match result: the condition is *not Inactive and not ModuleStalled*, so the bit means **its match pass ran** — and the run falsifies the old name out of its own table, because `chosen`, `defaults`, `memory`, `cpus` and `interrupt-controller` (the record's eight characters read `interrup…`) are nodes no driver can match and every one of them carries `0x1e` (`Registered|Matched|FirstPublish|FirstMatch`), with `xnu_live_dtk_matchpass = _registered = 0x15` of 21 and `_inactive = 0`. The key is renamed for that reason (`_matched` is gone from the report and the live channel alike, `_matchpass` with `_inactive` in its place), which is 490's `_recovered` recurring one layer up: a name asserting an outcome the kernel never tests. **The chain to the write is four links and two call sites in the whole file** — `registerService` (`:751`) → `startMatching( options )` (`:803`) → `doServiceMatch( options )` (`:862`, synchronous) or `_IOServiceJob::startJob` (`:838`) → `pingConfig( job )` (`:956`) → `_IOConfigThread::main` → `nub->doServiceMatch( job->options )` (`:4103`) — and my first two drafts of the claim named the wrong callers (`registerService` first, then `pingConfig`) and were refused by the check both times, which is why the chain is measured four times rather than asserted once. **The third census walks the level 487 never reached**: `entry_probe_service_plane` found two children because `IODTPlatformExpert::processTopLevel` calls `createNubs( this, … )` (`IOPlatformExpert.cpp:1360`, `:1363`) with `this` being the platform expert *instance*, and `createNubs` does `nub->attach( parent )` (`:1310-1311`) on each `new IOPlatformDevice` (`:1283`) — so the nubs hang one level **below** the root, and the new walk descends by taking each *row's* child set rather than the root's again. The run fills it: **`MSM8974P` with 26 service children** (four `cpu@N` nubs, twenty-one top-level nubs and `IODTNVRAM` — `processTopLevel`'s two `createNubs` calls plus its own `dtNVRAM->attach( this )`), **`IOPMroot…`** (`IOPMrootDomain`), fifteen rows of the class the source names `IOPlatformDevice` (the record reads `IOPlatfo…`, which is that class's *and* `IOPlatformExpert`'s first eight characters, so the class is the source's reading and not the record's - `entry_str8` publishes exactly eight, `entry_trace.c:1876`) and `IOResour…` (`IOResources`) with one, and **`MSM8974R` at `state0 = 0`** — attached, never registered, so it never reached `doServiceMatch` and this step has no reading of what creates it. `IOPMrootDomain`'s start is a *started driver* by construction and not by inference from the console: the only two routes to being attached in `gIOServicePlane` are `createNubs`'s `nub->attach( parent )` and `startCandidate`'s `ok = service->attach( this )` (`IOService.cpp:3483`), and `startCandidate` calls `service->detach( this )` when `service->start(this)` returns false (`:3505-3506`), so an attachment that survives is a start that succeeded. **487's prediction is falsified by the same run**: it expected `IOService` on the ground that no `IODTPlatformExpert` subclass existed for `configure` to run in, and `MSM8974P` — the fixture's own subclass — is the instance, `configure` ran, and the nub pass replaced the tree's raw entries with fifteen `IOPlatformDevice`s (the source's name for the class; the record's eight characters cannot separate it from `IOPlatformExpert`). So the goal's second half reads *partly*: the OS is entered, IOKit's nub pass ran over the whole tree, one Apple driver class started, and nothing describing a device of this machine has a driver of its own. **The cap is visible as a reading**: `ENTRY_PEX_DEEP` is 24 and the inner loop takes 16 of the root's children, so `seen = 0x13 = 19 = 2 + 16 + 1` against `MSM8974P`'s own count of 26 — a table that stopped at the cap and a small subtree must not print the same, which is why the `kids_of`/`seen`/`shown` triple exists; and every name and class printed here is at most eight characters wide, so the step's own first draft of that table had to be corrected for printing expansions as readings (defect 266). `tools/check_boot_completion.py` gained claim 6 and `tools/check_driver_plane_census.py` claims 8 and 9: the setter's whole write is one statement, the matched write is the **only** `copyNotifiers(gIOMatchedNotification` in that body, sits **outside** the balanced `while( keepGuessing )` loop and has no `getCount`/`findDrivers`/`matches->` between the loop and itself; the guard names both state bits; the four links are present; `doServiceMatch` has exactly **two** call sites in the file; no writer in `entry_stubs.c` still spells `_matched`; the third census reads the same six accessors and the same `__state[1]` offset, descends by a *row's* set, calls `entry_note_pexdeep(l1)` in the variable form and `entry_note_pexchild(` **twice**, keeps its three “no reading” reasons apart, and is called between the service census and the call that never returns — both selftests green at **62 mutations refused** (boot completion, from 55) and **58** (driver plane, from 45). Four of the new mutations found real holes in my first drafts rather than confirming them: the `_matched` rename had to be stated over the *file* rather than a function body; the `entry_note_pexdeep(` claim was satisfied by the early return's own `0u` call; the `entry_note_pexchild(` claim was satisfied because `_bump` replaces only the first of the two; and the wrapper-reorder mutation raised `ValueError` instead of mutating once the line it needed was removed. Two instrument defects of the step's own are recorded besides the misnomer: **twelve pex mutations were silently absent from `MUTATIONS`** because a `str.replace` for the tuple's tail did not match, so the selftest read 46 refused while twelve claims were never tested (a mutation that does not run is not a mutation), and a first draft of the reorder mutation used 4-space-indented needles against a 2-space host. **The image moved by exactly a page, and the arithmetic says so**: `copyin` `80014944 → 80015084` (**+0x740**) while `copypv`, `load_init_program_at_path` and `telemetry_take_sample` all move **+0x1000**, and `0x740 + 0x8C0 = 0x1000` — the symbol at that boundary is `locore_ExceptionLowVectorsBase` at the page-aligned `0x80016000`, so this step's 6368 added `.text` bytes moved everything before the vector block by `0x740` and the vector page's own alignment absorbed the rest. `.text` 5254728 → 5261096 (+6368), `.bss` 361032 → 361928 (+896), headroom 1508152 → 1507256, `xnu_arm_entry.bin` 5470836 sha256 `3053c775…`, `stage90-qcdt.img` 8491008 `ee0056cb…`, `kernel_size = 5966100`, `dt_size = 2521088`, 26 undefined, entry base `0x80000000`, entry point `0x80000074`. **Nothing about the OS's own behaviour moved**: the 1307-byte console block has the same sha256 as 490's (`7ecfdceb…`), the same eight aborts at the same sites on the same two threads with `sleh_seen = 0x20`, `_armed = 0x1c`, `_redirected = 0x1a`, `_storm = 9`, `getpid_count` doubling to `0x800000` with `last = 1` and `error = 0`, `irq_timer_count = tmr_qexp_over = 0x800`, the five tail calls present with `idx = 4` = `vm_pageout`, and zero `undef` records — and the tallies are on the live channel for the first time, because the run that computes them never reaches the report (490 measured that: its log ends with no report at all). One run, through the gate, exit 0, device back on Android on its own, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `entry_checks` 5 / `failures` 0, 533032 bytes. Owed: a name/class reader wider than eight characters, since `IOPlatfo…` cannot separate `IOPlatformDevice` from `IOPlatformExpert` - the distinction the step's central sentence rests on - and three `stage90-…` rows lose their suffixes to the same cap; the third census's own `OSArray::getCount` on the root's child set (`26` vs `shown = 19` is arithmetic, not a second reading); `IOPMrootDomain`'s start recorded rather than inferred, which needs the instrument to read `startCandidate`'s vtable slot (`APPLE_KEXT_COMPATIBILITY_VIRTUAL` expands to `virtual` on 32-bit, `OSMetaClass.h:93-99`, slot 153 in `IOKitDiagnosticsClient`'s vtable, so `--wrap` cannot catch it — 455's `getProperty` rule recurring); the ten nub rows the cap cut; `MSM8974R`'s creator; and, still open, 490's distinct-`(pc, lr)` frames band, the release as a reading, `vm_fault` as a caller-side record, 488's deferred flag-list derivation, and `xnu_live_dec_same`, absent here as in 488 and 490 |
| stage90 | [experiment-490](experiment-490-the-fault-was-the-design.md) | **the fault was the design — the two `Lcopyin_wordwise_loop` records 489 named as the frontier are Apple's own `EFAULT` route, and the run now says so in three numbers `far` and `pc` cannot produce.** `COPYIO_SET_RECOVER` (`machine_routines_asm.s:542-550`) arms `thread->recover` with `copyio_error` before `copyin`/`copyout` touch user memory, `sleh_abort` consumes the word (`trap.c:290-291`) and — only after `arm_fast_fault` **and** `vm_fault` fail — points `regs->pc` at it (`trap.c:456-461`), so the faulting instruction is *replaced* by the copy's `mov r0, #14` exit. **Armed is not spent**, and the run reports both: of the run's **32** data aborts **28** carried a non-zero word (`xnu_live_sleh_armed = 0x1c`) and **26** of those the handler actually re-pointed (`xnu_live_sleh_redirected = 0x1a`), read from the frame's `pc` after the call — the wrapper reads the word *before* `__real_sleh_abort` because the handler zeroes it, and reads the frame *after* it because only that says the copy returned `EFAULT`. The eight records the run names separate into kinds: **seq 1, 4** are `copyout` to user addresses (`0x1000`, `0x101f28`) called from `load_init_program_at_path` and `exec_copyout_strings`, armed and **not spent** — the pages were paged in and the copies retried, which is the same fact as 489's console silence seen from the other side; **seq 2, 3** are `memset`/`bcopy` to kernel addresses from `exec_save_path` and `copypv` with `recover = 0`, because libkern's copy routines arm nothing, and the distinction is the *call site* and not the loop; **seq 5, 6** are the fixture's own `ldr`/`str` at `0x1118`/`0x1124` (`entry+0x38`/`+0x44`) on the page `mmap` returned, in user mode, serviced; and **seq 7, 8** are the `copyin` of `task->all_image_info_addr = 0` from `telemetry_take_sample+0x158` (`ldm r0!` of 176 bytes from address 0), armed **and spent** — the site 489 called the frontier, whose `copyin` returns `EFAULT` and whose sampler tolerates it. `seen` reaches `0x20` while the frames band stops at 16 and the recovery band at 8, and the counters leave the unnamed tail no freedom: 28 − 4 and 26 − 2 are the same 24, so **every abort the run does not name is a copy-path fault converted into `EFAULT`** — the first eight of them the same `ldm` at `lr = 800a8d34` with `far = 0` and the same `sp`. **The third outcome is why two records have no `_back`**: `sleh_abort`'s user half reaches `goto exception_return;` with no `goto exit;` in between and that label ends in `thread_exception_return()` (`/* NOTREACHED */`), so a serviced user fault returns to user mode from inside the handler and the run's two `user = 1` records are exactly the two with no `_back` (8 entries, 6 returns). **The control is five builds**: 486's run D, 487's run, both 488 runs and this one take the **same eight aborts at the same sites on the same two threads** — only the entry image's own faulting `pc`s move (`+0x560` 486→488, `+0x40` between the two 490 runs), every kernel `lr` is identical (`8028bccc`, `8028d680`, `800424a0`, `8028d03c`, `800a8d34`; 487's build at −0x40), the fixture's `0x1118`/`0x1124` never move, and the 1307-byte OS console block has the same sha256 in all five (`7ecfdceb…`) — and 487's run stopping after four records is the build whose `+` expansion dropped `CONFIG_TELEMETRY`. Nothing here is a stop: `getpid_count` doubles to `0x800000` (8,388,608 calls, `getpid_last = 1`), zero `undef` records, zero `stub_hit=`, `irq_timer_count`/`tmr_qexp_over` both past `0x800`, and the run is cut by the SoC watchdog's own 25 s, not by a fault. `tools/check_fault_recovery.py` (new) states the reading over the *file* in **eight claims** and refuses **31 mutations**: the recovery label is an `EFAULT` exit nothing branches to; both copies arm it with that label's own address decoded out of the linked instructions; nothing else in the whole image stores a computed address there (5 stores, all copy paths, all naming `copyio_error`); the handler's arm is gated on `recover != 0` and ordered after both page-in attempts; `TH_RECOVER = 664` is materialised by `osfmk_arm_trap.o`'s own compiled `sleh_abort` as a read-then-zero pair through `r1`; the wrapper reads the word before the call that spends it; the wrapper reads the frame after it and the two counts are published as `_armed` and `_redirected` from the two tests the names claim; and the user half leaves through `thread_exception_return()`. Six defects of the step's own instruments are recorded, all classes this project keeps meeting: `_recovered` **was a name asserting the outcome it did not measure** (renamed `_armed`, with `_redirected` beside it); `body_of` read to the next `objdump` heading and so called a *local label* the body of `copyin` (486's `getMetaClass` lesson recurring); seven selftest mutations spelled their disassembly lines out and stopped mutating when the relink moved `copyin`, and were reported ACCEPTED (228's class); a needle that matched the same two lines earlier in `sleh_undef`; a mutation whose host was a local symbol, so it raised `TypeError` instead of mutating; and a first pass over the live channel that attributed each record's frame to the *next* record, because `_seen` is written before `_pc`. Step 3 green with `--selftest: all 31 mutations were refused`; **adding the last two claims and rebuilding reproduces the image and the payload byte-for-byte** (`xnu_arm_entry.bin` `1bec72f6…`, `stage90-qcdt.img` `1230f688…`), so the second run's log is the shipped build's; 5470836-byte `.bin`, `.text` 5254728, `.bss` 361032, headroom 1508152, 26 undefined, `kernel_size = 5966100`; two runs, exit 0, device back on its own, 25 × `persistent_write_attempted=0x00000000`, 87 × `failure_mask=0x00000000`, `entry_checks` 5 / `failures` 0 |
| stage90 | [experiment-489](experiment-489-the-silence-was-the-success.md) | **the silence was the success — the boot reached the OS, and the log that was read as a stop for two steps is the proof.** The console's last line (`load_init_program: attempting to load /sbin/launchd` and then nothing) was read as "`execve` never returned, process 1 never started", and 487's next step was aimed at the exec; `bsd/kern/kern_exec.c:5119-5173` says the opposite — three `attempting to load` printfs and two `failed loading` printfs, **no success arm that prints**, and one `panic` after the loop that the loop cannot fall out of — so silence after the last name in `init_programs[]` is the success reading. `DEBUG` off and `DEVELOPMENT` on makes `init_programs[]` two entries, so the loop's one `attempting`/`failed` pair per name and the absence of any `launchdsuffix` line is what pins the log's three lines to the loop rather than to the block above it. Five readings in 488's own run say the same thing and are quoted rather than re-measured, because **this step changes no image byte**: `lmf_ret=0` with no `osr_*` record (line 6278), `tail_seq` 1…5 / `tail_idx` 0…4 with the fifth written inside `__wrap_vm_pageout` (5582-5720), `getpid` = 1 with `error=0` and `caller=0x80280248` (6300-6302), `mmap` = `0x00102000` with all eight argument words (6306-6317), and the two user-mode aborts — `pc=0x00001118` fsr `0x007` then `pc=0x00001124` fsr `0x80f`, both on `far=0x00102000`, both `sleh_user=1`, both on thread `0xc0464690`, which *is* the thread that called `mmap` — **exactly the two fault sites the RAM disk's own 27 instructions predict** (`ldr r3, [r0]` at entry+0x38, `str r0, [r0]` at entry+0x44, entry `0x1000 + 28 + 0xC4 = 0x10E0`), so the prediction is confirmed to the address by the file the run executed. `tools/check_os_entry.py` (new) states the reading over the *file* in five claims and refuses **30 mutations**: the chain `bsd_ast` → `bsdinit_task` → `load_init_program` → `execve` with `/sbin/launchd` last in the table; the silence rule as three separable properties — the body's printing exhausted at 3 and 2 out of five `printf(` calls, **each call site** followed by an `if (!error)` whose failure arm prints or panics, no `if (!error)` arm printing (taken by shape, because the file writes both spellings), exactly one `panic` and nothing printing after the loop, and the `launchdsuffix` guard intact; `bsd_init()` before all five tail calls with both censuses called between the tail record and `__real_vm_pageout()`, **which is the wrapper's last statement** (`vm_pageout` is followed by Apple's NOTREACHED, so a record after it would say the wrapper returned); the two fault sites read out of the linked image's bytes; and the keys the readings come from being published by this build. Three defects of the check itself are recorded, all classes this project keeps meeting: the expected printf and `panic(` counts were wrong (3 and 2, not 2 and 1 — the `launchdsuffix` block has one of each), `extern void *entry_probe_dt_children(void);` was **counted as a call site** because a declaration has the shape of a call, and **three mutations were accepted because they edited a *derived* body** the selftest re-derives from source — 488's class exactly — so every mutation now edits source text with the re-derivation written once, and the third, `a_failure_line_moves_to_the_other_arm` (moving one `failed loading` line to another attempt, counts unchanged) is what the per-call-site rule exists for. Step 3 green with `xnu_entry_489` and `--selftest: all 30 mutations were refused`; image bytes unchanged from 488 (`xnu_arm_entry.bin` `759c49f1b3096991fbcb9bba1cf3485354fab5fc8e3b99204790b677dc1df664`, `.elf` `959b0a6932a75f1675decb1614fc4f2ed5e6d7b2a221a1a564d87850177e785d`) — **no device run, because there is nothing new to run**; and the frontier this step opens is not the exec but the telemetry path: after the two user aborts the next two published faults are both `Lcopyin_wordwise_loop` with `far=0`, `lr=telemetry_take_sample+0x158` (`ldr r0, [r0]` then `bl copyin`, 176 bytes to a stack buffer), on the fixture's own thread — while that thread's `getpid_count` keeps doubling past `0x8000`, so process 1 is alive and `copyin` faults instead of returning `EFAULT`; `sleh_seen` reaches `0x20` / `sleh_storm=9`, and 486's run D has the **same eight published records in the same order on the same two threads** — same four kernel copies, same two user aborts with the same `far`, same recurring fault with the same `lr=0x800a8d34` and `far=0` — with only the four copy-loop pcs differing because the rebuild moved them (0x560-0x570 higher, every `lr` the same), so it predates this step's fix rather than following from it; 487's run stopped at 4 records. **Corrected by 490:** "`copyin` faults instead of returning `EFAULT`" is the inverse of the source and the run - `copyin` arms `thread->recover` with `copyio_error` (`machine_routines_asm.s:542-550`) and `sleh_abort` re-points `pc` at it when the page cannot be paged in (`trap.c:456-461`), so those records are calls the kernel answered: 490's run reports 28 of its 32 aborts armed and 26 redirected, and `getpid_count` ends at `0x800000`, not `0x8000` |
| stage90 | [experiment-488](experiment-488-the-assembly-was-built-for-a-different-kernel.md) | **the assembly was built for a different kernel - and process 1 runs again.** 487's run stopped at the first return to user mode (`xnu_live_stub_hit_name_ptr = 0x804fe3c4` = the string `timer_state_event_kernel_to_user`, `xnu_live_stub_hit_caller = 0x80017f94` = `return_to_user_now + 4`), and the cause was not in the entry image but in **how a configuration is expanded**: `tools/xnu_config/select_master.sh` gives a configuration the tree does not declare the `+` attribute, and `+` is what every *untagged* `options` line matches - measured this session by declaring an empty attribute list, an undeclared name expands to **exactly 10** flags (`CONFIG_MACH_APPROXIMATE_TIME`, `CONFIG_MAX_THREADS`, `CONFIG_MSG_BSIZE_REL`, `ICMP_BANDLIM`, `MACH_FASTLINK`, `MULTICAST`, `NO_DIRECT_RPC`, `OLD_SEMWAIT_SIGNAL`, `SERIAL_CONSOLE`, `VIDEO_CONSOLE`), **none of them `CONFIG_SKIP_PRECISE_USER_KERNEL_TIME` and none `CONFIG_TELEMETRY`** - so `locore.s` kept `return_to_user_now`'s `bl EXT(timer_state_event_kernel_to_user)` and dropped all five of its telemetry branches. `XNU_MASTER_LOCAL` was the caller's business: `build_xnu_arm_kernel.sh` passes it, so the C objects always got the 110-flag expansion, while `gen_assym.sh`, `assemble_arm_layer.sh` and `xnu_arm_assemble.sh` did not, so both copies of `locore.o` got the 10. Measured, the two copies: **with** the options 37 undefined = 35 + `telemetry_needs_record` + `telemetry_mark_curthread`; **without** them 37 undefined = 35 + `timer_state_event_kernel_to_user` + `timer_state_event_user_to_kernel` - the same count and disjoint in the two names that matter, which is why the image's undefined list is 26 names before and after and has to be read by name. Ten is a *plausible* set (the kernel links, the boot runs, BSD's copyright and `BSD root: md0` print, the driver layer comes up), which is why it survived 487 whole. Closed at four levels - `select_master.sh` finds `tools/xnu_config/<dir>/<CONFIG>.local` by convention, `expand.sh` refuses an undeclared configuration, `arm_asm_defines.sh` and `gen_assym.sh` refuse an empty expansion, and the three consumers plus `build_xnu_arm_kernel.sh` replace the `done < <(...)` loop that hid all of it with a read whose status is seen - and then read at the *result* by new `tools/check_asm_config.py` (**4 claims, 21 mutations refused**, baseline checked first): the expansion is the declaration it is composed from (over option **names**, because `config/MASTER:288,289` declares `CONFIG_MSG_BSIZE` twice and its value is *expected* to move with the tag; RELEASE 108 / `STAGE90_XNU` 110 / `STAGE90_BOOT` 74 / `DEVELOPMENT` 124), the assembly's list is the configuration's minus the names `--exceptions` prints and nothing else (109 of 110, SLIDABLE removed), neither `locore.o` carries either timer function as undefined while both carry `telemetry_needs_record` **and** `telemetry_mark_curthread` (the positive control, so the claim is not an argument from absence), and **487's ask**: the two `struct thread` offsets the assembly addresses fields through are the ones the kernel's own compiled C materialises - `TH_KSTACKPTR` = 1480 inside `osfmk_arm_model_dep.o`'s `DebuggerXCall` (`model_dep.c:826`) and `osfmk_arm_trap.o`'s `sleh_undef` (`trap.c:237`), `TH_CTH_SELF` = 1496 inside `osfmk_arm_machdep_call.o`'s `thread_get_cthread_self` (`machdep_call.c:85`, `ldr r0, [r0, #1496]` - the instruction 468's run died on) and `osfmk_arm_pcb.o`'s `machine_thread_create` (`pcb.c:133`), each a **named function** rather than "the object contains the number", and `TH_CTH_DATA` deliberately uncompared because `pcb.c:133`'s two adjacent zero stores are one 16-byte NEON store at 1496, so the field 8 bytes above it has no immediate of its own. Three of the session's own defects: a mutation **accepted** by the selftest's first version (the control written as "neither of" a pair, 487's own mistake repeated in a check written after it), one that raised `ValueError` instead of mutating, and an immediates reader scoped to the object rather than to the symbol. Two runs through the gate, exit 0, device back on Android on its own (515332 / 515764 bytes): **zero `stub_hit=` lines**, 23 `getpid` records ending `0x00800000`, first user-mode abort PC `0x00001118`, the 967-byte OS console block byte-identical to 486's and 487's (`980921c683e3980e…`), `panic_entered = 0`, `entry_checks` 5 / `entry_failures` 0 / `entry_checksum = 0x9071ec79`, `cls_calls = 0x20`, `svc_kids = 2`, `sleh_storm = 9`, 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`. Layout: `.text` 5254568 -> **5254504**, image **5470836** (unchanged), `.bss` 361032 (unchanged), headroom 1508152, undefined 26 / wraps 59; entry image sha256 `759c49f1b3096991fbcb9bba1cf3485354fab5fc8e3b99204790b677dc1df664`, payload `stage90-qcdt.img` 8491008 sha256 `4b80513fcf2c97d6272e7af46abd7033006af6060e1520f9cd755163dc468e41`, `kernel_size = 5964890`, `dt_size = 2521088` - and the four-step build re-run end to end **after** every script edit produces the **byte-identical** entry image and payload, so the runs are of the shipped build. **489:** the third census of the platform expert instance `0xc0591740`'s own service children (`createNubs` attaches the 21 nubs to `this`); the release as a reading; and, owed since 480, `vm_fault` (`osfmk/arm/trap.c:443` user, `:563` kernel) as the wrapper that attributes the exec path's copies. **Still owed and stated as such:** the flag lists are still two lists - 487 asked for `gen_assym.sh`'s tuple to be *derived* from `build_xnu_arm_kernel.sh`'s, and it is not, because claim 4 already pins the only values whose divergence was measured to matter and the edit's failure mode is a whole-kernel re-baseline with nothing to measure it against |
| stage90 | [experiment-301](experiment-301-machine-o-and-processor-up-runs-real-code.md) | **`machine.o`, `processor_up` runs real code, and the stop is the one name it calls that is not** — `osfmk/kern/machine.c` (manifest:569), `.text` 0x5F0 / `.rodata.str1.1` 0xC7 / `.bss` 0x28, **6 resolved** (`processor_up`, `processor_assign`, `processor_shutdown`, `host_get_boot_info`, `host_reboot`, and the **storage** name `machine_info` `B 0x28`) and **1 added** (`commpage_update_active_cpus`). The object carries **two** downstream stub references and the step's claim is which one the path meets: `commpage_update_active_cpus` (called from `processor_up`) and `PEHaltRestart` (called from `processor_doshutdown`/`host_reboot`, neither on this path) — "stubs left in the object" would say two and predict nothing. **The body had to be read, and reading it is where the trap was**: `processor_up`'s nine calls are eight real plus the stop, and the inlined `enqueue_tail` carries three live `panic` paths (`queue.h:230`, `XNU_KERNEL_PRIVATE` arm) whose `cmp r8,#0 / beq <panic>` looked like "panics on an empty queue" — the state a fresh processor set is in. It is not: these queues are **circular** (`queue_init` sets `next = prev = que`), so the validator's equality tests hold and the `beq` is the `elt_next == 0` case, which cannot happen for an initialised set. **A panic path in a body is not a prediction that it runs**, and the deciding evidence was one line of `queue_init`, not the disassembly. **`stub_hit=commpage_update_active_cpus` at `xnu_entry_stub_caller=0x800e6664`** = `processor_up + 0xac`, the `bl` at 0x800e6660 — and the map puts `processor_up` at 0x800e65b8, so the key identifies the instruction and not just the function. All three build counts exact (755/668/87); `__bss_end` unchanged at 0x80167798 for the **second step running** and by the same mechanism (the object's 0x28 plus fill grows `realstubs.o`'s `.bss` *start* by 0x40, the retired `machine_info` stand-in shrinks its *size* by 0x40 — start + size unmoved), image unchanged at 1247700. **`.text` 0x117120 → 0x1176C0, 0x128 below the estimate, and the whole decomposition is measurable in the map**: `machine.o` `.text` +0x5F0, its `.rodata.str1.1` **+0x3A against 0xC7 in the object** (the map prints `0x3a … (size before relaxing) 0xc7`), `realstubs.o` `.text` −0x60 (**five** stubs retire, not one), its `.rodata.str1.4` −0x3C, alignment +0x12 = +0x5A0 to the byte. **A `.rodata.str*` size in an object is an upper bound, not a contribution** — a third kind of number in this project's list, after the total read as a component (298, 300) and the component read as a total. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, `xnu_entry_failures=0x00000000`, no `exception:` line, device back on its own (release 10, cancro) |

| stage90 | [experiment-300](experiment-300-priority-o-and-the-stop-leaves-thread-create-internal.md) | **`priority.o`, and the stop leaves `thread_create_internal` entirely — thread creation works and the walk reaches the first context switch** — `osfmk/kern/priority.o` linked: **13 resolved** (twelve functions plus the storage name `sched_run_buckets`), **0 added** — all 40 of its references are symbols the image already carries, the 250/270 shape inverted (773/684/89 → **760/672/88**). 299's stop is at `thread_create_internal + 0x3a4`, and every stub near it belongs to that function — so the prediction was not a lookup but a walk through five functions, read off the image call by call: `sched_set_thread_base_priority`'s body takes the else arm and its `blx r1` on `sched_multiq_dispatch + 0x24` is **not taken** (`sched_mode` is TH_MODE_FIXED because the parent is `kernel_task`, so `sched_compute_timeshare_priority` is *retired without being called*); `set_sched_pri` (real) does not early-return, calls the real `thread_run_queue_remove` which returns FALSE **with no calls inside it**, and returns; `thread_create_internal` runs to `return KERN_SUCCESS` with the thread written through the caller's out-pointer; **the first `kernel_thread_create` in this kernel returns a real thread** to `kernel_bootstrap`, whose five remaining statements run and **tail-call `load_context`** (`b load_context` — which is why the caller key is that function's, 252's/264's shape from the other side). **`stub_hit=processor_up` at `xnu_entry_stub_caller=0x8000e73c` = `load_context + 0x20`**, exactly as predicted; the falsifier was written down with it (any other stop means a real function on that path reaches a stub reading did not show). **This is the step where thread creation works, and where the walk leaves the return-to-caller idiom it has followed for a hundred steps** — `load_context` does not return, so from here the frontier is a context switch and the question becomes "what does the first thread run". Build: `__bss_end` **0x80167798 predicted and measured byte for byte** — the slot rule 299 got wrong, applied right (a 4-byte `aligned(64)` stand-in frees 0x40; the object's 0x14 plus fill costs 0x38). `.text` 0x116780 → 0x117120 (predicted 0x174 high: `size`'s text column is a **total**, not a summand — the read-only sections were added on top of a number that already contained them, 298's lesson in a second shape), image **unchanged at 1247700**. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, one `stub_hit=` line, no `exception:` line, device back on its own |

| stage90 | [experiment-299](experiment-299-kpc-thread-create-is-a-load-and-an-early-return.md) | **`kpc_thread_create` is a load and an early return, and the stop moves one call down** — `osfmk/kern/kpc_thread.o` linked (4 resolved, 5 added; undefined/function/storage 772/682/90 → **773/684/89**). The object *defines* `kpc_threads_counting` (`= 0`, in `.bss`), so the function's own first instruction — `movw/movt` it, `ldr`, `cmp #0`, `popeq` — takes the early return and the 298 stop is answered by a function that calls nothing. **The step's real content is a table of what sits *between* the two stubs:** after the `kpc` call there is `SCHED(initial_thread_sched_mode)` through `blx r1` on `*(sched_multiq_dispatch + 0x4c)` — a **real indirect call since 262** — before the next stub, so the prediction was not "the next stub in address order" but "this real call completes", and the run's evidence is that the stop is past it. **`stub_hit=sched_set_thread_base_priority` at `xnu_entry_stub_caller=0x8000b090` = `thread_create_internal + 0x3a4`**, the predicted address on the predicted stub; the guarded stub behind it (`cmp r1, #4 / bgt` on `task->max_priority`) was not reached, re-measuring 296's argument. The build's one miss is a rule: `__bss_end` was predicted −0x20 and did not move, because **`.bss` is a sum of aligned slots, not of sizes** — the retired `kpc_off_cpu_active` stand-in (`aligned(64)`, 4 bytes) frees exactly 0x40 from `realstubs.o` (0x1744 → 0x1704) and the new object costs exactly 0x40 (0x20 of `.bss` plus 0x20 of fill in front of those 64-byte-aligned arrays). `.text` 0x116360 → 0x116780, image **unchanged at 1247700** (the growth fitted the padding before the 16-KB-aligned `.data`). Two silent things recorded: the five names in `kpc_common.c` arrive as stubs nothing reaches, and `kpc_thread_init` has no caller so the object's lock is never initialised — harmless exactly because the function that runs does not take it. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, one `stub_hit=` line, no `exception:` line, device back on its own |

| stage90 | [experiment-298](experiment-298-the-orphan-sections-get-names-and-the-sysctl-set-gets-a-segment.md) | **The orphan sections get names, and `__DATA,__sysctl_set` gets a segment** — the six Mach-O input-section names the objects carry become named output sections: `*("__TEXT,*")` and `.ARM.exidx` into `.text`, `*("__DATA,*__data" "__DATA,*__const")` into `.data`, and `__DATA,__sysctl_set` into an output section of its own placed between `.data` and `.bss`, plus a `verify_sections` check that closes the set. **The quoting is load-bearing and was tested with a two-object link first** — a pattern containing a space or a comma must be quoted, and a quoted pattern still takes wildcards, so one entry covers both of XNU's spellings. Two of the six placements were wrong, and neither was the memset's business: `__TEXT, initcode` is *code* (`memorystatus_init`) and sat inside `__DATA`; and `__DATA,__sysctl_set` is read **by name through the Mach-O header** (`LINKER_SET_BEGIN(__sysctl_set)` → `getsectdatafromheader(_header, "__DATA", "__sysctl_set", &size)`), so it has to be inside the `__DATA` **segment**, where as an orphan it was not. The set is *not* merged into `.data` — its name is its interface, and a merged section has no address a Mach-O entry could describe. **A correction to 297, from the input sections' flags:** ld's placement is consistent, not arbitrary (every `A`-only section with the read-only group, every `WA` one after `.data`) — so the lesson is that the layout contract depended on a decision it had not made, not that ld is unpredictable. Every line held except one: `.text` +0x9C0, `.bss`/`__bss_start` +0xC0, `.sysctl_set` 0x80130930/0xa4, image +164, all exactly; and the `size` column's "text" is not `.text` (it is `.text` plus every read-only section outside it — the 2648-byte orphan group before, `.sysctl_set`'s 164 now), so that one predicted `+0x9C0` and measured `+12`. **`stub_hit=kpc_thread_create` at `xnu_entry_stub_caller=0x8000b034` — unchanged, which is the proof that nothing that executes moved.** 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |

| stage90 | [experiment-297](experiment-297-bss-start-is-the-start-of-bss-and-the-3252-bytes-that-were-erased.md) | **`__bss_start` is the start of `.bss`, and the 3252 bytes every run was erasing** — one line moves, from between the `.data` and `.bss` output sections to the first statement *inside* `.bss`. It was assigned between them, so it named the end of `.data`; it names the start of `.bss` only if the linker places nothing in the gap, and the linker put the two Mach-O orphans there (`"__DATA, __const"`, `"__DATA, __data"` — the **space** in those names is why `*(.data .data.*)` misses them). The payload memsets `[__bss_start, __bss_end)` *after* copying the image in, so **every run this project has ever made zeroed 3252 bytes of initialized data**: 122 `vm_allocation_site`s, all with `refcount = 2`, **129 non-zero bytes** in the file — 117 plain `kalloc` sites, 3 `kalloc_tag`, 2 `kalloc_tag_bt`, and the 5 tagged ones carry the `tag` that `vm_tag_alloc_locked`'s `if (site->tag) return;` reads. This is the project's own answer, taken from the other linker script: `stages/stage90/linker.ld:19` has always put the assignment *inside* `.bss`, so the two scripts had disagreed for as long as both existed and **the entry one was the wrong one**. Everything predicted held, and the **image is byte-identical** — the section table is unchanged address for address, so the only difference in what the device is handed is the one constant the payload compiles in. `__bss_start` 0x8012fc78 → **0x80130940**; `xnu_entry_bss_bytes` 0x37aa0 → **0x36dd8**; the copy ends at 0x80130930, so the two ranges are disjoint by 16 bytes. **The stop does not move** — `stub_hit=kpc_thread_create` at `xnu_entry_stub_caller=0x8000b034` — so the state is right and the boot has not yet reached the place where it mattered. Two checks: `verify_bss` (structural) and **`bss_start - ENTRY_BASE >= bin_size`** (arithmetic over the payload's own numbers — and the check that would have failed the 296 build, 0xCC8 short). 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |

| stage90 | [experiment-296](experiment-296-first-step-inside-thread-creation-and-bss-start-is-not-bss.md) | **The first step inside thread creation, and `__bss_start` is not the start of `.bss`** — `bsd_kern_kern_fork.o` + `osfmk_arm_status.o` for `uthread_alloc` and `machine_thread_state_initialize` (the uthread four really are defined in `bsd/kern/kern_fork.c` in this tree, not in a nonexistent `osfmk/kern/uthread.o`). **11 resolved and 32 added — the largest single-step increase in the stub count this walk has made**, because the frontier has moved into BSD process creation where an object is a subsystem rather than a file of accessors. The step turns on a task flag read one byte wide: `set_astledger` is guarded by `new_thread->task->rusage_cpu_flags & TASK_RUSECPU_FLAGS_PERTHR_LIMIT` (thread.c:1251, flag 0x02), the field is written as a halfword zero by `task_create_internal`, and **the run confirmed it is skipped** — the step's first falsifier, now a measured fact. **`stub_hit=kpc_thread_create` at `xnu_entry_stub_caller=0x8000b034` = `thread_create_internal + 0x348`**, as predicted before the build. The build's `__bss_start` line then uncovered a **latent defect in the instrument**: `entry.ld` only captures `*(.data .data.*)` between the two, so the objects' `__DATA, __const` and `__DATA, __data` — orphan sections, and the *space* in `"__DATA, __data"` is why the pattern misses them — sit **after `__bss_start` and before the real `.bss`**. The payload memsets that range after copying the image, so **every run this project has made has zeroed 3252 bytes of initialized data**, of which the 129 non-zero bytes are exactly the `refcount = 2` that `VM_ALLOC_SITE_STATIC` sets (`osfmk/mach/vm_types.h:176`). Inert so far — `kalloc_canblock` reads `flags`, zero either way — but refcounting is what `refcount` is for. Same family as the pad defect of 288/291: a value with two definitions, one the link's and one the instrument's assumption. Fixed in 297. Counts: 772 undefined / 682 function / 90 storage, text 1139704, image 1247536, `.data` unchanged at 0x80118000. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |

| stage90 | [experiment-295](experiment-295-four-objects-and-the-walk-reaches-thread-creation.md) | **Four objects in `kernel_bootstrap`'s tail, and the walk reaches thread creation** — `osfmk_atm_atm.o`, `osfmk_bank_bank.o`, `osfmk_voucher_ipc_pthread_priority.o` and `osfmk_corpses_corpse.o` for `atm_init`, `bank_init`, `ipc_pthread_priority_init` and `corpses_init`, the four stubs left in `kernel_bootstrap`'s tail at +0x2d4, +0x2f4, +0x304 and +0x308. 5408 + 6288 + 396 + 2652 bytes of `.text`, **29 resolved (26 functions + 3 storage — `corpse_for_fatal_memkill` and `exc_via_corpse_forking` are named like functions and *are* storage, the trap 294 recorded) and 7 added**. **The measurement had to be redone**: a `-dr` dump prints the whole section's relocations before the function's own, so a naive `--disassemble=NAME` parse attributed every call in `ipc_pthread_priority.o` to `ipc_pthread_priority_init` and made it look as though it called the stub `pthread_priority_canonicalize`. Scoped to the function's own range it makes **three** calls, all real — and so do the others (`atm_init` 14, `bank_init` 16, `corpses_init` 3). The frontier therefore moves somewhere new: `kernel_bootstrap` calls `kernel_thread_create` (real), which reaches `thread_create_internal`, whose first stub is **`uthread_alloc` at +0x084** — **the walk is inside thread creation**. `.data` moved a **third** time (0x80114000 → 0x80118000) on the marginal margin the ledger flagged, image → **1247424**, and `__bss_start` is 0x8012fc68 rather than the predicted 0x8012fc08 because **`.bss` itself grew by 4576** — the first time its content moved it rather than its alignment. Counts exact: **751 / 664 / 87**, text 1130904. **`stub_hit=uthread_alloc` at `xnu_entry_stub_caller=0x8000ad74` = `thread_create_internal + 0x088`** — and this is the first prediction whose caller address could be confirmed before the run, because `osfmk_kern_thread.o` links earlier than everything the step adds. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |

| stage90 | [experiment-294](experiment-294-three-objects-in-a-row-and-a-defect-in-the-ledgers-own-arithmetic.md) | **Three objects for three stubs in a row, and a defect in the ledger's own arithmetic** — `osfmk_kern_stack.o` + `osfmk_kern_thread_policy.o` + `osfmk_arm_pcb.o` for `stack_init`, `thread_policy_init` and `machine_thread_init`, which are `thread_init`'s tenth, eleventh and twelfth calls at +0xd4, +0xd8 and +0xdc — **three consecutive unconditional calls, the one shape where a multi-object step costs nothing in prediction quality**. 2544 + 12864 + 1132 bytes of `.text`, 570 of `.rodata`, 76 + 8 + 4 of `.bss`, **39 resolved** (34 functions + 5 storage, including `ads_zone`, the name 290 added, and `kernel_stack_size`) and 12 added. The three bodies are small enough to quote: `stack_init` is five real calls and four stores, **`thread_policy_init` is one call to `PE_parse_boot_argn` and nothing else**, and **`machine_thread_init` is `ads_zone = zinit(256, 16384, 16384, "arm debug state")` and a return** — the nine other calls a `-dr` dump seems to attribute to it belong to `machine_switch_context`. The stop is **`atm_init`, `kernel_bootstrap + 0x2d8` (caller 0x8000e3d8)**, so every one of `kernel_bootstrap`'s subsystem calls before and including `thread_init` has now run. **The build found a real defect in this ledger's own method**: `machine_stack_attach`/`detach`/`handoff` are undefined in `stack.o` but **defined by `pcb.o`, linked in the same step**, so a per-object measurement counts them as obligations the link settles for free — hence 776/687/89 predicted against **773/683/90 measured**, exact once the rule gains the qualifier *and* the function/storage split comes from the generated stub object rather than the reference site (`kpc_off_cpu_active` is a `B 0x4`). `.data` moved a second time as predicted, **0x80110000 → 0x80114000**, `__bss_start` → 0x8012bc08, image → **1230896** (+24 of it `stack.o`'s own `__DATA,__data`), text 1115928. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |

| stage90 | [experiment-293](experiment-293-bsd-kern-o-and-the-16kb-boundary-finally-moving.md) | **`osfmk_kern_bsd_kern.o`, a function that returns a constant, and the 16 KB boundary finally moving** — `osfmk/kern/bsd_kern.c`, **4488 bytes of `.text` + 16 of `.rodata.str1.1`, 61 definitions, 34 references, 23 resolved and 5 added** (it is also the object that defines `get_task_crash_label`, one of the three names 292 obliged, so this step is again worth two names for the price of one; 38 more of its definitions are referenced by nothing and arrive as dead code, as in 292). **The centre of the prediction is a function whose body is four instructions**: `get_task_uniqueid` loads `task->bsd_info` (offset 568), and returns `UINT64_MAX` with `mvn r0,#0 / mvn r1,#0` when it is NULL — which it is, because `task_create_internal` sets `bsd_info = NULL` at task.c:1045 and no BSD code has run. So the new body **does not reach `proc_uniqueid`** (which is one of the seven `under` names and is still a stub) at any of its three call sites in `coalitions_adopt_task`, and the step **overshoots the symbol it links**, as 290 did. What makes it interesting is the second half: `get_task_uniqueid` was the *last* stub on the whole path back out through `task_init`, so the run then cleared the rest of the coalition adopt for both types, `task_create_internal`'s tail (its `panic` not taken because the store precedes the call, `place_task_hold` not taken because `kernel_task` is still NULL), `task_init`'s tail call to `lck_spin_init`, and `kernel_bootstrap`'s handover to **`thread_init`** — every guard read, including the two `KDBG_RELEASE` `kernel_debug` calls behind the `.bss` global `kdebug_enable` at 0x80130f20, and `zinit`'s `btlog_create` behind the same `.bss` guards that `task_init`'s own `zinit` has satisfied in every run since 289. **The build moved the 16 KB boundary for the first time in four experiments, and predicted it**: `__TEXT,initcode` ended at 0x8010babc leaving 1348 bytes, the object brings 4504 and retires 23 names, so `.data` went **0x8010c000 → 0x80110000**, `__bss_start` 0x80123c08 → **0x80127c08**, image 1198104 → **1214488** (+16384, the `*fill*` the map file has always shown). Counts exact: **800 undefined / 706 function stubs / 94 storage**; text 1096376 → 1099960, giving a measured **51.1 bytes per retired stub name** (292's figure was ~62, 291's ~52, 290's ~65 — the cost is not a constant, and the ledger now carries all four). The pin tracked the move unprompted: the writes are 0x8015e448/0x8015e44c, 0x4000 further on, and the reserved slot moved with them. **`stub_hit=stack_init` at `xnu_entry_stub_caller=0x800092ac` = `thread_init + 0xd8`** — the first XNU function in this walk that is about creating a thread rather than filling in a structure. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |

| stage90 | [experiment-292](experiment-292-mac-mach-o-and-a-prediction-that-leaves-two-functions.md) | **`security_mac_mach.o`, and a prediction that leaves two functions entirely** — `security/mac_mach.c` (**not** `security/mac_exc.c`, which 291's own next-step note named and which is not a file in this tree), **3532 bytes of `.text` and nothing else** — no `.data`, no `.rodata`, no `.bss`, the first object of the walk whose whole contribution is code — **15 definitions, 19 references, 12 resolved and 3 added** (the two `mac_exc_*` names 291 obliged are here too, plus `mac_exc_create_label`'s eight siblings; the 3 added are `get_task_crash_label` from `osfmk/kern/bsd_kern.c` and `proc_self`/`proc_task` from `bsd/kern/kern_proc.c`; the other three definitions are dead code nothing references). Both `MAC_PERFORM` loops are measured no-ops rather than assumed: the object's own `ldr r0, [r6, #12]` is `mac_policy_list.staticmax` (offsets confirm the struct), which `mac_policy_init` zeroed, and `mac_policy_list_conditional_busy()` returns on `numloaded <= staticmax` before touching the lock. **291's next-step note was wrong and the image says so twice**: `task_create_internal` makes exactly **three** stub calls (`vm_shared_region_get` +0x300, `vm_shared_region_set` +0x30c, `task_affinity_create` +0x348) and all three are inside `if (parent_task != TASK_NULL)` — the arm `task_init`'s `TASK_NULL` argument skips, proved by the word at `[sp,#12]` being set to 1 only on the parent path and by the block it jumps to being the `KERNEL_SECURITY_TOKEN`/`BASEPRI_KERNEL` else. `thread_init`'s third call is the stub `stack_init` and that is where the frontier would land, but the first stop is one level deeper, in the coalition adoption: `coalitions_adopt_init_task` → `coalitions_adopt_task` (inlined `coalition_adopt_task_internal`, whose `coal_call` op is the real `i_coal_resource_adopt_task`) → **`if (get_task_uniqueid(task) != UINT64_MAX)`**, reached whether or not the adopt succeeds. Build exact (827 → **818** undefined, 733 → **724** function, 94 storage), image **unchanged at 1198104** with 4324 bytes of room before `.data` would move 16 KB. **Two numbers corrected the model**: a retired stub name is worth **about 62 bytes**, not 46; and **`__bss_end` did not move, because the function stubs have no `.bss` slot at all** — their bodies and name strings are the whole cost, so `.bss` moves only with the *storage* set — which makes the pinned write addresses (0x8015a448/0x8015a44c) more stable than the step expected. **`stub_hit=get_task_uniqueid` at `xnu_entry_stub_caller=0x800bd900` = `coalitions_adopt_task + 0x0dc`** — the first frontier in this walk outside osfmk/ipc, osfmk/kern and security, and the first run that leaves both functions 288 entered. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |

| stage90 | [experiment-291](experiment-291-ipc-tt-o-and-the-pad-replaced-by-a-pin.md) | **`osfmk_kern_ipc_tt.o`, and the pad replaced by a pin** — `osfmk/kern/ipc_tt.c`, the largest step since 288: **12676 bytes of `.text`, 60 definitions, 53 references, 37 resolved and 2 added** (the 37 being the whole port-name conversion surface plus `ipc_task_init` and its siblings — all already stubs — and the 2 being `mac_exc_free_action_label`/`mac_exc_inherit_action_label` from the same unlinked `security/mac_exc.c`). `ipc_task_init` is at object offset 0, so the object offsets are function offsets. **The prediction is the first of the walk that does not depend on a branch**: the `CONFIG_MACF` loop (thirteen unrolled iterations) precedes both arms of `if (parent == TASK_NULL)`, so the stop is `mac_exc_create_label` at `+0x0d8` either way — and the eight real calls before it each had their inner stub read rather than assumed (`zfree` behind a failed `it_entries_alloc`; `VM_ALLOC_SITE_STATIC(0, 0)` so `vm_tag_bt`'s `OSKextGetAllocationSiteForCaller` is not reached; `kmem_alloc_flags`' `trace_backtrace` behind the zero `.bss` word `log_leaks`; `ast_taken_kernel` behind `cpu_pending_ast & AST_URGENT`). **The build refused the step first**: thirty-seven retired stubs moved `ResetHandlerData - ExceptionLowVectorsBase` from 0x2448 to **0x2358, below the 512-byte pad's start**, and `verify_pad` — the check 288 built — stopped it with no device run spent (the five measured values 0x2404/0x24a8/0x24c0/0x2448/0x2358 show it is a treadmill, not a nit). **The fix gives the value one definition instead of re-aiming the pad**: `entry.ld` now defines `ExceptionLowVectorsBase = ENTRY_BASE` and `ResetHandlerData = __entry_reset_handler_data - 4`, a 16-byte reserved `.bss` slot, so the two writes land there and `cpu.c`'s page copy becomes a page copied onto itself — where before it took 4096 bytes of stub bodies over page zero, latent since the beginning. Build exact on the corrected counts (864 → **827** undefined, 770 → **733** function stubs, 94 storage), `__bss_start` unchanged at **0x80123c08**, image 1198056 → **1198104** (+48, exactly the object's `__DATA,__data`), text 1093400. **`stub_hit=mac_exc_create_label` at `xnu_entry_stub_caller=0x800c8d1c` = `ipc_task_init + 0x0dc`** — and the report arriving normally is also the measurement of the pinned instrument. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |

| stage90 | [experiment-290](experiment-290-machine-task-o-and-name-strings-that-live-in-text.md) | **`osfmk_arm_machine_task.o`, an empty function, and name strings that live in `.text`** — `osfmk/arm/machine_task.c`, the smallest step of the walk: **324 bytes of `.text` for five functions, 5 resolved and 2 added**. `machine_task_init` is the **last four bytes** of that text because its body is empty (`machine_task.c:171-175`: three `__unused` parameters, no statement) — a `bx lr`, so the step cannot stop on anything of its own. The two added names come from the *same* unlinked object, `osfmk/arm/pcb.c`: `ads_zone` (storage, 4 bytes) and `copy_debug_state` — this step obliges its own successor, and `ads_zone` is the four zero bytes `machine_task_set_state`/`machine_task_terminate` later hand to `zalloc`/`zfree`. **`stub_hit=ipc_task_init` at `xnu_entry_stub_caller=0x800bfde8` = `task_create_internal + 0x224`**, predicted from the address-ordered stub list 289's ledger wrote and from the eight `add`/`mov`/`str` instructions (no `bl`) between the two calls; the image shows the effect directly — `machine_task_init` was a stub at 0x800ede1c and is real at 0x800c8c3c. Counts, `__bss_start` (0x80123c08) and the **unchanged image** (1198056, first step in three where `.data` did not move) all exact; text 1082328 → **1082456**, and the reason **+128 rather than the object's +324** is a fact about the linker script — `entry.ld:46` puts `*(.rodata .rodata.*)` *inside* `.text`, so the stub object's symbol-name strings live there and retire with the stubs. `verify_pad` moved **down for the first time**, 0x24c0 → **0x2448** (386 symbols at 0x18 bytes apart between `ExceptionLowVectorsBase` and `ResetHandlerData` — the interval *is* the stub object), still inside the 512-byte pad that spans 0x23d4…0x25d4. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |

| stage90 | [experiment-289](experiment-289-task-policy-o-and-two-names-that-are-reachable.md) | **`osfmk_kern_task_policy.o`, and two new names that both turn out to be reachable** — `osfmk/kern/task_policy.c`, **14636 bytes of text, 71 definitions, 82 references**, **28 resolved** (26 functions plus the two storage stand-ins 288 had just added, `default_task_effective_policy` and `default_task_requested_policy`) and **8 added** (seven functions — `mig_strncpy`, `proc_apply_resource_actions`, `proc_apply_task_networkbg`, `proc_pidpathinfo_internal`, `proc_restore_resource_actions`, `thread_policy_update_complete_unlocked`, `thread_policy_update_tasklocked` — plus storage `thread_qos_policy_params`). The step is worth **two calls rather than one**: this object defines *both* of the two new names in `task_init`'s body that 288 introduced, `task_watch_init` **and** `proc_init_cpumon_params`, so the prediction is the longest chain of the walk — fourteen calls — and it turns on the third kind of stop-decider: `PE_get_default` looks `/defaults` up with `DTLookupEntry` and reaches the **stub** `IODTGetDefault` only when that lookup *fails*, while the payload's synthetic tree has a `defaults` child (`stage90_main.c:735`, `apple_dt.c:230`) and `kSuccess` is 1, so it takes the real `DTGetProperty` path. **`stub_hit=machine_task_init` at `xnu_entry_stub_caller=0x800bfdc4` = `task_create_internal + 0x200`** — the image has `bl machine_task_init` at 0x800bfdc0 — and the falsifier named in advance (a report naming `IODTGetDefault`) did not fire. Counts and `__bss_start` exact (887 → **867** undefined, 793 → **774** function stubs, 94 → **93** storage, 0x8011fbf8 → **0x80123c08**), text 1067704 → 1082328 (predicted 1082340), image 1181584 → 1198056, `.data` crossing another block `0x80108000 → 0x8010c000`, headroom 1727544. `verify_pad`'s new derived form prints `XNU writes 0x800024c4 and 0x800024c8 (ResetHandlerData - ExceptionLowVectorsBase = 0x24c0)` — 0x18 above 288's, still inside the pad. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |

| stage90 | [experiment-288](experiment-288-task-o-and-the-pads-contract-broken-by-a-constant.md) | **`osfmk_kern_task.o`, and the pad's contract broken by a constant** — `osfmk/kern/task.c`, the largest step since 280: **23504 bytes of text, 146 definitions, 224 references, 60 resolved and 59 added** (resolved includes `task_init` and `task_max`, so it retires a whole block of adjacent stubs; the added count is 59 and not 66 because the first pass compared against the image's symbol table without subtracting the generated stubs). Prediction read off the bodies: `init_task_ledgers` (24 `ledger_entry_add`, 11 `ledger_track_credit_only`, …, all real, each scanned in turn) and `coalition_create_internal` both complete, then `task_init` — retired by this very object — runs its five lock/`zinit` calls and stops on **`task_watch_init`**. **Five runs were silent first, and it was 282 again, reached a different way.** `cpu.c:570-580` writes to `gPhysBase + (&ResetHandlerData.cpu_data_entries − &ExceptionLowVectorsBase)`; 281 measured that difference as **0x2404/0x2408 and `verify_pad` had compared against those literals ever since**, while the difference spans *the generated stub object* and grows with every step of the walk. Read out of the image's own instruction stream at `cpu_machine_idle_init+0x1a8`: `movw r2,#0xb494 / movt r2,#0x800e` → `ResetHandlerData` 0x800eb494, `sub r5, r2, r5` with `r5` = `ExceptionLowVectorsBase` 0x800e8fec → **difference 0x24a8** → the writes land at **0x800024AC/0x800024B0**, 0x58 bytes past the 128-byte pad, inside `entry_epilogue`. The gap is 152 stub bodies at 0x18 bytes apart. A checkpoint at `machine_init` — which 286 and 287 reported from *past* — was silent too, which is what said the fault was the instrument's. **Fix: one value, one definition.** Pad 512 bytes (`b 1f` over 127 NOPs), and `verify_pad` now **derives** the two addresses from the linked image's `ResetHandlerData`/`ExceptionLowVectorsBase` at the struct's field offsets 4 and 8, printing them every build. **`stub_hit=task_watch_init` at `xnu_entry_stub_caller=0x800bf900`** = `task_init+0xb0`. Counts exact (888 → **887** undefined, 791 → **793** function stubs, 97 → **94** storage); `.data` crossed another 16 KB boundary, 0x80100000 → 0x80108000. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000` on every run including the silent ones, device back on its own each time |
| stage90 | [experiment-287](experiment-287-coalition-o-zinit-and-the-16kb-boundary.md) | **`osfmk_kern_coalition.o`, `zinit`'s only stub, and the 16 KB boundary** — `osfmk/kern/coalition.c`, **11312 bytes of text, 47 definitions, 44 references**, **13 resolved** (all functions) and **5 added** (four functions + one storage; four of the five from `osfmk_kern_task.o`). `coalitions_init` is twelve calls long and eleven are real; the step's real content is `zinit`, **0x97c bytes — three times what the name suggests** — whose whole extent contains exactly **one** stub call, `bl btlog_create`, guarded by `zalloc.c:2259-2360`'s `log_records_init == FALSE && zone_logging_enabled == TRUE`, with `zone_logging_enabled` set by `PE_parse_boot_argn("zlog1".."zlog10")`/`("zlog")` — and this payload's boot-args carry no `zlog` of any spelling, so `zinit` returns. **The third step in a row whose answer came from the boot args** (286's from an absent DT property, 285's from a storage stand-in's zero), named in the doc as a pattern: a stub walk's stop is decided by a missing *symbol*, an invented *value*, or a supplied/omitted *string*. Also the step where 284's arithmetic finally bites: the slack was 0x1be4 and this object carries 11312 bytes of text, so `.data` moved `0x800fc000 → 0x80100000` and `__bss_start` with it — **predicted 0x80117b68, measured 0x80117b68**. Counts exact (896 → **888** undefined, 800 → **791** function stubs, 96 → **97** storage), text 1031088 → 1042736, image 1132136 → 1148528. **`stub_hit=init_task_ledgers` at `xnu_entry_stub_caller=0x800bde8c`** = `coalitions_init+0xe8`. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |
| stage90 | [experiment-286](experiment-286-ntptime-and-a-prediction-that-is-a-chain.md) | **`bsd_kern_kern_ntptime.o`, and a prediction that is a chain rather than a name** — `osfmk/kern/ntptime.c`, **3068 bytes of text, 8 definitions, 24 references**, **2 resolved** (`ntp_init`, `ntp_update_second`) and **2 added** (`mac_system_check_settime`, `nanotime`), **all four functions** — so all three stub counts are *unchanged* by this step, the first time in a long while. The prediction had to follow **four functions past the stop**: `ntp_init`'s six callees are all real; `clock_config` ends in a real tail call; `machine_init` returns because `debug_enabled` is 0; `clock_init`→`clock_oldinit` (`b`, real since 285) makes no calls; `ledger_init` is a 5-instruction tail call into `lck_grp_init`, all real. **`debug_enabled` is the hinge and was read three ways, not assumed**: it is `SECURITY_READ_ONLY_SPECIAL_SECTION(... __TEXT,__const) debug_enabled = FALSE;` (`pe_init.c:39`); its only writer in the tree is `pe_init.c:350`, which copies it from the DT property `/chosen/debug-enabled`, and the payload's synthetic `/chosen` has no such property (the boot-args line's `debug=0x144` sets `debug_boot_arg`, which `PE_i_can_has_debugger` *reports*, and not this); and the linked `__TEXT,__const` word at `0x800fa640` is `0x00000000`. The falsifier was named in advance — `pmap_map_globals` is real and reachable only down that branch. **`stub_hit=coalitions_init` at `xnu_entry_stub_caller=0x8000e228`** = `bl coalitions_init`+4 in `kernel_bootstrap`. Build counts exact (896/800/96, `__bss_start` 0x80113b58 → 0x80113b60, headroom 1794232 → 1794040); text 1027984 → 1031088, image 1132128 → 1132136. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |
| stage90 | [experiment-285](experiment-285-clock-oldops-and-a-step-that-overshoots.md) | **`osfmk_kern_clock_oldops.o`, and a step that overshoots the symbol it links** — `osfmk/kern/clock_oldops.c`, **3128 bytes of text, 32 definitions, 25 references**, **10 resolved** (all functions, no storage, so no size the generator could get wrong) and **6 added** (four functions — `clock_alarm_reply`, `ipc_clock_enable`, `ipc_clock_init`, `port_name_to_clock` — plus two storage: `clock_count` D 4 and `clock_list` D 0x18 = `struct clock_list_entry[CLOCK_COUNT]`, CLOCK_COUNT 2). The prediction was that this step **does not** move the frontier: `clock_oldconfig`'s three direct callees are all real (and `thread_call_setup`/`timer_call_setup` were measured in the linked image to contain no stub), and its one indirect `blx r0` sits in a loop over `clock_list` guarded by `clock_count` — which arrives as a **storage stand-in of four zero bytes**, so the walk is entered zero times. **`stub_hit=ntp_init` at `xnu_entry_stub_caller=0x800b97e8` = `clock_config+0x6c`** — the instruction immediately after the `bl clock_oldconfig` the previous step stopped at. So the two names that decided the shape of the step are the two *storage* ones: the run does not stop because `clock_list` is empty, and it is empty because nothing has ever written it. All three build counts exact (900 → **896** undefined, 806 → **800** function stubs, 94 → **96** storage), `__bss_start` unchanged at 0x80113b58, headroom 1794552 → 1794232, and the **image unchanged at 1132128 bytes** — the 284 lesson applied (the read-only region ends at `0x800fa41c` with 0x1be4 bytes of slack, and this object's 3209 bytes fit inside it, so `.data`/`.bin` do not move). Text 1025040 → 1027984. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |
| stage90 | [experiment-284](experiment-284-osfmk-kern-clock-o-and-a-frontier-inside-the-object.md) | **`osfmk_kern_clock.o`, and the first frontier that is *inside* the object it links** — `osfmk/kern/clock.c`, **6316 bytes of text, 48 definitions, 41 references**, **17 resolved** (fifteen function stubs and two storage stand-ins whose sizes agree with the object byte for byte: `hz_tick_interval` `D` 4/`0x4`, `mach_absolutetime_asleep` `B` 8/`0x8`) and **8 added** (all functions: `clock_oldconfig`/`clock_oldinit`, `ntp_init`/`ntp_update_second`, the two `commpage_update_*`, the two `PESet/GetUTCTimeOfDay`), with the other 24 definitions names nothing has referenced yet and no collision anywhere. Unlike all 28 steps before it, the symbol the run stopped at is defined in an object whose own body still needs something: `clock_config` calls `arm_usimple_lock_init`, the four lock allocators and `lck_mtx_init` — **all real** — and then `clock_oldconfig`, which nothing references today, so the frontier **resumes inside the function the step links** and the name could be predicted from the body: **`stub_hit=clock_oldconfig` at `xnu_entry_stub_caller=0x800b97e4` = `clock_config+0x68`** (the `bl` at `0x800b97e0`, the sixth of eight). All three build deltas exact: 909 → **900** undefined, 813 → **806** function stubs, 96 → **94** storage. **Two readings the build corrected**: object-section offsets are not function offsets (the object's `.text` opens with `kdp_clock_is_locked`, 0xc bytes, so the first table was 12 bytes high — the linked `clock_config`/`bl` pair settled it before the device was touched), and the image grew **+8 bytes, not +6316** — the `.bin` ends at `__DATA,__data`, and `.data` is 16 KB-aligned at `0x800fc000` before and after, so within the 0x1be4 bytes of slack a step's text growth is invisible in image size (283's +16440 is the same effect seen from the side that crossed). Text 1019088 → 1025040, `__bss_start` 0x80113b50 → 0x80113b58, headroom 1794616 → 1794552. 25 x `persistent_write_attempted=0x00000000`, 87 x `failure_mask=0x00000000`, no `exception:` line, device back on its own |

Stage90's other notes live inside the snapshot directory:
`../../stages/stage90/README.md`, `IMPLEMENTATION.md`,
`IMPLEMENTATION_STATUS.md` and `QUICK_START.md`.
